#!/usr/bin/env php
<?php

/**
 * @file stackra
 * @description Entry point for the Stackra workspace CLI.
 *
 *   Loads Composer's autoloader, instantiates {@see \Stackra\Cli\Application},
 *   and delegates to its `run()` method. Non-zero exit codes bubble to the shell
 *   so CI + local scripts can gate on them.
 */

declare(strict_types=1);

$autoloadCandidates = [
    __DIR__.'/../vendor/autoload.php',
    __DIR__.'/../../../vendor/autoload.php',
];

$autoload = null;

foreach ($autoloadCandidates as $candidate) {
    if (file_exists($candidate)) {
        $autoload = $candidate;

        break;
    }
}

if ($autoload === null) {
    fwrite(STDERR, "stackra: could not locate vendor/autoload.php.\n".
        "Run `composer install` in tools/cli first.\n");
    exit(1);
}

require $autoload;

use Stackra\Cli\Application;

exit((new Application)->run());
