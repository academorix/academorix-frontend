{
  "id": "academorix://modules/notifications-mail/relations",
  "$version": 1,
  "title": "notifications-mail \u2014 model relationships",
  "description": "One entity: MailSuppression. Relates outward to Tenant + optionally to the source NotificationDelivery that triggered the suppression.",
  "internal": [],
  "outbound": [
    {
      "from": "MailSuppression",
      "kind": "belongsTo",
      "to": "tenancy::Tenant",
      "foreignKey": "tenant_id",
      "nullable": true,
      "description": "NULL = platform-wide suppression."
    },
    {
      "from": "MailSuppression",
      "kind": "belongsTo",
      "to": "notifications::NotificationDelivery",
      "foreignKey": "source_delivery_id",
      "nullable": true,
      "description": "The delivery whose provider webhook triggered this suppression. NULL for manual admin-created suppressions."
    }
  ],
  "inbound": {
    "description": "Consumers query MailSuppression via the MailSuppressionRegistry binding (`MailSuppressionRegistry::isSuppressed(email, tenant_id): bool`). No downstream FK.",
    "conventions": [
      "SendMailJob checks the registry before every send.",
      "The registry maintains a Redis-cached lookup (see caches.json) to avoid a DB hit per send."
    ]
  }
}
