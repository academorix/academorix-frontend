{
  "id": "academorix://modules/invitations/features",
  "$version": 1,
  "title": "invitations — feature keys",
  "description": "This module does not publish tenant-facing business features. Every tenant that has any invitable resource can send invitations by default. Downstream consumers may gate `HasInvitations` composition behind their own business features (e.g. teams feature-gates invite-to-team behind `teams.enabled`), but the invitations substrate itself is always on.",
  "reference": {
    "publishedBy": "feature-flag",
    "resolvedBy": "GET /api/auth/me → data.features (string[])",
    "seededFrom": "consumer modules — invitations is transparent"
  },
  "features": []
}
