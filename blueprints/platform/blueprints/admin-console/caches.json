{
  "id": "stackra://modules/admin-console/caches",
  "$version": 1,
  "title": "admin-console — cache keys",
  "description": "Cache keys the module owns + TTLs + invalidation triggers + tags.",
  "keys": []
}
