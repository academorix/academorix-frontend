{
  "id": "academorix://modules/branding/caches",
  "$version": 1,
  "title": "branding — cache keys",
  "description": "Cache keys the module owns + TTLs + invalidation triggers + tags.",
  "keys": []
}
