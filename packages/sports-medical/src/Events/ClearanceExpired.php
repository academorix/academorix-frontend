<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

namespace Academorix\Sports\Medical\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Clearance passed expires_at without renewal. Closes eligibility gate.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 10 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchClearanceExpiredNotification (guardian + medical + admin, cannot-opt-out)
 * - medical::UpdateEligibilityGate
 * - event::InvalidateFutureSquadSelections
 *
 * @category Medical
 *
 * @since    0.1.0
 */
final readonly class ClearanceExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $clearanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $expiredAt  ISO 8601
     */
    public function __construct(
        public readonly string $clearanceId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $expiredAt,
    ) {
    }
}
