<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

namespace Academorix\Sports\Medical\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Scheduled job fires at 90d / 30d / 7d before expires_at. Cannot-opt-out notifications.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchClearanceExpiringNotification (guardian + medical + admin)
 *
 * @category Medical
 *
 * @since    0.1.0
 */
final readonly class ClearanceExpiring implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $clearanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $expiresAt  ISO 8601
     * @param  string  $daysUntilExpiry  90 | 30 | 7
     */
    public function __construct(
        public readonly string $clearanceId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $expiresAt,
        public readonly string $daysUntilExpiry,
    ) {
    }
}
