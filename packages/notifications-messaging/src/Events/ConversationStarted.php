<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications messaging --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Messaging\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New Conversation row committed. Kind IN dm / group.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::NotifyConversationParticipants
 *
 * @category Messaging
 *
 * @since    0.1.0
 */
final readonly class ConversationStarted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $conversationId  ULID (cnv_*)
     * @param  string  $tenantId  ULID
     * @param  string  $kind  'dm'|'group'
     * @param  string  $createdByUserId  ULID
     * @param  int  $memberCount  int
     */
    public function __construct(
        public readonly string $conversationId,
        public readonly string $tenantId,
        public readonly string $kind,
        public readonly string $createdByUserId,
        public readonly int $memberCount,
    ) {
    }
}
