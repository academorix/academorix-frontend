<?php

/**
 * @file database/migrations/2026_07_15_120001_create_conversations_table.php
 *
 * @description
 * Create the `conversations` table for the `Conversation` entity.
 * Owned by the messaging module. Emitted from the module blueprint —
 * regenerate via `generate-module.py notifications messaging --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications messaging --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('conversations', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('scope', 16);
            $table->string('scope_ref_id', 30)->nullable();
            $table->string('subject', 500)->nullable();
            $table->boolean('is_group')->default('false');
            $table->timestampTz('last_message_at')->nullable();
            $table->timestampTz('archived_at')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->timestampTz('deleted_at')->nullable();
            $table->index(['tenant_id', 'last_message_at'], 'conversations_recent_idx');
            $table->index(['tenant_id', 'scope', 'scope_ref_id'], 'conversations_scope_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('conversations');
    }
};
