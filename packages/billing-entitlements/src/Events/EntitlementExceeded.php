<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py billing entitlements --force`.

declare(strict_types=1);

namespace Academorix\Billing\Entitlements\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Enforcer::enforce() rejected because value_used + delta > value_cap.
 *
 * ## Consumers
 *
 * - notifications::DispatchQuotaExceededNotification
 * - observability::AlertingListener
 *
 * @category Entitlements
 *
 * @since    0.1.0
 */
final readonly class EntitlementExceeded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $entitlementId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $key  string
     * @param  int  $valueCap  integer
     * @param  int  $valueUsed  integer
     * @param  int  $attemptedAmount  integer
     */
    public function __construct(
        public readonly string $entitlementId,
        public readonly string $tenantId,
        public readonly string $key,
        public readonly int $valueCap,
        public readonly int $valueUsed,
        public readonly int $attemptedAmount,
    ) {
    }
}
