<?php

/**
 * @file database/migrations/2026_07_15_120000_create_attribute_definitions_table.php
 *
 * @description
 * Create the `attribute_definitions` table for the `AttributeDefinition` entity.
 * Owned by the attributes module. Emitted from the module blueprint —
 * regenerate via `generate-module.py shared attributes --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared attributes --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('attribute_definitions', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('code', 64);
            $table->string('type', 16);
            $table->string('widget', 16);
            $table->jsonb('widget_config')->nullable();
            $table->jsonb('validation')->nullable();
            $table->jsonb('labels')->default('{}');
            $table->text('description')->nullable();
            $table->boolean('is_deprecated')->default('false');
            $table->timestampTz('deprecated_at')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'code'], 'attribute_definitions_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'type'], 'attribute_definitions_type_idx');
            $table->index(['tenant_id', 'is_deprecated'], 'attribute_definitions_active_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_deprecated = false
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('attribute_definitions');
    }
};
