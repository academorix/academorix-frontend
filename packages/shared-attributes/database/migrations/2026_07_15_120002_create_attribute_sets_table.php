<?php

/**
 * @file database/migrations/2026_07_15_120002_create_attribute_sets_table.php
 *
 * @description
 * Create the `attribute_sets` table for the `AttributeSet` entity.
 * Owned by the attributes module. Emitted from the module blueprint —
 * regenerate via `generate-module.py shared attributes --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared attributes --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('attribute_sets', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('code', 64);
            $table->string('entity_type', 64);
            $table->string('discriminator_field', 64);
            $table->string('discriminator_value', 64);
            $table->integer('version_number')->default('1');
            $table->string('status', 16)->default('draft');
            $table->boolean('is_active')->default('false');
            $table->timestampTz('activated_at')->nullable();
            $table->timestampTz('deactivated_at')->nullable();
            $table->jsonb('labels')->default('{}');
            $table->jsonb('definition_order')->default('[]');
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'entity_type', 'discriminator_value', 'version_number'], 'attribute_sets_versioned_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->unique(['tenant_id', 'entity_type', 'discriminator_value'], 'attribute_sets_active_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_active = true AND deleted_at IS NULL
            $table->index(['tenant_id', 'status'], 'attribute_sets_status_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('attribute_sets');
    }
};
