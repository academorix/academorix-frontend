<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared attributes --force`.

declare(strict_types=1);

namespace Academorix\Shared\Attributes\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. AttributeSet.version_number bumped due to a breaking change. Old version marked superseded but preserved for historic render. Load-bearing signal — every host resolver cache MUST invalidate on this event.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - cache::InvalidateHostResolverCache
 * - notifications::DispatchAttributeSetVersionedNotification (tenant admin + host module maintainers)
 *
 * @category Attributes
 *
 * @since    0.1.0
 */
final readonly class AttributeSetVersioned implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $setId  ULID
     * @param  string  $previousSetId  ULID | null
     * @param  string  $tenantId  ULID
     * @param  string  $entityType  string
     * @param  string  $discriminatorValue  string
     * @param  int  $previousVersionNumber  integer
     * @param  int  $newVersionNumber  integer
     * @param  string  $reason  string
     * @param  string  $changedByUserId  ULID
     */
    public function __construct(
        public readonly string $setId,
        public readonly string $previousSetId,
        public readonly string $tenantId,
        public readonly string $entityType,
        public readonly string $discriminatorValue,
        public readonly int $previousVersionNumber,
        public readonly int $newVersionNumber,
        public readonly string $reason,
        public readonly string $changedByUserId,
    ) {
    }
}
