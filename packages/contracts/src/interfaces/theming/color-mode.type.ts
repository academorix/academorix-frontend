/**
 * @file color-mode.type.ts
 * @module @stackra/contracts/interfaces/theming
 * @description User-facing color-mode preference.
 */

/** User-facing color mode preference. `'system'` follows the OS. */
export type ColorMode = "light" | "dark" | "system";
