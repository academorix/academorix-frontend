/**
 * @file resolved-mode.type.ts
 * @module @stackra/contracts/interfaces/theming
 * @description Resolved color-mode after `'system'` has been evaluated.
 */

/** Concrete color mode after resolving `'system'` against OS preference. */
export type ResolvedMode = "light" | "dark";
