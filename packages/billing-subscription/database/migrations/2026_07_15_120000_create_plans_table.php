<?php

/**
 * @file database/migrations/2026_07_15_120000_create_plans_table.php
 *
 * @description
 * Create the `plans` table for the `Plan` entity.
 * Owned by the subscription module. Emitted from the module blueprint —
 * regenerate via `generate-module.py billing subscription --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py billing subscription --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('plans', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('application_id', 64);
            $table->foreign('application_id')->references('id')->on('applications')->onDelete('restrict');
            $table->string('key', 100);
            $table->string('name', 200);
            $table->text('description')->nullable();
            $table->jsonb('translations')->default('\'{}\'::jsonb');
            $table->string('tier', 24);
            $table->string('billing_cycle', 16);
            $table->string('billing_mode', 16)->default('cashier');
            $table->unsignedBigInteger('price_micro_units');
            $table->string('currency', 3)->default('USD');
            $table->string('provider_price_id', 128)->nullable();
            $table->integer('trial_days')->default(0);
            $table->jsonb('default_entitlements')->default('\'{}\'::jsonb');
            $table->jsonb('included_features')->default('\'[]\'::jsonb');
            $table->boolean('is_system')->default(false);
            $table->boolean('is_public')->default(true);
            $table->boolean('is_deprecated')->default(false);
            $table->integer('sort_order')->default(0);
            $table->timestampTz('archived_at')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['application_id', 'key'], 'plans_application_key_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['application_id', 'tier', 'is_public'], 'plans_public_catalogue_idx');
            $table->index(['provider_price_id'], 'plans_provider_price_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE provider_price_id IS NOT NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('plans');
    }
};
