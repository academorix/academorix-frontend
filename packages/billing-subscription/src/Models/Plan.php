<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py billing subscription --force`.

declare(strict_types=1);

namespace Academorix\Billing\Subscription\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Billing\Subscription\Contracts\Data\PlanInterface;
use Academorix\Billing\Subscription\Database\Factories\PlanFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Laravel\Scout\Searchable;
use Mattiverse\Userstamps\Traits\Userstamps;

/**
 * Eloquent model for a Plan.
 *
 * Per-Application plan definition.
 *
 * @category Subscription
 *
 * @since    0.1.0
 */
#[Table(name: PlanInterface::TABLE, keyType: PlanInterface::KEY_TYPE)]
#[Fillable([
    PlanInterface::ATTR_APPLICATION_ID,
        PlanInterface::ATTR_KEY,
        PlanInterface::ATTR_NAME,
        PlanInterface::ATTR_DESCRIPTION,
        PlanInterface::ATTR_TRANSLATIONS,
        PlanInterface::ATTR_TIER,
        PlanInterface::ATTR_BILLING_CYCLE,
        PlanInterface::ATTR_BILLING_MODE,
        PlanInterface::ATTR_PRICE_MICRO_UNITS,
        PlanInterface::ATTR_CURRENCY,
        PlanInterface::ATTR_PROVIDER_PRICE_ID,
        PlanInterface::ATTR_TRIAL_DAYS,
        PlanInterface::ATTR_DEFAULT_ENTITLEMENTS,
        PlanInterface::ATTR_INCLUDED_FEATURES,
        PlanInterface::ATTR_IS_SYSTEM,
        PlanInterface::ATTR_IS_PUBLIC,
        PlanInterface::ATTR_IS_DEPRECATED,
        PlanInterface::ATTR_SORT_ORDER,
        PlanInterface::ATTR_ARCHIVED_AT,
        PlanInterface::ATTR_METADATA,
])]
#[UseFactory(PlanFactory::class)]
final class Plan extends Model implements PlanInterface
{
    use HasFactory;
    use HasUlids;
    // TODO(gen): resolve unknown trait `HasSystemFlag` — add its import + use line.
    use HasMetadata;
    use HasUserstamps;
    // TODO(gen): resolve unknown trait `HasAudit` — add its import + use line.
    // TODO(gen): resolve unknown trait `HasTranslations` — add its import + use line.
    use Filterable;
    use Searchable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        PlanInterface::ATTR_TIER => 'PlanTier',
        PlanInterface::ATTR_BILLING_CYCLE => 'BillingCycle',
        PlanInterface::ATTR_BILLING_MODE => 'BillingMode',
        PlanInterface::ATTR_PRICE_MICRO_UNITS => 'integer',
        PlanInterface::ATTR_TRIAL_DAYS => 'integer',
        PlanInterface::ATTR_DEFAULT_ENTITLEMENTS => 'array',
        PlanInterface::ATTR_INCLUDED_FEATURES => 'array',
        PlanInterface::ATTR_IS_SYSTEM => 'boolean',
        PlanInterface::ATTR_IS_PUBLIC => 'boolean',
        PlanInterface::ATTR_IS_DEPRECATED => 'boolean',
        PlanInterface::ATTR_ARCHIVED_AT => 'datetime',
        PlanInterface::ATTR_METADATA => 'array',
        PlanInterface::ATTR_TRANSLATIONS => 'array',
    ];
}
