/**
 * @file index.ts
 * @module @stackra/storage/core/constants
 * @description Barrel export for storage constants.
 *
 *   DI tokens (`STORAGE_MANAGER`, `STORAGE_CONFIG`, `STORAGE`) live
 *   in `@stackra/contracts` and are imported from there.
 */

export { DEFAULT_STORAGE_CONFIG } from './default-storage-config.constant';
