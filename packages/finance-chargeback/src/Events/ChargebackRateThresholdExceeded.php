<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance chargeback --force`.

declare(strict_types=1);

namespace Academorix\Finance\Chargeback\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MonitorChargebackRateJob detected the tenant's rolling 60-day chargeback rate crossed the network's VDMP/MDMP threshold (0.9% Visa / 1.5% Mastercard / 1.0% Amex / 1.0% Discover). P1 signal — merchant-account termination risk if sustained. Actionable — ops must intervene.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchChargebackRateThresholdExceededNotification (P1 admin + Academorix platform ops)
 * - observability::MetricsCollector (academorix.chargeback.rate_threshold_exceeded_total labelled by network)
 *
 * @category Chargeback
 *
 * @since    0.1.0
 */
final readonly class ChargebackRateThresholdExceeded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $network  string
     * @param  int  $rollingWindowDays  integer (default 60)
     * @param  int  $chargebacksCount  integer
     * @param  int  $transactionsCount  integer
     * @param  int  $currentRatePercent  number (basis points as decimal — 0.0092 = 0.92%)
     * @param  string  $thresholdPercent  number
     * @param  int  $daysOverThreshold  integer (how long the rate has been over threshold)
     * @param  string  $computedAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $network,
        public readonly int $rollingWindowDays,
        public readonly int $chargebacksCount,
        public readonly int $transactionsCount,
        public readonly int $currentRatePercent,
        public readonly string $thresholdPercent,
        public readonly int $daysOverThreshold,
        public readonly string $computedAt,
    ) {
    }
}
