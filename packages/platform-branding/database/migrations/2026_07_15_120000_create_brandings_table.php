<?php

/**
 * @file database/migrations/2026_07_15_120000_create_brandings_table.php
 *
 * @description
 * Create the `brandings` table for the `Branding` entity.
 * Owned by the branding module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform branding --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branding --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('brandings', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('domain_id', 64)->nullable();
            $table->foreign('domain_id')->references('id')->on('domains')->onDelete('set null');
            $table->string('name', 100);
            $table->boolean('is_default')->default(false);
            $table->string('theme', 16)->default('auto');
            $table->string('logo_url', 2048)->nullable();
            $table->string('logo_dark_url', 2048)->nullable();
            $table->string('favicon_url', 2048)->nullable();
            $table->string('primary_color', 7)->nullable();
            $table->string('secondary_color', 7)->nullable();
            $table->string('accent_color', 7)->nullable();
            $table->string('background_color', 7)->nullable();
            $table->string('surface_color', 7)->nullable();
            $table->string('text_color', 7)->nullable();
            $table->string('font_stack', 500)->nullable();
            $table->string('custom_font_url', 2048)->nullable();
            $table->jsonb('css_variables')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->jsonb('translations')->default('\'{}\'::jsonb');
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'is_default'], 'brandings_tenant_default_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_default = TRUE AND deleted_at IS NULL
            $table->unique(['tenant_id', 'domain_id'], 'brandings_tenant_domain_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE domain_id IS NOT NULL AND deleted_at IS NULL
            $table->index(['tenant_id', 'created_at'], 'brandings_tenant_created_at_index');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('brandings');
    }
};
