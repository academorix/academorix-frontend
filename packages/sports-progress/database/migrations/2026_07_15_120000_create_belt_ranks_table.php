<?php

/**
 * @file database/migrations/2026_07_15_120000_create_belt_ranks_table.php
 *
 * @description
 * Create the `belt_ranks` table for the `BeltRank` entity.
 * Owned by the progress module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports progress --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('belt_ranks', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30)->nullable();
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('sport_key', 32);
            $table->string('code', 32);
            $table->jsonb('labels')->default('{}');
            $table->string('color_hex', 7)->nullable();
            $table->integer('rank_order');
            $table->integer('min_training_hours')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'sport_key', 'code'], 'belt_ranks_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['sport_key', 'rank_order'], 'belt_ranks_order_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('belt_ranks');
    }
};
