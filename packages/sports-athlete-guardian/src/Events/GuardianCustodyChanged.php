<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-guardian --force`.

declare(strict_types=1);

namespace Academorix\Sports\AthleteGuardian\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * has_legal_custody flag flipped. Distinct event because custody changes are compliance-critical + drive downstream cascades (can_authorise_medical_care may auto-flip when has_legal_custody flips false).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — compliance-critical)
 * - activity::WriteToActivityLog
 * - notifications::DispatchGuardianCustodyChangedNotification (to guardian + admin)
 *
 * @category AthleteGuardian
 *
 * @since    0.1.0
 */
final readonly class GuardianCustodyChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $guardianId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  bool  $fromValue  boolean
     * @param  bool  $toValue  boolean
     * @param  string  $reason  string | null
     * @param  string  $changedByUserId  ULID
     */
    public function __construct(
        public readonly string $guardianId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly bool $fromValue,
        public readonly bool $toValue,
        public readonly string $reason,
        public readonly string $changedByUserId,
    ) {
    }
}
