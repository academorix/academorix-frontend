<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Platform\Teams\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Platform\Teams\Contracts\Data\TeamMemberInterface;
use Academorix\Platform\Teams\Database\Factories\TeamMemberFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a TeamMember.
 *
 * Polymorphic roster row.
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[Table(name: TeamMemberInterface::TABLE, keyType: TeamMemberInterface::KEY_TYPE)]
#[Fillable([
    TeamMemberInterface::ATTR_TENANT_ID,
        TeamMemberInterface::ATTR_TEAM_ID,
        TeamMemberInterface::ATTR_MEMBER_TYPE,
        TeamMemberInterface::ATTR_MEMBER_ID,
        TeamMemberInterface::ATTR_ROLE_ON_TEAM,
        TeamMemberInterface::ATTR_JERSEY_NUMBER,
        TeamMemberInterface::ATTR_POSITION,
        TeamMemberInterface::ATTR_STATUS,
        TeamMemberInterface::ATTR_JOINED_AT,
        TeamMemberInterface::ATTR_LEFT_AT,
        TeamMemberInterface::ATTR_LEFT_REASON,
        TeamMemberInterface::ATTR_NOTES,
        TeamMemberInterface::ATTR_METADATA,
])]
#[UseFactory(TeamMemberFactory::class)]
final class TeamMember extends Model implements TeamMemberInterface
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    // TODO(gen): resolve unknown trait `BelongsToTeam` — add its import + use line.
    use HasMetadata;
    use HasUserstamps;
    use Auditable;
    use HasActivityLog;
    use Filterable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        TeamMemberInterface::ATTR_MEMBER_TYPE => 'TeamMemberType',
        TeamMemberInterface::ATTR_ROLE_ON_TEAM => 'TeamMemberRole',
        TeamMemberInterface::ATTR_STATUS => 'TeamMemberStatus',
        TeamMemberInterface::ATTR_LEFT_REASON => 'TeamMemberLeftReason',
        TeamMemberInterface::ATTR_JERSEY_NUMBER => 'integer',
        TeamMemberInterface::ATTR_METADATA => 'array',
        TeamMemberInterface::ATTR_JOINED_AT => 'datetime',
        TeamMemberInterface::ATTR_LEFT_AT => 'datetime',
    ];
}
