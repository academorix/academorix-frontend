<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Platform\Teams\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TeamMember.status transitioned to `inactive` OR `graduated`. Fires from voluntary leave OR cascade (team archived).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchTeamMemberLeftNotification (to coach)
 *
 * @category Teams
 *
 * @since    0.1.0
 */
final readonly class TeamMemberLeft implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamMemberId  ULID
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $memberType  string
     * @param  string  $memberId  ULID
     * @param  string  $leftAt  ISO 8601
     * @param  string  $leftReason  'transferred' | 'graduated' | 'withdrew' | 'dropped' | 'injury' | 'disciplinary' | 'team_archived' | 'other'
     * @param  string  $cascadeTrigger  'user' | 'admin' | 'team_archived'
     */
    public function __construct(
        public readonly string $teamMemberId,
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $memberType,
        public readonly string $memberId,
        public readonly string $leftAt,
        public readonly string $leftReason,
        public readonly string $cascadeTrigger,
    ) {
    }
}
