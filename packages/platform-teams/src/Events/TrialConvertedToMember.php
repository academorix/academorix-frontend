<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Platform\Teams\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TeamTrial.status transitioned completed → converted. Fires from within the atomic conversion transaction's afterCommit. Companion event TeamMemberJoined fires in the same afterCommit.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchTrialDecisionNotification (to trialist — accepted)
 *
 * @category Teams
 *
 * @since    0.1.0
 */
final readonly class TrialConvertedToMember implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $trialId  ULID
     * @param  string  $teamMemberId  ULID (newly-created — see companion TeamMemberJoined event)
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $convertedAt  ISO 8601
     * @param  string  $convertedBy  ULID
     */
    public function __construct(
        public readonly string $trialId,
        public readonly string $teamMemberId,
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $convertedAt,
        public readonly string $convertedBy,
    ) {
    }
}
