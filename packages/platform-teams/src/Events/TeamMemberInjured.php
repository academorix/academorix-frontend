<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Platform\Teams\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TeamMember.status transitioned to `injured` (medical). Fires alongside TeamMemberStatusChanged.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Teams
 *
 * @since    0.1.0
 */
final readonly class TeamMemberInjured implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamMemberId  ULID
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $injuredAt  ISO 8601
     * @param  string  $recordedBy  ULID
     */
    public function __construct(
        public readonly string $teamMemberId,
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $injuredAt,
        public readonly string $recordedBy,
    ) {
    }
}
