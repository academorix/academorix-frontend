<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Platform\Teams\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Team.capacity or Team.capacity_waitlist changed. Distinct event because downstream capacity-gated flows (roster, waitlist, notifications) invalidate their caches on it.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateEligibilitySnapshot
 *
 * @category Teams
 *
 * @since    0.1.0
 */
final readonly class TeamCapacityChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $wasCapacity  int | null
     * @param  int  $newCapacity  int | null
     * @param  int  $wasCapacityWaitlist  int | null
     * @param  int  $newCapacityWaitlist  int | null
     * @param  string  $updatedBy  ULID
     */
    public function __construct(
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly int $wasCapacity,
        public readonly int $newCapacity,
        public readonly int $wasCapacityWaitlist,
        public readonly int $newCapacityWaitlist,
        public readonly string $updatedBy,
    ) {
    }
}
