<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Platform\Teams\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TeamTrial.status transitioned completed → rejected. decision_notes is required per the observer (anti-discrimination retention).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 3 years — anti-discrimination defence)
 * - activity::WriteToActivityLog
 * - notifications::DispatchTrialDecisionNotification (to trialist — rejected; fixed template, never surfaces decision_notes)
 *
 * @category Teams
 *
 * @since    0.1.0
 */
final readonly class TrialRejected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $trialId  ULID
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $rejectedAt  ISO 8601
     * @param  string  $rejectedBy  ULID
     */
    public function __construct(
        public readonly string $trialId,
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $rejectedAt,
        public readonly string $rejectedBy,
    ) {
    }
}
