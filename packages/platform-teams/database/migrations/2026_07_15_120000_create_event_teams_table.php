<?php

/**
 * @file database/migrations/2026_07_15_120000_create_event_teams_table.php
 *
 * @description
 * Create the `event_teams` table for the `EventTeam` entity.
 * Owned by the teams module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform teams --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('event_teams', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('team_id', 64);
            $table->foreign('team_id')->references('id')->on('teams')->onDelete('restrict');
            $table->string('event_id', 64)->nullable();
            $table->string('participation_type', 16)->default('participant');
            $table->timestampTz('registered_at');
            $table->timestampTz('withdrew_at')->nullable();
            $table->text('withdrawal_reason')->nullable();
            $table->timestampTz('checked_in_at')->nullable();
            $table->jsonb('result')->nullable();
            $table->integer('attendance_count')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 64)->nullable();
            $table->string('updated_by', 64)->nullable();
            $table->string('deleted_by', 64)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'event_id', 'team_id'], 'event_teams_active_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL AND event_id IS NOT NULL
            $table->index(['tenant_id', 'team_id'], 'event_teams_team_idx');
            $table->index(['tenant_id', 'event_id'], 'event_teams_event_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE event_id IS NOT NULL
            $table->index(['tenant_id', 'registered_at'], 'event_teams_registered_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('event_teams');
    }
};
