<?php

/**
 * @file database/migrations/2026_07_15_120003_create_teams_table.php
 *
 * @description
 * Create the `teams` table for the `Team` entity.
 * Owned by the teams module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform teams --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('teams', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('organization_id', 64);
            $table->foreign('organization_id')->references('id')->on('organizations')->onDelete('restrict');
            $table->string('branch_id', 64);
            $table->foreign('branch_id')->references('id')->on('branches')->onDelete('restrict');
            $table->string('season_id', 64)->nullable();
            $table->string('age_group_id', 64)->nullable();
            $table->string('coach_id', 64)->nullable();
            $table->string('name', 128);
            $table->string('slug', 128);
            $table->text('description')->nullable();
            $table->string('team_type', 32);
            $table->string('sport_key', 32)->nullable();
            $table->string('competitive_level', 32)->nullable();
            $table->string('gender_category', 16)->nullable();
            $table->integer('min_age')->nullable();
            $table->integer('max_age')->nullable();
            $table->integer('capacity')->nullable();
            $table->integer('capacity_waitlist')->nullable();
            $table->string('primary_color', 7)->nullable();
            $table->string('secondary_color', 7)->nullable();
            $table->string('logo_url', 512)->nullable();
            $table->jsonb('schedule_pattern')->nullable();
            $table->string('status', 16)->default('active');
            $table->boolean('is_default')->default(false);
            $table->integer('sort_order')->default(100);
            $table->timestampTz('registration_opens_at')->nullable();
            $table->timestampTz('registration_closes_at')->nullable();
            $table->timestampTz('last_registration_opened_notification')->nullable();
            $table->timestampTz('last_registration_closed_notification')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 64)->nullable();
            $table->string('updated_by', 64)->nullable();
            $table->string('deleted_by', 64)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'branch_id', 'slug'], 'teams_tenant_branch_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'branch_id', 'status'], 'teams_status_idx');
            $table->index(['tenant_id', 'team_type'], 'teams_type_idx');
            $table->index(['tenant_id', 'sport_key'], 'teams_sport_idx');
            $table->index(['tenant_id', 'coach_id'], 'teams_coach_idx');
            $table->index(['tenant_id', 'season_id'], 'teams_season_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE season_id IS NOT NULL
            $table->index(['tenant_id', 'registration_opens_at'], 'teams_registration_opens_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE status = 'active' AND registration_opens_at IS NOT NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('teams');
    }
};
