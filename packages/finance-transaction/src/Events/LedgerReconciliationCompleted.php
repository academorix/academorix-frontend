<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance transaction --force`.

declare(strict_types=1);

namespace Academorix\Finance\Transaction\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReconcileLedgerBalancesJob completed its nightly sweep. Reports total transactions scanned + per-account balance snapshots + any imbalances detected (fires LedgerImbalanceDetected per imbalance BEFORE this event). Every fire is expected; every scan-completed-with-imbalances-found is a P0.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — regulators love to see the nightly reconciler completing on schedule)
 * - monitoring::MetricsCollector (increments academorix.ledger.reconciliation_runs_total; labels: outcome='balanced' or 'imbalanced')
 *
 * @category Transaction
 *
 * @since    0.1.0
 */
final readonly class LedgerReconciliationCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  int  $transactionsScanned  integer
     * @param  int  $accountsScanned  integer
     * @param  int  $imbalancesDetected  integer (0 = healthy; > 0 = P0 signal)
     * @param  int  $reconciliationDurationMs  integer
     * @param  string  $completedAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly int $transactionsScanned,
        public readonly int $accountsScanned,
        public readonly int $imbalancesDetected,
        public readonly int $reconciliationDurationMs,
        public readonly string $completedAt,
    ) {
    }
}
