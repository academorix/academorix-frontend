<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance transaction --force`.

declare(strict_types=1);

namespace Academorix\Finance\Transaction\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TransactionObserver.creating committed a new transactions row (status='draft', before ledger entries attach). Fires exactly once per transaction. Precedes TransactionPosted by the balancing step in the same DB transaction.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — SOX 404)
 * - activity::WriteToActivityLog
 * - monitoring::MetricsCollector (increments academorix.transaction.created_total)
 *
 * @category Transaction
 *
 * @since    0.1.0
 */
final readonly class TransactionCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $transactionId  ULID (trx_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $transactionNumber  string (TXN-YYYY-NNNNNNNN — sequential per tenant per year)
     * @param  string  $kind  string (one of 12 TransactionKind values)
     * @param  string  $referenceType  string (Invoice / Payment / Refund / Chargeback / CouponRedemption / adjustment_row)
     * @param  string  $referenceId  ULID (polymorphic)
     * @param  int  $amountCents  integer (positive)
     * @param  string  $currency  string (ISO 4217)
     * @param  string  $baseCurrency  string (ISO 4217) | null
     * @param  int  $baseAmountCents  integer | null
     * @param  string  $effectiveDate  ISO 8601 date
     * @param  string  $createdByUserId  ULID (usr_*) | null
     * @param  string  $createdAt  ISO 8601
     */
    public function __construct(
        public readonly string $transactionId,
        public readonly string $tenantId,
        public readonly string $transactionNumber,
        public readonly string $kind,
        public readonly string $referenceType,
        public readonly string $referenceId,
        public readonly int $amountCents,
        public readonly string $currency,
        public readonly string $baseCurrency,
        public readonly int $baseAmountCents,
        public readonly string $effectiveDate,
        public readonly string $createdByUserId,
        public readonly string $createdAt,
    ) {
    }
}
