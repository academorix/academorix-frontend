<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Identity\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A single PAT was revoked (via HasSessionRevocation trait, direct API call, or platform-admin action). Distinguished from UserLoggedOut because it does NOT require the user to be the actor.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitSessionRevokedMetric
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class SessionRevoked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  int  $tokenId  integer
     * @param  string  $reason  'password_changed' | 'user_suspended' | 'user_disabled' | 'admin_revocation' | 'idle_timeout' | 'refresh_reuse'
     * @param  string  $actorId  ULID | null (null = system revocation)
     * @param  string  $revokedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $userId,
        public readonly int $tokenId,
        public readonly string $reason,
        public readonly string $actorId,
        public readonly string $revokedAt,
    ) {
    }
}
