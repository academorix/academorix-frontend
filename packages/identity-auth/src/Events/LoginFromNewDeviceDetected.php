<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Identity\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * DeviceFingerprinter reported the login's device fingerprint has not been seen for this User in the last `auth.session.new_device_lookback_days` (default 30).
 *
 * ## Consumers
 *
 * - notifications::DispatchLoginFromNewDeviceNotification
 * - activity::WriteToActivityLog
 * - analytics::TrackNewDeviceLogin
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class LoginFromNewDeviceDetected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $identityId  ULID
     * @param  string  $deviceFingerprint  string
     * @param  string  $geolocationHint  string | null (city, country — GeoIP best-effort)
     * @param  string  $detectedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $identityId,
        public readonly string $deviceFingerprint,
        public readonly string $geolocationHint,
        public readonly string $detectedAt,
    ) {
    }
}
