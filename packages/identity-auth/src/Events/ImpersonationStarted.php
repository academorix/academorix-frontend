<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Identity\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Platform staff invoked POST /platform/auth/impersonate/start with valid target + reason. `platform.impersonate` entitlement checked; delegation session recorded in `access/delegation` (Wave 1b module).
 *
 * ## Consumers
 *
 * - notifications::DispatchImpersonationSessionActiveNotification
 * - activity::WriteToActivityLog
 * - compliance::RecordImpersonationSession
 * - observability::EmitImpersonationStartedMetric
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class ImpersonationStarted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $impersonatorPlatformUserId  ULID (`plu_...`)
     * @param  string  $targetUserId  ULID (`usr_...` — the tenant User being impersonated)
     * @param  string  $targetApplicationId  ULID
     * @param  string  $targetTenantId  ULID
     * @param  string  $reason  string (mandatory free-form justification)
     * @param  string  $startedAt  ISO-8601 timestamp
     * @param  int  $sessionTtlSeconds  integer
     */
    public function __construct(
        public readonly string $impersonatorPlatformUserId,
        public readonly string $targetUserId,
        public readonly string $targetApplicationId,
        public readonly string $targetTenantId,
        public readonly string $reason,
        public readonly string $startedAt,
        public readonly int $sessionTtlSeconds,
    ) {
    }
}
