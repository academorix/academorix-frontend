<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Identity\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Bulk revocation — every active PAT for a User (or every User referencing an Identity in the cross-Application PasswordChanged case). Fires ONCE with the count; per-token SessionRevoked NOT fired for the bulk case.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitBulkSessionRevocationMetric
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class AllSessionsRevoked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID | null (null when the bulk was cross-Application on an Identity)
     * @param  string  $identityId  ULID | null (non-null on cross-Application bulk)
     * @param  int  $tokensRevokedCount  integer
     * @param  string  $reason  'password_changed' | 'identity_erased' | 'breach_detected' | 'admin_bulk_revoke'
     * @param  string  $actorId  ULID | null
     * @param  string  $revokedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $identityId,
        public readonly int $tokensRevokedCount,
        public readonly string $reason,
        public readonly string $actorId,
        public readonly string $revokedAt,
    ) {
    }
}
