<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Identity\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Specific `jti` added to the deny list — revoke a JWT immediately, before natural expiry. Used on password-change, user-suspend, breach response.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitJwtRevokedMetric
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class JwtRevoked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $jti  UUID
     * @param  string  $reason  'password_change' | 'user_suspended' | 'manual_revocation' | 'breach_response'
     * @param  string  $actorId  ULID | null
     * @param  string  $revokedAt  ISO-8601 timestamp
     * @param  string  $naturalExpiry  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $jti,
        public readonly string $reason,
        public readonly string $actorId,
        public readonly string $revokedAt,
        public readonly string $naturalExpiry,
    ) {
    }
}
