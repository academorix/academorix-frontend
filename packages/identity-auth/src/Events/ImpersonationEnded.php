<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Identity\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /platform/auth/impersonate/end OR TTL expiry OR explicit revocation. Impersonation PAT revoked.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - compliance::CloseImpersonationSession
 * - observability::EmitImpersonationEndedMetric
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class ImpersonationEnded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $impersonatorPlatformUserId  ULID
     * @param  string  $targetUserId  ULID
     * @param  int  $durationSeconds  integer
     * @param  string  $trigger  'self_service' | 'ttl_expired' | 'admin_revocation'
     * @param  string  $endedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $impersonatorPlatformUserId,
        public readonly string $targetUserId,
        public readonly int $durationSeconds,
        public readonly string $trigger,
        public readonly string $endedAt,
    ) {
    }
}
