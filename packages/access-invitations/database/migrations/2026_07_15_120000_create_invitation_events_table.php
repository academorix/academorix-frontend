<?php

/**
 * @file database/migrations/2026_07_15_120000_create_invitation_events_table.php
 *
 * @description
 * Create the `invitation_events` table for the `InvitationEvent` entity.
 * Owned by the invitations module. Emitted from the module blueprint —
 * regenerate via `generate-module.py access invitations --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access invitations --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('invitation_events', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('invitation_id', 64);
            $table->foreign('invitation_id')->references('id')->on('invitations')->onDelete('cascade');
            $table->string('application_id', 64);
            $table->foreign('application_id')->references('id')->on('applications')->onDelete('restrict');
            $table->string('tenant_id', 64)->nullable();
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('event', 24);
            $table->timestampTz('occurred_at');
            $table->string('actor_type', 32)->nullable();
            $table->string('actor_id', 64)->nullable();
            $table->string('ip_address')->nullable();
            $table->string('user_agent', 500)->nullable();
            $table->string('country_code', 2)->nullable();
            $table->string('city', 100)->nullable();
            $table->string('transport', 32)->nullable();
            $table->string('signal_id', 200)->nullable();
            $table->string('error_code', 64)->nullable();
            $table->text('error_message')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->timestampTz('created_at');
            $table->index(['invitation_id', 'occurred_at'], 'invitation_events_invitation_occurred_index');
            $table->unique(['signal_id', 'transport'], 'invitation_events_signal_transport_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE signal_id IS NOT NULL
            $table->index(['tenant_id', 'event', 'occurred_at'], 'invitation_events_tenant_event_occurred_index');
            $table->index(['event', 'occurred_at'], 'invitation_events_event_occurred_index');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('invitation_events');
    }
};
