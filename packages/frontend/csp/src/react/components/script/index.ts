/**
 * @file index.ts
 * @module @stackra/csp/react/components/script
 * @description Entity barrel — re-exports the `Script` component that
 *   renders an inline `<script>` tag stamped with the active nonce so it
 *   passes CSP.
 */

export { Script } from "./script.component";
