/**
 * @file index.ts
 * @module @stackra/csp/react/providers/nonce
 * @description Entity barrel — re-exports the `NonceProvider` React
 *   provider that seeds the current CSP nonce into `NonceContext` for
 *   descendants to read.
 */

export { NonceProvider } from "./nonce.provider";
