/**
 * @file index.ts
 * @module @stackra/sdui/core/registries
 * @description Public API barrel for the core `registries` category —
 *   re-exports the component / layout / page registries the SDUI renderer
 *   reads at runtime.
 */

export { ComponentRegistry } from "./component.registry";
export { LayoutRegistry } from "./layout.registry";
export { SduiPageRegistry } from "./sdui-page.registry";
