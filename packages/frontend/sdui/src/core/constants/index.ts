/**
 * @file index.ts
 * @module @stackra/sdui/core/constants
 * @description Public API barrel for the core `constants` category —
 *   re-exports the schema-version constants (`SDUI_SCHEMA_VERSION` /
 *   `SDUI_MIN_SUPPORTED_VERSION`) used by the SDUI validator.
 */

export { SDUI_SCHEMA_VERSION, SDUI_MIN_SUPPORTED_VERSION } from "./sdui-version.constant";
