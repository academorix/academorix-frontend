/**
 * @file index.ts
 * @module @stackra/sdui/core/utils
 * @description Public API barrel for the core `utils` category —
 *   re-exports the `getAtPath` / `setAtPath` dotted-path helpers used by
 *   binding resolvers.
 */

export { getAtPath, setAtPath } from "./dotted-path.util";
