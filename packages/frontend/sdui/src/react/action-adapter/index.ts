/**
 * @file index.ts
 * @module @stackra/sdui/react/action-adapter
 * @description Public API barrel for the React subpath's `action-adapter`
 *   category — re-exports the `useSduiActionAdapter` hook that bridges
 *   SDUI action descriptors into runtime callbacks.
 */

export { useSduiActionAdapter } from "./action-adapter";
