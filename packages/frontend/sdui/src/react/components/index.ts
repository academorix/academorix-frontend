/**
 * @file index.ts
 * @module @stackra/sdui/react/components
 * @description Public API barrel for the React subpath's `components`
 *   category — re-exports the `SduiScreenView` component that renders
 *   an entire SDUI screen tree and its `ISduiScreenViewProps` interface.
 */

export { SduiScreenView, type ISduiScreenViewProps } from "./SduiScreenView";
