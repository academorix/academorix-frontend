/**
 * @file index.ts
 * @module @stackra/actions/core/decorators
 * @description Public API barrel for the `decorators` category — re-exports
 *   the `@ActionHandler(...)` metadata decorator that binds a handler class
 *   to an action name.
 */

export { ActionHandler } from "./action-handler.decorator";
