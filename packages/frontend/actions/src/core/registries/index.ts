/**
 * @file index.ts
 * @module @stackra/actions/core/registries
 * @description Public API barrel for the `registries` category — re-exports
 *   `ActionRegistry`, the discovery-driven registry that maps action names
 *   to their handler classes.
 */

export { ActionRegistry } from "./action.registry";
