/**
 * @file index.ts
 * @module @stackra/scope/react/components
 * @description Public API barrel for the React subpath's `components`
 *   category — re-exports the `ScopeSwitcher` compound component and its
 *   `ScopeSwitcherProps` interface.
 */

export { ScopeSwitcher } from "./scope-switcher";
export type { ScopeSwitcherProps } from "./scope-switcher";
