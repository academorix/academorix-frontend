/**
 * @file index.ts
 * @module @stackra/scope/react/interfaces
 * @description Public API barrel for the React subpath's `interfaces`
 *   category — re-exports the `ScopeSwitcherProps` interface consumed by
 *   the scope switcher component.
 */

export type { ScopeSwitcherProps } from "./scope-switcher-props.interface";
