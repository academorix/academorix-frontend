/**
 * @file index.ts
 * @module @stackra/support/registries
 * @description Public API barrel for registry base classes.
 *
 *   `BaseRegistry` is the abstract key→value registry every
 *   domain-specific registry in the monorepo extends.
 */

export { BaseRegistry } from "./base.registry";
