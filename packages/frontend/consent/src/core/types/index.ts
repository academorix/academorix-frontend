/**
 * @file index.ts
 * @module @stackra/consent/core/types
 * @description Barrel for package-owned types. `IConsentCategory`,
 *   `IConsentManager`, `IConsentStorageAdapter`, and `ITranslatableLabel`
 *   live in `@stackra/contracts` — imported directly, never re-exported
 *   from here.
 */

export type { IConsentModuleOptions } from "./consent-module-options.interface";
