/**
 * @file index.ts
 * @module @stackra/i18n/core/types
 * @description Barrel export for package-owned pure type aliases.
 *
 *   Cross-package types (`I18nTranslation`, `Path`, `PathValue`,
 *   `IfAnyOrNever`) live in `@stackra/contracts` and are imported from
 *   there directly.
 */

export type { ExportFormat } from "./export-format.type";
