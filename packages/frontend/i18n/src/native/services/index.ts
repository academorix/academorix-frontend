/**
 * @file index.ts
 * @module @stackra/i18n/native/services
 * @description Public API barrel for the native subpath's `services`
 *   category. Currently empty — writing direction is handled by
 *   `NativeDirectionAdapter` under `../adapters` — reserved for future
 *   native-only service classes.
 */

export {};
