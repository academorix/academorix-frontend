/**
 * @file index.ts
 * @module @stackra/i18n/react/components/language-toggle
 * @description Entity barrel — re-exports the `LanguageToggle` component
 *   that toggles between two configured locales in one click.
 */

export { LanguageToggle } from "./language-toggle.component";
