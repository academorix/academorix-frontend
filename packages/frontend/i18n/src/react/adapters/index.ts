/**
 * @file index.ts
 * @module @stackra/i18n/react/adapters
 * @description Barrel export for web-only adapters. Locale storage is
 *   handled by `StorageBackedLocaleAdapter` in `../core/adapters` —
 *   one class for both platforms.
 */

export { WebDirectionAdapter } from "./web-direction.adapter";
