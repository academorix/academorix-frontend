/**
 * @file index.ts
 * @module @stackra/sync/core/constants
 * @description Barrel for package-owned constants. DI tokens live in
 *   `@stackra/contracts` — imported directly by consumers, never re-exported
 *   from here.
 */

export { DEFAULT_SYNC_CONFIG } from "./default-sync-config.constant";
