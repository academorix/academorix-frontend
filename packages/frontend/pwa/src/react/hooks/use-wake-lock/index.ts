/**
 * @file index.ts
 * @module @stackra/pwa/react/hooks/use-wake-lock
 * @description Entity barrel — re-exports the `useWakeLock` hook that
 *   acquires the Screen Wake Lock API and its `IUseWakeLockResult`
 *   return-shape interface.
 */

export { useWakeLock, type IUseWakeLockResult } from "./use-wake-lock.hook";
