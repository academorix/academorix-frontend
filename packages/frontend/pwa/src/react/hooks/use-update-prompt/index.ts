/**
 * @file index.ts
 * @module @stackra/pwa/react/hooks/use-update-prompt
 * @description Entity barrel — re-exports the `useUpdatePrompt` hook that
 *   detects a waiting service-worker update and its
 *   `IUseUpdatePromptResult` return-shape interface.
 */

export { useUpdatePrompt } from "./use-update-prompt.hook";
export type { IUseUpdatePromptResult } from "./use-update-prompt.interface";
