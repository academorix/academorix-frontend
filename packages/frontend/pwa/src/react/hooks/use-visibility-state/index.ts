/**
 * @file index.ts
 * @module @stackra/pwa/react/hooks/use-visibility-state
 * @description Entity barrel — re-exports the `useVisibilityState` hook that
 *   reads `document.visibilityState` reactively.
 */

export { useVisibilityState } from "./use-visibility-state.hook";
