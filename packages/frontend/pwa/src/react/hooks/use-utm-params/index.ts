/**
 * @file index.ts
 * @module @stackra/pwa/react/hooks/use-utm-params
 * @description Entity barrel — re-exports the `useUtmParams` hook that
 *   captures UTM query parameters from the referring URL and its
 *   `IPwaUtmParams` shape.
 */

export { useUtmParams } from "./use-utm-params.hook";
export type { IPwaUtmParams } from "./use-utm-params.interface";
