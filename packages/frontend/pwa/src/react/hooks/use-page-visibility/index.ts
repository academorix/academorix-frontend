/**
 * @file index.ts
 * @module @stackra/pwa/react/hooks/use-page-visibility
 * @description Entity barrel — re-exports the `usePageVisibility` hook that
 *   listens to `document.visibilitychange` events and its
 *   `PageVisibilityCallback` type.
 */

export { usePageVisibility, type PageVisibilityCallback } from "./use-page-visibility.hook";
