/**
 * @file index.ts
 * @module @stackra/pwa/react/hooks/use-standalone-mode
 * @description Entity barrel — re-exports the `useStandaloneMode` hook that
 *   reports whether the PWA is running in standalone (installed) mode.
 */

export { useStandaloneMode } from "./use-standalone-mode.hook";
