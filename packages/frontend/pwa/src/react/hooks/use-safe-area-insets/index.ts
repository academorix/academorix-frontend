/**
 * @file index.ts
 * @module @stackra/pwa/react/hooks/use-safe-area-insets
 * @description Entity barrel — re-exports the `useSafeAreaInsets` hook that
 *   reads CSS `env(safe-area-inset-*)` values and its
 *   `IUseSafeAreaInsetsResult` return-shape interface.
 */

export { useSafeAreaInsets } from "./use-safe-area-insets.hook";
export type { IUseSafeAreaInsetsResult } from "./use-safe-area-insets.interface";
