/**
 * @file index.ts
 * @module @stackra/pipeline/types
 * @description Public API barrel for the `types` category — re-exports the
 *   package's type aliases (see `code-standards.md`).
 */

export type { PipelineFactory } from "./pipeline-factory.type";
