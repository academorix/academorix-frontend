/**
 * @file devtools-shell.interface.ts
 * @module @stackra/devtools/native/components
 * @description Props for the native devtools shell.
 */

/** Props for the native devtools shell. */
export interface DevtoolsShellProps {
  /** Optional className. */
  readonly className?: string;
}
