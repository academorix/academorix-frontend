/**
 * @file overview-panel.interface.ts
 * @module @stackra/devtools/native/components
 * @description Props for the native {@link OverviewPanel}.
 */

/** Props for the native {@link OverviewPanel}. */
export interface OverviewPanelProps {
  /** Optional extra classes for the root. */
  readonly className?: string;
}
