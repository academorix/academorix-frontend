/**
 * @file index.ts
 * @module @stackra/container/core/discovery/constants
 * @description Public API barrel for the discovery sub-domain's `constants`
 *   category — re-exports `DEFAULT_DISCOVERY_CONFIG`, the default options
 *   merged into every `DiscoveryModule.forRoot(...)` call.
 */

export { DEFAULT_DISCOVERY_CONFIG } from "./default-discovery-config.constant";
