/**
 * @file index.ts
 * @module @stackra/container/core/discovery/utils
 * @description Public API barrel for the discovery sub-domain's `utils`
 *   category — re-exports the `mergeDiscoveryConfig` helper that applies
 *   `DEFAULT_DISCOVERY_CONFIG` to consumer-supplied options.
 */

export { mergeDiscoveryConfig } from "./merge-config.util";
