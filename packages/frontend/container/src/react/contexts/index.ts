/**
 * @file index.ts
 * @module @stackra/container/react/contexts
 * @description Barrel export for React contexts owned by the container's
 *   React subpath.
 *
 *   - {@link ContainerContext} — React context holding the DI container resolver.
 */

export { ContainerContext } from "./container";
