/**
 * @file index.ts
 * @module @stackra/events/core/registries
 * @description Public barrel for the `registries/` category — every
 *   registry class the events package ships is re-exported here.
 */

export { EventTransportRegistry } from "./event-transport.registry";
