/**
 * @file index.ts
 * @module @stackra/ui/actions/registries
 * @description Public barrel for the actions `registries/` category —
 *   every registry class the actions subdomain ships is re-exported
 *   here (currently `OverlayRegistry`).
 */

export { OverlayRegistry } from "./overlay.registry";
