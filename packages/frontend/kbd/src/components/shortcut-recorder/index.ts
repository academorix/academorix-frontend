/**
 * @file index.ts
 * @module @stackra/kbd/components/shortcut-recorder
 * @description Entity barrel — re-exports the `ShortcutRecorder` component
 *   that captures a user-pressed chord and returns it to a callback along
 *   with its props interface.
 */

export { ShortcutRecorder } from "./shortcut-recorder.component";
export type { ShortcutRecorderProps } from "./shortcut-recorder.component";
