/**
 * @file index.ts
 * @module @stackra/kbd/components/keyboard-shortcut
 * @description Entity barrel — re-exports the `KeyboardShortcut` declarative
 *   component that registers a single shortcut with the hotkey registry
 *   while its host is mounted.
 */

export { KeyboardShortcut } from "./keyboard-shortcut.component";
