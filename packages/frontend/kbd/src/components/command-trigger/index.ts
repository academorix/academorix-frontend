/**
 * @file index.ts
 * @module @stackra/kbd/components/command-trigger
 * @description Entity barrel — re-exports the `CommandTrigger` component that
 *   opens the command palette on activation.
 */

export { CommandTrigger } from "./command-trigger.component";
