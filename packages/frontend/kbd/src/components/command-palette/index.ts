/**
 * @file index.ts
 * @module @stackra/kbd/components/command-palette
 * @description Entity barrel — re-exports the `CommandPalette` compound
 *   component together with its typed props / variant shapes.
 */

export { CommandPalette } from "./command-palette.component";
export type { CommandPaletteProps, CommandPaletteVariant } from "./command-palette.component";
