/**
 * @file index.ts
 * @module @stackra/kbd/components/keyboard-catalog
 * @description Entity barrel — re-exports the `KeyboardCatalog` component
 *   that renders every registered shortcut grouped by scope + category.
 */

export { KeyboardCatalog } from "./keyboard-catalog.component";
