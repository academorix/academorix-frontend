/**
 * @file index.ts
 * @module @stackra/kbd/hooks/use-command
 * @description Entity barrel — re-exports the `useCommand` hook that
 *   registers a single command in the palette while its host is mounted.
 */

export { useCommand } from "./use-command.hook";
