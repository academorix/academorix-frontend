/**
 * @file index.ts
 * @module @stackra/kbd/hooks/use-command-palette
 * @description Entity barrel — re-exports the `useCommandPalette` hook that
 *   reads and controls the command-palette open state from React.
 */

export { useCommandPalette } from "./use-command-palette.hook";
