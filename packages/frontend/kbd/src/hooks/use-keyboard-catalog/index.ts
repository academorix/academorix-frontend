/**
 * @file index.ts
 * @module @stackra/kbd/hooks/use-keyboard-catalog
 * @description Entity barrel — re-exports the `useKeyboardCatalog` hook
 *   that reads the current shortcut catalog + open/close state.
 */

export { useKeyboardCatalog } from "./use-keyboard-catalog.hook";
