/**
 * @file index.ts
 * @module @stackra/kbd/hooks/use-draggable
 * @description Entity barrel — re-exports the `useDraggable` hook that
 *   wires a keyboard-driven drag interaction to a target element.
 */

export { useDraggable } from "./use-draggable.hook";
