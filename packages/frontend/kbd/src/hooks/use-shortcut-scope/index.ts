/**
 * @file index.ts
 * @module @stackra/kbd/hooks/use-shortcut-scope
 * @description Entity barrel — re-exports the `useShortcutScope` hook that
 *   scopes a subtree's shortcut registrations to a named scope.
 */

export { useShortcutScope } from "./use-shortcut-scope.hook";
