/**
 * @file index.ts
 * @module @stackra/kbd/hooks/use-shortcut
 * @description Entity barrel — re-exports the `useShortcut` hook that
 *   registers a shortcut + handler while its host component is mounted.
 */

export { useShortcut } from "./use-shortcut.hook";
