/**
 * @file index.ts
 * @module @stackra/network/detectors
 * @description Core network detectors — only platform-agnostic (Node.js) detectors.
 *   Browser-specific detectors live in `./react`, native detectors in `./native`.
 */

export { NodeNetworkDetector } from "./node-network.detector";
