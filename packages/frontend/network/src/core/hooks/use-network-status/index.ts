/**
 * @file index.ts
 * @module @stackra/network/core/hooks/use-network-status
 * @description Entity barrel — re-exports the cross-platform
 *   `useNetworkStatus` hook that subscribes to `NetworkService` status
 *   changes and returns a snapshot to React consumers.
 */

export { useNetworkStatus } from "./use-network-status.hook";
