/**
 * @file index.ts
 * @module @stackra/network/core/hooks
 * @description Barrel for the cross-platform network hooks (shared by the
 *   `react` and `native` subpaths).
 */

export { useNetworkStatus } from "./use-network-status";
export type { UseNetworkStatusResult } from "../interfaces";
