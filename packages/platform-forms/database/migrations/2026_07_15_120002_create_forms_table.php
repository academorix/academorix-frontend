<?php

/**
 * @file database/migrations/2026_07_15_120002_create_forms_table.php
 *
 * @description
 * Create the `forms` table for the `Form` entity.
 * Owned by the forms module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform forms --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform forms --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('forms', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('slug', 100);
            $table->string('title', 200);
            $table->text('description')->nullable();
            $table->string('kind', 32);
            $table->string('status', 16)->default('draft');
            $table->string('published_version_id', 30)->nullable();
            $table->foreign('published_version_id')->references('id')->on('form_versions')->onDelete('restrict');
            $table->string('handoff_slug', 100)->nullable();
            $table->jsonb('handoff_config')->nullable();
            $table->timestampTz('opens_at')->nullable();
            $table->timestampTz('closes_at')->nullable();
            $table->boolean('requires_authentication')->default('false');
            $table->string('public_url_signature', 128)->nullable();
            $table->string('locale_default', 8)->default('en');
            $table->jsonb('locales_available')->default('["en"]');
            $table->jsonb('branding_snapshot')->nullable();
            $table->integer('submission_count')->default('0');
            $table->jsonb('settings')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'slug'], 'forms_tenant_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'kind', 'status'], 'forms_kind_status_idx');
            $table->unique(['public_url_signature'], 'forms_public_signature_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE public_url_signature IS NOT NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('forms');
    }
};
