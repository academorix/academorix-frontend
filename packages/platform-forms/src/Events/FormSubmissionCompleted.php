<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform forms --force`.

declare(strict_types=1);

namespace Academorix\Platform\Forms\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P0 event. User completed the form. status: in_progress → completed. Handoff fires next (sync or via RunFormHandoffJob).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::EmailSubmissionReceipt
 * - jobs::RunFormHandoffJob
 * - reporting::TrackFormCompletion
 *
 * @category Forms
 *
 * @since    0.1.0
 */
final readonly class FormSubmissionCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $submissionId  ULID
     * @param  string  $formId  ULID
     * @param  string  $formVersionId  ULID
     * @param  string  $submitterEmailSnapshot  string|null
     * @param  string  $handoffSlug  string|null
     */
    public function __construct(
        public readonly string $submissionId,
        public readonly string $formId,
        public readonly string $formVersionId,
        public readonly string $submitterEmailSnapshot,
        public readonly string $handoffSlug,
    ) {
    }
}
