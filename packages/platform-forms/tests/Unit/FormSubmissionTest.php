<?php

declare(strict_types=1);

use Academorix\Platform\Forms\Models\FormSubmission;

it('is a placeholder — replace with actual FormSubmission unit tests', function (): void {
    expect(FormSubmission::class)->toBeString();
});
