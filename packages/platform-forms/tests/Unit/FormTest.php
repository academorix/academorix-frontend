<?php

declare(strict_types=1);

use Academorix\Platform\Forms\Models\Form;

it('is a placeholder — replace with actual Form unit tests', function (): void {
    expect(Form::class)->toBeString();
});
