<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Workflow\Approvals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Tenant admin authored a new template. Version = 1 always.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord
 * - notifications::DispatchApprovalTemplateChangedNotification (opt-in to tenant admins)
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final readonly class ApprovalTemplateCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalTemplateId  ULID (`apt_...`)
     * @param  string  $tenantId  ULID
     * @param  string  $applicationId  ULID
     * @param  string  $actionKey  string
     * @param  string  $name  string
     * @param  int  $version  integer (always 1 on create)
     * @param  int  $priority  integer
     * @param  string  $authorUserId  ULID
     */
    public function __construct(
        public readonly string $approvalTemplateId,
        public readonly string $tenantId,
        public readonly string $applicationId,
        public readonly string $actionKey,
        public readonly string $name,
        public readonly int $version,
        public readonly int $priority,
        public readonly string $authorUserId,
    ) {
    }
}
