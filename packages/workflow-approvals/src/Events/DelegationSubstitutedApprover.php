<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Workflow\Approvals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * During ApproverSelectorEngine::resolve(), an approver in the resolved set was substituted for their delegate because an active `access/delegation::RoleDelegation` covers the role/time-window. Fires PER substitution — a single instance-create with 3 delegated approvers fires 3 events.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord (audit-only — no ops action)
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final readonly class DelegationSubstitutedApprover implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalRequirementId  ULID
     * @param  string  $approvalInstanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $delegatorId  ULID (the original resolved user)
     * @param  string  $delegateId  ULID (the substitute)
     * @param  string  $roleDelegationId  ULID (the access/delegation::RoleDelegation row that authorised this substitution)
     * @param  string  $substitutedAt  ISO-8601
     */
    public function __construct(
        public readonly string $approvalRequirementId,
        public readonly string $approvalInstanceId,
        public readonly string $tenantId,
        public readonly string $delegatorId,
        public readonly string $delegateId,
        public readonly string $roleDelegationId,
        public readonly string $substitutedAt,
    ) {
    }
}
