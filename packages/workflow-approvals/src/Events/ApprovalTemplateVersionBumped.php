<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Workflow\Approvals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Tenant admin edited a template's semantic-behaviour fields (when_expression, groups[].selector_expression, groups[].quorum, groups[].sort_order, on_timeout, allows_recall) — the template's version column increments AND a new row is written (old row preserved for audit).
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord (P0 — governance change)
 * - notifications::DispatchApprovalTemplateChangedNotification
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final readonly class ApprovalTemplateVersionBumped implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalTemplateId  ULID (the NEW row's id)
     * @param  string  $priorApprovalTemplateId  ULID (the OLD row's id, is_active=false now)
     * @param  string  $tenantId  ULID
     * @param  string  $applicationId  ULID
     * @param  string  $actionKey  string
     * @param  string  $name  string
     * @param  int  $priorVersion  integer
     * @param  int  $newVersion  integer
     * @param  string  $changedFields  string[]
     * @param  string  $editorUserId  ULID
     */
    public function __construct(
        public readonly string $approvalTemplateId,
        public readonly string $priorApprovalTemplateId,
        public readonly string $tenantId,
        public readonly string $applicationId,
        public readonly string $actionKey,
        public readonly string $name,
        public readonly int $priorVersion,
        public readonly int $newVersion,
        public readonly string $changedFields,
        public readonly string $editorUserId,
    ) {
    }
}
