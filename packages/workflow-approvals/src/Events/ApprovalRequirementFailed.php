<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Workflow\Approvals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A reject decision was recorded on a requirement whose quorum demands unanimous approval (`quorum_type='all'`) OR the reject-with-quorum-any short-circuits the group. Consequence: the requirement transitions to failed AND the instance transitions to rejected.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord
 * - broadcast::PublishToApprovalChannels
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final readonly class ApprovalRequirementFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalRequirementId  ULID
     * @param  string  $approvalInstanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $groupName  string
     * @param  string  $failedAt  ISO-8601
     * @param  string  $rejectingApproverId  ULID
     * @param  string  $rejectionComment  string
     */
    public function __construct(
        public readonly string $approvalRequirementId,
        public readonly string $approvalInstanceId,
        public readonly string $tenantId,
        public readonly string $groupName,
        public readonly string $failedAt,
        public readonly string $rejectingApproverId,
        public readonly string $rejectionComment,
    ) {
    }
}
