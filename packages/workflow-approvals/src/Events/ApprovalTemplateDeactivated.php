<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Workflow\Approvals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Tenant admin flipped is_active=false OR peer activation deactivated this row via the observer. Distinct from soft-delete (still-active-but-hidden vs archived-permanently).
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final readonly class ApprovalTemplateDeactivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalTemplateId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $actionKey  string
     * @param  string  $name  string
     * @param  string  $trigger  'explicit' | 'peer_activation'
     * @param  string  $actorUserId  ULID
     */
    public function __construct(
        public readonly string $approvalTemplateId,
        public readonly string $tenantId,
        public readonly string $actionKey,
        public readonly string $name,
        public readonly string $trigger,
        public readonly string $actorUserId,
    ) {
    }
}
