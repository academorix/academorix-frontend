<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Workflow\Approvals\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Workflow\Approvals\Contracts\Data\ApprovalTemplateInterface;
use Academorix\Workflow\Approvals\Database\Factories\ApprovalTemplateFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Laravel\Scout\Searchable;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a ApprovalTemplate.
 *
 * Tenant-authored approval rules.
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
#[Table(name: ApprovalTemplateInterface::TABLE, keyType: ApprovalTemplateInterface::KEY_TYPE)]
#[Fillable([
    ApprovalTemplateInterface::ATTR_TENANT_ID,
        ApprovalTemplateInterface::ATTR_APPLICATION_ID,
        ApprovalTemplateInterface::ATTR_ACTION_KEY,
        ApprovalTemplateInterface::ATTR_NAME,
        ApprovalTemplateInterface::ATTR_DESCRIPTION,
        ApprovalTemplateInterface::ATTR_WHEN_EXPRESSION,
        ApprovalTemplateInterface::ATTR_PRIORITY,
        ApprovalTemplateInterface::ATTR_EXPIRES_AFTER_HOURS,
        ApprovalTemplateInterface::ATTR_ON_TIMEOUT,
        ApprovalTemplateInterface::ATTR_REMINDER_CONFIG,
        ApprovalTemplateInterface::ATTR_ALLOWS_RECALL,
        ApprovalTemplateInterface::ATTR_VERSION,
        ApprovalTemplateInterface::ATTR_IS_ACTIVE,
        ApprovalTemplateInterface::ATTR_METADATA,
])]
#[UseFactory(ApprovalTemplateFactory::class)]
final class ApprovalTemplate extends Model implements ApprovalTemplateInterface
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    use HasMetadata;
    use HasUserstamps;
    use HasActivityLog;
    use Auditable;
    use Filterable;
    use Searchable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        ApprovalTemplateInterface::ATTR_PRIORITY => 'integer',
        ApprovalTemplateInterface::ATTR_EXPIRES_AFTER_HOURS => 'integer',
        ApprovalTemplateInterface::ATTR_ON_TIMEOUT => 'OnTimeoutBehaviour',
        ApprovalTemplateInterface::ATTR_REMINDER_CONFIG => 'array',
        ApprovalTemplateInterface::ATTR_ALLOWS_RECALL => 'boolean',
        ApprovalTemplateInterface::ATTR_VERSION => 'integer',
        ApprovalTemplateInterface::ATTR_IS_ACTIVE => 'boolean',
        ApprovalTemplateInterface::ATTR_METADATA => 'array',
    ];
}
