<?php

/**
 * @file database/migrations/2026_07_15_120003_create_approval_reminders_table.php
 *
 * @description
 * Create the `approval_reminders` table for the `ApprovalReminder` entity.
 * Owned by the approvals module. Emitted from the module blueprint —
 * regenerate via `generate-module.py workflow approvals --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('approval_reminders', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('approval_requirement_id', 64);
            $table->foreign('approval_requirement_id')->references('id')->on('approval_requirements')->onDelete('cascade');
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('approver_id', 64);
            $table->foreign('approver_id')->references('id')->on('users')->onDelete('cascade');
            $table->integer('reminder_number');
            $table->timestampTz('sent_at');
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['approval_requirement_id', 'approver_id', 'reminder_number'], 'approval_reminders_composite_unique');
            $table->index(['approver_id', 'sent_at'], 'approval_reminders_by_approver_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('approval_reminders');
    }
};
