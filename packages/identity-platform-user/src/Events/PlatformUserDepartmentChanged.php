<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity platform-user --force`.

declare(strict_types=1);

namespace Academorix\Identity\PlatformUser\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PlatformUser.department mutated. Emitted separately from the generic profile update so downstream services with per-department routing (Slack channel selection, on-call rotation membership) can react cleanly.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRow
 * - observability::EmitPlatformUserDepartmentChangedMetric
 * - analytics::TrackPlatformUserDepartmentChanged
 *
 * @category PlatformUser
 *
 * @since    0.1.0
 */
final readonly class PlatformUserDepartmentChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $platformUserId  ULID
     * @param  string  $previousDepartment  string (enum value)
     * @param  string  $newDepartment  string (enum value)
     * @param  string  $changedAt  ISO-8601 timestamp
     * @param  string  $actorId  ULID
     */
    public function __construct(
        public readonly string $platformUserId,
        public readonly string $previousDepartment,
        public readonly string $newDepartment,
        public readonly string $changedAt,
        public readonly string $actorId,
    ) {
    }
}
