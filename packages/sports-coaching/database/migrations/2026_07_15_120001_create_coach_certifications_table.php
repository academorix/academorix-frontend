<?php

/**
 * @file database/migrations/2026_07_15_120001_create_coach_certifications_table.php
 *
 * @description
 * Create the `coach_certifications` table for the `CoachCertification` entity.
 * Owned by the coaching module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports coaching --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('coach_certifications', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('coaching_profile_id', 30);
            $table->foreign('coaching_profile_id')->references('id')->on('coaching_profiles')->onDelete('cascade');
            $table->string('issuing_body', 256);
            $table->string('certification_name', 256);
            $table->string('level', 32)->nullable();
            $table->date('issued_at');
            $table->date('expires_at')->nullable();
            $table->string('verification_url', 512)->nullable();
            $table->string('document_url', 512)->nullable();
            $table->string('verified_by_admin_user_id', 30)->nullable();
            $table->foreign('verified_by_admin_user_id')->references('id')->on('users')->onDelete('set null');
            $table->timestampTz('verified_at')->nullable();
            $table->text('verification_notes')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'coaching_profile_id', 'issuing_body', 'certification_name', 'issued_at'], 'coach_certifications_body_name_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'expires_at'], 'coach_certifications_expires_idx');
            $table->index(['tenant_id', 'coaching_profile_id', 'verified_at'], 'coach_certifications_profile_verified_idx');
            $table->index(['tenant_id', 'coaching_profile_id', 'issuing_body'], 'coach_certifications_body_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('coach_certifications');
    }
};
