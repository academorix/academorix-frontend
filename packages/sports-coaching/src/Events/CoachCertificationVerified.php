<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Sports\Coaching\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Admin invoked /verify on the certification. verified_by_admin_user_id + verified_at set.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
final readonly class CoachCertificationVerified implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $certificationId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $coachingProfileId  ULID
     * @param  string  $verifiedByAdminUserId  ULID
     * @param  string  $verifiedAt  ISO 8601
     */
    public function __construct(
        public readonly string $certificationId,
        public readonly string $tenantId,
        public readonly string $coachingProfileId,
        public readonly string $verifiedByAdminUserId,
        public readonly string $verifiedAt,
    ) {
    }
}
