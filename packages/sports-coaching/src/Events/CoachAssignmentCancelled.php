<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Sports\Coaching\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Assignment deleted before end_date OR cascade-cancelled by CoachingProfileDeactivated / TeamArchived / EventArchived.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchCoachAssignmentCancelledNotification (to coach)
 * - cache::InvalidateCoachAssignmentsCache
 * - cache::InvalidateAssignableAssignmentsCache
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
final readonly class CoachAssignmentCancelled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $assignmentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $coachingProfileId  ULID
     * @param  string  $trigger  'manual' | 'coaching_profile_deactivated' | 'team_archived' | 'event_archived'
     * @param  string  $cancelledByUserId  ULID | null
     */
    public function __construct(
        public readonly string $assignmentId,
        public readonly string $tenantId,
        public readonly string $coachingProfileId,
        public readonly string $trigger,
        public readonly string $cancelledByUserId,
    ) {
    }
}
