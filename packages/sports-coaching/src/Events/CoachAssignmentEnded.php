<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Sports\Coaching\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * end_date reached (nightly job flips) OR admin invoked /end. Terminal but non-purging.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateCoachAssignmentsCache
 * - cache::InvalidateAssignableAssignmentsCache
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
final readonly class CoachAssignmentEnded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $assignmentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $coachingProfileId  ULID
     * @param  string  $assignableType  string
     * @param  string  $assignableId  ULID
     * @param  string  $role  string
     * @param  int  $durationDays  integer
     * @param  bool  $reachedScheduledEnd  boolean
     * @param  string  $endedByUserId  ULID | null (null when nightly job)
     */
    public function __construct(
        public readonly string $assignmentId,
        public readonly string $tenantId,
        public readonly string $coachingProfileId,
        public readonly string $assignableType,
        public readonly string $assignableId,
        public readonly string $role,
        public readonly int $durationDays,
        public readonly bool $reachedScheduledEnd,
        public readonly string $endedByUserId,
    ) {
    }
}
