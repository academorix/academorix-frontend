<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Sports\Coaching\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * NotifyCertificationExpiringJob detected certification within cadence window (90 / 30 / 7 days before expires_at). Fires ONCE PER CADENCE per certification.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchCoachCertificationExpiringSoonNotification (to coach + admin — cannot-opt-out)
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
final readonly class CoachCertificationExpiringSoon implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $certificationId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $coachingProfileId  ULID
     * @param  int  $cadenceDays  integer (90 | 30 | 7)
     * @param  string  $issuingBody  string
     * @param  string  $certificationName  string
     * @param  string  $expiresAt  ISO 8601 date
     */
    public function __construct(
        public readonly string $certificationId,
        public readonly string $tenantId,
        public readonly string $coachingProfileId,
        public readonly int $cadenceDays,
        public readonly string $issuingBody,
        public readonly string $certificationName,
        public readonly string $expiresAt,
    ) {
    }
}
