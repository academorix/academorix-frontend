<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Sports\Coaching\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A CoachSkillRating row committed (fresh rating on a previously-unrated ratable).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchCoachSkillRatingRecordedNotification (to coach — opt-in only, per notify_on_rating preference)
 * - cache::InvalidateCoachRatingsCache
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
final readonly class CoachSkillRatingRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $ratingId  ULID (csr_*)
     * @param  string  $tenantId  ULID
     * @param  string  $coachingProfileId  ULID
     * @param  string  $ratableType  'Sport' | 'Discipline' | 'Position'
     * @param  string  $ratableId  ULID (spt_* | dsc_* | pos_*)
     * @param  string  $ratingScale  'stars_1_5' | 'level_5_stage'
     * @param  int  $ratingValue  integer 1..5
     * @param  string  $ratedByUserId  ULID
     * @param  string  $ratedAt  ISO 8601
     */
    public function __construct(
        public readonly string $ratingId,
        public readonly string $tenantId,
        public readonly string $coachingProfileId,
        public readonly string $ratableType,
        public readonly string $ratableId,
        public readonly string $ratingScale,
        public readonly int $ratingValue,
        public readonly string $ratedByUserId,
        public readonly string $ratedAt,
    ) {
    }
}
