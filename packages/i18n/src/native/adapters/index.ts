/**
 * @file index.ts
 * @module @stackra/i18n/native/adapters
 * @description Barrel export for native-only adapters. Locale storage
 *   is handled by `StorageBackedLocaleAdapter` in `../core/adapters` —
 *   one class for both platforms.
 */

export { NativeDirectionAdapter } from './native-direction.adapter';
