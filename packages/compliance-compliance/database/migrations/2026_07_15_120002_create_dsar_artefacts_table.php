<?php

/**
 * @file database/migrations/2026_07_15_120002_create_dsar_artefacts_table.php
 *
 * @description
 * Create the `dsar_artefacts` table for the `DsarArtefact` entity.
 * Owned by the compliance module. Emitted from the module blueprint —
 * regenerate via `generate-module.py compliance compliance --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py compliance compliance --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('dsar_artefacts', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('dsar_id', 30);
            $table->foreign('dsar_id')->references('id')->on('dsars')->onDelete('cascade');
            $table->string('contributor_module', 64);
            $table->string('contributor_class', 128);
            $table->string('subject_column', 64);
            $table->integer('row_count')->nullable();
            $table->jsonb('redacted_columns')->default('\'[]\'::jsonb');
            $table->string('format', 16)->default('json');
            $table->string('file_id', 30)->nullable();
            $table->foreign('file_id')->references('id')->on('files')->onDelete('set null');
            $table->string('state', 32)->default('pending');
            $table->timestampTz('started_at')->nullable();
            $table->timestampTz('completed_at')->nullable();
            $table->text('error_message')->nullable();
            $table->integer('duration_ms')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['dsar_id', 'contributor_module'], 'dsar_artefacts_dsar_contributor_uidx');
            $table->index(['state', 'created_at'], 'dsar_artefacts_state_idx');
            $table->index(['file_id'], 'dsar_artefacts_file_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE file_id IS NOT NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('dsar_artefacts');
    }
};
