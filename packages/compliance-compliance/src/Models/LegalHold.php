<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py compliance compliance --force`.

declare(strict_types=1);

namespace Academorix\Compliance\Compliance\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Compliance\Compliance\Contracts\Data\LegalHoldInterface;
use Academorix\Compliance\Compliance\Database\Factories\LegalHoldFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Mattiverse\Userstamps\Traits\Userstamps;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a LegalHold.
 *
 * Freezes retention on a subject / tenant / case / class scope.
 *
 * @category Compliance
 *
 * @since    0.1.0
 */
#[Table(name: LegalHoldInterface::TABLE, keyType: LegalHoldInterface::KEY_TYPE)]
#[Fillable([
    LegalHoldInterface::ATTR_TENANT_ID,
        LegalHoldInterface::ATTR_SCOPE,
        LegalHoldInterface::ATTR_SUBJECT_TYPE,
        LegalHoldInterface::ATTR_SUBJECT_ID,
        LegalHoldInterface::ATTR_CASE_REF,
        LegalHoldInterface::ATTR_CLASS_TARGET,
        LegalHoldInterface::ATTR_REASON,
        LegalHoldInterface::ATTR_APPLIED_BY_USER_ID,
        LegalHoldInterface::ATTR_APPROVED_BY_USER_ID,
        LegalHoldInterface::ATTR_APPLIED_AT,
        LegalHoldInterface::ATTR_EXPIRES_AT,
        LegalHoldInterface::ATTR_RELEASED_AT,
        LegalHoldInterface::ATTR_RELEASED_BY_USER_ID,
        LegalHoldInterface::ATTR_RELEASED_REASON,
        LegalHoldInterface::ATTR_METADATA,
])]
#[UseFactory(LegalHoldFactory::class)]
final class LegalHold extends Model implements LegalHoldInterface
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    use HasMetadata;
    use HasUserstamps;
    // TODO(gen): resolve unknown trait `HasAuditable` — add its import + use line.
    use HasActivityLog;
    use Filterable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        LegalHoldInterface::ATTR_SCOPE => 'LegalHoldScope',
        LegalHoldInterface::ATTR_APPLIED_AT => 'datetime',
        LegalHoldInterface::ATTR_EXPIRES_AT => 'datetime',
        LegalHoldInterface::ATTR_RELEASED_AT => 'datetime',
        LegalHoldInterface::ATTR_METADATA => 'array',
    ];
}
