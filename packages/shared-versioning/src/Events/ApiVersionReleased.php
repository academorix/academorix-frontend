<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared versioning --force`.

declare(strict_types=1);

namespace Academorix\Shared\Versioning\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ApiVersion state transitions draft → released. Fires once per version, ever.
 *
 * ## Consumers
 *
 * - notifications::TrackApiVersionReleasedListener
 * - compliance::AuditApiVersionReleasedListener
 *
 * @category Versioning
 *
 * @since    0.1.0
 */
final readonly class ApiVersionReleased implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $apiVersionId  ULID
     * @param  string  $slug  string
     * @param  string  $name  string
     * @param  string  $scheme  'semver' | 'calver'
     * @param  string  $releasedAt  ISO 8601
     * @param  string  $actorId  ULID | null
     */
    public function __construct(
        public readonly string $apiVersionId,
        public readonly string $slug,
        public readonly string $name,
        public readonly string $scheme,
        public readonly string $releasedAt,
        public readonly string $actorId,
    ) {
    }
}
