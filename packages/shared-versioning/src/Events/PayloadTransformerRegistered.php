<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared versioning --force`.

declare(strict_types=1);

namespace Academorix\Shared\Versioning\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Build-time compiler discovered a #[AsPayloadTransformer] class + registered it with PayloadTransformerRegistry. Fired at boot for observability, not for consumers to act on.
 *
 * ## Consumers
 *
 * - observability::MetricsCollector
 *
 * @category Versioning
 *
 * @since    0.1.0
 */
final readonly class PayloadTransformerRegistered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $transformerClass  string
     * @param  string  $surface  'rest' | 'webhook' | 'graphql'
     * @param  string  $event  string — event name or wildcard '*'
     * @param  string  $fromVersion  string
     * @param  string  $toVersion  string
     */
    public function __construct(
        public readonly string $transformerClass,
        public readonly string $surface,
        public readonly string $event,
        public readonly string $fromVersion,
        public readonly string $toVersion,
    ) {
    }
}
