<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared versioning --force`.

declare(strict_types=1);

namespace Academorix\Shared\Versioning\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ApiVersion state transitions deprecated → sunset. Fired by SunsetApiVersionJob when now() > sunset_at, or manually via endpoint.
 *
 * ## Consumers
 *
 * - webhook::AutoUpgradePinnedSubscriptions
 * - notifications::PublishSunsetFinalNoticesListener
 * - compliance::AuditApiVersionSunsetListener
 *
 * @category Versioning
 *
 * @since    0.1.0
 */
final readonly class ApiVersionSunset implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $apiVersionId  ULID
     * @param  string  $slug  string
     * @param  string  $sunsetAt  ISO 8601
     * @param  string  $migrationTargetSlug  string | null
     * @param  bool  $autoUpgradePerformed  boolean — true when migration_target_slug set + upgrades executed
     * @param  int  $affectedSubscriptionCount  integer
     * @param  string  $actorId  ULID | null | 'system' when scheduled
     */
    public function __construct(
        public readonly string $apiVersionId,
        public readonly string $slug,
        public readonly string $sunsetAt,
        public readonly string $migrationTargetSlug,
        public readonly bool $autoUpgradePerformed,
        public readonly int $affectedSubscriptionCount,
        public readonly string $actorId,
    ) {
    }
}
