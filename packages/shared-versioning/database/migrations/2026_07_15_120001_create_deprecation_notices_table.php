<?php

/**
 * @file database/migrations/2026_07_15_120001_create_deprecation_notices_table.php
 *
 * @description
 * Create the `deprecation_notices` table for the `DeprecationNotice` entity.
 * Owned by the versioning module. Emitted from the module blueprint —
 * regenerate via `generate-module.py shared versioning --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared versioning --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('deprecation_notices', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('api_version_id', 64);
            $table->foreign('api_version_id')->references('id')->on('api_versions')->onDelete('cascade');
            $table->string('notice_type', 24);
            $table->string('title', 200);
            $table->text('notice_markdown');
            $table->jsonb('affected_endpoints')->default('[]');
            $table->jsonb('affected_events')->default('[]');
            $table->string('migration_guide_url', 500)->nullable();
            $table->string('state', 16)->default('draft');
            $table->timestampTz('published_at')->nullable();
            $table->boolean('notify_customers')->default(true);
            $table->timestampTz('notified_at')->nullable();
            $table->boolean('is_system')->default(true);
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->index(['api_version_id', 'state', 'published_at'], 'deprecation_notices_version_state_index');
            $table->index(['notice_type', 'state'], 'deprecation_notices_type_state_index');
            $table->index(['published_at'], 'deprecation_notices_published_at_index');
            // Partial-index predicate — hand-add via DB::statement: WHERE state = 'published'
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('deprecation_notices');
    }
};
