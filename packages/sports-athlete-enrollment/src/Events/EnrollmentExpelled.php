<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Academorix\Sports\AthleteEnrollment\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Status transitioned active → expelled. Terminal (disciplinary termination). Audit-critical.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchEnrollmentExpelledNotification (to primary guardian + admin + safeguarding_officer — cannot-opt-out)
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
final readonly class EnrollmentExpelled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $enrollmentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $teamId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $expulsionReason  string
     * @param  string  $expelledAt  ISO 8601
     * @param  string  $expelledByUserId  ULID
     */
    public function __construct(
        public readonly string $enrollmentId,
        public readonly string $tenantId,
        public readonly string $teamId,
        public readonly string $athleteId,
        public readonly string $expulsionReason,
        public readonly string $expelledAt,
        public readonly string $expelledByUserId,
    ) {
    }
}
