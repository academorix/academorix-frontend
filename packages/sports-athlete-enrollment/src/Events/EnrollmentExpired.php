<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Academorix\Sports\AthleteEnrollment\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ExpireExpiredProvisionalEnrollmentsJob detected an enrollment past registration_closes_at + expiry_ttl_days without reaching confirmed. Fires per expired enrollment. Releases 1 unit of athlete_enrollment_slot.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchEnrollmentExpiredNotification (to primary guardian + admin)
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
final readonly class EnrollmentExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $enrollmentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $expiredAt  ISO 8601
     * @param  string  $priorStatus  string
     * @param  int  $daysSinceSubmission  integer
     */
    public function __construct(
        public readonly string $enrollmentId,
        public readonly string $tenantId,
        public readonly string $expiredAt,
        public readonly string $priorStatus,
        public readonly int $daysSinceSubmission,
    ) {
    }
}
