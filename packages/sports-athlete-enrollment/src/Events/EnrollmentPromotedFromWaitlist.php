<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Academorix\Sports\AthleteEnrollment\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PromoteWaitlistedEnrollmentsJob picked the earliest-waitlisted enrollment when team capacity opened. Status transitions waitlisted → submitted (or directly to confirmed if all other preconditions already met).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchEnrollmentPromotedFromWaitlistNotification (cannot-opt-out — the guardian wants to know)
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
final readonly class EnrollmentPromotedFromWaitlist implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $enrollmentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $teamId  ULID
     * @param  int  $wasWaitlistPosition  integer
     * @param  int  $daysOnWaitlist  integer
     * @param  string  $newStatus  string
     */
    public function __construct(
        public readonly string $enrollmentId,
        public readonly string $tenantId,
        public readonly string $teamId,
        public readonly int $wasWaitlistPosition,
        public readonly int $daysOnWaitlist,
        public readonly string $newStatus,
    ) {
    }
}
