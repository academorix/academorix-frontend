<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Sports\Athlete\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Archived athlete restored (deleted_at cleared, status → active). Consumes 1 unit of athlete_slot. Only within retention window.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
final readonly class AthleteRestored implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $athleteId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $restoredByUserId  ULID
     * @param  string  $wasArchivedAt  ISO 8601
     */
    public function __construct(
        public readonly string $athleteId,
        public readonly string $tenantId,
        public readonly string $restoredByUserId,
        public readonly string $wasArchivedAt,
    ) {
    }
}
