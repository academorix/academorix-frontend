<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Platform\Staff\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Staff.employment_status transitioned offboarded → active (rehire). RARE — most rehires happen as new Staff rows; the reactivation path preserves the original employment history for tax / seniority calculations. Observer consumes 1 unit of staff_slot entitlement + resets ended_at to null.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchStaffWelcomeNotification (as re-onboarding)
 *
 * @category Staff
 *
 * @since    0.1.0
 */
final readonly class StaffReactivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $staffId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $reactivatedAt  ISO 8601
     * @param  string  $reactivatedByUserId  ULID
     * @param  string  $wasOffboardedAt  YYYY-MM-DD
     * @param  int  $daysSinceOffboarding  integer
     */
    public function __construct(
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $reactivatedAt,
        public readonly string $reactivatedByUserId,
        public readonly string $wasOffboardedAt,
        public readonly int $daysSinceOffboarding,
    ) {
    }
}
