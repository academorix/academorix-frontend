<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Platform\Staff\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * StaffObserver.updating detected employment_status transition `pre_hire → active`. Also fires when a Staff is created directly at `active` status (the same-tenant admin-driven hire path). Fires exactly once per Staff row.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchStaffWelcomeNotification (cannot-opt-out to the new hire's User)
 * - cache::InvalidateBranchStaffListCache
 * - cache::InvalidateBranchManagerCache (if is_branch_manager=true on hire)
 *
 * @category Staff
 *
 * @since    0.1.0
 */
final readonly class StaffHired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $staffId  ULID (stf_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $branchId  ULID (brn_*)
     * @param  string  $userId  ULID (usr_*)
     * @param  int  $employmentType  string (full_time / part_time / contract / volunteer / intern / seasonal)
     * @param  string  $position  string
     * @param  string  $department  string | null
     * @param  string  $startedAt  YYYY-MM-DD
     * @param  string  $probationEndsAt  YYYY-MM-DD | null
     * @param  string  $hiredByUserId  ULID (usr_*)
     */
    public function __construct(
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $userId,
        public readonly int $employmentType,
        public readonly string $position,
        public readonly string $department,
        public readonly string $startedAt,
        public readonly string $probationEndsAt,
        public readonly string $hiredByUserId,
    ) {
    }
}
