<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Platform\Staff\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A new entry was appended to Coach.certifications. Fires per certification added — batch updates emit N events. When coach_certifications_verified entitlement is on (Enterprise), the certification enters pending-verification state and HR gets a review notification.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchCertificationAddedNotification (to HR when coach_certifications_verified is on)
 *
 * @category Staff
 *
 * @since    0.1.0
 */
final readonly class CoachCertificationAdded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $coachId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $certification  {'organization': 'string', 'certification': 'string', 'certified_at': 'YYYY-MM-DD', 'expires_at': 'YYYY-MM-DD | null', 'certificate_url': 'string | null'}
     * @param  string  $addedByUserId  ULID
     * @param  bool  $requiresVerification  boolean
     */
    public function __construct(
        public readonly string $coachId,
        public readonly string $tenantId,
        public readonly string $certification,
        public readonly string $addedByUserId,
        public readonly bool $requiresVerification,
    ) {
    }
}
