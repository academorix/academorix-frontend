/**
 * @file eslint.config.mjs
 * @description ESLint flat config for @academorix/realtime. Ships React
 * hooks so we extend the workspace React base config.
 */

import reactConfig from "@academorix/eslint-config/react";

export default [...reactConfig];
