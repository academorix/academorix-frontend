<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Finance\Payment\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Manual capture flow: capture_method='manual' with partial amount. amount_cents < intent.amount_cents. The invoice becomes 'partially_paid' rather than 'paid'.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - invoice::MarkInvoicePartiallyPaid
 * - monitoring::MetricsCollector
 *
 * @category Payment
 *
 * @since    0.1.0
 */
final readonly class PaymentPartiallyCaptured implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $paymentId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $amountCents  integer
     * @param  int  $intentAmountCents  integer
     * @param  string  $currency  string
     * @param  string  $capturedAt  ISO 8601
     */
    public function __construct(
        public readonly string $paymentId,
        public readonly string $tenantId,
        public readonly int $amountCents,
        public readonly int $intentAmountCents,
        public readonly string $currency,
        public readonly string $capturedAt,
    ) {
    }
}
