<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Finance\Payment\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PaymentMethodObserver detected a duplicate via fingerprint_hash. Existing method re-activated (if soft-deleted or is_active=false); new insert refused.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector
 *
 * @category Payment
 *
 * @since    0.1.0
 */
final readonly class PaymentMethodDuplicateDetected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $customerType  string
     * @param  string  $customerId  ULID
     * @param  string  $existingPaymentMethodId  ULID
     * @param  string  $provider  string
     * @param  string  $fingerprintHash  string (SHA-256)
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $customerType,
        public readonly string $customerId,
        public readonly string $existingPaymentMethodId,
        public readonly string $provider,
        public readonly string $fingerprintHash,
        public readonly string $at,
    ) {
    }
}
