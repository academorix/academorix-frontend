<?php

/**
 * @file database/migrations/2026_07_15_120000_create_mfa_challenges_table.php
 *
 * @description
 * Create the `mfa_challenges` table for the `MfaChallenge` entity.
 * Owned by the mfa module. Emitted from the module blueprint —
 * regenerate via `generate-module.py identity mfa --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity mfa --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('mfa_challenges', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('identity_id', 64);
            $table->foreign('identity_id')->references('id')->on('identities')->onDelete('cascade');
            $table->string('method', 24);
            $table->string('state', 24)->default('pending');
            $table->string('webauthn_challenge')->nullable();
            $table->string('webauthn_credential_id_hint', 64)->nullable();
            $table->foreign('webauthn_credential_id_hint')->references('id')->on('webauthn_credentials')->onDelete('set null');
            $table->integer('attempts_remaining');
            $table->string('request_ip')->nullable();
            $table->string('user_agent', 255)->nullable();
            $table->timestampTz('issued_at');
            $table->timestampTz('expires_at');
            $table->timestampTz('consumed_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->index(['identity_id', 'state'], 'auth_mfa_challenges_identity_state_idx');
            $table->index(['expires_at'], 'auth_mfa_challenges_expiry_scan_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE state IN ('pending', 'expired')
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('mfa_challenges');
    }
};
