<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity mfa --force`.

declare(strict_types=1);

namespace Academorix\Identity\Mfa\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /api/v1/me/mfa/totp/enroll succeeded — TOTP secret persisted to Identity.mfa_secret_encrypted via identity/identity's encryptor. Recovery codes generated + hashed atomically. Fires identity::MfaEnrolled downstream via the identity module's observer.
 *
 * ## Consumers
 *
 * - notifications::DispatchMfaEnrolledConfirmationNotification
 * - activity::WriteToActivityLog
 * - observability::EmitMfaEnrollmentCompletedMetric
 * - analytics::TrackMfaEnrolled
 *
 * @category Mfa
 *
 * @since    0.1.0
 */
final readonly class MfaEnrollmentConfirmed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $method  'totp'
     * @param  string  $confirmedAt  ISO-8601 timestamp
     * @param  int  $recoveryCodesCount  integer (10)
     * @param  int  $elapsedFromPreviewMs  integer (duration from MfaEnrollmentPreviewIssued to confirm — signal for suspicious rushed enrolments)
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $method,
        public readonly string $confirmedAt,
        public readonly int $recoveryCodesCount,
        public readonly int $elapsedFromPreviewMs,
    ) {
    }
}
