<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity mfa --force`.

declare(strict_types=1);

namespace Academorix\Identity\Mfa\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * identity/auth::LoginAction issued a challenge because the resolved Identity has MFA enrolled. A row was written to the shared auth_mfa_challenges table with a 5-minute TTL + 3-attempt cap.
 *
 * ## Consumers
 *
 * - observability::EmitMfaChallengeIssuedMetric
 *
 * @category Mfa
 *
 * @since    0.1.0
 */
final readonly class MfaChallengeIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $challengeId  ULID (`mfc_...`)
     * @param  string  $method  'totp' | 'webauthn' | 'recovery_code'
     * @param  string  $issuedAt  ISO-8601 timestamp
     * @param  string  $expiresAt  ISO-8601 timestamp
     * @param  string  $requestIp  string (IPv4 / IPv6)
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $challengeId,
        public readonly string $method,
        public readonly string $issuedAt,
        public readonly string $expiresAt,
        public readonly string $requestIp,
    ) {
    }
}
