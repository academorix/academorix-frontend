<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Observability\Monitoring\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MonitoringProviderConfigObserver.updated detected is_active → false OR row soft-deleted.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
final readonly class MonitoringProviderDeactivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $providerConfigId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $deactivatedByUserId  ULID | null
     * @param  string  $deactivatedAt  ISO 8601
     * @param  string  $reason  'manual' | 'tenant_suspended' | 'entitlement_revoked'
     */
    public function __construct(
        public readonly string $providerConfigId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $deactivatedByUserId,
        public readonly string $deactivatedAt,
        public readonly string $reason,
    ) {
    }
}
