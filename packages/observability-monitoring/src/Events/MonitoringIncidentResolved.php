<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Observability\Monitoring\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MonitoringIncidentObserver.updated detected status transition to 'resolved'. Either every alert in the incident resolved (auto) or ops manually resolved.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - monitoring::MaybeSchedulePostmortem (when severity=p1: fire MonitoringPostmortemRequiredNotification + transition status to 'postmortem' if not already)
 * - monitoring::MetricsCollector (increments academorix.monitoring.incidents.resolved_total)
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
final readonly class MonitoringIncidentResolved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $monitoringIncidentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $resolvedAt  ISO 8601
     * @param  string  $resolvedBy  'auto' | 'manual'
     * @param  string  $resolvedByUserId  ULID | null
     * @param  int  $timeToResolveSeconds  integer
     * @param  string  $severity  'p1' | 'p2' | 'p3' | 'p4'
     */
    public function __construct(
        public readonly string $monitoringIncidentId,
        public readonly string $tenantId,
        public readonly string $resolvedAt,
        public readonly string $resolvedBy,
        public readonly string $resolvedByUserId,
        public readonly int $timeToResolveSeconds,
        public readonly string $severity,
    ) {
    }
}
