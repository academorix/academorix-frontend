<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Observability\Monitoring\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * HealthCheckObserver.created committed a new health_checks row.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — infrastructure config is compliance-relevant)
 * - monitoring::ScheduleFirstRun (dispatches RunHealthCheckJob delayed by interval_seconds/2 so the check doesn't fire at boot burst)
 * - monitoring::MetricsCollector (increments academorix.monitoring.health_checks.created_total)
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
final readonly class HealthCheckCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $healthCheckId  ULID (hck_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $name  string
     * @param  string  $checkType  string (10 enum values)
     * @param  int  $intervalSeconds  integer
     * @param  bool  $isEnabled  boolean
     * @param  string  $createdByUserId  ULID
     * @param  string  $createdAt  ISO 8601
     */
    public function __construct(
        public readonly string $healthCheckId,
        public readonly string $tenantId,
        public readonly string $name,
        public readonly string $checkType,
        public readonly int $intervalSeconds,
        public readonly bool $isEnabled,
        public readonly string $createdByUserId,
        public readonly string $createdAt,
    ) {
    }
}
