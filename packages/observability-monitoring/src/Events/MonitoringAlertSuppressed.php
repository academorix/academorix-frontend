<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Observability\Monitoring\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MonitoringAlertObserver.created inserted a row with status='suppressed'. The threshold breached but the policy's suppression_config matched (after-hours, weekend, silenced_until).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (compliance record — 'we saw this signal but chose not to page')
 * - monitoring::MetricsCollector (increments academorix.monitoring.alerts.suppressed_total{reason=...})
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
final readonly class MonitoringAlertSuppressed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $monitoringAlertId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $monitoringAlertPolicyId  ULID
     * @param  string  $suppressionReason  'after_hours' | 'weekend' | 'silenced'
     * @param  string  $signalValue  number
     * @param  string  $suppressedAt  ISO 8601
     */
    public function __construct(
        public readonly string $monitoringAlertId,
        public readonly string $tenantId,
        public readonly string $monitoringAlertPolicyId,
        public readonly string $suppressionReason,
        public readonly string $signalValue,
        public readonly string $suppressedAt,
    ) {
    }
}
