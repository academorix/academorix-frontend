<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Observability\Monitoring\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CRITICAL. MonitoringIncidentObserver.created inserted a new monitoring_incidents row with status='open'. GroupAlertsIntoIncidentJob determined the fired alert did not attach to any existing incident + opened a new one.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — every incident is compliance-relevant)
 * - notifications::DispatchMonitoringIncidentOpenedNotification (P1: page on-call via oncall_page_config; P2+: notify primary; P3+: dashboard-only)
 * - monitoring::NotifyProviders (dispatches NotifyProvidersJob for the incident's oncall_page_config)
 * - monitoring::ScheduleEscalation (dispatches EscalateIncidentJob delayed by the severity's SLA)
 * - monitoring::MetricsCollector (increments academorix.monitoring.incidents.opened_total{severity=...})
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
final readonly class MonitoringIncidentOpened implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $monitoringIncidentId  ULID (mci_*)
     * @param  string  $tenantId  ULID
     * @param  string  $title  string
     * @param  string  $severity  'p1' | 'p2' | 'p3' | 'p4'
     * @param  string  $openedAt  ISO 8601
     * @param  string  $affectedServices  string[]
     * @param  string  $initialAlertId  ULID (the alert that opened the incident)
     */
    public function __construct(
        public readonly string $monitoringIncidentId,
        public readonly string $tenantId,
        public readonly string $title,
        public readonly string $severity,
        public readonly string $openedAt,
        public readonly string $affectedServices,
        public readonly string $initialAlertId,
    ) {
    }
}
