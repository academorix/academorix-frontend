<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Academorix\Growth\Analytics\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CircuitBreakerFailureListener transitioned state from closed → open.
 *
 * ## Consumers
 *
 * - notifications::DispatchCircuitBreakerNotificationListener — fires AnalyticsProviderCircuitBreakerOpenedNotification to tenant admins + platform-admin on-call
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
final readonly class AnalyticsProviderCircuitBreakerOpened implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $providerConfigId  ULID (apc_)
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  int  $failureCount  int
     * @param  float  $failureRate  float
     * @param  string  $openedAt  ISO 8601
     */
    public function __construct(
        public readonly string $providerConfigId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly int $failureCount,
        public readonly float $failureRate,
        public readonly string $openedAt,
    ) {
    }
}
