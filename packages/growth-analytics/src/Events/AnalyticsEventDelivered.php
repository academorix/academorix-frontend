<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Academorix\Growth\Analytics\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * delivered_provider_count reached eligible_provider_count — all providers delivered successfully.
 *
 * ## Consumers
 *
 * - webhook::PublishWireEventListener — emits analytics.event.delivered/v1 to external consumers
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
final readonly class AnalyticsEventDelivered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $analyticsEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $eventName  string
     * @param  int  $deliveredProviderCount  int
     * @param  int  $eligibleProviderCount  int
     * @param  string  $deliveredAt  ISO 8601
     */
    public function __construct(
        public readonly string $analyticsEventId,
        public readonly string $tenantId,
        public readonly string $eventName,
        public readonly int $deliveredProviderCount,
        public readonly int $eligibleProviderCount,
        public readonly string $deliveredAt,
    ) {
    }
}
