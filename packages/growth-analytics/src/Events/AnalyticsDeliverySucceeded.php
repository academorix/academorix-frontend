<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Academorix\Growth\Analytics\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * HTTP call returned 2xx (or provider-specific success shape).
 *
 * ## Consumers
 *
 * - analytics::UpdateDeliveredCountListener — increments AnalyticsEvent.delivered_provider_count
 * - analytics::CircuitBreakerSuccessListener — records success into circuit_breaker_state
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
final readonly class AnalyticsDeliverySucceeded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $deliveryId  ULID
     * @param  string  $analyticsEventId  ULID
     * @param  string  $provider  string
     * @param  int  $latencyMs  int
     * @param  int  $responseHttpStatus  int
     */
    public function __construct(
        public readonly string $deliveryId,
        public readonly string $analyticsEventId,
        public readonly string $provider,
        public readonly int $latencyMs,
        public readonly int $responseHttpStatus,
    ) {
    }
}
