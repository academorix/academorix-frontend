<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Academorix\Growth\Analytics\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AnalyticsEvent with category='funnel_step' passed capture. Semantic mirror of AnalyticsEventCaptured for consumers that only care about funnel milestones.
 *
 * ## Consumers
 *
 * - marketing::CorrelateAttributionListener (Wave 5)
 * - referrals::CheckConversionListener (Wave 5)
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
final readonly class AnalyticsFunnelStepReached implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $analyticsEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $funnelKey  string (from properties.funnel_key)
     * @param  string  $stepKey  string (from properties.step_key)
     * @param  int  $stepIndex  int (from properties.step_index)
     */
    public function __construct(
        public readonly string $analyticsEventId,
        public readonly string $tenantId,
        public readonly string $funnelKey,
        public readonly string $stepKey,
        public readonly int $stepIndex,
    ) {
    }
}
