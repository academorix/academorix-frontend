<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Growth\Attribution\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Umbrella event for scalar-field mutations on the attributions row that are NOT covered by a more specific event (subject_id populate → AttributionSubjectIdentified; lifecycle_state → dedicated events; last_touch_* → AttributionLastTouchUpdated).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
final readonly class AttributionUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attributionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedFields  string[]
     */
    public function __construct(
        public readonly string $attributionId,
        public readonly string $tenantId,
        public readonly string $changedFields,
    ) {
    }
}
