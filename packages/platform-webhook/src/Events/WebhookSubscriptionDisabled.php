<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform webhook --force`.

declare(strict_types=1);

namespace Academorix\Platform\Webhook\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * state → disabled (auto: 410 Gone / consecutive_failure_threshold / api_version_sunset / tenant_suspended; or manual).
 *
 * ## Consumers
 *
 * - notifications::DispatchWebhookSubscriptionDisabledNotification
 * - compliance::AuditListener
 *
 * @category Webhook
 *
 * @since    0.1.0
 */
final readonly class WebhookSubscriptionDisabled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $subscriptionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $reason  'gone_410' | 'failure_threshold' | 'manual' | 'api_version_sunset' | 'tenant_suspended'
     * @param  string  $disabledAt  ISO 8601
     * @param  int  $consecutiveFailureCount  integer
     */
    public function __construct(
        public readonly string $subscriptionId,
        public readonly string $tenantId,
        public readonly string $reason,
        public readonly string $disabledAt,
        public readonly int $consecutiveFailureCount,
    ) {
    }
}
