<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform webhook --force`.

declare(strict_types=1);

namespace Academorix\Platform\Webhook\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Inbound receiver accepted a signature-verified request. Fanned out to downstream module listeners.
 *
 * ## Consumers
 *
 * - notifications-mail::IngestMailProviderWebhookListener
 * - notifications-push::IngestPushProviderWebhookListener
 * - notifications-sms::IngestSmsProviderWebhookListener
 * - subscription::IngestStripeWebhookListener
 * - any module with namespace-scoped ingestion
 *
 * @category Webhook
 *
 * @since    0.1.0
 */
final readonly class InboundWebhookReceived implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $receiptId  ULID
     * @param  string  $namespace  string — e.g. 'notifications-mail'
     * @param  string  $provider  string — e.g. 'mailgun'
     * @param  array  $headers  object — sanitised header set
     * @param  array  $payload  object — raw parsed body
     * @param  string  $requestId  string
     * @param  string  $receivedAt  ISO 8601
     */
    public function __construct(
        public readonly string $receiptId,
        public readonly string $namespace,
        public readonly string $provider,
        public readonly array $headers,
        public readonly array $payload,
        public readonly string $requestId,
        public readonly string $receivedAt,
    ) {
    }
}
