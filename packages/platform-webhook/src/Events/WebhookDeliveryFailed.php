<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform webhook --force`.

declare(strict_types=1);

namespace Academorix\Platform\Webhook\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Retryable failure (408/425/429/5xx or network error). state → failedRetryable + next_attempt_at scheduled.
 *
 * ## Consumers
 *
 * - observability::MetricsCollector
 *
 * @category Webhook
 *
 * @since    0.1.0
 */
final readonly class WebhookDeliveryFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $deliveryId  ULID
     * @param  string  $subscriptionId  ULID
     * @param  string  $eventName  string
     * @param  int  $attemptNumber  integer
     * @param  int  $responseStatus  integer | null
     * @param  string  $errorClass  string | null
     * @param  string  $nextAttemptAt  ISO 8601
     */
    public function __construct(
        public readonly string $deliveryId,
        public readonly string $subscriptionId,
        public readonly string $eventName,
        public readonly int $attemptNumber,
        public readonly int $responseStatus,
        public readonly string $errorClass,
        public readonly string $nextAttemptAt,
    ) {
    }
}
