<?php

/**
 * @file database/migrations/2026_07_15_120000_create_disciplines_table.php
 *
 * @description
 * Create the `disciplines` table for the `Discipline` entity.
 * Owned by the registry module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports registry --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('disciplines', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30)->nullable();
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('sport_id', 30);
            $table->foreign('sport_id')->references('id')->on('sports')->onDelete('restrict');
            $table->string('name', 64);
            $table->string('slug', 48);
            $table->text('description')->nullable();
            $table->string('format_type', 16)->nullable();
            $table->integer('player_count_min')->nullable();
            $table->integer('player_count_max')->nullable();
            $table->integer('session_duration_default_minutes')->nullable();
            $table->integer('sort_order')->default(100);
            $table->boolean('is_active')->default(true);
            $table->boolean('is_platform_seeded')->default(false);
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['sport_id', 'slug'], 'disciplines_sport_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'slug'], 'disciplines_tenant_slug_idx');
            $table->index(['sport_id', 'sort_order'], 'disciplines_picker_idx');
            $table->index(['is_platform_seeded', 'is_active'], 'disciplines_flags_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('disciplines');
    }
};
