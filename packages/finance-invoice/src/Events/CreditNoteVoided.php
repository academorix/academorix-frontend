<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Finance\Invoice\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Rare + audit-heavy. Draft credit notes may be voided by admin before issuance (typo/mistake). Issued/applied credit notes CANNOT be voided — they're immutable financial records.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — every void of a financial record is compliance-critical)
 * - activity::WriteToActivityLog
 * - monitoring::MetricsCollector
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
final readonly class CreditNoteVoided implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $creditNoteId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $voidReason  string
     * @param  string  $voidedByUserId  ULID
     * @param  string  $voidedAt  ISO 8601
     */
    public function __construct(
        public readonly string $creditNoteId,
        public readonly string $tenantId,
        public readonly string $voidReason,
        public readonly string $voidedByUserId,
        public readonly string $voidedAt,
    ) {
    }
}
