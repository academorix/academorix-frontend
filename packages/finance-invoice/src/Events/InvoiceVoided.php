<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Finance\Invoice\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * InvoiceObserver.updating detected status transition draft/open → void. REFUSED when status IN ('paid', 'partially_paid') — those must go through credit-note issuance. Requires void_reason + admin+finance role.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — compliance-relevant)
 * - activity::WriteToActivityLog
 * - monitoring::MetricsCollector
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
final readonly class InvoiceVoided implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $invoiceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceNumber  string
     * @param  string  $voidReason  string
     * @param  string  $voidedByUserId  ULID
     * @param  string  $voidedAt  ISO 8601
     */
    public function __construct(
        public readonly string $invoiceId,
        public readonly string $tenantId,
        public readonly string $invoiceNumber,
        public readonly string $voidReason,
        public readonly string $voidedByUserId,
        public readonly string $voidedAt,
    ) {
    }
}
