<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Finance\Invoice\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * InvoiceObserver.updating detected status transition draft → open. All lines have tax_calculation_id + totals frozen. Precedes InvoiceSent (dispatched separately by SendInvoiceEmailJob).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - monitoring::MetricsCollector (increments academorix.invoice.finalized_total)
 * - invoice::EnqueueGenerateInvoicePDFJob (generates PDF + uploads to S3)
 * - invoice::EnqueueSendInvoiceEmailJob (delivers to customer.email)
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
final readonly class InvoiceFinalized implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $invoiceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceNumber  string
     * @param  string  $customerType  string
     * @param  string  $customerId  ULID
     * @param  int  $lineCount  integer
     * @param  int  $subtotalCents  integer
     * @param  int  $discountAmountCents  integer
     * @param  int  $taxAmountCents  integer
     * @param  int  $totalCents  integer
     * @param  string  $currency  string
     * @param  string  $issueDate  ISO 8601 date
     * @param  string  $dueDate  ISO 8601 date
     * @param  string  $finalizedByUserId  ULID | null
     * @param  string  $finalizedAt  ISO 8601
     */
    public function __construct(
        public readonly string $invoiceId,
        public readonly string $tenantId,
        public readonly string $invoiceNumber,
        public readonly string $customerType,
        public readonly string $customerId,
        public readonly int $lineCount,
        public readonly int $subtotalCents,
        public readonly int $discountAmountCents,
        public readonly int $taxAmountCents,
        public readonly int $totalCents,
        public readonly string $currency,
        public readonly string $issueDate,
        public readonly string $dueDate,
        public readonly string $finalizedByUserId,
        public readonly string $finalizedAt,
    ) {
    }
}
