<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Finance\Invoice\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * InvoiceLineObserver.creating committed a new invoice_lines row. Only fires while parent invoice.status='draft' — post-finalize additions are refused.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - invoice::EnqueueCalculateInvoiceTaxJob (when tax_calculation_id null)
 * - monitoring::MetricsCollector
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
final readonly class InvoiceLineAdded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $invoiceLineId  ULID (ivl_*)
     * @param  string  $invoiceId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $lineNumber  integer
     * @param  string  $itemType  string (membership_charge / addon / setup_fee / late_fee / adjustment / one_time_service)
     * @param  float  $quantity  decimal
     * @param  int  $unitPriceCents  integer
     * @param  int  $totalCents  integer
     * @param  string  $currency  string
     * @param  string  $addedAt  ISO 8601
     */
    public function __construct(
        public readonly string $invoiceLineId,
        public readonly string $invoiceId,
        public readonly string $tenantId,
        public readonly int $lineNumber,
        public readonly string $itemType,
        public readonly float $quantity,
        public readonly int $unitPriceCents,
        public readonly int $totalCents,
        public readonly string $currency,
        public readonly string $addedAt,
    ) {
    }
}
