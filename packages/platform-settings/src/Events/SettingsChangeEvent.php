<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform settings --force`.

declare(strict_types=1);

namespace Academorix\Platform\Settings\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * One or more fields written via SettingsWriter. One event per changed field (batchable). Fires afterCommit on the SettingValue row.
 *
 * ## Consumers
 *
 * - activity::WriteSettingsChangeToActivity
 * - audit::WriteSettingsChangeToAudit
 * - modules that need to react to specific settings changes
 *
 * @category Settings
 *
 * @since    0.1.0
 */
final readonly class SettingsChangeEvent implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $group  string
     * @param  string  $fieldName  string
     * @param  string  $scopeType  'system' | 'tenant' | 'user'
     * @param  string  $scopeId  string | null
     * @param  string  $oldValue  mixed (masked if sensitive)
     * @param  string  $newValue  mixed (masked if sensitive)
     * @param  string  $actorId  ULID | null
     * @param  bool  $isSensitive  boolean
     * @param  string  $changedAt  ISO 8601
     */
    public function __construct(
        public readonly string $group,
        public readonly string $fieldName,
        public readonly string $scopeType,
        public readonly string $scopeId,
        public readonly string $oldValue,
        public readonly string $newValue,
        public readonly string $actorId,
        public readonly bool $isSensitive,
        public readonly string $changedAt,
    ) {
    }
}
