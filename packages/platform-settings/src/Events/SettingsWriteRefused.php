<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform settings --force`.

declare(strict_types=1);

namespace Academorix\Platform\Settings\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Write refused by validation, locked, or permission. Emitted so ops can spot misuse.
 *
 * ## Consumers
 *
 * - observability::MetricsCollector
 * - compliance::AuditListener
 *
 * @category Settings
 *
 * @since    0.1.0
 */
final readonly class SettingsWriteRefused implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $group  string
     * @param  string  $fieldName  string
     * @param  string  $scopeType  string
     * @param  string  $reason  'validation_failed' | 'locked' | 'permission_denied' | 'sensitive_reveal_denied'
     * @param  string  $actorId  ULID | null
     */
    public function __construct(
        public readonly string $group,
        public readonly string $fieldName,
        public readonly string $scopeType,
        public readonly string $reason,
        public readonly string $actorId,
    ) {
    }
}
