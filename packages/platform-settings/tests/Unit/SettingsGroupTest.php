<?php

declare(strict_types=1);

use Academorix\Platform\Settings\Models\SettingsGroup;

it('is a placeholder — replace with actual SettingsGroup unit tests', function (): void {
    expect(SettingsGroup::class)->toBeString();
});
