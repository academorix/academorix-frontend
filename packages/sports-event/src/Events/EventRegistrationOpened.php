<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Sports\Event\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Event.status transitioned to registration_open. Automated (RollEventStatusJob when now() reaches registration_opens_at) OR manual admin.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchEventRegistrationOpeningNotification
 *
 * @category Event
 *
 * @since    0.1.0
 */
final readonly class EventRegistrationOpened implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $openedAt  ISO 8601
     * @param  string  $registrationClosesAt  ISO 8601 | null
     * @param  string  $trigger  'automated_phase_roll' | 'manual_admin'
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $openedAt,
        public readonly string $registrationClosesAt,
        public readonly string $trigger,
    ) {
    }
}
