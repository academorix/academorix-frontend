<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Sports\Event\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Event.min_age, max_age, OR age_group_ids narrowed after registrations opened. AUDIT-CRITICAL when the narrowing excludes existing registrants — same refund-liability shape as EventPricingChanged.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years, critical severity)
 * - activity::WriteToActivityLog
 * - notifications::DispatchEventMinorEligibilityCorrectedNotification (cannot-opt-out to excluded registrants + guardians of excluded minors)
 *
 * @category Event
 *
 * @since    0.1.0
 */
final readonly class EventMinorEligibilityCorrected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedBy  ULID
     * @param  int  $before  {'min_age': 'integer | null', 'max_age': 'integer | null', 'age_group_ids': 'string[] | null'}
     * @param  int  $after  {'min_age': 'integer | null', 'max_age': 'integer | null', 'age_group_ids': 'string[] | null'}
     * @param  int  $excludedRegistrationCount  integer
     * @param  int  $excludedMinorCount  integer
     * @param  string  $auditSeverity  critical
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $changedBy,
        public readonly int $before,
        public readonly int $after,
        public readonly int $excludedRegistrationCount,
        public readonly int $excludedMinorCount,
        public readonly string $auditSeverity,
    ) {
    }
}
