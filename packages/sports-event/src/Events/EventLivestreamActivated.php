<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Sports\Event\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Event.livestream_url set for the first time OR livestream_password rotated. Fires the notification to registered participants (private-tier password delivery).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchEventLivestreamActivatedNotification (to registered participants — password delivery via secure email)
 *
 * @category Event
 *
 * @since    0.1.0
 */
final readonly class EventLivestreamActivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $activatedAt  ISO 8601
     * @param  bool  $isPasswordProtected  boolean
     * @param  string  $activatedBy  ULID
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $activatedAt,
        public readonly bool $isPasswordProtected,
        public readonly string $activatedBy,
    ) {
    }
}
