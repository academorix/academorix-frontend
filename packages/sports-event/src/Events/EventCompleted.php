<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Sports\Event\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Event.status transitioned in_progress -> completed. Fires from RollEventStatusJob at ends_at + 1h grace OR manual admin.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Event
 *
 * @since    0.1.0
 */
final readonly class EventCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $completedAt  ISO 8601
     * @param  int  $finalRegisteredCount  integer
     * @param  string  $trigger  'automated_phase_roll' | 'manual_admin'
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $completedAt,
        public readonly int $finalRegisteredCount,
        public readonly string $trigger,
    ) {
    }
}
