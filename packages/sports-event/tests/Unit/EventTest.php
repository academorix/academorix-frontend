<?php

declare(strict_types=1);

use Academorix\Sports\Event\Models\Event;

it('is a placeholder — replace with actual Event unit tests', function (): void {
    expect(Event::class)->toBeString();
});
