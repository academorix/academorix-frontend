<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports age-group --force`.

declare(strict_types=1);

namespace Academorix\Sports\AgeGroup\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AgeGroup soft-deleted (deleted_at set). Refused when is_default=true (observer catches it). Refused when is_seeded=true (seeded defaults cannot be deleted). Refused when any inbound row (Team, AthleteEnrollment via non-snapshot column, EventDivision) still references this AgeGroup (AGE_GROUP_IN_USE). Archived AgeGroups vanish from selectors but existing rows stay valid.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category AgeGroup
 *
 * @since    0.1.0
 */
final readonly class AgeGroupArchived implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $ageGroupId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $archivedBy  ULID
     * @param  string  $reason  string | null
     * @param  string  $hardDeleteScheduledFor  ISO 8601 timestamp
     */
    public function __construct(
        public readonly string $ageGroupId,
        public readonly string $tenantId,
        public readonly string $archivedBy,
        public readonly string $reason,
        public readonly string $hardDeleteScheduledFor,
    ) {
    }
}
