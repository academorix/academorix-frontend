<?php

/**
 * @file database/migrations/2026_07_15_120000_create_age_groups_table.php
 *
 * @description
 * Create the `age_groups` table for the `AgeGroup` entity.
 * Owned by the age-group module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports age-group --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports age-group --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('age_groups', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('organization_id', 30)->nullable();
            $table->foreign('organization_id')->references('id')->on('organizations')->onDelete('restrict');
            $table->string('name', 64);
            $table->string('slug', 32);
            $table->string('sport_key', 32)->nullable();
            $table->text('description')->nullable();
            $table->integer('min_age_inclusive');
            $table->integer('max_age_inclusive');
            $table->string('cutoff_date_kind', 32);
            $table->integer('cutoff_month')->nullable();
            $table->integer('cutoff_day')->nullable();
            $table->string('gender_category', 16)->nullable();
            $table->boolean('is_youth')->default(false);
            $table->boolean('is_default')->default(false);
            $table->boolean('is_seeded')->default(false);
            $table->integer('sort_order')->default(100);
            $table->string('color', 7)->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'organization_id', 'sport_key', 'slug'], 'age_groups_scope_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->unique(['tenant_id', 'organization_id', 'sport_key'], 'age_groups_scope_default_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_default = true AND deleted_at IS NULL
            $table->index(['tenant_id', 'sport_key', 'min_age_inclusive', 'max_age_inclusive'], 'age_groups_bounds_lookup_idx');
            $table->index(['tenant_id', 'is_youth'], 'age_groups_youth_idx');
            $table->index(['tenant_id', 'is_seeded'], 'age_groups_seeded_idx');
            $table->index(['deleted_at'], 'age_groups_deleted_at_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('age_groups');
    }
};
