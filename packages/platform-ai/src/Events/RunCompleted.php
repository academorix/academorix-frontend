<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform ai --force`.

declare(strict_types=1);

namespace Academorix\Platform\Ai\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AiRun.status flipped to 'completed'. Tokens + cost + latency stamped.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::RecordAiLatency
 * - ai::DecrementTokenPool
 * - growth::analytics::TrackAiRun
 *
 * @category Ai
 *
 * @since    0.1.0
 */
final readonly class RunCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $runId  ULID
     * @param  string  $conversationId  ULID | null
     * @param  string  $tenantId  ULID
     * @param  string  $persona  string
     * @param  int  $tokensIn  integer
     * @param  int  $tokensOut  integer
     * @param  int  $costMinor  integer
     * @param  int  $latencyMs  integer
     */
    public function __construct(
        public readonly string $runId,
        public readonly string $conversationId,
        public readonly string $tenantId,
        public readonly string $persona,
        public readonly int $tokensIn,
        public readonly int $tokensOut,
        public readonly int $costMinor,
        public readonly int $latencyMs,
    ) {
    }
}
