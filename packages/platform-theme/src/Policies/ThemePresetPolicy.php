<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Academorix\Platform\Theme\Policies;

use Academorix\User\Models\User;
use Academorix\Platform\Theme\Models\ThemePreset;

/**
 * Authorization policy for {@see \Academorix\Platform\Theme\Models\ThemePreset}.
 *
 * Wired via `#[UsePolicy]` on the Model.
 *
 * @category Theme
 *
 * @since    0.1.0
 */
final class ThemePresetPolicy
{
    /**
     * any authenticated tenant user — sees platform presets (tenant_id NULL) + tenant custom presets
     *
     * @return bool
     */
    public function viewAny(User $user, ThemePreset $model): bool
    {
        return $user->can('theme.read');
    }
    /**
     * platform preset visible to every tenant; tenant custom scoped by BelongsToTenant
     *
     * @return bool
     */
    public function view(User $user, ThemePreset $model): bool
    {
        return $user->can('theme.read');
    }
    /**
     * admin + design-role — tenant custom presets only (tenant_id != null)
     *
     * @return bool
     */
    public function create(User $user, ThemePreset $model): bool
    {
        return $user->can('theme.manage');
    }
    /**
     * platform_admin guard — Academorix staff only.
     *
     * @return bool
     */
    public function createPlatformPreset(User $user, ThemePreset $model): bool
    {
        return $user->can('platform.theme.manage-presets');
    }
    /**
     * admin + design-role — mutates tenant custom preset only.
     *
     * @return bool
     */
    public function update(User $user, ThemePreset $model): bool
    {
        return $user->can('theme.manage');
    }
    /**
     * admin + design-role — clones ANY preset (platform or tenant custom) into a new tenant custom preset.
     *
     * @return bool
     */
    public function duplicate(User $user, ThemePreset $model): bool
    {
        return $user->can('theme.manage');
    }
    /**
     * admin + design-role — flips is_active false → true (fires ThemePresetPublished)
     *
     * @return bool
     */
    public function publish(User $user, ThemePreset $model): bool
    {
        return $user->can('theme.manage');
    }
    /**
     * admin + design-role for tenant custom; platform_admin required for platform presets.
     *
     * @return bool
     */
    public function delete(User $user, ThemePreset $model): bool
    {
        return $user->can('theme.manage');
    }
}
