<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Academorix\Platform\Theme\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ThemePreset.is_active transitions false → true. For tenant custom presets: makes the preset visible to admin selection UI. For platform presets: cross-tenant announcement.
 *
 * ## Consumers
 *
 * - notifications::SendThemePresetPublishedNotification
 * - activity::WriteThemePresetPublishedToActivity
 *
 * @category Theme
 *
 * @since    0.1.0
 */
final readonly class ThemePresetPublished implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $presetId  tps_ULID
     * @param  string  $tenantId  ten_ULID | null
     * @param  string  $slug  string
     * @param  string  $publishedAt  ISO 8601
     */
    public function __construct(
        public readonly string $presetId,
        public readonly string $tenantId,
        public readonly string $slug,
        public readonly string $publishedAt,
    ) {
    }
}
