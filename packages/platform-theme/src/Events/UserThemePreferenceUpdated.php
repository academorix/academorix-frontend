<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Academorix\Platform\Theme\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * user::user_theme_preferences row updated — mode preference (light/dark/auto) OR accessibility overrides. Event fires HERE (not in user module) because theme is the resolver — user module owns the ROW, theme owns the SEMANTICS.
 *
 * ## Consumers
 *
 * - activity::WriteUserThemePreferenceUpdatedToActivity
 *
 * @category Theme
 *
 * @since    0.1.0
 */
final readonly class UserThemePreferenceUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  usr_ULID
     * @param  string  $tenantId  ten_ULID
     * @param  string  $preferredMode  'auto' | 'light' | 'dark'
     * @param  string  $accessibilityOverrides  AccessibilityFlagsPayload
     * @param  string  $updatedAt  ISO 8601
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $tenantId,
        public readonly string $preferredMode,
        public readonly string $accessibilityOverrides,
        public readonly string $updatedAt,
    ) {
    }
}
