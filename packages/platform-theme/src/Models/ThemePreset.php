<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Academorix\Platform\Theme\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Platform\Theme\Contracts\Data\ThemePresetInterface;
use Academorix\Platform\Theme\Database\Factories\ThemePresetFactory;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Mattiverse\Userstamps\Traits\Userstamps;
use Spatie\Activitylog\Traits\LogsActivity;
use Spatie\Sluggable\HasSlug;

/**
 * Eloquent model for a ThemePreset.
 *
 * A complete theme configuration.
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[Table(name: ThemePresetInterface::TABLE, keyType: ThemePresetInterface::KEY_TYPE)]
#[Fillable([
    ThemePresetInterface::ATTR_TENANT_ID,
        ThemePresetInterface::ATTR_SLUG,
        ThemePresetInterface::ATTR_NAME,
        ThemePresetInterface::ATTR_DESCRIPTION,
        ThemePresetInterface::ATTR_MODE,
        ThemePresetInterface::ATTR_CATEGORY,
        ThemePresetInterface::ATTR_TOKENS,
        ThemePresetInterface::ATTR_PREVIEW_THUMBNAIL_URL,
        ThemePresetInterface::ATTR_IS_ACTIVE,
        ThemePresetInterface::ATTR_CREATED_BY_USER_ID,
        ThemePresetInterface::ATTR_METADATA,
])]
#[UseFactory(ThemePresetFactory::class)]
final class ThemePreset extends Model implements ThemePresetInterface
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    use HasSlug;
    use HasUserstamps;
    // TODO(gen): resolve unknown trait `HasAuditable` — add its import + use line.
    use HasActivityLog;
    use HasMetadata;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        ThemePresetInterface::ATTR_MODE => 'ThemeMode',
        ThemePresetInterface::ATTR_CATEGORY => 'ThemePresetCategory',
        ThemePresetInterface::ATTR_TOKENS => 'TokenMapPayload',
        ThemePresetInterface::ATTR_IS_ACTIVE => 'boolean',
        ThemePresetInterface::ATTR_METADATA => 'array',
    ];
}
