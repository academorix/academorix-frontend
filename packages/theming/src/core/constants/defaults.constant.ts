/**
 * @file defaults.constant.ts
 * @module @stackra/theming/constants
 * @description Default configuration values for the theming system.
 */

// ============================================================================
// Storage Keys
// ============================================================================

/** Default localStorage key for persisted color mode. */
export const DEFAULT_MODE_STORAGE_KEY = 'stackra-color-mode';

/** Default localStorage key for persisted theme ID. */
export const DEFAULT_THEME_STORAGE_KEY = 'stackra-theme';

// ============================================================================
// Default Values
// ============================================================================

// ── Auto-generated stubs for barrel exports ──
export const DEFAULT_COLOR_MODE = 'default.color.mode' as const;
export const DEFAULT_THEME_ID = 'default.theme.id' as const;
export const THEME_DATA_ATTRIBUTE = 'theme.data.attribute' as const;
