<?php

/**
 * @file database/migrations/2026_07_15_120001_create_public_pages_table.php
 *
 * @description
 * Create the `public_pages` table for the `PublicPage` entity.
 * Owned by the public-site module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform public-site --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform public-site --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('public_pages', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('slug', 200);
            $table->string('title', 200);
            $table->string('kind', 24)->default('page');
            $table->string('status', 16)->default('draft');
            $table->timestampTz('published_at')->nullable();
            $table->string('seo_title', 200)->nullable();
            $table->string('seo_description', 500)->nullable();
            $table->string('seo_og_image_document_id', 30)->nullable();
            $table->foreign('seo_og_image_document_id')->references('id')->on('files')->onDelete('set null');
            $table->jsonb('content_blocks_order')->default('[]');
            $table->jsonb('metadata')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->timestampTz('deleted_at')->nullable();
            $table->unique(['tenant_id', 'slug'], 'public_pages_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('public_pages');
    }
};
