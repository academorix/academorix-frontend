<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform public-site --force`.

declare(strict_types=1);

namespace Academorix\Platform\PublicSite\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Page committed with status=published. CDN cache invalidated.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateCdn
 *
 * @category PublicSite
 *
 * @since    0.1.0
 */
final readonly class PagePublished implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $pageId  ULID (pgs_*)
     * @param  string  $tenantId  ULID
     * @param  string  $slug  string
     * @param  string  $publishedByUserId  ULID
     */
    public function __construct(
        public readonly string $pageId,
        public readonly string $tenantId,
        public readonly string $slug,
        public readonly string $publishedByUserId,
    ) {
    }
}
