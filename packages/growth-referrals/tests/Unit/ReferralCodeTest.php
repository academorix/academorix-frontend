<?php

declare(strict_types=1);

use Academorix\Growth\Referrals\Models\ReferralCode;

it('is a placeholder — replace with actual ReferralCode unit tests', function (): void {
    expect(ReferralCode::class)->toBeString();
});
