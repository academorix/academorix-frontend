<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Growth\Referrals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReferralProgram.status transitioned active -> paused. New claims refused; existing referrals in-flight complete.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
final readonly class ReferralProgramPaused implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $programId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $pausedAt  ISO 8601
     * @param  string  $pausedBy  ULID
     * @param  string  $reason  string | null
     */
    public function __construct(
        public readonly string $programId,
        public readonly string $tenantId,
        public readonly string $pausedAt,
        public readonly string $pausedBy,
        public readonly string $reason,
    ) {
    }
}
