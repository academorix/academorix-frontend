<?php

/**
 * @file database/migrations/2026_07_15_120002_create_referral_programs_table.php
 *
 * @description
 * Create the `referral_programs` table for the `ReferralProgram` entity.
 * Owned by the referrals module. Emitted from the module blueprint —
 * regenerate via `generate-module.py growth referrals --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('referral_programs', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('name', 128);
            $table->string('slug', 64);
            $table->text('description')->nullable();
            $table->string('status', 16)->default('draft');
            $table->string('side', 16)->default('single_sided');
            $table->string('reward_type_referrer', 32);
            $table->integer('reward_amount_referrer');
            $table->string('reward_currency_referrer', 3)->nullable();
            $table->string('reward_type_referred', 32)->nullable();
            $table->integer('reward_amount_referred')->nullable();
            $table->string('reward_currency_referred', 3)->nullable();
            $table->string('trigger_event', 48);
            $table->string('vesting_rule', 32);
            $table->integer('vesting_hold_days')->nullable();
            $table->integer('vesting_n_events')->nullable();
            $table->integer('max_rewards_per_referrer')->nullable();
            $table->integer('max_rewards_per_period')->nullable();
            $table->integer('max_rewards_per_program')->nullable();
            $table->integer('rewards_issued_count')->default(0);
            $table->integer('rewards_issued_amount_cents')->default(0);
            $table->jsonb('fraud_config')->default('\'{}\'::jsonb');
            $table->jsonb('eligibility_config')->default('\'{}\'::jsonb');
            $table->timestampTz('program_starts_at');
            $table->timestampTz('program_ends_at')->nullable();
            $table->string('terms_url', 512)->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 64)->nullable();
            $table->string('updated_by', 64)->nullable();
            $table->string('deleted_by', 64)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'slug'], 'referral_programs_tenant_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'status'], 'referral_programs_status_idx');
            $table->index(['tenant_id', 'program_starts_at', 'program_ends_at'], 'referral_programs_window_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('referral_programs');
    }
};
