<?php

/**
 * @file database/migrations/2026_07_15_120003_create_referral_rewards_table.php
 *
 * @description
 * Create the `referral_rewards` table for the `ReferralReward` entity.
 * Owned by the referrals module. Emitted from the module blueprint —
 * regenerate via `generate-module.py growth referrals --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('referral_rewards', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('referral_id', 64);
            $table->foreign('referral_id')->references('id')->on('referrals')->onDelete('restrict');
            $table->string('recipient_type', 32);
            $table->string('recipient_id', 64);
            $table->string('recipient_role', 16);
            $table->string('reward_type', 32);
            $table->integer('reward_amount');
            $table->string('reward_currency', 3)->nullable();
            $table->string('status', 16)->default('pending');
            $table->timestampTz('vesting_starts_at');
            $table->timestampTz('vesting_completes_at');
            $table->timestampTz('materialized_at')->nullable();
            $table->string('finance_credit_id', 64)->nullable();
            $table->timestampTz('paid_at')->nullable();
            $table->string('clawback_reason', 32)->nullable();
            $table->timestampTz('clawback_at')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 64)->nullable();
            $table->string('updated_by', 64)->nullable();
            $table->string('deleted_by', 64)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['referral_id', 'recipient_role'], 'referral_rewards_referral_role_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'recipient_type', 'recipient_id', 'status'], 'referral_rewards_by_recipient_idx');
            $table->index(['tenant_id', 'status', 'vesting_completes_at'], 'referral_rewards_vesting_idx');
            $table->index(['tenant_id', 'status', 'materialized_at'], 'referral_rewards_materialized_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('referral_rewards');
    }
};
