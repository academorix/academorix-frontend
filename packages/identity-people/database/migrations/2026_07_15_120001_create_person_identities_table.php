<?php

/**
 * @file database/migrations/2026_07_15_120001_create_person_identities_table.php
 *
 * @description
 * Create the `person_identities` table for the `PersonIdentity` entity.
 * Owned by the people module. Emitted from the module blueprint —
 * regenerate via `generate-module.py identity people --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity people --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('person_identities', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('academorix_id', 16);
            $table->string('legal_name', 200);
            $table->date('date_of_birth');
            $table->string('nationality', 2)->nullable();
            $table->string('verification_status', 16)->default('unverified');
            $table->timestampTz('verified_at')->nullable();
            $table->string('verified_by_platform_user_id', 30)->nullable();
            $table->timestampTz('frozen_at')->nullable();
            $table->string('frozen_reason', 200)->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['academorix_id'], 'person_identities_academorix_id_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['legal_name', 'date_of_birth'], 'person_identities_lookup_idx');
            $table->index(['verification_status'], 'person_identities_verification_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('person_identities');
    }
};
