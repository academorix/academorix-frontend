<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Finance\Coupon\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Coupon.valid_until has passed AND coupon still shows is_active=true. Detected by ExpireCouponsJob nightly sweep OR by CouponValidator.canRedeem() refusing an expired code and firing this event. Idempotent — fires exactly once per coupon.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchCouponExpiredNotification (customer if the coupon was owned; admin)
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
final readonly class CouponExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $couponId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $code  string
     * @param  string  $validUntil  ISO 8601
     * @param  string  $expiredAt  ISO 8601
     * @param  int  $usageCount  integer
     * @param  int  $usageCap  integer | null
     */
    public function __construct(
        public readonly string $couponId,
        public readonly string $tenantId,
        public readonly string $code,
        public readonly string $validUntil,
        public readonly string $expiredAt,
        public readonly int $usageCount,
        public readonly int $usageCap,
    ) {
    }
}
