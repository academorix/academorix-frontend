<?php

/**
 * @file database/migrations/2026_07_15_120000_create_membership_plans_table.php
 *
 * @description
 * Create the `membership_plans` table for the `MembershipPlan` entity.
 * Owned by the membership module. Emitted from the module blueprint —
 * regenerate via `generate-module.py finance membership --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('membership_plans', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('branch_id', 30);
            $table->foreign('branch_id')->references('id')->on('branches')->onDelete('restrict');
            $table->string('name', 128);
            $table->string('slug', 64);
            $table->text('description')->nullable();
            $table->unsignedBigInteger('price_cents');
            $table->string('currency', 3);
            $table->string('billing_interval', 16);
            $table->integer('billing_interval_count')->default(1);
            $table->integer('trial_days')->default(0);
            $table->integer('passes_per_period')->nullable();
            $table->boolean('passes_carry_over')->default(false);
            $table->string('age_group_id', 30)->nullable();
            $table->foreign('age_group_id')->references('id')->on('age_groups')->onDelete('restrict');
            $table->jsonb('prerequisites')->nullable();
            $table->jsonb('addon_config')->nullable();
            $table->string('refund_policy', 32)->default('no_refund');
            $table->integer('refund_grace_days')->default(14);
            $table->boolean('is_active')->default(true);
            $table->timestampTz('effective_from')->nullable();
            $table->timestampTz('effective_until')->nullable();
            $table->integer('max_active_members')->nullable();
            $table->integer('current_active_members')->default(0);
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'branch_id', 'slug'], 'membership_plans_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'branch_id', 'is_active'], 'membership_plans_active_idx');
            $table->index(['tenant_id', 'age_group_id'], 'membership_plans_age_group_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE age_group_id IS NOT NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('membership_plans');
    }
};
