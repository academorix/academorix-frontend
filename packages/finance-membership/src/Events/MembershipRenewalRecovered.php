<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Finance\Membership\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MembershipRenewal.status = succeeded AFTER a prior attempt failed. Distinct from MembershipRenewed (first-attempt success) — this is the retry-ladder recovery path. Growth analytics track dunning-effectiveness with this signal.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchRenewalRecoveredNotification
 * - growth::analytics::TrackDunningRecovery
 *
 * @category Membership
 *
 * @since    0.1.0
 */
final readonly class MembershipRenewalRecovered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $membershipId  ULID
     * @param  string  $renewalId  ULID (mbn_*)
     * @param  string  $tenantId  ULID
     * @param  int  $recoveredOnAttempt  integer
     * @param  string  $recoveredAt  ISO 8601
     */
    public function __construct(
        public readonly string $membershipId,
        public readonly string $renewalId,
        public readonly string $tenantId,
        public readonly int $recoveredOnAttempt,
        public readonly string $recoveredAt,
    ) {
    }
}
