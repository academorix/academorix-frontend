<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Finance\Membership\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Status transitioned pending → active after invoice.paid webhook (or manual mark-paid). Sets current_period_starts_at + current_period_ends_at + next_renewal_at from plan.billing_interval. IssuePassesForPeriodJob dispatches after commit to generate the period's passes.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchMembershipActivatedNotification (to primary guardian + admin — celebratory)
 * - membership::GeneratePassesOnMembershipActivated (dispatches IssuePassesForPeriodJob)
 * - growth::analytics::TrackMembershipActivation
 *
 * @category Membership
 *
 * @since    0.1.0
 */
final readonly class MembershipActivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $membershipId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $currentPeriodStartsAt  ISO 8601
     * @param  string  $currentPeriodEndsAt  ISO 8601
     * @param  string  $nextRenewalAt  ISO 8601 | null
     * @param  string  $activatedAt  ISO 8601
     */
    public function __construct(
        public readonly string $membershipId,
        public readonly string $tenantId,
        public readonly string $currentPeriodStartsAt,
        public readonly string $currentPeriodEndsAt,
        public readonly string $nextRenewalAt,
        public readonly string $activatedAt,
    ) {
    }
}
