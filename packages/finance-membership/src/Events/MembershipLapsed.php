<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Finance\Membership\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Terminal. Fires when consecutive_failed_renewal_count >= 3 (after final retry ladder attempt). Distinct from cancelled — no customer intent, involuntary termination for payment failure. Remaining passes expire immediately.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — SOX 404)
 * - activity::WriteToActivityLog
 * - notifications::DispatchMembershipLapsedNotification (transactional_required — customer + admin)
 * - membership::ExpirePassesOnMembershipLapsed
 * - growth::analytics::TrackInvoluntaryChurn
 *
 * @category Membership
 *
 * @since    0.1.0
 */
final readonly class MembershipLapsed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $membershipId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $lapsedAt  ISO 8601
     * @param  int  $consecutiveFailedRenewalCount  integer
     * @param  string  $lastRenewalId  ULID (mbn_*)
     * @param  string  $lastFailureReason  string
     */
    public function __construct(
        public readonly string $membershipId,
        public readonly string $tenantId,
        public readonly string $lapsedAt,
        public readonly int $consecutiveFailedRenewalCount,
        public readonly string $lastRenewalId,
        public readonly string $lastFailureReason,
    ) {
    }
}
