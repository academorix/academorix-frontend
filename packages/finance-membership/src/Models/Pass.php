<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Finance\Membership\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Finance\Membership\Contracts\Data\PassInterface;
use Academorix\Finance\Membership\Database\Factories\PassFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Foundation\Concerns\HasPrefixedUlid;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Laravel\Scout\Searchable;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a Pass.
 *
 * A per-session admission credit issued by an active membership.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[Table(name: PassInterface::TABLE, keyType: PassInterface::KEY_TYPE)]
#[Fillable([
    PassInterface::ATTR_TENANT_ID,
        PassInterface::ATTR_MEMBERSHIP_ID,
        PassInterface::ATTR_PASS_TYPE,
        PassInterface::ATTR_VALID_FROM,
        PassInterface::ATTR_VALID_UNTIL,
        PassInterface::ATTR_CONSUMED_AT,
        PassInterface::ATTR_CONSUMED_BY_SESSION_ID,
        PassInterface::ATTR_REFUNDED_AT,
        PassInterface::ATTR_REFUND_REASON,
        PassInterface::ATTR_METADATA,
])]
#[UseFactory(PassFactory::class)]
final class Pass extends Model implements PassInterface
{
    use HasFactory;
    use HasPrefixedUlid;
    use BelongsToTenant;
    // TODO(gen): resolve unknown trait `BelongsToMembership` — add its import + use line.
    use HasMetadata;
    use HasUserstamps;
    use Auditable;
    use HasActivityLog;
    use Filterable;
    use Searchable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        PassInterface::ATTR_PASS_TYPE => 'PassType',
        PassInterface::ATTR_VALID_FROM => 'datetime',
        PassInterface::ATTR_VALID_UNTIL => 'datetime',
        PassInterface::ATTR_CONSUMED_AT => 'datetime',
        PassInterface::ATTR_REFUNDED_AT => 'datetime',
    ];
}
