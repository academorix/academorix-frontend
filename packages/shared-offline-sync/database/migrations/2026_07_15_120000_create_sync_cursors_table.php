<?php

/**
 * @file database/migrations/2026_07_15_120000_create_sync_cursors_table.php
 *
 * @description
 * Create the `sync_cursors` table for the `SyncCursor` entity.
 * Owned by the offline-sync module. Emitted from the module blueprint —
 * regenerate via `generate-module.py shared offline-sync --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared offline-sync --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('sync_cursors', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('user_id', 30);
            $table->foreign('user_id')->references('id')->on('users')->onDelete('cascade');
            $table->string('device_id', 200);
            $table->string('entity_type', 64);
            $table->timestampTz('last_synced_at')->nullable();
            $table->string('last_synced_id', 30)->nullable();
            $table->integer('records_synced_count')->default('0');
            $table->integer('conflict_count')->default('0');
            $table->jsonb('metadata')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'user_id', 'device_id', 'entity_type'], 'sync_cursors_unique');
            $table->index(['tenant_id', 'last_synced_at'], 'sync_cursors_stale_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('sync_cursors');
    }
};
