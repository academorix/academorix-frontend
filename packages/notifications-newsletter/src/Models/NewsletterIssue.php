<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications newsletter --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Newsletter\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Notifications\Newsletter\Contracts\Data\NewsletterIssueInterface;
use Academorix\Notifications\Newsletter\Database\Factories\NewsletterIssueFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Laravel\Scout\Searchable;
use Mattiverse\Userstamps\Traits\Userstamps;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a NewsletterIssue.
 *
 * An individual issue of a newsletter.
 *
 * @category Newsletter
 *
 * @since    0.1.0
 */
#[Table(name: NewsletterIssueInterface::TABLE, keyType: NewsletterIssueInterface::KEY_TYPE)]
#[Fillable([
    NewsletterIssueInterface::ATTR_TENANT_ID,
        NewsletterIssueInterface::ATTR_NEWSLETTER_ID,
        NewsletterIssueInterface::ATTR_SLUG,
        NewsletterIssueInterface::ATTR_ISSUE_NUMBER,
        NewsletterIssueInterface::ATTR_SUBJECT,
        NewsletterIssueInterface::ATTR_PREHEADER,
        NewsletterIssueInterface::ATTR_CONTENT_BLOCKS,
        NewsletterIssueInterface::ATTR_TEMPLATE_REF,
        NewsletterIssueInterface::ATTR_VARIABLES,
        NewsletterIssueInterface::ATTR_STATE,
        NewsletterIssueInterface::ATTR_SCHEDULED_AT,
        NewsletterIssueInterface::ATTR_SENT_AT,
        NewsletterIssueInterface::ATTR_METADATA,
        NewsletterIssueInterface::ATTR_TRANSLATIONS,
])]
#[UseFactory(NewsletterIssueFactory::class)]
final class NewsletterIssue extends Model implements NewsletterIssueInterface
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    use HasMetadata;
    use HasUserstamps;
    // TODO(gen): resolve unknown trait `HasTranslations` — add its import + use line.
    // TODO(gen): resolve unknown trait `HasVersions` — add its import + use line.
    use HasActivityLog;
    use Filterable;
    use Searchable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        NewsletterIssueInterface::ATTR_STATE => 'NewsletterIssueState',
        NewsletterIssueInterface::ATTR_CONTENT_BLOCKS => 'array',
        NewsletterIssueInterface::ATTR_SCHEDULED_AT => 'datetime',
        NewsletterIssueInterface::ATTR_SENT_AT => 'datetime',
        NewsletterIssueInterface::ATTR_VARIABLES => 'array',
        NewsletterIssueInterface::ATTR_METADATA => 'array',
        NewsletterIssueInterface::ATTR_TRANSLATIONS => 'array',
    ];
}
