<?php

/**
 * @file database/migrations/2026_07_15_120002_create_registration_tasks_table.php
 *
 * @description
 * Create the `registration_tasks` table for the `RegistrationTask` entity.
 * Owned by the registrations module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports registrations --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registrations --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('registration_tasks', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('registration_id', 30);
            $table->foreign('registration_id')->references('id')->on('registrations')->onDelete('cascade');
            $table->string('assignee_user_id', 30);
            $table->foreign('assignee_user_id')->references('id')->on('users')->onDelete('restrict');
            $table->string('title', 200);
            $table->text('description')->nullable();
            $table->timestampTz('due_at');
            $table->string('status', 16)->default('open');
            $table->string('priority', 8)->default('normal');
            $table->timestampTz('completed_at')->nullable();
            $table->string('completed_by_user_id', 30)->nullable();
            $table->foreign('completed_by_user_id')->references('id')->on('users')->onDelete('set null');
            $table->string('completion_note', 500)->nullable();
            $table->timestampTz('reminder_sent_at')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->index(['tenant_id', 'assignee_user_id', 'status', 'due_at'], 'registration_tasks_assignee_open_idx');
            $table->index(['tenant_id', 'registration_id', 'status'], 'registration_tasks_registration_idx');
            $table->index(['tenant_id', 'status', 'due_at'], 'registration_tasks_overdue_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE status = 'open'
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('registration_tasks');
    }
};
