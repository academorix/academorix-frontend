<?php

/**
 * @file database/migrations/2026_07_15_120000_create_offers_table.php
 *
 * @description
 * Create the `offers` table for the `Offer` entity.
 * Owned by the registrations module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports registrations --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registrations --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('offers', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('registration_id', 30);
            $table->foreign('registration_id')->references('id')->on('registrations')->onDelete('cascade');
            $table->string('team_id', 30);
            $table->foreign('team_id')->references('id')->on('teams')->onDelete('restrict');
            $table->string('season_id', 30);
            $table->foreign('season_id')->references('id')->on('seasons')->onDelete('restrict');
            $table->string('membership_plan_id', 30)->nullable();
            $table->foreign('membership_plan_id')->references('id')->on('membership_plans')->onDelete('restrict');
            $table->unsignedBigInteger('offered_amount_minor')->nullable();
            $table->string('currency', 3)->nullable();
            $table->string('status', 16)->default('open');
            $table->string('signature', 128);
            $table->timestampTz('offered_at');
            $table->timestampTz('expires_at');
            $table->timestampTz('accepted_at')->nullable();
            $table->string('accepted_by_user_id', 30)->nullable();
            $table->foreign('accepted_by_user_id')->references('id')->on('users')->onDelete('set null');
            $table->timestampTz('declined_at')->nullable();
            $table->string('declined_reason', 500)->nullable();
            $table->timestampTz('withdrawn_at')->nullable();
            $table->string('withdrawn_reason', 500)->nullable();
            $table->boolean('auto_decline_on_expire')->default('true');
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->index(['tenant_id', 'registration_id', 'status'], 'offers_registration_status_idx');
            $table->unique(['signature'], 'offers_signature_unique');
            $table->index(['tenant_id', 'expires_at'], 'offers_expiry_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE status = 'open'
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('offers');
    }
};
