<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared transfer --force`.

declare(strict_types=1);

namespace Academorix\Shared\Transfer\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * XferJob transitioned to succeeded (zero failed rows). Fires from MarkXferJobCompletedJob.
 *
 * ## Consumers
 *
 * - activity::HasActivityLog
 * - audit::HasAudit
 * - SendXferJobNotificationJob (chained; sends XferJobCompletedNotification)
 * - any downstream module listening for its own entity's completion (e.g. newsletter subscribes on kind=import + entity=subscribers)
 *
 * @category Transfer
 *
 * @since    0.1.0
 */
final readonly class XferJobCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $xferJobId  ULID
     * @param  string  $kind  string
     * @param  string  $entity  string
     * @param  array  $counters  counters object
     * @param  string  $resultArtifactId  ULID (xart_) | null
     * @param  string  $finishedAt  ISO 8601
     */
    public function __construct(
        public readonly string $xferJobId,
        public readonly string $kind,
        public readonly string $entity,
        public readonly array $counters,
        public readonly string $resultArtifactId,
        public readonly string $finishedAt,
    ) {
    }
}
