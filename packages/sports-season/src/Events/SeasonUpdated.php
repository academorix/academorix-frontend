<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Sports\Season\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Any Season scalar field mutated except the specific-transition fields below (status, is_current, start_date, end_date, sport_key, late_registration_fee_cents, capacity_target). Umbrella event for renames, description edits, primary_color changes, sort_order tweaks, allows_late_registration toggles.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Season
 *
 * @since    0.1.0
 */
final readonly class SeasonUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedFields  string[] (e.g. ['name', 'primary_color', 'sort_order'])
     * @param  string  $updatedBy  ULID
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly string $changedFields,
        public readonly string $updatedBy,
    ) {
    }
}
