<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Sports\Season\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The `is_current=true` flag flipped within a scope tuple. Fires with both the newly-current row + the previously-current row so downstream caches can drop keys keyed on either. AUDIT-CRITICAL: year-over-year reports pivot on `is_current`; changing it shifts the whole reporting frame.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial + reporting record)
 * - activity::WriteToActivityLog
 * - notifications::DispatchSeasonCurrentChangedNotification (cannot-opt-out — to owner + admin)
 * - cache::InvalidateCurrentSeasonCache (keyed by (tenant, org, branch, sport_key))
 *
 * @category Season
 *
 * @since    0.1.0
 */
final readonly class SeasonCurrentChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $organizationId  ULID | null
     * @param  string  $branchId  ULID | null
     * @param  string  $sportKey  string | null
     * @param  string  $newCurrentSeasonId  ULID
     * @param  string  $previousCurrentSeasonId  ULID | null
     * @param  string  $changedBy  ULID | null (null = system reconciler)
     * @param  string  $auditSeverity  critical
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $organizationId,
        public readonly string $branchId,
        public readonly string $sportKey,
        public readonly string $newCurrentSeasonId,
        public readonly string $previousCurrentSeasonId,
        public readonly string $changedBy,
        public readonly string $auditSeverity,
    ) {
    }
}
