<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Sports\Season\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Season.status transitioned in_progress -> completed OR playoffs -> completed. Terminal-adjacent state; only archive follows. Fires from automated RollSeasonPhaseJob (when now() reaches end_date OR playoff_ends_at) OR manual admin action.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchSeasonCompletedNotification (opt-in to enrolled athletes — season-summary)
 *
 * @category Season
 *
 * @since    0.1.0
 */
final readonly class SeasonCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $completedAt  ISO 8601
     * @param  int  $finalEnrolledCount  integer
     * @param  string  $trigger  'automated_phase_roll' | 'manual_admin'
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly string $completedAt,
        public readonly int $finalEnrolledCount,
        public readonly string $trigger,
    ) {
    }
}
