<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Sports\Season\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Season transitioned from registration_closed into the late-registration window (now() >= registration_closes_at AND now() < late_registration_ends_at AND allows_late_registration=true). Fires from CloseExpiredRegistrationsJob when it observes the crossover — the Season row itself stays status=registration_closed; this event is informational to notification consumers.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Season
 *
 * @since    0.1.0
 */
final readonly class SeasonLateRegistrationOpened implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $openedAt  ISO 8601
     * @param  string  $lateRegistrationEndsAt  ISO 8601
     * @param  int  $lateRegistrationFeeCents  integer
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly string $openedAt,
        public readonly string $lateRegistrationEndsAt,
        public readonly int $lateRegistrationFeeCents,
    ) {
    }
}
