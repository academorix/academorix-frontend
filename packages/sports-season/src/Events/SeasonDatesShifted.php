<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Sports\Season\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Season.start_date OR Season.end_date OR Season.playoff_starts_at OR Season.playoff_ends_at changed on an in-flight Season (status IN in_progress, playoffs). AUDIT-WARN: affects enrolled athletes' bookings + coach schedules; regulator interest for minors' programs. Refused when status IN in_progress/playoffs/completed UNLESS caller carries `season.dates.shift` permission.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchSeasonDatesShiftedNotification (cannot-opt-out — to enrolled athletes + coaches)
 *
 * @category Season
 *
 * @since    0.1.0
 */
final readonly class SeasonDatesShifted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $shiftedBy  ULID
     * @param  string  $before  {'start_date': 'ISO 8601 date', 'end_date': 'ISO 8601 date', 'playoff_starts_at': 'ISO 8601 | null', 'playoff_ends_at': 'ISO 8601 | null'}
     * @param  string  $after  {'start_date': 'ISO 8601 date', 'end_date': 'ISO 8601 date', 'playoff_starts_at': 'ISO 8601 | null', 'playoff_ends_at': 'ISO 8601 | null'}
     * @param  int  $affectedEnrollmentCount  integer
     * @param  string  $auditSeverity  warn
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly string $shiftedBy,
        public readonly string $before,
        public readonly string $after,
        public readonly int $affectedEnrollmentCount,
        public readonly string $auditSeverity,
    ) {
    }
}
