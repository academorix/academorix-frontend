<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Sports\Season\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Season.status transitioned completed -> archived. Soft-delete completed; the row is hidden from tenant selectors + booking flows but readable via `?include_archived=true`. Refused when active downstream enrollments still reference this Season.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Season
 *
 * @since    0.1.0
 */
final readonly class SeasonArchived implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $archivedAt  ISO 8601
     * @param  string  $archivedBy  ULID
     * @param  string  $reason  string | null
     * @param  string  $hardDeleteScheduledFor  ISO 8601
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly string $archivedAt,
        public readonly string $archivedBy,
        public readonly string $reason,
        public readonly string $hardDeleteScheduledFor,
    ) {
    }
}
