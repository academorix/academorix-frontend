<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Notifications\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SendNotificationJob accepted onto its queue. Emitted for observability, not for consumers to act on.
 *
 * ## Consumers
 *
 * - observability::MetricsCollector
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
final readonly class NotificationQueued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $notificationId  ULID
     * @param  string  $deliveryId  ULID
     * @param  string  $queue  'notifications' | 'notifications-critical' | 'notifications-digests'
     * @param  string  $channel  'mail' | 'sms' | 'push' | 'in_app'
     * @param  string  $queuedAt  ISO 8601
     */
    public function __construct(
        public readonly string $notificationId,
        public readonly string $deliveryId,
        public readonly string $queue,
        public readonly string $channel,
        public readonly string $queuedAt,
    ) {
    }
}
