<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Notifications\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * User changed preferences (opt-in / opt-out / quiet hours / digest mode). Fires per-row (bulk PATCH fans out into many events).
 *
 * ## Consumers
 *
 * - notifications::PreferenceCacheInvalidator
 * - notifications-mail::UpdateSuppressionListener (on unsubscribe)
 * - compliance::AuditPreferenceChangeListener (Art. 7 consent trail)
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
final readonly class PreferenceUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $categorySlug  string
     * @param  string  $channel  string
     * @param  bool  $enabled  boolean
     * @param  string  $digestMode  'immediate' | 'daily' | 'weekly' | 'off'
     * @param  string  $source  'user_settings' | 'unsubscribe_link' | 'admin_override' | 'seed'
     * @param  string  $changedAt  ISO 8601
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $tenantId,
        public readonly string $categorySlug,
        public readonly string $channel,
        public readonly bool $enabled,
        public readonly string $digestMode,
        public readonly string $source,
        public readonly string $changedAt,
    ) {
    }
}
