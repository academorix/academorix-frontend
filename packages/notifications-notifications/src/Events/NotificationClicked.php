<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Notifications\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * User clicked a tracked link. Recorded per-link with the target URL.
 *
 * ## Consumers
 *
 * - invitations::MarkInvitationClickedListener
 * - analytics::TrackClickListener
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
final readonly class NotificationClicked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $notificationId  ULID
     * @param  string  $deliveryId  ULID
     * @param  string  $channel  string
     * @param  string  $clickedAt  ISO 8601
     * @param  string  $clickedUrl  string — hashed for restricted URLs (unsubscribe tokens etc.)
     * @param  string  $openedIp  string | null
     * @param  string  $userAgent  string | null
     */
    public function __construct(
        public readonly string $notificationId,
        public readonly string $deliveryId,
        public readonly string $channel,
        public readonly string $clickedAt,
        public readonly string $clickedUrl,
        public readonly string $openedIp,
        public readonly string $userAgent,
    ) {
    }
}
