<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Notifications\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Caller invokes `$user->notify(...)` OR `Notifications::dispatch(...)`. Fires after the Notification row commits + before channel-module handoff.
 *
 * ## Consumers
 *
 * - notifications-mail::SendMailListener
 * - notifications-sms::SendSmsListener
 * - notifications-push::SendPushListener
 * - notifications-in-app::BroadcastInAppListener
 * - notifications::DigestScheduler (batching short-circuit if digest_mode≠immediate)
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
final readonly class NotificationDispatched implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $notificationId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $applicationId  ULID | null
     * @param  string  $categorySlug  string — module-namespaced e.g. invitations.invitation_sent
     * @param  string  $templateKey  string — typically matches category_slug
     * @param  string  $priority  'critical' | 'transactional' | 'product' | 'marketing'
     * @param  string  $channelsRequested  string[] — subset of ['mail','sms','push','in_app'] resolved from preferences
     * @param  string  $recipient  {'id': 'ULID | null (anonymous / invite-by-email)', 'type': "'user' | 'anonymous'", 'email': 'string | null', 'phone': 'string (E.164) | null', 'name': 'string', 'locale': 'string (ISO 639-1)', 'timezone': 'string (IANA)', 'consent_age_gate': "'adult' | 'minor_over_consent_age' | 'minor_under_consent_age'"}
     * @param  string  $tenant  {'id': 'ULID', 'name': 'string', 'slug': 'string', 'logo_url': 'string | null', 'primary_color': 'string (hex)', 'sender_identity': "string — 'Display Name <no-reply@domain>'", 'postal_address': 'string — required for CAN-SPAM footer', 'country_code': 'string (ISO 3166-1 alpha-2) — gates CASL express-consent enforcement'}
     * @param  string  $actor  {'type': "'user' | 'platform_user' | 'system'", 'id': 'ULID | null'}
     * @param  array  $variables  object — template variables (module-defined)
     * @param  string  $occurredAt  ISO 8601 timestamp
     */
    public function __construct(
        public readonly string $notificationId,
        public readonly string $tenantId,
        public readonly string $applicationId,
        public readonly string $categorySlug,
        public readonly string $templateKey,
        public readonly string $priority,
        public readonly string $channelsRequested,
        public readonly string $recipient,
        public readonly string $tenant,
        public readonly string $actor,
        public readonly array $variables,
        public readonly string $occurredAt,
    ) {
    }
}
