<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Notifications\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Channel module exhausted retries OR received a hard-failure signal (hard bounce, unsubscribed, invalid device token). No further attempts.
 *
 * ## Consumers
 *
 * - invitations::MarkInvitationBouncedListener
 * - notifications-mail::SuppressAddressListener (hard bounce only)
 * - notifications-push::ExpireSubscriptionListener (invalid token only)
 * - compliance::AuditNotificationFailedListener
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
final readonly class NotificationFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $notificationId  ULID
     * @param  string  $deliveryId  ULID
     * @param  string  $channel  string
     * @param  string  $provider  string
     * @param  string  $failureKind  'hard_bounce' | 'soft_bounce_exhausted' | 'invalid_recipient' | 'unsubscribed' | 'provider_error' | 'quota_exceeded'
     * @param  string  $errorCode  string — module-namespaced e.g. NOTIFICATIONS_MAIL_HARD_BOUNCE
     * @param  string  $errorMessage  string — provider-reported, sanitized
     * @param  string  $failedAt  ISO 8601
     */
    public function __construct(
        public readonly string $notificationId,
        public readonly string $deliveryId,
        public readonly string $channel,
        public readonly string $provider,
        public readonly string $failureKind,
        public readonly string $errorCode,
        public readonly string $errorMessage,
        public readonly string $failedAt,
    ) {
    }
}
