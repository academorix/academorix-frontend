<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Notifications\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Notifications\Notifications\Contracts\Data\NotificationCategoryInterface;
use Academorix\Notifications\Notifications\Database\Factories\NotificationCategoryFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Laravel\Scout\Searchable;
use Mattiverse\Userstamps\Traits\Userstamps;

/**
 * Eloquent model for a NotificationCategory.
 *
 * Registry entry seeded from every module's notifications.
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
#[Table(name: NotificationCategoryInterface::TABLE, keyType: NotificationCategoryInterface::KEY_TYPE)]
#[Fillable([
    NotificationCategoryInterface::ATTR_TENANT_ID,
        NotificationCategoryInterface::ATTR_SLUG,
        NotificationCategoryInterface::ATTR_OWNING_MODULE,
        NotificationCategoryInterface::ATTR_DISPLAY_NAME,
        NotificationCategoryInterface::ATTR_DESCRIPTION,
        NotificationCategoryInterface::ATTR_DEFAULT_CHANNELS,
        NotificationCategoryInterface::ATTR_PRIORITY,
        NotificationCategoryInterface::ATTR_CONSENT_TIER,
        NotificationCategoryInterface::ATTR_OPT_OUT_ALLOWED,
        NotificationCategoryInterface::ATTR_IS_SYSTEM,
        NotificationCategoryInterface::ATTR_SORT_ORDER,
        NotificationCategoryInterface::ATTR_METADATA,
        NotificationCategoryInterface::ATTR_TRANSLATIONS,
])]
#[UseFactory(NotificationCategoryFactory::class)]
final class NotificationCategory extends Model implements NotificationCategoryInterface
{
    use HasFactory;
    use HasUlids;
    // TODO(gen): resolve unknown trait `HasSystemFlag` — add its import + use line.
    // TODO(gen): resolve unknown trait `BelongsToTenantOptional` — add its import + use line.
    use HasMetadata;
    use HasUserstamps;
    // TODO(gen): resolve unknown trait `HasAuditable` — add its import + use line.
    // TODO(gen): resolve unknown trait `HasTranslations` — add its import + use line.
    // TODO(gen): resolve unknown trait `Sortable` — add its import + use line.
    use Filterable;
    // TODO(gen): resolve unknown trait `Sluggable` — add its import + use line.
    use Searchable;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        NotificationCategoryInterface::ATTR_IS_SYSTEM => 'boolean',
        NotificationCategoryInterface::ATTR_OPT_OUT_ALLOWED => 'boolean',
        NotificationCategoryInterface::ATTR_DEFAULT_CHANNELS => 'array',
        NotificationCategoryInterface::ATTR_METADATA => 'array',
        NotificationCategoryInterface::ATTR_TRANSLATIONS => 'array',
    ];
}
