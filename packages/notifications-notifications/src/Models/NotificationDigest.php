<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Notifications\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Notifications\Notifications\Contracts\Data\NotificationDigestInterface;
use Academorix\Notifications\Notifications\Database\Factories\NotificationDigestFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Mattiverse\Userstamps\Traits\Userstamps;

/**
 * Eloquent model for a NotificationDigest.
 *
 * Batching state for a `(user, category, channel, scheduled_for)` window.
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
#[Table(name: NotificationDigestInterface::TABLE, keyType: NotificationDigestInterface::KEY_TYPE)]
#[Fillable([
    NotificationDigestInterface::ATTR_TENANT_ID,
        NotificationDigestInterface::ATTR_USER_ID,
        NotificationDigestInterface::ATTR_CATEGORY_SLUG,
        NotificationDigestInterface::ATTR_CHANNEL,
        NotificationDigestInterface::ATTR_WINDOW,
        NotificationDigestInterface::ATTR_SCHEDULED_FOR,
        NotificationDigestInterface::ATTR_STATE,
        NotificationDigestInterface::ATTR_DELIVERED_NOTIFICATION_ID,
        NotificationDigestInterface::ATTR_DELIVERED_AT,
        NotificationDigestInterface::ATTR_SKIP_REASON,
        NotificationDigestInterface::ATTR_METADATA,
])]
#[UseFactory(NotificationDigestFactory::class)]
final class NotificationDigest extends Model implements NotificationDigestInterface
{
    use HasFactory;
    use HasUlids;
    // TODO(gen): resolve unknown trait `HasStates` — add its import + use line.
    use BelongsToTenant;
    use HasMetadata;
    use HasUserstamps;
    use Filterable;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        NotificationDigestInterface::ATTR_STATE => 'NotificationDigestState',
        NotificationDigestInterface::ATTR_SCHEDULED_FOR => 'datetime',
        NotificationDigestInterface::ATTR_DELIVERED_AT => 'datetime',
        NotificationDigestInterface::ATTR_METADATA => 'array',
    ];
}
