<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

namespace Academorix\Platform\Branch\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ProvisionBranch action completed. Fires exactly once per new `branches.id`.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::MaybeDispatchBranchCreatedNotification
 * - branch::MaybeDispatchGeocodeAddressJob (fires only when tenant has branch_geocoding entitlement)
 *
 * @category Branch
 *
 * @since    0.1.0
 */
final readonly class BranchCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $branchId  ULID (brn_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $organizationId  ULID (org_*)
     * @param  string  $regionId  ULID (rgn_*)
     * @param  string  $name  string
     * @param  string  $slug  string
     * @param  bool  $isDefault  boolean
     * @param  string  $createdBy  ULID (usr_*) | null
     */
    public function __construct(
        public readonly string $branchId,
        public readonly string $tenantId,
        public readonly string $organizationId,
        public readonly string $regionId,
        public readonly string $name,
        public readonly string $slug,
        public readonly bool $isDefault,
        public readonly string $createdBy,
    ) {
    }
}
