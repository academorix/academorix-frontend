<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

namespace Academorix\Platform\Branch\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Branch.coordinates was set OR changed. Distinct from BranchUpdated because the Wave 3 geofencing module subscribes to this event to recompute polygon coverage for every geofence attached to this Branch.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - geofencing::RecomputeGeofenceCoverageJob (Wave 3, when the geofencing module lands)
 *
 * @category Branch
 *
 * @since    0.1.0
 */
final readonly class BranchCoordinatesUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $branchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $oldCoordinates  { lat, lng, accuracy_m, source } | null
     * @param  string  $newCoordinates  { lat, lng, accuracy_m, source }
     * @param  string  $source  'manual' | 'geocoded' | 'imported'
     * @param  string  $updatedBy  ULID
     */
    public function __construct(
        public readonly string $branchId,
        public readonly string $tenantId,
        public readonly string $oldCoordinates,
        public readonly string $newCoordinates,
        public readonly string $source,
        public readonly string $updatedBy,
    ) {
    }
}
