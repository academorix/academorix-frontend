<?php

/**
 * @file database/migrations/2026_07_15_120000_create_branchs_table.php
 *
 * @description
 * Create the `branchs` table for the `Branch` entity.
 * Owned by the branch module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform branch --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('branchs', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('organization_id', 64);
            $table->foreign('organization_id')->references('id')->on('organizations')->onDelete('restrict');
            $table->string('region_id', 64);
            $table->foreign('region_id')->references('id')->on('regions')->onDelete('restrict');
            $table->string('name', 200);
            $table->string('slug', 100);
            $table->string('code', 32)->nullable();
            $table->text('description')->nullable();
            $table->string('logo_url', 500)->nullable();
            $table->jsonb('address')->nullable();
            $table->jsonb('coordinates')->nullable();
            $table->string('phone', 20)->nullable();
            $table->string('email', 200)->nullable();
            $table->string('website_url', 500)->nullable();
            $table->jsonb('opening_hours')->nullable()->default('{}');
            $table->integer('capacity')->nullable();
            $table->jsonb('amenities')->default('[]');
            $table->string('status', 16)->default('active');
            $table->boolean('is_default')->default(false);
            $table->date('opened_date')->nullable();
            $table->date('closed_date')->nullable();
            $table->integer('sort_order')->default(100);
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'organization_id', 'slug'], 'branches_tenant_org_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->unique(['tenant_id', 'organization_id'], 'branches_default_per_org_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_default = true AND deleted_at IS NULL
            $table->index(['tenant_id', 'organization_id'], 'branches_tenant_org_idx');
            $table->index(['tenant_id', 'region_id'], 'branches_tenant_region_idx');
            $table->index(['tenant_id', 'status'], 'branches_tenant_status_idx');
            $table->index(['tenant_id', 'sort_order'], 'branches_tenant_sort_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('branchs');
    }
};
