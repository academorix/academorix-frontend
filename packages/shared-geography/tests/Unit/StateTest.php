<?php

declare(strict_types=1);

use Academorix\Shared\Geography\Models\State;

it('is a placeholder — replace with actual State unit tests', function (): void {
    expect(State::class)->toBeString();
});
