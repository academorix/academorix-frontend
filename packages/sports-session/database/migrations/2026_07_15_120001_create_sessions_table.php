<?php

/**
 * @file database/migrations/2026_07_15_120001_create_sessions_table.php
 *
 * @description
 * Create the `sessions` table for the `Session` entity.
 * Owned by the session module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports session --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('sessions', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('branch_id', 64);
            $table->foreign('branch_id')->references('id')->on('branches')->onDelete('restrict');
            $table->string('resource_booking_id', 64);
            $table->foreign('resource_booking_id')->references('id')->on('resource_bookings')->onDelete('restrict');
            $table->string('team_id', 64)->nullable();
            $table->foreign('team_id')->references('id')->on('teams')->onDelete('restrict');
            $table->string('season_id', 64)->nullable();
            $table->foreign('season_id')->references('id')->on('seasons')->onDelete('restrict');
            $table->string('event_id', 64)->nullable();
            $table->foreign('event_id')->references('id')->on('events')->onDelete('restrict');
            $table->string('sport_key', 32)->nullable();
            $table->string('name', 128);
            $table->string('slug', 64)->nullable();
            $table->text('description')->nullable();
            $table->string('session_type', 24);
            $table->string('head_coach_id', 64)->nullable();
            $table->foreign('head_coach_id')->references('id')->on('coaches')->onDelete('restrict');
            $table->jsonb('assistant_coach_ids')->nullable();
            $table->string('supervising_staff_id', 64)->nullable();
            $table->foreign('supervising_staff_id')->references('id')->on('staff')->onDelete('restrict');
            $table->integer('attendance_capacity')->nullable();
            $table->integer('attendance_min')->nullable();
            $table->string('age_group_id', 64)->nullable();
            $table->foreign('age_group_id')->references('id')->on('age_groups')->onDelete('restrict');
            $table->string('gender_category', 16)->nullable();
            $table->string('curriculum_topic', 256)->nullable();
            $table->string('lesson_plan_url', 512)->nullable();
            $table->jsonb('equipment_needed')->nullable();
            $table->string('status', 24)->default('scheduled');
            $table->integer('checked_in_athletes_count')->default(0);
            $table->integer('absent_athletes_count')->default(0);
            $table->text('cancellation_reason')->nullable();
            $table->timestampTz('cancellation_notice_at')->nullable();
            $table->text('session_notes')->nullable();
            $table->decimal('session_rating', 15, 4)->nullable();
            $table->integer('session_rating_count')->default(0);
            $table->decimal('coach_rating_input', 15, 4)->nullable();
            $table->boolean('safeguarding_incident_flag')->default(false);
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 64)->nullable();
            $table->string('updated_by', 64)->nullable();
            $table->string('deleted_by', 64)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'slug'], 'sessions_scope_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL AND slug IS NOT NULL
            $table->unique(['tenant_id', 'resource_booking_id'], 'sessions_booking_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'status'], 'sessions_status_idx');
            $table->index(['tenant_id', 'branch_id', 'status'], 'sessions_branch_status_idx');
            $table->index(['tenant_id', 'team_id'], 'sessions_team_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE team_id IS NOT NULL
            $table->index(['tenant_id', 'season_id'], 'sessions_season_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE season_id IS NOT NULL
            $table->index(['tenant_id', 'event_id'], 'sessions_event_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE event_id IS NOT NULL
            $table->index(['tenant_id', 'head_coach_id'], 'sessions_head_coach_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE head_coach_id IS NOT NULL
            $table->index(['tenant_id', 'sport_key'], 'sessions_sport_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE sport_key IS NOT NULL
            $table->index(['tenant_id', 'safeguarding_incident_flag'], 'sessions_safeguarding_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE safeguarding_incident_flag = true
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('sessions');
    }
};
