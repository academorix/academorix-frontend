/**
 * @file devtools-launcher.interface.ts
 * @module @stackra/devtools/native/components
 * @description Props for the native {@link DevtoolsLauncher}.
 */

/** Props for the native launcher. */
export interface DevtoolsLauncherProps {
  /** Extra classes for the launcher. */
  readonly className?: string;
}
