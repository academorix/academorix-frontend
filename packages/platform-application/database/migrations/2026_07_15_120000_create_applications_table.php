<?php

/**
 * @file database/migrations/2026_07_15_120000_create_applications_table.php
 *
 * @description
 * Create the `applications` table for the `Application` entity.
 * Owned by the application module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform application --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform application --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('applications', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('slug', 63);
            $table->string('name', 200);
            $table->text('description')->nullable();
            $table->string('default_business_type', 32)->nullable();
            $table->string('default_locale', 10)->default('en');
            $table->string('default_timezone', 64)->default('UTC');
            $table->string('default_currency', 3)->default('USD');
            $table->string('central_host', 200);
            $table->string('platform_admin_host', 200);
            $table->jsonb('config')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->boolean('is_default')->default(false);
            $table->boolean('is_system')->default(false);
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['slug'], 'applications_slug_unique');
            $table->unique(['central_host'], 'applications_central_host_unique');
            $table->unique(['platform_admin_host'], 'applications_platform_admin_host_unique');
            $table->index(['is_default'], 'applications_is_default_index');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_default = TRUE
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('applications');
    }
};
