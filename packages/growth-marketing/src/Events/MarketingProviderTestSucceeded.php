<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Growth\Marketing\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Test-mode dispatch returned 2xx + validation passed.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchMarketingProviderTestSucceededNotification (opt-in to the tenant admin who ran the test)
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
final readonly class MarketingProviderTestSucceeded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $providerConfigId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  int  $latencyMs  integer
     * @param  string  $succeededAt  ISO 8601
     */
    public function __construct(
        public readonly string $providerConfigId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly int $latencyMs,
        public readonly string $succeededAt,
    ) {
    }
}
