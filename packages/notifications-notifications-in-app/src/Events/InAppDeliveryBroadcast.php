<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-in-app --force`.

declare(strict_types=1);

namespace Academorix\Notifications\NotificationsInApp\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * InAppTransport::send() finished writing the delivery row + queued the WebSocket broadcast. Fires exactly once per (tenant, user, notification).
 *
 * ## Consumers
 *
 * - notifications::MetricsCollector
 *
 * @category NotificationsInApp
 *
 * @since    0.1.0
 */
final readonly class InAppDeliveryBroadcast implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $notificationDeliveryId  ULID
     * @param  string  $notificationId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $userId  ULID
     * @param  string  $categorySlug  string
     * @param  string  $priority  'critical' | 'transactional' | 'product' | 'marketing'
     * @param  string  $title  string
     * @param  string  $bodyHtml  string
     * @param  string  $actionUrl  string | null
     * @param  string  $createdAt  ISO-8601
     */
    public function __construct(
        public readonly string $notificationDeliveryId,
        public readonly string $notificationId,
        public readonly string $tenantId,
        public readonly string $userId,
        public readonly string $categorySlug,
        public readonly string $priority,
        public readonly string $title,
        public readonly string $bodyHtml,
        public readonly string $actionUrl,
        public readonly string $createdAt,
    ) {
    }
}
