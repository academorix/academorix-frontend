<?php

/**
 * @file database/migrations/2026_07_15_120001_create_file_variants_table.php
 *
 * @description
 * Create the `file_variants` table for the `FileVariant` entity.
 * Owned by the storage module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform storage --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform storage --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('file_variants', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('file_id', 64);
            $table->foreign('file_id')->references('id')->on('files')->onDelete('cascade');
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('variant_key', 64);
            $table->string('generated_by_conversion', 128);
            $table->string('mime_type', 128);
            $table->integer('width')->nullable();
            $table->integer('height')->nullable();
            $table->unsignedBigInteger('size_bytes');
            $table->string('disk', 32);
            $table->string('path', 1024);
            $table->timestampTz('generated_at');
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['file_id', 'variant_key'], 'file_variants_key_unique');
            $table->index(['tenant_id', 'variant_key'], 'file_variants_tenant_key_idx');
            $table->index(['file_id'], 'file_variants_file_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('file_variants');
    }
};
