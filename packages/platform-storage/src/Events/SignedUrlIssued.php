<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform storage --force`.

declare(strict_types=1);

namespace Academorix\Platform\Storage\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A signed URL was issued and its SignedUrlAudit row committed. High-volume; sampled to config('storage.telemetry.signed_url_issued_sample_ratio', 0.1).
 *
 * @category Storage
 *
 * @since    0.1.0
 */
final readonly class SignedUrlIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $signedUrlAuditId  sua_ULID
     * @param  string  $fileId  fil_ULID
     * @param  string  $tenantId  ULID
     * @param  string  $issuedToUserId  ULID | null
     * @param  string  $reason  string
     * @param  int  $ttlSeconds  integer
     * @param  string  $expiresAt  ISO-8601
     */
    public function __construct(
        public readonly string $signedUrlAuditId,
        public readonly string $fileId,
        public readonly string $tenantId,
        public readonly string $issuedToUserId,
        public readonly string $reason,
        public readonly int $ttlSeconds,
        public readonly string $expiresAt,
    ) {
    }
}
