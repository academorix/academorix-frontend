<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform storage --force`.

declare(strict_types=1);

namespace Academorix\Platform\Storage\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * File row committed with virus_scan_state=pending. AV scan queued.
 *
 * ## Consumers
 *
 * - storage::QueueAntivirusScan
 * - storage::QueueVariantGeneration
 * - activity::WriteToActivityLog
 * - audit::WriteAuditEntry
 * - search::IndexUploadedFile (opt-in per tenant)
 *
 * @category Storage
 *
 * @since    0.1.0
 */
final readonly class FileUploaded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $fileId  fil_ULID
     * @param  string  $tenantId  ULID
     * @param  string  $ownerId  ULID | null
     * @param  string  $kind  string
     * @param  string  $collection  string
     * @param  string  $filename  string
     * @param  string  $mimeType  string
     * @param  int  $sizeBytes  integer
     * @param  string  $sha256  string (64 hex)
     * @param  string  $fileableType  string | null
     * @param  string  $fileableId  string | null
     * @param  string  $visibility  'public' | 'tenant' | 'restricted'
     */
    public function __construct(
        public readonly string $fileId,
        public readonly string $tenantId,
        public readonly string $ownerId,
        public readonly string $kind,
        public readonly string $collection,
        public readonly string $filename,
        public readonly string $mimeType,
        public readonly int $sizeBytes,
        public readonly string $sha256,
        public readonly string $fileableType,
        public readonly string $fileableId,
        public readonly string $visibility,
    ) {
    }
}
