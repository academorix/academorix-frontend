<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform storage --force`.

declare(strict_types=1);

namespace Academorix\Platform\Storage\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * archived_at was set (single=true replacement OR user-initiated archive). Distinct from FileErased — archive keeps the blob until retention window elapses.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 *
 * @category Storage
 *
 * @since    0.1.0
 */
final readonly class FileArchived implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $fileId  fil_ULID
     * @param  string  $tenantId  ULID
     * @param  string  $archivedBy  ULID | null
     * @param  string  $reason  'replaced' | 'user_archived' | 'kind_retention_policy'
     */
    public function __construct(
        public readonly string $fileId,
        public readonly string $tenantId,
        public readonly string $archivedBy,
        public readonly string $reason,
    ) {
    }
}
