<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform storage --force`.

declare(strict_types=1);

namespace Academorix\Platform\Storage\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The upload byte stream reached the server + is being persisted. Fires INSIDE the DB transaction that creates the File row (afterCommit=false) so same-transaction sub-work (audit trail insert, quota reservation) lands atomically.
 *
 * ## Consumers
 *
 * - storage::EnforceUploadQuotaListener
 *
 * @category Storage
 *
 * @since    0.1.0
 */
final readonly class FileUploading implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $ownerId  ULID | null
     * @param  string  $kind  string
     * @param  int  $declaredSizeBytes  integer
     * @param  string  $declaredMimeType  string
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $ownerId,
        public readonly string $kind,
        public readonly int $declaredSizeBytes,
        public readonly string $declaredMimeType,
    ) {
    }
}
