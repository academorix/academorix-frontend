<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform storage --force`.

declare(strict_types=1);

namespace Academorix\Platform\Storage\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Platform\Storage\Contracts\Data\FileInterface;
use Academorix\Platform\Storage\Database\Factories\FileFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Mattiverse\Userstamps\Traits\Userstamps;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a File.
 *
 * The primary domain entity of the storage module.
 *
 * @category Storage
 *
 * @since    0.1.0
 */
#[Table(name: FileInterface::TABLE, keyType: FileInterface::KEY_TYPE)]
#[Fillable([
    FileInterface::ATTR_TENANT_ID,
        FileInterface::ATTR_OWNER_ID,
        FileInterface::ATTR_FILEABLE_TYPE,
        FileInterface::ATTR_FILEABLE_ID,
        FileInterface::ATTR_KIND,
        FileInterface::ATTR_COLLECTION,
        FileInterface::ATTR_FILENAME,
        FileInterface::ATTR_NAME,
        FileInterface::ATTR_MIME_TYPE,
        FileInterface::ATTR_SIZE_BYTES,
        FileInterface::ATTR_SHA256,
        FileInterface::ATTR_DISK,
        FileInterface::ATTR_PATH,
        FileInterface::ATTR_VISIBILITY,
        FileInterface::ATTR_VIRUS_SCAN_STATE,
        FileInterface::ATTR_VIRUS_SCAN_RESULT,
        FileInterface::ATTR_SCANNED_AT,
        FileInterface::ATTR_DEDUPABLE,
        FileInterface::ATTR_REFERENCE_COUNT,
        FileInterface::ATTR_GENERATED_VARIANT_KEYS,
        FileInterface::ATTR_IS_SYSTEM,
        FileInterface::ATTR_ARCHIVED_AT,
        FileInterface::ATTR_CUSTOM_PROPERTIES,
        FileInterface::ATTR_METADATA,
])]
#[UseFactory(FileFactory::class)]
final class File extends Model implements FileInterface
{
    use HasFactory;
    use HasUlids;
    // TODO(gen): resolve unknown trait `HasSystemFlag` — add its import + use line.
    use BelongsToTenant;
    use HasMetadata;
    use HasUserstamps;
    // TODO(gen): resolve unknown trait `HasAuditable` — add its import + use line.
    use HasActivityLog;
    use Filterable;
    // TODO(gen): resolve unknown trait `IsFile` — add its import + use line.
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        FileInterface::ATTR_KIND => 'FileKindCast',
        FileInterface::ATTR_VISIBILITY => 'FileVisibility',
        FileInterface::ATTR_VIRUS_SCAN_STATE => 'VirusScanState',
        FileInterface::ATTR_SIZE_BYTES => 'integer',
        FileInterface::ATTR_IS_SYSTEM => 'boolean',
        FileInterface::ATTR_DEDUPABLE => 'boolean',
        FileInterface::ATTR_GENERATED_VARIANT_KEYS => 'array',
        FileInterface::ATTR_CUSTOM_PROPERTIES => 'array',
        FileInterface::ATTR_METADATA => 'array',
        FileInterface::ATTR_SCANNED_AT => 'datetime',
        FileInterface::ATTR_ARCHIVED_AT => 'datetime',
    ];
}
