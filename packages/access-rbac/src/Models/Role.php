<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Academorix\Access\Rbac\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Access\Rbac\Contracts\Data\RoleInterface;
use Academorix\Access\Rbac\Database\Factories\RoleFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Laravel\Scout\Searchable;
use Mattiverse\Userstamps\Traits\Userstamps;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a Role.
 *
 * Spatie's `roles` table augmented with Academorix scope columns (application_id, tenant_id, is_system, description, sort_order).
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
#[Table(name: RoleInterface::TABLE, keyType: RoleInterface::KEY_TYPE)]
#[Fillable([
    RoleInterface::ATTR_APPLICATION_ID,
        RoleInterface::ATTR_TENANT_ID,
        RoleInterface::ATTR_NAME,
        RoleInterface::ATTR_GUARD_NAME,
        RoleInterface::ATTR_IS_SYSTEM,
        RoleInterface::ATTR_DESCRIPTION,
        RoleInterface::ATTR_SORT_ORDER,
        RoleInterface::ATTR_METADATA,
])]
#[UseFactory(RoleFactory::class)]
final class Role extends Model implements RoleInterface
{
    use HasFactory;
    use HasUlids;
    // TODO(gen): resolve unknown trait `HasSystemFlag` — add its import + use line.
    use HasMetadata;
    use HasUserstamps;
    use HasActivityLog;
    use Filterable;
    use Searchable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        RoleInterface::ATTR_IS_SYSTEM => 'boolean',
        RoleInterface::ATTR_SORT_ORDER => 'integer',
        RoleInterface::ATTR_METADATA => 'array',
    ];
}
