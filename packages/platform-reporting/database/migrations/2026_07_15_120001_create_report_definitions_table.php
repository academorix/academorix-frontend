<?php

/**
 * @file database/migrations/2026_07_15_120001_create_report_definitions_table.php
 *
 * @description
 * Create the `report_definitions` table for the `ReportDefinition` entity.
 * Owned by the reporting module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform reporting --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform reporting --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('report_definitions', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30)->nullable();
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('code', 64);
            $table->string('name', 200);
            $table->string('kind', 24);
            $table->text('description')->nullable();
            $table->jsonb('parameters_schema')->nullable();
            $table->jsonb('output_columns')->default('[]');
            $table->string('sql_template_hash', 64)->nullable();
            $table->string('refresh_cadence', 16)->nullable();
            $table->string('audience_scope', 16)->default('tenant_admin');
            $table->boolean('is_system')->default('false');
            $table->jsonb('metadata')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->timestampTz('deleted_at')->nullable();
            $table->unique(['tenant_id', 'code'], 'report_definitions_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('report_definitions');
    }
};
