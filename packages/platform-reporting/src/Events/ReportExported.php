<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform reporting --force`.

declare(strict_types=1);

namespace Academorix\Platform\Reporting\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Report downloaded / emailed / API-consumed. Full audit trail per export.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention 7yr for PII/Financial/Medical)
 * - notifications::AlertOnBulkPiiExport
 *
 * @category Reporting
 *
 * @since    0.1.0
 */
final readonly class ReportExported implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $reportId  ULID
     * @param  string  $exportedByUserId  ULID
     * @param  string  $exportMethod  'download'|'email'|'api'|'scheduled_delivery'
     * @param  string  $sensitivity  string
     * @param  int  $rowCount  int
     */
    public function __construct(
        public readonly string $reportId,
        public readonly string $exportedByUserId,
        public readonly string $exportMethod,
        public readonly string $sensitivity,
        public readonly int $rowCount,
    ) {
    }
}
