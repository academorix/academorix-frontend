<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared search --force`.

declare(strict_types=1);

namespace Academorix\Shared\Search\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * One kind=click_through analytics event was persisted via POST /api/v1/search/click. Position is 1-based.
 *
 * ## Consumers
 *
 * - observability::MetricsCollector — click_through counter + position histogram
 *
 * @category Search
 *
 * @since    0.1.0
 */
final readonly class SearchClickThroughRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $searchAnalyticsEventId  ULID
     * @param  string  $tenantId  ULID | null
     * @param  string  $clickedResultType  string
     * @param  string  $clickedResultId  string
     * @param  int  $clickedPosition  integer
     */
    public function __construct(
        public readonly string $searchAnalyticsEventId,
        public readonly string $tenantId,
        public readonly string $clickedResultType,
        public readonly string $clickedResultId,
        public readonly int $clickedPosition,
    ) {
    }
}
