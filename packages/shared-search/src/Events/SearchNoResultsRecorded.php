<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared search --force`.

declare(strict_types=1);

namespace Academorix\Shared\Search\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * One kind=no_results analytics event was persisted. Zero-result queries indicate index gaps, synonym gaps, or user typos beyond the tolerance threshold — admin dashboards watch this.
 *
 * ## Consumers
 *
 * - observability::MetricsCollector — no_results counter increment
 *
 * @category Search
 *
 * @since    0.1.0
 */
final readonly class SearchNoResultsRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $searchAnalyticsEventId  ULID
     * @param  string  $tenantId  ULID | null
     * @param  string  $engine  string
     * @param  array  $indexNames  array<string>
     * @param  string  $queryHash  SHA-256
     */
    public function __construct(
        public readonly string $searchAnalyticsEventId,
        public readonly string $tenantId,
        public readonly string $engine,
        public readonly array $indexNames,
        public readonly string $queryHash,
    ) {
    }
}
