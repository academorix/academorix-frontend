<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared search --force`.

declare(strict_types=1);

namespace Academorix\Shared\Search\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SearchSyncJob transitioned to succeeded OR partially_succeeded. Fires from ReindexCoordinatorJob after final aggregation. Consumers filter on status for the partial variant.
 *
 * ## Consumers
 *
 * - activity::HasActivityLog
 * - audit::HasAudit
 * - SwapIndexAliasJob (chained; only for kind=reindex with status=succeeded)
 * - SendSearchSyncNotificationJob
 *
 * @category Search
 *
 * @since    0.1.0
 */
final readonly class SearchSyncJobCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $searchSyncJobId  ULID
     * @param  string  $kind  string
     * @param  string  $status  'succeeded' | 'partially_succeeded'
     * @param  array  $counters  counters object
     * @param  int  $targetVersion  integer
     * @param  string  $finishedAt  ISO 8601
     */
    public function __construct(
        public readonly string $searchSyncJobId,
        public readonly string $kind,
        public readonly string $status,
        public readonly array $counters,
        public readonly int $targetVersion,
        public readonly string $finishedAt,
    ) {
    }
}
