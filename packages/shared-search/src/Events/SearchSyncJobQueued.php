<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared search --force`.

declare(strict_types=1);

namespace Academorix\Shared\Search\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SearchSyncJob row inserted (status=queued). Fires from SearchSyncJobObserver.created.
 *
 * ## Consumers
 *
 * - activity::HasActivityLog
 * - audit::HasAudit
 * - SPA subscribes to user.{userId}.search + tenant.{tenantId}.search channels
 *
 * @category Search
 *
 * @since    0.1.0
 */
final readonly class SearchSyncJobQueued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $searchSyncJobId  ULID (ssync_)
     * @param  string  $searchIndexId  ULID (sidx_)
     * @param  string  $tenantId  ULID | null
     * @param  string  $causerType  string
     * @param  string  $causerId  string
     * @param  string  $kind  'reindex' | 'backfill' | 'flush' | 'alias_swap' | 'single_document'
     * @param  string  $source  'live' | 'xfer_artifact'
     */
    public function __construct(
        public readonly string $searchSyncJobId,
        public readonly string $searchIndexId,
        public readonly string $tenantId,
        public readonly string $causerType,
        public readonly string $causerId,
        public readonly string $kind,
        public readonly string $source,
    ) {
    }
}
