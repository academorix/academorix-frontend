<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Academorix\Access\Grants\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * GrantIssuer::issue() completes: row inserted, permissions validated, tenant + grantor guards passed. First domain event on the happy path.
 *
 * ## Consumers
 *
 * - grants::InvalidateGrantEvaluationCache (drop L1 + L2 cache entries for the subject)
 * - notifications::DispatchGrantIssuedNotification (email + in-app to subject)
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRow (per-column diff)
 * - sports::SyncAthleteAclCache (when resource_type=athlete)
 * - finance::SyncInvoiceAclCache (when resource_type=invoice)
 *
 * @category Grants
 *
 * @since    0.1.0
 */
final readonly class GrantIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $grant  AccessGrant
     * @param  string  $grantor  User|null
     * @param  string  $subject  Model (resolved via GrantableRegistry)
     * @param  string  $resource  Model (resolved via GrantableRegistry)
     * @param  string  $source  string (manual | access_request | delegation | bulk_provision)
     */
    public function __construct(
        public readonly string $grant,
        public readonly string $grantor,
        public readonly string $subject,
        public readonly string $resource,
        public readonly string $source,
    ) {
    }
}
