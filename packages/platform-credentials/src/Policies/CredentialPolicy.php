<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform credentials --force`.

declare(strict_types=1);

namespace Academorix\Platform\Credentials\Policies;

use Academorix\User\Models\User;
use Academorix\Platform\Credentials\Models\Credential;

/**
 * Authorization policy for {@see \Academorix\Platform\Credentials\Models\Credential}.
 *
 * Wired via `#[UsePolicy]` on the Model.
 *
 * @category Credentials
 *
 * @since    0.1.0
 */
final class CredentialPolicy
{
    /**
     * front_desk + branch_manager + admin + owner.
     *
     * @return bool
     */
    public function viewAny(User $user, Credential $model): bool
    {
        return $user->can('credentials.viewAny');
    }
    /**
     * view ability.
     *
     * @return bool
     */
    public function view(User $user, Credential $model): bool
    {
        return $user->can('credentials.view');
    }
    /**
     * front_desk + branch_manager + admin + owner
     *
     * @return bool
     */
    public function issue(User $user, Credential $model): bool
    {
        return $user->can('credentials.issue');
    }
    /**
     * front_desk (reason=lost only) + branch_manager + admin + owner
     *
     * @return bool
     */
    public function revoke(User $user, Credential $model): bool
    {
        return $user->can('credentials.revoke');
    }
    /**
     * holder self OR guardian OR staff
     *
     * @return bool
     */
    public function reportLost(User $user, Credential $model): bool
    {
        return $user->can('credentials.report-lost');
    }
}
