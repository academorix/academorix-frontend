<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform credentials --force`.

declare(strict_types=1);

namespace Academorix\Platform\Credentials\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P0 event. Same NFC UID scanned at two gates within impossible time window. Security incident.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention 7yr)
 * - safeguarding::CreateSecurityIncident
 * - notifications::AlertSecurityOfficer
 * - jobs::AutoRevokeSuspectedCloneJob
 *
 * @category Credentials
 *
 * @since    0.1.0
 */
final readonly class CloneUidDetected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $credentialId  ULID
     * @param  string  $gateAId  ULID
     * @param  string  $gateATime  ISO 8601
     * @param  string  $gateBId  ULID
     * @param  string  $gateBTime  ISO 8601
     * @param  float  $distanceKm  float
     * @param  int  $minTravelSecondsRequired  int
     */
    public function __construct(
        public readonly string $credentialId,
        public readonly string $gateAId,
        public readonly string $gateATime,
        public readonly string $gateBId,
        public readonly string $gateBTime,
        public readonly float $distanceKm,
        public readonly int $minTravelSecondsRequired,
    ) {
    }
}
