<?php

declare(strict_types=1);

use Academorix\Platform\Credentials\Models\Credential;

it('is a placeholder — replace with actual Credential unit tests', function (): void {
    expect(Credential::class)->toBeString();
});
