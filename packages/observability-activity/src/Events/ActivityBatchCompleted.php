<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability activity --force`.

declare(strict_types=1);

namespace Academorix\Observability\Activity\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ActivityBatcher.completeBatch(requestId) on request terminate. Fires when the request wrote at least one activity — empty batches emit no completion event.
 *
 * ## Consumers
 *
 * - activity::EmitBatchCollapsedFeedItem (renders 'Alice made N changes' as one collapsed row on the feed WebSocket channel)
 * - monitoring::EmitBatchLatencyMetric
 *
 * @category Activity
 *
 * @since    0.1.0
 */
final readonly class ActivityBatchCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $batchUuid  string
     * @param  string  $actor  User|null
     * @param  int  $activitiesWritten  integer
     * @param  int  $durationMs  integer
     * @param  array  $logNamesTouched  array<string>
     */
    public function __construct(
        public readonly string $batchUuid,
        public readonly string $actor,
        public readonly int $activitiesWritten,
        public readonly int $durationMs,
        public readonly array $logNamesTouched,
    ) {
    }
}
