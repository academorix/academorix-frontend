<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability activity --force`.

declare(strict_types=1);

namespace Academorix\Observability\Activity\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PATCH /api/v1/activity-retention-policies/{policy} completed — retention_days or override_reason or expires_at changed. Fires only when the diff is non-trivial (skipping when only updated_at / updated_by moved).
 *
 * ## Consumers
 *
 * - compliance::EmitRetentionOverrideChangedSignal
 * - notifications::MaybeDispatchRegulatorHoldNotification (fires when the threshold is crossed either way)
 *
 * @category Activity
 *
 * @since    0.1.0
 */
final readonly class ActivityRetentionPolicyUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $policy  ActivityRetentionPolicy
     * @param  string  $actor  User
     * @param  array  $dirtyFields  array<string>
     * @param  array  $priorValues  array<string, mixed>
     * @param  bool  $crossesRegulatorThreshold  boolean (prior <= 365 && now > 365, or vice versa)
     */
    public function __construct(
        public readonly string $policy,
        public readonly string $actor,
        public readonly array $dirtyFields,
        public readonly array $priorValues,
        public readonly bool $crossesRegulatorThreshold,
    ) {
    }
}
