<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-sms --force`.

declare(strict_types=1);

namespace Academorix\Notifications\NotificationsSms\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Permanent failure (invalid number, unreachable carrier). Adds to SmsOptOut with reason=invalid_number.
 *
 * ## Consumers
 *
 * - notifications::UpdateDeliveryFailedListener
 * - notifications-sms::AddOptOutListener
 *
 * @category NotificationsSms
 *
 * @since    0.1.0
 */
final readonly class SmsUndeliverable implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $notificationId  ULID
     * @param  string  $deliveryId  ULID
     * @param  string  $provider  string
     * @param  string  $phone  string (E.164)
     * @param  string  $errorCode  string
     */
    public function __construct(
        public readonly string $notificationId,
        public readonly string $deliveryId,
        public readonly string $provider,
        public readonly string $phone,
        public readonly string $errorCode,
    ) {
    }
}
