<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-push --force`.

declare(strict_types=1);

namespace Academorix\Notifications\NotificationsPush\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SendPushJob completed — provider accepted the payload + returned message id.
 *
 * ## Consumers
 *
 * - notifications::UpdateDeliverySentListener
 *
 * @category NotificationsPush
 *
 * @since    0.1.0
 */
final readonly class PushSent implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $notificationId  ULID
     * @param  string  $deliveryId  ULID
     * @param  string  $subscriptionId  ULID
     * @param  string  $provider  'fcm' | 'apns' | 'expo' | 'onesignal'
     * @param  string  $providerMessageId  string
     * @param  string  $platform  string
     * @param  string  $sentAt  ISO 8601
     */
    public function __construct(
        public readonly string $notificationId,
        public readonly string $deliveryId,
        public readonly string $subscriptionId,
        public readonly string $provider,
        public readonly string $providerMessageId,
        public readonly string $platform,
        public readonly string $sentAt,
    ) {
    }
}
