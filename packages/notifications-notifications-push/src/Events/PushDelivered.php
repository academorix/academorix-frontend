<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-push --force`.

declare(strict_types=1);

namespace Academorix\Notifications\NotificationsPush\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Provider webhook confirms endpoint delivery (FCM success or APNs feedback of successful delivery).
 *
 * ## Consumers
 *
 * - notifications::UpdateDeliveryDeliveredListener
 *
 * @category NotificationsPush
 *
 * @since    0.1.0
 */
final readonly class PushDelivered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $notificationId  ULID
     * @param  string  $deliveryId  ULID
     * @param  string  $provider  string
     * @param  string  $deliveredAt  ISO 8601
     */
    public function __construct(
        public readonly string $notificationId,
        public readonly string $deliveryId,
        public readonly string $provider,
        public readonly string $deliveredAt,
    ) {
    }
}
