<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-push --force`.

declare(strict_types=1);

namespace Academorix\Notifications\NotificationsPush\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Provider reports the device token is no longer valid (uninstalled app, revoked notification permission, token rotated). Fires from either SendPushJob (immediate at send) or IngestPushProviderWebhookJob (via APNs Feedback Service).
 *
 * ## Consumers
 *
 * - notifications-push::ExpireSubscriptionListener
 *
 * @category NotificationsPush
 *
 * @since    0.1.0
 */
final readonly class PushInvalidToken implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $subscriptionId  ULID
     * @param  string  $provider  string
     * @param  string  $reason  'invalid_token' | 'unregistered' | 'permission_revoked'
     * @param  string  $reportedAt  ISO 8601
     */
    public function __construct(
        public readonly string $subscriptionId,
        public readonly string $provider,
        public readonly string $reason,
        public readonly string $reportedAt,
    ) {
    }
}
