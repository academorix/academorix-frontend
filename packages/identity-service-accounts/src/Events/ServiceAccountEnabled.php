<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity service-accounts --force`.

declare(strict_types=1);

namespace Academorix\Identity\ServiceAccounts\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * is_enabled transitioned from false to true. Fires only for genuine re-enables — the initial creation fires ServiceAccountCreated, not this.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitServiceAccountEnabledMetric
 * - cache::InvalidateServiceAccountLookup
 *
 * @category ServiceAccounts
 *
 * @since    0.1.0
 */
final readonly class ServiceAccountEnabled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $serviceAccountId  ULID
     * @param  string  $applicationId  ULID
     * @param  string  $actorPlatformUserId  ULID
     * @param  int  $disabledDurationDays  integer (days between last disable and this enable)
     * @param  string  $enabledAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $serviceAccountId,
        public readonly string $applicationId,
        public readonly string $actorPlatformUserId,
        public readonly int $disabledDurationDays,
        public readonly string $enabledAt,
    ) {
    }
}
