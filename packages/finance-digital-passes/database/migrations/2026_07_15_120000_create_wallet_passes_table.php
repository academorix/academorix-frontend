<?php

/**
 * @file database/migrations/2026_07_15_120000_create_wallet_passes_table.php
 *
 * @description
 * Create the `wallet_passes` table for the `WalletPass` entity.
 * Owned by the digital-passes module. Emitted from the module blueprint —
 * regenerate via `generate-module.py finance digital-passes --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance digital-passes --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('wallet_passes', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('holder_type', 32);
            $table->string('holder_id', 30);
            $table->string('membership_id', 30)->nullable();
            $table->foreign('membership_id')->references('id')->on('memberships')->onDelete('cascade');
            $table->string('kind', 24);
            $table->string('provider', 16);
            $table->string('serial', 128);
            $table->string('status', 16)->default('issued');
            $table->timestampTz('installed_at')->nullable();
            $table->string('device_id', 200)->nullable();
            $table->string('push_token', 500)->nullable();
            $table->timestampTz('expires_at')->nullable();
            $table->timestampTz('revoked_at')->nullable();
            $table->string('revoked_reason', 200)->nullable();
            $table->text('qr_payload')->nullable();
            $table->string('pass_document_id', 30)->nullable();
            $table->foreign('pass_document_id')->references('id')->on('files')->onDelete('set null');
            $table->jsonb('metadata')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->timestampTz('deleted_at')->nullable();
            $table->unique(['tenant_id', 'serial'], 'wallet_passes_serial_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'holder_type', 'holder_id'], 'wallet_passes_holder_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('wallet_passes');
    }
};
