<?php

/**
 * @file database/migrations/2026_07_15_120002_create_facilities_table.php
 *
 * @description
 * Create the `facilities` table for the `Facility` entity.
 * Owned by the facility module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform facility --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('facilities', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('branch_id', 64);
            $table->foreign('branch_id')->references('id')->on('branches')->onDelete('restrict');
            $table->string('name', 200);
            $table->string('slug', 200);
            $table->text('description')->nullable();
            $table->string('facility_type', 32);
            $table->string('sport_category', 64)->nullable();
            $table->integer('capacity_persons')->default(1);
            $table->integer('min_booking_duration_minutes')->default(60);
            $table->integer('max_booking_duration_minutes')->default(240);
            $table->integer('booking_slot_step_minutes')->default(30);
            $table->integer('advance_booking_window_days')->default(30);
            $table->integer('cancellation_notice_hours')->default(24);
            $table->jsonb('availability_json')->default('\'{}\'::jsonb');
            $table->jsonb('blackout_dates')->default('\'[]\'::jsonb');
            $table->jsonb('pricing_json');
            $table->jsonb('image_urls')->nullable();
            $table->jsonb('amenities')->nullable();
            $table->string('status', 16)->default('active');
            $table->timestampTz('retired_at')->nullable();
            $table->integer('sort_order')->default(0);
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 64)->nullable();
            $table->string('updated_by', 64)->nullable();
            $table->string('deleted_by', 64)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'branch_id', 'slug'], 'facilities_tenant_branch_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'branch_id', 'status'], 'facilities_status_idx');
            $table->index(['tenant_id', 'facility_type'], 'facilities_type_idx');
            $table->index(['tenant_id', 'sport_category'], 'facilities_sport_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('facilities');
    }
};
