<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Platform\Facility\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Pass.used_credits incremented by 1 during BookingCreated (when the booking booked_for_user has an active pass covering this facility). The observer atomically increments in the same transaction as the booking-create — a failure to consume aborts the booking.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - observability::MetricsCollector (increments academorix.facility.pass_credits.consumed)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
final readonly class PassCreditConsumed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $passId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $consumedCredits  integer (always 1 in Wave 2 — bulk consumption reserved for future)
     * @param  int  $usedCreditsAfter  integer
     * @param  int  $totalCredits  integer
     * @param  string  $consumingBookingId  ULID | null (null = day-pass consumption if we later route DayPass through Pass)
     */
    public function __construct(
        public readonly string $passId,
        public readonly string $tenantId,
        public readonly int $consumedCredits,
        public readonly int $usedCreditsAfter,
        public readonly int $totalCredits,
        public readonly string $consumingBookingId,
    ) {
    }
}
