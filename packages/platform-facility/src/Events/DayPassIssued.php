<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Platform\Facility\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * DayPassRepository committed a new day_passes row from the POS surface. Fires exactly once per row.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - finance::AttributeDayPassRevenue (Wave 4)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
final readonly class DayPassIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $dayPassId  ULID (dpa_*)
     * @param  string  $tenantId  ULID
     * @param  string  $branchId  ULID
     * @param  string  $issuedToUserId  ULID | null
     * @param  string  $issuedToProfileName  string | null (present for anonymous)
     * @param  string  $validOnDate  YYYY-MM-DD
     * @param  int  $priceAmountCents  integer
     * @param  string  $priceCurrency  string (ISO 4217)
     * @param  string  $paymentMethod  'cash' | 'card' | 'wallet' | 'free' | 'comp'
     * @param  string  $issuedByUserId  ULID
     * @param  string  $receiptNumber  string
     */
    public function __construct(
        public readonly string $dayPassId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $issuedToUserId,
        public readonly string $issuedToProfileName,
        public readonly string $validOnDate,
        public readonly int $priceAmountCents,
        public readonly string $priceCurrency,
        public readonly string $paymentMethod,
        public readonly string $issuedByUserId,
        public readonly string $receiptNumber,
    ) {
    }
}
