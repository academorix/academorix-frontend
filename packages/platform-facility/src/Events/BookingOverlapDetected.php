<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Platform\Facility\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CRITICAL — the SERIALIZABLE overlap-detection observer detected a double-booking after the fact. Fires ONLY as a paranoia safety net (e.g. a raw SQL write bypassed the observer, or a race in a non-SERIALIZABLE transaction slipped through). Every fire indicates ops attention is required. This event exists so alerting has a first-class signal to hook — it should NEVER fire in production.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::AlertOpsOnBookingOverlap (page the on-call)
 * - observability::MetricsCollector (increments academorix.facility.overlaps.detected — this counter should stay at 0)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
final readonly class BookingOverlapDetected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $facilityId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $bookingAId  ULID
     * @param  string  $bookingBId  ULID
     * @param  string  $overlapWindowStartsAt  ISO 8601
     * @param  string  $overlapWindowEndsAt  ISO 8601
     * @param  string  $detectedAt  ISO 8601
     * @param  string  $detectedBy  'observer' | 'reconciler_job' | 'audit_scan'
     */
    public function __construct(
        public readonly string $facilityId,
        public readonly string $tenantId,
        public readonly string $bookingAId,
        public readonly string $bookingBId,
        public readonly string $overlapWindowStartsAt,
        public readonly string $overlapWindowEndsAt,
        public readonly string $detectedAt,
        public readonly string $detectedBy,
    ) {
    }
}
