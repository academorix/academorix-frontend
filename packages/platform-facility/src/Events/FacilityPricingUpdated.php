<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Platform\Facility\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Facility.pricing_json JSONB blob mutated. Any sub-field change (model, currency, tiers[], peak_hours) fires this event. Financial-record retention 7 years applies to the resulting audit row.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - finance::InvalidatePricingCache (Wave 4)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
final readonly class FacilityPricingUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $facilityId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $updatedBy  ULID
     * @param  array  $before  object (previous pricing_json snapshot)
     * @param  array  $after  object (new pricing_json snapshot)
     * @param  string  $changedFields  string[] (dotted paths, e.g. ['tiers[0].rate', 'peak_hours.friday'])
     */
    public function __construct(
        public readonly string $facilityId,
        public readonly string $tenantId,
        public readonly string $updatedBy,
        public readonly array $before,
        public readonly array $after,
        public readonly string $changedFields,
    ) {
    }
}
