<?php

/**
 * @file database/migrations/2026_07_15_120001_create_attendance_policies_table.php
 *
 * @description
 * Create the `attendance_policies` table for the `AttendancePolicy` entity.
 * Owned by the attendance module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports attendance --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('attendance_policies', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('branch_id', 30)->nullable();
            $table->foreign('branch_id')->references('id')->on('branches')->onDelete('restrict');
            $table->string('membership_plan_id', 30)->nullable();
            $table->foreign('membership_plan_id')->references('id')->on('membership_plans')->onDelete('restrict');
            $table->string('name', 128);
            $table->integer('grace_period_minutes')->default(15);
            $table->integer('late_after_minutes')->default(30);
            $table->string('late_arrival_penalty_type', 32)->default('notify_only');
            $table->unsignedBigInteger('no_show_fee_cents')->nullable();
            $table->string('no_show_fee_currency', 3)->nullable();
            $table->integer('max_unexcused_absences_per_period')->default(3);
            $table->integer('absence_freeze_period_days')->default(30);
            $table->jsonb('auto_excuse_reasons')->nullable();
            $table->jsonb('notification_config')->nullable();
            $table->boolean('is_active')->default(true);
            $table->timestampTz('effective_from')->nullable();
            $table->timestampTz('effective_until')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'branch_id', 'membership_plan_id'], 'attendance_policies_active_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_active = true AND deleted_at IS NULL
            $table->index(['tenant_id', 'is_active'], 'attendance_policies_active_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('attendance_policies');
    }
};
