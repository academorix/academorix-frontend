<?php

/**
 * @file database/migrations/2026_07_15_120002_create_attendance_records_table.php
 *
 * @description
 * Create the `attendance_records` table for the `AttendanceRecord` entity.
 * Owned by the attendance module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports attendance --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('attendance_records', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('session_id', 30);
            $table->foreign('session_id')->references('id')->on('sessions')->onDelete('restrict');
            $table->string('athlete_id', 30);
            $table->foreign('athlete_id')->references('id')->on('athletes')->onDelete('restrict');
            $table->string('athlete_enrollment_id', 30);
            $table->foreign('athlete_enrollment_id')->references('id')->on('athlete_enrollments')->onDelete('restrict');
            $table->string('consumed_pass_id', 30)->nullable();
            $table->foreign('consumed_pass_id')->references('id')->on('passes')->onDelete('restrict');
            $table->string('membership_id', 30)->nullable();
            $table->foreign('membership_id')->references('id')->on('memberships')->onDelete('restrict');
            $table->string('status', 24)->default('present');
            $table->timestampTz('checked_in_at');
            $table->string('checked_in_via', 24);
            $table->string('checked_in_by_user_id', 30)->nullable();
            $table->foreign('checked_in_by_user_id')->references('id')->on('users')->onDelete('restrict');
            $table->timestampTz('checked_out_at')->nullable();
            $table->string('left_early_reason', 256)->nullable();
            $table->text('session_notes')->nullable();
            $table->string('signature_url_signed', 512)->nullable();
            $table->string('device_fingerprint_hash', 64)->nullable();
            $table->string('ip_address')->nullable();
            $table->decimal('gps_lat', 15, 4)->nullable();
            $table->decimal('gps_lng', 15, 4)->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'session_id', 'athlete_id'], 'attendance_records_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'session_id', 'checked_in_at'], 'attendance_records_by_session_time_idx');
            $table->index(['tenant_id', 'athlete_id', 'checked_in_at'], 'attendance_records_by_athlete_time_idx');
            $table->index(['tenant_id', 'consumed_pass_id'], 'attendance_records_consumed_pass_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE consumed_pass_id IS NOT NULL
            $table->index(['tenant_id', 'device_fingerprint_hash'], 'attendance_records_fingerprint_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE device_fingerprint_hash IS NOT NULL
            $table->index(['tenant_id', 'status'], 'attendance_records_status_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('attendance_records');
    }
};
