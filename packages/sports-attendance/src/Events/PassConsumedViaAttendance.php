<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Sports\Attendance\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Bridges finance::PassConsumed to attendance context. Fires alongside AttendanceRecorded when a Pass was consumed as part of the check-in. Distinct from finance's own PassConsumed which is the raw money-side event.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
final readonly class PassConsumedViaAttendance implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attendanceRecordId  ULID
     * @param  string  $consumedPassId  ULID (pss_*)
     * @param  string  $membershipId  ULID (mbr_*)
     * @param  string  $tenantId  ULID
     * @param  string  $consumedAt  ISO 8601
     */
    public function __construct(
        public readonly string $attendanceRecordId,
        public readonly string $consumedPassId,
        public readonly string $membershipId,
        public readonly string $tenantId,
        public readonly string $consumedAt,
    ) {
    }
}
