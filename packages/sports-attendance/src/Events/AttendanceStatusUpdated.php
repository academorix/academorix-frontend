<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Sports\Attendance\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Attendance status mutated within the 48h post-session correction window (present → late-marked-explicitly, or notes added).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
final readonly class AttendanceStatusUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attendanceRecordId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromStatus  string
     * @param  string  $toStatus  string
     * @param  string  $changedByUserId  ULID
     * @param  string  $reason  string | null
     */
    public function __construct(
        public readonly string $attendanceRecordId,
        public readonly string $tenantId,
        public readonly string $fromStatus,
        public readonly string $toStatus,
        public readonly string $changedByUserId,
        public readonly string $reason,
    ) {
    }
}
