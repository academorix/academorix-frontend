<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform organization --force`.

declare(strict_types=1);

namespace Academorix\Platform\Organization\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * One or more mutable fields changed. Distinct from ParentChanged / DefaultChanged / StatusChanged / BrandingUpdated — those are the SPECIFIC events; this fires for everything else (name / slug / short_name / description / website_url / contact fields / sort_order / established_date / category).
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 *
 * @category Organization
 *
 * @since    0.1.0
 */
final readonly class OrganizationUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $organization  Organization
     * @param  string  $changedFields  string[]
     * @param  array  $oldValues  object (keyed by changed field)
     * @param  string  $actorId  string | null
     */
    public function __construct(
        public readonly string $organization,
        public readonly string $changedFields,
        public readonly array $oldValues,
        public readonly string $actorId,
    ) {
    }
}
