<?php

/**
 * @file database/migrations/2026_07_15_120000_create_organizations_table.php
 *
 * @description
 * Create the `organizations` table for the `Organization` entity.
 * Owned by the organization module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform organization --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform organization --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('organizations', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('parent_id', 30)->nullable();
            $table->foreign('parent_id')->references('id')->on('organizations')->onDelete('set null');
            $table->string('name', 128);
            $table->string('slug', 64);
            $table->string('short_name', 32)->nullable();
            $table->text('description')->nullable();
            $table->string('category', 32);
            $table->string('status', 16)->default('active');
            $table->boolean('is_default')->default(false);
            $table->integer('sort_order')->default(100);
            $table->string('logo_url', 512)->nullable();
            $table->string('primary_color', 7)->nullable();
            $table->string('secondary_color', 7)->nullable();
            $table->string('website_url', 512)->nullable();
            $table->string('support_email', 254)->nullable();
            $table->string('support_phone', 20)->nullable();
            $table->string('contact_person_name', 128)->nullable();
            $table->date('established_date')->nullable();
            $table->string('tree_path', 1024)->default('');
            $table->integer('tree_depth')->default(0);
            $table->text('notes')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'slug'], 'organizations_tenant_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->unique(['tenant_id', 'is_default'], 'organizations_tenant_default_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_default = true AND deleted_at IS NULL
            $table->index(['tenant_id', 'parent_id'], 'organizations_tenant_parent_idx');
            $table->index(['tenant_id', 'status', 'sort_order'], 'organizations_tenant_status_sort_idx');
            $table->index(['tree_path'], 'organizations_tree_path_idx');
            $table->index(['tenant_id', 'tree_depth'], 'organizations_tenant_depth_idx');
            $table->index(['deleted_at'], 'organizations_deleted_at_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('organizations');
    }
};
