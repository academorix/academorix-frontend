/**
 * @file processor-options.interface.ts
 * @module @stackra/queue/core/interfaces
 *
 * @description
 * Legacy re-export of `IProcessorOptions`. The canonical shape now
 * lives in `@stackra/contracts/interfaces/queue`. New code should
 * import from `@stackra/contracts` directly.
 */

export type { IProcessorOptions } from "@stackra/contracts";
