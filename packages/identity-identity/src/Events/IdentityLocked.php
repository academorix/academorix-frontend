<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity identity --force`.

declare(strict_types=1);

namespace Academorix\Identity\Identity\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * failed_attempts_count reached `identity.lockout.max_failed_attempts` (default 5). locked_until set to now() + `lockout_minutes` (progressive backoff when consecutive lockouts occur within one hour).
 *
 * ## Consumers
 *
 * - notifications::DispatchAccountLockedNotification
 * - notifications::MaybeDispatchSuspiciousLoginNotification
 * - activity::WriteToActivityLog
 * - observability::EmitIdentityLockedMetric
 *
 * @category Identity
 *
 * @since    0.1.0
 */
final readonly class IdentityLocked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $lockedUntil  ISO-8601 timestamp
     * @param  int  $failedAttemptsCount  integer
     * @param  int  $lockoutNumber  integer (1st, 2nd, 3rd … lockout in the current hour — drives progressive backoff)
     * @param  string  $requestIp  string
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $lockedUntil,
        public readonly int $failedAttemptsCount,
        public readonly int $lockoutNumber,
        public readonly string $requestIp,
    ) {
    }
}
