<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity identity --force`.

declare(strict_types=1);

namespace Academorix\Identity\Identity\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /api/v1/auth/password/forgot passed rate-limit + resolved a real Identity. NEVER fired when the email is unknown (email-enumeration safety — the endpoint still returns 200 so the caller can't distinguish).
 *
 * ## Consumers
 *
 * - notifications::DispatchPasswordResetNotification
 *
 * @category Identity
 *
 * @since    0.1.0
 */
final readonly class PasswordResetRequested implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $emailFingerprint  string
     * @param  string  $tokenExpiresAt  ISO-8601 timestamp
     * @param  string  $requestIp  string (IPv4 or IPv6)
     * @param  string  $userAgent  string (truncated to 255 chars)
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $emailFingerprint,
        public readonly string $tokenExpiresAt,
        public readonly string $requestIp,
        public readonly string $userAgent,
    ) {
    }
}
