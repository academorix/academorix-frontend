<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity identity --force`.

declare(strict_types=1);

namespace Academorix\Identity\Identity\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Identity holder invoked the regenerate-recovery-codes flow. Old codes purged, new codes hashed + persisted, plaintext shown once in the response.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 *
 * @category Identity
 *
 * @since    0.1.0
 */
final readonly class RecoveryCodesRegenerated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $regeneratedAt  ISO-8601 timestamp
     * @param  int  $newCodesCount  integer
     * @param  string  $actorId  ULID (identity holder OR platform admin)
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $regeneratedAt,
        public readonly int $newCodesCount,
        public readonly string $actorId,
    ) {
    }
}
