<?php

/**
 * @file database/migrations/2026_07_15_120000_create_identities_table.php
 *
 * @description
 * Create the `identities` table for the `Identity` entity.
 * Owned by the identity module. Emitted from the module blueprint —
 * regenerate via `generate-module.py identity identity --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity identity --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('identities', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('email');
            $table->text('password_hash');
            $table->text('mfa_secret_encrypted')->nullable();
            $table->jsonb('mfa_recovery_codes_hashed')->nullable();
            $table->timestampTz('email_verified_at')->nullable();
            $table->text('email_verification_token_hash')->nullable();
            $table->timestampTz('email_verification_expires_at')->nullable();
            $table->jsonb('password_history_hashed')->default('\'[]\'::jsonb');
            $table->timestampTz('password_changed_at')->default('now()');
            $table->timestampTz('last_login_at')->nullable();
            $table->timestampTz('last_failed_at')->nullable();
            $table->timestampTz('locked_until')->nullable();
            $table->integer('failed_attempts_count')->default(0);
            $table->date('dob')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['email'], 'identities_email_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['locked_until'], 'identities_locked_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE locked_until IS NOT NULL
            $table->index(['email_verified_at'], 'identities_unverified_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE email_verified_at IS NULL AND deleted_at IS NULL
            $table->index(['deleted_at'], 'identities_erasure_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NOT NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('identities');
    }
};
