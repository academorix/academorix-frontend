<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Finance\Refund\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Refund.status transitioned processing -> succeeded. Provider webhook OR CheckRefundProviderStatusJob confirmed terminal success. FIRES THE FINALIZATION CHAIN: credit note creation + offsetting transaction + invoice state update + clawback fan-out + marketing negative-conversion. P1 event.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - activity::WriteToActivityLog
 * - invoice::MarkInvoiceRefundedOnFullRefundSucceeded (state update + accumulate amount_refunded_cents)
 * - invoice::CreateCreditNoteOnRefundSucceeded (via CreditNoteIssuer — creates reversal invoice)
 * - transaction::RecordOffsettingTransactionOnRefundSucceeded (via TransactionRecorder — debit refunds_owed, credit cash)
 * - refund::ClawbackCoordinator (dispatches RefundClawbackReferral + RefundClawbackCoupon downstream)
 * - marketing::FireMarketingRefundIssuedOnRefundSucceeded (negative-value conversion — ad-network ROAS adjustment)
 * - notifications::DispatchRefundSucceededNotification (customer — money received; note: for cards, this is estimated delivery not exact)
 * - observability::MetricsCollector (academorix.refund.succeeded_total labelled by reason)
 *
 * @category Refund
 *
 * @since    0.1.0
 */
final readonly class RefundSucceeded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $refundId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $refundNumber  string
     * @param  string  $paymentId  ULID
     * @param  string  $invoiceId  ULID
     * @param  string  $creditNoteId  ULID (crn_*) | null (populated post-CreditNoteIssuer)
     * @param  string  $transactionId  ULID (trx_*) | null (populated post-TransactionRecorder)
     * @param  int  $amountCents  integer
     * @param  string  $currency  string
     * @param  int  $providerFeeReversalCents  integer | null
     * @param  string  $reason  string
     * @param  string  $refundType  string
     * @param  string  $provider  string
     * @param  string  $providerReferenceId  string | null
     * @param  string  $refundedTo  string
     * @param  string  $customerType  'user' | 'athlete'
     * @param  string  $customerId  ULID
     * @param  string  $initiatedByUserId  ULID
     * @param  string  $approvedByUserId  ULID | null
     * @param  string  $processedAt  ISO 8601
     */
    public function __construct(
        public readonly string $refundId,
        public readonly string $tenantId,
        public readonly string $refundNumber,
        public readonly string $paymentId,
        public readonly string $invoiceId,
        public readonly string $creditNoteId,
        public readonly string $transactionId,
        public readonly int $amountCents,
        public readonly string $currency,
        public readonly int $providerFeeReversalCents,
        public readonly string $reason,
        public readonly string $refundType,
        public readonly string $provider,
        public readonly string $providerReferenceId,
        public readonly string $refundedTo,
        public readonly string $customerType,
        public readonly string $customerId,
        public readonly string $initiatedByUserId,
        public readonly string $approvedByUserId,
        public readonly string $processedAt,
    ) {
    }
}
