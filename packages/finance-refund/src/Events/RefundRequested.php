<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Finance\Refund\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * RefundRepository finished a successful create. Fires exactly once per refund. Distinct from `RefundApproved` — this fires unconditionally at initiation, before approval routing.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial-audit)
 * - activity::WriteToActivityLog
 * - cache::InvalidateTenantRefundListCache
 * - notifications::DispatchRefundRequestedNotification (customer — request confirmation)
 *
 * @category Refund
 *
 * @since    0.1.0
 */
final readonly class RefundRequested implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $refundId  ULID (rfd_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $refundNumber  string (REF-2026-00000001)
     * @param  string  $paymentId  ULID (pay_*)
     * @param  string  $invoiceId  ULID (ivc_*)
     * @param  int  $amountCents  integer
     * @param  string  $currency  string (ISO 4217)
     * @param  string  $reason  'customer_requested' | 'product_not_received' | 'product_unacceptable' | 'duplicate_charge' | 'fraud_confirmed' | 'subscription_cancelled' | 'admin_correction' | 'chargeback_prevention' | 'regulator_required' | 'accounting_correction'
     * @param  string  $reasonNote  string
     * @param  string  $refundType  'full' | 'partial' | 'prorated'
     * @param  string  $refundedTo  'original_payment_method' | 'customer_balance' | 'bank_transfer' | 'check'
     * @param  bool  $requiresApproval  boolean
     * @param  string  $initiatedByUserId  ULID (usr_*)
     * @param  string  $initiatedAt  ISO 8601
     */
    public function __construct(
        public readonly string $refundId,
        public readonly string $tenantId,
        public readonly string $refundNumber,
        public readonly string $paymentId,
        public readonly string $invoiceId,
        public readonly int $amountCents,
        public readonly string $currency,
        public readonly string $reason,
        public readonly string $reasonNote,
        public readonly string $refundType,
        public readonly string $refundedTo,
        public readonly bool $requiresApproval,
        public readonly string $initiatedByUserId,
        public readonly string $initiatedAt,
    ) {
    }
}
