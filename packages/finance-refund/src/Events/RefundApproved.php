<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Finance\Refund\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Refund.status transitioned awaiting_approval -> approved. Approver clicked approve OR requires_approval=false path auto-transitioned at creation.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial-audit)
 * - activity::WriteToActivityLog
 * - notifications::DispatchRefundApprovedNotification (customer + admin)
 *
 * @category Refund
 *
 * @since    0.1.0
 */
final readonly class RefundApproved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $refundId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $approvedByUserId  ULID | null (null when auto-approved)
     * @param  string  $approvedAt  ISO 8601
     * @param  bool  $autoApproved  boolean
     */
    public function __construct(
        public readonly string $refundId,
        public readonly string $tenantId,
        public readonly string $approvedByUserId,
        public readonly string $approvedAt,
        public readonly bool $autoApproved,
    ) {
    }
}
