<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access requests --force`.

declare(strict_types=1);

namespace Academorix\Requests\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Requests module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Requests
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum RequestsPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `access-requests.viewAny` — List access requests where the caller participates (as requester, approver, or resource owner).
     */
    #[Label('List access requests where the caller participates (as requester, approver, or r')]
    #[Description('List access requests where the caller participates (as requester, approver, or resource owner). Seeded on every user — access requests are self-service by design.')]
    case AccessRequestsViewAny = 'access-requests.viewAny';

    /**
     * `access-requests.view` — Show a specific access request.
     */
    #[Label('Show a specific access request')]
    #[Description('Show a specific access request. Same participation-based visibility.')]
    case AccessRequestsView = 'access-requests.view';

    /**
     * `access-requests.create` — Submit a new access request.
     */
    #[Label('Submit a new access request')]
    #[Description('Submit a new access request. Seeded on every user by default — the whole point of the module is self-service.')]
    case AccessRequestsCreate = 'access-requests.create';

    /**
     * `access-requests.withdraw` — Withdraw your own pending request.
     */
    #[Label('Withdraw your own pending request')]
    #[Description('Withdraw your own pending request. Seeded on every user.')]
    case AccessRequestsWithdraw = 'access-requests.withdraw';

    /**
     * `access-requests.approve` — Approve a request where you're in the resolved approvers set.
     */
    #[Label('Approve a request where you\'re in the resolved approvers set')]
    #[Description('Approve a request where you\'re in the resolved approvers set. Seeded on every user — approver membership is data-driven (via workflow/approvals selectors + access/delegation), not seeded.')]
    case AccessRequestsApprove = 'access-requests.approve';

    /**
     * `access-requests.reject` — Reject a request where you're in the resolved approvers set.
     */
    #[Label('Reject a request where you\'re in the resolved approvers set')]
    #[Description('Reject a request where you\'re in the resolved approvers set.')]
    case AccessRequestsReject = 'access-requests.reject';

    /**
     * `access-requests.manage_any_tenant` — Tenant-admin scope.
     */
    #[Label('Tenant-admin scope')]
    #[Description('Tenant-admin scope. Grants visibility over every access request in the tenant, regardless of participation. Does NOT confer approve/reject authority — that still comes from the approver-resolver set.')]
    case AccessRequestsManageAnyTenant = 'access-requests.manage_any_tenant';

    /**
     * `access-requests.manage_any` — Cross-tenant read for Academorix ops (abuse investigation, compliance reporting, support tickets).
     */
    #[Label('Cross-tenant read for Academorix ops (abuse investigation, compliance reporting,')]
    #[Description('Cross-tenant read for Academorix ops (abuse investigation, compliance reporting, support tickets). Scoped to a small platform-admin role tier.')]
    case AccessRequestsManageAny = 'access-requests.manage_any';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::AccessRequestsViewAny => Guard::Sanctum,
            self::AccessRequestsView => Guard::Sanctum,
            self::AccessRequestsCreate => Guard::Sanctum,
            self::AccessRequestsWithdraw => Guard::Sanctum,
            self::AccessRequestsApprove => Guard::Sanctum,
            self::AccessRequestsReject => Guard::Sanctum,
            self::AccessRequestsManageAnyTenant => Guard::Sanctum,
            self::AccessRequestsManageAny => Guard::PlatformAdmin,
        };
    }
}
