<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Stackra\Rbac\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan rbac:list-principal-roles` — List principal roles — rbac command.
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'rbac:list-principal-roles',
    description: 'List principal roles — rbac command.',
)]
final class ListPrincipalRolesCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List principal roles — rbac command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List principal roles — rbac command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
