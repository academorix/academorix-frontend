<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Stackra\Rbac\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan rbac:purge-expired-assignments` — Purge expired assignments — rbac command.
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'rbac:purge-expired-assignments',
    description: 'Purge expired assignments — rbac command.',
)]
final class PurgeExpiredAssignmentsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Purge expired assignments — rbac command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Purge expired assignments — rbac command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
