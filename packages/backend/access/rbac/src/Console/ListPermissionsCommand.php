<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Stackra\Rbac\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan rbac:list-permissions` — List permissions — rbac command.
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'rbac:list-permissions',
    description: 'List permissions — rbac command.',
)]
final class ListPermissionsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List permissions — rbac command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List permissions — rbac command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
