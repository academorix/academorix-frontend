<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Stackra\Rbac\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * RbacSettings — Access control.
 *
 * Role + permission notification preferences + assignment lifecycle defaults.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'rbac',
    label: 'Access control',
    description: 'Role + permission notification preferences + assignment lifecycle defaults.',
    icon: 'shield-check',
    scope: 'tenant',
    permission: 'roles.viewAny.tenant',
    sortOrder: 20,
)]
class RbacSettings extends Settings
{

    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', description: 'Cadence + channels for RBAC-related notifications.', icon: 'bell-alert', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify members when roles are assigned', group: 'Notifications', sortOrder: 1, helpText: 'Off = only silent seed-time assignments; owner/admin-initiated assignments still notify.')]
    public bool $notify_on_role_assigned = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify members when roles are revoked', group: 'Notifications', sortOrder: 2, helpText: 'Cannot be disabled for revocations triggered by system-security events (breach response, principal_disabled).')]
    public bool $notify_on_role_revoked = true;

    #[SettingField(controlType: ControlType::Number, label: 'Days before expiry to send reminder', group: 'Notifications', sortOrder: 3, min: 1, max: 30, helpText: 'Framework floor: 1 day. Framework ceiling: 30 days.')]
    public int $assignment_expiring_reminder_days = 7;


    // ── Assignment defaults ──────────────────────────────────
    #[SettingGroup(label: 'Assignment defaults', description: 'Default behaviour for role assignments.', icon: 'adjustments-horizontal', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Require a reason on role assignments', group: 'Assignment defaults', sortOrder: 0, helpText: 'When on, admins must include a `reason` field on every assignment. Audit-friendly for regulated tenants.')]
    public bool $require_reason_on_assign = false;

    #[SettingField(controlType: ControlType::Toggle, label: 'Require a reason on role revocations', group: 'Assignment defaults', sortOrder: 0, helpText: 'Default on — revocations are more consequential than assignments.')]
    public bool $require_reason_on_revoke = true;

    #[SettingField(controlType: ControlType::Number, label: 'Default expiration window (days)', group: 'Assignment defaults', sortOrder: 0, min: 1, max: 365, helpText: 'Applied when an admin ticks \'time-bounded\' without specifying a specific date. Requires time_bounded_role_assignments entitlement.')]
    public int $default_time_bounded_duration_days = 90;


    // ── Custom roles ──────────────────────────────────
    #[SettingGroup(label: 'Custom roles', description: 'Custom role creation policy.', icon: 'sparkles', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Select, label: 'Naming convention', group: 'Custom roles', sortOrder: 0, options: ['snake_case' => 'snake_case (e.g. senior_coach)', 'kebab_case' => 'kebab-case (e.g. senior-coach)'], helpText: 'Applied by the tenant\'s custom-role naming validator. Framework floor is snake_case; this is a display preference.')]
    public string $custom_role_naming_convention = 'snake_case';

    #[SettingField(controlType: ControlType::Text, label: 'Default permissions on new custom roles', group: 'Custom roles', sortOrder: 0, helpText: 'When creating a custom role, pre-select these permissions. Speeds up common configurations.')]
    public array $custom_role_default_permission_set = [];

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'rbac';
    }
}
