<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Stackra\Rbac\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * `model_has_roles` row hard-deleted (spatie does not soft-delete pivots).
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteToAuditLog
 * - rbac::InvalidatePrincipalPermissionCache
 * - notifications::DispatchRoleRevokedNotification
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RoleRevokedFromPrincipal implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $roleId  ULID
     * @param  string  $roleName  string
     * @param  string  $principalType  string
     * @param  string  $principalId  ULID
     * @param  string  $trigger  'admin_revoke' | 'assignment_expired' | 'role_deleted' | 'principal_disabled'
     * @param  string  $actorId  ULID | null
     * @param  string  $revokedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $roleId,
        public readonly string $roleName,
        public readonly string $principalType,
        public readonly string $principalId,
        public readonly string $trigger,
        public readonly string $actorId,
        public readonly string $revokedAt,
    ) {
    }
}
