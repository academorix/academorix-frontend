<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Stackra\Rbac\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * `roles` row soft-deleted (`deleted_at` set). Custom roles only — system roles refuse deletion.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteToAuditLog
 * - rbac::InvalidateTenantRoleCatalogueCache
 * - rbac::PurgeAllPrincipalPermissionCachesForRole
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RoleDeleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $roleId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $name  string
     * @param  int  $principalCountBeforeDelete  integer
     * @param  string  $actorId  ULID
     * @param  string  $deletedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $roleId,
        public readonly string $tenantId,
        public readonly string $name,
        public readonly int $principalCountBeforeDelete,
        public readonly string $actorId,
        public readonly string $deletedAt,
    ) {
    }
}
