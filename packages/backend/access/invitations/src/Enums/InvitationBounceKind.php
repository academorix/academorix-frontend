<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access invitations --force`.

declare(strict_types=1);

namespace Stackra\Invitations\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `bounce_kind` on `invitation`.
 *
 * Enum: `hard` (permanent — email invalid), `soft` (transient — retryable), `complaint` (recipient marked as spam).
 *
 * @category Invitations
 *
 * @since    0.1.0
 */
enum InvitationBounceKind: string
{
    use Enum;

    case Hard = 'hard';
    case Soft = 'soft';
    case Complaint = 'complaint';
}
