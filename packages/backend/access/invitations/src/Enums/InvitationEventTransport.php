<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access invitations --force`.

declare(strict_types=1);

namespace Stackra\Invitations\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `transport` on `invitation-event`.
 *
 * Mail transport that reported this event: `sendgrid`, `ses`, `postmark`, `mailgun`, `twilio`, `slack`.
 *
 * @category Invitations
 *
 * @since    0.1.0
 */
enum InvitationEventTransport: string
{
    use Enum;

    case Sendgrid = 'sendgrid';
    case Ses = 'ses';
    case Postmark = 'postmark';
    case Mailgun = 'mailgun';
    case Twilio = 'twilio';
    case Slack = 'slack';
}
