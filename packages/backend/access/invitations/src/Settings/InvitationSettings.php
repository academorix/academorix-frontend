<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access invitations --force`.

declare(strict_types=1);

namespace Academorix\Invitations\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * InvitationSettings — Invitations.
 *
 * Expiry + resends + delivery channels + rate limits + sender identity + acceptance flow.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Invitations
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'invitations',
    label: 'Invitations',
    description: 'Expiry + resends + delivery channels + rate limits + sender identity + acceptance flow.',
    icon: 'envelope-open',
    scope: 'tenant',
    permission: 'invitations.send',
    sortOrder: 25,
)]
class InvitationSettings extends Settings
{

    // ── Expiry ──────────────────────────────────
    #[SettingGroup(label: 'Expiry', icon: 'clock', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Number, label: 'Default expiry (days)', validation: ['required', 'integer', 'min:1', 'max:90'], group: 'Expiry', sortOrder: 1, min: 1, max: 90, helpText: 'How long a new invitation stays valid before it auto-expires.')]
    public int $expires_after_days = 14;

    #[SettingField(controlType: ControlType::Toggle, label: 'Send a reminder 48h before expiry', validation: ['boolean'], group: 'Expiry', sortOrder: 2)]
    public bool $send_reminder = true;


    // ── Resends ──────────────────────────────────
    #[SettingGroup(label: 'Resends', icon: 'arrow-path', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Number, label: 'Max resends per invitation', validation: ['required', 'integer', 'min:0', 'max:10'], group: 'Resends', sortOrder: 1, min: 0, max: 10)]
    public int $max_resends = 3;


    // ── Delivery channels ──────────────────────────────────
    #[SettingGroup(label: 'Delivery channels', description: 'Which channels are available to invitees. Some channels require an active integration or an enterprise plan.', icon: 'signal', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Email (always available)', validation: ['boolean'], group: 'Delivery channels', sortOrder: 1)]
    public bool $channels_email = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'SMS', validation: ['boolean'], group: 'Delivery channels', sortOrder: 2)]
    public bool $channels_sms = false;

    #[SettingField(controlType: ControlType::Toggle, label: 'Slack', validation: ['boolean'], group: 'Delivery channels', sortOrder: 3)]
    public bool $channels_slack = false;


    // ── Sending limits ──────────────────────────────────
    #[SettingGroup(label: 'Sending limits', description: 'Per-inviter and per-tenant guardrails to prevent accidental over-sending. Enterprise plans can lift them via ops.', icon: 'chart-bar', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Number, label: 'Max per inviter per day', validation: ['required', 'integer', 'min:1', 'max:1000'], group: 'Sending limits', sortOrder: 1, min: 1, max: 1000)]
    public int $throttle_per_inviter_per_day = 50;

    #[SettingField(controlType: ControlType::Number, label: 'Max per tenant per day', validation: ['required', 'integer', 'min:1', 'max:10000'], group: 'Sending limits', sortOrder: 2, min: 1, max: 10000)]
    public int $throttle_per_tenant_per_day = 250;


    // ── Sender identity ──────────────────────────────────
    #[SettingGroup(label: 'Sender identity', description: 'How your invitation emails are branded.', icon: 'identification', sortOrder: 5)]
    #[SettingField(controlType: ControlType::Text, label: 'Sender name', validation: ['nullable', 'string', 'max:200'], group: 'Sender identity', sortOrder: 1, helpText: 'Empty = tenant name.')]
    public ?string $sender_name = null;

    #[SettingField(controlType: ControlType::Email, label: 'Reply-to email', validation: ['nullable', 'email', 'max:254'], group: 'Sender identity', sortOrder: 2, helpText: 'Empty = tenant\'s billing contact email.')]
    public ?string $sender_reply_to = null;


    // ── Acceptance flow ──────────────────────────────────
    #[SettingGroup(label: 'Acceptance flow', icon: 'check-badge', sortOrder: 6)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Require MFA for owner + admin invitations', validation: ['boolean'], group: 'Acceptance flow', sortOrder: 1, helpText: 'Invitees accepting an owner or admin role must enrol MFA during accept (they cannot skip).')]
    public bool $acceptance_require_mfa_for_admins = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Force SSO when tenant has SSO', validation: ['boolean'], group: 'Acceptance flow', sortOrder: 2, helpText: 'Invitations sent to a domain matching the tenant\'s SSO IdP route through SSO instead of password creation.')]
    public bool $acceptance_require_sso = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'invitations';
    }
}
