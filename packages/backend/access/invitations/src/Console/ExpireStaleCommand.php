<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access invitations --force`.

declare(strict_types=1);

namespace Academorix\Invitations\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan invitations:expire-stale` — Expire stale — invitations command.
 *
 * @category Invitations
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'invitations:expire-stale',
    description: 'Expire stale — invitations command.',
)]
final class ExpireStaleCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Expire stale — invitations command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Expire stale — invitations command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
