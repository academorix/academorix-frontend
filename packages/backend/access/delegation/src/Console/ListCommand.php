<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Stackra\Delegation\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan delegations:list` — List every delegations row.
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'delegations:list',
    description: 'List every delegations row.',
)]
final class ListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List every delegations row', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List every delegations row completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
