<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Stackra\Delegation\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan impersonation:list-active` — List active — impersonation command.
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'impersonation:list-active',
    description: 'List active — impersonation command.',
)]
final class ListActiveCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List active — impersonation command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List active — impersonation command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
