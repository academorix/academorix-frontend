<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Stackra\Delegation\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan impersonation:force-end` — Force end — impersonation command.
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'impersonation:force-end',
    description: 'Force end — impersonation command.',
)]
final class ForceEndCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Force end — impersonation command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Force end — impersonation command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
