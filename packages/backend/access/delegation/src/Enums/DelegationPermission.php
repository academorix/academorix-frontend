<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Academorix\Delegation\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Delegation module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum DelegationPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `delegations.viewAny` — Every tenant member can see their own delegations (as delegator + as delegate).
     */
    #[Label('Every tenant member can see their own delegations (as delegator + as delegate)')]
    #[Description('Every tenant member can see their own delegations (as delegator + as delegate). Admin sees all.')]
    case DelegationsViewAny = 'delegations.viewAny';

    /**
     * `delegations.view` — View a specific delegation — delegator, delegate, or admin.
     */
    #[Label('View a specific delegation — delegator, delegate, or admin')]
    #[Description('View a specific delegation — delegator, delegate, or admin.')]
    case DelegationsView = 'delegations.view';

    /**
     * `delegations.create` — Every tenant member can create outbound delegations of their own roles.
     */
    #[Label('Every tenant member can create outbound delegations of their own roles')]
    #[Description('Every tenant member can create outbound delegations of their own roles. Admin can create on behalf of another user via manage_any (rare — mostly break-glass).')]
    case DelegationsCreate = 'delegations.create';

    /**
     * `delegations.revoke` — The delegator can revoke their own outbound delegations.
     */
    #[Label('The delegator can revoke their own outbound delegations')]
    #[Description('The delegator can revoke their own outbound delegations. Admin can revoke any.')]
    case DelegationsRevoke = 'delegations.revoke';

    /**
     * `delegations.manage_any` — Admin-level cross-delegation management.
     */
    #[Label('Admin-level cross-delegation management')]
    #[Description('Admin-level cross-delegation management. Enables create-on-behalf-of + revoke-any + extend-ends-at (rare).')]
    case DelegationsManageAny = 'delegations.manage_any';

    /**
     * `platform.impersonate` — PERMISSION to invoke impersonation.
     */
    #[Label('PERMISSION to invoke impersonation')]
    #[Description('PERMISSION to invoke impersonation. Also requires the ENTITLEMENT `impersonation` on the PlatformUser row (see entitlements.json). Default-seeded on support department roles + super_admin. Product / engineering / other departments do NOT get this by default; must be granted per-PlatformUser by super_admin.')]
    case PlatformImpersonate = 'platform.impersonate';

    /**
     * `platform.impersonation.view` — View own + adjacent impersonation sessions.
     */
    #[Label('View own + adjacent impersonation sessions')]
    #[Description('View own + adjacent impersonation sessions. Regular support agents see their own; security + super_admin see all in scope.')]
    case PlatformImpersonationView = 'platform.impersonation.view';

    /**
     * `platform.impersonation.viewAll` — Cross-tenant listing of active + historical impersonation sessions.
     */
    #[Label('Cross-tenant listing of active + historical impersonation sessions')]
    #[Description('Cross-tenant listing of active + historical impersonation sessions.')]
    case PlatformImpersonationViewAll = 'platform.impersonation.viewAll';

    /**
     * `platform.impersonation.audit` — Read the compliance evidence report.
     */
    #[Label('Read the compliance evidence report')]
    #[Description('Read the compliance evidence report. DPO is included because they own the compliance evidence-review workflow.')]
    case PlatformImpersonationAudit = 'platform.impersonation.audit';

    /**
     * `platform.impersonation.terminate` — Force-terminate a session.
     */
    #[Label('Force-terminate a session')]
    #[Description('Force-terminate a session. Restricted to security + super_admin — even the impersonator\'s own /end route doesn\'t need this permission (they end via a different code path). Force-terminate is the LOCKDOWN action, distinct from self-service end.')]
    case PlatformImpersonationTerminate = 'platform.impersonation.terminate';

    /**
     * `platform.impersonation.extend` — Extend a session past its default TTL.
     */
    #[Label('Extend a session past its default TTL')]
    #[Description('Extend a session past its default TTL. Reserved for super_admin — even security cannot extend. Requires the target session to hold impersonation_extended_ttl entitlement (enterprise contract-negotiated).')]
    case PlatformImpersonationExtend = 'platform.impersonation.extend';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::DelegationsViewAny => Guard::Sanctum,
            self::DelegationsView => Guard::Sanctum,
            self::DelegationsCreate => Guard::Sanctum,
            self::DelegationsRevoke => Guard::Sanctum,
            self::DelegationsManageAny => Guard::Sanctum,
            self::PlatformImpersonate => Guard::PlatformAdmin,
            self::PlatformImpersonationView => Guard::PlatformAdmin,
            self::PlatformImpersonationViewAll => Guard::PlatformAdmin,
            self::PlatformImpersonationAudit => Guard::PlatformAdmin,
            self::PlatformImpersonationTerminate => Guard::PlatformAdmin,
            self::PlatformImpersonationExtend => Guard::PlatformAdmin,
        };
    }
}
