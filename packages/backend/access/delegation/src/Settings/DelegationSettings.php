<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Academorix\Delegation\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * DelegationSettings — Delegation.
 *
 * Tenant-wide delegation policy — window caps, notification behaviour, emergency-flow eligibility.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'delegation',
    label: 'Delegation',
    description: 'Tenant-wide delegation policy — window caps, notification behaviour, emergency-flow eligibility.',
    icon: 'user-group',
    scope: 'tenant',
    permission: 'delegations.viewAny',
    sortOrder: 20,
)]
class DelegationSettings extends Settings
{

    // ── Window policy ──────────────────────────────────
    #[SettingGroup(label: 'Window policy', description: 'Caps on delegation windows within this tenant. Cannot exceed the platform hard cap of 90 days.', icon: 'clock', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Number, label: 'Maximum delegation duration (days)', group: 'Window policy', sortOrder: 1, min: 1, max: 90, helpText: 'Set below the platform cap if your compliance regime requires shorter windows.')]
    public int $max_delegation_days = 90;

    #[SettingField(controlType: ControlType::Number, label: 'Minimum advance notice (hours)', group: 'Window policy', sortOrder: 2, min: 0, max: 168, helpText: 'How far in advance a delegation must be created before it can activate. 0 = immediate. Emergency delegations bypass this if the tenant holds the emergency_delegation entitlement.')]
    public int $min_advance_notice_hours = 0;


    // ── Notification cadence ──────────────────────────────────
    #[SettingGroup(label: 'Notification cadence', icon: 'bell-alert', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify delegate when delegation activates', group: 'Notification cadence', sortOrder: 1)]
    public bool $notify_delegate_on_activation = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify delegator on expiration', group: 'Notification cadence', sortOrder: 2)]
    public bool $notify_delegator_on_expiration = true;

    #[SettingField(controlType: ControlType::Number, label: 'Send \'expiring soon\' notice (hours before end)', group: 'Notification cadence', sortOrder: 3, min: 1, max: 168)]
    public int $notify_expiring_hours_ahead = 24;


    // ── Emergency delegation ──────────────────────────────────
    #[SettingGroup(label: 'Emergency delegation', icon: 'exclamation-triangle', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Allow emergency-type delegations', group: 'Emergency delegation', sortOrder: 1, helpText: 'Bypasses the min_advance_notice_hours check. Only requestable by users holding the emergency_delegation permission (default: admin + owner).')]
    public bool $emergency_type_available = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'delegation';
    }
}
