<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Stackra\Delegation\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Security team fired `POST /impersonation/{session}/force-terminate` — a lockdown event. Sets ended_at + end_trigger=security_lockdown. Notifies security team via ImpersonationForcedTerminationNotification (cannot-opt-out).
 *
 * ## Consumers
 *
 * - notifications::DispatchImpersonationForcedTerminationNotification
 * - audit::WriteHeavyAuditRow
 * - activity::WriteToActivityLog
 * - observability::EmitImpersonationForcedTerminatedMetric
 * - monitoring::PageOnCallOnLockdown
 * - analytics::TrackImpersonationForcedTerminated
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ImpersonationSessionForcedTerminate implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID (imp_)
     * @param  string  $impersonatorPlatformUserId  ULID (plu_)
     * @param  string  $impersonatedUserId  ULID (usr_)
     * @param  string  $targetTenantId  ULID (ten_)
     * @param  string  $terminatedBy  ULID (plu_) — super_admin or security
     * @param  string  $terminationReason  string (min 40)
     * @param  string  $terminatedAt  ISO-8601 timestamp
     * @param  int  $durationSecondsBeforeTermination  integer
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $impersonatorPlatformUserId,
        public readonly string $impersonatedUserId,
        public readonly string $targetTenantId,
        public readonly string $terminatedBy,
        public readonly string $terminationReason,
        public readonly string $terminatedAt,
        public readonly int $durationSecondsBeforeTermination,
    ) {
    }
}
