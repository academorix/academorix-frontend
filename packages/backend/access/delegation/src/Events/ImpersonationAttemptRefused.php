<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Stackra\Delegation\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ImpersonationSessionObserver::creating refused. Reason falls into: entitlement missing (PlatformUser lacks `platform.impersonate`), PlatformUser status != active, target User doesn't exist / belongs to different tenant, target tenant is disabled, target application is disabled, TTL request out of bounds, reason too short. No row created; the event fires so security can spot patterns.
 *
 * ## Consumers
 *
 * - audit::WriteHeavyAuditRow
 * - observability::EmitImpersonationRefusedMetric
 * - monitoring::AlertOnRepeatedRefusals
 * - analytics::TrackImpersonationRefused
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ImpersonationAttemptRefused implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attemptedByPlatformUserId  ULID (plu_)
     * @param  string  $attemptedImpersonatedUserId  ULID (usr_) | null
     * @param  string  $attemptedTargetApplicationId  ULID (app_) | null
     * @param  string  $attemptedTargetTenantId  ULID (ten_) | null
     * @param  string  $refusalReason  string — machine-readable code (missing_entitlement / operator_not_active / target_not_found / cross_tenant_target / target_tenant_disabled / target_application_disabled / ttl_out_of_bounds / reason_too_short)
     * @param  string  $refusalDetail  string (human-readable)
     * @param  string  $attemptedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $attemptedByPlatformUserId,
        public readonly string $attemptedImpersonatedUserId,
        public readonly string $attemptedTargetApplicationId,
        public readonly string $attemptedTargetTenantId,
        public readonly string $refusalReason,
        public readonly string $refusalDetail,
        public readonly string $attemptedAt,
    ) {
    }
}
