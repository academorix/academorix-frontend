<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Stackra\Delegation\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * DELETE /me/delegations/{delegation} succeeded — the delegator (or admin) revoked before ends_at was reached. RoleDelegationObserver::updated on `revoked_at` transitioning from null to non-null.
 *
 * ## Consumers
 *
 * - notifications::DispatchDelegationRevokedEarlyNotification
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRow
 * - observability::EmitDelegationRevokedMetric
 * - analytics::TrackDelegationRevokedEarly
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class DelegationRevokedEarly implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $delegationId  ULID (dlg_)
     * @param  string  $tenantId  ULID (ten_)
     * @param  string  $delegatorId  ULID (usr_)
     * @param  string  $delegateId  ULID (usr_)
     * @param  string  $roleId  ULID (rol_) | null
     * @param  string  $revokedAt  ISO-8601 timestamp
     * @param  string  $revokedBy  ULID (usr_) — the delegator OR an admin
     * @param  string  $revokeReason  string
     * @param  string  $plannedEndsAt  ISO-8601 timestamp (the original ends_at, retained for audit narrative)
     */
    public function __construct(
        public readonly string $delegationId,
        public readonly string $tenantId,
        public readonly string $delegatorId,
        public readonly string $delegateId,
        public readonly string $roleId,
        public readonly string $revokedAt,
        public readonly string $revokedBy,
        public readonly string $revokeReason,
        public readonly string $plannedEndsAt,
    ) {
    }
}
