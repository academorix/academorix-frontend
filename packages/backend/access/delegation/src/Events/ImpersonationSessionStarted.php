<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Stackra\Delegation\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ImpersonationSessionObserver::created after all safety gates passed (entitlement check, PlatformUser is active, target tenant is enabled, snapshot fields captured). CRITICAL severity — routes to notifications-critical + fires the cannot-opt-out notification to the impersonated User.
 *
 * ## Consumers
 *
 * - notifications::DispatchImpersonationSessionStartedNotification
 * - audit::WriteHeavyAuditRow
 * - activity::WriteToActivityLog
 * - observability::EmitImpersonationStartedMetric
 * - monitoring::AlertOnImpersonationStart
 * - analytics::TrackImpersonationStarted
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ImpersonationSessionStarted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID (imp_)
     * @param  string  $impersonatorPlatformUserId  ULID (plu_)
     * @param  string  $impersonatedUserId  ULID (usr_)
     * @param  string  $targetApplicationId  ULID (app_)
     * @param  string  $targetTenantId  ULID (ten_)
     * @param  string  $reason  string (min 40)
     * @param  string  $supportTicketReference  string | null
     * @param  bool  $isReadOnly  boolean
     * @param  string  $startedAt  ISO-8601 timestamp
     * @param  string  $endsAt  ISO-8601 timestamp
     * @param  string  $impersonatorDisplayName  string (snapshot)
     * @param  string  $impersonatorDepartment  string (snapshot)
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $impersonatorPlatformUserId,
        public readonly string $impersonatedUserId,
        public readonly string $targetApplicationId,
        public readonly string $targetTenantId,
        public readonly string $reason,
        public readonly string $supportTicketReference,
        public readonly bool $isReadOnly,
        public readonly string $startedAt,
        public readonly string $endsAt,
        public readonly string $impersonatorDisplayName,
        public readonly string $impersonatorDepartment,
    ) {
    }
}
