<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Stackra\Delegation\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ActivateDelegationsJob observed `starts_at <= now() AND state was pending`. Also fires when a delegation is created with starts_at already in the past (i.e. immediate activation on POST). Delegate gets notified — 'you can now act as X'.
 *
 * ## Consumers
 *
 * - notifications::DispatchDelegationActivatedNotification
 * - delegation::DispatchNotifyDelegateOfActivationJob
 * - activity::WriteToActivityLog
 * - observability::EmitDelegationActivatedMetric
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class DelegationActivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $delegationId  ULID (dlg_)
     * @param  string  $tenantId  ULID (ten_)
     * @param  string  $delegatorId  ULID (usr_)
     * @param  string  $delegateId  ULID (usr_)
     * @param  string  $roleId  ULID (rol_) | null
     * @param  string  $activatedAt  ISO-8601 timestamp
     * @param  string  $endsAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $delegationId,
        public readonly string $tenantId,
        public readonly string $delegatorId,
        public readonly string $delegateId,
        public readonly string $roleId,
        public readonly string $activatedAt,
        public readonly string $endsAt,
    ) {
    }
}
