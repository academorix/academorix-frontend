<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Stackra\Delegation\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ExpireDelegationsJob observed `ends_at <= now() AND revoked_at IS NULL AND state was active`. Terminal state. Delegator gets notified.
 *
 * ## Consumers
 *
 * - notifications::DispatchDelegationExpiredNotification
 * - delegation::DispatchNotifyDelegatorOfExpirationJob
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRow
 * - observability::EmitDelegationExpiredMetric
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class DelegationExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $delegationId  ULID (dlg_)
     * @param  string  $tenantId  ULID (ten_)
     * @param  string  $delegatorId  ULID (usr_)
     * @param  string  $delegateId  ULID (usr_)
     * @param  string  $roleId  ULID (rol_) | null
     * @param  string  $expiredAt  ISO-8601 timestamp
     * @param  string  $durationHours  number
     */
    public function __construct(
        public readonly string $delegationId,
        public readonly string $tenantId,
        public readonly string $delegatorId,
        public readonly string $delegateId,
        public readonly string $roleId,
        public readonly string $expiredAt,
        public readonly string $durationHours,
    ) {
    }
}
