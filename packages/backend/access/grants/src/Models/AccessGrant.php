<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Stackra\Grants\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Attributes\WithoutIncrementing;
use Illuminate\Database\Eloquent\Model;
use Stackra\Grants\Contracts\Data\AccessGrantInterface;
use Stackra\Grants\Database\Factories\AccessGrantFactory;
use Stackra\Foundation\Concerns\Filterable;
use Stackra\Foundation\Concerns\HasMetadata;
use Stackra\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Laravel\Scout\Searchable;
use Wildside\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use OwenIt\Auditing\Contracts\Auditable as AuditableContract;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a AccessGrant.
 *
 * One per-resource dynamic access grant.
 *
 * @category Grants
 *
 * @since    0.1.0
 */
#[Table(name: AccessGrantInterface::TABLE, key: AccessGrantInterface::PRIMARY_KEY, keyType: AccessGrantInterface::KEY_TYPE)]
#[Fillable([
    AccessGrantInterface::ATTR_TENANT_ID,
        AccessGrantInterface::ATTR_SUBJECT_TYPE,
        AccessGrantInterface::ATTR_SUBJECT_ID,
        AccessGrantInterface::ATTR_RESOURCE_TYPE,
        AccessGrantInterface::ATTR_RESOURCE_ID,
        AccessGrantInterface::ATTR_PERMISSIONS,
        AccessGrantInterface::ATTR_DECISION,
        AccessGrantInterface::ATTR_GRANTED_BY,
        AccessGrantInterface::ATTR_GRANTED_AT,
        AccessGrantInterface::ATTR_EXPIRES_AT,
        AccessGrantInterface::ATTR_REVOKED_AT,
        AccessGrantInterface::ATTR_REVOKED_BY,
        AccessGrantInterface::ATTR_REVOKED_REASON,
        AccessGrantInterface::ATTR_REASON,
        AccessGrantInterface::ATTR_SOURCE,
        AccessGrantInterface::ATTR_SOURCE_REFERENCE,
        AccessGrantInterface::ATTR_METADATA,
])]
#[UseFactory(AccessGrantFactory::class)]
#[WithoutIncrementing]
final class AccessGrant extends Model implements AccessGrantInterface, AuditableContract
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    use HasMetadata;
    use Userstamps;
    use LogsActivity;
    use Auditable;
    use Filterable;
    use Searchable;
    use SoftDeletes;

    /**
     * Cast map — from the blueprint's `x-eloquent.casts`.
     *
     * @var array<string, string>
     */
    protected $casts = [
        AccessGrantInterface::ATTR_DECISION => GrantDecision::class,
        AccessGrantInterface::ATTR_SOURCE => GrantSource::class,
        AccessGrantInterface::ATTR_PERMISSIONS => 'array',
        AccessGrantInterface::ATTR_GRANTS => 'array',
        AccessGrantInterface::ATTR_METADATA => 'array',
        AccessGrantInterface::ATTR_GRANTED_AT => 'datetime',
        AccessGrantInterface::ATTR_EXPIRES_AT => 'datetime',
        AccessGrantInterface::ATTR_REVOKED_AT => 'datetime',
    ];
}
