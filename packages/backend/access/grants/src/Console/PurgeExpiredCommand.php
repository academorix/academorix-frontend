<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Academorix\Grants\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan grants:purge-expired` — Purge expired — grants command.
 *
 * @category Grants
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'grants:purge-expired',
    description: 'Purge expired — grants command.',
)]
final class PurgeExpiredCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Purge expired — grants command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Purge expired — grants command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
