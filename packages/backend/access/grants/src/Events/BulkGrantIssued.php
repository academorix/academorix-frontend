<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Academorix\Grants\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * BulkGrantIssueJob finished processing a CSV batch. Fires ONCE per batch (not per row) so downstream analytics can aggregate correctly.
 *
 * ## Consumers
 *
 * - analytics::RecordBulkGrantIssued (functional consent tier)
 * - notifications::DispatchBulkGrantCompletedNotification (to admin)
 *
 * @category Grants
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class BulkGrantIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $batchId  string
     * @param  string  $tenant  Tenant
     * @param  string  $actor  User (the admin who uploaded)
     * @param  int  $grantsCreated  integer
     * @param  int  $grantsFailed  integer
     * @param  int  $durationMs  integer
     */
    public function __construct(
        public readonly string $batchId,
        public readonly string $tenant,
        public readonly string $actor,
        public readonly int $grantsCreated,
        public readonly int $grantsFailed,
        public readonly int $durationMs,
    ) {
    }
}
