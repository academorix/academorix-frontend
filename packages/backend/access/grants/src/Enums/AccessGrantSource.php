<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Stackra\Grants\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `source` on `access-grant`.
 *
 * Enum: `manual` (admin CRUD), `access_request` (materialised from a workflow/approvals instance), `delegation` (issued via access/delegation flow — reserved for Wave 1b delegation module), `bulk_provis...
 *
 * @category Grants
 *
 * @since    0.1.0
 */
enum AccessGrantSource: string
{
    use Enum;

    case Manual = 'manual';
    case AccessRequest = 'access_request';
    case Delegation = 'delegation';
    case BulkProvision = 'bulk_provision';
}
