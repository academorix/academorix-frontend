<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py billing subscription --force`.

declare(strict_types=1);

namespace Stackra\Subscription\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan subscription:sync-cashier` — Sync cashier — subscription command.
 *
 * @category Subscription
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'subscription:sync-cashier',
    description: 'Sync cashier — subscription command.',
)]
final class SyncCashierCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Sync cashier — subscription command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Sync cashier — subscription command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
