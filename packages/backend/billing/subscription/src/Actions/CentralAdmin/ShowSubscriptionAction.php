<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py billing subscription --force`.

declare(strict_types=1);

namespace Stackra\Subscription\Actions\CentralAdmin;

use Stackra\Subscription\Contracts\Repositories\SubscriptionRepositoryInterface;
use Stackra\Subscription\Data\SubscriptionData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/platform/subscriptions/{subscription}` — show action (central_admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Subscription
 *
 * @since    0.1.0
 */
#[AsAction(name: 'subscription.subscriptions.show')]
#[Get('/api/v1/platform/subscriptions/{subscription}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowSubscriptionAction
{
    use AsController;

    public function __construct(
        private readonly SubscriptionRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `subscription` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): SubscriptionData
    {
        return SubscriptionData::from($this->repository->findOrFail($id));
    }
}
