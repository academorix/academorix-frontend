<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py billing subscription --force`.

declare(strict_types=1);

namespace Stackra\Subscription\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * SubscriptionSettings — Billing preferences.
 *
 * How the tenant handles subscription lifecycle events.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Subscription
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'subscription',
    label: 'Billing preferences',
    description: 'How the tenant handles subscription lifecycle events.',
    icon: 'credit-card',
    scope: 'tenant',
    permission: 'subscription.manage',
    sortOrder: 30,
)]
class SubscriptionSettings extends Settings
{

    // ── Billing contact ──────────────────────────────────
    #[SettingGroup(label: 'Billing contact', description: 'Where billing emails + invoices are sent. Falls back to tenant owner if empty.', icon: 'envelope', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Email, label: 'Billing email', validation: ['nullable', 'email'], group: 'Billing contact', sortOrder: 1)]
    public ?string $billing_email = null;

    #[SettingField(controlType: ControlType::Toggle, label: 'CC tenant owner on invoices', validation: ['boolean'], group: 'Billing contact', sortOrder: 2)]
    public bool $cc_owner_on_invoices = true;


    // ── Auto-renewal ──────────────────────────────────
    #[SettingGroup(label: 'Auto-renewal', icon: 'arrow-path', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Auto-renew subscription', validation: ['boolean'], group: 'Auto-renewal', sortOrder: 1, helpText: 'When off, cancel_at_period_end=true on the next renewal date.')]
    public bool $auto_renew_enabled = true;


    // ── Invoice preferences ──────────────────────────────────
    #[SettingGroup(label: 'Invoice preferences', icon: 'document-text', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Select, label: 'Invoice language', validation: ['required', 'in:en,fr,es,de'], group: 'Invoice preferences', sortOrder: 1, options: ['en' => 'English', 'fr' => 'Français', 'es' => 'Español', 'de' => 'Deutsch'])]
    public string $invoice_language = 'en';

    #[SettingField(controlType: ControlType::Text, label: 'Tax ID (VAT / GST / EIN)', validation: ['nullable', 'string', 'max:64'], group: 'Invoice preferences', sortOrder: 2, helpText: 'Included on invoices for tax purposes. Format varies by jurisdiction.')]
    public ?string $tax_id = null;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'subscription';
    }
}
