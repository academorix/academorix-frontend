<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py billing entitlements --force`.

declare(strict_types=1);

namespace Stackra\Entitlements\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan entitlements:reconcile` — Reconcile the entitlements data.
 *
 * @category Entitlements
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'entitlements:reconcile',
    description: 'Reconcile the entitlements data.',
)]
final class ReconcileCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Reconcile the entitlements data', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Reconcile the entitlements data completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
