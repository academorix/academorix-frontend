<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py billing entitlements --force`.

declare(strict_types=1);

namespace Stackra\Entitlements\Services;

use Stackra\Entitlements\Contracts\Services\PlanEntitlementSyncerInterface;
use Illuminate\Container\Attributes\Scoped;
use Stackra\Entitlements\Contracts\Repositories\EntitlementUsageRepositoryInterface;

/**
 * Concrete service — PlanEntitlementSyncer.
 *
 * Implements {@see PlanEntitlementSyncerInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Entitlements
 *
 * @since    0.1.0
 */
#[Scoped]
final class PlanEntitlementSyncer implements PlanEntitlementSyncerInterface
{
    /**
     * @param  EntitlementUsageRepositoryInterface  $entitlementUsageRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly EntitlementUsageRepositoryInterface $entitlementUsageRepository,
    ) {
    }
}
