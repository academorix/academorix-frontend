<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability audit --force`.

declare(strict_types=1);

namespace Stackra\Audit\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan audit:export` — Export the audit rows.
 *
 * @category Audit
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'audit:export',
    description: 'Export the audit rows.',
)]
final class ExportCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Export the audit rows', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Export the audit rows completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
