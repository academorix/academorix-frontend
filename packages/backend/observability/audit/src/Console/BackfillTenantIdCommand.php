<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability audit --force`.

declare(strict_types=1);

namespace Stackra\Audit\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan audit:backfill-tenant-id` — Backfill tenant id — audit command.
 *
 * @category Audit
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'audit:backfill-tenant-id',
    description: 'Backfill tenant id — audit command.',
)]
final class BackfillTenantIdCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Backfill tenant id — audit command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Backfill tenant id — audit command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
