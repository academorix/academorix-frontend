<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability audit --force`.

declare(strict_types=1);

namespace Stackra\Audit\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Audit module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Audit
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum AuditPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `audit.audits.viewAny` — List audit rows in tenant.
     */
    #[Label('List audit rows in tenant')]
    #[Description('List audit rows in tenant. Compliance-officer scoped.')]
    case AuditAuditsViewAny = 'audit.audits.viewAny';

    /**
     * `audit.audits.view` — View a single audit row (redacted diff by default).
     */
    #[Label('View a single audit row (redacted diff by default)')]
    #[Description('View a single audit row (redacted diff by default). Compliance-officer scoped.')]
    case AuditAuditsView = 'audit.audits.view';

    /**
     * `audit.audits.reveal_raw_diff` — Reveal raw old_values + new_values on an audit row.
     */
    #[Label('Reveal raw old_values + new_values on an audit row')]
    #[Description('Reveal raw old_values + new_values on an audit row. Reserved for regulator investigations. Writes a chain-of-custody row (meta-audit) on every reveal.')]
    case AuditAuditsRevealRawDiff = 'audit.audits.reveal_raw_diff';

    /**
     * `audit.audits.export` — Request an audit export (DSAR, regulator, legal-hold).
     */
    #[Label('Request an audit export (DSAR, regulator, legal-hold)')]
    #[Description('Request an audit export (DSAR, regulator, legal-hold). Requires audit_export entitlement (Medium+).')]
    case AuditAuditsExport = 'audit.audits.export';

    /**
     * `audit.retention-policies.viewAny` — List AuditRetentionPolicy rows.
     */
    #[Label('List AuditRetentionPolicy rows')]
    #[Description('List AuditRetentionPolicy rows.')]
    case AuditRetentionPoliciesViewAny = 'audit.retention-policies.viewAny';

    /**
     * `audit.retention-policies.view` — View a single retention policy.
     */
    #[Label('View a single retention policy')]
    #[Description('View a single retention policy.')]
    case AuditRetentionPoliciesView = 'audit.retention-policies.view';

    /**
     * `audit.retention-policies.create` — Create a retention policy override (Enterprise, audit_retention_extended entitlement).
     */
    #[Label('Create a retention policy override (Enterprise, audit_retention_extended entitle')]
    #[Description('Create a retention policy override (Enterprise, audit_retention_extended entitlement).')]
    case AuditRetentionPoliciesCreate = 'audit.retention-policies.create';

    /**
     * `audit.retention-policies.update` — Update a retention policy (retention_years, override_ends_at, override_reason).
     */
    #[Label('Update a retention policy (retention_years, override_ends_at, override_reason)')]
    #[Description('Update a retention policy (retention_years, override_ends_at, override_reason).')]
    case AuditRetentionPoliciesUpdate = 'audit.retention-policies.update';

    /**
     * `audit.retention-policies.delete` — Soft-delete a retention policy.
     */
    #[Label('Soft-delete a retention policy')]
    #[Description('Soft-delete a retention policy. Resets retention_years to config default.')]
    case AuditRetentionPoliciesDelete = 'audit.retention-policies.delete';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::AuditAuditsViewAny => Guard::Sanctum,
            self::AuditAuditsView => Guard::Sanctum,
            self::AuditAuditsRevealRawDiff => Guard::Sanctum,
            self::AuditAuditsExport => Guard::Sanctum,
            self::AuditRetentionPoliciesViewAny => Guard::Sanctum,
            self::AuditRetentionPoliciesView => Guard::Sanctum,
            self::AuditRetentionPoliciesCreate => Guard::Sanctum,
            self::AuditRetentionPoliciesUpdate => Guard::Sanctum,
            self::AuditRetentionPoliciesDelete => Guard::Sanctum,
        };
    }
}
