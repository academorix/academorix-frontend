<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability audit --force`.

declare(strict_types=1);

namespace Stackra\Audit\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AuditRetentionPolicyObserver.created — a new tenant-scoped retention override was persisted.
 *
 * ## Consumers
 *
 * - audit::DispatchRetentionPolicyExtendedNotificationListener — fires AuditRetentionPolicyExtendedNotification to the tenant's compliance officer
 * - audit::InvalidateRetentionCacheListener — invalidates the per-tenant retention cache so PurgeExpiredAuditsJob picks up the new cutoff on its next run
 *
 * @category Audit
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AuditRetentionPolicyCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $auditRetentionPolicyId  ULID (arp_)
     * @param  string  $tenantId  ULID
     * @param  int  $retentionYears  int
     * @param  string  $overrideReason  string
     * @param  string  $overrideStartsAt  ISO 8601
     * @param  string  $overrideEndsAt  ISO 8601 | null
     */
    public function __construct(
        public readonly string $auditRetentionPolicyId,
        public readonly string $tenantId,
        public readonly int $retentionYears,
        public readonly string $overrideReason,
        public readonly string $overrideStartsAt,
        public readonly string $overrideEndsAt,
    ) {
    }
}
