<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability audit --force`.

declare(strict_types=1);

namespace Academorix\Audit\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AuditObserver.created — vendor `AuditableObserver` wrote an `audits` row, AuditObserver validated it, event fires afterCommit. Subscribed for downstream compliance signals ONLY; NEVER used to mutate audit data. Payload is intentionally narrow — no old_values/new_values, no field-level diff details — so consumers who need those details must query the audit row via AuditRepository. This prevents PII from leaking into the event queue's serialized payloads.
 *
 * ## Consumers
 *
 * - compliance::PublishComplianceMetricsListener — increments `academorix.compliance.audit.records_total` counter
 * - monitoring::EmitAuditFireRateListener — feeds an ops signal for detecting unusual audit-fire spikes (potential data-mutation storm)
 * - activity::CorrelateAuditToActivityListener (planned Wave 6) — some domain mutations correlate an audit row to an activity_log row; the listener adds `related_audit_id` on the corresponding activity row
 *
 * @category Audit
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AuditRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  int  $auditId  bigint (vendor default PK)
     * @param  string  $tenantId  ULID (ten_)
     * @param  string  $applicationId  ULID (app_) | null
     * @param  string  $event  string (created / updated / deleted / restored / custom event)
     * @param  string  $auditableType  string (FQCN)
     * @param  string  $auditableId  string (subject PK)
     * @param  string  $userType  string (FQCN) | null
     * @param  int  $userId  bigint | ULID | null
     * @param  string  $createdAt  ISO 8601
     */
    public function __construct(
        public readonly int $auditId,
        public readonly string $tenantId,
        public readonly string $applicationId,
        public readonly string $event,
        public readonly string $auditableType,
        public readonly string $auditableId,
        public readonly string $userType,
        public readonly int $userId,
        public readonly string $createdAt,
    ) {
    }
}
