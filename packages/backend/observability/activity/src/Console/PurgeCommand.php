<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability activity --force`.

declare(strict_types=1);

namespace Academorix\Activity\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan activity:purge` — Purge expired activity data.
 *
 * @category Activity
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'activity:purge',
    description: 'Purge expired activity data.',
)]
final class PurgeCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Purge expired activity data', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Purge expired activity data completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
