<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability activity --force`.

declare(strict_types=1);

namespace Academorix\Activity\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan activity:count` — Count — activity command.
 *
 * @category Activity
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'activity:count',
    description: 'Count — activity command.',
)]
final class CountCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Count — activity command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Count — activity command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
