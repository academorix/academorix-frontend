<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability activity --force`.

declare(strict_types=1);

namespace Stackra\Activity\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan activity:extend-retention` — Extend retention — activity command.
 *
 * @category Activity
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'activity:extend-retention',
    description: 'Extend retention — activity command.',
)]
final class ExtendRetentionCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Extend retention — activity command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Extend retention — activity command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
