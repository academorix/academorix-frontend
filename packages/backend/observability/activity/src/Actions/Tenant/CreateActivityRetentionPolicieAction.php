<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability activity --force`.

declare(strict_types=1);

namespace Stackra\Activity\Actions\Tenant;

use Stackra\Activity\Contracts\Repositories\ActivityRetentionPolicyRepositoryInterface;
use Stackra\Activity\Data\ActivityRetentionPolicyData;
use Stackra\Activity\Data\Requests\CreateActivityRetentionPolicyRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/activity-retention-policies` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Activity
 *
 * @since    0.1.0
 */
#[AsAction(name: 'activity.activity_retention_policies.create')]
#[Post('/api/v1/activity-retention-policies')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateActivityRetentionPolicieAction
{
    use AsController;

    public function __construct(
        private readonly ActivityRetentionPolicyRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `activity-retention-policie` from the validated request payload.
     *
     * @param  CreateActivityRetentionPolicyRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateActivityRetentionPolicyRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(ActivityRetentionPolicyData::from($model), JsonResponse::HTTP_CREATED);
    }
}
