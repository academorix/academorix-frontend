<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Stackra\Monitoring\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `provider` on `monitoring-provider-config`.
 *
 * Enum matching MonitoringProvider enum: sentry / datadog / pagerduty / opsgenie / prometheus / custom_webhook.
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
enum MonitoringProviderConfigProvider: string
{
    use Enum;

    case Sentry = 'sentry';
    case Datadog = 'datadog';
    case Pagerduty = 'pagerduty';
    case Opsgenie = 'opsgenie';
    case Prometheus = 'prometheus';
    case CustomWebhook = 'custom_webhook';
}
