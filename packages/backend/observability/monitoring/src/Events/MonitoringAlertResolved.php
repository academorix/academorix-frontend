<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Stackra\Monitoring\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MonitoringAlertObserver.updated detected status transition to 'resolved'. Either signal cleared (auto-resolve) or ops manually resolved.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MaybeResolveIncident (if this was the last firing alert in its incident → transition incident.status = 'resolved')
 * - monitoring::MetricsCollector (increments stackra.monitoring.alerts.resolved_total{resolved_by=auto|manual})
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MonitoringAlertResolved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $monitoringAlertId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $resolvedAt  ISO 8601
     * @param  string  $resolvedBy  'auto' | 'manual'
     * @param  string  $resolvedByUserId  ULID | null (null = auto-resolve)
     * @param  int  $timeToResolveSeconds  integer
     */
    public function __construct(
        public readonly string $monitoringAlertId,
        public readonly string $tenantId,
        public readonly string $resolvedAt,
        public readonly string $resolvedBy,
        public readonly string $resolvedByUserId,
        public readonly int $timeToResolveSeconds,
    ) {
    }
}
