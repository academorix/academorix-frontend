<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Stackra\Monitoring\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Provider recovered — half-open probe succeeded OR ops manually reset via monitoring:reset-circuit-breaker command / POST endpoint.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MonitoringProviderCircuitBreakerClosed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $providerConfigId  ULID
     * @param  string  $closedAt  ISO 8601
     * @param  bool  $wasAutoClose  boolean
     * @param  string  $closedByUserId  ULID | null
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $providerConfigId,
        public readonly string $closedAt,
        public readonly bool $wasAutoClose,
        public readonly string $closedByUserId,
    ) {
    }
}
