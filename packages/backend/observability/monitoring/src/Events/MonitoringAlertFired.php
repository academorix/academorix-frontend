<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Monitoring\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CRITICAL. MonitoringAlertObserver.created inserted a new monitoring_alerts row with status='firing'. The AlertPolicyEvaluator detected a threshold breach that met the policy's consecutive_breaches_required. Every P1 fire pages on-call.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — every alert is a compliance-relevant safety signal)
 * - monitoring::GroupIntoIncident (dispatches GroupAlertsIntoIncidentJob to attach or open an incident)
 * - monitoring::NotifyProviders (dispatches NotifyProvidersJob for the policy's notify_provider_configs)
 * - notifications::DispatchMonitoringAlertFiredNotification (P1: page on-call, P2: alert primary, P3+P4: dashboard-only)
 * - monitoring::MetricsCollector (increments academorix.monitoring.alerts.fired_total{severity=...})
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MonitoringAlertFired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $monitoringAlertId  ULID (mal_*)
     * @param  string  $tenantId  ULID
     * @param  string  $monitoringAlertPolicyId  ULID
     * @param  string  $severity  'p1' | 'p2' | 'p3' | 'p4'
     * @param  string  $signalType  string
     * @param  string  $signalSource  string
     * @param  string  $signalValue  number (the metric value that breached)
     * @param  array  $signalContext  object
     * @param  string  $firedAt  ISO 8601
     */
    public function __construct(
        public readonly string $monitoringAlertId,
        public readonly string $tenantId,
        public readonly string $monitoringAlertPolicyId,
        public readonly string $severity,
        public readonly string $signalType,
        public readonly string $signalSource,
        public readonly string $signalValue,
        public readonly array $signalContext,
        public readonly string $firedAt,
    ) {
    }
}
