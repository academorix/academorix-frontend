<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Stackra\Monitoring\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MonitoringAlertObserver.updated detected status transition firing → acknowledged. A human took ownership.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MaybeAckIncident (if this was the last unacknowledged alert in its incident → transition incident.status = 'acknowledged')
 * - monitoring::MetricsCollector
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MonitoringAlertAcknowledged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $monitoringAlertId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $acknowledgedByUserId  ULID
     * @param  string  $acknowledgedAt  ISO 8601
     * @param  int  $timeToAckSeconds  integer (fired_at → acknowledged_at)
     */
    public function __construct(
        public readonly string $monitoringAlertId,
        public readonly string $tenantId,
        public readonly string $acknowledgedByUserId,
        public readonly string $acknowledgedAt,
        public readonly int $timeToAckSeconds,
    ) {
    }
}
