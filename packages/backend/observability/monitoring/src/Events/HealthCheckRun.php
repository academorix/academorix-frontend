<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Stackra\Monitoring\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * HealthCheckRunObserver.created inserted a new health_check_runs row. High-volume — one fire per (health_check × interval_seconds).
 *
 * ## Consumers
 *
 * - monitoring::EvaluateAlertPolicies (checks whether this run triggers any threshold breaches)
 * - monitoring::UpdateHealthCheckDenormalizedState (updates last_run_at + last_run_status + consecutive_failure_count on the parent)
 * - monitoring::MetricsCollector (increments stackra.monitoring.health_check_runs.total{status=...})
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class HealthCheckRun implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $healthCheckRunId  ULID (hcr_*)
     * @param  string  $healthCheckId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $status  'healthy' | 'degraded' | 'unhealthy' | 'timeout' | 'error'
     * @param  int  $latencyMs  integer | null
     * @param  string  $runAt  ISO 8601
     */
    public function __construct(
        public readonly string $healthCheckRunId,
        public readonly string $healthCheckId,
        public readonly string $tenantId,
        public readonly string $status,
        public readonly int $latencyMs,
        public readonly string $runAt,
    ) {
    }
}
