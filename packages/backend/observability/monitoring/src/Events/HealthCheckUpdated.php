<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Stackra\Monitoring\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * HealthCheckObserver.updated committed changes to a health_checks row. Interval or target changes require re-scheduling.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MaybeRescheduleHealthCheck (reschedules RunHealthCheckJob when interval_seconds or target changed)
 * - monitoring::MetricsCollector
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class HealthCheckUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $healthCheckId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedFields  string[]
     * @param  string  $updatedByUserId  ULID
     * @param  string  $updatedAt  ISO 8601
     */
    public function __construct(
        public readonly string $healthCheckId,
        public readonly string $tenantId,
        public readonly string $changedFields,
        public readonly string $updatedByUserId,
        public readonly string $updatedAt,
    ) {
    }
}
