<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Monitoring\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan monitoring:resolve-incident` — Resolve incident — monitoring command.
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'monitoring:resolve-incident',
    description: 'Resolve incident — monitoring command.',
)]
final class ResolveIncidentCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Resolve incident — monitoring command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Resolve incident — monitoring command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
