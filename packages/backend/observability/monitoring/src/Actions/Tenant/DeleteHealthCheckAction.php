<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Monitoring\Actions\Tenant;

use Academorix\Monitoring\Contracts\Repositories\HealthCheckRepositoryInterface;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Delete;
use Illuminate\Http\Response;

/**
 * `DELETE /api/v1/monitoring/health-checks/{check}` — delete action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Delete(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
#[AsAction(name: 'monitoring.health_checks.delete')]
#[Delete('/api/v1/monitoring/health-checks/{check}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class DeleteHealthCheckAction
{
    use AsController;

    public function __construct(
        private readonly HealthCheckRepositoryInterface $repository,
    ) {
    }

    /**
     * Soft-delete one `health-check` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): Response
    {
        $this->repository->delete($id);

        return response()->noContent();
    }
}
