<?php

declare(strict_types=1);

use Stackra\Transfer\Models\XferArtifact;

it('is a placeholder — replace with actual XferArtifact unit tests', function (): void {
    expect(XferArtifact::class)->toBeString();
});
