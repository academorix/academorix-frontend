<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared transfer --force`.

declare(strict_types=1);

namespace Academorix\Transfer\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan transfer:prune` — Prune stale transfer data.
 *
 * @category Transfer
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'transfer:prune',
    description: 'Prune stale transfer data.',
)]
final class PruneCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Prune stale transfer data', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Prune stale transfer data completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
