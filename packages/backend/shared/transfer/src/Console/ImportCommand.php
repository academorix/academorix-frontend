<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared transfer --force`.

declare(strict_types=1);

namespace Stackra\Transfer\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan transfer:import` — Import the transfer rows.
 *
 * @category Transfer
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'transfer:import',
    description: 'Import the transfer rows.',
)]
final class ImportCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Import the transfer rows', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Import the transfer rows completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
