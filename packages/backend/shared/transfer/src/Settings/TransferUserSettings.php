<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared transfer --force`.

declare(strict_types=1);

namespace Stackra\Transfer\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * TransferUserSettings — Data transfer — my preferences.
 *
 * Per-user override of the tenant defaults.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Transfer
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'transfer_user',
    label: 'Data transfer — my preferences',
    description: 'Per-user override of the tenant defaults.',
    icon: 'user-circle',
    scope: 'user',
    permission: 'settings.read',
    sortOrder: 20,
)]
class TransferUserSettings extends Settings
{

    // ── My notification channels ──────────────────────────────────
    #[SettingGroup(label: 'My notification channels', icon: 'bell', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Text, label: 'How I want to be notified', validation: ['nullable', 'array', 'min:1'], group: 'My notification channels', sortOrder: 1, options: ['mail' => 'Email', 'broadcast' => 'Real-time (in-app toast)', 'database' => 'In-app inbox', 'push' => 'Mobile push', 'sms' => 'SMS'], helpText: 'Empty = follow tenant default.')]
    public ?string $notify_channels = null;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'transfer_user';
    }
}
