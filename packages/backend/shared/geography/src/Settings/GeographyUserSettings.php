<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared geography --force`.

declare(strict_types=1);

namespace Stackra\Geography\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * GeographyUserSettings — My region.
 *
 * Per-user region preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Geography
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'geography_user',
    label: 'My region',
    description: 'Per-user region preferences.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 6,
)]
class GeographyUserSettings extends Settings
{

    // ── Region ──────────────────────────────────
    #[SettingGroup(label: 'Region', icon: 'map', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Country', group: 'Region', sortOrder: 1)]
    public ?string $preferred_country_code = null;

    #[SettingField(controlType: ControlType::Select, label: 'Currency', group: 'Region', sortOrder: 2)]
    public ?string $preferred_currency_code = null;

    #[SettingField(controlType: ControlType::Select, label: 'Timezone', group: 'Region', sortOrder: 3)]
    public ?string $preferred_timezone = null;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'geography_user';
    }
}
