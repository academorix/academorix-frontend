<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared geography --force`.

declare(strict_types=1);

namespace Stackra\Geography\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * GeographySettings — Region defaults.
 *
 * Tenant-wide defaults for country, currency, timezone, and language.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Geography
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'geography',
    label: 'Region defaults',
    description: 'Tenant-wide defaults for country, currency, timezone, and language.',
    icon: 'globe-alt',
    scope: 'tenant',
    permission: 'tenant_locales.viewAny',
    sortOrder: 12,
)]
class GeographySettings extends Settings
{

    // ── Defaults ──────────────────────────────────
    #[SettingGroup(label: 'Defaults', icon: 'map-pin', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Default country', group: 'Defaults', sortOrder: 1, helpText: 'Used as pre-selected value on signup + form dropdowns.')]
    public ?string $default_country_code = null;

    #[SettingField(controlType: ControlType::Select, label: 'Default currency', group: 'Defaults', sortOrder: 2, helpText: 'Used for pricing display when the tenant doesn\'t specify a currency per row.')]
    public ?string $default_currency_code = null;

    #[SettingField(controlType: ControlType::Select, label: 'Default timezone', group: 'Defaults', sortOrder: 3, helpText: 'IANA timezone name. Used for date rendering.')]
    public ?string $default_timezone = null;

    #[SettingField(controlType: ControlType::Select, label: 'Default reference language', group: 'Defaults', sortOrder: 4, helpText: 'Distinct from the tenant\'s active locale (see Languages settings). Used for content-authoring hints.')]
    public ?string $default_geography_language_code = null;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'geography';
    }
}
