<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared geography --force`.

declare(strict_types=1);

namespace Academorix\Geography\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan geography:reconcile-vendor` — Reconcile vendor — geography command.
 *
 * @category Geography
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'geography:reconcile-vendor',
    description: 'Reconcile vendor — geography command.',
)]
final class ReconcileVendorCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Reconcile vendor — geography command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Reconcile vendor — geography command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
