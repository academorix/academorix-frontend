<?php

declare(strict_types=1);

use Stackra\Geography\Models\Timezone;

it('is a placeholder — replace with actual Timezone unit tests', function (): void {
    expect(Timezone::class)->toBeString();
});
