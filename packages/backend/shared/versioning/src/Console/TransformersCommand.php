<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared versioning --force`.

declare(strict_types=1);

namespace Academorix\Versioning\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan versioning:transformers` — Transformers — versioning command.
 *
 * @category Versioning
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'versioning:transformers',
    description: 'Transformers — versioning command.',
)]
final class TransformersCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Transformers — versioning command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Transformers — versioning command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
