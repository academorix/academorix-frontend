<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared versioning --force`.

declare(strict_types=1);

namespace Stackra\Versioning\Actions\Platform;

use Stackra\Versioning\Contracts\Repositories\ApiVersionRepositoryInterface;
use Stackra\Versioning\Data\ApiVersionData;
use Stackra\Versioning\Data\Requests\UpdateApiVersionRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/platform/versioning/api-versions/{slug}` — update action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Versioning
 *
 * @since    0.1.0
 */
#[AsAction(name: 'versioning.api_versions.update')]
#[Patch('/api/v1/platform/versioning/api-versions/{slug}')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class UpdateApiVersionAction
{
    use AsController;

    public function __construct(
        private readonly ApiVersionRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `api-version` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateApiVersionRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateApiVersionRequestData $data): ApiVersionData
    {
        $model = $this->repository->update($id, $data->toArray());

        return ApiVersionData::from($model);
    }
}
