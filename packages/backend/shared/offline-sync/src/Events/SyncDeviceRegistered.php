<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared offline-sync --force`.

declare(strict_types=1);

namespace Academorix\OfflineSync\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New SyncDevice row. Device registered with server + granted a client_id.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 *
 * @category OfflineSync
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SyncDeviceRegistered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $deviceId  ULID (syd_*)
     * @param  string  $tenantId  ULID
     * @param  string  $userId  ULID
     * @param  string  $platform  'ios'|'android'|'macos'|'windows'|'web'
     * @param  string  $appVersion  string
     */
    public function __construct(
        public readonly string $deviceId,
        public readonly string $tenantId,
        public readonly string $userId,
        public readonly string $platform,
        public readonly string $appVersion,
    ) {
    }
}
