<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared offline-sync --force`.

declare(strict_types=1);

namespace Academorix\OfflineSync\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan offline-sync:list` — List every offline sync row.
 *
 * @category OfflineSync
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'offline-sync:list',
    description: 'List every offline sync row.',
)]
final class ListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List every offline sync row', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List every offline sync row completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
