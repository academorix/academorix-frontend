<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared telemetry --force`.

declare(strict_types=1);

namespace Stackra\Telemetry\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Telemetry module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Telemetry
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum TelemetryPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `platform.telemetry.view` — View pipeline status + registries + traces + logs across all three signals.
     */
    #[Label('View pipeline status + registries + traces + logs across all three signals')]
    #[Description('View pipeline status + registries + traces + logs across all three signals.')]
    case PlatformTelemetryView = 'platform.telemetry.view';

    /**
     * `platform.telemetry.manage` — Trigger test-export, test-emit, flip kill switches, force-flush.
     */
    #[Label('Trigger test-export, test-emit, flip kill switches, force-flush')]
    #[Description('Trigger test-export, test-emit, flip kill switches, force-flush.')]
    case PlatformTelemetryManage = 'platform.telemetry.manage';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::PlatformTelemetryView => Guard::PlatformAdmin,
            self::PlatformTelemetryManage => Guard::PlatformAdmin,
        };
    }
}
