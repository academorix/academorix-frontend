<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared localization --force`.

declare(strict_types=1);

namespace Stackra\Localization\Actions\Tenant;

use Stackra\Localization\Contracts\Repositories\TenantLocaleRepositoryInterface;
use Stackra\Localization\Data\TenantLocaleData;
use Stackra\Localization\Data\Requests\UpdateTenantLocaleRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/tenant-locales/{language}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Localization
 *
 * @since    0.1.0
 */
#[AsAction(name: 'localization.tenant_locales.update')]
#[Patch('/api/v1/tenant-locales/{language}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateTenantLocaleAction
{
    use AsController;

    public function __construct(
        private readonly TenantLocaleRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `tenant-locale` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateTenantLocaleRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateTenantLocaleRequestData $data): TenantLocaleData
    {
        $model = $this->repository->update($id, $data->toArray());

        return TenantLocaleData::from($model);
    }
}
