<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared foundation --force`.

declare(strict_types=1);

namespace Academorix\Foundation\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan module:sync` — Synchronise the module.
 *
 * @category Foundation
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'module:sync',
    description: 'Synchronise the module.',
)]
final class SyncCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Synchronise the module', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Synchronise the module completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
