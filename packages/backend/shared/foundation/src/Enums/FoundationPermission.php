<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared foundation --force`.

declare(strict_types=1);

namespace Stackra\Foundation\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Foundation module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Foundation
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum FoundationPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `platform.modules.view` — Read the module registry: `GET /api/v1/platform/modules` + `GET /api/v1/platform/modules/{module}`.
     */
    #[Label('Read the module registry: `GET /api/v1/platform/modules` + `GET /api/v1/platform')]
    #[Description('Read the module registry: `GET /api/v1/platform/modules` + `GET /api/v1/platform/modules/{module}`. Reveals which modules loaded + their dependency graph. Restricted because it discloses deployment structure.')]
    case PlatformModulesView = 'platform.modules.view';

    /**
     * `platform.health.view` — Read detailed health probe output (`GET /api/v1/platform/health/detail`) + subscribe to the `platform.
     */
    #[Label('Read detailed health probe output (`GET /api/v1/platform/health/detail`) + subsc')]
    #[Description('Read detailed health probe output (`GET /api/v1/platform/health/detail`) + subscribe to the `platform.health` broadcast channel. Detailed output includes error messages which may leak internals — restricted to trusted ops staff.')]
    case PlatformHealthView = 'platform.health.view';

    /**
     * `platform.search.manage` — Reindex or flush a Scout search index (`POST /api/v1/platform/search/reindex`, `POST /api/v1/platform/search/flush`).
     */
    #[Label('Reindex or flush a Scout search index (`POST /api/v1/platform/search/reindex`, `')]
    #[Description('Reindex or flush a Scout search index (`POST /api/v1/platform/search/reindex`, `POST /api/v1/platform/search/flush`). Reindex is safe (async job dispatch); flush is destructive + requires confirmation.')]
    case PlatformSearchManage = 'platform.search.manage';

    /**
     * `platform.config.reload` — Trigger a config hot-reload (`POST /api/v1/platform/config/reload`).
     */
    #[Label('Trigger a config hot-reload (`POST /api/v1/platform/config/reload`)')]
    #[Description('Trigger a config hot-reload (`POST /api/v1/platform/config/reload`). Sensitive — an incorrect config in prod after a Doppler push can be surfaced across every module without a redeploy.')]
    case PlatformConfigReload = 'platform.config.reload';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::PlatformModulesView => Guard::PlatformAdmin,
            self::PlatformHealthView => Guard::PlatformAdmin,
            self::PlatformSearchManage => Guard::PlatformAdmin,
            self::PlatformConfigReload => Guard::PlatformAdmin,
        };
    }
}
