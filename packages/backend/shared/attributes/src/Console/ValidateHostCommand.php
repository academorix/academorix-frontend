<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared attributes --force`.

declare(strict_types=1);

namespace Academorix\Attributes\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan attributes:validate-host` — Validate host — attributes command.
 *
 * @category Attributes
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'attributes:validate-host',
    description: 'Validate host — attributes command.',
)]
final class ValidateHostCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Validate host — attributes command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Validate host — attributes command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
