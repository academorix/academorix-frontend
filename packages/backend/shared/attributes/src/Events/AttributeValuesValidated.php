<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared attributes --force`.

declare(strict_types=1);

namespace Academorix\Attributes\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * HasSchemalessAttributes trait fired the validator on host save and passed. Fires alongside the host's own persistence event.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (verbose channel)
 *
 * @category Attributes
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AttributeValuesValidated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $hostType  string
     * @param  string  $hostId  ULID
     * @param  string  $setId  ULID
     * @param  string  $tenantId  ULID
     */
    public function __construct(
        public readonly string $hostType,
        public readonly string $hostId,
        public readonly string $setId,
        public readonly string $tenantId,
    ) {
    }
}
