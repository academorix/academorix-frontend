<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared attributes --force`.

declare(strict_types=1);

namespace Academorix\Attributes\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AttributeDefinition mutated. Labels / description / widget_config changes fire this without a version bump. Type or validation changes MUST fire AttributeSetVersioned on every set referencing this definition.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - cache::InvalidateDefinitionCache
 * - attributes::VersionSetsReferencingDefinition
 *
 * @category Attributes
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AttributeDefinitionUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $definitionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedFields  string[]
     * @param  bool  $isBreaking  boolean
     * @param  string  $changedByUserId  ULID
     */
    public function __construct(
        public readonly string $definitionId,
        public readonly string $tenantId,
        public readonly string $changedFields,
        public readonly bool $isBreaking,
        public readonly string $changedByUserId,
    ) {
    }
}
