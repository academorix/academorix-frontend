<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared attributes --force`.

declare(strict_types=1);

namespace Stackra\Attributes\Actions\Tenant;

use Stackra\Attributes\Contracts\Repositories\AttributeGroupRepositoryInterface;
use Stackra\Attributes\Data\AttributeGroupData;
use Stackra\Attributes\Data\Requests\UpdateAttributeGroupRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/attribute-groups/{group}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Attributes
 *
 * @since    0.1.0
 */
#[AsAction(name: 'attributes.attribute_groups.update')]
#[Patch('/api/v1/attribute-groups/{group}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateAttributeGroupAction
{
    use AsController;

    public function __construct(
        private readonly AttributeGroupRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `attribute-group` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateAttributeGroupRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateAttributeGroupRequestData $data): AttributeGroupData
    {
        $model = $this->repository->update($id, $data->toArray());

        return AttributeGroupData::from($model);
    }
}
