<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared attributes --force`.

declare(strict_types=1);

namespace Stackra\Attributes\Actions\Tenant;

use Stackra\Attributes\Contracts\Repositories\AttributeDefinitionRepositoryInterface;
use Stackra\Attributes\Data\AttributeDefinitionData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;
use Illuminate\Http\Request;
use Illuminate\Pagination\LengthAwarePaginator;
use Spatie\LaravelData\PaginatedDataCollection;

/**
 * `GET /api/v1/attribute-definitions` — list action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Attributes
 *
 * @since    0.1.0
 */
#[AsAction(name: 'attributes.attribute_definitions.list')]
#[Get('/api/v1/attribute-definitions')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ListAttributeDefinitionAction
{
    use AsController;

    public function __construct(
        private readonly AttributeDefinitionRepositoryInterface $repository,
    ) {
    }

    /**
     * List `attribute-definitions` for the current caller.
     *
     * The `Repository` base picks up `filter[...]` / `sort=...` /
     * `include=...` query parameters via the `#[Filterable]` attribute
     * on the concrete repository — no per-Action wiring needed.
     *
     * @return PaginatedDataCollection<int, AttributeDefinitionData>  Paginated wire-visible DTOs.
     */
    public function __invoke(Request $request): PaginatedDataCollection
    {
        /** @var LengthAwarePaginator<int, \Stackra\Attributes\Models\AttributeDefinition> $page */
        $page = $this->repository->paginate(
            perPage: (int) $request->integer('per_page', 15),
        );

        return AttributeDefinitionData::collect($page, PaginatedDataCollection::class);
    }
}
