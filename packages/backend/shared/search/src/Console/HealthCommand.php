<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared search --force`.

declare(strict_types=1);

namespace Stackra\Search\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan search:health` — Health — search command.
 *
 * @category Search
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'search:health',
    description: 'Health — search command.',
)]
final class HealthCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Health — search command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Health — search command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
