<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared search --force`.

declare(strict_types=1);

namespace Academorix\Search\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan search:flush` — Flush — search command.
 *
 * @category Search
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'search:flush',
    description: 'Flush — search command.',
)]
final class FlushCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Flush — search command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Flush — search command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
