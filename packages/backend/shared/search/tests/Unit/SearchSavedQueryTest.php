<?php

declare(strict_types=1);

use Stackra\Search\Models\SearchSavedQuery;

it('is a placeholder — replace with actual SearchSavedQuery unit tests', function (): void {
    expect(SearchSavedQuery::class)->toBeString();
});
