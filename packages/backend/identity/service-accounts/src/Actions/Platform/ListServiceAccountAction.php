<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity service-accounts --force`.

declare(strict_types=1);

namespace Stackra\ServiceAccounts\Actions\Platform;

use Stackra\ServiceAccounts\Contracts\Repositories\ServiceAccountRepositoryInterface;
use Stackra\ServiceAccounts\Data\ServiceAccountData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;
use Illuminate\Http\Request;
use Illuminate\Pagination\LengthAwarePaginator;
use Spatie\LaravelData\PaginatedDataCollection;

/**
 * `GET /api/v1/platform/service-accounts` — list action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category ServiceAccounts
 *
 * @since    0.1.0
 */
#[AsAction(name: 'service_accounts.list')]
#[Get('/api/v1/platform/service-accounts')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class ListServiceAccountAction
{
    use AsController;

    public function __construct(
        private readonly ServiceAccountRepositoryInterface $repository,
    ) {
    }

    /**
     * List `service-accounts` for the current caller.
     *
     * The `Repository` base picks up `filter[...]` / `sort=...` /
     * `include=...` query parameters via the `#[Filterable]` attribute
     * on the concrete repository — no per-Action wiring needed.
     *
     * @return PaginatedDataCollection<int, ServiceAccountData>  Paginated wire-visible DTOs.
     */
    public function __invoke(Request $request): PaginatedDataCollection
    {
        /** @var LengthAwarePaginator<int, \Stackra\ServiceAccounts\Models\ServiceAccount> $page */
        $page = $this->repository->paginate(
            perPage: (int) $request->integer('per_page', 15),
        );

        return ServiceAccountData::collect($page, PaginatedDataCollection::class);
    }
}
