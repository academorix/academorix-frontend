<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity service-accounts --force`.

declare(strict_types=1);

namespace Academorix\ServiceAccounts\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Token-exchange OR test-JWT issuance path. Payload captures the JWT's public metadata (jti, iat, exp, aud) NEVER the encoded token itself.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitServiceAccountJwtIssuedMetric
 * - analytics::TrackServiceAccountJwtIssued (sampled 1/100)
 *
 * @category ServiceAccounts
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ServiceAccountJwtIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $serviceAccountId  ULID
     * @param  string  $applicationId  ULID
     * @param  string  $kid  string
     * @param  string  $aud  string
     * @param  int  $issuedTtlSeconds  integer
     * @param  string  $jti  UUID (the JWT's jti claim)
     * @param  string  $trigger  'exchange' | 'test'
     * @param  string  $issuedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $serviceAccountId,
        public readonly string $applicationId,
        public readonly string $kid,
        public readonly string $aud,
        public readonly int $issuedTtlSeconds,
        public readonly string $jti,
        public readonly string $trigger,
        public readonly string $issuedAt,
    ) {
    }
}
