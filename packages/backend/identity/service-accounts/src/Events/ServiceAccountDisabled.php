<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity service-accounts --force`.

declare(strict_types=1);

namespace Academorix\ServiceAccounts\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * is_enabled transitioned from true to false. Also fires as part of the ServiceAccountExpired + delete flows (each of those fires two events: the domain-specific one AND this).
 *
 * ## Consumers
 *
 * - cache::InvalidateServiceAccountLookup
 * - activity::WriteToActivityLog
 * - observability::EmitServiceAccountDisabledMetric
 * - analytics::TrackServiceAccountDisabled
 *
 * @category ServiceAccounts
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ServiceAccountDisabled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $serviceAccountId  ULID
     * @param  string  $applicationId  ULID
     * @param  string  $trigger  'manual' | 'expired' | 'dormant' | 'suspicious_activity' | 'incident'
     * @param  string  $actorPlatformUserId  ULID | null (null for scheduled/automated triggers)
     * @param  string  $disabledAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $serviceAccountId,
        public readonly string $applicationId,
        public readonly string $trigger,
        public readonly string $actorPlatformUserId,
        public readonly string $disabledAt,
    ) {
    }
}
