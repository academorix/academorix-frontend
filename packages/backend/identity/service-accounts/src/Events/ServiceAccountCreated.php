<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity service-accounts --force`.

declare(strict_types=1);

namespace Stackra\ServiceAccounts\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New ServiceAccount row inserted, first secret hashed, initial expires_at set. Fires exactly once per SA, ever.
 *
 * ## Consumers
 *
 * - notifications::DispatchServiceAccountCreatedNotification
 * - activity::WriteToActivityLog
 * - observability::EmitServiceAccountCreatedMetric
 * - analytics::TrackServiceAccountCreated
 *
 * @category ServiceAccounts
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ServiceAccountCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $serviceAccountId  ULID
     * @param  string  $applicationId  ULID
     * @param  string  $tenantId  ULID | null
     * @param  string  $expiresAt  ISO-8601 timestamp
     * @param  string  $signerKid  string
     * @param  string  $actorPlatformUserId  ULID (the platform_admin who provisioned the SA)
     * @param  string  $createdAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $serviceAccountId,
        public readonly string $applicationId,
        public readonly string $tenantId,
        public readonly string $expiresAt,
        public readonly string $signerKid,
        public readonly string $actorPlatformUserId,
        public readonly string $createdAt,
    ) {
    }
}
