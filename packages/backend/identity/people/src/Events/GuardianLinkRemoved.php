<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity people --force`.

declare(strict_types=1);

namespace Academorix\People\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PersonGuardianLink revoked. Cross-tenant propagation — active links may be affected.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (central retention: 10 years)
 * - notifications::DispatchGuardianLinkRemovedNotification
 *
 * @category People
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class GuardianLinkRemoved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $guardianLinkId  ULID
     * @param  string  $minorPersonIdentityId  ULID
     * @param  string  $guardianPersonIdentityId  ULID
     * @param  string  $revokedAt  ISO 8601
     * @param  string  $revokedReason  string | null
     */
    public function __construct(
        public readonly string $guardianLinkId,
        public readonly string $minorPersonIdentityId,
        public readonly string $guardianPersonIdentityId,
        public readonly string $revokedAt,
        public readonly string $revokedReason,
    ) {
    }
}
