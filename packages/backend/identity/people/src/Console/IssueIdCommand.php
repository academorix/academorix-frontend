<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity people --force`.

declare(strict_types=1);

namespace Stackra\People\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan people:issue-id` — Issue id — people command.
 *
 * @category People
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'people:issue-id',
    description: 'Issue id — people command.',
)]
final class IssueIdCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Issue id — people command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Issue id — people command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
