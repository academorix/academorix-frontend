<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity identity --force`.

declare(strict_types=1);

namespace Stackra\Identity\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan identity:list` — List every identity row.
 *
 * @category Identity
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'identity:list',
    description: 'List every identity row.',
)]
final class ListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List every identity row', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List every identity row completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
