<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity identity --force`.

declare(strict_types=1);

namespace Stackra\Identity\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Identity module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Identity
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum IdentityPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `platform.identity.viewAny` — List identities (redacted — email fingerprint + metadata only).
     */
    #[Label('List identities (redacted — email fingerprint + metadata only)')]
    #[Description('List identities (redacted — email fingerprint + metadata only).')]
    case PlatformIdentityViewAny = 'platform.identity.viewAny';

    /**
     * `platform.identity.view` — Show one identity (redacted).
     */
    #[Label('Show one identity (redacted)')]
    #[Description('Show one identity (redacted). See `platform.identity.view.pii` to unmute.')]
    case PlatformIdentityView = 'platform.identity.view';

    /**
     * `platform.identity.view.pii` — Unmute email + dob on identity detail.
     */
    #[Label('Unmute email + dob on identity detail')]
    #[Description('Unmute email + dob on identity detail. NEVER unmutes regulated_secret fields. Every use of this permission writes an audit-log entry with the actor + reason. Provisioned narrowly — typically only compliance + security.')]
    case PlatformIdentityViewPii = 'platform.identity.view.pii';

    /**
     * `platform.identity.unlock` — Clear locked_until on an identity.
     */
    #[Label('Clear locked_until on an identity')]
    #[Description('Clear locked_until on an identity. Fires IdentityUnlocked(trigger=admin_action).')]
    case PlatformIdentityUnlock = 'platform.identity.unlock';

    /**
     * `platform.identity.force_password_reset` — Invalidate the current password + revoke sessions + issue a fresh reset token.
     */
    #[Label('Invalidate the current password + revoke sessions + issue a fresh reset token')]
    #[Description('Invalidate the current password + revoke sessions + issue a fresh reset token.')]
    case PlatformIdentityForcePasswordReset = 'platform.identity.force_password_reset';

    /**
     * `platform.identity.rotate_mfa_secret` — Re-encrypt Identity.
     */
    #[Label('Re-encrypt Identity')]
    #[Description('Re-encrypt Identity.mfa_secret_encrypted under a fresh KMS data key. Used during key rotation.')]
    case PlatformIdentityRotateMfaSecret = 'platform.identity.rotate_mfa_secret';

    /**
     * `platform.identity.erase` — Soft-delete an identity (start GDPR hold-period).
     */
    #[Label('Soft-delete an identity (start GDPR hold-period)')]
    #[Description('Soft-delete an identity (start GDPR hold-period). Compliance-officer scope; audit-critical.')]
    case PlatformIdentityErase = 'platform.identity.erase';

    /**
     * `platform.identity.restore` — Reverse an in-flight soft-delete before the hold-period elapses.
     */
    #[Label('Reverse an in-flight soft-delete before the hold-period elapses')]
    #[Description('Reverse an in-flight soft-delete before the hold-period elapses.')]
    case PlatformIdentityRestore = 'platform.identity.restore';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::PlatformIdentityViewAny => Guard::PlatformAdmin,
            self::PlatformIdentityView => Guard::PlatformAdmin,
            self::PlatformIdentityViewPii => Guard::PlatformAdmin,
            self::PlatformIdentityUnlock => Guard::PlatformAdmin,
            self::PlatformIdentityForcePasswordReset => Guard::PlatformAdmin,
            self::PlatformIdentityRotateMfaSecret => Guard::PlatformAdmin,
            self::PlatformIdentityErase => Guard::PlatformAdmin,
            self::PlatformIdentityRestore => Guard::PlatformAdmin,
        };
    }
}
