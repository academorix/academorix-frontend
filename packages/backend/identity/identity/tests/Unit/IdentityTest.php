<?php

declare(strict_types=1);

use Stackra\Identity\Models\Identity;

it('is a placeholder — replace with actual Identity unit tests', function (): void {
    expect(Identity::class)->toBeString();
});
