<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity mfa --force`.

declare(strict_types=1);

namespace Stackra\Mfa\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `state` on `mfa-challenge`.
 *
 * Enum: pending / succeeded / failed / expired / invalidated.
 *
 * @category Mfa
 *
 * @since    0.1.0
 */
enum MfaChallengeState: string
{
    use Enum;

    case Pending = 'pending';
    case Succeeded = 'succeeded';
    case Failed = 'failed';
    case Expired = 'expired';
    case Invalidated = 'invalidated';
}
