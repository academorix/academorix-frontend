<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity mfa --force`.

declare(strict_types=1);

namespace Stackra\Mfa\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * MfaUserSettings — My two-factor preferences.
 *
 * Per-user MFA preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Mfa
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'mfa_user',
    label: 'My two-factor preferences',
    description: 'Per-user MFA preferences.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 10,
)]
class MfaUserSettings extends Settings
{

    // ── Preferred method ──────────────────────────────────
    #[SettingGroup(label: 'Preferred method', icon: 'shield-check', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Preferred challenge method at login', group: 'Preferred method', sortOrder: 1, options: ['webauthn' => 'Passkey / hardware key (fastest)', 'totp' => 'Authenticator app', 'recovery_code' => 'Recovery code (fallback only)'], helpText: 'When you have multiple factors enrolled, which one to prompt for first.')]
    public string $preferred_challenge_method = 'webauthn';

    #[SettingField(controlType: ControlType::Toggle, label: 'Hide low-recovery-codes reminders', group: 'Preferred method', sortOrder: 2, helpText: 'Suppress the periodic nudge to regenerate. You\'ll still see the in-app dot when only a few codes remain.')]
    public bool $hide_recovery_codes_reminder = false;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'mfa_user';
    }
}
