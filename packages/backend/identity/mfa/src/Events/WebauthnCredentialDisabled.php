<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity mfa --force`.

declare(strict_types=1);

namespace Academorix\Mfa\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PATCH /api/v1/me/mfa/webauthn/credentials/{id} with `disabled_at=<now>` — user chose to temporarily disable without deleting. Preserves the row for the 90-day retention window (CleanupDisabledWebauthnCredentialsJob).
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitWebauthnCredentialDisabledMetric
 *
 * @category Mfa
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class WebauthnCredentialDisabled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $credentialId  ULID
     * @param  string  $deviceName  string
     * @param  string  $disabledAt  ISO-8601 timestamp
     * @param  string  $actorId  ULID (user OR platform admin)
     * @param  string  $reason  'user_action' | 'admin_force_disable' | 'lost_device'
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $credentialId,
        public readonly string $deviceName,
        public readonly string $disabledAt,
        public readonly string $actorId,
        public readonly string $reason,
    ) {
    }
}
