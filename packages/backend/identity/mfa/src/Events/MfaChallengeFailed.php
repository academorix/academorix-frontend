<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity mfa --force`.

declare(strict_types=1);

namespace Academorix\Mfa\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Challenge verification rejected. attempts_remaining decremented; challenge row invalidates on zero. Fires MfaChallengeFailed EVERY time the code / assertion mismatch occurs, even mid-attempts.
 *
 * ## Consumers
 *
 * - mfa::EvaluateSuspiciousActivitySignal
 * - activity::WriteToActivityLog
 * - observability::EmitMfaChallengeFailedMetric
 *
 * @category Mfa
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MfaChallengeFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $challengeId  ULID
     * @param  string  $method  'totp' | 'webauthn' | 'recovery_code'
     * @param  string  $reason  'code_mismatch' | 'assertion_signature_invalid' | 'sign_count_replay' | 'recovery_code_invalid' | 'expired' | 'attempts_exhausted'
     * @param  int  $attemptsRemaining  integer (0 = the challenge is now invalidated and the caller must restart from login)
     * @param  string  $failedAt  ISO-8601 timestamp
     * @param  string  $requestIp  string
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $challengeId,
        public readonly string $method,
        public readonly string $reason,
        public readonly int $attemptsRemaining,
        public readonly string $failedAt,
        public readonly string $requestIp,
    ) {
    }
}
