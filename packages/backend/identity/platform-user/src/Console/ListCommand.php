<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity platform-user --force`.

declare(strict_types=1);

namespace Stackra\PlatformUser\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan platform-user:list` — List every platform user row.
 *
 * @category PlatformUser
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'platform-user:list',
    description: 'List every platform user row.',
)]
final class ListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List every platform user row', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List every platform user row completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
