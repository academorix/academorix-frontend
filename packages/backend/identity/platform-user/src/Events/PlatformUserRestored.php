<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity platform-user --force`.

declare(strict_types=1);

namespace Stackra\PlatformUser\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PlatformUser.status transitioned from `suspended` back to `active`. Idempotent — restoring an already-active row is a no-op that emits no event.
 *
 * ## Consumers
 *
 * - notifications::DispatchPlatformUserRestoredNotification
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRow
 * - observability::EmitPlatformUserRestoredMetric
 *
 * @category PlatformUser
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PlatformUserRestored implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $platformUserId  ULID
     * @param  string  $previousReason  string (max 1000; the reason that suspended them)
     * @param  string  $restoredAt  ISO-8601 timestamp
     * @param  string  $actorId  ULID
     */
    public function __construct(
        public readonly string $platformUserId,
        public readonly string $previousReason,
        public readonly string $restoredAt,
        public readonly string $actorId,
    ) {
    }
}
