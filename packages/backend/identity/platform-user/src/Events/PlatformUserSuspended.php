<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity platform-user --force`.

declare(strict_types=1);

namespace Academorix\PlatformUser\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PlatformUser.status transitioned from `active` to `suspended`. Temporary state — for investigations, leaves of absence, or reversible policy violations. Distinct from `offboarded` (terminal).
 *
 * ## Consumers
 *
 * - notifications::DispatchPlatformUserSuspendedNotification
 * - auth::RevokeAllActivePlatformSessions
 * - access::PausePlatformImpersonationSessions
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRow
 * - observability::EmitPlatformUserSuspendedMetric
 *
 * @category PlatformUser
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PlatformUserSuspended implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $platformUserId  ULID
     * @param  string  $reason  string (max 1000 chars; captured at suspend time)
     * @param  string  $suspendedAt  ISO-8601 timestamp
     * @param  string  $actorId  ULID
     */
    public function __construct(
        public readonly string $platformUserId,
        public readonly string $reason,
        public readonly string $suspendedAt,
        public readonly string $actorId,
    ) {
    }
}
