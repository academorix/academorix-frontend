<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity platform-user --force`.

declare(strict_types=1);

namespace Stackra\PlatformUser\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New PlatformUser row inserted after passing the HirePlatformUserInput safety gates (MFA enrolled on Identity, unique identity_id). Fires exactly once per PlatformUser, ever.
 *
 * ## Consumers
 *
 * - notifications::DispatchPlatformUserHiredNotification
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRow
 * - observability::EmitPlatformUserHiredMetric
 * - analytics::TrackPlatformUserHired
 *
 * @category PlatformUser
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PlatformUserHired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $platformUserId  ULID
     * @param  string  $identityId  ULID
     * @param  string  $department  string (enum value)
     * @param  string  $employmentType  string (enum value)
     * @param  string  $hireDate  ISO-8601 date
     * @param  string  $managerId  ULID | null
     * @param  string  $actorId  ULID (the super_admin invoking the hire)
     * @param  string  $hiredAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $platformUserId,
        public readonly string $identityId,
        public readonly string $department,
        public readonly string $employmentType,
        public readonly string $hireDate,
        public readonly string $managerId,
        public readonly string $actorId,
        public readonly string $hiredAt,
    ) {
    }
}
