<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity platform-user --force`.

declare(strict_types=1);

namespace Stackra\PlatformUser\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PlatformUser.status transitioned to `offboarded` (terminal). offboarded_at populated. Dispatches RevokePlatformUserAccessJob synchronously in the same request-cycle so the 24h SLA is measured from event emission.
 *
 * ## Consumers
 *
 * - platform-user::DispatchRevokePlatformUserAccessJob
 * - notifications::DispatchPlatformUserOffboardingScheduledNotification
 * - auth::RevokeAllActivePlatformSessions
 * - access::TerminateActivePlatformImpersonationSessions
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRow
 * - observability::EmitPlatformUserOffboardedMetric
 * - analytics::TrackPlatformUserOffboarded
 *
 * @category PlatformUser
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PlatformUserOffboarded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $platformUserId  ULID
     * @param  string  $previousStatus  'pending' | 'active' | 'suspended'
     * @param  string  $reason  string (required, 20–1000 chars)
     * @param  string  $effectiveAt  ISO-8601 timestamp (may be future-dated up to 30 days)
     * @param  string  $offboardedAt  ISO-8601 timestamp (of the state transition)
     * @param  string  $actorId  ULID (the super_admin invoking the offboard)
     */
    public function __construct(
        public readonly string $platformUserId,
        public readonly string $previousStatus,
        public readonly string $reason,
        public readonly string $effectiveAt,
        public readonly string $offboardedAt,
        public readonly string $actorId,
    ) {
    }
}
