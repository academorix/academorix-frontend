<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity platform-user --force`.

declare(strict_types=1);

namespace Stackra\PlatformUser\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * PlatformUserSettings — Platform staff.
 *
 * Company-wide policies for Stackra internal staff management.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category PlatformUser
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'platform_user',
    label: 'Platform staff',
    description: 'Company-wide policies for Stackra internal staff management.',
    icon: 'user-group',
    scope: 'platform',
    permission: 'platform.platform_users.viewAny',
    sortOrder: 10,
)]
class PlatformUserSettings extends Settings
{

    // ── Onboarding ──────────────────────────────────
    #[SettingGroup(label: 'Onboarding', description: 'Reminder cadence + auto-promotion policy for pending platform staff.', icon: 'clipboard-document-check', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Text, label: 'Reminder days', group: 'Onboarding', sortOrder: 1, helpText: 'Days-since-hire on which to remind pending staffers + their managers.')]
    public array $onboarding_reminder_days = [1, 3, 7, 14];

    #[SettingField(controlType: ControlType::Number, label: 'Escalate to super_admin after (days)', group: 'Onboarding', sortOrder: 2, min: 7, max: 90)]
    public int $escalate_after_days = 30;

    #[SettingField(controlType: ControlType::Toggle, label: 'Auto-promote pending → active on checklist completion', group: 'Onboarding', sortOrder: 3)]
    public bool $auto_promote_on_checklist_complete = true;


    // ── Offboarding ──────────────────────────────────
    #[SettingGroup(label: 'Offboarding', description: 'SLA + retention window policy. Changes require compliance-officer co-sign in staging + prod.', icon: 'user-minus', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Number, label: 'Access-revocation SLA (hours)', group: 'Offboarding', sortOrder: 1, min: 4, max: 72, helpText: 'SOC 2 CC6.7 target. Alerting fires at half-SLA.')]
    public int $sla_hours = 24;

    #[SettingField(controlType: ControlType::Number, label: 'Retention hold (days)', group: 'Offboarding', sortOrder: 2, min: 30, max: 365, helpText: 'Days between soft-delete + hard-delete of offboarded rows.')]
    public int $retention_hold_days = 90;

    #[SettingField(controlType: ControlType::Number, label: 'Max future-scheduled offboard (days)', group: 'Offboarding', sortOrder: 3, min: 0, max: 90)]
    public int $max_future_scheduling_days = 30;


    // ── MFA enforcement ──────────────────────────────────
    #[SettingGroup(label: 'MFA enforcement', description: 'MFA gate on the platform_admin guard. Turning off requires incident-commander sign-off + a rollback ETA.', icon: 'shield-check', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Require MFA on hire', group: 'MFA enforcement', sortOrder: 1)]
    public bool $require_on_hire = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Require MFA on every request', group: 'MFA enforcement', sortOrder: 2)]
    public bool $require_on_every_request = true;

    #[SettingField(controlType: ControlType::Number, label: 'Platform session TTL (seconds)', group: 'MFA enforcement', sortOrder: 3, min: 300, max: 28800, helpText: '1h default. Shorter than the tenant plane\'s 8h.')]
    public int $session_ttl_seconds = 3600;


    // ── On-call ──────────────────────────────────
    #[SettingGroup(label: 'On-call', description: 'Rotation resolver backend + PagerDuty settings.', icon: 'bell-alert', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enable on-call rotation', group: 'On-call', sortOrder: 1)]
    public bool $enabled = false;

    #[SettingField(controlType: ControlType::Select, label: 'Default backend', group: 'On-call', sortOrder: 2, options: ['internal' => 'Internal (rotation table)', 'pagerduty' => 'PagerDuty'])]
    public string $default_backend = 'internal';

    #[SettingField(controlType: ControlType::Text, label: 'PagerDuty escalation policy id', group: 'On-call', sortOrder: 3, helpText: 'Read on every OnCallResolver query when backend=pagerduty. API key resolves separately via Doppler.')]
    public string $pagerduty_escalation_policy_id = '';


    // ── Audit ──────────────────────────────────
    #[SettingGroup(label: 'Audit', description: 'Correlation + retention on the audit trail.', icon: 'document-magnifying-glass', sortOrder: 5)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Correlate audit rows across request', group: 'Audit', sortOrder: 1)]
    public bool $correlate_across_requests = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'platform_user';
    }
}
