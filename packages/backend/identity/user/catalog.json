{
  "$schema": "https://gist.githubusercontent.com/academorix-user/073a1ab687cd93ede7ae927b96a025ea/raw/catalog.v1.json",
  "name": "academorix/user",
  "tier": "saas",
  "surfaces": ["core"],
  "kind": "feature",
  "purpose": "Per-Application user projection — User (Identity × Application × Tenant), Profile PII satellite, and TenantMember pivot for cross-tenant membership.",
  "capabilities": [
    "User Eloquent model on sanctum guard, application-scoped",
    "Profile 1:1 PII satellite (name, phone, avatar, locale, tz)",
    "TenantMember pivot linking a User to a Tenant + Role",
    "UNIQUE(identity_id, application_id) constraint",
    "Cascading tenant_id via BelongsToTenant trait",
    "Fortify-compatible identifiers surface"
  ],
  "when_to_use": "Every request that authenticates a tenant-audience caller resolves through a User row here.",
  "when_not_to_use": "For the global credential record (email + password + MFA), reach for the sibling academorix/identity package.",
  "peer_deps": [
    "academorix-identity/user-sdk",
    "academorix/crud",
    "academorix/foundation",
    "academorix/routing",
    "academorix/service-provider",
    "academorix/support",
    "spatie/laravel-data"
  ],
  "backend_pair": null,
  "size_gzip_kb": {},
  "maturity": "alpha",
  "owning_agent": "laravel-feature-builder",
  "docs": ["packages/backend/identity/user/README.md"]
}
