<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Stackra\User\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Soft-deleted User row restored within the erasure hold window (default 30 days).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category User
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class UserRestored implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $restoredBy  ULID
     * @param  string  $wasDeletedAt  ISO 8601 timestamp
     * @param  int  $daysInHold  integer
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $tenantId,
        public readonly string $restoredBy,
        public readonly string $wasDeletedAt,
        public readonly int $daysInHold,
    ) {
    }
}
