<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Stackra\User\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * User.preferred_locale updated. Distinct from ProfileUpdated because Profile.locale is a separate override.
 *
 * ## Consumers
 *
 * - localization::HandleUserLocaleChange (fires localization::LocaleChanged)
 * - activity::WriteToActivityLog
 *
 * @category User
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class UserPreferredLocaleChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromLocale  string | null
     * @param  string  $toLocale  string
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $tenantId,
        public readonly string $fromLocale,
        public readonly string $toLocale,
    ) {
    }
}
