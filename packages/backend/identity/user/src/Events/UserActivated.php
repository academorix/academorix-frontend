<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Stackra\User\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * User.status transitioned pending → active (post-email-verification OR admin activation).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchWelcomeEmail
 *
 * @category User
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class UserActivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $activatedBy  ULID | null (null = self-activation via email verification)
     * @param  string  $previousStatus  'pending' | 'suspended'
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $tenantId,
        public readonly string $activatedBy,
        public readonly string $previousStatus,
    ) {
    }
}
