<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Stackra\User\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * User.status transitioned active → suspended. Also invalidates every Sanctum PAT + JWT refresh token attached to this User.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - auth::RevokeAllTokens
 * - notifications::DispatchUserSuspendedNotification
 *
 * @category User
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class UserSuspended implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $suspendedBy  ULID
     * @param  int  $reason  string | null (free-form ops note; internal-tier)
     * @param  string  $reactivationScheduledFor  ISO 8601 timestamp | null (null = indefinite)
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $tenantId,
        public readonly string $suspendedBy,
        public readonly int $reason,
        public readonly string $reactivationScheduledFor,
    ) {
    }
}
