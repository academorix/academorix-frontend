<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Stackra\User\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TenantMember.role_id updated (admin promoted / demoted a member).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - access::InvalidatePermissionCache
 * - notifications::MaybeDispatchRoleChangedNotification
 *
 * @category User
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TenantMembershipRoleChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $membershipId  ULID
     * @param  string  $fromRoleId  ULID
     * @param  string  $toRoleId  ULID
     * @param  string  $changedBy  ULID
     * @param  bool  $isPromotion  boolean (role hierarchy comparison; null when roles are peers)
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $tenantId,
        public readonly string $membershipId,
        public readonly string $fromRoleId,
        public readonly string $toRoleId,
        public readonly string $changedBy,
        public readonly bool $isPromotion,
    ) {
    }
}
