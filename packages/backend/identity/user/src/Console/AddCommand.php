<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Academorix\User\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan tenant-member:add` — Add — tenant member command.
 *
 * @category User
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'tenant-member:add',
    description: 'Add — tenant member command.',
)]
final class AddCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Add — tenant member command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Add — tenant member command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
