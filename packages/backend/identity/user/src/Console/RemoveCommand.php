<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Stackra\User\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan tenant-member:remove` — Remove — tenant member command.
 *
 * @category User
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'tenant-member:remove',
    description: 'Remove — tenant member command.',
)]
final class RemoveCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Remove — tenant member command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Remove — tenant member command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
