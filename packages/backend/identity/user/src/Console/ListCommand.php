<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Academorix\User\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan tenant-member:list` — List every tenant member row.
 *
 * @category User
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'tenant-member:list',
    description: 'List every tenant member row.',
)]
final class ListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List every tenant member row', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List every tenant member row completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
