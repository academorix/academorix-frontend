<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Stackra\User\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan user:switch-default-tenant` — Switch default tenant — user command.
 *
 * @category User
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'user:switch-default-tenant',
    description: 'Switch default tenant — user command.',
)]
final class SwitchDefaultTenantCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Switch default tenant — user command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Switch default tenant — user command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
