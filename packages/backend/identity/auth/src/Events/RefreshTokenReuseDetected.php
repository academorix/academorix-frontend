<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Stackra\Auth\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * An already-consumed refresh token was presented again — canonical BCP defense signal from RFC draft-ietf-oauth-security-topics. The reuse indicates an attacker has captured a token; every PAT + refresh token in the family is invalidated + every session for the User revoked as a breach response.
 *
 * ## Consumers
 *
 * - auth::LockdownOnBreachDetected
 * - notifications::DispatchBreachAlertNotification
 * - activity::WriteToActivityLog
 * - observability::EmitBreachSignalMetric
 *
 * @category Auth
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RefreshTokenReuseDetected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $familyId  ULID
     * @param  string  $presentedRefreshId  ULID (the already-consumed token that was replayed)
     * @param  string  $detectedAt  ISO-8601 timestamp
     * @param  string  $requestIpHash  string
     * @param  string  $userAgentHash  string
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $familyId,
        public readonly string $presentedRefreshId,
        public readonly string $detectedAt,
        public readonly string $requestIpHash,
        public readonly string $userAgentHash,
    ) {
    }
}
