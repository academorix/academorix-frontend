<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Stackra\Auth\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New JwtSigningKey row inserted with is_active=true; previous key marked retired_at=now() (kept for verification during rotation window).
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitJwtKeyRotatedMetric
 *
 * @category Auth
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class JwtSigningKeyRotated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $newKeyId  ULID
     * @param  string  $newKid  string
     * @param  string  $retiredKeyId  ULID | null
     * @param  string  $retiredKid  string | null
     * @param  string  $applicationId  ULID | null (null = global key)
     * @param  string  $rotatedAt  ISO-8601 timestamp
     * @param  string  $actorId  ULID | null (null = scheduled rotation)
     */
    public function __construct(
        public readonly string $newKeyId,
        public readonly string $newKid,
        public readonly string $retiredKeyId,
        public readonly string $retiredKid,
        public readonly string $applicationId,
        public readonly string $rotatedAt,
        public readonly string $actorId,
    ) {
    }
}
