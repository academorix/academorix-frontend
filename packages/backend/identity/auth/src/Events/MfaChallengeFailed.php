<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Stackra\Auth\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MFA verification failed — wrong code / expired / already consumed. Third failure hard-deletes the challenge; the login flow restarts from step 1.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitMfaChallengeFailedMetric
 *
 * @category Auth
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MfaChallengeFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $challengeId  ULID
     * @param  string  $challengeType  string
     * @param  int  $attemptsCount  integer
     * @param  bool  $isTerminal  boolean (true = 3rd failure, challenge deleted)
     * @param  string  $failedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $challengeId,
        public readonly string $challengeType,
        public readonly int $attemptsCount,
        public readonly bool $isTerminal,
        public readonly string $failedAt,
    ) {
    }
}
