<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Auth\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `challenge_type` on `auth-mfa-challenge`.
 *
 * 'totp' | 'webauthn' | 'recovery_code'
 *
 * @category Auth
 *
 * @since    0.1.0
 */
enum AuthMfaChallengeChallengeType: string
{
    use Enum;

    case Totp = 'totp';
    case Webauthn = 'webauthn';
    case RecoveryCode = 'recovery_code';
}
