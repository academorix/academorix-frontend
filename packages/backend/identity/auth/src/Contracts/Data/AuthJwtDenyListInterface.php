<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Stackra\Auth\Contracts\Data;

use Illuminate\Container\Attributes\Bind;
use Stackra\Auth\Models\AuthJwtDenyList;

/**
 * Table shape for the `auth_jwt_deny_lists` table.
 *
 * Deny-list entry for a specific jti — revoke a JWT immediately before its natural expiry.
 *
 * @category Auth
 *
 * @since    0.1.0
 */
#[Bind(AuthJwtDenyList::class)]
interface AuthJwtDenyListInterface
{
    /**
     * Table name.
     */
    public const string TABLE = 'auth_jwt_deny_lists';

    /**
     * Primary key column.
     */
    public const string PRIMARY_KEY = 'id';

    /**
     * Primary key column type — string (ULID with prefix `ajd_`).
     */
    public const string KEY_TYPE = 'string';

    // ── Columns ───────────────────────────────────────────────

    public const string ATTR_ID         = 'id';
    public const string ATTR_JTI        = 'jti';
    public const string ATTR_REASON     = 'reason';
    public const string ATTR_REVOKED_AT = 'revoked_at';
    public const string ATTR_EXPIRES_AT = 'expires_at';
    public const string ATTR_CREATED_BY = 'created_by';
    public const string ATTR_UPDATED_BY = 'updated_by';
    public const string ATTR_CREATED_AT = 'created_at';
    public const string ATTR_UPDATED_AT = 'updated_at';
}
