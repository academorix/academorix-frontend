<?php

declare(strict_types=1);

use Stackra\Auth\Models\AuthRefreshToken;

it('is a placeholder — replace with actual AuthRefreshToken unit tests', function (): void {
    expect(AuthRefreshToken::class)->toBeString();
});
