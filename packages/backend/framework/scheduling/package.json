{
  "name": "@stackra/scheduling",
  "version": "0.0.0",
  "private": true,
  "description": "Turborepo shim for the PHP `stackra/scheduling` package. NOTE: composer install is exposed as `install:composer` — never plain `install` — so pnpm's lifecycle does not run it during a root `pnpm install`.",
  "scripts": {
    "install:composer": "composer install --no-interaction --prefer-dist",
    "lint": "../../apps/template/vendor/bin/pint --test --config=../../config/pint.json src tests",
    "lint:fix": "../../apps/template/vendor/bin/pint --config=../../config/pint.json src tests",
    "analyse": "../../apps/template/vendor/bin/phpstan analyse -c ../../config/phpstan-base.neon src",
    "test": "./vendor/bin/pest"
  }
}
