{
  "name": "@stackra/settings",
  "version": "0.1.0",
  "private": true,
  "description": "Turborepo shim for the PHP `stackra/settings` package. Ships the attribute-driven settings platform \u2014 registry + service + audit log + non-theme setting classes.",
  "scripts": {
    "install:composer": "composer install --no-interaction --prefer-dist",
    "lint": "vendor/bin/pint --test",
    "lint:fix": "vendor/bin/pint",
    "analyse": "vendor/bin/phpstan analyse --memory-limit=1G",
    "test": "vendor/bin/pest --colors=always"
  }
}
