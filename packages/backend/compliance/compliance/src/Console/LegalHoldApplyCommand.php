<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py compliance compliance --force`.

declare(strict_types=1);

namespace Academorix\Compliance\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan compliance:legal-hold-apply` — Legal hold apply — compliance command.
 *
 * @category Compliance
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'compliance:legal-hold-apply',
    description: 'Legal hold apply — compliance command.',
)]
final class LegalHoldApplyCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Legal hold apply — compliance command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Legal hold apply — compliance command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
