<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py compliance compliance --force`.

declare(strict_types=1);

namespace Stackra\Compliance\Actions\Tenant;

use Stackra\Compliance\Contracts\Repositories\SafeguardingIncidentRepositoryInterface;
use Stackra\Compliance\Data\SafeguardingIncidentData;
use Stackra\Compliance\Data\Requests\CreateSafeguardingIncidentRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/compliance/safeguarding-incidents` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Compliance
 *
 * @since    0.1.0
 */
#[AsAction(name: 'compliance.safeguarding_incidents.create')]
#[Post('/api/v1/compliance/safeguarding-incidents')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateSafeguardingIncidentAction
{
    use AsController;

    public function __construct(
        private readonly SafeguardingIncidentRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `safeguarding-incident` from the validated request payload.
     *
     * @param  CreateSafeguardingIncidentRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateSafeguardingIncidentRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(SafeguardingIncidentData::from($model), JsonResponse::HTTP_CREATED);
    }
}
