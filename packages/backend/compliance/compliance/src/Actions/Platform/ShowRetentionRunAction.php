<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py compliance compliance --force`.

declare(strict_types=1);

namespace Stackra\Compliance\Actions\Platform;

use Stackra\Compliance\Contracts\Repositories\RetentionRunRepositoryInterface;
use Stackra\Compliance\Data\RetentionRunData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/platform/compliance/retention-runs/{run}` — show action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Compliance
 *
 * @since    0.1.0
 */
#[AsAction(name: 'compliance.retention_runs.show')]
#[Get('/api/v1/platform/compliance/retention-runs/{run}')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class ShowRetentionRunAction
{
    use AsController;

    public function __construct(
        private readonly RetentionRunRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `retention-run` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): RetentionRunData
    {
        return RetentionRunData::from($this->repository->findOrFail($id));
    }
}
