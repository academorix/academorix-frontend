<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py compliance compliance --force`.

declare(strict_types=1);

namespace Stackra\Compliance\Actions\Platform;

use Stackra\Compliance\Contracts\Repositories\SubprocessorRepositoryInterface;
use Stackra\Compliance\Data\SubprocessorData;
use Stackra\Compliance\Data\Requests\UpdateSubprocessorRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/platform/compliance/subprocessors/{subprocessor}` — update action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Compliance
 *
 * @since    0.1.0
 */
#[AsAction(name: 'compliance.subprocessors.update')]
#[Patch('/api/v1/platform/compliance/subprocessors/{subprocessor}')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class UpdateSubprocessorAction
{
    use AsController;

    public function __construct(
        private readonly SubprocessorRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `subprocessor` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateSubprocessorRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateSubprocessorRequestData $data): SubprocessorData
    {
        $model = $this->repository->update($id, $data->toArray());

        return SubprocessorData::from($model);
    }
}
