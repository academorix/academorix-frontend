<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py compliance compliance --force`.

declare(strict_types=1);

namespace Academorix\Compliance\Actions\Platform;

use Academorix\Compliance\Contracts\Repositories\ConsentCategoryRepositoryInterface;
use Academorix\Compliance\Data\ConsentCategoryData;
use Academorix\Compliance\Data\Requests\UpdateConsentCategoryRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/platform/compliance/consent-categories/{category}` — update action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Compliance
 *
 * @since    0.1.0
 */
#[AsAction(name: 'compliance.consent_categories.update')]
#[Patch('/api/v1/platform/compliance/consent-categories/{category}')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class UpdateConsentCategorieAction
{
    use AsController;

    public function __construct(
        private readonly ConsentCategoryRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `consent-categorie` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateConsentCategoryRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateConsentCategoryRequestData $data): ConsentCategoryData
    {
        $model = $this->repository->update($id, $data->toArray());

        return ConsentCategoryData::from($model);
    }
}
