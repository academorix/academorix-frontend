<?php

declare(strict_types=1);

use Stackra\Compliance\Models\Subprocessor;

it('is a placeholder — replace with actual Subprocessor unit tests', function (): void {
    expect(Subprocessor::class)->toBeString();
});
