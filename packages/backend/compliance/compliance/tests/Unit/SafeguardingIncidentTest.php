<?php

declare(strict_types=1);

use Stackra\Compliance\Models\SafeguardingIncident;

it('is a placeholder — replace with actual SafeguardingIncident unit tests', function (): void {
    expect(SafeguardingIncident::class)->toBeString();
});
