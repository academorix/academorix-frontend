<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-mail --force`.

declare(strict_types=1);

namespace Stackra\NotificationsMail\Actions\Tenant;

use Stackra\NotificationsMail\Contracts\Repositories\MailSuppressionRepositoryInterface;
use Stackra\NotificationsMail\Data\MailSuppressionData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/tenant/mail-suppressions/{suppression}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category NotificationsMail
 *
 * @since    0.1.0
 */
#[AsAction(name: 'notifications_mail.mail_suppressions.show')]
#[Get('/api/v1/tenant/mail-suppressions/{suppression}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowMailSuppressionAction
{
    use AsController;

    public function __construct(
        private readonly MailSuppressionRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `mail-suppression` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): MailSuppressionData
    {
        return MailSuppressionData::from($this->repository->findOrFail($id));
    }
}
