<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-mail --force`.

declare(strict_types=1);

namespace Stackra\NotificationsMail\Actions\Tenant;

use Stackra\NotificationsMail\Contracts\Repositories\MailSuppressionRepositoryInterface;
use Stackra\NotificationsMail\Data\MailSuppressionData;
use Stackra\NotificationsMail\Data\Requests\CreateMailSuppressionRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/tenant/mail-suppressions` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category NotificationsMail
 *
 * @since    0.1.0
 */
#[AsAction(name: 'notifications_mail.mail_suppressions.create')]
#[Post('/api/v1/tenant/mail-suppressions')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateMailSuppressionAction
{
    use AsController;

    public function __construct(
        private readonly MailSuppressionRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `mail-suppression` from the validated request payload.
     *
     * @param  CreateMailSuppressionRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateMailSuppressionRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(MailSuppressionData::from($model), JsonResponse::HTTP_CREATED);
    }
}
