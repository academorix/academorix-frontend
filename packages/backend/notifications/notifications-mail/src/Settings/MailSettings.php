<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-mail --force`.

declare(strict_types=1);

namespace Stackra\Notifications\Mail\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * MailSettings — Email.
 *
 * Email channel routing + brand overlays.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category NotificationsMail
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'mail',
    label: 'Email',
    description: 'Email channel routing + brand overlays.',
    icon: 'envelope',
    scope: 'tenant',
    permission: 'settings.read',
    sortOrder: 12,
)]
class MailSettings extends Settings
{

    // ── Provider preference ──────────────────────────────────
    #[SettingGroup(label: 'Provider preference', description: 'Which mail provider to route sends through. Overrides platform default.', icon: 'arrows-right-left', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Default provider', validation: ['nullable', 'string', 'in:mailgun,sendgrid,aws-ses,postmark,resend'], group: 'Provider preference', sortOrder: 1, options: ['mailgun' => 'Mailgun', 'sendgrid' => 'SendGrid', 'aws-ses' => 'AWS SES', 'postmark' => 'Postmark', 'resend' => 'Resend'], helpText: 'Null = platform default from config.')]
    public ?string $default_provider = null;


    // ── Dedicated IP ──────────────────────────────────
    #[SettingGroup(label: 'Dedicated IP', description: 'Sends from a dedicated IP (SendGrid / SES). Requires notifications.mail.dedicated_ip entitlement.', icon: 'server-stack', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enable dedicated IP', validation: ['boolean'], group: 'Dedicated IP', sortOrder: 1)]
    public bool $dedicated_ip_enabled = false;

    #[SettingField(controlType: ControlType::Text, label: 'IP pool name', validation: ['nullable', 'string', 'max:100'], group: 'Dedicated IP', sortOrder: 2, helpText: 'Provider-side IP pool name.')]
    public ?string $dedicated_ip_pool_name = null;


    // ── Footer customisation ──────────────────────────────────
    #[SettingGroup(label: 'Footer customisation', description: 'Overlay on top of core\'s brand.footer_text.', icon: 'document-text', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Text, label: 'Company name (footer)', validation: ['nullable', 'string', 'max:200'], group: 'Footer customisation', sortOrder: 1, helpText: 'Overrides tenant.name in CAN-SPAM footer.')]
    public ?string $footer_company_name = null;

    #[SettingField(controlType: ControlType::Textarea, label: 'Postal address override', validation: ['nullable', 'string', 'max:500'], group: 'Footer customisation', sortOrder: 2, helpText: 'Overrides core\'s sender.postal_address for mail-specific compliance edge cases.')]
    public ?string $footer_postal_address_override = null;

    #[SettingField(controlType: ControlType::Text, label: 'Unsubscribe link text', validation: ['required', 'string', 'max:100'], group: 'Footer customisation', sortOrder: 3)]
    public string $footer_unsubscribe_text = 'Unsubscribe from these emails';

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'mail';
    }
}
