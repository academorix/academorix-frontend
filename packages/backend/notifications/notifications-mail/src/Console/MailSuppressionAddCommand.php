<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-mail --force`.

declare(strict_types=1);

namespace Academorix\NotificationsMail\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan notifications:mail:suppression-add` — Mail:suppression add — notifications command.
 *
 * @category NotificationsMail
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'notifications:mail:suppression-add',
    description: 'Mail:suppression add — notifications command.',
)]
final class MailSuppressionAddCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Mail:suppression add — notifications command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Mail:suppression add — notifications command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
