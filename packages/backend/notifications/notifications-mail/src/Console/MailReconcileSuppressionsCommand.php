<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-mail --force`.

declare(strict_types=1);

namespace Stackra\NotificationsMail\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan notifications:mail:reconcile-suppressions` — Mail:reconcile suppressions — notifications command.
 *
 * @category NotificationsMail
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'notifications:mail:reconcile-suppressions',
    description: 'Mail:reconcile suppressions — notifications command.',
)]
final class MailReconcileSuppressionsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Mail:reconcile suppressions — notifications command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Mail:reconcile suppressions — notifications command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
