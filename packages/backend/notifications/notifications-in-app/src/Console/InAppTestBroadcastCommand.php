<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-in-app --force`.

declare(strict_types=1);

namespace Stackra\NotificationsInApp\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan notifications:in-app:test-broadcast` — In app:test broadcast — notifications command.
 *
 * @category NotificationsInApp
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'notifications:in-app:test-broadcast',
    description: 'In app:test broadcast — notifications command.',
)]
final class InAppTestBroadcastCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('In app:test broadcast — notifications command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('In app:test broadcast — notifications command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
