<?php

declare(strict_types=1);

use Stackra\Notifications\Models\NotificationTemplate;

it('is a placeholder — replace with actual NotificationTemplate unit tests', function (): void {
    expect(NotificationTemplate::class)->toBeString();
});
