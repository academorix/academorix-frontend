<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications announcements --force`.

declare(strict_types=1);

namespace Stackra\Announcements\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan announcements:list` — List every announcements row.
 *
 * @category Announcements
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'announcements:list',
    description: 'List every announcements row.',
)]
final class ListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List every announcements row', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List every announcements row completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
