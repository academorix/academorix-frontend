<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications announcements --force`.

declare(strict_types=1);

namespace Academorix\Announcements\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Recipient opened the announcement.
 *
 * ## Consumers
 *
 * - realtime::UpdateReadCount
 * - notifications::TrackEngagement
 *
 * @category Announcements
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AnnouncementRead implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $deliveryId  ULID
     * @param  string  $announcementId  ULID
     * @param  string  $readByUserId  ULID
     * @param  string  $readAt  ISO 8601
     */
    public function __construct(
        public readonly string $deliveryId,
        public readonly string $announcementId,
        public readonly string $readByUserId,
        public readonly string $readAt,
    ) {
    }
}
