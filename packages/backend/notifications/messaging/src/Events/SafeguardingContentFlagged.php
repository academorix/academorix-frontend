<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications messaging --force`.

declare(strict_types=1);

namespace Stackra\Messaging\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P0 event. Content matched a keyword filter or reported. Sent to safeguarding module for review.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention 7yr)
 * - safeguarding::CreateIncident
 * - notifications::AlertModerators
 *
 * @category Messaging
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SafeguardingContentFlagged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $messageId  ULID
     * @param  string  $conversationId  ULID
     * @param  string  $flagReason  'keyword_match'|'user_report'
     * @param  array  $detectedKeywords  array<string>
     */
    public function __construct(
        public readonly string $messageId,
        public readonly string $conversationId,
        public readonly string $flagReason,
        public readonly array $detectedKeywords,
    ) {
    }
}
