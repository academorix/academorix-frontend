<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

namespace Academorix\Leads\Actions\Tenant;

use Academorix\Leads\Contracts\Repositories\LeadRepositoryInterface;
use Academorix\Leads\Data\LeadData;
use Academorix\Leads\Data\Requests\CreateLeadRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/leads` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Leads
 *
 * @since    0.1.0
 */
#[AsAction(name: 'leads.create')]
#[Post('/api/v1/leads')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateLeadAction
{
    use AsController;

    public function __construct(
        private readonly LeadRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `lead` from the validated request payload.
     *
     * @param  CreateLeadRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateLeadRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(LeadData::from($model), JsonResponse::HTTP_CREATED);
    }
}
