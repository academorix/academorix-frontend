<?php

/**
 * @file database/migrations/2026_07_15_120000_create_lead_activities_table.php
 *
 * @description
 * Create the `lead_activities` table for the `LeadActivity` entity.
 * Owned by the leads module. Every column + index reference
 * goes through {@see Academorix\Leads\Contracts\Data\LeadActivityInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

use Academorix\Leads\Contracts\Data\LeadActivityInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(LeadActivityInterface::TABLE, function (Blueprint $table): void {
            $table->string(LeadActivityInterface::ATTR_ID, 64)->primary();
            $table->string(LeadActivityInterface::ATTR_TENANT_ID, 64);
            $table->foreign(LeadActivityInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(LeadActivityInterface::ATTR_LEAD_ID, 64);
            $table->foreign(LeadActivityInterface::ATTR_LEAD_ID)->references('id')->on('leads')->onDelete('cascade');
            $table->string(LeadActivityInterface::ATTR_TYPE, 32);
            $table->text(LeadActivityInterface::ATTR_BODY)->nullable();
            $table->jsonb(LeadActivityInterface::ATTR_METADATA)->nullable();
            $table->string(LeadActivityInterface::ATTR_ACTOR_ID, 64)->nullable();
            $table->foreign(LeadActivityInterface::ATTR_ACTOR_ID)->references('id')->on('users')->onDelete('set null');
            $table->timestampTz(LeadActivityInterface::ATTR_OCCURRED_AT);
            $table->string(LeadActivityInterface::ATTR_CREATED_BY, 64)->nullable();
            $table->string(LeadActivityInterface::ATTR_UPDATED_BY, 64)->nullable();
            $table->timestampTz(LeadActivityInterface::ATTR_CREATED_AT);
            $table->timestampTz(LeadActivityInterface::ATTR_UPDATED_AT);
            $table->index([LeadActivityInterface::ATTR_TENANT_ID, LeadActivityInterface::ATTR_LEAD_ID, LeadActivityInterface::ATTR_OCCURRED_AT], 'lead_activities_lead_time_idx');
            $table->index([LeadActivityInterface::ATTR_TENANT_ID, LeadActivityInterface::ATTR_TYPE, LeadActivityInterface::ATTR_OCCURRED_AT], 'lead_activities_type_time_idx');
        });

        DB::statement('CREATE INDEX lead_activities_actor_time_idx ON ' . LeadActivityInterface::TABLE . ' (' . LeadActivityInterface::ATTR_TENANT_ID . ', ' . LeadActivityInterface::ATTR_ACTOR_ID . ', ' . LeadActivityInterface::ATTR_OCCURRED_AT . ') WHERE actor_id IS NOT NULL');
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(LeadActivityInterface::TABLE);
    }
};
