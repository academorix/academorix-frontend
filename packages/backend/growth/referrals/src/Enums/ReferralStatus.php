<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `status` on `referral`.
 *
 * Enum: pending_claim / claimed / signed_up / trigger_pending / vesting / vested / rewarded / cancelled / expired / fraudulent.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
enum ReferralStatus: string
{
    use Enum;

    case PendingClaim = 'pending_claim';
    case Claimed = 'claimed';
    case SignedUp = 'signed_up';
    case TriggerPending = 'trigger_pending';
    case Vesting = 'vesting';
    case Vested = 'vested';
    case Rewarded = 'rewarded';
    case Cancelled = 'cancelled';
    case Expired = 'expired';
    case Fraudulent = 'fraudulent';
}
