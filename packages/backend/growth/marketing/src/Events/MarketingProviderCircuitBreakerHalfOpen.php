<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The open_until timer elapsed. HalfOpenCircuitBreakerJob transitions the breaker to HALF_OPEN + dispatches 1 test event (half_open_test_events). Success closes the breaker; failure re-opens it.
 *
 * ## Consumers
 *
 * - monitoring::MetricsCollector
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MarketingProviderCircuitBreakerHalfOpen implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $providerConfigId  ULID
     * @param  string  $halfOpenAt  ISO 8601
     * @param  string  $testEventId  ULID (mev_*) — the event used for the probe
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $providerConfigId,
        public readonly string $halfOpenAt,
        public readonly string $testEventId,
    ) {
    }
}
