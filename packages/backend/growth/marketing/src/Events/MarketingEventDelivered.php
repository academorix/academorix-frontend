<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Every eligible provider delivery succeeded (delivered_provider_count == eligible_provider_count). Terminal state — event.status transitions to 'delivered'.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector (increments academorix.marketing.events.delivered_total)
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MarketingEventDelivered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $marketingEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $eventType  string
     * @param  int  $deliveredProviderCount  integer
     * @param  int  $totalLatencyMs  integer (capture → last-delivery)
     * @param  string  $deliveredAt  ISO 8601
     */
    public function __construct(
        public readonly string $marketingEventId,
        public readonly string $tenantId,
        public readonly string $eventType,
        public readonly int $deliveredProviderCount,
        public readonly int $totalLatencyMs,
        public readonly string $deliveredAt,
    ) {
    }
}
