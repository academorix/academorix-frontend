<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CRITICAL. consecutive_failure_count reached failure_threshold. Sets circuit_breaker_open_until = now() + open_duration_seconds. Blocks all subsequent dispatches until the timer elapses. Every fire is a P1 signal — the provider is dead + attribution reporting will degrade until reset.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchMarketingProviderCircuitBreakerOpenedNotification (cannot-opt-out — pages ops + notifies tenant owner)
 * - monitoring::MetricsCollector (increments academorix.marketing.circuit_breakers.opened_total)
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MarketingProviderCircuitBreakerOpened implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $providerConfigId  ULID (mpc_*)
     * @param  int  $consecutiveFailureCount  integer
     * @param  string  $openUntil  ISO 8601
     * @param  string  $lastErrorCode  string | null
     * @param  string  $lastErrorMessage  string | null
     * @param  string  $openedAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $providerConfigId,
        public readonly int $consecutiveFailureCount,
        public readonly string $openUntil,
        public readonly string $lastErrorCode,
        public readonly string $lastErrorMessage,
        public readonly string $openedAt,
    ) {
    }
}
