<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Ops or tenant admin invoked POST /dead-letters/{id}/replay OR the marketing:replay-dead-letter command. Enqueues a fresh DispatchToProviderJob with attempt_number=1. Increments dead_letter.replay_attempts + sets last_replay_at.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - monitoring::MetricsCollector
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MarketingDeadLetterReplayed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $marketingDeadLetterId  ULID
     * @param  string  $marketingEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $replayAttemptNumber  integer (dead_letter.replay_attempts after increment)
     * @param  string  $replayedByUserId  ULID
     * @param  string  $replayedAt  ISO 8601
     */
    public function __construct(
        public readonly string $marketingDeadLetterId,
        public readonly string $marketingEventId,
        public readonly string $tenantId,
        public readonly int $replayAttemptNumber,
        public readonly string $replayedByUserId,
        public readonly string $replayedAt,
    ) {
    }
}
