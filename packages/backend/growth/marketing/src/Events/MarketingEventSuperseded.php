<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A newer event with the same (tenant_id, deduplication_key) arrived while the earlier event was still in-flight. The observer marks the earlier event.status = 'superseded' + cancels its pending DispatchToProviderJob. Only fires when the old event has NOT yet delivered — delivered events are immutable.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MarketingEventSuperseded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $supersededEventId  ULID (the older event)
     * @param  string  $supersedingEventId  ULID (the newer event)
     * @param  string  $tenantId  ULID
     * @param  string  $deduplicationKey  string
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $supersededEventId,
        public readonly string $supersedingEventId,
        public readonly string $tenantId,
        public readonly string $deduplicationKey,
        public readonly string $at,
    ) {
    }
}
