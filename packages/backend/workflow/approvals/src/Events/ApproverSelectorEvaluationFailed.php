<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Stackra\Approvals\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ExpressionLanguageAdapter caught a parse error OR runtime error OR timeout evaluating a selector_expression at instance-create time. THE INSTANCE FAILS TO CREATE — the action itself refuses with 500 approval.selector_evaluation_failed. Operators + template author notified so they fix the template.
 *
 * ## Consumers
 *
 * - notifications::NotifyTemplateAuthorOfFailure (opt-in — the author of the template gets an email)
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord (P1)
 * - observability::MetricsCollector (feeds stackra.approvals.selector.evaluation_failures counter)
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ApproverSelectorEvaluationFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalTemplateId  ULID
     * @param  string  $approvalTemplateApproverId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $actionKey  string
     * @param  string  $selectorExpression  string (the raw source that failed)
     * @param  string  $failureReason  'parse_error' | 'runtime_error' | 'timeout' | 'invalid_return_type'
     * @param  string  $errorMessage  string (redacted — no secrets, no context values)
     * @param  string  $failedAt  ISO-8601
     */
    public function __construct(
        public readonly string $approvalTemplateId,
        public readonly string $approvalTemplateApproverId,
        public readonly string $tenantId,
        public readonly string $actionKey,
        public readonly string $selectorExpression,
        public readonly string $failureReason,
        public readonly string $errorMessage,
        public readonly string $failedAt,
    ) {
    }
}
