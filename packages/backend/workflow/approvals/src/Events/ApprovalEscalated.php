<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Approvals\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * template.on_timeout='escalate' AND the sweeper hit the timeout. New escalation approvers were resolved via template.escalation_selector + appended to the pending requirement's resolved_approvers. expires_at extended by template.expires_after_hours / 2.
 *
 * ## Consumers
 *
 * - notifications::NotifyApproversJob → ApprovalRequestedNotification to the new escalation targets
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ApprovalEscalated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalInstanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $escalatedAt  ISO-8601
     * @param  string  $priorExpiresAt  ISO-8601
     * @param  string  $newExpiresAt  ISO-8601
     * @param  string  $addedApproverIds  ULID[]
     */
    public function __construct(
        public readonly string $approvalInstanceId,
        public readonly string $tenantId,
        public readonly string $escalatedAt,
        public readonly string $priorExpiresAt,
        public readonly string $newExpiresAt,
        public readonly string $addedApproverIds,
    ) {
    }
}
