<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Stackra\Approvals\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * DispatchApprovalRemindersJob wrote a new `approval_reminders` row AND dispatched an ApprovalReminderNotification to a specific approver. Fires ONCE per reminder — de-duplicated by the (requirement, approver, reminder_number) composite unique.
 *
 * ## Consumers
 *
 * - observability::MetricsCollector
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ApprovalReminderDispatched implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalReminderId  ULID (`apn_...`)
     * @param  string  $approvalRequirementId  ULID
     * @param  string  $approvalInstanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $approverId  ULID
     * @param  int  $reminderNumber  integer (1, 2, 3, ...)
     * @param  string  $sentAt  ISO-8601
     */
    public function __construct(
        public readonly string $approvalReminderId,
        public readonly string $approvalRequirementId,
        public readonly string $approvalInstanceId,
        public readonly string $tenantId,
        public readonly string $approverId,
        public readonly int $reminderNumber,
        public readonly string $sentAt,
    ) {
    }
}
