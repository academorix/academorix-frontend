<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Approvals\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Requester POSTed withdraw. Instance transitions pending→cancelled AND every pending requirement transitions to failed.
 *
 * ## Consumers
 *
 * - notifications::NotifyPendingApproversJob → ApprovalCancelledNotification (to the pending approvers, so they don't waste time deciding a withdrawn request)
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord
 * - observability::MetricsCollector
 * - broadcast::PublishToApprovalChannels
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ApprovalCancelled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalInstanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $requesterId  ULID
     * @param  string  $cancelledAt  ISO-8601
     * @param  string  $withdrawReason  string | null
     */
    public function __construct(
        public readonly string $approvalInstanceId,
        public readonly string $tenantId,
        public readonly string $requesterId,
        public readonly string $cancelledAt,
        public readonly string $withdrawReason,
    ) {
    }
}
