<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Stackra\Approvals\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Quorum reached on one requirement (after ApprovalDecisionRecorded's quorum-recompute). Downstream requirements with next sort_order may unblock.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - broadcast::PublishToApprovalChannels
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ApprovalRequirementSatisfied implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalRequirementId  ULID
     * @param  string  $approvalInstanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $groupName  string
     * @param  string  $satisfiedAt  ISO-8601
     * @param  string  $unblockedRequirementIds  ULID[] (requirements transitioned pending→blocked→pending because this satisfied their sort_order predecessor)
     */
    public function __construct(
        public readonly string $approvalRequirementId,
        public readonly string $approvalInstanceId,
        public readonly string $tenantId,
        public readonly string $groupName,
        public readonly string $satisfiedAt,
        public readonly string $unblockedRequirementIds,
    ) {
    }
}
