<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Stackra\Approvals\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Tenant admin edited a template's IMMUTABLE-post-instance fields (name, priority, description) — no version bump. Distinct from ApprovalTemplateVersionBumped which fires on when_expression / group changes.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ApprovalTemplateUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalTemplateId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedFields  string[] (fields that changed)
     * @param  string  $editorUserId  ULID
     */
    public function __construct(
        public readonly string $approvalTemplateId,
        public readonly string $tenantId,
        public readonly string $changedFields,
        public readonly string $editorUserId,
    ) {
    }
}
