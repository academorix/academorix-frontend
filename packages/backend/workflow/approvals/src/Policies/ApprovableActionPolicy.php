<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Stackra\Approvals\Policies;

use Stackra\User\Models\User;
use Stackra\Approvals\Models\ApprovableAction;

/**
 * Authorization policy for {@see \Stackra\Approvals\Models\ApprovableAction}.
 *
 * Wired via `#[UsePolicy]` on the Model.
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final class ApprovableActionPolicy
{
    /**
     * tenant plane: any authenticated user in the tenant can list approvable-actions their tenant has active templates on (used by requester UI to explain 'why did this need approval?').
     *
     * @return bool
     */
    public function viewAny(User $user, ApprovableAction $model): bool
    {
        return $user->can('approvals.actions.viewAny');
    }
    /**
     * same as viewAny
     *
     * @return bool
     */
    public function view(User $user, ApprovableAction $model): bool
    {
        return $user->can('approvals.actions.view');
    }
    /**
     * platform_admin only — usually the seeder does the writes; manual creation is a break-glass for actions registered outside the boot-time discovery pass.
     *
     * @return bool
     */
    public function platformCreate(User $user, ApprovableAction $model): bool
    {
        return $user->can('platform.approvals.actions.create');
    }
    /**
     * platform_admin only — mostly toggles is_active.
     *
     * @return bool
     */
    public function platformUpdate(User $user, ApprovableAction $model): bool
    {
        return $user->can('platform.approvals.actions.update');
    }
}
