<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow tasks --force`.

declare(strict_types=1);

namespace Stackra\Tasks\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan tasks:cancel` — Cancel — tasks command.
 *
 * @category Tasks
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'tasks:cancel',
    description: 'Cancel — tasks command.',
)]
final class CancelCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Cancel — tasks command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Cancel — tasks command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
