<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow tasks --force`.

declare(strict_types=1);

namespace Stackra\Tasks\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan tasks:assign` — Assign — tasks command.
 *
 * @category Tasks
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'tasks:assign',
    description: 'Assign — tasks command.',
)]
final class AssignCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Assign — tasks command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Assign — tasks command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
