<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow tasks --force`.

declare(strict_types=1);

namespace Academorix\Tasks\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan tasks:automate-tick` — Automate tick — tasks command.
 *
 * @category Tasks
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'tasks:automate-tick',
    description: 'Automate tick — tasks command.',
)]
final class AutomateTickCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Automate tick — tasks command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Automate tick — tasks command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
