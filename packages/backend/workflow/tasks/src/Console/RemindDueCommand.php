<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow tasks --force`.

declare(strict_types=1);

namespace Academorix\Tasks\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan tasks:remind-due` — Remind due — tasks command.
 *
 * @category Tasks
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'tasks:remind-due',
    description: 'Remind due — tasks command.',
)]
final class RemindDueCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Remind due — tasks command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Remind due — tasks command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
