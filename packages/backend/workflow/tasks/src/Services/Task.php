<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow tasks --force`.

declare(strict_types=1);

namespace Stackra\Tasks\Services;

use Stackra\Tasks\Contracts\Services\TaskInterface;
use Illuminate\Container\Attributes\Scoped;
use Stackra\Tasks\Contracts\Repositories\TaskAssignmentRepositoryInterface;

/**
 * Concrete service — Task.
 *
 * Implements {@see TaskInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Tasks
 *
 * @since    0.1.0
 */
#[Scoped]
final class Task implements TaskInterface
{
    /**
     * @param  TaskAssignmentRepositoryInterface  $taskAssignmentRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly TaskAssignmentRepositoryInterface $taskAssignmentRepository,
    ) {
    }
}
