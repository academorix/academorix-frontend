<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow tasks --force`.

declare(strict_types=1);

namespace Academorix\Tasks\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Tasks module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Tasks
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum TasksPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `tasks.viewAny` — Narrows by assignment: user sees tasks they're an assignee/collaborator/watcher of, unless they hold viewAll below.
     */
    #[Label('Narrows by assignment: user sees tasks they\'re an assignee/collaborator/watcher')]
    #[Description('Narrows by assignment: user sees tasks they\'re an assignee/collaborator/watcher of, unless they hold viewAll below.')]
    case TasksViewAny = 'tasks.viewAny';

    /**
     * `tasks.viewAll` — See every task in the tenant regardless of assignment.
     */
    #[Label('See every task in the tenant regardless of assignment')]
    #[Description('See every task in the tenant regardless of assignment.')]
    case TasksViewAll = 'tasks.viewAll';

    /**
     * `tasks.view` — Tasks View
     */
    #[Label('Tasks View')]
    #[Description('Tasks View')]
    case TasksView = 'tasks.view';

    /**
     * `tasks.create` — Tasks Create
     */
    #[Label('Tasks Create')]
    #[Description('Tasks Create')]
    case TasksCreate = 'tasks.create';

    /**
     * `tasks.update` — Update task fields (title/description/priority/due).
     */
    #[Label('Update task fields (title/description/priority/due)')]
    #[Description('Update task fields (title/description/priority/due).')]
    case TasksUpdate = 'tasks.update';

    /**
     * `tasks.delete` — Soft-delete.
     */
    #[Label('Soft-delete')]
    #[Description('Soft-delete.')]
    case TasksDelete = 'tasks.delete';

    /**
     * `tasks.complete` — Tasks Complete
     */
    #[Label('Tasks Complete')]
    #[Description('Tasks Complete')]
    case TasksComplete = 'tasks.complete';

    /**
     * `tasks.cancel` — Tasks Cancel
     */
    #[Label('Tasks Cancel')]
    #[Description('Tasks Cancel')]
    case TasksCancel = 'tasks.cancel';

    /**
     * `task-assignments.create` — Assign a task to a user.
     */
    #[Label('Assign a task to a user')]
    #[Description('Assign a task to a user.')]
    case TaskAssignmentsCreate = 'task-assignments.create';

    /**
     * `task-assignments.update` — Task-assignments Update
     */
    #[Label('Task-assignments Update')]
    #[Description('Task-assignments Update')]
    case TaskAssignmentsUpdate = 'task-assignments.update';

    /**
     * `task-assignments.delete` — Task-assignments Delete
     */
    #[Label('Task-assignments Delete')]
    #[Description('Task-assignments Delete')]
    case TaskAssignmentsDelete = 'task-assignments.delete';

    /**
     * `task-assignments.accept` — Assignee accepts their own assignment.
     */
    #[Label('Assignee accepts their own assignment')]
    #[Description('Assignee accepts their own assignment.')]
    case TaskAssignmentsAccept = 'task-assignments.accept';

    /**
     * `task-assignments.decline` — Task-assignments Decline
     */
    #[Label('Task-assignments Decline')]
    #[Description('Task-assignments Decline')]
    case TaskAssignmentsDecline = 'task-assignments.decline';

    /**
     * `task-comments.create` — Comment on tasks user has access to.
     */
    #[Label('Comment on tasks user has access to')]
    #[Description('Comment on tasks user has access to.')]
    case TaskCommentsCreate = 'task-comments.create';

    /**
     * `task-comments.update` — Edit own comment within 15 minutes of creation.
     */
    #[Label('Edit own comment within 15 minutes of creation')]
    #[Description('Edit own comment within 15 minutes of creation.')]
    case TaskCommentsUpdate = 'task-comments.update';

    /**
     * `task-comments.delete` — Soft-delete own comment.
     */
    #[Label('Soft-delete own comment')]
    #[Description('Soft-delete own comment.')]
    case TaskCommentsDelete = 'task-comments.delete';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::TasksViewAny => Guard::Sanctum,
            self::TasksViewAll => Guard::Sanctum,
            self::TasksView => Guard::Sanctum,
            self::TasksCreate => Guard::Sanctum,
            self::TasksUpdate => Guard::Sanctum,
            self::TasksDelete => Guard::Sanctum,
            self::TasksComplete => Guard::Sanctum,
            self::TasksCancel => Guard::Sanctum,
            self::TaskAssignmentsCreate => Guard::Sanctum,
            self::TaskAssignmentsUpdate => Guard::Sanctum,
            self::TaskAssignmentsDelete => Guard::Sanctum,
            self::TaskAssignmentsAccept => Guard::Sanctum,
            self::TaskAssignmentsDecline => Guard::Sanctum,
            self::TaskCommentsCreate => Guard::Sanctum,
            self::TaskCommentsUpdate => Guard::Sanctum,
            self::TaskCommentsDelete => Guard::Sanctum,
        };
    }
}
