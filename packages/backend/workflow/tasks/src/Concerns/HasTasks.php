<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow tasks --force`.

declare(strict_types=1);

namespace Stackra\Tasks\Concerns;

/**
 * Trait — HasTasks.
 *
 * TODO(gen): describe the compositional intent. Common patterns:
 * BelongsTo<X> (adds an `$x_id` FK + `x()` relation), Has<X>
 * (inverse — adds a `has<X>()` scope + a `<x>()` relation),
 * Manages<X> (opinionated helpers that operate on <X> children).
 *
 * @category Tasks
 *
 * @since    0.1.0
 */
trait HasTasks
{
    // TODO(gen): implement the trait's composition surface.
}
