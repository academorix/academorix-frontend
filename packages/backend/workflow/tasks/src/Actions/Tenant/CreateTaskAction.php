<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow tasks --force`.

declare(strict_types=1);

namespace Stackra\Tasks\Actions\Tenant;

use Stackra\Tasks\Contracts\Repositories\TaskRepositoryInterface;
use Stackra\Tasks\Data\TaskData;
use Stackra\Tasks\Data\Requests\CreateTaskRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/tasks` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Tasks
 *
 * @since    0.1.0
 */
#[AsAction(name: 'tasks.create')]
#[Post('/api/v1/tasks')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateTaskAction
{
    use AsController;

    public function __construct(
        private readonly TaskRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `task` from the validated request payload.
     *
     * @param  CreateTaskRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateTaskRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(TaskData::from($model), JsonResponse::HTTP_CREATED);
    }
}
