<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow tasks --force`.

declare(strict_types=1);

namespace Stackra\Tasks\Actions\Tenant;

use Stackra\Tasks\Contracts\Repositories\TaskRepositoryInterface;
use Stackra\Tasks\Data\TaskData;
use Stackra\Tasks\Data\Requests\UpdateTaskRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/tasks/{task}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Tasks
 *
 * @since    0.1.0
 */
#[AsAction(name: 'tasks.update')]
#[Patch('/api/v1/tasks/{task}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateTaskAction
{
    use AsController;

    public function __construct(
        private readonly TaskRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `task` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateTaskRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateTaskRequestData $data): TaskData
    {
        $model = $this->repository->update($id, $data->toArray());

        return TaskData::from($model);
    }
}
