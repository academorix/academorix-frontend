<?php

declare(strict_types=1);

use Stackra\Tasks\Models\TaskComment;

it('is a placeholder — replace with actual TaskComment unit tests', function (): void {
    expect(TaskComment::class)->toBeString();
});
