<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan credit-note:apply` — Apply — credit note command.
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'credit-note:apply',
    description: 'Apply — credit note command.',
)]
final class ApplyCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Apply — credit note command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Apply — credit note command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
