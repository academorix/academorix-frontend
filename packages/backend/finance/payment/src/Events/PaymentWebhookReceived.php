<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Every inbound provider webhook (verified or unverified). Low-signal audit trail event — feeds security incident investigations.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (compliance record — every inbound webhook logged)
 * - monitoring::MetricsCollector
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PaymentWebhookReceived implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID | null (null when tenant cannot be resolved from the webhook body — signature-invalid case)
     * @param  string  $provider  string
     * @param  string  $providerEventId  string | null
     * @param  string  $providerEventType  string | null
     * @param  bool  $signatureValid  boolean
     * @param  string  $receivedAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $providerEventId,
        public readonly string $providerEventType,
        public readonly bool $signatureValid,
        public readonly string $receivedAt,
    ) {
    }
}
