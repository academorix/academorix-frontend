<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TaxCalculator::calculate() persisted a tax_calculations row. Terminal state — the calculation is IMMUTABLE thereafter.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years / 10 for Enterprise)
 * - tax::InvalidateCalculationCountCache
 * - monitoring::MetricsCollector (increments academorix.tax.calculations.completed_total; records dispatch duration)
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TaxCalculationCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $taxCalculationId  ULID (txc_*)
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceLineId  ULID | null
     * @param  int  $sourceAmountCents  integer
     * @param  string  $sourceCurrency  string
     * @param  int  $taxAmountCents  integer
     * @param  string  $provider  string
     * @param  bool  $appliedExemption  boolean
     * @param  int  $latencyMs  integer
     * @param  string  $calculatedAt  ISO 8601
     */
    public function __construct(
        public readonly string $taxCalculationId,
        public readonly string $tenantId,
        public readonly string $invoiceLineId,
        public readonly int $sourceAmountCents,
        public readonly string $sourceCurrency,
        public readonly int $taxAmountCents,
        public readonly string $provider,
        public readonly bool $appliedExemption,
        public readonly int $latencyMs,
        public readonly string $calculatedAt,
    ) {
    }
}
