<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance wallet --force`.

declare(strict_types=1);

namespace Academorix\Wallet\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan wallet:reconcile` — Reconcile the wallet data.
 *
 * @category Wallet
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'wallet:reconcile',
    description: 'Reconcile the wallet data.',
)]
final class ReconcileCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Reconcile the wallet data', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Reconcile the wallet data completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
