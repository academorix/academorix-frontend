<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance dunning --force`.

declare(strict_types=1);

namespace Academorix\Dunning\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan dunning:list-runs` — List runs — dunning command.
 *
 * @category Dunning
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'dunning:list-runs',
    description: 'List runs — dunning command.',
)]
final class ListRunsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List runs — dunning command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List runs — dunning command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
