<?php

declare(strict_types=1);

use Academorix\Expenses\Models\ExpenseCategory;

it('is a placeholder — replace with actual ExpenseCategory unit tests', function (): void {
    expect(ExpenseCategory::class)->toBeString();
});
