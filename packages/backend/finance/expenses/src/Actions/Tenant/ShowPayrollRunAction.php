<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Academorix\Expenses\Actions\Tenant;

use Academorix\Expenses\Contracts\Repositories\PayrollRunRepositoryInterface;
use Academorix\Expenses\Data\PayrollRunData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Get;

/**
 * `GET /api/v1/payroll-runs/{run}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
#[AsAction(name: 'expenses.payroll_runs.show')]
#[Get('/api/v1/payroll-runs/{run}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowPayrollRunAction
{
    use AsController;

    public function __construct(
        private readonly PayrollRunRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `payroll-run` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): PayrollRunData
    {
        return PayrollRunData::from($this->repository->findOrFail($id));
    }
}
