<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance order --force`.

declare(strict_types=1);

namespace Academorix\Order\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * OrderLine deleted. Triggers total recalculation.
 *
 * ## Consumers
 *
 * - order::RecalculateOrderTotalsListener
 *
 * @category Order
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class OrderLineRemoved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $orderId  ULID
     * @param  string  $orderLineId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $orderId,
        public readonly string $orderLineId,
        public readonly string $tenantId,
        public readonly string $at,
    ) {
    }
}
