<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance digital-passes --force`.

declare(strict_types=1);

namespace Academorix\DigitalPasses\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Pass revoked — lost / stolen / membership cancelled / security. Push-notify device.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - jobs::PushRevocationUpdateJob
 * - notifications::NotifyPassRevoked
 *
 * @category DigitalPasses
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PassRevoked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $walletPassId  ULID
     * @param  string  $revokedByUserId  ULID
     * @param  string  $reason  'lost'|'stolen'|'membership_cancelled'|'security'|'other'
     */
    public function __construct(
        public readonly string $walletPassId,
        public readonly string $revokedByUserId,
        public readonly string $reason,
    ) {
    }
}
