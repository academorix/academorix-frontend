<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Refund\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Provider webhook (via payment::PaymentWebhookHandler dispatching a refund-status webhook) OR ReconcileRefundsJob detected a state change on the provider side. The refund's status is now aligned with the provider's authoritative record. Distinct from RefundSucceeded / RefundFailed — this event fires FIRST (before terminal transition) to record the reconciliation itself.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - observability::MetricsCollector (academorix.refund.reconciliations_total labelled by drift_detected)
 *
 * @category Refund
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RefundProviderReconciled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $refundId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $providerReferenceId  string
     * @param  string  $providerStatus  string (provider-native status)
     * @param  string  $reconciledLocalStatus  string (our terminal status after reconciliation)
     * @param  bool  $driftDetected  boolean (true when the local status differed from provider)
     * @param  string  $reconciledAt  ISO 8601
     */
    public function __construct(
        public readonly string $refundId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $providerReferenceId,
        public readonly string $providerStatus,
        public readonly string $reconciledLocalStatus,
        public readonly bool $driftDetected,
        public readonly string $reconciledAt,
    ) {
    }
}
