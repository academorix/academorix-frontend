<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Refund\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan refund:generate-credit-note` — Generate credit note — refund command.
 *
 * @category Refund
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'refund:generate-credit-note',
    description: 'Generate credit note — refund command.',
)]
final class GenerateCreditNoteCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Generate credit note — refund command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Generate credit note — refund command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
