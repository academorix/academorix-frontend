<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform ai --force`.

declare(strict_types=1);

namespace Stackra\Ai\Bootstrappers;

use Stackra\Ai\Contracts\Services\ToolDiscoveryBootstrapperInterface;
use Illuminate\Container\Attributes\Scoped;
use Stackra\Ai\Contracts\Repositories\AiConversationRepositoryInterface;

/**
 * Concrete service — ToolDiscoveryBootstrapper.
 *
 * Implements {@see ToolDiscoveryBootstrapperInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * Moved from `Services/` to `Bootstrappers/` on 2026-07-21 (Phase
 * E3) — the class name pattern `*Bootstrapper` belongs under
 * `Bootstrappers/` per `.kiro/steering/code-standards.md` and
 * `.kiro/steering/package-architecture.md` § Locked folder table.
 *
 * @category Ai
 *
 * @since    0.1.0
 */
#[Scoped]
final class ToolDiscoveryBootstrapper implements ToolDiscoveryBootstrapperInterface
{
    /**
     * @param  AiConversationRepositoryInterface  $aiConversationRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly AiConversationRepositoryInterface $aiConversationRepository,
    ) {
    }
}
