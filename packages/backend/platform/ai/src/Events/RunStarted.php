<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform ai --force`.

declare(strict_types=1);

namespace Stackra\Ai\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New AiRun created — model call in-flight. Feeds cost-tracking pre-commit.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (verbose channel)
 * - ai::TrackTokenUsage
 *
 * @category Ai
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RunStarted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $runId  ULID (arv_*)
     * @param  string  $conversationId  ULID | null
     * @param  string  $tenantId  ULID
     * @param  string  $persona  string
     * @param  string  $provider  'openai' | 'anthropic' | 'gemini' | 'groq' | 'xai' | 'ollama'
     * @param  string  $model  string
     * @param  string  $startedAt  ISO 8601
     */
    public function __construct(
        public readonly string $runId,
        public readonly string $conversationId,
        public readonly string $tenantId,
        public readonly string $persona,
        public readonly string $provider,
        public readonly string $model,
        public readonly string $startedAt,
    ) {
    }
}
