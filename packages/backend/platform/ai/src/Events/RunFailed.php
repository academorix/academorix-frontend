<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform ai --force`.

declare(strict_types=1);

namespace Academorix\Ai\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AiRun.status flipped to 'failed'. Fail-closed: no partial responses reach user.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::AlertOnAiFailure
 * - activity::WriteToActivityLog
 *
 * @category Ai
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RunFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $runId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $persona  string
     * @param  string  $provider  string
     * @param  string  $errorMessage  string
     * @param  string  $errorCode  string | null
     */
    public function __construct(
        public readonly string $runId,
        public readonly string $tenantId,
        public readonly string $persona,
        public readonly string $provider,
        public readonly string $errorMessage,
        public readonly string $errorCode,
    ) {
    }
}
