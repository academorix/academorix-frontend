<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform ai --force`.

declare(strict_types=1);

namespace Stackra\Ai\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AiEmbedding created or updated. Fires for background SearchIndexer persona on new drills / coach notes / files.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (verbose channel)
 * - search::UpdateRagIndex
 *
 * @category Ai
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EmbeddingIndexed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $embeddingId  ULID (aie_*)
     * @param  string  $tenantId  ULID
     * @param  string  $sourceType  string
     * @param  string  $sourceId  ULID
     * @param  string  $model  string
     */
    public function __construct(
        public readonly string $embeddingId,
        public readonly string $tenantId,
        public readonly string $sourceType,
        public readonly string $sourceId,
        public readonly string $model,
    ) {
    }
}
