<?php

/**
 * @file database/migrations/2026_07_15_120003_create_ai_runs_table.php
 *
 * @description
 * Create the `ai_runs` table for the `AiRun` entity.
 * Owned by the ai module. Every column + index reference
 * goes through {@see Stackra\Ai\Contracts\Data\AiRunInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform ai --force`.

declare(strict_types=1);

use Stackra\Ai\Contracts\Data\AiRunInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(AiRunInterface::TABLE, function (Blueprint $table): void {
            $table->string(AiRunInterface::ATTR_ID, 30)->primary();
            $table->string(AiRunInterface::ATTR_TENANT_ID, 30);
            $table->foreign(AiRunInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(AiRunInterface::ATTR_CONVERSATION_ID, 30)->nullable();
            $table->foreign(AiRunInterface::ATTR_CONVERSATION_ID)->references('id')->on('ai_conversations')->onDelete('cascade');
            $table->string(AiRunInterface::ATTR_PERSONA, 32);
            $table->string(AiRunInterface::ATTR_PROVIDER, 32);
            $table->string(AiRunInterface::ATTR_MODEL, 64);
            $table->string(AiRunInterface::ATTR_PROMPT_HASH, 64);
            $table->string(AiRunInterface::ATTR_RESPONSE_HASH, 64)->nullable();
            $table->string(AiRunInterface::ATTR_STATUS, 16)->default('running');
            $table->timestampTz(AiRunInterface::ATTR_STARTED_AT);
            $table->timestampTz(AiRunInterface::ATTR_COMPLETED_AT)->nullable();
            $table->integer(AiRunInterface::ATTR_LATENCY_MS)->nullable();
            $table->integer(AiRunInterface::ATTR_TOKENS_IN)->nullable();
            $table->integer(AiRunInterface::ATTR_TOKENS_OUT)->nullable();
            $table->unsignedBigInteger(AiRunInterface::ATTR_COST_MINOR)->nullable();
            $table->text(AiRunInterface::ATTR_ERROR_MESSAGE)->nullable();
            $table->jsonb(AiRunInterface::ATTR_METADATA)->nullable();
            $table->timestampTz(AiRunInterface::ATTR_CREATED_AT);
            $table->timestampTz(AiRunInterface::ATTR_UPDATED_AT);
            $table->index([AiRunInterface::ATTR_TENANT_ID, AiRunInterface::ATTR_CONVERSATION_ID, AiRunInterface::ATTR_STARTED_AT], 'ai_runs_conversation_time_idx');
            $table->index([AiRunInterface::ATTR_TENANT_ID, AiRunInterface::ATTR_PERSONA, AiRunInterface::ATTR_STATUS], 'ai_runs_persona_status_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(AiRunInterface::TABLE);
    }
};
