<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform webhook --force`.

declare(strict_types=1);

namespace Stackra\Webhook\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `state` on `webhook-delivery`.
 *
 * spatie state: pending, inFlight, delivered, failedRetryable, failedPermanent, cancelled.
 *
 * @category Webhook
 *
 * @since    0.1.0
 */
enum WebhookDeliveryState: string
{
    use Enum;

    case Pending = 'pending';
    case InFlight = 'inFlight';
    case Delivered = 'delivered';
    case FailedRetryable = 'failedRetryable';
    case FailedPermanent = 'failedPermanent';
    case Cancelled = 'cancelled';
}
