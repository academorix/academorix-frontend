<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Stackra\Region\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan region:archive` — Archive — region command.
 *
 * @category Region
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'region:archive',
    description: 'Archive — region command.',
)]
final class ArchiveCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Archive — region command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Archive — region command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
