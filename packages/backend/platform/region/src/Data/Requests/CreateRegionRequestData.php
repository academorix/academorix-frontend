<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Stackra\Region\Data\Requests;

use Spatie\LaravelData\Attributes\MapInputName;
use Spatie\LaravelData\Data;
use Spatie\LaravelData\Mappers\SnakeCaseMapper;
use Spatie\LaravelData\Attributes\Validation\Max;
use Spatie\LaravelData\Attributes\Validation\Min;
use Spatie\LaravelData\Attributes\Validation\Regex;
use Spatie\LaravelData\Attributes\Validation\StringType;

/**
 * Server-side create request DTO for `Region`.
 *
 * Consumed by the corresponding Action; validated on construction
 * through Spatie Data's property-attribute pipeline (no `rules()`
 * method). See `.kiro/steering/data-first.md`.
 *
 * @category Region
 *
 * @since    0.1.0
 */
#[MapInputName(SnakeCaseMapper::class)]
final class CreateRegionRequestData extends Data
{
    public function __construct(
        #[StringType, Min(2), Max(128)]
        public string $name,

        #[StringType, Min(2), Max(64), Regex('/^[a-z][a-z0-9-]*[a-z0-9]$/')]
        public string $slug,

        #[StringType, Regex('/^[A-Z]{2}$/')]
        public ?string $countryCode = null,

        #[StringType, Max(8), Regex('/^[A-Z]{2}-[A-Z0-9]{1,3}$/')]
        public ?string $subdivisionCode = null,

        #[StringType, Regex('/^[A-Z]{3}$/')]
        public string $currency,

        #[StringType, Min(1), Max(8)]
        public string $currencySymbol,

        #[StringType, Min(3), Max(64)]
        public string $timezone,

        #[StringType, Max(16), Regex('/^[a-z]{2,3}(-[A-Za-z]{4})?(-[A-Z]{2}|-[0-9]{3})?(-[A-Za-z0-9]{5,8})*$/')]
        public string $locale,

        public bool $rtl,

        public array $weekendDays,

        public array $taxConfig,

        public int $fiscalYearStartMonth,

        #[StringType]
        public string $status,

        public bool $isDefault,

        public int $sortOrder,

        #[StringType]
        public ?string $notes = null,

        public ?array $metadata = null,
    ) {
    }
}
