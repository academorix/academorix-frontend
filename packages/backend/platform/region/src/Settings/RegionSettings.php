<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Stackra\Region\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * RegionSettings — Regions.
 *
 * Tenant-wide policy for commercial + regulatory zones.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Region
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'region',
    label: 'Regions',
    description: 'Tenant-wide policy for commercial + regulatory zones.',
    icon: 'globe-alt',
    scope: 'tenant',
    permission: 'regions.manage',
    sortOrder: 40,
)]
class RegionSettings extends Settings
{

    // ── Provisioning ──────────────────────────────────
    #[SettingGroup(label: 'Provisioning', description: 'How new regions get created.', icon: 'plus-circle', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Seed default region asynchronously', group: 'Provisioning', sortOrder: 1, helpText: 'When on, the default region is provisioned on a background queue immediately after tenant creation. Off = the default region is created inside the tenant-provisioning transaction (slower but the tenant is never briefly regionless).')]
    public bool $async_seed = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Require tax config when creating a region', group: 'Provisioning', sortOrder: 2, helpText: 'When on, POST /regions refuses without a fully-formed tax_config. When off, defaults from the country\'s shipped defaults auto-fill.')]
    public bool $require_tax_config_on_create = false;

    #[SettingField(controlType: ControlType::Toggle, label: 'Reject unknown country codes', group: 'Provisioning', sortOrder: 3, helpText: 'When on, region create refuses country codes not in the shipped defaults table. Off = accept any ISO 3166-1 code (needed for supranational zones).')]
    public bool $reject_unknown_country_code = true;


    // ── Reconciliation ──────────────────────────────────
    #[SettingGroup(label: 'Reconciliation', description: 'How the system heals default-region drift.', icon: 'arrow-path', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Alert on default-region drift', group: 'Reconciliation', sortOrder: 1, helpText: 'When on, the reconciler pages ops on drift (a tenant with 0 or 2+ default regions). Off = drift is only logged.')]
    public bool $alert_on_drift = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Auto-heal drift', group: 'Reconciliation', sortOrder: 2, helpText: 'When on, the reconciler auto-heals drift (promote-oldest OR re-seed-from-country_code). Off by default — surface + acknowledge before healing.')]
    public bool $auto_heal = false;


    // ── Retention ──────────────────────────────────
    #[SettingGroup(label: 'Retention', description: 'How long archived regions are kept.', icon: 'archive-box', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Number, label: 'Days to hold archived regions', group: 'Retention', sortOrder: 1, min: 730, max: 2555, helpText: 'Days a soft-deleted (archived) region stays recoverable. Compliance floor: 730 days (2 years — financial record retention). Enterprise tenants may extend up to 2555 (7 years).')]
    public int $archived_regions_retention_days = 730;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'region';
    }
}
