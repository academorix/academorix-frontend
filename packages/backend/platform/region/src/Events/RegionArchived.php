<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Academorix\Region\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Region.status transitioned to archived. Refused when is_default=true (observer catches it). Refused when any inbound row (Branch, Membership) still references this Region (REGION_IN_USE). Archived Regions vanish from selectors but existing rows stay valid.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::MaybeDispatchRegionArchivedNotification
 *
 * @category Region
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RegionArchived implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $regionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $archivedBy  ULID
     * @param  string  $reason  string | null
     * @param  string  $hardDeleteScheduledFor  ISO 8601 timestamp
     */
    public function __construct(
        public readonly string $regionId,
        public readonly string $tenantId,
        public readonly string $archivedBy,
        public readonly string $reason,
        public readonly string $hardDeleteScheduledFor,
    ) {
    }
}
