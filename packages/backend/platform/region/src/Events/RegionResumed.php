<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Stackra\Region\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Region.status transitioned paused → active. Reverses RegionPaused. New bookings + memberships accepted again.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - branch::InvalidateRegionAvailabilityCache
 * - facility::InvalidateBookingWindowCache
 * - finance::ResumeNewMembershipsInRegion
 *
 * @category Region
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RegionResumed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $regionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $resumedBy  ULID
     * @param  string  $wasPausedAt  ISO 8601 timestamp
     */
    public function __construct(
        public readonly string $regionId,
        public readonly string $tenantId,
        public readonly string $resumedBy,
        public readonly string $wasPausedAt,
    ) {
    }
}
