<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Coach.status transitioned. Values: active / paused / archived. Distinct from Staff.employment_status — a Staff may be active while their Coach is paused (temporary coaching hiatus during a busy admin period).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchCoachOnLeaveNotification (when to_status='paused' or 'archived' — to active team members, cannot-opt-out)
 *
 * @category Staff
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CoachStatusChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $coachId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromStatus  'active' | 'paused' | 'archived'
     * @param  string  $toStatus  'active' | 'paused' | 'archived'
     * @param  string  $reason  string | null
     * @param  string  $changedByUserId  ULID
     */
    public function __construct(
        public readonly string $coachId,
        public readonly string $tenantId,
        public readonly string $fromStatus,
        public readonly string $toStatus,
        public readonly string $reason,
        public readonly string $changedByUserId,
    ) {
    }
}
