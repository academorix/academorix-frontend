<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Stackra\Staff\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Staff.employment_status transitioned suspended → active. Distinct from StaffReturnedFromLeave.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchStaffReinstatedNotification
 *
 * @category Staff
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class StaffReinstated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $staffId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $reinstatedAt  ISO 8601
     * @param  string  $reinstatedByUserId  ULID
     */
    public function __construct(
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $reinstatedAt,
        public readonly string $reinstatedByUserId,
    ) {
    }
}
