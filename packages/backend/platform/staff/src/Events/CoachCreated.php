<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Stackra\Staff\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CoachRepository committed a new coaches row. Fires when a Staff first assumes a coaching role — either at Staff hire (admin sets up the coach profile alongside the Staff row) OR later (a receptionist promoted to assistant coach gets a Coach row attached).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateBranchCoachListCache
 * - notifications::DispatchCoachWelcomeNotification (opt-in to the coach's User)
 *
 * @category Staff
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CoachCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $coachId  ULID (coa_*)
     * @param  string  $staffId  ULID (stf_*)
     * @param  string  $tenantId  ULID
     * @param  string  $sportKey  string
     * @param  string  $coachingLevel  string (assistant / head / senior / director / consultant)
     * @param  string  $specialization  string | null
     * @param  string  $coachStartedAt  YYYY-MM-DD
     * @param  string  $createdByUserId  ULID
     */
    public function __construct(
        public readonly string $coachId,
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $sportKey,
        public readonly string $coachingLevel,
        public readonly string $specialization,
        public readonly string $coachStartedAt,
        public readonly string $createdByUserId,
    ) {
    }
}
