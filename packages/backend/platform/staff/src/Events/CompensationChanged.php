<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Any of Staff.compensation_type / compensation_amount_cents / compensation_currency / pay_frequency mutated. PRIVATE-TIER event — the payload is NEVER routed through the activity feed and the audit row lands in the dedicated `staff_compensation_history` table with 7-year retention. Consumers include the Finance module (Wave 4 payroll re-computation) and the HR audit trail.
 *
 * ## Consumers
 *
 * - audit::WriteToCompensationHistoryLog (retention: 7 years; NOT the normal audits table)
 * - notifications::DispatchCompensationChangedNotification (to affected staff ONLY, via HR channel — never in-app default)
 * - finance::RecomputePayroll (Wave 4)
 *
 * @category Staff
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CompensationChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $staffId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedByUserId  ULID
     * @param  int  $before  {'compensation_type': 'string | null', 'compensation_amount_cents': 'integer | null', 'compensation_currency': 'string | null', 'pay_frequency': 'string | null'}
     * @param  int  $after  {'compensation_type': 'string | null', 'compensation_amount_cents': 'integer | null', 'compensation_currency': 'string | null', 'pay_frequency': 'string | null'}
     * @param  string  $changedFields  string[]
     * @param  string  $effectiveAt  YYYY-MM-DD
     */
    public function __construct(
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $changedByUserId,
        public readonly int $before,
        public readonly int $after,
        public readonly string $changedFields,
        public readonly string $effectiveAt,
    ) {
    }
}
