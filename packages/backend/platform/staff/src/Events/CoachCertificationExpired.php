<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Stackra\Staff\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ExpireCoachCertificationsJob detected a certification past its expires_at. The certification stays in the array but is tagged with `expired_at`. Fires per expired certification.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchCoachCertificationExpiringNotification (final — the 30/14/7-day pre-expiry reminders fired earlier from ExpiringSoon jobs)
 *
 * @category Staff
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CoachCertificationExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $coachId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $certification  {'organization': 'string', 'certification': 'string', 'certified_at': 'YYYY-MM-DD', 'expires_at': 'YYYY-MM-DD'}
     * @param  string  $expiredAt  ISO 8601
     */
    public function __construct(
        public readonly string $coachId,
        public readonly string $tenantId,
        public readonly string $certification,
        public readonly string $expiredAt,
    ) {
    }
}
