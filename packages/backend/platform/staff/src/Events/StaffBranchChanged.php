<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Staff.branch_id mutated. RARE — most staff have one home branch for their entire tenure. When it changes, notifies old + new branch managers. Wave 2b marks Staff.branch_id immutable post-create; this event fires only via the Wave 3+ branch-transfer flow.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchStaffBranchChangedNotification (to old + new branch managers)
 * - cache::InvalidateBranchStaffListCache (both source + dest branches)
 *
 * @category Staff
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class StaffBranchChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $staffId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromBranchId  ULID (brn_*)
     * @param  string  $toBranchId  ULID (brn_*)
     * @param  string  $effectiveAt  YYYY-MM-DD
     * @param  string  $reason  string | null
     * @param  string  $transferredByUserId  ULID
     */
    public function __construct(
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $fromBranchId,
        public readonly string $toBranchId,
        public readonly string $effectiveAt,
        public readonly string $reason,
        public readonly string $transferredByUserId,
    ) {
    }
}
