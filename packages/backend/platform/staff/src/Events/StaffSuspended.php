<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Stackra\Staff\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Staff.employment_status transitioned active/on_leave → suspended. AUDIT-CRITICAL — suspensions are disciplinary and every fire is retained 7 years.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchStaffSuspendedNotification (to branch manager — cannot-opt-out; the suspended staff themselves are notified via HR channel)
 * - staff::CascadeCoachStatusOnStaffOffboarding (pauses linked Coach)
 *
 * @category Staff
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class StaffSuspended implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $staffId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $suspendedAt  ISO 8601
     * @param  string  $suspensionReason  string
     * @param  string  $suspensionExpiresAt  YYYY-MM-DD | null
     * @param  string  $suspendedByUserId  ULID
     */
    public function __construct(
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $suspendedAt,
        public readonly string $suspensionReason,
        public readonly string $suspensionExpiresAt,
        public readonly string $suspendedByUserId,
    ) {
    }
}
