<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Stackra\Staff\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * StaffUserSettings — My staff record.
 *
 * Per-user preferences for how staff-lifecycle events appear in their inbox.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Staff
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'staff_user',
    label: 'My staff record',
    description: 'Per-user preferences for how staff-lifecycle events appear in their inbox.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 55,
)]
class StaffUserSettings extends Settings
{

    // ── Reminders ──────────────────────────────────
    #[SettingGroup(label: 'Reminders', icon: 'bell', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Send me probation reminders', group: 'Reminders', sortOrder: 1)]
    public bool $receive_probation_reminders = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Send me certification expiring reminders', group: 'Reminders', sortOrder: 2)]
    public bool $receive_certification_reminders = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'staff_user';
    }
}
