<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Stackra\Staff\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * StaffSettings — Staff & Coaches.
 *
 * Tenant-wide staff lifecycle defaults + feature opt-ins.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Staff
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'staff',
    label: 'Staff & Coaches',
    description: 'Tenant-wide staff lifecycle defaults + feature opt-ins.',
    icon: 'user-group',
    scope: 'tenant',
    permission: 'staff.viewAny',
    sortOrder: 40,
)]
class StaffSettings extends Settings
{

    // ── Hire defaults ──────────────────────────────────
    #[SettingGroup(label: 'Hire defaults', description: 'Defaults applied to new Staff rows. Can be overridden per hire.', icon: 'user-plus', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Default employment type', group: 'Hire defaults', sortOrder: 1, options: ['full_time' => 'Full-time', 'part_time' => 'Part-time', 'contract' => 'Contract', 'volunteer' => 'Volunteer', 'intern' => 'Intern', 'seasonal' => 'Seasonal'])]
    public string $default_employment_type = 'full_time';

    #[SettingField(controlType: ControlType::Number, label: 'Default probation period (days)', group: 'Hire defaults', sortOrder: 2, min: 0, max: 365, helpText: '0 = no probation.')]
    public int $default_probation_days = 90;

    #[SettingField(controlType: ControlType::Number, label: 'Default weekly hours', group: 'Hire defaults', sortOrder: 3, min: 0, max: 168)]
    public int $default_weekly_hours = 40;


    // ── Compensation tracking ──────────────────────────────────
    #[SettingGroup(label: 'Compensation tracking', description: 'Opt-in to compensation tracking. When enabled, HR + owner can record compensation_* fields; the observer refuses writes without the compensation permission.', icon: 'banknotes', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Track compensation in Stackra', group: 'Compensation tracking', sortOrder: 1, helpText: 'Enable this to track wage/salary/hourly rate on staff. Data is regulated_financial-tier, retained 7 years. Turning this on writes a compliance audit row + requires re-consent by the tenant admin.')]
    public bool $enable_compensation_tracking = false;

    #[SettingField(controlType: ControlType::Number, label: 'Compensation change threshold (%)', group: 'Compensation tracking', sortOrder: 2, min: 5, max: 200, helpText: 'Compensation changes above this percent require the large-delta bypass permission. Guards against payroll fat-finger errors.')]
    public int $compensation_max_delta_percent = 50;

    #[SettingField(controlType: ControlType::Select, label: 'Default compensation currency', group: 'Compensation tracking', sortOrder: 3, options: ['USD' => 'US Dollar', 'EUR' => 'Euro', 'GBP' => 'Pound Sterling', 'AED' => 'UAE Dirham', 'CAD' => 'Canadian Dollar', 'AUD' => 'Australian Dollar'], helpText: 'Falls back to the branch region\'s currency when unset.')]
    public string $default_currency = 'USD';


    // ── Org chart ──────────────────────────────────
    #[SettingGroup(label: 'Org chart', icon: 'share', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Track reporting hierarchy', group: 'Org chart', sortOrder: 1, helpText: 'Enterprise feature. When on, staff can be assigned a reports_to_staff_id and the org-chart visualisation appears in the SDUI.')]
    public bool $enable_org_chart = false;


    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', icon: 'bell', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Text, label: 'Probation reminder cadence', group: 'Notifications', sortOrder: 1, options: ['30' => '30 days before', '14' => '14 days before', '7' => '7 days before', '3' => '3 days before', '1' => '1 day before'])]
    public array $probation_reminder_thresholds_days = [14, 7, 1];

    #[SettingField(controlType: ControlType::Text, label: 'Coach certification expiring cadence', group: 'Notifications', sortOrder: 2, options: ['60' => '60 days before', '30' => '30 days before', '14' => '14 days before', '7' => '7 days before'])]
    public array $certification_expiring_thresholds_days = [30, 14, 7];

    #[SettingField(controlType: ControlType::Text, label: 'Offboarding-in-progress reminder cadence', group: 'Notifications', sortOrder: 3, options: ['1' => '1 day', '3' => '3 days', '7' => '7 days'])]
    public array $offboarding_reminder_cadence_days = [1, 3, 7];

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'staff';
    }
}
