<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan staff:put-on-leave` — Put on leave — staff command.
 *
 * @category Staff
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'staff:put-on-leave',
    description: 'Put on leave — staff command.',
)]
final class PutOnLeaveCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Put on leave — staff command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Put on leave — staff command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
