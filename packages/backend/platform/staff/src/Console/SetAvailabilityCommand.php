<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Stackra\Staff\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan coach:set-availability` — Set availability — coach command.
 *
 * @category Staff
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'coach:set-availability',
    description: 'Set availability — coach command.',
)]
final class SetAvailabilityCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Set availability — coach command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Set availability — coach command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
