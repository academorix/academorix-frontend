<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branding --force`.

declare(strict_types=1);

namespace Stackra\Branding\Actions\Platform;

use Stackra\Branding\Contracts\Repositories\BrandingRepositoryInterface;
use Stackra\Branding\Data\BrandingData;
use Stackra\Branding\Data\Requests\CreateBrandingRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/brandings` — create action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Branding
 *
 * @since    0.1.0
 */
#[AsAction(name: 'branding.brandings.create')]
#[Post('/api/v1/brandings')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class CreateBrandingAction
{
    use AsController;

    public function __construct(
        private readonly BrandingRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `branding` from the validated request payload.
     *
     * @param  CreateBrandingRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateBrandingRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(BrandingData::from($model), JsonResponse::HTTP_CREATED);
    }
}
