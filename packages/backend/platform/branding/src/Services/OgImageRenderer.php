<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branding --force`.

declare(strict_types=1);

namespace Stackra\Branding\Services;

use Stackra\Branding\Contracts\Services\OgImageRendererInterface;
use Illuminate\Container\Attributes\Scoped;
use Stackra\Branding\Contracts\Repositories\BrandingRepositoryInterface;

/**
 * Concrete service — OgImageRenderer.
 *
 * Implements {@see OgImageRendererInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Branding
 *
 * @since    0.1.0
 */
#[Scoped]
final class OgImageRenderer implements OgImageRendererInterface
{
    /**
     * @param  BrandingRepositoryInterface  $brandingRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly BrandingRepositoryInterface $brandingRepository,
    ) {
    }
}
