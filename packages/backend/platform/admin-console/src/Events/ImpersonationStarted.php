<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform admin-console --force`.

declare(strict_types=1);

namespace Academorix\AdminConsole\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P0 event. Platform admin impersonated a tenant user for support. Full audit + notify tenant owner.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::NotifyTenantOwnerOfImpersonation
 *
 * @category AdminConsole
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ImpersonationStarted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $impersonationId  ULID (imp_*)
     * @param  string  $platformUserId  ULID
     * @param  string  $targetUserId  ULID
     * @param  string  $targetTenantId  ULID
     * @param  string  $reason  string
     * @param  string  $expiresAt  ISO 8601
     */
    public function __construct(
        public readonly string $impersonationId,
        public readonly string $platformUserId,
        public readonly string $targetUserId,
        public readonly string $targetTenantId,
        public readonly string $reason,
        public readonly string $expiresAt,
    ) {
    }
}
