<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform credentials --force`.

declare(strict_types=1);

namespace Academorix\Credentials\Actions\Tenant;

use Academorix\Credentials\Contracts\Repositories\GateRepositoryInterface;
use Academorix\Credentials\Data\GateData;
use Academorix\Credentials\Data\Requests\CreateGateRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/gates` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Credentials
 *
 * @since    0.1.0
 */
#[AsAction(name: 'credentials.gates.create')]
#[Post('/api/v1/gates')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateGateAction
{
    use AsController;

    public function __construct(
        private readonly GateRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `gate` from the validated request payload.
     *
     * @param  CreateGateRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateGateRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(GateData::from($model), JsonResponse::HTTP_CREATED);
    }
}
