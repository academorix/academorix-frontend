<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform credentials --force`.

declare(strict_types=1);

namespace Stackra\Credentials\Actions\Tenant;

use Stackra\Credentials\Contracts\Repositories\CredentialRepositoryInterface;
use Stackra\Credentials\Data\CredentialData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/credentials/{credential}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Credentials
 *
 * @since    0.1.0
 */
#[AsAction(name: 'credentials.show')]
#[Get('/api/v1/credentials/{credential}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowCredentialAction
{
    use AsController;

    public function __construct(
        private readonly CredentialRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `credential` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): CredentialData
    {
        return CredentialData::from($this->repository->findOrFail($id));
    }
}
