<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform credentials --force`.

declare(strict_types=1);

namespace Academorix\Credentials\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Gate settings updated (mode, allowed_credential_kinds, entry_rules).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - realtime::PushGateConfig
 *
 * @category Credentials
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class GateConfigured implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $gateId  ULID
     * @param  string  $configuredByUserId  ULID
     * @param  string  $mode  'entry'|'exit'|'both'
     */
    public function __construct(
        public readonly string $gateId,
        public readonly string $configuredByUserId,
        public readonly string $mode,
    ) {
    }
}
