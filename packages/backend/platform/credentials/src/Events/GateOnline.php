<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform credentials --force`.

declare(strict_types=1);

namespace Stackra\Credentials\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Gate device heartbeat received. status: offline → online.
 *
 * ## Consumers
 *
 * - realtime::UpdateGateStatus
 *
 * @category Credentials
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class GateOnline implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $gateId  ULID (gte_*)
     * @param  string  $deviceId  ULID
     * @param  string  $connectedAt  ISO 8601
     */
    public function __construct(
        public readonly string $gateId,
        public readonly string $deviceId,
        public readonly string $connectedAt,
    ) {
    }
}
