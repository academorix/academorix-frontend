<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform credentials --force`.

declare(strict_types=1);

namespace Stackra\Credentials\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New Credential row committed. Kind IN nfc_tag / rfid_tag / qr_code / wallet_pass. Serial + status='active'.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::NotifyCredentialIssued
 *
 * @category Credentials
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CredentialIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $credentialId  ULID (cre_*)
     * @param  string  $tenantId  ULID
     * @param  string  $holderType  'athlete'|'staff'|'user'
     * @param  string  $holderId  ULID
     * @param  string  $kind  'nfc_tag'|'rfid_tag'|'qr_code'|'wallet_pass'
     * @param  string  $serial  string
     * @param  string  $issuedByUserId  ULID
     */
    public function __construct(
        public readonly string $credentialId,
        public readonly string $tenantId,
        public readonly string $holderType,
        public readonly string $holderId,
        public readonly string $kind,
        public readonly string $serial,
        public readonly string $issuedByUserId,
    ) {
    }
}
