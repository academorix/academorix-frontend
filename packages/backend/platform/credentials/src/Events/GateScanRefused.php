<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform credentials --force`.

declare(strict_types=1);

namespace Stackra\Credentials\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Scan failed — revoked / expired / cloned / unknown credential. Gate does NOT open.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention 1yr)
 * - activity::WriteToActivityLog
 * - notifications::AlertOnRefusedScanRate
 * - realtime::BroadcastGateAlert
 *
 * @category Credentials
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class GateScanRefused implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $credentialId  ULID|null
     * @param  string  $gateId  ULID
     * @param  string  $attemptedAt  ISO 8601
     * @param  string  $reasonCode  'revoked'|'expired'|'unknown'|'wrong_tenant'|'cloned_uid_detected'|'holder_left'|'no_active_membership'|'safeguarding_hold'|'device_offline'|'invalid_signature'
     */
    public function __construct(
        public readonly string $credentialId,
        public readonly string $gateId,
        public readonly string $attemptedAt,
        public readonly string $reasonCode,
    ) {
    }
}
