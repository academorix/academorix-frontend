<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform credentials --force`.

declare(strict_types=1);

namespace Stackra\Credentials\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Gate device stopped heartbeating. status: online → offline. Alert ops.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::AlertOpsGateOffline
 *
 * @category Credentials
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class GateOffline implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $gateId  ULID
     * @param  string  $deviceId  ULID
     * @param  string  $lastSeenAt  ISO 8601
     */
    public function __construct(
        public readonly string $gateId,
        public readonly string $deviceId,
        public readonly string $lastSeenAt,
    ) {
    }
}
