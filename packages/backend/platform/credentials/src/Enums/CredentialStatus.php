<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform credentials --force`.

declare(strict_types=1);

namespace Academorix\Credentials\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `status` on `credential`.
 *
 * Backed enum — one case per allowed value.
 *
 * @category Credentials
 *
 * @since    0.1.0
 */
enum CredentialStatus: string
{
    use Enum;

    case Issued = 'issued';
    case Active = 'active';
    case Lost = 'lost';
    case Revoked = 'revoked';
    case Replaced = 'replaced';
}
