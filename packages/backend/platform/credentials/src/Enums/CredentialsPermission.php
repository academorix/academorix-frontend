<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform credentials --force`.

declare(strict_types=1);

namespace Stackra\Credentials\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Credentials module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Credentials
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum CredentialsPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `credentials.viewAny` — Credentials ViewAny
     */
    #[Label('Credentials ViewAny')]
    #[Description('Credentials ViewAny')]
    case CredentialsViewAny = 'credentials.viewAny';

    /**
     * `credentials.view` — Users see own credentials.
     */
    #[Label('Users see own credentials')]
    #[Description('Users see own credentials.')]
    case CredentialsView = 'credentials.view';

    /**
     * `credentials.issue` — Issue a new credential to a holder.
     */
    #[Label('Issue a new credential to a holder')]
    #[Description('Issue a new credential to a holder.')]
    case CredentialsIssue = 'credentials.issue';

    /**
     * `credentials.revoke` — Front desk can only revoke with reason=lost.
     */
    #[Label('Front desk can only revoke with reason=lost')]
    #[Description('Front desk can only revoke with reason=lost.')]
    case CredentialsRevoke = 'credentials.revoke';

    /**
     * `credentials.report-lost` — Report a credential lost/stolen.
     */
    #[Label('Report a credential lost/stolen')]
    #[Description('Report a credential lost/stolen.')]
    case CredentialsReportLost = 'credentials.report-lost';

    /**
     * `gates.viewAny` — Gates ViewAny
     */
    #[Label('Gates ViewAny')]
    #[Description('Gates ViewAny')]
    case GatesViewAny = 'gates.viewAny';

    /**
     * `gates.view` — Gates View
     */
    #[Label('Gates View')]
    #[Description('Gates View')]
    case GatesView = 'gates.view';

    /**
     * `gates.create` — Gates Create
     */
    #[Label('Gates Create')]
    #[Description('Gates Create')]
    case GatesCreate = 'gates.create';

    /**
     * `gates.update` — Gates Update
     */
    #[Label('Gates Update')]
    #[Description('Gates Update')]
    case GatesUpdate = 'gates.update';

    /**
     * `gates.configure` — Change entry rules / modes / allowed credential kinds.
     */
    #[Label('Change entry rules / modes / allowed credential kinds')]
    #[Description('Change entry rules / modes / allowed credential kinds. High-impact.')]
    case GatesConfigure = 'gates.configure';

    /**
     * `gates.delete` — Gates Delete
     */
    #[Label('Gates Delete')]
    #[Description('Gates Delete')]
    case GatesDelete = 'gates.delete';

    /**
     * `gate-scans.viewAny` — Front desk sees current-day scans only.
     */
    #[Label('Front desk sees current-day scans only')]
    #[Description('Front desk sees current-day scans only.')]
    case GateScansViewAny = 'gate-scans.viewAny';

    /**
     * `gate-scans.view` — Gate-scans View
     */
    #[Label('Gate-scans View')]
    #[Description('Gate-scans View')]
    case GateScansView = 'gate-scans.view';

    /**
     * `gate-scans.security-audit` — Full historical scan audit — for security investigations (clone detection).
     */
    #[Label('Full historical scan audit — for security investigations (clone detection)')]
    #[Description('Full historical scan audit — for security investigations (clone detection).')]
    case GateScansSecurityAudit = 'gate-scans.security-audit';

    /**
     * `platform.credentials.audit` — Cross-tenant credential + gate security audit.
     */
    #[Label('Cross-tenant credential + gate security audit')]
    #[Description('Cross-tenant credential + gate security audit.')]
    case PlatformCredentialsAudit = 'platform.credentials.audit';

    /**
     * `platform.gate-scans.audit` — Cross-tenant scan pattern audit — clone-uid detection tuning.
     */
    #[Label('Cross-tenant scan pattern audit — clone-uid detection tuning')]
    #[Description('Cross-tenant scan pattern audit — clone-uid detection tuning.')]
    case PlatformGateScansAudit = 'platform.gate-scans.audit';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::CredentialsViewAny => Guard::Sanctum,
            self::CredentialsView => Guard::Sanctum,
            self::CredentialsIssue => Guard::Sanctum,
            self::CredentialsRevoke => Guard::Sanctum,
            self::CredentialsReportLost => Guard::Sanctum,
            self::GatesViewAny => Guard::Sanctum,
            self::GatesView => Guard::Sanctum,
            self::GatesCreate => Guard::Sanctum,
            self::GatesUpdate => Guard::Sanctum,
            self::GatesConfigure => Guard::Sanctum,
            self::GatesDelete => Guard::Sanctum,
            self::GateScansViewAny => Guard::Sanctum,
            self::GateScansView => Guard::Sanctum,
            self::GateScansSecurityAudit => Guard::Sanctum,
            self::PlatformCredentialsAudit => Guard::PlatformAdmin,
            self::PlatformGateScansAudit => Guard::PlatformAdmin,
        };
    }
}
