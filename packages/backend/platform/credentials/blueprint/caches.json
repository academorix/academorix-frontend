{
  "id": "stackra://modules/credentials/caches",
  "$version": 1,
  "title": "credentials — cache keys",
  "description": "Cache keys the module owns + TTLs + invalidation triggers + tags.",
  "keys": []
}
