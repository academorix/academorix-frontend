<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform safeguarding --force`.

declare(strict_types=1);

namespace Stackra\Safeguarding\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Safeguarding module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Safeguarding
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum SafeguardingPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `safeguarding.incidents.viewAny` — See incident list.
     */
    #[Label('See incident list')]
    #[Description('See incident list. Officer + owner only.')]
    case SafeguardingIncidentsViewAny = 'safeguarding.incidents.viewAny';

    /**
     * `safeguarding.incidents.view` — See individual incident detail.
     */
    #[Label('See individual incident detail')]
    #[Description('See individual incident detail. Case-access grants apply.')]
    case SafeguardingIncidentsView = 'safeguarding.incidents.view';

    /**
     * `safeguarding.incidents.report` — Report a new incident.
     */
    #[Label('Report a new incident')]
    #[Description('Report a new incident. Broad access to encourage disclosure.')]
    case SafeguardingIncidentsReport = 'safeguarding.incidents.report';

    /**
     * `safeguarding.incidents.investigate` — Open investigation.
     */
    #[Label('Open investigation')]
    #[Description('Open investigation. Safeguarding officer only.')]
    case SafeguardingIncidentsInvestigate = 'safeguarding.incidents.investigate';

    /**
     * `safeguarding.incidents.resolve` — Mark incident resolved.
     */
    #[Label('Mark incident resolved')]
    #[Description('Mark incident resolved.')]
    case SafeguardingIncidentsResolve = 'safeguarding.incidents.resolve';

    /**
     * `safeguarding.incidents.escalate` — Escalate to external authorities.
     */
    #[Label('Escalate to external authorities')]
    #[Description('Escalate to external authorities. Applies legal hold.')]
    case SafeguardingIncidentsEscalate = 'safeguarding.incidents.escalate';

    /**
     * `safeguarding.incidents.grant-access` — Grant per-incident case access to another user.
     */
    #[Label('Grant per-incident case access to another user')]
    #[Description('Grant per-incident case access to another user. Full audit chain.')]
    case SafeguardingIncidentsGrantAccess = 'safeguarding.incidents.grant-access';

    /**
     * `safeguarding.staff-checks.viewAny` — Safeguarding Staff-checks ViewAny
     */
    #[Label('Safeguarding Staff-checks ViewAny')]
    #[Description('Safeguarding Staff-checks ViewAny')]
    case SafeguardingStaffChecksViewAny = 'safeguarding.staff-checks.viewAny';

    /**
     * `safeguarding.staff-checks.view` — Safeguarding Staff-checks View
     */
    #[Label('Safeguarding Staff-checks View')]
    #[Description('Safeguarding Staff-checks View')]
    case SafeguardingStaffChecksView = 'safeguarding.staff-checks.view';

    /**
     * `safeguarding.staff-checks.create` — Safeguarding Staff-checks Create
     */
    #[Label('Safeguarding Staff-checks Create')]
    #[Description('Safeguarding Staff-checks Create')]
    case SafeguardingStaffChecksCreate = 'safeguarding.staff-checks.create';

    /**
     * `safeguarding.staff-checks.update` — Safeguarding Staff-checks Update
     */
    #[Label('Safeguarding Staff-checks Update')]
    #[Description('Safeguarding Staff-checks Update')]
    case SafeguardingStaffChecksUpdate = 'safeguarding.staff-checks.update';

    /**
     * `safeguarding.staff-checks.renew` — Safeguarding Staff-checks Renew
     */
    #[Label('Safeguarding Staff-checks Renew')]
    #[Description('Safeguarding Staff-checks Renew')]
    case SafeguardingStaffChecksRenew = 'safeguarding.staff-checks.renew';

    /**
     * `platform.safeguarding.audit` — Cross-tenant safeguarding audit.
     */
    #[Label('Cross-tenant safeguarding audit')]
    #[Description('Cross-tenant safeguarding audit. Anonymised metadata for regulatory reporting (Ofsted / DBS statistics).')]
    case PlatformSafeguardingAudit = 'platform.safeguarding.audit';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::SafeguardingIncidentsViewAny => Guard::Sanctum,
            self::SafeguardingIncidentsView => Guard::Sanctum,
            self::SafeguardingIncidentsReport => Guard::Sanctum,
            self::SafeguardingIncidentsInvestigate => Guard::Sanctum,
            self::SafeguardingIncidentsResolve => Guard::Sanctum,
            self::SafeguardingIncidentsEscalate => Guard::Sanctum,
            self::SafeguardingIncidentsGrantAccess => Guard::Sanctum,
            self::SafeguardingStaffChecksViewAny => Guard::Sanctum,
            self::SafeguardingStaffChecksView => Guard::Sanctum,
            self::SafeguardingStaffChecksCreate => Guard::Sanctum,
            self::SafeguardingStaffChecksUpdate => Guard::Sanctum,
            self::SafeguardingStaffChecksRenew => Guard::Sanctum,
            self::PlatformSafeguardingAudit => Guard::PlatformAdmin,
        };
    }
}
