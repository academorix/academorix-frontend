<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform safeguarding --force`.

declare(strict_types=1);

namespace Stackra\Safeguarding\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New Incident row (safeguarding / injury / discipline / near-miss / property_damage). status='submitted'.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention 7yr, legal-hold)
 * - notifications::AlertSafeguardingOfficer
 * - notifications::AlertBranchManagerOnP0
 *
 * @category Safeguarding
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class IncidentReported implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $incidentId  ULID (sfi_*)
     * @param  string  $tenantId  ULID
     * @param  string  $kind  'safeguarding'|'injury'|'discipline'|'near_miss'|'property_damage'
     * @param  string  $severity  'low'|'medium'|'high'|'critical'
     * @param  string  $involvedAthleteId  ULID|null
     * @param  string  $reporterUserId  ULID
     * @param  string  $occurredAt  ISO 8601
     */
    public function __construct(
        public readonly string $incidentId,
        public readonly string $tenantId,
        public readonly string $kind,
        public readonly string $severity,
        public readonly string $involvedAthleteId,
        public readonly string $reporterUserId,
        public readonly string $occurredAt,
    ) {
    }
}
