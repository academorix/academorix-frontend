<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform safeguarding --force`.

declare(strict_types=1);

namespace Academorix\Safeguarding\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P0 event. Escalated to external authorities (police, CPS, DBS). Legal-hold applies to every associated record.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (indefinite retention)
 * - notifications::AlertOwner
 * - compliance::ApplyLegalHold
 *
 * @category Safeguarding
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class IncidentEscalated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $incidentId  ULID
     * @param  string  $escalatedTo  'police'|'child_protective_services'|'dbs'|'ofsted'|'other'
     * @param  string  $escalatedByUserId  ULID
     * @param  string  $externalReference  string|null
     */
    public function __construct(
        public readonly string $incidentId,
        public readonly string $escalatedTo,
        public readonly string $escalatedByUserId,
        public readonly string $externalReference,
    ) {
    }
}
