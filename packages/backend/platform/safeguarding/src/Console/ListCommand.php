<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform safeguarding --force`.

declare(strict_types=1);

namespace Stackra\Safeguarding\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan safeguarding:list` — List every safeguarding row.
 *
 * @category Safeguarding
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'safeguarding:list',
    description: 'List every safeguarding row.',
)]
final class ListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List every safeguarding row', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List every safeguarding row completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
