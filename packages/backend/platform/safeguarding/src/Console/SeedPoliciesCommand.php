<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform safeguarding --force`.

declare(strict_types=1);

namespace Academorix\Safeguarding\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan safeguarding:seed-policies` — Seed policies — safeguarding command.
 *
 * @category Safeguarding
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'safeguarding:seed-policies',
    description: 'Seed policies — safeguarding command.',
)]
final class SeedPoliciesCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Seed policies — safeguarding command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Seed policies — safeguarding command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
