<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform safeguarding --force`.

declare(strict_types=1);

namespace Stackra\Safeguarding\Actions\Tenant;

use Stackra\Safeguarding\Contracts\Repositories\PolicyAcknowledgementRepositoryInterface;
use Stackra\Safeguarding\Data\PolicyAcknowledgementData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;
use Illuminate\Http\Request;
use Illuminate\Pagination\LengthAwarePaginator;
use Spatie\LaravelData\PaginatedDataCollection;

/**
 * `GET /api/v1/policy-acknowledgements` — list action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Safeguarding
 *
 * @since    0.1.0
 */
#[AsAction(name: 'safeguarding.policy_acknowledgements.list')]
#[Get('/api/v1/policy-acknowledgements')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ListPolicyAcknowledgementAction
{
    use AsController;

    public function __construct(
        private readonly PolicyAcknowledgementRepositoryInterface $repository,
    ) {
    }

    /**
     * List `policy-acknowledgements` for the current caller.
     *
     * The `Repository` base picks up `filter[...]` / `sort=...` /
     * `include=...` query parameters via the `#[Filterable]` attribute
     * on the concrete repository — no per-Action wiring needed.
     *
     * @return PaginatedDataCollection<int, PolicyAcknowledgementData>  Paginated wire-visible DTOs.
     */
    public function __invoke(Request $request): PaginatedDataCollection
    {
        /** @var LengthAwarePaginator<int, \Stackra\Safeguarding\Models\PolicyAcknowledgement> $page */
        $page = $this->repository->paginate(
            perPage: (int) $request->integer('per_page', 15),
        );

        return PolicyAcknowledgementData::collect($page, PaginatedDataCollection::class);
    }
}
