<?php

declare(strict_types=1);

use Stackra\Safeguarding\Models\BackgroundCheck;

it('is a placeholder — replace with actual BackgroundCheck unit tests', function (): void {
    expect(BackgroundCheck::class)->toBeString();
});
