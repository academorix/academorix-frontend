<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform integrations --force`.

declare(strict_types=1);

namespace Stackra\Integrations\Actions\Platform;

use Stackra\Integrations\Contracts\Repositories\AppRepositoryInterface;
use Stackra\Integrations\Data\AppData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/platform/apps/{app}` — show action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Integrations
 *
 * @since    0.1.0
 */
#[AsAction(name: 'integrations.apps.show')]
#[Get('/api/v1/platform/apps/{app}')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class ShowAppAction
{
    use AsController;

    public function __construct(
        private readonly AppRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `app` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): AppData
    {
        return AppData::from($this->repository->findOrFail($id));
    }
}
