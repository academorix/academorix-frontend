<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform integrations --force`.

declare(strict_types=1);

namespace Academorix\Integrations\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `kind` on `integration-provider`.
 *
 * payment / crm / email / sms / calendar / chat / wallet / accounting / webhook / sso / video / storage.
 *
 * @category Integrations
 *
 * @since    0.1.0
 */
enum IntegrationProviderKind: string
{
    use Enum;

    case Payment = 'payment';
    case Crm = 'crm';
    case Email = 'email';
    case Sms = 'sms';
    case Calendar = 'calendar';
    case Chat = 'chat';
    case Wallet = 'wallet';
    case Accounting = 'accounting';
    case Webhook = 'webhook';
    case Sso = 'sso';
    case Video = 'video';
    case Storage = 'storage';
}
