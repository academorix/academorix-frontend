<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform integrations --force`.

declare(strict_types=1);

namespace Stackra\Integrations\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan apps:approve` — Approve — apps command.
 *
 * @category Integrations
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'apps:approve',
    description: 'Approve — apps command.',
)]
final class ApproveCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Approve — apps command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Approve — apps command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
