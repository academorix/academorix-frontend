<?php

declare(strict_types=1);

use Stackra\Integrations\Models\AppWebhookSubscription;

it('is a placeholder — replace with actual AppWebhookSubscription unit tests', function (): void {
    expect(AppWebhookSubscription::class)->toBeString();
});
