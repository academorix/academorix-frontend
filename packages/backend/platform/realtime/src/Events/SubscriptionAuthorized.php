<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform realtime --force`.

declare(strict_types=1);

namespace Stackra\Realtime\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Reverb broadcasting/auth endpoint issued a signed channel URL for a User.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::TrackAuthLatency
 *
 * @category Realtime
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SubscriptionAuthorized implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $subscriptionId  ULID (bsn_*)
     * @param  string  $tenantId  ULID
     * @param  string  $channelId  ULID
     * @param  string  $userId  ULID (usr_*)
     */
    public function __construct(
        public readonly string $subscriptionId,
        public readonly string $tenantId,
        public readonly string $channelId,
        public readonly string $userId,
    ) {
    }
}
