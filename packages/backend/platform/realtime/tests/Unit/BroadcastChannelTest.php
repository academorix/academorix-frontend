<?php

declare(strict_types=1);

use Stackra\Realtime\Models\BroadcastChannel;

it('is a placeholder — replace with actual BroadcastChannel unit tests', function (): void {
    expect(BroadcastChannel::class)->toBeString();
});
