<?php

declare(strict_types=1);

use Stackra\Forms\Models\FormVersion;

it('is a placeholder — replace with actual FormVersion unit tests', function (): void {
    expect(FormVersion::class)->toBeString();
});
