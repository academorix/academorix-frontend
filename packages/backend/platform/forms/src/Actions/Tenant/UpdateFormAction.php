<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform forms --force`.

declare(strict_types=1);

namespace Academorix\Forms\Actions\Tenant;

use Academorix\Forms\Contracts\Repositories\FormRepositoryInterface;
use Academorix\Forms\Data\FormData;
use Academorix\Forms\Data\Requests\UpdateFormRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/forms/{form}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Forms
 *
 * @since    0.1.0
 */
#[AsAction(name: 'forms.update')]
#[Patch('/api/v1/forms/{form}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateFormAction
{
    use AsController;

    public function __construct(
        private readonly FormRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `form` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateFormRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateFormRequestData $data): FormData
    {
        $model = $this->repository->update($id, $data->toArray());

        return FormData::from($model);
    }
}
