<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform forms --force`.

declare(strict_types=1);

namespace Stackra\Forms\Actions\Tenant;

use Stackra\Forms\Contracts\Repositories\FormSubmissionRepositoryInterface;
use Stackra\Forms\Data\FormSubmissionData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/form-submissions/{submission}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Forms
 *
 * @since    0.1.0
 */
#[AsAction(name: 'forms.form_submissions.show')]
#[Get('/api/v1/form-submissions/{submission}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowFormSubmissionAction
{
    use AsController;

    public function __construct(
        private readonly FormSubmissionRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `form-submission` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): FormSubmissionData
    {
        return FormSubmissionData::from($this->repository->findOrFail($id));
    }
}
