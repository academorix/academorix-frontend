<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform forms --force`.

declare(strict_types=1);

namespace Academorix\Forms\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Form.published_version_id set; Form.status → 'published'. Public URL becomes live.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::WarmPublicFormCache
 * - notifications::NotifyFormPublished
 *
 * @category Forms
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class FormPublished implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $formId  ULID
     * @param  string  $formVersionId  ULID
     * @param  string  $publishedByUserId  ULID
     * @param  string  $publicUrlSignature  string
     */
    public function __construct(
        public readonly string $formId,
        public readonly string $formVersionId,
        public readonly string $publishedByUserId,
        public readonly string $publicUrlSignature,
    ) {
    }
}
