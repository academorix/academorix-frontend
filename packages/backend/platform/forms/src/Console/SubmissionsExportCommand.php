<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform forms --force`.

declare(strict_types=1);

namespace Stackra\Forms\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan forms:submissions:export` — Submissions:export — forms command.
 *
 * @category Forms
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'forms:submissions:export',
    description: 'Submissions:export — forms command.',
)]
final class SubmissionsExportCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Submissions:export — forms command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Submissions:export — forms command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
