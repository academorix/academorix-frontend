<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform forms --force`.

declare(strict_types=1);

namespace Stackra\Forms\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan forms:archive` — Archive — forms command.
 *
 * @category Forms
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'forms:archive',
    description: 'Archive — forms command.',
)]
final class ArchiveCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Archive — forms command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Archive — forms command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
