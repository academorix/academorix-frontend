<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform forms --force`.

declare(strict_types=1);

namespace Academorix\Forms\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan forms:sessions:purge-expired` — Sessions:purge expired — forms command.
 *
 * @category Forms
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'forms:sessions:purge-expired',
    description: 'Sessions:purge expired — forms command.',
)]
final class SessionsPurgeExpiredCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Sessions:purge expired — forms command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Sessions:purge expired — forms command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
