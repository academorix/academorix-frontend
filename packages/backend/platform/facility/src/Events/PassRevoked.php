<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Admin invoked the revoke action. Sets deleted_at + revocation_reason. Distinct from PassExhausted (used-up) and PassExpired (time-up). Refund handling is Finance module's job.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - activity::WriteToActivityLog
 * - notifications::DispatchPassRevokedNotification (cannot-opt-out — the pass owner needs to know)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PassRevoked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $passId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $revokedAt  ISO 8601
     * @param  string  $revokedByUserId  ULID
     * @param  string  $revocationReason  string
     * @param  int  $unusedCreditsAtRevocation  integer
     */
    public function __construct(
        public readonly string $passId,
        public readonly string $tenantId,
        public readonly string $revokedAt,
        public readonly string $revokedByUserId,
        public readonly string $revocationReason,
        public readonly int $unusedCreditsAtRevocation,
    ) {
    }
}
