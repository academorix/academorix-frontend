<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Stackra\Facility\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Any Facility scalar field mutated except status (FacilityStatusChanged), pricing_json (FacilityPricingUpdated), availability_json (FacilityAvailabilityChanged), and blackout_dates (FacilityBlackoutAdded / FacilityBlackoutRemoved). Umbrella event for renames, capacity changes, sport_category changes, min/max duration tweaks, advance_booking_window_days edits.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class FacilityUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $facilityId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $branchId  ULID
     * @param  string  $changedFields  string[]
     * @param  string  $updatedBy  ULID
     */
    public function __construct(
        public readonly string $facilityId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $changedFields,
        public readonly string $updatedBy,
    ) {
    }
}
