<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Stackra\Facility\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ResourceBooking.starts_at / ends_at mutated on an in-flight booking. Rescheduling into a conflicting slot is refused by the observer (overlap detection runs on reschedule too). Rescheduling triggers a fresh overlap check + capacity check + availability check.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchBookingConfirmedNotification (as a re-confirmation)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class BookingRescheduled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $bookingId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $facilityId  ULID
     * @param  string  $rescheduledByUserId  ULID
     * @param  string  $wasStartsAt  ISO 8601
     * @param  string  $wasEndsAt  ISO 8601
     * @param  string  $newStartsAt  ISO 8601
     * @param  string  $newEndsAt  ISO 8601
     */
    public function __construct(
        public readonly string $bookingId,
        public readonly string $tenantId,
        public readonly string $facilityId,
        public readonly string $rescheduledByUserId,
        public readonly string $wasStartsAt,
        public readonly string $wasEndsAt,
        public readonly string $newStartsAt,
        public readonly string $newEndsAt,
    ) {
    }
}
