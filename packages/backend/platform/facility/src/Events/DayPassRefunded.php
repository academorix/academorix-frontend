<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Stackra\Facility\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Manual refund action from staff (Wave 4 refund flow reversal). Requires DayPass.consumed_at IS NULL AND staff has facility.day_passes.refund permission.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - activity::WriteToActivityLog
 * - finance::ReverseDayPassRevenue (Wave 4)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class DayPassRefunded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $dayPassId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $branchId  ULID
     * @param  string  $refundedAt  ISO 8601
     * @param  string  $refundedByUserId  ULID
     * @param  string  $refundReason  string
     * @param  int  $refundAmountCents  integer (may differ from price_amount_cents on partial refunds)
     */
    public function __construct(
        public readonly string $dayPassId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $refundedAt,
        public readonly string $refundedByUserId,
        public readonly string $refundReason,
        public readonly int $refundAmountCents,
    ) {
    }
}
