<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PassProvisioner committed a new passes row. Fires either from a Wave 4 Membership provisioning listener OR from manual admin action.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - activity::WriteToActivityLog
 * - notifications::DispatchPassIssuedNotification (opt-in to the pass owner)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PassIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $passId  ULID (pas_*)
     * @param  string  $tenantId  ULID
     * @param  string  $branchId  ULID
     * @param  string  $facilityId  ULID | null (null = valid at any facility in the branch)
     * @param  string  $membershipId  ULID | null (Wave 4 — null in Wave 2 placeholder rows)
     * @param  string  $validFrom  YYYY-MM-DD
     * @param  string  $validUntil  YYYY-MM-DD
     * @param  int  $totalCredits  integer
     * @param  bool  $isTransferable  boolean
     * @param  string  $issuedByUserId  ULID
     */
    public function __construct(
        public readonly string $passId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $facilityId,
        public readonly string $membershipId,
        public readonly string $validFrom,
        public readonly string $validUntil,
        public readonly int $totalCredits,
        public readonly bool $isTransferable,
        public readonly string $issuedByUserId,
    ) {
    }
}
