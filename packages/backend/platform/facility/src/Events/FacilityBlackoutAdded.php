<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Stackra\Facility\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A blackout window was appended to Facility.blackout_dates. Fires per blackout added — a multi-blackout admin update emits N events.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - facility::CancelBookingsInBlackoutWindow (cascades: finds bookings inside the window + fires BookingCancelled on each)
 * - notifications::DispatchFacilityBlackoutAffectsBookingNotification (cannot-opt-out to affected bookers)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class FacilityBlackoutAdded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $facilityId  ULID
     * @param  string  $tenantId  ULID
     * @param  array  $blackoutId  string (opaque handle for the JSON array element)
     * @param  string  $startsAt  ISO 8601
     * @param  string  $endsAt  ISO 8601
     * @param  string  $reason  string | null
     * @param  int  $affectedBookingCount  integer (bookings inside the window at the time of add; cascaded to BookingCancelled)
     */
    public function __construct(
        public readonly string $facilityId,
        public readonly string $tenantId,
        public readonly array $blackoutId,
        public readonly string $startsAt,
        public readonly string $endsAt,
        public readonly string $reason,
        public readonly int $affectedBookingCount,
    ) {
    }
}
