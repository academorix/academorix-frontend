<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Stackra\Facility\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * FacilityRepository finished a successful create. Fires exactly once per (tenant_id, branch_id, slug) tuple.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateBranchFacilityListCache
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class FacilityCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $facilityId  ULID (fac_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $branchId  ULID (brc_*)
     * @param  string  $name  string
     * @param  string  $slug  string
     * @param  string  $facilityType  string (court / field / pool / studio / room / gym_floor / equipment / lane / general)
     * @param  string  $sportCategory  string | null
     * @param  int  $capacityPersons  integer
     * @param  string  $createdBy  ULID (usr_*)
     */
    public function __construct(
        public readonly string $facilityId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $name,
        public readonly string $slug,
        public readonly string $facilityType,
        public readonly string $sportCategory,
        public readonly int $capacityPersons,
        public readonly string $createdBy,
    ) {
    }
}
