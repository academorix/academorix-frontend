<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ExpireDayPassesJob transitioned a DayPass past valid_on_date without consumption. No refund flow triggered — day-pass revenue was already recognised on purchase.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class DayPassExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $dayPassId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $branchId  ULID
     * @param  string  $wasValidOnDate  YYYY-MM-DD
     * @param  string  $expiredAt  ISO 8601
     */
    public function __construct(
        public readonly string $dayPassId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $wasValidOnDate,
        public readonly string $expiredAt,
    ) {
    }
}
