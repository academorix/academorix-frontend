<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Facility.status transitioned to `retired` OR facility soft-delete completed with the `--cascade-bookings` flag. Terminal. Fires alongside FacilityStatusChanged.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - facility::CancelFutureBookingsOnFacilityRetired (cascades: cancels every pending/confirmed booking with starts_at > now; fires BookingCancelled per booking)
 * - notifications::DispatchFacilityBlackoutAffectsBookingNotification
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class FacilityRetired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $facilityId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $branchId  ULID
     * @param  string  $retiredAt  ISO 8601
     * @param  string  $retiredBy  ULID
     * @param  string  $reason  string | null
     * @param  int  $cascadedBookingCount  integer
     * @param  string  $hardDeleteScheduledFor  ISO 8601
     */
    public function __construct(
        public readonly string $facilityId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $retiredAt,
        public readonly string $retiredBy,
        public readonly string $reason,
        public readonly int $cascadedBookingCount,
        public readonly string $hardDeleteScheduledFor,
    ) {
    }
}
