<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Stackra\Facility\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ExpirePassesJob transitioned a pass past valid_until (with unused credits — expired-and-unused). Refunds handled by Finance module case-by-case, not automatic.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchPassExpiringNotification (dispatched 7 days BEFORE expiry — this event fires ON expiry)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PassExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $passId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $wasValidUntil  YYYY-MM-DD
     * @param  int  $unusedCredits  integer
     * @param  string  $expiredAt  ISO 8601
     */
    public function __construct(
        public readonly string $passId,
        public readonly string $tenantId,
        public readonly string $wasValidUntil,
        public readonly int $unusedCredits,
        public readonly string $expiredAt,
    ) {
    }
}
