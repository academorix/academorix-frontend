<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Stackra\Facility\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Pass.assignable_user_ids mutated OR the primary owner-of-record on the pass changed. Refused when pass.is_transferable=false.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PassTransferred implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $passId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $wasAssignableUserIds  ULID[]
     * @param  string  $newAssignableUserIds  ULID[]
     * @param  string  $transferredByUserId  ULID
     */
    public function __construct(
        public readonly string $passId,
        public readonly string $tenantId,
        public readonly string $wasAssignableUserIds,
        public readonly string $newAssignableUserIds,
        public readonly string $transferredByUserId,
    ) {
    }
}
