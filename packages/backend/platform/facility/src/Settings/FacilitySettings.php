<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * FacilitySettings — Facilities & Bookings.
 *
 * Tenant-wide defaults for facility creation + booking policy.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'facility',
    label: 'Facilities & Bookings',
    description: 'Tenant-wide defaults for facility creation + booking policy.',
    icon: 'map-pin',
    scope: 'tenant',
    permission: 'facilities.viewAny',
    sortOrder: 30,
)]
class FacilitySettings extends Settings
{

    // ── Booking defaults ──────────────────────────────────
    #[SettingGroup(label: 'Booking defaults', description: 'Default values applied to newly-created facilities. Can be overridden per-facility.', icon: 'clock', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Number, label: 'Minimum booking duration (minutes)', group: 'Booking defaults', sortOrder: 1, min: 15, max: 480)]
    public int $default_min_duration_minutes = 60;

    #[SettingField(controlType: ControlType::Number, label: 'Maximum booking duration (minutes)', group: 'Booking defaults', sortOrder: 2, min: 30, max: 1440)]
    public int $default_max_duration_minutes = 240;

    #[SettingField(controlType: ControlType::Select, label: 'Booking slot granularity', group: 'Booking defaults', sortOrder: 3, options: ['15' => '15 min', '30' => '30 min', '60' => '1 hour', '120' => '2 hours'])]
    public int $default_slot_step_minutes = 30;

    #[SettingField(controlType: ControlType::Number, label: 'How far in advance can users book (days)', group: 'Booking defaults', sortOrder: 4, min: 1, max: 365)]
    public int $default_advance_booking_window_days = 30;

    #[SettingField(controlType: ControlType::Number, label: 'Cancellation notice required (hours)', group: 'Booking defaults', sortOrder: 5, min: 0, max: 168, helpText: 'Cancellations inside this window may incur a fee (per your refund policy).')]
    public int $default_cancellation_notice_hours = 24;


    // ── Capacity policy ──────────────────────────────────
    #[SettingGroup(label: 'Capacity policy', icon: 'users', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enforce facility capacity', group: 'Capacity policy', sortOrder: 1, helpText: 'When off, capacity is a UI hint only. Gyms with overbook waivers may prefer this.')]
    public bool $enforce_capacity = true;

    #[SettingField(controlType: ControlType::Number, label: 'Default capacity for new facilities', group: 'Capacity policy', sortOrder: 2, min: 1)]
    public int $default_capacity_persons = 1;


    // ── Reminders ──────────────────────────────────
    #[SettingGroup(label: 'Reminders', icon: 'bell', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Text, label: 'Booking reminder offsets', group: 'Reminders', sortOrder: 1, options: ['48' => '48 hours before', '24' => '24 hours before', '4' => '4 hours before', '1' => '1 hour before', '0' => 'Do not send reminders'], helpText: 'Users can opt out individually per their profile settings.')]
    public array $reminder_offsets_hours = [24, 1];


    // ── Day passes ──────────────────────────────────
    #[SettingGroup(label: 'Day passes', icon: 'ticket', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Currency, label: 'Default day-pass price (cents)', group: 'Day passes', sortOrder: 1, min: 0)]
    public int $day_pass_default_price_cents = 1500;

    #[SettingField(controlType: ControlType::Number, label: 'Refund window (days)', group: 'Day passes', sortOrder: 2, min: 0, max: 90)]
    public int $day_pass_refund_window_days = 30;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'facility';
    }
}
