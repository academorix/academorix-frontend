<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Stackra\Facility\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * FacilityUserSettings — My bookings.
 *
 * Per-user booking preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'facility_user',
    label: 'My bookings',
    description: 'Per-user booking preferences.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 25,
)]
class FacilityUserSettings extends Settings
{

    // ── Reminders ──────────────────────────────────
    #[SettingGroup(label: 'Reminders', icon: 'bell', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Send me booking reminders', group: 'Reminders', sortOrder: 1)]
    public bool $receive_booking_reminders = true;

    #[SettingField(controlType: ControlType::Text, label: 'Reminder channels', group: 'Reminders', sortOrder: 2, options: ['mail' => 'Email', 'push' => 'Push notification', 'in_app' => 'In-app', 'sms' => 'SMS (usage charged)'])]
    public array $reminder_channels = ['push', 'in_app'];


    // ── Preferences ──────────────────────────────────
    #[SettingGroup(label: 'Preferences', icon: 'adjustments-horizontal', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Text, label: 'My usual facility', group: 'Preferences', sortOrder: 1, helpText: 'Pre-selected in the booking widget when set.')]
    public ?string $default_facility_id = null;

    #[SettingField(controlType: ControlType::Toggle, label: 'Show price on the slot picker', group: 'Preferences', sortOrder: 2)]
    public bool $show_price_in_slots = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'facility_user';
    }
}
