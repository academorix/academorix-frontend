<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Stackra\Facility\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan facility:add-blackout` — Add blackout — facility command.
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'facility:add-blackout',
    description: 'Add blackout — facility command.',
)]
final class AddBlackoutCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Add blackout — facility command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Add blackout — facility command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
