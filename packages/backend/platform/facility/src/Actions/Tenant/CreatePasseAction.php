<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Stackra\Facility\Actions\Tenant;

use Stackra\Facility\Contracts\Repositories\PassRepositoryInterface;
use Stackra\Facility\Data\PassData;
use Stackra\Facility\Data\Requests\CreatePassRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/passes` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsAction(name: 'facility.passes.create')]
#[Post('/api/v1/passes')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreatePasseAction
{
    use AsController;

    public function __construct(
        private readonly PassRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `passe` from the validated request payload.
     *
     * @param  CreatePassRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreatePassRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(PassData::from($model), JsonResponse::HTTP_CREATED);
    }
}
