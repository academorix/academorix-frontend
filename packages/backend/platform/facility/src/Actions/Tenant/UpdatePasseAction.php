<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Actions\Tenant;

use Academorix\Facility\Contracts\Repositories\PassRepositoryInterface;
use Academorix\Facility\Data\PassData;
use Academorix\Facility\Data\Requests\UpdatePassRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/passes/{pass}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsAction(name: 'facility.passes.update')]
#[Patch('/api/v1/passes/{pass}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdatePasseAction
{
    use AsController;

    public function __construct(
        private readonly PassRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `passe` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdatePassRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdatePassRequestData $data): PassData
    {
        $model = $this->repository->update($id, $data->toArray());

        return PassData::from($model);
    }
}
