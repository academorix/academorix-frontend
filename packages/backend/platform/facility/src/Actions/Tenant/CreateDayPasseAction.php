<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Actions\Tenant;

use Academorix\Facility\Contracts\Repositories\DayPassRepositoryInterface;
use Academorix\Facility\Data\DayPassData;
use Academorix\Facility\Data\Requests\CreateDayPassRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/day-passes` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsAction(name: 'facility.day_passes.create')]
#[Post('/api/v1/day-passes')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateDayPasseAction
{
    use AsController;

    public function __construct(
        private readonly DayPassRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `day-passe` from the validated request payload.
     *
     * @param  CreateDayPassRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateDayPassRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(DayPassData::from($model), JsonResponse::HTTP_CREATED);
    }
}
