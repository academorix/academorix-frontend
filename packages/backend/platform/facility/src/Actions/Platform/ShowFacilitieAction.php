<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Stackra\Facility\Actions\Platform;

use Stackra\Facility\Contracts\Repositories\FacilityRepositoryInterface;
use Stackra\Facility\Data\FacilityData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/platform/facilities/{facility}` — show action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsAction(name: 'facility.facilities.show')]
#[Get('/api/v1/platform/facilities/{facility}')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class ShowFacilitieAction
{
    use AsController;

    public function __construct(
        private readonly FacilityRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `facilitie` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): FacilityData
    {
        return FacilityData::from($this->repository->findOrFail($id));
    }
}
