<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Stackra\Theme\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * ThemePlatformSettings — Theme platform.
 *
 * Configuration for the design token substrate — rollback retention, accessibility audit cadence, default theme on tenant provisioning.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'theme_platform',
    label: 'Theme platform',
    description: 'Configuration for the design token substrate — rollback retention, accessibility audit cadence, default theme on tenant provisioning.',
    icon: 'swatch',
    scope: 'tenant',
    permission: 'theme.read',
    sortOrder: 23,
)]
class ThemePlatformSettings extends Settings
{

    // ── Rollback ──────────────────────────────────
    #[SettingGroup(label: 'Rollback', description: 'How many historical themes to retain for rollback.', icon: 'arrow-uturn-left', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Number, label: 'Rollback retention (activations)', validation: ['nullable', 'integer', 'min:1', 'max:100'], group: 'Rollback', sortOrder: 1, min: 1, max: 100, helpText: 'Empty = follow entitlement default (5 for standard, 20 for theme_rollback_extended tenants). Explicit value overrides — capped at entitlement max.')]
    public ?int $rollback_retention_override = null;


    // ── Accessibility audit ──────────────────────────────────
    #[SettingGroup(label: 'Accessibility audit', description: 'Contrast auditor cadence + threshold.', icon: 'eye', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Audit on activate', validation: ['boolean'], group: 'Accessibility audit', sortOrder: 1, helpText: 'Run the ContrastAuditor after every ThemeActivated. Findings surface as ThemeAccessibilityRecommendationNotification.')]
    public bool $audit_on_activate = true;

    #[SettingField(controlType: ControlType::Select, label: 'WCAG target level', validation: ['required', 'in:AA,AAA'], group: 'Accessibility audit', sortOrder: 2, options: ['AA' => 'AA (4.5:1 body / 3:1 large)', 'AAA' => 'AAA (7:1 body / 4.5:1 large)'])]
    public string $wcag_level = 'AA';


    // ── Auto-provisioning ──────────────────────────────────
    #[SettingGroup(label: 'Auto-provisioning', description: 'Default theme seeded when a new tenant is provisioned.', icon: 'sparkles', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Select, label: 'Default preset for new tenants', validation: ['required', 'string', 'in:light-default,dark-default,high-contrast-light,high-contrast-dark,brand-forward-blue,brand-forward-green'], group: 'Auto-provisioning', sortOrder: 1, options: ['light-default' => 'Light Default', 'dark-default' => 'Dark Default', 'high-contrast-light' => 'High Contrast Light', 'high-contrast-dark' => 'High Contrast Dark', 'brand-forward-blue' => 'Brand Forward Blue', 'brand-forward-green' => 'Brand Forward Green'], helpText: 'SeedDefaultThemeForNewTenant reads this on TenantProvisioned to pick which preset the new tenant\'s theme references.')]
    public string $default_preset_slug = 'light-default';

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'theme_platform';
    }
}
