<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Stackra\Theme\Actions\Platform;

use Stackra\Theme\Contracts\Repositories\ThemePresetRepositoryInterface;
use Stackra\Theme\Data\ThemePresetData;
use Stackra\Theme\Data\Requests\CreateThemePresetRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/platform/theme-presets` — create action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[AsAction(name: 'theme.theme_presets.create')]
#[Post('/api/v1/platform/theme-presets')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class CreateThemePresetAction
{
    use AsController;

    public function __construct(
        private readonly ThemePresetRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `theme-preset` from the validated request payload.
     *
     * @param  CreateThemePresetRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateThemePresetRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(ThemePresetData::from($model), JsonResponse::HTTP_CREATED);
    }
}
