<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Stackra\Theme\Actions\Tenant;

use Stackra\Theme\Contracts\Repositories\ThemeTokenOverrideRepositoryInterface;
use Stackra\Theme\Data\ThemeTokenOverrideData;
use Stackra\Theme\Data\Requests\CreateThemeTokenOverrideRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/theme-token-overrides` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[AsAction(name: 'theme.theme_token_overrides.create')]
#[Post('/api/v1/theme-token-overrides')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateThemeTokenOverrideAction
{
    use AsController;

    public function __construct(
        private readonly ThemeTokenOverrideRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `theme-token-override` from the validated request payload.
     *
     * @param  CreateThemeTokenOverrideRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateThemeTokenOverrideRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(ThemeTokenOverrideData::from($model), JsonResponse::HTTP_CREATED);
    }
}
