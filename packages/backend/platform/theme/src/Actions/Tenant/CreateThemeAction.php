<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Stackra\Theme\Actions\Tenant;

use Stackra\Theme\Contracts\Repositories\ThemeRepositoryInterface;
use Stackra\Theme\Data\ThemeData;
use Stackra\Theme\Data\Requests\CreateThemeRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/themes` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[AsAction(name: 'theme.themes.create')]
#[Post('/api/v1/themes')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateThemeAction
{
    use AsController;

    public function __construct(
        private readonly ThemeRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `theme` from the validated request payload.
     *
     * @param  CreateThemeRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateThemeRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(ThemeData::from($model), JsonResponse::HTTP_CREATED);
    }
}
