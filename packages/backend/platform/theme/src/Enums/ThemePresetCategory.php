<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Stackra\Theme\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `category` on `theme-preset`.
 *
 * Enum: platform_default / tenant_custom / brand_forward / high_contrast / minimal.
 *
 * @category Theme
 *
 * @since    0.1.0
 */
enum ThemePresetCategory: string
{
    use Enum;

    case PlatformDefault = 'platform_default';
    case TenantCustom = 'tenant_custom';
    case BrandForward = 'brand_forward';
    case HighContrast = 'high_contrast';
    case Minimal = 'minimal';
}
