<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Stackra\Theme\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New ThemeTokenOverride row inserted. One event per (theme_id, token_key) — batched at the request level.
 *
 * ## Consumers
 *
 * - theme::RegenerateCssOnThemeActivated
 * - activity::WriteThemeTokenOverriddenToActivity
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ThemeTokenOverridden implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $overrideId  tto_ULID
     * @param  string  $themeId  thm_ULID
     * @param  string  $tenantId  ten_ULID
     * @param  string  $tokenKey  string — canonical vocabulary key (e.g. 'color.primary.500')
     * @param  string  $tokenType  ThemeTokenType
     * @param  string  $reason  string | null
     */
    public function __construct(
        public readonly string $overrideId,
        public readonly string $themeId,
        public readonly string $tenantId,
        public readonly string $tokenKey,
        public readonly string $tokenType,
        public readonly string $reason,
    ) {
    }
}
