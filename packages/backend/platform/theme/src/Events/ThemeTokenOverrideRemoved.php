<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Academorix\Theme\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ThemeTokenOverride hard-deleted or soft-deleted. Falls through to the underlying preset value in next resolution.
 *
 * ## Consumers
 *
 * - theme::RegenerateCssOnThemeActivated
 * - activity::WriteThemeTokenOverrideRemovedToActivity
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ThemeTokenOverrideRemoved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $overrideId  tto_ULID
     * @param  string  $themeId  thm_ULID
     * @param  string  $tenantId  ten_ULID
     * @param  string  $tokenKey  string
     * @param  string  $removedAt  ISO 8601
     */
    public function __construct(
        public readonly string $overrideId,
        public readonly string $themeId,
        public readonly string $tenantId,
        public readonly string $tokenKey,
        public readonly string $removedAt,
    ) {
    }
}
