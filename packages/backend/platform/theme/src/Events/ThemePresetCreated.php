<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Academorix\Theme\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New ThemePreset row inserted. tenant_id NULL = platform preset (fires only during platform-admin operations); non-null = tenant custom preset.
 *
 * ## Consumers
 *
 * - activity::WriteThemePresetCreatedToActivity
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ThemePresetCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $presetId  tps_ULID
     * @param  string  $tenantId  ten_ULID | null
     * @param  string  $slug  string
     * @param  string  $name  string
     * @param  string  $mode  'light' | 'dark'
     * @param  string  $category  ThemePresetCategory
     * @param  bool  $isPlatform  bool — derived from tenant_id IS NULL
     * @param  string  $createdAt  ISO 8601
     */
    public function __construct(
        public readonly string $presetId,
        public readonly string $tenantId,
        public readonly string $slug,
        public readonly string $name,
        public readonly string $mode,
        public readonly string $category,
        public readonly bool $isPlatform,
        public readonly string $createdAt,
    ) {
    }
}
