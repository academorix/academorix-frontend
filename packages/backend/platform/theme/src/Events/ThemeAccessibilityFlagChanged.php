<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Stackra\Theme\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Theme.accessibility_flags jsonb changes. One event per boolean flip within the payload — high_contrast, reduced_motion, larger_text, focus_visible_always.
 *
 * ## Consumers
 *
 * - activity::WriteThemeAccessibilityFlagChangedToActivity
 * - audit::WriteThemeAccessibilityFlagChangedToAudit
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ThemeAccessibilityFlagChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $themeId  thm_ULID
     * @param  string  $tenantId  ten_ULID
     * @param  string  $flag  'high_contrast' | 'reduced_motion' | 'larger_text' | 'focus_visible_always'
     * @param  bool  $newValue  bool
     * @param  bool  $oldValue  bool
     * @param  string  $changedByUserId  usr_ULID | null
     */
    public function __construct(
        public readonly string $themeId,
        public readonly string $tenantId,
        public readonly string $flag,
        public readonly bool $newValue,
        public readonly bool $oldValue,
        public readonly string $changedByUserId,
    ) {
    }
}
