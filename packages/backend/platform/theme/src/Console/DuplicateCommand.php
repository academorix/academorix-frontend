<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Academorix\Theme\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan theme-preset:duplicate` — Duplicate a theme preset.
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'theme-preset:duplicate',
    description: 'Duplicate a theme preset.',
)]
final class DuplicateCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Duplicate a theme preset', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Duplicate a theme preset completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
