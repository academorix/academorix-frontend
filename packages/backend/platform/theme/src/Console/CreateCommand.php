<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Stackra\Theme\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan theme-preset:create` — Create a new theme preset.
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'theme-preset:create',
    description: 'Create a new theme preset.',
)]
final class CreateCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Create a new theme preset', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Create a new theme preset completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
