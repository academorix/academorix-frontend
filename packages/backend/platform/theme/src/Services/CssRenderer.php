<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Stackra\Theme\Services;

use Stackra\Theme\Contracts\Services\CssRendererInterface;
use Illuminate\Container\Attributes\Scoped;
use Stackra\Theme\Contracts\Repositories\ThemePresetRepositoryInterface;

/**
 * Concrete service — CssRenderer.
 *
 * Implements {@see CssRendererInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[Scoped]
final class CssRenderer implements CssRendererInterface
{
    /**
     * @param  ThemePresetRepositoryInterface  $themePresetRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly ThemePresetRepositoryInterface $themePresetRepository,
    ) {
    }
}
