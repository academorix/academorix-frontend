<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform reception --force`.

declare(strict_types=1);

namespace Stackra\Reception\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New Visitor row created. Guest check-in via kiosk or front desk.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - realtime::UpdateReceptionDashboard
 * - attendance::LogVisitorPresence
 *
 * @category Reception
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class VisitorCheckedIn implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $visitorId  ULID (vst_*)
     * @param  string  $tenantId  ULID
     * @param  string  $branchId  ULID
     * @param  string  $kind  'walk_in'|'prospect'|'visitor'|'contractor'|'delivery'
     * @param  string  $checkedInAt  ISO 8601
     * @param  string  $escortedByUserId  ULID|null
     */
    public function __construct(
        public readonly string $visitorId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $kind,
        public readonly string $checkedInAt,
        public readonly string $escortedByUserId,
    ) {
    }
}
