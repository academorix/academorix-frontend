<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform reception --force`.

declare(strict_types=1);

namespace Academorix\Reception\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan reception:daily-report` — Daily report — reception command.
 *
 * @category Reception
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'reception:daily-report',
    description: 'Daily report — reception command.',
)]
final class DailyReportCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Daily report — reception command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Daily report — reception command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
