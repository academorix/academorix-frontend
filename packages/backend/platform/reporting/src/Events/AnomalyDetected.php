<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform reporting --force`.

declare(strict_types=1);

namespace Stackra\Reporting\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Reporting anomaly detector flagged unusual data pattern (e.g. attendance drop, revenue spike). Feeds ai/AnomalyDetectorPersona.
 *
 * ## Consumers
 *
 * - ai::TriggerAnomalyPersonaScan
 * - notifications::NotifyReportingAnomaly
 *
 * @category Reporting
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AnomalyDetected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $anomalyKind  string
     * @param  string  $affectedModule  string
     * @param  float  $confidence  float
     * @param  string  $detectedAt  ISO 8601
     */
    public function __construct(
        public readonly string $anomalyKind,
        public readonly string $affectedModule,
        public readonly float $confidence,
        public readonly string $detectedAt,
    ) {
    }
}
