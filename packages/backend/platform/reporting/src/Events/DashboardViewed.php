<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform reporting --force`.

declare(strict_types=1);

namespace Stackra\Reporting\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * User opened a dashboard. HIGH VOLUME — sampled 5%.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog (sampled)
 *
 * @category Reporting
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class DashboardViewed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $dashboardId  ULID (dsb_*)
     * @param  string  $userId  ULID
     * @param  string  $viewedAt  ISO 8601
     */
    public function __construct(
        public readonly string $dashboardId,
        public readonly string $userId,
        public readonly string $viewedAt,
    ) {
    }
}
