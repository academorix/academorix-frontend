<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform reporting --force`.

declare(strict_types=1);

namespace Academorix\Reporting\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Report row committed. status='generated'. FileId set.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::NotifyReportReady
 *
 * @category Reporting
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ReportGenerated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $reportId  ULID (rpt_*)
     * @param  string  $tenantId  ULID
     * @param  string  $kind  'attendance_summary'|'revenue_report'|'staff_compliance'|'athlete_progress'|'membership_churn'|'roster_export'|'custom_sql'
     * @param  int  $sensitivity  'public'|'internal'|'pii'|'financial'|'medical'
     * @param  string  $generatedByUserId  ULID
     * @param  int  $rowCount  int
     * @param  string  $fileId  ULID
     */
    public function __construct(
        public readonly string $reportId,
        public readonly string $tenantId,
        public readonly string $kind,
        public readonly int $sensitivity,
        public readonly string $generatedByUserId,
        public readonly int $rowCount,
        public readonly string $fileId,
    ) {
    }
}
