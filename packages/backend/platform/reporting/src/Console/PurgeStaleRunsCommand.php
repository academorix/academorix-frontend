<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform reporting --force`.

declare(strict_types=1);

namespace Stackra\Reporting\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan reporting:purge-stale-runs` — Purge stale runs — reporting command.
 *
 * @category Reporting
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'reporting:purge-stale-runs',
    description: 'Purge stale runs — reporting command.',
)]
final class PurgeStaleRunsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Purge stale runs — reporting command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Purge stale runs — reporting command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
