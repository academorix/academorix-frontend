<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform reporting --force`.

declare(strict_types=1);

namespace Stackra\Reporting\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan reporting:seed-definitions` — Seed definitions — reporting command.
 *
 * @category Reporting
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'reporting:seed-definitions',
    description: 'Seed definitions — reporting command.',
)]
final class SeedDefinitionsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Seed definitions — reporting command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Seed definitions — reporting command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
