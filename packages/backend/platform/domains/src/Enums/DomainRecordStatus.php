<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform domains --force`.

declare(strict_types=1);

namespace Academorix\Domains\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `status` on `domain-record`.
 *
 * Enum: `unknown`, `matches`, `missing`, `mismatch`, `error`.
 *
 * @category Domains
 *
 * @since    0.1.0
 */
enum DomainRecordStatus: string
{
    use Enum;

    case Unknown = 'unknown';
    case Matches = 'matches';
    case Missing = 'missing';
    case Mismatch = 'mismatch';
    case Error = 'error';
}
