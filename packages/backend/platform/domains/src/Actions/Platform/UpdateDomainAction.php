<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform domains --force`.

declare(strict_types=1);

namespace Stackra\Domains\Actions\Platform;

use Stackra\Domains\Contracts\Repositories\DomainRepositoryInterface;
use Stackra\Domains\Data\DomainData;
use Stackra\Domains\Data\Requests\UpdateDomainRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/domains/{id}` — update action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Domains
 *
 * @since    0.1.0
 */
#[AsAction(name: 'domains.update')]
#[Patch('/api/v1/domains/{id}')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class UpdateDomainAction
{
    use AsController;

    public function __construct(
        private readonly DomainRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `domain` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateDomainRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateDomainRequestData $data): DomainData
    {
        $model = $this->repository->update($id, $data->toArray());

        return DomainData::from($model);
    }
}
