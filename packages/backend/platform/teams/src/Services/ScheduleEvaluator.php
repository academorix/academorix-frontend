<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Stackra\Teams\Services;

use Stackra\Teams\Contracts\Services\ScheduleEvaluatorInterface;
use Illuminate\Container\Attributes\Scoped;
use Stackra\Teams\Contracts\Repositories\EventTeamRepositoryInterface;

/**
 * Concrete service — ScheduleEvaluator.
 *
 * Implements {@see ScheduleEvaluatorInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[Scoped]
final class ScheduleEvaluator implements ScheduleEvaluatorInterface
{
    /**
     * @param  EventTeamRepositoryInterface  $eventTeamRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly EventTeamRepositoryInterface $eventTeamRepository,
    ) {
    }
}
