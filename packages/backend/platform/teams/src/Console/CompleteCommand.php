<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan trial:complete` — Complete — trial command.
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'trial:complete',
    description: 'Complete — trial command.',
)]
final class CompleteCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Complete — trial command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Complete — trial command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
