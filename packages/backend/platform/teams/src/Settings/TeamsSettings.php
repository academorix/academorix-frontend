<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Stackra\Teams\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * TeamsSettings — Teams.
 *
 * Tenant-wide defaults for team creation + trial workflow + eligibility policy.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'teams',
    label: 'Teams',
    description: 'Tenant-wide defaults for team creation + trial workflow + eligibility policy.',
    icon: 'users',
    scope: 'tenant',
    permission: 'teams.viewAny',
    sortOrder: 40,
)]
class TeamsSettings extends Settings
{

    // ── Team defaults ──────────────────────────────────
    #[SettingGroup(label: 'Team defaults', description: 'Default values applied to newly-created teams. Can be overridden per-team.', icon: 'user-group', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Default team type', group: 'Team defaults', sortOrder: 1, options: ['squad' => 'Squad (sports team)', 'class' => 'Class (gym / fitness)', 'cohort' => 'Cohort (school)', 'group' => 'Group (general)', 'crew' => 'Crew (informal)', 'other' => 'Other'])]
    public string $default_team_type = 'squad';

    #[SettingField(controlType: ControlType::Number, label: 'Default team capacity', group: 'Team defaults', sortOrder: 2, min: 1, max: 500, helpText: 'Leave blank for no cap. Overrides per-team available on the team detail screen.')]
    public ?int $default_capacity = null;

    #[SettingField(controlType: ControlType::Number, label: 'Default waitlist size', group: 'Team defaults', sortOrder: 3, min: 0, max: 100)]
    public ?int $default_capacity_waitlist = null;


    // ── Eligibility policy ──────────────────────────────────
    #[SettingGroup(label: 'Eligibility policy', icon: 'shield-check', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enforce age eligibility on roster-add', group: 'Eligibility policy', sortOrder: 1, helpText: 'When on, athletes outside team\'s min/max age are refused with a clear error.')]
    public bool $enforce_age_eligibility = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Enforce gender eligibility on roster-add', group: 'Eligibility policy', sortOrder: 2)]
    public bool $enforce_gender_eligibility = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Allow mixed-gender youth teams', group: 'Eligibility policy', sortOrder: 3, helpText: 'Off by default (child-safety mandate). When off, teams with gender_category=\'mixed\' AND min_age<18 are refused at create-time.')]
    public bool $allow_mixed_youth = false;


    // ── Capacity policy ──────────────────────────────────
    #[SettingGroup(label: 'Capacity policy', icon: 'chart-bar', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enforce team capacity', group: 'Capacity policy', sortOrder: 1, helpText: 'When off, capacity is a UI hint only. Rare — used only when overbook + waiver flow is in place.')]
    public bool $enforce_capacity = true;


    // ── Trials ──────────────────────────────────
    #[SettingGroup(label: 'Trials', icon: 'clipboard-check', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Select, label: 'Default trial type', group: 'Trials', sortOrder: 1, options: ['individual' => 'Individual', 'group' => 'Group', 'one_day' => 'One-day', 'multi_session' => 'Multi-session'])]
    public string $default_trial_type = 'individual';

    #[SettingField(controlType: ControlType::Currency, label: 'Default trial fee (cents)', group: 'Trials', sortOrder: 2, min: 0, helpText: 'Wave 4 Finance module wires payment collection. Wave 2 accepts the value + records payment_status=\'none\'.')]
    public int $default_trial_fee_cents = 0;

    #[SettingField(controlType: ControlType::Text, label: 'Trial reminder offsets', group: 'Trials', sortOrder: 3, options: ['48' => '48 hours before', '24' => '24 hours before', '4' => '4 hours before', '1' => '1 hour before', '0' => 'Do not send reminders'])]
    public array $trial_reminder_offsets_hours = [24, 1];


    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', icon: 'bell', sortOrder: 5)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify safeguarding lead on coach changes for adult teams', group: 'Notifications', sortOrder: 1, helpText: 'Default off — only minor-only teams route to safeguarding by default. Turning on notifies for every coach change.')]
    public bool $coach_change_notify_minors_only = false;

    #[SettingField(controlType: ControlType::Text, label: 'Schedule-change notification channels', group: 'Notifications', sortOrder: 2, options: ['mail' => 'Email', 'push' => 'Push notification', 'in_app' => 'In-app', 'sms' => 'SMS (usage charged)'], helpText: 'Schedule changes cannot be opted out by members. This chooses the delivery channels.')]
    public array $schedule_change_notify_channels = ['mail', 'in_app', 'push'];

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'teams';
    }
}
