<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Stackra\Teams\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Team hard-deleted (PurgeArchivedTeamsJob past 730-day hold). Cascade FK removes downstream rows.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TeamDeleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $deletedAt  ISO 8601
     * @param  int  $cascadedMemberCount  int
     * @param  int  $cascadedTrialCount  int
     * @param  int  $cascadedEventTeamCount  int
     */
    public function __construct(
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $deletedAt,
        public readonly int $cascadedMemberCount,
        public readonly int $cascadedTrialCount,
        public readonly int $cascadedEventTeamCount,
    ) {
    }
}
