<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TeamMember row created with status='active'. Also fires as the target of a Trial conversion (fired from within the atomic conversion transaction's afterCommit).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchTeamMemberJoinedNotification (to team coach + captain)
 * - cache::InvalidateTeamRosterCache
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TeamMemberJoined implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamMemberId  ULID (trm_*)
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $memberType  'athlete' | 'coach' | 'staff' | 'user'
     * @param  string  $memberId  ULID
     * @param  string  $roleOnTeam  string
     * @param  int  $jerseyNumber  int | null
     * @param  string  $position  string | null
     * @param  string  $joinedViaTrialId  ULID | null (trial conversion source when applicable)
     * @param  string  $joinedAt  ISO 8601
     */
    public function __construct(
        public readonly string $teamMemberId,
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $memberType,
        public readonly string $memberId,
        public readonly string $roleOnTeam,
        public readonly int $jerseyNumber,
        public readonly string $position,
        public readonly string $joinedViaTrialId,
        public readonly string $joinedAt,
    ) {
    }
}
