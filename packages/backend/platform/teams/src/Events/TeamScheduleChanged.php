<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Stackra\Teams\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Team.schedule_pattern JSONB mutated. Wave 3 Sports module listens to this to recompute ResourceBooking suggestions.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchTeamScheduleChangedNotification (cannot-opt-out to active members)
 * - cache::InvalidateTeamScheduleCache
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TeamScheduleChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $daysOfWeekTouched  string[]
     * @param  string  $updatedBy  ULID
     */
    public function __construct(
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $daysOfWeekTouched,
        public readonly string $updatedBy,
    ) {
    }
}
