<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Stackra\Teams\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * event_teams.checked_in_at set from null → now().
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EventTeamCheckedIn implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventTeamId  ULID
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $checkedInAt  ISO 8601
     * @param  string  $checkedInBy  ULID
     * @param  int  $attendanceCount  int | null
     */
    public function __construct(
        public readonly string $eventTeamId,
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $checkedInAt,
        public readonly string $checkedInBy,
        public readonly int $attendanceCount,
    ) {
    }
}
