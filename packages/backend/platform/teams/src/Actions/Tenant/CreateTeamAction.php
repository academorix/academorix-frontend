<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Actions\Tenant;

use Academorix\Teams\Contracts\Repositories\TeamRepositoryInterface;
use Academorix\Teams\Data\TeamData;
use Academorix\Teams\Data\Requests\CreateTeamRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/teams` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[AsAction(name: 'teams.create')]
#[Post('/api/v1/teams')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateTeamAction
{
    use AsController;

    public function __construct(
        private readonly TeamRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `team` from the validated request payload.
     *
     * @param  CreateTeamRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateTeamRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(TeamData::from($model), JsonResponse::HTTP_CREATED);
    }
}
