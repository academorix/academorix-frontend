<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Stackra\Teams\Actions\Tenant;

use Stackra\Teams\Contracts\Repositories\EventTeamRepositoryInterface;
use Stackra\Teams\Data\EventTeamData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/teams/{team}/event-teams/{eventTeam}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[AsAction(name: 'teams.event_teams.show')]
#[Get('/api/v1/teams/{team}/event-teams/{eventTeam}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowEventTeamAction
{
    use AsController;

    public function __construct(
        private readonly EventTeamRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `event-team` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): EventTeamData
    {
        return EventTeamData::from($this->repository->findOrFail($id));
    }
}
