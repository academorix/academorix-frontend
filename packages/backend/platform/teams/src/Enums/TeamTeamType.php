<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Stackra\Teams\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `team_type` on `team`.
 *
 * Enum: squad / class / cohort / group / crew / other.
 *
 * @category Teams
 *
 * @since    0.1.0
 */
enum TeamTeamType: string
{
    use Enum;

    case Squad = 'squad';
    case ClassTeamType = 'class';
    case Cohort = 'cohort';
    case Group = 'group';
    case Crew = 'crew';
    case Other = 'other';
}
