<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Stackra\Teams\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Teams module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum TeamsPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `teams.viewAny` — Teams ViewAny
     */
    #[Label('Teams ViewAny')]
    #[Description('Teams ViewAny')]
    case TeamsViewAny = 'teams.viewAny';

    /**
     * `teams.view` — Teams View
     */
    #[Label('Teams View')]
    #[Description('Teams View')]
    case TeamsView = 'teams.view';

    /**
     * `teams.create` — Teams Create
     */
    #[Label('Teams Create')]
    #[Description('Teams Create')]
    case TeamsCreate = 'teams.create';

    /**
     * `teams.update` — Teams Update
     */
    #[Label('Teams Update')]
    #[Description('Teams Update')]
    case TeamsUpdate = 'teams.update';

    /**
     * `teams.delete` — Teams Delete
     */
    #[Label('Teams Delete')]
    #[Description('Teams Delete')]
    case TeamsDelete = 'teams.delete';

    /**
     * `teams.archive` — Transition team to archived status — terminal.
     */
    #[Label('Transition team to archived status — terminal')]
    #[Description('Transition team to archived status — terminal. Cascades members when combined with --cascade-members flag.')]
    case TeamsArchive = 'teams.archive';

    /**
     * `teams.pause` — Teams Pause
     */
    #[Label('Teams Pause')]
    #[Description('Teams Pause')]
    case TeamsPause = 'teams.pause';

    /**
     * `teams.manage-schedule` — Edit schedule_pattern.
     */
    #[Label('Edit schedule_pattern')]
    #[Description('Edit schedule_pattern.')]
    case TeamsManageSchedule = 'teams.manage-schedule';

    /**
     * `teams.manage-registration` — Open + close registration windows.
     */
    #[Label('Open + close registration windows')]
    #[Description('Open + close registration windows.')]
    case TeamsManageRegistration = 'teams.manage-registration';

    /**
     * `teams.assign-coach` — Change coach_id — child-safety-audit-critical when team includes minors.
     */
    #[Label('Change coach_id — child-safety-audit-critical when team includes minors')]
    #[Description('Change coach_id — child-safety-audit-critical when team includes minors.')]
    case TeamsAssignCoach = 'teams.assign-coach';

    /**
     * `teams.view-audit-usage` — See utilisation reports (roster retention, trial conversion rate, event participation).
     */
    #[Label('See utilisation reports (roster retention, trial conversion rate, event particip')]
    #[Description('See utilisation reports (roster retention, trial conversion rate, event participation).')]
    case TeamsViewAuditUsage = 'teams.view-audit-usage';

    /**
     * `team_members.viewAny` — See team_members owned by the caller (as coach) or where they themselves are a member.
     */
    #[Label('See team_members owned by the caller (as coach) or where they themselves are a m')]
    #[Description('See team_members owned by the caller (as coach) or where they themselves are a member. Admins additionally see all via the TeamMemberPolicy scope hook.')]
    case TeamMembersViewAny = 'team_members.viewAny';

    /**
     * `team_members.view-all` — See EVERY roster row in the tenant.
     */
    #[Label('See EVERY roster row in the tenant')]
    #[Description('See EVERY roster row in the tenant. Distinct from team_members.viewAny (own-only).')]
    case TeamMembersViewAll = 'team_members.view-all';

    /**
     * `team_members.view` — Team Members View
     */
    #[Label('Team Members View')]
    #[Description('Team Members View')]
    case TeamMembersView = 'team_members.view';

    /**
     * `team_members.create` — Add a member to a team's roster.
     */
    #[Label('Add a member to a team\'s roster')]
    #[Description('Add a member to a team\'s roster.')]
    case TeamMembersCreate = 'team_members.create';

    /**
     * `team_members.update` — Team Members Update
     */
    #[Label('Team Members Update')]
    #[Description('Team Members Update')]
    case TeamMembersUpdate = 'team_members.update';

    /**
     * `team_members.delete` — Soft-delete a terminal (inactive/graduated) roster row.
     */
    #[Label('Soft-delete a terminal (inactive/graduated) roster row')]
    #[Description('Soft-delete a terminal (inactive/graduated) roster row.')]
    case TeamMembersDelete = 'team_members.delete';

    /**
     * `team_members.change-role` — Team Members Change-role
     */
    #[Label('Team Members Change-role')]
    #[Description('Team Members Change-role')]
    case TeamMembersChangeRole = 'team_members.change-role';

    /**
     * `team_members.mark-injured` — Team Members Mark-injured
     */
    #[Label('Team Members Mark-injured')]
    #[Description('Team Members Mark-injured')]
    case TeamMembersMarkInjured = 'team_members.mark-injured';

    /**
     * `team_members.mark-graduated` — Team Members Mark-graduated
     */
    #[Label('Team Members Mark-graduated')]
    #[Description('Team Members Mark-graduated')]
    case TeamMembersMarkGraduated = 'team_members.mark-graduated';

    /**
     * `team_members.suspend` — Disciplinary suspension.
     */
    #[Label('Disciplinary suspension')]
    #[Description('Disciplinary suspension. More sensitive than mark-injured — restricted to branch_manager+.')]
    case TeamMembersSuspend = 'team_members.suspend';

    /**
     * `team_capacity_override` — Bypass CapacityGate refusals on roster-add.
     */
    #[Label('Bypass CapacityGate refusals on roster-add')]
    #[Description('Bypass CapacityGate refusals on roster-add. Rarely granted; audit-critical.')]
    case TeamCapacityOverride = 'team_capacity_override';

    /**
     * `trials.viewAny` — Trials ViewAny
     */
    #[Label('Trials ViewAny')]
    #[Description('Trials ViewAny')]
    case TrialsViewAny = 'trials.viewAny';

    /**
     * `trials.view` — Trials View
     */
    #[Label('Trials View')]
    #[Description('Trials View')]
    case TrialsView = 'trials.view';

    /**
     * `trials.view-anonymous` — See anonymous trialist profile fields (name/phone/email/dob).
     */
    #[Label('See anonymous trialist profile fields (name/phone/email/dob)')]
    #[Description('See anonymous trialist profile fields (name/phone/email/dob). Restricted to compliance + admin. Redacted from wire projection for callers lacking this permission.')]
    case TrialsViewAnonymous = 'trials.view-anonymous';

    /**
     * `trials.schedule` — Trials Schedule
     */
    #[Label('Trials Schedule')]
    #[Description('Trials Schedule')]
    case TrialsSchedule = 'trials.schedule';

    /**
     * `trials.evaluate` — Write evaluation_notes + complete / reject a trial.
     */
    #[Label('Write evaluation_notes + complete / reject a trial')]
    #[Description('Write evaluation_notes + complete / reject a trial. Restricted to the team\'s coach OR branch_manager+.')]
    case TrialsEvaluate = 'trials.evaluate';

    /**
     * `trials.evaluate-any` — Evaluate trials for teams the caller doesn't coach.
     */
    #[Label('Evaluate trials for teams the caller doesn\'t coach')]
    #[Description('Evaluate trials for teams the caller doesn\'t coach. Cover-shift scenario.')]
    case TrialsEvaluateAny = 'trials.evaluate-any';

    /**
     * `trials.convert` — Atomic conversion of a trial to a team member.
     */
    #[Label('Atomic conversion of a trial to a team member')]
    #[Description('Atomic conversion of a trial to a team member. Requires trials.evaluate too.')]
    case TrialsConvert = 'trials.convert';

    /**
     * `trials.cancel` — Trials Cancel
     */
    #[Label('Trials Cancel')]
    #[Description('Trials Cancel')]
    case TrialsCancel = 'trials.cancel';

    /**
     * `event_teams.viewAny` — Event Teams ViewAny
     */
    #[Label('Event Teams ViewAny')]
    #[Description('Event Teams ViewAny')]
    case EventTeamsViewAny = 'event_teams.viewAny';

    /**
     * `event_teams.view` — Event Teams View
     */
    #[Label('Event Teams View')]
    #[Description('Event Teams View')]
    case EventTeamsView = 'event_teams.view';

    /**
     * `event_teams.register` — Event Teams Register
     */
    #[Label('Event Teams Register')]
    #[Description('Event Teams Register')]
    case EventTeamsRegister = 'event_teams.register';

    /**
     * `event_teams.update` — Event Teams Update
     */
    #[Label('Event Teams Update')]
    #[Description('Event Teams Update')]
    case EventTeamsUpdate = 'event_teams.update';

    /**
     * `event_teams.withdraw` — Event Teams Withdraw
     */
    #[Label('Event Teams Withdraw')]
    #[Description('Event Teams Withdraw')]
    case EventTeamsWithdraw = 'event_teams.withdraw';

    /**
     * `event_teams.check-in` — Mark team as checked in at the event.
     */
    #[Label('Mark team as checked in at the event')]
    #[Description('Mark team as checked in at the event.')]
    case EventTeamsCheckIn = 'event_teams.check-in';

    /**
     * `event_teams.record-result` — Record the result JSONB.
     */
    #[Label('Record the result JSONB')]
    #[Description('Record the result JSONB. Restricted to coaches + branch_manager+.')]
    case EventTeamsRecordResult = 'event_teams.record-result';

    /**
     * `platform.teams.viewAny` — Platform Teams ViewAny
     */
    #[Label('Platform Teams ViewAny')]
    #[Description('Platform Teams ViewAny')]
    case PlatformTeamsViewAny = 'platform.teams.viewAny';

    /**
     * `platform.teams.view` — Platform Teams View
     */
    #[Label('Platform Teams View')]
    #[Description('Platform Teams View')]
    case PlatformTeamsView = 'platform.teams.view';

    /**
     * `platform.teams.force-close` — Support intervention to force-archive a team bypassing every tenant-level guard.
     */
    #[Label('Support intervention to force-archive a team bypassing every tenant-level guard')]
    #[Description('Support intervention to force-archive a team bypassing every tenant-level guard. Every fire is audit-critical + notifies the tenant admin.')]
    case PlatformTeamsForceClose = 'platform.teams.force-close';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::TeamsViewAny => Guard::Sanctum,
            self::TeamsView => Guard::Sanctum,
            self::TeamsCreate => Guard::Sanctum,
            self::TeamsUpdate => Guard::Sanctum,
            self::TeamsDelete => Guard::Sanctum,
            self::TeamsArchive => Guard::Sanctum,
            self::TeamsPause => Guard::Sanctum,
            self::TeamsManageSchedule => Guard::Sanctum,
            self::TeamsManageRegistration => Guard::Sanctum,
            self::TeamsAssignCoach => Guard::Sanctum,
            self::TeamsViewAuditUsage => Guard::Sanctum,
            self::TeamMembersViewAny => Guard::Sanctum,
            self::TeamMembersViewAll => Guard::Sanctum,
            self::TeamMembersView => Guard::Sanctum,
            self::TeamMembersCreate => Guard::Sanctum,
            self::TeamMembersUpdate => Guard::Sanctum,
            self::TeamMembersDelete => Guard::Sanctum,
            self::TeamMembersChangeRole => Guard::Sanctum,
            self::TeamMembersMarkInjured => Guard::Sanctum,
            self::TeamMembersMarkGraduated => Guard::Sanctum,
            self::TeamMembersSuspend => Guard::Sanctum,
            self::TeamCapacityOverride => Guard::Sanctum,
            self::TrialsViewAny => Guard::Sanctum,
            self::TrialsView => Guard::Sanctum,
            self::TrialsViewAnonymous => Guard::Sanctum,
            self::TrialsSchedule => Guard::Sanctum,
            self::TrialsEvaluate => Guard::Sanctum,
            self::TrialsEvaluateAny => Guard::Sanctum,
            self::TrialsConvert => Guard::Sanctum,
            self::TrialsCancel => Guard::Sanctum,
            self::EventTeamsViewAny => Guard::Sanctum,
            self::EventTeamsView => Guard::Sanctum,
            self::EventTeamsRegister => Guard::Sanctum,
            self::EventTeamsUpdate => Guard::Sanctum,
            self::EventTeamsWithdraw => Guard::Sanctum,
            self::EventTeamsCheckIn => Guard::Sanctum,
            self::EventTeamsRecordResult => Guard::Sanctum,
            self::PlatformTeamsViewAny => Guard::PlatformAdmin,
            self::PlatformTeamsView => Guard::PlatformAdmin,
            self::PlatformTeamsForceClose => Guard::PlatformAdmin,
        };
    }
}
