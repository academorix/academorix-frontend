<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `participation_type` on `event-team`.
 *
 * Enum: home / away / host / guest / participant.
 *
 * @category Teams
 *
 * @since    0.1.0
 */
enum EventTeamParticipationType: string
{
    use Enum;

    case Home = 'home';
    case Away = 'away';
    case Host = 'host';
    case Guest = 'guest';
    case Participant = 'participant';
}
