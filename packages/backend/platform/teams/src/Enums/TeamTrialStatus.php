<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Stackra\Teams\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `status` on `team-trial`.
 *
 * Enum: scheduled / in_progress / completed / converted / rejected / no_show / cancelled.
 *
 * @category Teams
 *
 * @since    0.1.0
 */
enum TeamTrialStatus: string
{
    use Enum;

    case Scheduled = 'scheduled';
    case InProgress = 'in_progress';
    case Completed = 'completed';
    case Converted = 'converted';
    case Rejected = 'rejected';
    case NoShow = 'no_show';
    case Cancelled = 'cancelled';
}
