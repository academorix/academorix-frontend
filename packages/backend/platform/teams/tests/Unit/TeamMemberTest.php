<?php

declare(strict_types=1);

use Stackra\Teams\Models\TeamMember;

it('is a placeholder — replace with actual TeamMember unit tests', function (): void {
    expect(TeamMember::class)->toBeString();
});
