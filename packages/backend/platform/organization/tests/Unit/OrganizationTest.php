<?php

declare(strict_types=1);

use Stackra\Organization\Models\Organization;

it('is a placeholder — replace with actual Organization unit tests', function (): void {
    expect(Organization::class)->toBeString();
});
