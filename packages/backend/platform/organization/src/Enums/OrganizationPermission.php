<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform organization --force`.

declare(strict_types=1);

namespace Academorix\Organization\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Organization module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Organization
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum OrganizationPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `organizations.viewAny.tenant` — List Organizations for the current tenant.
     */
    #[Label('List Organizations for the current tenant')]
    #[Description('List Organizations for the current tenant. Any tenant member can see the org list.')]
    case OrganizationsViewAnyTenant = 'organizations.viewAny.tenant';

    /**
     * `organizations.view.tenant` — Organizations View Tenant
     */
    #[Label('Organizations View Tenant')]
    #[Description('Organizations View Tenant')]
    case OrganizationsViewTenant = 'organizations.view.tenant';

    /**
     * `organizations.create.tenant` — Create a new Organization.
     */
    #[Label('Create a new Organization')]
    #[Description('Create a new Organization. Consumes organization_slot. Refused at cap.')]
    case OrganizationsCreateTenant = 'organizations.create.tenant';

    /**
     * `organizations.update.tenant` — Organizations Update Tenant
     */
    #[Label('Organizations Update Tenant')]
    #[Description('Organizations Update Tenant')]
    case OrganizationsUpdateTenant = 'organizations.update.tenant';

    /**
     * `organizations.delete.tenant` — Soft-delete + archive.
     */
    #[Label('Soft-delete + archive')]
    #[Description('Soft-delete + archive. Refused on is_default / has descendants / referenced by branches.')]
    case OrganizationsDeleteTenant = 'organizations.delete.tenant';

    /**
     * `organizations.reparent.tenant` — Move an Organization under a new parent.
     */
    #[Label('Move an Organization under a new parent')]
    #[Description('Move an Organization under a new parent. Requires organization_reparent entitlement. Every reparent is audit-critical.')]
    case OrganizationsReparentTenant = 'organizations.reparent.tenant';

    /**
     * `organizations.set_default.tenant` — Flip is_default=true.
     */
    #[Label('Flip is_default=true')]
    #[Description('Flip is_default=true. Audit-critical (shifts every downstream default-org filter).')]
    case OrganizationsSetDefaultTenant = 'organizations.set_default.tenant';

    /**
     * `organizations.pause.tenant` — Organizations Pause Tenant
     */
    #[Label('Organizations Pause Tenant')]
    #[Description('Organizations Pause Tenant')]
    case OrganizationsPauseTenant = 'organizations.pause.tenant';

    /**
     * `organizations.resume.tenant` — Organizations Resume Tenant
     */
    #[Label('Organizations Resume Tenant')]
    #[Description('Organizations Resume Tenant')]
    case OrganizationsResumeTenant = 'organizations.resume.tenant';

    /**
     * `organizations.archive.tenant` — Organizations Archive Tenant
     */
    #[Label('Organizations Archive Tenant')]
    #[Description('Organizations Archive Tenant')]
    case OrganizationsArchiveTenant = 'organizations.archive.tenant';

    /**
     * `organizations.branding.update.tenant` — Write logo_url + primary_color + secondary_color + description.
     */
    #[Label('Write logo_url + primary_color + secondary_color + description')]
    #[Description('Write logo_url + primary_color + secondary_color + description. Requires organization_branding entitlement.')]
    case OrganizationsBrandingUpdateTenant = 'organizations.branding.update.tenant';

    /**
     * `organizations.tree.view.tenant` — Read the tenant tree (org hierarchy).
     */
    #[Label('Read the tenant tree (org hierarchy)')]
    #[Description('Read the tenant tree (org hierarchy). Same audience as viewAny.')]
    case OrganizationsTreeViewTenant = 'organizations.tree.view.tenant';

    /**
     * `platform.organizations.viewAny` — Cross-tenant Organization observability.
     */
    #[Label('Cross-tenant Organization observability')]
    #[Description('Cross-tenant Organization observability.')]
    case PlatformOrganizationsViewAny = 'platform.organizations.viewAny';

    /**
     * `platform.organizations.view` — Platform Organizations View
     */
    #[Label('Platform Organizations View')]
    #[Description('Platform Organizations View')]
    case PlatformOrganizationsView = 'platform.organizations.view';

    /**
     * `platform.organizations.tree.view` — Platform Organizations Tree View
     */
    #[Label('Platform Organizations Tree View')]
    #[Description('Platform Organizations Tree View')]
    case PlatformOrganizationsTreeView = 'platform.organizations.tree.view';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::OrganizationsViewAnyTenant => Guard::Sanctum,
            self::OrganizationsViewTenant => Guard::Sanctum,
            self::OrganizationsCreateTenant => Guard::Sanctum,
            self::OrganizationsUpdateTenant => Guard::Sanctum,
            self::OrganizationsDeleteTenant => Guard::Sanctum,
            self::OrganizationsReparentTenant => Guard::Sanctum,
            self::OrganizationsSetDefaultTenant => Guard::Sanctum,
            self::OrganizationsPauseTenant => Guard::Sanctum,
            self::OrganizationsResumeTenant => Guard::Sanctum,
            self::OrganizationsArchiveTenant => Guard::Sanctum,
            self::OrganizationsBrandingUpdateTenant => Guard::Sanctum,
            self::OrganizationsTreeViewTenant => Guard::Sanctum,
            self::PlatformOrganizationsViewAny => Guard::PlatformAdmin,
            self::PlatformOrganizationsView => Guard::PlatformAdmin,
            self::PlatformOrganizationsTreeView => Guard::PlatformAdmin,
        };
    }
}
