<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform organization --force`.

declare(strict_types=1);

namespace Academorix\Organization\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan organization:rebuild-tree-paths` — Rebuild tree paths — organization command.
 *
 * @category Organization
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'organization:rebuild-tree-paths',
    description: 'Rebuild tree paths — organization command.',
)]
final class RebuildTreePathsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Rebuild tree paths — organization command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Rebuild tree paths — organization command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
