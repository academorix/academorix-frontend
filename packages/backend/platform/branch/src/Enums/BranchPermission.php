<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

namespace Academorix\Branch\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Branch module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Branch
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum BranchPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `branches.viewAny` — Base variant — tenant-scoped list across every branch under the caller's active tenant.
     */
    #[Label('Base variant — tenant-scoped list across every branch under the caller\'s active')]
    #[Description('Base variant — tenant-scoped list across every branch under the caller\'s active tenant.')]
    case BranchesViewAny = 'branches.viewAny';

    /**
     * `branches.viewAny.tenant` — Branches ViewAny Tenant
     */
    #[Label('Branches ViewAny Tenant')]
    #[Description('Branches ViewAny Tenant')]
    case BranchesViewAnyTenant = 'branches.viewAny.tenant';

    /**
     * `branches.viewAny.own` — Members see only the branches they participate in (via TenantMember / TeamMember / AthleteEnrollment).
     */
    #[Label('Members see only the branches they participate in (via TenantMember / TeamMember')]
    #[Description('Members see only the branches they participate in (via TenantMember / TeamMember / AthleteEnrollment).')]
    case BranchesViewAnyOwn = 'branches.viewAny.own';

    /**
     * `branches.view` — Read a single Branch.
     */
    #[Label('Read a single Branch')]
    #[Description('Read a single Branch. Every tenant member can read the branches they have visibility on.')]
    case BranchesView = 'branches.view';

    /**
     * `branches.view.own` — Branches View Own
     */
    #[Label('Branches View Own')]
    #[Description('Branches View Own')]
    case BranchesViewOwn = 'branches.view.own';

    /**
     * `branches.create` — Also gated by the `branch_slot` entitlement.
     */
    #[Label('Also gated by the `branch_slot` entitlement')]
    #[Description('Also gated by the `branch_slot` entitlement.')]
    case BranchesCreate = 'branches.create';

    /**
     * `branches.update` — Branches Update
     */
    #[Label('Branches Update')]
    #[Description('Branches Update')]
    case BranchesUpdate = 'branches.update';

    /**
     * `branches.update_hours` — Fine-grained permission for opening-hours edits.
     */
    #[Label('Fine-grained permission for opening-hours edits')]
    #[Description('Fine-grained permission for opening-hours edits. Separated because staff-tier roles may need to update hours without being able to touch address / capacity.')]
    case BranchesUpdateHours = 'branches.update_hours';

    /**
     * `branches.update_hours.branch` — Reception role can update hours only for the branches they're assigned to.
     */
    #[Label('Reception role can update hours only for the branches they\'re assigned to')]
    #[Description('Reception role can update hours only for the branches they\'re assigned to.')]
    case BranchesUpdateHoursBranch = 'branches.update_hours.branch';

    /**
     * `branches.update_address` — Address edits are audit-sensitive — kept as a distinct permission.
     */
    #[Label('Address edits are audit-sensitive — kept as a distinct permission')]
    #[Description('Address edits are audit-sensitive — kept as a distinct permission.')]
    case BranchesUpdateAddress = 'branches.update_address';

    /**
     * `branches.update_coordinates` — Manual coordinate pin drops.
     */
    #[Label('Manual coordinate pin drops')]
    #[Description('Manual coordinate pin drops. Distinct from `update_address` because pinning without editing the address is a common map-view flow.')]
    case BranchesUpdateCoordinates = 'branches.update_coordinates';

    /**
     * `branches.update_capacity` — Fire-safety cap edits.
     */
    #[Label('Fire-safety cap edits')]
    #[Description('Fire-safety cap edits. Compliance-relevant — kept as a distinct permission for audit clarity.')]
    case BranchesUpdateCapacity = 'branches.update_capacity';

    /**
     * `branches.pause` — Branches Pause
     */
    #[Label('Branches Pause')]
    #[Description('Branches Pause')]
    case BranchesPause = 'branches.pause';

    /**
     * `branches.resume` — Branches Resume
     */
    #[Label('Branches Resume')]
    #[Description('Branches Resume')]
    case BranchesResume = 'branches.resume';

    /**
     * `branches.close` — Higher bar than update — closure affects downstream operations.
     */
    #[Label('Higher bar than update — closure affects downstream operations')]
    #[Description('Higher bar than update — closure affects downstream operations. Owner-tier by default.')]
    case BranchesClose = 'branches.close';

    /**
     * `branches.reopen` — Branches Reopen
     */
    #[Label('Branches Reopen')]
    #[Description('Branches Reopen')]
    case BranchesReopen = 'branches.reopen';

    /**
     * `branches.set_default` — Branches Set Default
     */
    #[Label('Branches Set Default')]
    #[Description('Branches Set Default')]
    case BranchesSetDefault = 'branches.set_default';

    /**
     * `branches.geocode` — Explicitly trigger re-geocoding via `POST /branches/{branch}/geocode`.
     */
    #[Label('Explicitly trigger re-geocoding via `POST /branches/{branch}/geocode`')]
    #[Description('Explicitly trigger re-geocoding via `POST /branches/{branch}/geocode`. Also gated by the `branch_geocoding` entitlement.')]
    case BranchesGeocode = 'branches.geocode';

    /**
     * `branches.audit_address` — Read the address-audit CLI-style report — validates the structured address against the country's postal format + reports drift from the geocoded canonical form.
     */
    #[Label('Read the address-audit CLI-style report — validates the structured address again')]
    #[Description('Read the address-audit CLI-style report — validates the structured address against the country\'s postal format + reports drift from the geocoded canonical form.')]
    case BranchesAuditAddress = 'branches.audit_address';

    /**
     * `platform.branches.viewAny` — Cross-tenant read for support tooling.
     */
    #[Label('Cross-tenant read for support tooling')]
    #[Description('Cross-tenant read for support tooling.')]
    case PlatformBranchesViewAny = 'platform.branches.viewAny';

    /**
     * `platform.branches.view` — Platform Branches View
     */
    #[Label('Platform Branches View')]
    #[Description('Platform Branches View')]
    case PlatformBranchesView = 'platform.branches.view';

    /**
     * `platform.branches.force_close` — Compliance-forced closure that bypasses the dependency-check kill switch.
     */
    #[Label('Compliance-forced closure that bypasses the dependency-check kill switch')]
    #[Description('Compliance-forced closure that bypasses the dependency-check kill switch. Super-admin only — every use fires an alert.')]
    case PlatformBranchesForceClose = 'platform.branches.force_close';

    /**
     * `platform.branches.hard_delete` — Two-person approval via `branch:hard-delete --platform-support`.
     */
    #[Label('Two-person approval via `branch:hard-delete --platform-support`')]
    #[Description('Two-person approval via `branch:hard-delete --platform-support`. Never exposed on any HTTP surface — CLI-only.')]
    case PlatformBranchesHardDelete = 'platform.branches.hard_delete';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::BranchesViewAny => Guard::Sanctum,
            self::BranchesViewAnyTenant => Guard::Sanctum,
            self::BranchesViewAnyOwn => Guard::Sanctum,
            self::BranchesView => Guard::Sanctum,
            self::BranchesViewOwn => Guard::Sanctum,
            self::BranchesCreate => Guard::Sanctum,
            self::BranchesUpdate => Guard::Sanctum,
            self::BranchesUpdateHours => Guard::Sanctum,
            self::BranchesUpdateHoursBranch => Guard::Sanctum,
            self::BranchesUpdateAddress => Guard::Sanctum,
            self::BranchesUpdateCoordinates => Guard::Sanctum,
            self::BranchesUpdateCapacity => Guard::Sanctum,
            self::BranchesPause => Guard::Sanctum,
            self::BranchesResume => Guard::Sanctum,
            self::BranchesClose => Guard::Sanctum,
            self::BranchesReopen => Guard::Sanctum,
            self::BranchesSetDefault => Guard::Sanctum,
            self::BranchesGeocode => Guard::Sanctum,
            self::BranchesAuditAddress => Guard::Sanctum,
            self::PlatformBranchesViewAny => Guard::PlatformAdmin,
            self::PlatformBranchesView => Guard::PlatformAdmin,
            self::PlatformBranchesForceClose => Guard::PlatformAdmin,
            self::PlatformBranchesHardDelete => Guard::PlatformAdmin,
        };
    }
}
