<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

namespace Academorix\Branch\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan branch:update-hours` — Update hours — branch command.
 *
 * @category Branch
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'branch:update-hours',
    description: 'Update hours — branch command.',
)]
final class UpdateHoursCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Update hours — branch command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Update hours — branch command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
