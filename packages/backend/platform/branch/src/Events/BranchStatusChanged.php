<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

namespace Academorix\Branch\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Branch.status transitioned to any new value. A superset event; the specific-state events below fire alongside for the routing case that cares.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Branch
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class BranchStatusChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $branchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromStatus  'active' | 'paused' | 'closed'
     * @param  string  $toStatus  'active' | 'paused' | 'closed'
     * @param  string  $changedBy  ULID
     * @param  string  $reason  string | null
     */
    public function __construct(
        public readonly string $branchId,
        public readonly string $tenantId,
        public readonly string $fromStatus,
        public readonly string $toStatus,
        public readonly string $changedBy,
        public readonly string $reason,
    ) {
    }
}
