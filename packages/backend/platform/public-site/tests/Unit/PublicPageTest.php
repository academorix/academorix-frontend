<?php

declare(strict_types=1);

use Stackra\PublicSite\Models\PublicPage;

it('is a placeholder — replace with actual PublicPage unit tests', function (): void {
    expect(PublicPage::class)->toBeString();
});
