<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform public-site --force`.

declare(strict_types=1);

namespace Academorix\PublicSite\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan public-site:list` — List every public site row.
 *
 * @category PublicSite
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'public-site:list',
    description: 'List every public site row.',
)]
final class ListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List every public site row', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List every public site row completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
