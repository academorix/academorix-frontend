<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform public-site --force`.

declare(strict_types=1);

namespace Stackra\PublicSite\Actions\Tenant;

use Stackra\PublicSite\Contracts\Repositories\PublicPageRepositoryInterface;
use Stackra\PublicSite\Data\PublicPageData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/public-pages/{page}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category PublicSite
 *
 * @since    0.1.0
 */
#[AsAction(name: 'public_site.public_pages.show')]
#[Get('/api/v1/public-pages/{page}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowPublicPageAction
{
    use AsController;

    public function __construct(
        private readonly PublicPageRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `public-page` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): PublicPageData
    {
        return PublicPageData::from($this->repository->findOrFail($id));
    }
}
