<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform tenancy --force`.

declare(strict_types=1);

namespace Stackra\Tenancy\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * TenantNotificationSettings — Admin notifications.
 *
 * Which notifications reach the tenant's admins.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Tenancy
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'tenant_notifications',
    label: 'Admin notifications',
    description: 'Which notifications reach the tenant\'s admins.',
    icon: 'bell',
    scope: 'tenant',
    permission: 'tenants.manage_own_settings',
    sortOrder: 5,
)]
class TenantNotificationSettings extends Settings
{

    // ── Lifecycle events ──────────────────────────────────
    #[SettingGroup(label: 'Lifecycle events', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Email on suspend / resume / archive', validation: ['boolean'], group: 'Lifecycle events', sortOrder: 1)]
    public bool $tenant_lifecycle_email = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'In-app notification on lifecycle', validation: ['boolean'], group: 'Lifecycle events', sortOrder: 2)]
    public bool $tenant_lifecycle_in_app = true;


    // ── Domain + branding ──────────────────────────────────
    #[SettingGroup(label: 'Domain + branding', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Email on domain verification result', validation: ['boolean'], group: 'Domain + branding', sortOrder: 1)]
    public bool $domain_verification_email = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'In-app notification on branding change', validation: ['boolean'], group: 'Domain + branding', sortOrder: 2)]
    public bool $branding_updated_in_app = true;


    // ── Integrations ──────────────────────────────────
    #[SettingGroup(label: 'Integrations', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Route integration failures to Slack', validation: ['boolean'], group: 'Integrations', sortOrder: 1)]
    public bool $integration_failure_slack = false;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'tenant_notifications';
    }
}
