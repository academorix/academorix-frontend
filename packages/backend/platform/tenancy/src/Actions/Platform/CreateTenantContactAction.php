<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform tenancy --force`.

declare(strict_types=1);

namespace Stackra\Tenancy\Actions\Platform;

use Stackra\Tenancy\Contracts\Repositories\TenantContactRepositoryInterface;
use Stackra\Tenancy\Data\TenantContactData;
use Stackra\Tenancy\Data\Requests\CreateTenantContactRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/tenant-contacts` — create action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Tenancy
 *
 * @since    0.1.0
 */
#[AsAction(name: 'tenancy.tenant_contacts.create')]
#[Post('/api/v1/tenant-contacts')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class CreateTenantContactAction
{
    use AsController;

    public function __construct(
        private readonly TenantContactRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `tenant-contact` from the validated request payload.
     *
     * @param  CreateTenantContactRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateTenantContactRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(TenantContactData::from($model), JsonResponse::HTTP_CREATED);
    }
}
