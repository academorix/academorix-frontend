<?php

/**
 * @file database/migrations/2026_07_15_120001_create_registration_activities_table.php
 *
 * @description
 * Create the `registration_activities` table for the `RegistrationActivity` entity.
 * Owned by the registrations module. Every column + index reference
 * goes through {@see Academorix\Registrations\Contracts\Data\RegistrationActivityInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registrations --force`.

declare(strict_types=1);

use Academorix\Registrations\Contracts\Data\RegistrationActivityInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(RegistrationActivityInterface::TABLE, function (Blueprint $table): void {
            $table->string(RegistrationActivityInterface::ATTR_ID, 30)->primary();
            $table->string(RegistrationActivityInterface::ATTR_TENANT_ID, 30);
            $table->foreign(RegistrationActivityInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(RegistrationActivityInterface::ATTR_REGISTRATION_ID, 30);
            $table->foreign(RegistrationActivityInterface::ATTR_REGISTRATION_ID)->references('id')->on('registrations')->onDelete('cascade');
            $table->string(RegistrationActivityInterface::ATTR_KIND, 32);
            $table->string(RegistrationActivityInterface::ATTR_ACTOR_USER_ID, 30)->nullable();
            $table->foreign(RegistrationActivityInterface::ATTR_ACTOR_USER_ID)->references('id')->on('users')->onDelete('set null');
            $table->text(RegistrationActivityInterface::ATTR_BODY)->nullable();
            $table->timestampTz(RegistrationActivityInterface::ATTR_OCCURRED_AT);
            $table->jsonb(RegistrationActivityInterface::ATTR_CONTEXT)->nullable();
            $table->jsonb(RegistrationActivityInterface::ATTR_METADATA)->nullable();
            $table->string(RegistrationActivityInterface::ATTR_CREATED_BY, 30)->nullable();
            $table->string(RegistrationActivityInterface::ATTR_UPDATED_BY, 30)->nullable();
            $table->timestampTz(RegistrationActivityInterface::ATTR_CREATED_AT);
            $table->timestampTz(RegistrationActivityInterface::ATTR_UPDATED_AT);
            $table->index([RegistrationActivityInterface::ATTR_TENANT_ID, RegistrationActivityInterface::ATTR_REGISTRATION_ID, RegistrationActivityInterface::ATTR_OCCURRED_AT], 'registration_activities_timeline_idx');
            $table->index([RegistrationActivityInterface::ATTR_TENANT_ID, RegistrationActivityInterface::ATTR_KIND, RegistrationActivityInterface::ATTR_OCCURRED_AT], 'registration_activities_kind_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(RegistrationActivityInterface::TABLE);
    }
};
