<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Hard-delete completed via PurgeArchivedSeasonsJob (past retention window + no inbound rows) OR via platform-support CLI (two-person path). Wave 3a refuses public hard-delete.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — the audit outlives the row)
 * - activity::WriteToActivityLog
 *
 * @category Season
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SeasonDeleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID (the row is gone; the ULID is preserved in the audit trail)
     * @param  string  $tenantId  ULID
     * @param  string  $organizationId  ULID | null
     * @param  string  $branchId  ULID | null
     * @param  string  $mode  'soft' | 'hard'
     * @param  string  $deletedBy  ULID (platform-support operator when mode='hard')
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly string $organizationId,
        public readonly string $branchId,
        public readonly string $mode,
        public readonly string $deletedBy,
    ) {
    }
}
