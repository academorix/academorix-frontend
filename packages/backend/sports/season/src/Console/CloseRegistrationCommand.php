<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan season:close-registration` — Close registration — season command.
 *
 * @category Season
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'season:close-registration',
    description: 'Close registration — season command.',
)]
final class CloseRegistrationCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Close registration — season command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Close registration — season command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
