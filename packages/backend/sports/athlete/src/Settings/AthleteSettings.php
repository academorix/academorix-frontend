<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Athlete\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * AthleteSettings — Athletes.
 *
 * Tenant-wide athlete lifecycle defaults + feature opt-ins.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'athlete',
    label: 'Athletes',
    description: 'Tenant-wide athlete lifecycle defaults + feature opt-ins.',
    icon: 'user-group',
    scope: 'tenant',
    permission: 'athletes.viewAny',
    sortOrder: 45,
)]
class AthleteSettings extends Settings
{

    // ── Minor age boundaries ──────────────────────────────────
    #[SettingGroup(label: 'Minor age boundaries', description: 'Age at which the athlete is considered a minor for consent + safeguarding purposes.', icon: 'shield-check', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Number, label: 'Minor age threshold', group: 'Minor age boundaries', sortOrder: 1, min: 13, max: 18, helpText: 'Athletes below this age require guardian-recorded consent. 13 = US COPPA minimum; 16 = GDPR default; 18 = safeguarding-conservative.')]
    public int $minor_min_age = 16;


    // ── Medical tracking ──────────────────────────────────
    #[SettingGroup(label: 'Medical tracking', description: 'Enable medical fields on athlete rows. Data is regulated_health-tier, retained 7 years.', icon: 'heart', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Track medical information', group: 'Medical tracking', sortOrder: 1, helpText: 'Enables the medical_* columns + the /medical sub-resource. Regulated_health-tier data with 7-year retention. Turning this on writes a compliance audit row.')]
    public bool $enable_medical_tracking = false;


    // ── Consent workflow ──────────────────────────────────
    #[SettingGroup(label: 'Consent workflow', description: 'Full recordable consent workflow with expiry + revocation history.', icon: 'document-check', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enable full consent workflow', group: 'Consent workflow', sortOrder: 1, helpText: 'Enterprise-only. Adds 12-month re-consent cadence + per-purpose expiry log. When off, consent is a bare three-boolean snapshot.')]
    public bool $enable_consent_workflow = false;

    #[SettingField(controlType: ControlType::Number, label: 'Re-consent cadence (days)', group: 'Consent workflow', sortOrder: 2, min: 90, max: 730, helpText: 'How often consents must be re-recorded. GDPR audit best-practice is annual (365 days).')]
    public int $reconsent_cadence_days = 365;


    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', icon: 'bell', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Text, label: 'Consent expiring reminder cadence', group: 'Notifications', sortOrder: 1, options: ['60' => '60 days before', '30' => '30 days before', '14' => '14 days before', '7' => '7 days before', '1' => '1 day before'])]
    public array $consent_reminder_thresholds_days = [30, 14, 7];

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify guardians when child\'s age group flips', group: 'Notifications', sortOrder: 2, helpText: 'Sends AthleteAgeGroupSnapshotChangedNotification when the nightly job detects a birthday-drift or catalog-change flip.')]
    public bool $age_group_change_notify_guardian = true;


    // ── Retention ──────────────────────────────────
    #[SettingGroup(label: 'Retention', icon: 'trash', sortOrder: 5)]
    #[SettingField(controlType: ControlType::Number, label: 'Purge graduated athletes after (years)', group: 'Retention', sortOrder: 1, min: 0, max: 30, helpText: '0 = never purge graduated athletes (default, retains alumni indefinitely). Set to 10 or 15 for privacy-conscious tenants that don\'t want long-term alumni retention.')]
    public int $graduate_purge_after_years = 0;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'athlete';
    }
}
