<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Athlete\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Any Athlete scalar field mutated except status (dedicated transition events), consent_* (AthleteConsentRecorded / AthleteConsentRevoked / AthletePhotoConsentChanged), medical_* (AthleteMedicalUpdated — private-tier), emergency_contact_* (AthleteEmergencyContactUpdated), user_id (AthleteLinkedToUser), current_age_group_id (AthleteAgeGroupSnapshotUpdated).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AthleteUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $athleteId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedFields  string[]
     * @param  string  $updatedByUserId  ULID
     */
    public function __construct(
        public readonly string $athleteId,
        public readonly string $tenantId,
        public readonly string $changedFields,
        public readonly string $updatedByUserId,
    ) {
    }
}
