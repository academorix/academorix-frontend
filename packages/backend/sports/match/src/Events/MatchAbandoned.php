<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /matches/{match}/abandon. Only from status=IN_PROGRESS. Terminal. Partial match_events are preserved but no result publish.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — abandonments are compliance-relevant for injury / safeguarding claims)
 * - notifications::DispatchMatchCancelledNotification
 * - monitoring::MetricsCollector
 *
 * @category Match
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MatchAbandoned implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $matchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $reason  string
     * @param  int  $abandonedAtMinute  integer
     * @param  string  $abandonedByUserId  ULID
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $matchId,
        public readonly string $tenantId,
        public readonly string $reason,
        public readonly int $abandonedAtMinute,
        public readonly string $abandonedByUserId,
        public readonly string $at,
    ) {
    }
}
