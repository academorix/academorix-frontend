<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

namespace Academorix\Progress\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan grading:schedule` — Schedule — grading command.
 *
 * @category Progress
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'grading:schedule',
    description: 'Schedule — grading command.',
)]
final class ScheduleCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Schedule — grading command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Schedule — grading command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
