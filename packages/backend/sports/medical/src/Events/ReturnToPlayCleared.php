<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

namespace Academorix\Medical\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Injury.status flipped to 'cleared' by medical role. Re-opens eligibility gate.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 10 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchReturnToPlayClearedNotification (guardian + athlete + coach, cannot-opt-out — celebration)
 * - medical::UpdateEligibilityGate
 *
 * @category Medical
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ReturnToPlayCleared implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $injuryId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $clearedAt  ISO 8601
     * @param  string  $clearedByUserId  ULID
     * @param  int  $daysSinceOnset  integer
     */
    public function __construct(
        public readonly string $injuryId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $clearedAt,
        public readonly string $clearedByUserId,
        public readonly int $daysSinceOnset,
    ) {
    }
}
