<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports performance --force`.

declare(strict_types=1);

namespace Academorix\Performance\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Athlete's performance profile recomputed (weighted average of recent test results + trend line).
 *
 * ## Consumers
 *
 * - ai::UpdatePerformanceEmbedding
 * - reporting::UpdatePerformanceDashboard
 *
 * @category Performance
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PerformanceProfileUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $athleteId  ULID
     * @param  string  $profileId  ULID (prf_*)
     * @param  string  $computedAt  ISO 8601
     */
    public function __construct(
        public readonly string $athleteId,
        public readonly string $profileId,
        public readonly string $computedAt,
    ) {
    }
}
