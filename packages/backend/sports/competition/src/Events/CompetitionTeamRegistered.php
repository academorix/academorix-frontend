<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports competition --force`.

declare(strict_types=1);

namespace Academorix\Competition\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New CompetitionTeam row (participant enrolled) — with entry_fee_invoice_id set when kind=paid.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - finance::RaiseEntryFeeInvoice
 *
 * @category Competition
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CompetitionTeamRegistered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $competitionTeamId  ULID (cpt_*)
     * @param  string  $competitionId  ULID
     * @param  string  $teamId  ULID
     * @param  int  $seed  int|null
     */
    public function __construct(
        public readonly string $competitionTeamId,
        public readonly string $competitionId,
        public readonly string $teamId,
        public readonly int $seed,
    ) {
    }
}
