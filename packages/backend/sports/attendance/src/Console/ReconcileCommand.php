<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan attendance:reconcile` — Reconcile the attendance data.
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'attendance:reconcile',
    description: 'Reconcile the attendance data.',
)]
final class ReconcileCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Reconcile the attendance data', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Reconcile the attendance data completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
