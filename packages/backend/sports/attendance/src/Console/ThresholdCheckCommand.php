<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan absence:threshold-check` — Threshold check — absence command.
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'absence:threshold-check',
    description: 'Threshold check — absence command.',
)]
final class ThresholdCheckCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Threshold check — absence command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Threshold check — absence command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
