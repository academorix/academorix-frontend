<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform safeguarding --force`.

declare(strict_types=1);

namespace Academorix\Platform\Safeguarding\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Safeguarding officer opened investigation. status: submitted → investigating. Assigned to investigator.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::NotifyReporter
 * - notifications::NotifyInvestigator
 *
 * @category Safeguarding
 *
 * @since    0.1.0
 */
final readonly class IncidentInvestigationOpened implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $incidentId  ULID
     * @param  string  $openedByUserId  ULID
     * @param  string  $assignedInvestigatorId  ULID
     */
    public function __construct(
        public readonly string $incidentId,
        public readonly string $openedByUserId,
        public readonly string $assignedInvestigatorId,
    ) {
    }
}
