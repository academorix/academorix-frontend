<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform safeguarding --force`.

declare(strict_types=1);

namespace Academorix\Platform\Safeguarding\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. StaffCheck expires within 60 days. Notify staff + branch_manager + admin.
 *
 * ## Consumers
 *
 * - notifications::NotifyStaffCheckExpiring
 * - reporting::UpdateComplianceDashboard
 *
 * @category Safeguarding
 *
 * @since    0.1.0
 */
final readonly class StaffCheckExpiring implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $checkId  ULID
     * @param  string  $staffUserId  ULID
     * @param  string  $kind  string
     * @param  string  $expiresAt  date
     * @param  int  $daysUntilExpiry  int
     */
    public function __construct(
        public readonly string $checkId,
        public readonly string $staffUserId,
        public readonly string $kind,
        public readonly string $expiresAt,
        public readonly int $daysUntilExpiry,
    ) {
    }
}
