<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform safeguarding --force`.

declare(strict_types=1);

namespace Academorix\Platform\Safeguarding\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Renewed check submitted. New expires_at.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::NotifyRenewalConfirmed
 *
 * @category Safeguarding
 *
 * @since    0.1.0
 */
final readonly class StaffCheckRenewed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $oldCheckId  ULID
     * @param  string  $newCheckId  ULID
     * @param  string  $expiresAt  date
     */
    public function __construct(
        public readonly string $oldCheckId,
        public readonly string $newCheckId,
        public readonly string $expiresAt,
    ) {
    }
}
