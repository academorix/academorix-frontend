<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform safeguarding --force`.

declare(strict_types=1);

namespace Academorix\Platform\Safeguarding\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Platform\Safeguarding\Contracts\Data\BackgroundCheckInterface;
use Academorix\Platform\Safeguarding\Database\Factories\BackgroundCheckFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Foundation\Concerns\HasPrefixedUlid;
use Academorix\Staff\Concerns\BelongsToStaff;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a BackgroundCheck.
 *
 * Per-Staff clearance record — DBS / SafeSport / state-BCI / custom.
 *
 * @category Safeguarding
 *
 * @since    0.1.0
 */
#[Table(name: BackgroundCheckInterface::TABLE, keyType: BackgroundCheckInterface::KEY_TYPE)]
#[Fillable([
    BackgroundCheckInterface::ATTR_TENANT_ID,
        BackgroundCheckInterface::ATTR_STAFF_ID,
        BackgroundCheckInterface::ATTR_CHECK_TYPE,
        BackgroundCheckInterface::ATTR_STATUS,
        BackgroundCheckInterface::ATTR_ISSUING_AUTHORITY,
        BackgroundCheckInterface::ATTR_ISSUED_AT,
        BackgroundCheckInterface::ATTR_EXPIRES_AT,
        BackgroundCheckInterface::ATTR_VERIFIED_AT,
        BackgroundCheckInterface::ATTR_VERIFIED_BY_USER_ID,
        BackgroundCheckInterface::ATTR_VERIFICATION_URL,
        BackgroundCheckInterface::ATTR_DOCUMENT_ID,
        BackgroundCheckInterface::ATTR_REFERENCE_NUMBER,
        BackgroundCheckInterface::ATTR_METADATA,
])]
#[UseFactory(BackgroundCheckFactory::class)]
final class BackgroundCheck extends Model implements BackgroundCheckInterface
{
    use HasFactory;
    use HasPrefixedUlid;
    use BelongsToTenant;
    use BelongsToStaff;
    use HasMetadata;
    use HasUserstamps;
    use Auditable;
    use HasActivityLog;
    use Filterable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

}
