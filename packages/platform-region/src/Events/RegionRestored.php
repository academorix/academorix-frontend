<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Academorix\Platform\Region\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Archived Region transitioned back to active. Restoration allowed within the 730-day retention window (financial retention).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Region
 *
 * @since    0.1.0
 */
final readonly class RegionRestored implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $regionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $restoredBy  ULID
     * @param  string  $wasArchivedAt  ISO 8601 timestamp
     */
    public function __construct(
        public readonly string $regionId,
        public readonly string $tenantId,
        public readonly string $restoredBy,
        public readonly string $wasArchivedAt,
    ) {
    }
}
