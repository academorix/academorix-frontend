<?php

declare(strict_types=1);

use Academorix\Platform\Region\Models\Region;

it('is a placeholder — replace with actual Region unit tests', function (): void {
    expect(Region::class)->toBeString();
});
