<?php

/**
 * @file database/migrations/2026_07_15_120002_create_tax_jurisdictions_table.php
 *
 * @description
 * Create the `tax_jurisdictions` table for the `TaxJurisdiction` entity.
 * Owned by the tax module. Emitted from the module blueprint —
 * regenerate via `generate-module.py finance tax --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('tax_jurisdictions', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64)->nullable();
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('country_code', 2);
            $table->string('state_code', 6)->nullable();
            $table->string('city', 128)->nullable();
            $table->string('postal_code_pattern', 32)->nullable();
            $table->string('name', 256);
            $table->string('tax_type', 32);
            $table->string('registration_number', 64)->nullable();
            $table->string('nexus_type', 16)->nullable();
            $table->date('effective_from')->nullable();
            $table->date('effective_to')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 64)->nullable();
            $table->string('updated_by', 64)->nullable();
            $table->string('deleted_by', 64)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'country_code', 'state_code', 'city', 'postal_code_pattern', 'tax_type'], 'tax_jurisdictions_tuple_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['country_code', 'state_code'], 'tax_jurisdictions_geo_idx');
            $table->index(['tenant_id', 'tax_type'], 'tax_jurisdictions_type_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('tax_jurisdictions');
    }
};
