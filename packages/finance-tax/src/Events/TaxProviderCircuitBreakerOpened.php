<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Finance\Tax\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CRITICAL. consecutive_failure_count reached failure_threshold. Sets circuit_breaker_open_until = now() + open_duration_seconds. Blocks all subsequent calculations until the timer elapses. Every fire is P1 — invoice lines are queuing up blocked.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchTaxProviderCircuitBreakerOpenedNotification (cannot-opt-out — pages finance ops)
 * - monitoring::MetricsCollector (increments academorix.tax.circuit_breakers.opened_total)
 *
 * @category Tax
 *
 * @since    0.1.0
 */
final readonly class TaxProviderCircuitBreakerOpened implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $providerConfigId  ULID
     * @param  int  $consecutiveFailureCount  integer
     * @param  string  $openUntil  ISO 8601
     * @param  string  $lastErrorCode  string | null
     * @param  string  $lastErrorMessage  string | null
     * @param  string  $openedAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $providerConfigId,
        public readonly int $consecutiveFailureCount,
        public readonly string $openUntil,
        public readonly string $lastErrorCode,
        public readonly string $lastErrorMessage,
        public readonly string $openedAt,
    ) {
    }
}
