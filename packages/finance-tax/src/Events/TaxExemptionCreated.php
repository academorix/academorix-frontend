<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Finance\Tax\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TaxExemptionObserver.creating committed a new exemption row with verification_status='pending'.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - tax::DispatchVerifyTaxExemptionJob (async cross-check against public registry when available)
 * - monitoring::MetricsCollector
 *
 * @category Tax
 *
 * @since    0.1.0
 */
final readonly class TaxExemptionCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $taxExemptionId  ULID (txe_*)
     * @param  string  $tenantId  ULID
     * @param  string  $customerType  string (User / Athlete / Organization)
     * @param  string  $customerId  ULID
     * @param  string  $exemptionType  string (9 enum values)
     * @param  string  $issuingJurisdictionId  ULID
     * @param  string  $validFrom  date
     * @param  string  $validUntil  date | null
     */
    public function __construct(
        public readonly string $taxExemptionId,
        public readonly string $tenantId,
        public readonly string $customerType,
        public readonly string $customerId,
        public readonly string $exemptionType,
        public readonly string $issuingJurisdictionId,
        public readonly string $validFrom,
        public readonly string $validUntil,
    ) {
    }
}
