<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Actions\Tenant;

use Academorix\Invoice\Contracts\Repositories\InvoiceRepositoryInterface;
use Academorix\Invoice\Data\InvoiceData;
use Academorix\Invoice\Data\Requests\CreateInvoiceRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/invoices` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsAction(name: 'invoice.invoices.create')]
#[Post('/api/v1/invoices')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateInvoiceAction
{
    use AsController;

    public function __construct(
        private readonly InvoiceRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `invoice` from the validated request payload.
     *
     * @param  CreateInvoiceRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateInvoiceRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(InvoiceData::from($model), JsonResponse::HTTP_CREATED);
    }
}
