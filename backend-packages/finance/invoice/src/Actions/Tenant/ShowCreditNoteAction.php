<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Actions\Tenant;

use Academorix\Invoice\Contracts\Repositories\CreditNoteRepositoryInterface;
use Academorix\Invoice\Data\CreditNoteData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Get;

/**
 * `GET /api/v1/credit-notes/{credit_note}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Get(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsController]
#[Get('/api/v1/credit-notes/{credit_note}')]
final class ShowCreditNoteAction
{
    public function __construct(
        private readonly CreditNoteRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `credit-note` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): CreditNoteData
    {
        return CreditNoteData::from($this->repository->findOrFail($id));
    }
}
