<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `reason` on `credit-note`.
 *
 * Enum: refund / adjustment / write_off / duplicate / customer_dispute_resolved / accounting_correction.
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
enum CreditNoteReason: string
{
    use Enum;

    case Refund = 'refund';
    case Adjustment = 'adjustment';
    case WriteOff = 'write_off';
    case Duplicate = 'duplicate';
    case CustomerDisputeResolved = 'customer_dispute_resolved';
    case AccountingCorrection = 'accounting_correction';
}
