<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `item_type` on `invoice-line`.
 *
 * Enum: membership_charge / addon / setup_fee / late_fee / adjustment / one_time_service / tax_line / discount_line.
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
enum InvoiceLineItemType: string
{
    use Enum;

    case MembershipCharge = 'membership_charge';
    case Addon = 'addon';
    case SetupFee = 'setup_fee';
    case LateFee = 'late_fee';
    case Adjustment = 'adjustment';
    case OneTimeService = 'one_time_service';
    case TaxLine = 'tax_line';
    case DiscountLine = 'discount_line';
}
