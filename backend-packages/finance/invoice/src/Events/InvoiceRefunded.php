<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Refund module fired refund.issued for an invoice's payment. InvoiceObserver reflects the state change: status transitions paid → refunded (full refund) OR stays 'paid' (partial refund). A CreditNote is issued in parallel via GenerateCreditNoteJob.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchInvoiceRefundedNotification (customer)
 * - monitoring::MetricsCollector
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
final readonly class InvoiceRefunded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $invoiceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceNumber  string
     * @param  int  $refundAmountCents  integer
     * @param  bool  $isFullRefund  boolean
     * @param  string  $currency  string
     * @param  string  $creditNoteId  ULID (crn_*)
     * @param  string  $refundedAt  ISO 8601
     */
    public function __construct(
        public readonly string $invoiceId,
        public readonly string $tenantId,
        public readonly string $invoiceNumber,
        public readonly int $refundAmountCents,
        public readonly bool $isFullRefund,
        public readonly string $currency,
        public readonly string $creditNoteId,
        public readonly string $refundedAt,
    ) {
    }
}
