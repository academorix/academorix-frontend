<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * InvoiceObserver.creating committed a new invoices row. Fires exactly once per invoice. Precedes any InvoiceLineAdded events.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record per SOX §404)
 * - activity::WriteToActivityLog (staff-visible feed)
 * - monitoring::MetricsCollector (increments academorix.invoice.created_total)
 * - notifications::DispatchInvoiceCreatedNotification (customer receipt-style when auto_send=true, else silent)
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
final readonly class InvoiceCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $invoiceId  ULID (ivc_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $invoiceNumber  string (2026-000001 shape)
     * @param  string  $customerType  string (User / Athlete / Organization)
     * @param  string  $customerId  ULID (polymorphic)
     * @param  string  $membershipId  ULID (mbr_*) | null
     * @param  string  $membershipRenewalId  ULID (mrn_*) | null
     * @param  string  $currency  string (ISO 4217)
     * @param  string  $issueDate  ISO 8601 date
     * @param  string  $dueDate  ISO 8601 date
     * @param  string  $status  'draft'
     * @param  string  $createdByUserId  ULID | null
     * @param  string  $createdAt  ISO 8601
     */
    public function __construct(
        public readonly string $invoiceId,
        public readonly string $tenantId,
        public readonly string $invoiceNumber,
        public readonly string $customerType,
        public readonly string $customerId,
        public readonly string $membershipId,
        public readonly string $membershipRenewalId,
        public readonly string $currency,
        public readonly string $issueDate,
        public readonly string $dueDate,
        public readonly string $status,
        public readonly string $createdByUserId,
        public readonly string $createdAt,
    ) {
    }
}
