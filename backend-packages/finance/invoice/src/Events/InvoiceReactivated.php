<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Rare. Admin manually revives a written-off invoice (e.g. the customer paid after all). Status transitions written_off → paid + records the recovery.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - monitoring::MetricsCollector
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
final readonly class InvoiceReactivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $invoiceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $reactivatedByUserId  ULID
     * @param  string  $reactivatedAt  ISO 8601
     * @param  string  $recoveryNote  string
     */
    public function __construct(
        public readonly string $invoiceId,
        public readonly string $tenantId,
        public readonly string $reactivatedByUserId,
        public readonly string $reactivatedAt,
        public readonly string $recoveryNote,
    ) {
    }
}
