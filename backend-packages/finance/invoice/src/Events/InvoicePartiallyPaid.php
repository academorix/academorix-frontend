<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * InvoiceObserver.updating detected 0 < amount_paid_cents < total_cents. Status transitions to 'partially_paid'. Non-terminal — the invoice can still reach 'paid' with future payments.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - monitoring::MetricsCollector
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
final readonly class InvoicePartiallyPaid implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $invoiceId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $amountPaidCents  integer
     * @param  int  $amountDueCents  integer
     * @param  int  $totalCents  integer
     * @param  string  $currency  string
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $invoiceId,
        public readonly string $tenantId,
        public readonly int $amountPaidCents,
        public readonly int $amountDueCents,
        public readonly int $totalCents,
        public readonly string $currency,
        public readonly string $at,
    ) {
    }
}
