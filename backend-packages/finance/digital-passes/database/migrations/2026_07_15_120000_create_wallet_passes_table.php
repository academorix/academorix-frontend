<?php

/**
 * @file database/migrations/2026_07_15_120000_create_wallet_passes_table.php
 *
 * @description
 * Create the `wallet_passes` table for the `WalletPass` entity.
 * Owned by the digital-passes module. Every column + index reference
 * goes through {@see Academorix\DigitalPasses\Contracts\Data\WalletPassInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance digital-passes --force`.

declare(strict_types=1);

use Academorix\DigitalPasses\Contracts\Data\WalletPassInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(WalletPassInterface::TABLE, function (Blueprint $table): void {
            $table->string(WalletPassInterface::ATTR_ID, 30)->primary();
            $table->string(WalletPassInterface::ATTR_TENANT_ID, 30);
            $table->foreign(WalletPassInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(WalletPassInterface::ATTR_HOLDER_TYPE, 32);
            $table->string(WalletPassInterface::ATTR_HOLDER_ID, 30);
            $table->string(WalletPassInterface::ATTR_MEMBERSHIP_ID, 30)->nullable();
            $table->foreign(WalletPassInterface::ATTR_MEMBERSHIP_ID)->references('id')->on('memberships')->onDelete('cascade');
            $table->string(WalletPassInterface::ATTR_KIND, 24);
            $table->string(WalletPassInterface::ATTR_PROVIDER, 16);
            $table->string(WalletPassInterface::ATTR_SERIAL, 128);
            $table->string(WalletPassInterface::ATTR_STATUS, 16)->default('issued');
            $table->timestampTz(WalletPassInterface::ATTR_INSTALLED_AT)->nullable();
            $table->string(WalletPassInterface::ATTR_DEVICE_ID, 200)->nullable();
            $table->string(WalletPassInterface::ATTR_PUSH_TOKEN, 500)->nullable();
            $table->timestampTz(WalletPassInterface::ATTR_EXPIRES_AT)->nullable();
            $table->timestampTz(WalletPassInterface::ATTR_REVOKED_AT)->nullable();
            $table->string(WalletPassInterface::ATTR_REVOKED_REASON, 200)->nullable();
            $table->text(WalletPassInterface::ATTR_QR_PAYLOAD)->nullable();
            $table->string(WalletPassInterface::ATTR_PASS_DOCUMENT_ID, 30)->nullable();
            $table->foreign(WalletPassInterface::ATTR_PASS_DOCUMENT_ID)->references('id')->on('files')->onDelete('set null');
            $table->jsonb(WalletPassInterface::ATTR_METADATA)->nullable();
            $table->timestampTz(WalletPassInterface::ATTR_CREATED_AT);
            $table->timestampTz(WalletPassInterface::ATTR_UPDATED_AT);
            $table->timestampTz(WalletPassInterface::ATTR_DELETED_AT)->nullable();
            $table->index([WalletPassInterface::ATTR_TENANT_ID, WalletPassInterface::ATTR_HOLDER_TYPE, WalletPassInterface::ATTR_HOLDER_ID], 'wallet_passes_holder_idx');
        });

        DB::statement('CREATE UNIQUE INDEX wallet_passes_serial_unique ON ' . WalletPassInterface::TABLE . ' (' . WalletPassInterface::ATTR_TENANT_ID . ', ' . WalletPassInterface::ATTR_SERIAL . ') WHERE deleted_at IS NULL');
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(WalletPassInterface::TABLE);
    }
};
