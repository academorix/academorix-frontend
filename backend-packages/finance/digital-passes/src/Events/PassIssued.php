<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance digital-passes --force`.

declare(strict_types=1);

namespace Academorix\DigitalPasses\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New WalletPass row. Provider selected (apple / google / generic_qr). Signed JWT QR payload minted.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::NotifyPassIssued
 *
 * @category DigitalPasses
 *
 * @since    0.1.0
 */
final readonly class PassIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $walletPassId  ULID (wpp_*)
     * @param  string  $tenantId  ULID
     * @param  string  $holderId  ULID
     * @param  string  $provider  'apple'|'google'|'generic_qr'
     * @param  string  $kind  'membership_card'|'event_ticket'|'day_pass_card'
     * @param  string  $serial  string
     */
    public function __construct(
        public readonly string $walletPassId,
        public readonly string $tenantId,
        public readonly string $holderId,
        public readonly string $provider,
        public readonly string $kind,
        public readonly string $serial,
    ) {
    }
}
