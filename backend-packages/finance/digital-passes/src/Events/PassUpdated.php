<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance digital-passes --force`.

declare(strict_types=1);

namespace Academorix\DigitalPasses\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Pass re-minted (membership renewal, branding change). APNs/FCM push dispatched.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - jobs::PushWalletUpdateJob
 *
 * @category DigitalPasses
 *
 * @since    0.1.0
 */
final readonly class PassUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $walletPassId  ULID
     * @param  string  $reason  'membership_renewed'|'branding_changed'|'holder_data_changed'|'manual'
     */
    public function __construct(
        public readonly string $walletPassId,
        public readonly string $reason,
    ) {
    }
}
