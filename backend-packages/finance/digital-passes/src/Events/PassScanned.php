<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance digital-passes --force`.

declare(strict_types=1);

namespace Academorix\DigitalPasses\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * QR scanned at credentials gate. Forwarded to attendance module.
 *
 * ## Consumers
 *
 * - credentials::VerifyPassJwt
 * - attendance::LogPassScan
 * - activity::WriteToActivityLog
 *
 * @category DigitalPasses
 *
 * @since    0.1.0
 */
final readonly class PassScanned implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $walletPassId  ULID
     * @param  string  $gateId  ULID
     * @param  string  $scannedAt  ISO 8601
     */
    public function __construct(
        public readonly string $walletPassId,
        public readonly string $gateId,
        public readonly string $scannedAt,
    ) {
    }
}
