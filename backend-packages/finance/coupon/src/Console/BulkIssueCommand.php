<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan coupon:bulk-issue` — Bulk issue — coupon command.
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'coupon:bulk-issue',
    description: 'Bulk issue — coupon command.',
)]
final class BulkIssueCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Bulk issue — coupon command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Bulk issue — coupon command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
