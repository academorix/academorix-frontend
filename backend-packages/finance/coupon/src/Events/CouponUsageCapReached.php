<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Coupon.usage_count reached Coupon.usage_cap. Fires from CouponObserver.updated when the increment crosses the threshold. P2 signal — the campaign ran out.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchCouponUsageCapReachedNotification (owner + marketing role)
 * - observability::MetricsCollector (academorix.coupons.usage_cap_reached_total)
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
final readonly class CouponUsageCapReached implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $couponId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $code  string
     * @param  int  $usageCap  integer
     * @param  string  $issuanceSource  string
     * @param  string  $reachedAt  ISO 8601
     */
    public function __construct(
        public readonly string $couponId,
        public readonly string $tenantId,
        public readonly string $code,
        public readonly int $usageCap,
        public readonly string $issuanceSource,
        public readonly string $reachedAt,
    ) {
    }
}
