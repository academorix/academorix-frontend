<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CouponRepository committed an update to a coupon (name, description, valid_until, usage_cap, per_customer_limit, is_stackable, applicable_plan_ids). NOT fired for `discount_type` / `discount_amount` / `discount_currency` mutations — those refuse post-redemption via observer.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateCouponCache
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
final readonly class CouponUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $couponId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedFields  string[]
     * @param  string  $updatedBy  ULID
     */
    public function __construct(
        public readonly string $couponId,
        public readonly string $tenantId,
        public readonly string $changedFields,
        public readonly string $updatedBy,
    ) {
    }
}
