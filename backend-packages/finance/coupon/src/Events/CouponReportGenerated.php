<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * GenerateCouponUsageReportJob finished — the weekly per-tenant report is stored + available for download.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchWeeklyCouponReportNotification (owner + marketing role — opt-in)
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
final readonly class CouponReportGenerated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $reportId  ULID
     * @param  string  $periodStart  ISO 8601
     * @param  string  $periodEnd  ISO 8601
     * @param  string  $reportUrl  string (signed S3 URL, 7d TTL)
     * @param  int  $couponCount  integer
     * @param  int  $redemptionCount  integer
     * @param  int  $totalDiscountCents  integer
     * @param  int  $clawbackCount  integer
     * @param  string  $generatedAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $reportId,
        public readonly string $periodStart,
        public readonly string $periodEnd,
        public readonly string $reportUrl,
        public readonly int $couponCount,
        public readonly int $redemptionCount,
        public readonly int $totalDiscountCents,
        public readonly int $clawbackCount,
        public readonly string $generatedAt,
    ) {
    }
}
