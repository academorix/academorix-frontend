<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CouponRepository finished a successful create. Fires exactly once per (tenant_id, code) tuple. Distinct from `CouponIssuedManually` / `CouponIssuedByReferralProgram` — those carry issuance-context; this fires unconditionally.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial-audit)
 * - activity::WriteToActivityLog
 * - cache::InvalidateTenantCouponListCache
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
final readonly class CouponCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $couponId  ULID (cpn_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $code  string
     * @param  string  $name  string
     * @param  string  $discountType  'percent' | 'fixed_amount' | 'free_period' | 'first_month_free' | 'bogo' | 'free_shipping'
     * @param  string  $discountAmount  number | null
     * @param  string  $discountCurrency  string | null (ISO 4217)
     * @param  string  $applicability  'any' | 'membership_only' | 'specific_membership_plans' | 'minimum_order_value'
     * @param  string  $issuanceSource  'manual' | 'referral' | 'marketing_campaign' | 'import' | 'api'
     * @param  string  $validFrom  ISO 8601
     * @param  string  $validUntil  ISO 8601 | null
     * @param  int  $usageCap  integer | null
     * @param  int  $perCustomerLimit  integer
     * @param  bool  $isStackable  boolean
     * @param  string  $createdBy  ULID (usr_*)
     */
    public function __construct(
        public readonly string $couponId,
        public readonly string $tenantId,
        public readonly string $code,
        public readonly string $name,
        public readonly string $discountType,
        public readonly string $discountAmount,
        public readonly string $discountCurrency,
        public readonly string $applicability,
        public readonly string $issuanceSource,
        public readonly string $validFrom,
        public readonly string $validUntil,
        public readonly int $usageCap,
        public readonly int $perCustomerLimit,
        public readonly bool $isStackable,
        public readonly string $createdBy,
    ) {
    }
}
