<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Admin manually reversed a redemption via POST /coupons/redemptions/{redemption}/reverse (correction path — not a refund-cascade). Distinct from CouponClawbackCompleted which flows from a Finance refund.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchCouponClawbackNotification (customer + admin)
 * - finance::AdjustInvoiceLineDiscount (Wave 4 — the invoice module reads this and adjusts revenue-recognition)
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CouponRedemptionReversed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $redemptionId  ULID
     * @param  string  $couponId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $reversedByUserId  ULID
     * @param  string  $reason  string (admin-provided)
     * @param  int  $reversedAmountCents  integer
     * @param  string  $reversedAt  ISO 8601
     */
    public function __construct(
        public readonly string $redemptionId,
        public readonly string $couponId,
        public readonly string $tenantId,
        public readonly string $reversedByUserId,
        public readonly string $reason,
        public readonly int $reversedAmountCents,
        public readonly string $reversedAt,
    ) {
    }
}
