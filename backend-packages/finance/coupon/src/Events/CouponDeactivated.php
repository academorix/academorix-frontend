<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Coupon.is_active transitioned true -> false. Causes: admin manual, usage_cap_reached auto-deactivate, applicable_plan_archived cascade, referral program archived cascade.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchCouponDeactivatedInternalNotification (owner + marketing role)
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
final readonly class CouponDeactivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $couponId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $deactivatedAt  ISO 8601
     * @param  string  $deactivatedByUserId  ULID | null (null = system cascade)
     * @param  string  $reason  'manual' | 'usage_cap_reached' | 'applicable_plan_archived' | 'referral_program_archived' | 'expired_via_job'
     */
    public function __construct(
        public readonly string $couponId,
        public readonly string $tenantId,
        public readonly string $deactivatedAt,
        public readonly string $deactivatedByUserId,
        public readonly string $reason,
    ) {
    }
}
