<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payout --force`.

declare(strict_types=1);

namespace Academorix\Payout\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Payout.status transitions -> failed. Provider rejected the payout.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchPayoutFailedNotificationListener
 * - monitoring::MetricsCollector
 *
 * @category Payout
 *
 * @since    0.1.0
 */
final readonly class PayoutFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $payoutId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $reason  string
     * @param  string  $failedAt  ISO 8601
     */
    public function __construct(
        public readonly string $payoutId,
        public readonly string $tenantId,
        public readonly string $reason,
        public readonly string $failedAt,
    ) {
    }
}
