<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payout --force`.

declare(strict_types=1);

namespace Academorix\Payout\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Reconciliation found drift_minor != 0. INCIDENT SIGNAL.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchPayoutDriftNotificationListener
 * - monitoring::MetricsCollector
 *
 * @category Payout
 *
 * @since    0.1.0
 */
final readonly class PayoutReconciliationDrift implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $payoutId  ULID
     * @param  string  $reconciliationId  ULID
     * @param  int  $driftMinor  integer
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $payoutId,
        public readonly string $reconciliationId,
        public readonly int $driftMinor,
        public readonly string $at,
    ) {
    }
}
