<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payout --force`.

declare(strict_types=1);

namespace Academorix\Payout\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `provider` on `payout`.
 *
 * Enum: stripe / paddle / checkout_com / square / razorpay.
 *
 * @category Payout
 *
 * @since    0.1.0
 */
enum PayoutProvider: string
{
    use Enum;

    case Stripe = 'stripe';
    case Paddle = 'paddle';
    case CheckoutCom = 'checkout_com';
    case Square = 'square';
    case Razorpay = 'razorpay';
}
