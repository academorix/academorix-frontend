<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * PaymentUserSettings — My payment preferences.
 *
 * Per-user preferences for payment notifications.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'payment_user',
    label: 'My payment preferences',
    description: 'Per-user preferences for payment notifications.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 45,
)]
class PaymentUserSettings extends Settings
{

    // ── Payment notifications ──────────────────────────────────
    #[SettingGroup(label: 'Payment notifications', icon: 'bell', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Send me payment receipts by email', group: 'Payment notifications', sortOrder: 1)]
    public bool $receive_payment_receipts = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me when my saved cards are about to expire', group: 'Payment notifications', sortOrder: 2)]
    public bool $receive_method_expiring_alerts = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'payment_user';
    }
}
