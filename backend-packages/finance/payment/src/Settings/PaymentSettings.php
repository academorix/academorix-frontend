<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * PaymentSettings — Payment processing.
 *
 * Tenant-wide defaults for payment captures + provider routing.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'payment',
    label: 'Payment processing',
    description: 'Tenant-wide defaults for payment captures + provider routing.',
    icon: 'credit-card',
    scope: 'tenant',
    permission: 'payment.providers.viewAny',
    sortOrder: 50,
)]
class PaymentSettings extends Settings
{

    // ── Provider defaults ──────────────────────────────────
    #[SettingGroup(label: 'Provider defaults', description: 'Which provider handles new payment intents when the caller doesn\'t specify.', icon: 'cog-6-tooth', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Default provider', group: 'Provider defaults', sortOrder: 1, helpText: 'Fallback provider when the caller doesn\'t specify one at intent creation.')]
    public string $default_provider = 'stripe';

    #[SettingField(controlType: ControlType::Text, label: 'Regional routing', group: 'Provider defaults', sortOrder: 2, helpText: 'Route customers from specific country codes to specific providers. Example: {US: stripe, EU: adyen, GB: adyen}. Overrides default_provider.')]
    public array $regional_provider_routing = [];


    // ── 3D Secure 2 (SCA) ──────────────────────────────────
    #[SettingGroup(label: '3D Secure 2 (SCA)', description: '3DS2 authentication settings for card transactions.', icon: 'shield-check', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Number, label: '3DS2 required over amount', group: '3D Secure 2 (SCA)', sortOrder: 1, min: 0, max: 100000, helpText: 'Card transactions above this amount require 3DS2. Default €30 (3000 cents) matches PSD2 SCA threshold. In EU regions the PSD2 threshold is legally mandated; you can only INCREASE the requirement (lower threshold), not decrease it.')]
    public int $sca_threshold_cents = 3000;

    #[SettingField(controlType: ControlType::Toggle, label: 'Force 3DS2 on all card transactions', group: '3D Secure 2 (SCA)', sortOrder: 2, helpText: 'Enterprise-only. Requires 3DS2 on every card transaction regardless of amount. Increases friction but reduces fraud risk.')]
    public bool $force_3ds2 = false;


    // ── Capture behavior ──────────────────────────────────
    #[SettingGroup(label: 'Capture behavior', description: 'How captures are handled.', icon: 'arrow-down-tray', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Select, label: 'Default capture method', group: 'Capture behavior', sortOrder: 1, options: ['automatic' => 'Automatic (capture at authorization)', 'manual' => 'Manual (authorization hold; capture later)'], helpText: 'Manual capture holds funds without transferring them. Useful for pre-authorized bookings. Requires payment_manual_capture entitlement.')]
    public string $default_capture_method = 'automatic';

    #[SettingField(controlType: ControlType::Number, label: 'Intent timeout (hours)', group: 'Capture behavior', sortOrder: 2, min: 1, max: 168, helpText: 'Payment intents in non-terminal status for longer than this are automatically cancelled.')]
    public int $intent_timeout_hours = 24;


    // ── Fraud & risk ──────────────────────────────────
    #[SettingGroup(label: 'Fraud & risk', description: 'Fraud detection settings.', icon: 'exclamation-triangle', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Number, label: 'Auto-approve risk score threshold', group: 'Fraud & risk', sortOrder: 1, min: 0, max: 100, helpText: 'Payments with provider risk score below this threshold auto-approve. Above → route to review queue.')]
    public int $auto_approve_risk_threshold = 50;


    // ── Dispute management ──────────────────────────────────
    #[SettingGroup(label: 'Dispute management', description: 'Dispute handling defaults.', icon: 'scale', sortOrder: 5)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Auto-collect dispute evidence', group: 'Dispute management', sortOrder: 1, helpText: 'When a dispute opens, automatically collect supporting evidence (invoice, membership, communication log, refund policy) into an evidence pack.')]
    public bool $auto_collect_evidence = true;

    #[SettingField(controlType: ControlType::Number, label: 'High-value dispute threshold', group: 'Dispute management', sortOrder: 2, min: 1000, max: 10000000, helpText: 'Disputes above this amount fire as P1 notifications. Default $500.')]
    public int $high_value_dispute_threshold_cents = 50000;

    #[SettingField(controlType: ControlType::Text, label: 'Notify these users on new disputes', group: 'Dispute management', sortOrder: 3, helpText: 'Every user listed receives PaymentDisputeOpenedNotification. Tenant owners always notified regardless.')]
    public array $dispute_notify_users = [];


    // ── Notifications routing ──────────────────────────────────
    #[SettingGroup(label: 'Notifications routing', description: 'Where circuit-breaker alerts + reconcile drift alerts are delivered.', icon: 'bell-alert', sortOrder: 6)]
    #[SettingField(controlType: ControlType::Text, label: 'Notify these users on circuit-breaker opens', group: 'Notifications routing', sortOrder: 1)]
    public array $circuit_breaker_notify_users = [];

    #[SettingField(controlType: ControlType::Text, label: 'Notify these users on reconcile drift', group: 'Notifications routing', sortOrder: 2)]
    public array $reconcile_drift_notify_users = [];

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'payment';
    }
}
