<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Services;

use Academorix\Payment\Contracts\Services\FraudCheckerInterface;
use Illuminate\Container\Attributes\Scoped;
use Academorix\Payment\Contracts\Repositories\PaymentDisputeRepositoryInterface;

/**
 * Concrete service — FraudChecker.
 *
 * Implements {@see FraudCheckerInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[Scoped]
final class FraudChecker implements FraudCheckerInterface
{
    /**
     * @param  PaymentDisputeRepositoryInterface  $paymentDisputeRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly PaymentDisputeRepositoryInterface $paymentDisputeRepository,
    ) {
    }
}
