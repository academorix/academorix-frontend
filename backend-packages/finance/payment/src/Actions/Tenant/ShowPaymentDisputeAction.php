<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Actions\Tenant;

use Academorix\Payment\Contracts\Repositories\PaymentDisputeRepositoryInterface;
use Academorix\Payment\Data\PaymentDisputeData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Get;

/**
 * `GET /api/v1/payment-disputes/{dispute}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsAction(name: 'payment.payment_disputes.show')]
#[Get('/api/v1/payment-disputes/{dispute}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowPaymentDisputeAction
{
    use AsController;

    public function __construct(
        private readonly PaymentDisputeRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `payment-dispute` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): PaymentDisputeData
    {
        return PaymentDisputeData::from($this->repository->findOrFail($id));
    }
}
