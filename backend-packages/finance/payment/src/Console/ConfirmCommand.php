<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan payment-intent:confirm` — Confirm — payment intent command.
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'payment-intent:confirm',
    description: 'Confirm — payment intent command.',
)]
final class ConfirmCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Confirm — payment intent command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Confirm — payment intent command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
