<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Status transitioned to 'requires_action'. Customer must complete a step-up flow (typically 3DS2 challenge). next_action_type + next_action_url populated.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchPaymentActionRequiredNotification (customer — SMS + email + push)
 * - monitoring::MetricsCollector
 *
 * @category Payment
 *
 * @since    0.1.0
 */
final readonly class PaymentIntentRequiresAction implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $paymentIntentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $nextActionType  string
     * @param  string  $nextActionUrl  string | null
     * @param  int  $amountCents  integer
     * @param  string  $currency  string
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $paymentIntentId,
        public readonly string $tenantId,
        public readonly string $nextActionType,
        public readonly string $nextActionUrl,
        public readonly int $amountCents,
        public readonly string $currency,
        public readonly string $at,
    ) {
    }
}
