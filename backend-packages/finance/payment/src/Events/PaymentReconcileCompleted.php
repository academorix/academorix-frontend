<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReconcilePaymentIntentsJob completed a reconciliation run. Payload carries drift counts.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchPaymentReconcileDriftNotification (admin — when drift_count > 0)
 * - monitoring::MetricsCollector
 *
 * @category Payment
 *
 * @since    0.1.0
 */
final readonly class PaymentReconcileCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  int  $intentsPolled  integer
     * @param  int  $driftCount  integer
     * @param  int  $reconciledCount  integer
     * @param  string  $runAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly int $intentsPolled,
        public readonly int $driftCount,
        public readonly int $reconciledCount,
        public readonly string $runAt,
    ) {
    }
}
