<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Status transitioned to 'succeeded'. Money captured. A Payment row was atomically created in the same DB transaction — this event fires after that transaction commits. Precedes PaymentCaptured (which fires from the Payment side).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - monitoring::MetricsCollector (increments academorix.payment.intents.succeeded_total)
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PaymentIntentSucceeded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $paymentIntentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $paymentId  ULID (pay_*)
     * @param  int  $amountCents  integer
     * @param  string  $currency  string
     * @param  string  $succeededAt  ISO 8601
     */
    public function __construct(
        public readonly string $paymentIntentId,
        public readonly string $tenantId,
        public readonly string $paymentId,
        public readonly int $amountCents,
        public readonly string $currency,
        public readonly string $succeededAt,
    ) {
    }
}
