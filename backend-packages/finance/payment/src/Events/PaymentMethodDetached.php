<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PaymentMethodObserver.deleted (soft-delete) OR is_active transitioned false. Provider-side detach dispatched (Stripe DetachPaymentMethod, Adyen storedPaymentMethod delete).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector
 *
 * @category Payment
 *
 * @since    0.1.0
 */
final readonly class PaymentMethodDetached implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $paymentMethodId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $customerType  string
     * @param  string  $customerId  ULID
     * @param  string  $provider  string
     * @param  string  $deactivatedReason  string
     * @param  string  $detachedAt  ISO 8601
     */
    public function __construct(
        public readonly string $paymentMethodId,
        public readonly string $tenantId,
        public readonly string $customerType,
        public readonly string $customerId,
        public readonly string $provider,
        public readonly string $deactivatedReason,
        public readonly string $detachedAt,
    ) {
    }
}
