<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CRITICAL. N consecutive failures against a provider triggered the circuit breaker. Every fire is a P1 signal — captures against this provider are paused until the breaker resets.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchPaymentProviderCircuitBreakerOpenedNotification (cannot-opt-out — pages ops + notifies tenant owner)
 * - monitoring::MetricsCollector (increments academorix.payment.circuit_breakers.opened_total)
 *
 * @category Payment
 *
 * @since    0.1.0
 */
final readonly class PaymentProviderCircuitBreakerOpened implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  int  $consecutiveFailureCount  integer
     * @param  string  $openUntil  ISO 8601
     * @param  string  $lastErrorCode  string | null
     * @param  string  $openedAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly int $consecutiveFailureCount,
        public readonly string $openUntil,
        public readonly string $lastErrorCode,
        public readonly string $openedAt,
    ) {
    }
}
