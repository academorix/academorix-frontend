<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PaymentMethodObserver.created — customer saved a payment method (SetupIntent flow or first payment with save=true).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - notifications::DispatchPaymentMethodAttachedNotification (customer — confirmation)
 * - monitoring::MetricsCollector
 *
 * @category Payment
 *
 * @since    0.1.0
 */
final readonly class PaymentMethodAttached implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $paymentMethodId  ULID (pmt_*)
     * @param  string  $tenantId  ULID
     * @param  string  $customerType  string
     * @param  string  $customerId  ULID
     * @param  string  $provider  string
     * @param  string  $methodType  string
     * @param  string  $brand  string | null
     * @param  string  $last4  string | null
     * @param  string  $attachedAt  ISO 8601
     */
    public function __construct(
        public readonly string $paymentMethodId,
        public readonly string $tenantId,
        public readonly string $customerType,
        public readonly string $customerId,
        public readonly string $provider,
        public readonly string $methodType,
        public readonly string $brand,
        public readonly string $last4,
        public readonly string $attachedAt,
    ) {
    }
}
