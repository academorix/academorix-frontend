<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `capture_method` on `payment-intent`.
 *
 * Enum: automatic (capture on authorization — default) / manual (capture later via POST /payments/{payment}/capture — requires payment_manual_capture entitlement, Medium+).
 *
 * @category Payment
 *
 * @since    0.1.0
 */
enum PaymentIntentCaptureMethod: string
{
    use Enum;

    case Automatic = 'automatic';
    case Manual = 'manual';
}
