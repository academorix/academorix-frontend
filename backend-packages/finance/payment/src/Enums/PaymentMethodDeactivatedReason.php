<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `deactivated_reason` on `payment-method`.
 *
 * Enum: customer_removed / card_expired / provider_failure / duplicate / fraud_flag.
 *
 * @category Payment
 *
 * @since    0.1.0
 */
enum PaymentMethodDeactivatedReason: string
{
    use Enum;

    case CustomerRemoved = 'customer_removed';
    case CardExpired = 'card_expired';
    case ProviderFailure = 'provider_failure';
    case Duplicate = 'duplicate';
    case FraudFlag = 'fraud_flag';
}
