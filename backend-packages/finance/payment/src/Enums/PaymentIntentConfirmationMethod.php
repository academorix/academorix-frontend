<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `confirmation_method` on `payment-intent`.
 *
 * Enum: automatic (provider auto-confirms after method attach) / manual (customer explicitly confirms via SDK).
 *
 * @category Payment
 *
 * @since    0.1.0
 */
enum PaymentIntentConfirmationMethod: string
{
    use Enum;

    case Automatic = 'automatic';
    case Manual = 'manual';
}
