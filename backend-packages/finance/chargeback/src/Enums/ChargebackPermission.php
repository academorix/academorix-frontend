<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance chargeback --force`.

declare(strict_types=1);

namespace Academorix\Chargeback\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Chargeback module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Chargeback
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum ChargebackPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `chargebacks.viewAny` — Chargebacks ViewAny
     */
    #[Label('Chargebacks ViewAny')]
    #[Description('Chargebacks ViewAny')]
    case ChargebacksViewAny = 'chargebacks.viewAny';

    /**
     * `chargebacks.view` — Chargebacks View
     */
    #[Label('Chargebacks View')]
    #[Description('Chargebacks View')]
    case ChargebacksView = 'chargebacks.view';

    /**
     * `chargebacks.view-evidence` — View chargeback evidence bundles.
     */
    #[Label('View chargeback evidence bundles')]
    #[Description('View chargeback evidence bundles. Evidence contains customer PII (IP, device fingerprint hash, service attendance, communication logs) — segregated permission from view. Some support roles need chargeback context but not evidence drill-down.')]
    case ChargebacksViewEvidence = 'chargebacks.view-evidence';

    /**
     * `chargebacks.submit-evidence` — Submit evidence to the acquiring bank.
     */
    #[Label('Submit evidence to the acquiring bank')]
    #[Description('Submit evidence to the acquiring bank. Requires `chargeback_evidence_submission` entitlement. Support role does NOT hold this permission — evidence submission is finance-tier only.')]
    case ChargebacksSubmitEvidence = 'chargebacks.submit-evidence';

    /**
     * `chargebacks.accept` — RDR path — auto-refund below threshold to close pre-chargeback state.
     */
    #[Label('RDR path — auto-refund below threshold to close pre-chargeback state')]
    #[Description('RDR path — auto-refund below threshold to close pre-chargeback state. Requires `chargeback_rdr` entitlement (Medium+). Only valid when parent chargeback.rdr_eligible=true.')]
    case ChargebacksAccept = 'chargebacks.accept';

    /**
     * `chargebacks.mark-lost` — Admin escape hatch — force-mark a chargeback as lost after ops investigation.
     */
    #[Label('Admin escape hatch — force-mark a chargeback as lost after ops investigation')]
    #[Description('Admin escape hatch — force-mark a chargeback as lost after ops investigation. High-privilege — only admin + owner (not finance).')]
    case ChargebacksMarkLost = 'chargebacks.mark-lost';

    /**
     * `chargebacks.reconcile` — Dispatches CheckChargebackStatusJob.
     */
    #[Label('Dispatches CheckChargebackStatusJob')]
    #[Description('Dispatches CheckChargebackStatusJob. Sweeps stuck chargebacks after provider outage recovery.')]
    case ChargebacksReconcile = 'chargebacks.reconcile';

    /**
     * `chargebacks.view-report` — Read the chargeback rate report.
     */
    #[Label('Read the chargeback rate report')]
    #[Description('Read the chargeback rate report. Marketing role holds for fraud model tuning (chargeback rate + reason distribution feed ad-network fraud model retraining tuning).')]
    case ChargebacksViewReport = 'chargebacks.view-report';

    /**
     * `chargebacks.view-financials` — Read the confidential financial fields: amount_cents, currency, fee_cents, provider_metadata, provider_reference_id.
     */
    #[Label('Read the confidential financial fields: amount_cents, currency, fee_cents, provi')]
    #[Description('Read the confidential financial fields: amount_cents, currency, fee_cents, provider_metadata, provider_reference_id. Redactor strips these when the caller lacks this permission.')]
    case ChargebacksViewFinancials = 'chargebacks.view-financials';

    /**
     * `platform.chargebacks.viewAny` — Platform Chargebacks ViewAny
     */
    #[Label('Platform Chargebacks ViewAny')]
    #[Description('Platform Chargebacks ViewAny')]
    case PlatformChargebacksViewAny = 'platform.chargebacks.viewAny';

    /**
     * `platform.chargebacks.view` — Platform Chargebacks View
     */
    #[Label('Platform Chargebacks View')]
    #[Description('Platform Chargebacks View')]
    case PlatformChargebacksView = 'platform.chargebacks.view';

    /**
     * `platform.chargebacks.view-rate-alerts` — Cross-tenant merchant-account risk monitoring — surfaces every tenant approaching VDMP/MDMP thresholds.
     */
    #[Label('Cross-tenant merchant-account risk monitoring — surfaces every tenant approachin')]
    #[Description('Cross-tenant merchant-account risk monitoring — surfaces every tenant approaching VDMP/MDMP thresholds. Actionable for Academorix ops (fraud review recommendations, RDR enrollment suggestions, in extreme cases account termination).')]
    case PlatformChargebacksViewRateAlerts = 'platform.chargebacks.view-rate-alerts';

    /**
     * `platform.chargebacks.pii.view` — Cross-tenant view WITH customer PII (customer_id / customer name / email unmasked + full evidence bundles including IP + device fingerprint).
     */
    #[Label('Cross-tenant view WITH customer PII (customer_id / customer name / email unmaske')]
    #[Description('Cross-tenant view WITH customer PII (customer_id / customer name / email unmasked + full evidence bundles including IP + device fingerprint). Reserved for regulator-facing investigations. Default view hashes PII.')]
    case PlatformChargebacksPiiView = 'platform.chargebacks.pii.view';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::ChargebacksViewAny => Guard::Sanctum,
            self::ChargebacksView => Guard::Sanctum,
            self::ChargebacksViewEvidence => Guard::Sanctum,
            self::ChargebacksSubmitEvidence => Guard::Sanctum,
            self::ChargebacksAccept => Guard::Sanctum,
            self::ChargebacksMarkLost => Guard::Sanctum,
            self::ChargebacksReconcile => Guard::Sanctum,
            self::ChargebacksViewReport => Guard::Sanctum,
            self::ChargebacksViewFinancials => Guard::Sanctum,
            self::PlatformChargebacksViewAny => Guard::PlatformAdmin,
            self::PlatformChargebacksView => Guard::PlatformAdmin,
            self::PlatformChargebacksViewRateAlerts => Guard::PlatformAdmin,
            self::PlatformChargebacksPiiView => Guard::PlatformAdmin,
        };
    }
}
