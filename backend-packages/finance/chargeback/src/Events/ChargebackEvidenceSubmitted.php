<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance chargeback --force`.

declare(strict_types=1);

namespace Academorix\Chargeback\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ChargebackEvidenceRepository finished a successful create. Fires per submission attempt. Distinct from ChargebackFiled — this fires each time the merchant submits (or resubmits) evidence.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchChargebackEvidenceSubmittedNotification (admin — confirmation)
 * - observability::MetricsCollector (academorix.chargeback.evidence_submissions_total labelled by submission_attempt_number)
 *
 * @category Chargeback
 *
 * @since    0.1.0
 */
final readonly class ChargebackEvidenceSubmitted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $chargebackEvidenceId  ULID (cev_*)
     * @param  string  $chargebackId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $submissionAttemptNumber  integer
     * @param  string  $submittedByUserId  ULID (usr_*)
     * @param  string  $submittedAt  ISO 8601
     * @param  int  $evidenceShapeCompletenessPercent  integer 0-100 (how many of the 8 evidence fields are populated)
     */
    public function __construct(
        public readonly string $chargebackEvidenceId,
        public readonly string $chargebackId,
        public readonly string $tenantId,
        public readonly int $submissionAttemptNumber,
        public readonly string $submittedByUserId,
        public readonly string $submittedAt,
        public readonly int $evidenceShapeCompletenessPercent,
    ) {
    }
}
