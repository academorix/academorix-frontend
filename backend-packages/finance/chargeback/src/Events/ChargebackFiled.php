<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance chargeback --force`.

declare(strict_types=1);

namespace Academorix\Chargeback\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ChargebackRepository finished a successful create. Fires exactly once per chargeback. P0 signal — money has been forcibly reversed by the bank. Distinct from a customer-initiated PaymentDispute (which fires payment::PaymentDisputeFiled) — this fires when the bank ESCALATES to a full chargeback.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial-audit)
 * - activity::WriteToActivityLog
 * - cache::InvalidateTenantChargebackListCache
 * - notifications::DispatchChargebackFiledNotification (P0 admin — immediate SMS + push + email)
 * - chargeback::CollectChargebackEvidenceJob (auto-collect evidence)
 * - observability::MetricsCollector (academorix.chargeback.filed_total labelled by network + reason_category)
 *
 * @category Chargeback
 *
 * @since    0.1.0
 */
final readonly class ChargebackFiled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $chargebackId  ULID (cbk_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $chargebackNumber  string (CBK-2026-00000001)
     * @param  string  $paymentId  ULID (pay_*)
     * @param  string  $invoiceId  ULID (ivc_*)
     * @param  string  $paymentDisputeId  ULID (pdi_*) | null (null when originated directly via webhook, not escalated from a dispute)
     * @param  string  $network  'visa' | 'mastercard' | 'amex' | 'discover' | 'jcb' | 'unionpay' | 'diners' | 'other'
     * @param  string  $reasonCode  string (network-native like '10.4' or '4837' or 'F24')
     * @param  string  $reasonCategory  'fraud' | 'authorization' | 'processing_errors' | 'consumer_disputes' | 'card_recovery'
     * @param  string  $reasonDescription  string (human-readable)
     * @param  int  $amountCents  integer
     * @param  string  $currency  string (ISO 4217)
     * @param  int  $feeCents  integer (network chargeback fee)
     * @param  string  $evidenceDueBy  ISO 8601
     * @param  string  $provider  'stripe' | 'paddle' | 'adyen' | 'square' | 'paypal' | 'custom_webhook'
     * @param  bool  $rdrEligible  boolean
     * @param  bool  $ce3Eligible  boolean
     * @param  string  $filedAt  ISO 8601
     */
    public function __construct(
        public readonly string $chargebackId,
        public readonly string $tenantId,
        public readonly string $chargebackNumber,
        public readonly string $paymentId,
        public readonly string $invoiceId,
        public readonly string $paymentDisputeId,
        public readonly string $network,
        public readonly string $reasonCode,
        public readonly string $reasonCategory,
        public readonly string $reasonDescription,
        public readonly int $amountCents,
        public readonly string $currency,
        public readonly int $feeCents,
        public readonly string $evidenceDueBy,
        public readonly string $provider,
        public readonly bool $rdrEligible,
        public readonly bool $ce3Eligible,
        public readonly string $filedAt,
    ) {
    }
}
