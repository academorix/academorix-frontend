<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance chargeback --force`.

declare(strict_types=1);

namespace Academorix\Chargeback\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ChargebackLost fired + ClawbackCoordinator detected that the disputed invoice had a redeemed coupon. Cascades to coupon module for redemption reversal.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - coupon::ProcessCouponClawbackOnInvoiceCharged (reverses redemption + decrements coupon.usage_count with clawback_reason='chargeback')
 *
 * @category Chargeback
 *
 * @since    0.1.0
 */
final readonly class ChargebackClawbackCoupon implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $chargebackId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceId  ULID
     * @param  string  $couponRedemptionId  ULID (crd_*)
     * @param  string  $couponId  ULID (cpn_*)
     * @param  string  $requestedAt  ISO 8601
     */
    public function __construct(
        public readonly string $chargebackId,
        public readonly string $tenantId,
        public readonly string $invoiceId,
        public readonly string $couponRedemptionId,
        public readonly string $couponId,
        public readonly string $requestedAt,
    ) {
    }
}
