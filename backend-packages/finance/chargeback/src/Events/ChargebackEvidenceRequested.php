<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance chargeback --force`.

declare(strict_types=1);

namespace Academorix\Chargeback\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The bank has requested evidence — this fires alongside ChargebackFiled (initial request) OR later when the bank requests ADDITIONAL evidence after reviewing an initial submission (CE3.0 second pass). Sets evidence_due_by on the chargeback.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchChargebackEvidenceReminderNotification (admin — asks for evidence)
 *
 * @category Chargeback
 *
 * @since    0.1.0
 */
final readonly class ChargebackEvidenceRequested implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $chargebackId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $submissionAttemptNumber  integer
     * @param  string  $evidenceDueBy  ISO 8601
     * @param  bool  $additionalEvidenceRequested  boolean (false on initial request; true on CE3.0 second pass)
     * @param  string  $networkRequestNotes  string | null (bank's rationale for additional evidence)
     */
    public function __construct(
        public readonly string $chargebackId,
        public readonly string $tenantId,
        public readonly int $submissionAttemptNumber,
        public readonly string $evidenceDueBy,
        public readonly bool $additionalEvidenceRequested,
        public readonly string $networkRequestNotes,
    ) {
    }
}
