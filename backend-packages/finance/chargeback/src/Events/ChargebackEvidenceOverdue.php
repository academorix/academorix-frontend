<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance chargeback --force`.

declare(strict_types=1);

namespace Academorix\Chargeback\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * NotifyChargebackEvidenceOverdueJob detected a chargeback with evidence_due_by approaching (7 / 3 / 1 day cadence). Actionable — deadline missed = auto-loss. Not fired on the deadline itself; the transition to expired handles that separately via MarkExpiredChargebacksJob.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchChargebackEvidenceOverdueNotification (admin — escalating cadence with days_remaining)
 * - observability::MetricsCollector (academorix.chargeback.evidence_overdue_total labelled by days_remaining)
 *
 * @category Chargeback
 *
 * @since    0.1.0
 */
final readonly class ChargebackEvidenceOverdue implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $chargebackId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $chargebackNumber  string
     * @param  int  $daysRemaining  integer (7 | 3 | 1)
     * @param  string  $evidenceDueBy  ISO 8601
     * @param  int  $currentEvidenceCompletenessPercent  integer 0-100
     */
    public function __construct(
        public readonly string $chargebackId,
        public readonly string $tenantId,
        public readonly string $chargebackNumber,
        public readonly int $daysRemaining,
        public readonly string $evidenceDueBy,
        public readonly int $currentEvidenceCompletenessPercent,
    ) {
    }
}
