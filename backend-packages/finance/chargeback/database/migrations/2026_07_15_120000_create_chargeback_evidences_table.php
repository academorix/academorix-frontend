<?php

/**
 * @file database/migrations/2026_07_15_120000_create_chargeback_evidences_table.php
 *
 * @description
 * Create the `chargeback_evidences` table for the `ChargebackEvidence` entity.
 * Owned by the chargeback module. Emitted from the module blueprint —
 * regenerate via `generate-module.py finance chargeback --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance chargeback --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('chargeback_evidences', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('chargeback_id', 64);
            $table->foreign('chargeback_id')->references('id')->on('chargebacks')->onDelete('cascade');
            $table->integer('submission_attempt_number');
            $table->jsonb('invoice_evidence');
            $table->jsonb('receipt_evidence');
            $table->jsonb('service_documentation');
            $table->jsonb('refund_policy');
            $table->jsonb('communication_log');
            $table->jsonb('shipping_docs')->nullable();
            $table->jsonb('customer_signature_evidence');
            $table->text('uncategorized_text')->nullable();
            $table->timestampTz('submitted_at');
            $table->string('submitted_by_user_id', 64);
            $table->foreign('submitted_by_user_id')->references('id')->on('users')->onDelete('restrict');
            $table->jsonb('provider_response')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 64)->nullable();
            $table->string('updated_by', 64)->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'chargeback_id', 'submission_attempt_number'], 'chargeback_evidence_attempt_unique');
            $table->index(['tenant_id', 'chargeback_id'], 'chargeback_evidence_chargeback_idx');
            $table->index(['tenant_id', 'submitted_by_user_id'], 'chargeback_evidence_submitter_idx');
            $table->index(['submitted_at'], 'chargeback_evidence_submitted_at_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('chargeback_evidences');
    }
};
