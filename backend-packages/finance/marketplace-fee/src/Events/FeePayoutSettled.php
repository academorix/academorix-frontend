<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance marketplace-fee --force`.

declare(strict_types=1);

namespace Academorix\MarketplaceFee\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * FeePayout.status pending -> settled. Stripe transfer succeeded.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchFeePayoutSettledNotificationListener
 *
 * @category MarketplaceFee
 *
 * @since    0.1.0
 */
final readonly class FeePayoutSettled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $feePayoutId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $totalFeeMinor  integer
     * @param  string  $settlementTransferId  string
     * @param  string  $settledAt  ISO 8601
     */
    public function __construct(
        public readonly string $feePayoutId,
        public readonly string $tenantId,
        public readonly int $totalFeeMinor,
        public readonly string $settlementTransferId,
        public readonly string $settledAt,
    ) {
    }
}
