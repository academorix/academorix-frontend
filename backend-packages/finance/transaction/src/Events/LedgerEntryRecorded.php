<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance transaction --force`.

declare(strict_types=1);

namespace Academorix\Transaction\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TransactionLedgerEntryObserver.creating committed a ledger entry row. Fires once per entry — a typical invoice-paid transaction fires TWICE (one debit entry + one credit entry).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - monitoring::MetricsCollector (increments academorix.ledger.entries_recorded_total; labels: account, side)
 * - cache::InvalidateAccountBalanceCache (invalidates the specific account's balance cache)
 *
 * @category Transaction
 *
 * @since    0.1.0
 */
final readonly class LedgerEntryRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $ledgerEntryId  ULID (tld_*)
     * @param  string  $transactionId  ULID (trx_*)
     * @param  string  $tenantId  ULID
     * @param  string  $account  string (one of 14 LedgerAccount values)
     * @param  string  $accountCurrency  string (ISO 4217)
     * @param  int  $debitCents  integer (0 when this is a credit entry)
     * @param  int  $creditCents  integer (0 when this is a debit entry)
     * @param  string  $description  string (<= 256 chars)
     * @param  string  $recordedAt  ISO 8601
     */
    public function __construct(
        public readonly string $ledgerEntryId,
        public readonly string $transactionId,
        public readonly string $tenantId,
        public readonly string $account,
        public readonly string $accountCurrency,
        public readonly int $debitCents,
        public readonly int $creditCents,
        public readonly string $description,
        public readonly string $recordedAt,
    ) {
    }
}
