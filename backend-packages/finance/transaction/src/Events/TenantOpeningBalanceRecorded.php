<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance transaction --force`.

declare(strict_types=1);

namespace Academorix\Transaction\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * RecordOpeningBalanceOnTenantProvisioned hook fired on tenancy::TenantProvisioned. Creates one transaction with kind='opening_balance' + ledger entries at zero for every enabled account (or non-zero when migrating from a legacy system with existing balances). Fires exactly once per tenant.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — the tenant's ledger origin story)
 * - activity::WriteToActivityLog
 *
 * @category Transaction
 *
 * @since    0.1.0
 */
final readonly class TenantOpeningBalanceRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $transactionId  ULID (trx_*)
     * @param  string  $baseCurrency  string (ISO 4217)
     * @param  string  $accountsSeeded  string[] (list of chart-of-accounts entries with entries)
     * @param  string  $recordedAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $transactionId,
        public readonly string $baseCurrency,
        public readonly string $accountsSeeded,
        public readonly string $recordedAt,
    ) {
    }
}
