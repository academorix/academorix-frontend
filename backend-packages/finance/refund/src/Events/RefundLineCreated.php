<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Refund\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * RefundLineRepository finished a successful create. Fires per line at partial / prorated refund creation. Distinct from RefundRequested (parent) — one parent request emits one RefundRequested + N RefundLineCreated events.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - activity::WriteToActivityLog
 *
 * @category Refund
 *
 * @since    0.1.0
 */
final readonly class RefundLineCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $refundLineId  ULID (rfl_*)
     * @param  string  $refundId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceLineId  ULID (iln_*)
     * @param  int  $amountCents  integer
     * @param  string  $currency  string
     * @param  array  $prorationConfig  object | null (frozen unused_period + pass_credit)
     * @param  string  $reasonNote  string | null
     */
    public function __construct(
        public readonly string $refundLineId,
        public readonly string $refundId,
        public readonly string $tenantId,
        public readonly string $invoiceLineId,
        public readonly int $amountCents,
        public readonly string $currency,
        public readonly array $prorationConfig,
        public readonly string $reasonNote,
    ) {
    }
}
