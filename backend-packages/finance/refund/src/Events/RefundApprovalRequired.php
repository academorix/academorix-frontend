<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Refund\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * RefundObserver.creating computed `requires_approval=true` (amount / staleness threshold OR always-approve reason). Distinct from RefundRequested (which fires unconditionally). Dispatches NotifyRefundApprovalRequiredJob to seed approver notifications.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial-audit)
 * - activity::WriteToActivityLog
 * - notifications::DispatchRefundApprovalRequiredNotification (admin approvers)
 * - observability::MetricsCollector (academorix.refund.approvals_pending_total)
 *
 * @category Refund
 *
 * @since    0.1.0
 */
final readonly class RefundApprovalRequired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $refundId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $amountCents  integer
     * @param  string  $currency  string
     * @param  string  $reason  string
     * @param  string  $reasonNote  string
     * @param  string  $trigger  'amount_threshold' | 'staleness_threshold' | 'both' | 'always_approve_reason'
     * @param  int  $amountThresholdCents  integer | null
     * @param  int  $daysSincePayment  integer
     * @param  int  $daysThreshold  integer | null
     * @param  string  $initiatedByUserId  ULID
     */
    public function __construct(
        public readonly string $refundId,
        public readonly string $tenantId,
        public readonly int $amountCents,
        public readonly string $currency,
        public readonly string $reason,
        public readonly string $reasonNote,
        public readonly string $trigger,
        public readonly int $amountThresholdCents,
        public readonly int $daysSincePayment,
        public readonly int $daysThreshold,
        public readonly string $initiatedByUserId,
    ) {
    }
}
