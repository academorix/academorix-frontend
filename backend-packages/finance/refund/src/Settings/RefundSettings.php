<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Refund\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * RefundSettings — Refunds.
 *
 * Tenant-wide refund workflow configuration.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Refund
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'refund',
    label: 'Refunds',
    description: 'Tenant-wide refund workflow configuration.',
    icon: 'arrow-uturn-left',
    scope: 'tenant',
    permission: 'refunds.viewAny',
    sortOrder: 58,
)]
class RefundSettings extends Settings
{

    // ── Approval workflow ──────────────────────────────────
    #[SettingGroup(label: 'Approval workflow', description: 'Thresholds that trigger the require-approval flow.', icon: 'shield-check', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Number, label: 'Approval threshold amount (cents)', group: 'Approval workflow', sortOrder: 1, min: 0, helpText: 'Refunds above this cents amount require approval. Default 10000 = $100.')]
    public int $approval_threshold_amount_cents = 10000;

    #[SettingField(controlType: ControlType::Number, label: 'Approval threshold days-since-payment', group: 'Approval workflow', sortOrder: 2, min: 0, max: 3650, helpText: 'Refunds initiated more than N days after the parent payment.processed_at require approval. Default 30. Stale refunds are higher-risk.')]
    public int $approval_threshold_days_since_payment = 30;

    #[SettingField(controlType: ControlType::Toggle, label: 'Always require approval', group: 'Approval workflow', sortOrder: 3, helpText: 'Every refund requires approval regardless of amount / staleness / reason. High-risk industries only.')]
    public bool $approval_always_require = false;

    #[SettingField(controlType: ControlType::Number, label: 'Approval SLA hours', group: 'Approval workflow', sortOrder: 4, min: 1, max: 720, helpText: 'Hours after which an unapproved refund fires a P2 signal. Actionable — consumer-protection-law SLAs often require 14-30 day refund resolution.')]
    public int $approval_sla_hours = 24;


    // ── Customer self-service ──────────────────────────────────
    #[SettingGroup(label: 'Customer self-service', description: 'Customer-initiated refund request configuration.', icon: 'user-circle', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enable customer self-service', group: 'Customer self-service', sortOrder: 1, helpText: 'When on, customers can request refunds via POST /refunds/mine. When off, only admin can initiate refunds.')]
    public bool $customer_self_service_enabled = true;

    #[SettingField(controlType: ControlType::Text, label: 'Customer-allowed reasons', group: 'Customer self-service', sortOrder: 2, options: ['customer_requested' => 'Customer requested', 'product_not_received' => 'Product not received', 'product_unacceptable' => 'Product unacceptable', 'subscription_cancelled' => 'Subscription cancelled'], helpText: 'Reasons a customer can specify via POST /refunds/mine. Admin-only reasons are always excluded.')]
    public array $customer_allowed_reasons = ['customer_requested', 'product_not_received', 'product_unacceptable', 'subscription_cancelled'];


    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', icon: 'bell', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Select, label: 'Customer notification cadence', group: 'Notifications', sortOrder: 1, options: ['state_transitions' => 'Every state transition (recommended)', 'terminal_only' => 'Terminal states only (succeeded / failed / rejected)'])]
    public string $notify_customer_cadence = 'state_transitions';

    #[SettingField(controlType: ControlType::Text, label: 'Admin notifications on statuses', group: 'Notifications', sortOrder: 2, options: ['requested' => 'Requested', 'approval_required' => 'Approval required', 'approved' => 'Approved', 'processing' => 'Processing', 'succeeded' => 'Succeeded', 'failed' => 'Failed', 'provider_error' => 'Provider error', 'rejected' => 'Rejected', 'cancelled' => 'Cancelled'])]
    public array $notify_admins_on_status = ['approval_required', 'failed', 'provider_error', 'succeeded'];

    #[SettingField(controlType: ControlType::Text, label: 'Additional admin notification recipients', group: 'Notifications', sortOrder: 3, helpText: 'Users to always notify on admin-cadence events. Overrides the default (any user holding refunds.viewAny permission).')]
    public ?string $notification_recipients = null;


    // ── Proration ──────────────────────────────────
    #[SettingGroup(label: 'Proration', icon: 'chart-bar', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Number, label: 'Pass valuation (cents)', group: 'Proration', sortOrder: 1, min: 0, helpText: 'Per-pass valuation for pass-credit accounting during prorated refunds. Used to compute unused_pass_credit_cents = unused_pass_count × pass_valuation_cents.')]
    public int $pass_valuation_cents = 500;

    #[SettingField(controlType: ControlType::Select, label: 'Monthly billing day count', group: 'Proration', sortOrder: 2, options: ['30' => '30 days (accountant-friendly, default)'], helpText: 'How many days a monthly billing interval counts as for proration. Default 30 avoids DST + month-length nondeterminism.')]
    public int $month_day_count = 30;


    // ── Retention ──────────────────────────────────
    #[SettingGroup(label: 'Retention', icon: 'archive-box', sortOrder: 5)]
    #[SettingField(controlType: ControlType::Select, label: 'Retention window', group: 'Retention', sortOrder: 1, options: ['7' => '7 years (default)', '10' => '10 years (Enterprise)'], helpText: '10 years requires the refund_extended_retention entitlement.')]
    public int $retention_years = 7;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'refund';
    }
}
