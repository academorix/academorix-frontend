<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance gateway --force`.

declare(strict_types=1);

namespace Academorix\Gateway\Services;

use Academorix\Gateway\Contracts\Services\PaddleGatewayDriverInterface;
use Illuminate\Container\Attributes\Scoped;
use Academorix\Gateway\Contracts\Repositories\GatewayWebhookEventRepositoryInterface;

/**
 * Concrete service — PaddleGatewayDriver.
 *
 * Implements {@see PaddleGatewayDriverInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Gateway
 *
 * @since    0.1.0
 */
#[Scoped]
final class PaddleGatewayDriver implements PaddleGatewayDriverInterface
{
    /**
     * @param  GatewayWebhookEventRepositoryInterface  $gatewayWebhookEventRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly GatewayWebhookEventRepositoryInterface $gatewayWebhookEventRepository,
    ) {
    }
}
