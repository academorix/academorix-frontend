<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance gateway --force`.

declare(strict_types=1);

namespace Academorix\Gateway\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Webhook processing raised an exception. Retry ladder scheduled unless dead_letter reached.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchWebhookProcessingFailedNotification
 * - monitoring::MetricsCollector
 *
 * @category Gateway
 *
 * @since    0.1.0
 */
final readonly class WebhookProcessingFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $webhookEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $eventType  string
     * @param  int  $retryCount  integer
     * @param  string  $error  string
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $webhookEventId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $eventType,
        public readonly int $retryCount,
        public readonly string $error,
        public readonly string $at,
    ) {
    }
}
