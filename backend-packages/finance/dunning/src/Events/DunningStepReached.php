<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance dunning --force`.

declare(strict_types=1);

namespace Academorix\Dunning\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TickDunningStepJob advanced current_step. Fires the step's configured action (notify / retry / escalate).
 *
 * ## Consumers
 *
 * - dunning::AttemptRetryListener
 * - notifications::DispatchDunningReminderNotificationListener
 *
 * @category Dunning
 *
 * @since    0.1.0
 */
final readonly class DunningStepReached implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $dunningRunId  ULID
     * @param  int  $stepIndex  integer
     * @param  string  $action  string
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $dunningRunId,
        public readonly int $stepIndex,
        public readonly string $action,
        public readonly string $at,
    ) {
    }
}
