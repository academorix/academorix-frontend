<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance order --force`.

declare(strict_types=1);

namespace Academorix\Order\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New OrderAdjustment (coupon, credit, discount) committed.
 *
 * ## Consumers
 *
 * - order::RecalculateOrderTotalsListener
 * - coupon::MarkCouponRedeemedListener
 * - wallet::HoldWalletBalanceListener
 *
 * @category Order
 *
 * @since    0.1.0
 */
final readonly class OrderAdjustmentApplied implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $orderId  ULID
     * @param  string  $adjustmentId  ULID (adj_*)
     * @param  string  $kind  string
     * @param  int  $amountMinor  integer
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $orderId,
        public readonly string $adjustmentId,
        public readonly string $kind,
        public readonly int $amountMinor,
        public readonly string $at,
    ) {
    }
}
