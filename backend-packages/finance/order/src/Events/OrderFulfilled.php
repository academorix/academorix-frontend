<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance order --force`.

declare(strict_types=1);

namespace Academorix\Order\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Order status -> fulfilled (goods delivered / service performed).
 *
 * ## Consumers
 *
 * - notifications::DispatchOrderFulfilledNotificationListener
 * - audit::WriteToAuditLog
 *
 * @category Order
 *
 * @since    0.1.0
 */
final readonly class OrderFulfilled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $orderId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fulfilledAt  ISO 8601
     */
    public function __construct(
        public readonly string $orderId,
        public readonly string $tenantId,
        public readonly string $fulfilledAt,
    ) {
    }
}
