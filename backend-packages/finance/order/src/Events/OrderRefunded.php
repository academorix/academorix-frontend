<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance order --force`.

declare(strict_types=1);

namespace Academorix\Order\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Order status -> refunded. Sum of refunds >= amount_paid.
 *
 * ## Consumers
 *
 * - invoice::MarkInvoiceRefundedListener
 * - membership::RevokeMembershipListener
 * - entitlements::RevokeEntitlementsListener
 * - notifications::DispatchOrderRefundedNotificationListener
 * - audit::WriteToAuditLog
 *
 * @category Order
 *
 * @since    0.1.0
 */
final readonly class OrderRefunded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $orderId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $amountRefundedMinor  integer
     * @param  string  $refundedAt  ISO 8601
     */
    public function __construct(
        public readonly string $orderId,
        public readonly string $tenantId,
        public readonly int $amountRefundedMinor,
        public readonly string $refundedAt,
    ) {
    }
}
