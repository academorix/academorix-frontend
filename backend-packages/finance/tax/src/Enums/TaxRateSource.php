<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `source` on `tax-rate`.
 *
 * Enum: manual / provider_synced / import / migration.
 *
 * @category Tax
 *
 * @since    0.1.0
 */
enum TaxRateSource: string
{
    use Enum;

    case Manual = 'manual';
    case ProviderSynced = 'provider_synced';
    case Import = 'import';
    case Migration = 'migration';
}
