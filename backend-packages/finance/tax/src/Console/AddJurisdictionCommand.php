<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan tax:add-jurisdiction` — Add jurisdiction — tax command.
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'tax:add-jurisdiction',
    description: 'Add jurisdiction — tax command.',
)]
final class AddJurisdictionCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Add jurisdiction — tax command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Add jurisdiction — tax command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
