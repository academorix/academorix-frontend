<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Scheduled sweep found a rate whose effective_to <= today. Automatic transition. Note: this does NOT delete the rate — historical rates persist for calculation-freeze lookups.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - tax::InvalidateRateCache
 * - monitoring::MetricsCollector
 *
 * @category Tax
 *
 * @since    0.1.0
 */
final readonly class TaxRateExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $taxRateId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $taxJurisdictionId  ULID
     * @param  string  $rateType  string
     * @param  string  $expiredAt  date
     */
    public function __construct(
        public readonly string $taxRateId,
        public readonly string $tenantId,
        public readonly string $taxJurisdictionId,
        public readonly string $rateType,
        public readonly string $expiredAt,
    ) {
    }
}
