<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Scheduled ExpireExemptionsJob found an exemption whose valid_until <= today. Automatic transition verified → expired.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years post-expiry)
 * - tax::InvalidateExemptionCache
 * - monitoring::MetricsCollector
 *
 * @category Tax
 *
 * @since    0.1.0
 */
final readonly class TaxExemptionExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $taxExemptionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $customerType  string
     * @param  string  $customerId  ULID
     * @param  string  $exemptionType  string
     * @param  string  $expiredAt  date
     */
    public function __construct(
        public readonly string $taxExemptionId,
        public readonly string $tenantId,
        public readonly string $customerType,
        public readonly string $customerId,
        public readonly string $exemptionType,
        public readonly string $expiredAt,
    ) {
    }
}
