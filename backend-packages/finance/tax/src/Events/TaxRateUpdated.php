<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TaxRateObserver.updating committed a mutation on an existing rate.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - tax::InvalidateRateCache
 * - monitoring::MetricsCollector
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TaxRateUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $taxRateId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedFields  string[]
     * @param  string  $updatedAt  ISO 8601
     */
    public function __construct(
        public readonly string $taxRateId,
        public readonly string $tenantId,
        public readonly string $changedFields,
        public readonly string $updatedAt,
    ) {
    }
}
