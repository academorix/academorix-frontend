<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TaxProviderConfigObserver committed a new provider config row.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — provider connection is a compliance-relevant integration)
 * - notifications::DispatchTaxConfigurationChangedNotification (audit trail for tenant admins)
 * - monitoring::MetricsCollector
 *
 * @category Tax
 *
 * @since    0.1.0
 */
final readonly class TaxProviderConfigured implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $providerConfigId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $enabledJurisdictions  string[] (country codes)
     * @param  string  $configuredByUserId  ULID
     * @param  string  $configuredAt  ISO 8601
     */
    public function __construct(
        public readonly string $providerConfigId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $enabledJurisdictions,
        public readonly string $configuredByUserId,
        public readonly string $configuredAt,
    ) {
    }
}
