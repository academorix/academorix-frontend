<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Actions\Tenant;

use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Delete;

/**
 * `DELETE /api/v1/tax/providers/{provider}` — delete action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Delete(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsController]
#[Delete('/api/v1/tax/providers/{provider}')]
final class DeleteProviderAction
{
    /**
     * Execute the action.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function __invoke(): mixed
    {
        // Hand-implement the domain logic here.
        return null;
    }
}
