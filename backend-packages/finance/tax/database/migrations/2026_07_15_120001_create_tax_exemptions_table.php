<?php

/**
 * @file database/migrations/2026_07_15_120001_create_tax_exemptions_table.php
 *
 * @description
 * Create the `tax_exemptions` table for the `TaxExemption` entity.
 * Owned by the tax module. Every column + index reference
 * goes through {@see Academorix\Tax\Contracts\Data\TaxExemptionInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

use Academorix\Tax\Contracts\Data\TaxExemptionInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(TaxExemptionInterface::TABLE, function (Blueprint $table): void {
            $table->string(TaxExemptionInterface::ATTR_ID, 64)->primary();
            $table->string(TaxExemptionInterface::ATTR_TENANT_ID, 64);
            $table->foreign(TaxExemptionInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(TaxExemptionInterface::ATTR_CUSTOMER_TYPE, 96);
            $table->string(TaxExemptionInterface::ATTR_CUSTOMER_ID, 64)->nullable();
            $table->string(TaxExemptionInterface::ATTR_EXEMPTION_TYPE, 32);
            $table->string(TaxExemptionInterface::ATTR_CERTIFICATE_NUMBER, 128);
            $table->string(TaxExemptionInterface::ATTR_CERTIFICATE_DOCUMENT_URL, 512);
            $table->string(TaxExemptionInterface::ATTR_ISSUING_JURISDICTION_ID, 64);
            $table->foreign(TaxExemptionInterface::ATTR_ISSUING_JURISDICTION_ID)->references('id')->on('tax_jurisdictions')->onDelete('restrict');
            $table->date(TaxExemptionInterface::ATTR_VALID_FROM);
            $table->date(TaxExemptionInterface::ATTR_VALID_UNTIL)->nullable();
            $table->string(TaxExemptionInterface::ATTR_VERIFICATION_STATUS, 16)->default('pending');
            $table->string(TaxExemptionInterface::ATTR_VERIFIED_BY_USER_ID, 64)->nullable();
            $table->foreign(TaxExemptionInterface::ATTR_VERIFIED_BY_USER_ID)->references('id')->on('users')->onDelete('restrict');
            $table->timestampTz(TaxExemptionInterface::ATTR_VERIFIED_AT)->nullable();
            $table->jsonb(TaxExemptionInterface::ATTR_METADATA)->nullable();
            $table->string(TaxExemptionInterface::ATTR_CREATED_BY, 64)->nullable();
            $table->string(TaxExemptionInterface::ATTR_UPDATED_BY, 64)->nullable();
            $table->string(TaxExemptionInterface::ATTR_DELETED_BY, 64)->nullable();
            $table->timestampTz(TaxExemptionInterface::ATTR_DELETED_AT)->nullable();
            $table->timestampTz(TaxExemptionInterface::ATTR_CREATED_AT);
            $table->timestampTz(TaxExemptionInterface::ATTR_UPDATED_AT);
            $table->index([TaxExemptionInterface::ATTR_TENANT_ID, TaxExemptionInterface::ATTR_CUSTOMER_TYPE, TaxExemptionInterface::ATTR_CUSTOMER_ID], 'tax_exemptions_customer_idx');
            $table->index([TaxExemptionInterface::ATTR_TENANT_ID, TaxExemptionInterface::ATTR_VERIFICATION_STATUS, TaxExemptionInterface::ATTR_VALID_UNTIL], 'tax_exemptions_status_expiry_idx');
            $table->index([TaxExemptionInterface::ATTR_ISSUING_JURISDICTION_ID], 'tax_exemptions_jurisdiction_idx');
        });

        DB::statement('CREATE UNIQUE INDEX tax_exemptions_customer_type_jurisdiction_unique ON ' . TaxExemptionInterface::TABLE . ' (' . TaxExemptionInterface::ATTR_TENANT_ID . ', ' . TaxExemptionInterface::ATTR_CUSTOMER_TYPE . ', ' . TaxExemptionInterface::ATTR_CUSTOMER_ID . ', ' . TaxExemptionInterface::ATTR_EXEMPTION_TYPE . ', ' . TaxExemptionInterface::ATTR_ISSUING_JURISDICTION_ID . ') WHERE deleted_at IS NULL AND verification_status = \'verified\'');
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(TaxExemptionInterface::TABLE);
    }
};
