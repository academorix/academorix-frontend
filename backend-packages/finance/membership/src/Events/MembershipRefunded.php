<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Refund module completed a refund tied to this membership. Fires from the refund module downstream; membership records refund_id + refund_amount_cents in metadata.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — SOX 404)
 * - activity::WriteToActivityLog
 * - notifications::DispatchMembershipRefundedNotification
 *
 * @category Membership
 *
 * @since    0.1.0
 */
final readonly class MembershipRefunded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $membershipId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $refundId  ULID | null (Wave 4.5+)
     * @param  int  $refundAmountCents  integer
     * @param  string  $refundReason  string
     * @param  string  $refundedAt  ISO 8601
     */
    public function __construct(
        public readonly string $membershipId,
        public readonly string $tenantId,
        public readonly string $refundId,
        public readonly int $refundAmountCents,
        public readonly string $refundReason,
        public readonly string $refundedAt,
    ) {
    }
}
