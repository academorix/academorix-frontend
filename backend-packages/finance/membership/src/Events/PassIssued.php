<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * One pass issued for a membership period. Fires N times when a period grants N passes.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - cache::InvalidatePassCache
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PassIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $passId  ULID (pss_*)
     * @param  string  $membershipId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $passType  string
     * @param  string  $validFrom  ISO 8601
     * @param  string  $validUntil  ISO 8601
     */
    public function __construct(
        public readonly string $passId,
        public readonly string $membershipId,
        public readonly string $tenantId,
        public readonly string $passType,
        public readonly string $validFrom,
        public readonly string $validUntil,
    ) {
    }
}
