<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Plan archived (is_active=false). Refused when active memberships reference the plan — enforced by PreventPlanArchiveWithActiveMemberships hook. Releases 1 unit of membership_plan_slot.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MembershipPlanArchived implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $planId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $archivedAt  ISO 8601
     * @param  string  $archivedByUserId  ULID
     */
    public function __construct(
        public readonly string $planId,
        public readonly string $tenantId,
        public readonly string $archivedAt,
        public readonly string $archivedByUserId,
    ) {
    }
}
