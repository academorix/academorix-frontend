<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MembershipCreator committed. Snapshots price / coupon / tax at signing. Fires alongside the atomic Invoice.create in the same DB transaction — if the invoice-create fails, this event never fires because the transaction rolls back.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchMembershipCreatedNotification (to primary guardian + admin)
 * - membership::CreateInvoiceOnMembershipCreated (atomic — same transaction)
 * - membership::SnapshotTaxCalculationOnMembershipCreated (freezes tax_calculation_snapshot)
 * - membership::SnapshotCouponRedemptionOnMembershipCreated (freezes coupon_snapshot + increments coupon usage counter)
 *
 * @category Membership
 *
 * @since    0.1.0
 */
final readonly class MembershipCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $membershipId  ULID (mbr_*)
     * @param  string  $tenantId  ULID
     * @param  string  $regionId  ULID
     * @param  string  $branchId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $membershipPlanId  ULID (mbp_*)
     * @param  string  $primaryGuardianId  ULID (agu_*) | null
     * @param  int  $priceSnapshotCents  integer
     * @param  string  $currency  string (ISO 4217)
     * @param  string  $billingInterval  string
     * @param  string  $couponId  ULID (cpn_*) | null
     * @param  int  $couponDiscountCents  integer
     * @param  string  $taxCalculationId  ULID (txc_*) | null
     * @param  int  $taxAmountCents  integer
     * @param  int  $totalAmountCents  integer
     * @param  string  $createdByUserId  ULID
     */
    public function __construct(
        public readonly string $membershipId,
        public readonly string $tenantId,
        public readonly string $regionId,
        public readonly string $branchId,
        public readonly string $athleteId,
        public readonly string $membershipPlanId,
        public readonly string $primaryGuardianId,
        public readonly int $priceSnapshotCents,
        public readonly string $currency,
        public readonly string $billingInterval,
        public readonly string $couponId,
        public readonly int $couponDiscountCents,
        public readonly string $taxCalculationId,
        public readonly int $taxAmountCents,
        public readonly int $totalAmountCents,
        public readonly string $createdByUserId,
    ) {
    }
}
