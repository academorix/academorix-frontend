<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MembershipRenewal.status = failed_transient. Payment failed but retryable. Increments membership.consecutive_failed_renewal_count. TriggerRetryLadderOnRenewalFailed schedules the next attempt at day+3/+7/+14 per attempt_number.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchMembershipRenewalFailedNotification (transactional_required — dunning email)
 * - membership::TriggerRetryLadderOnRenewalFailed (schedules RetryFailedRenewalJob)
 *
 * @category Membership
 *
 * @since    0.1.0
 */
final readonly class MembershipRenewalFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $membershipId  ULID
     * @param  string  $renewalId  ULID (mbn_*)
     * @param  string  $tenantId  ULID
     * @param  int  $attemptNumber  integer
     * @param  string  $failureReason  string
     * @param  int  $consecutiveFailedRenewalCount  integer
     * @param  string  $nextAttemptAt  ISO 8601 | null
     */
    public function __construct(
        public readonly string $membershipId,
        public readonly string $renewalId,
        public readonly string $tenantId,
        public readonly int $attemptNumber,
        public readonly string $failureReason,
        public readonly int $consecutiveFailedRenewalCount,
        public readonly string $nextAttemptAt,
    ) {
    }
}
