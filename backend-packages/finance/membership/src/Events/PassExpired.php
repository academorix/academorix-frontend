<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ExpireStalePassesJob nightly sweep found an unconsumed pass past valid_until. Distinct from PassRefunded (admin-initiated return).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - cache::InvalidatePassCache
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PassExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $passId  ULID
     * @param  string  $membershipId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $expiredAt  ISO 8601
     */
    public function __construct(
        public readonly string $passId,
        public readonly string $membershipId,
        public readonly string $tenantId,
        public readonly string $expiredAt,
    ) {
    }
}
