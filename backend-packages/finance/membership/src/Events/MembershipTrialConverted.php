<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Trial ended AND the first paid renewal succeeded. High-signal for growth analytics + funnel measurement.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchTrialConvertedNotification (celebratory)
 * - growth::analytics::TrackTrialConversion
 *
 * @category Membership
 *
 * @since    0.1.0
 */
final readonly class MembershipTrialConverted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $membershipId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $convertedAt  ISO 8601
     * @param  string  $firstPaidRenewalId  ULID (mbn_*)
     */
    public function __construct(
        public readonly string $membershipId,
        public readonly string $tenantId,
        public readonly string $convertedAt,
        public readonly string $firstPaidRenewalId,
    ) {
    }
}
