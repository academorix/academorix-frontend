<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1. Chargeback module fired a dispute affecting a payment on this membership. Membership status stays active — dispute lifecycle is chargeback module's concern. Admin notified via cannot-opt-out channel.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — SOX 404)
 * - notifications::DispatchMembershipChargedBackNotification (admin-only, cannot-opt-out)
 *
 * @category Membership
 *
 * @since    0.1.0
 */
final readonly class MembershipChargedBack implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $membershipId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $chargebackId  ULID | null (Wave 4.5+)
     * @param  int  $amountCents  integer
     * @param  string  $reasonCode  string
     * @param  string  $filedAt  ISO 8601
     */
    public function __construct(
        public readonly string $membershipId,
        public readonly string $tenantId,
        public readonly string $chargebackId,
        public readonly int $amountCents,
        public readonly string $reasonCode,
        public readonly string $filedAt,
    ) {
    }
}
