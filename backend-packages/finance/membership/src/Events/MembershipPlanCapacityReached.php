<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * plan.current_active_members reached plan.max_active_members. Admin notified so they can raise capacity or communicate a waitlist.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchPlanCapacityReachedNotification (admin — informational)
 *
 * @category Membership
 *
 * @since    0.1.0
 */
final readonly class MembershipPlanCapacityReached implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $planId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $maxActiveMembers  integer
     * @param  int  $currentActiveMembers  integer
     * @param  string  $reachedAt  ISO 8601
     */
    public function __construct(
        public readonly string $planId,
        public readonly string $tenantId,
        public readonly int $maxActiveMembers,
        public readonly int $currentActiveMembers,
        public readonly string $reachedAt,
    ) {
    }
}
