<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Admin (or customer via self-serve) migrated the membership to a higher-price plan. Prorated adjustment via invoice module. Passes for current period recomputed per new plan.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchMembershipPlanChangedNotification
 *
 * @category Membership
 *
 * @since    0.1.0
 */
final readonly class MembershipPlanUpgraded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $membershipId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromPlanId  ULID
     * @param  string  $toPlanId  ULID
     * @param  int  $prorationAmountCents  integer
     * @param  string  $changedAt  ISO 8601
     */
    public function __construct(
        public readonly string $membershipId,
        public readonly string $tenantId,
        public readonly string $fromPlanId,
        public readonly string $toPlanId,
        public readonly int $prorationAmountCents,
        public readonly string $changedAt,
    ) {
    }
}
