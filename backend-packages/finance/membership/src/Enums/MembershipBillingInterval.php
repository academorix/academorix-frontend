<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `billing_interval` on `membership`.
 *
 * Enum: monthly / quarterly / annual / lifetime.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
enum MembershipBillingInterval: string
{
    use Enum;

    case Monthly = 'monthly';
    case Quarterly = 'quarterly';
    case Annual = 'annual';
    case Lifetime = 'lifetime';
}
