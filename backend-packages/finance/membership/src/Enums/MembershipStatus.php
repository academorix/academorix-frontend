<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `status` on `membership`.
 *
 * Enum: pending / active / paused / cancelled / lapsed / refunded / expired.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
enum MembershipStatus: string
{
    use Enum;

    case Pending = 'pending';
    case Active = 'active';
    case Paused = 'paused';
    case Cancelled = 'cancelled';
    case Lapsed = 'lapsed';
    case Refunded = 'refunded';
    case Expired = 'expired';
}
