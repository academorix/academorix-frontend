<?php

/**
 * @file database/migrations/2026_07_15_120000_create_budgets_table.php
 *
 * @description
 * Create the `budgets` table for the `Budget` entity.
 * Owned by the expenses module. Emitted from the module blueprint —
 * regenerate via `generate-module.py finance expenses --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('budgets', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('branch_id', 30)->nullable();
            $table->foreign('branch_id')->references('id')->on('branches')->onDelete('cascade');
            $table->string('expense_category_id', 30)->nullable();
            $table->foreign('expense_category_id')->references('id')->on('expense_categories')->onDelete('cascade');
            $table->string('cost_center_id', 30)->nullable();
            $table->foreign('cost_center_id')->references('id')->on('cost_centers')->onDelete('cascade');
            $table->date('period_start');
            $table->date('period_end');
            $table->unsignedBigInteger('amount_minor');
            $table->unsignedBigInteger('used_amount_minor')->default('0');
            $table->string('currency', 3);
            $table->integer('alert_threshold_percent')->nullable()->default('80');
            $table->jsonb('metadata')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->timestampTz('deleted_at')->nullable();
            $table->index(['tenant_id', 'branch_id', 'expense_category_id', 'period_start'], 'budgets_scope_period_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('budgets');
    }
};
