<?php

/**
 * @file database/migrations/2026_07_15_120000_create_budgets_table.php
 *
 * @description
 * Create the `budgets` table for the `Budget` entity.
 * Owned by the expenses module. Every column + index reference
 * goes through {@see Academorix\Expenses\Contracts\Data\BudgetInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

use Academorix\Expenses\Contracts\Data\BudgetInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(BudgetInterface::TABLE, function (Blueprint $table): void {
            $table->string(BudgetInterface::ATTR_ID, 30)->primary();
            $table->string(BudgetInterface::ATTR_TENANT_ID, 30);
            $table->foreign(BudgetInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(BudgetInterface::ATTR_BRANCH_ID, 30)->nullable();
            $table->foreign(BudgetInterface::ATTR_BRANCH_ID)->references('id')->on('branches')->onDelete('cascade');
            $table->string(BudgetInterface::ATTR_EXPENSE_CATEGORY_ID, 30)->nullable();
            $table->foreign(BudgetInterface::ATTR_EXPENSE_CATEGORY_ID)->references('id')->on('expense_categories')->onDelete('cascade');
            $table->string(BudgetInterface::ATTR_COST_CENTER_ID, 30)->nullable();
            $table->foreign(BudgetInterface::ATTR_COST_CENTER_ID)->references('id')->on('cost_centers')->onDelete('cascade');
            $table->date(BudgetInterface::ATTR_PERIOD_START);
            $table->date(BudgetInterface::ATTR_PERIOD_END);
            $table->unsignedBigInteger(BudgetInterface::ATTR_AMOUNT_MINOR);
            $table->unsignedBigInteger(BudgetInterface::ATTR_USED_AMOUNT_MINOR)->default('0');
            $table->string(BudgetInterface::ATTR_CURRENCY, 3);
            $table->integer(BudgetInterface::ATTR_ALERT_THRESHOLD_PERCENT)->nullable()->default('80');
            $table->jsonb(BudgetInterface::ATTR_METADATA)->nullable();
            $table->timestampTz(BudgetInterface::ATTR_CREATED_AT);
            $table->timestampTz(BudgetInterface::ATTR_UPDATED_AT);
            $table->timestampTz(BudgetInterface::ATTR_DELETED_AT)->nullable();
            $table->index([BudgetInterface::ATTR_TENANT_ID, BudgetInterface::ATTR_BRANCH_ID, BudgetInterface::ATTR_EXPENSE_CATEGORY_ID, BudgetInterface::ATTR_PERIOD_START], 'budgets_scope_period_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(BudgetInterface::TABLE);
    }
};
