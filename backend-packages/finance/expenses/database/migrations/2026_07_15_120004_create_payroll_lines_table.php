<?php

/**
 * @file database/migrations/2026_07_15_120004_create_payroll_lines_table.php
 *
 * @description
 * Create the `payroll_lines` table for the `PayrollLine` entity.
 * Owned by the expenses module. Emitted from the module blueprint —
 * regenerate via `generate-module.py finance expenses --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('payroll_lines', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('payroll_run_id', 30);
            $table->foreign('payroll_run_id')->references('id')->on('payroll_runs')->onDelete('cascade');
            $table->string('staff_id', 30);
            $table->foreign('staff_id')->references('id')->on('staff')->onDelete('restrict');
            $table->integer('sessions_delivered')->default('0');
            $table->decimal('hours_worked', 15, 4)->nullable();
            $table->unsignedBigInteger('gross_amount_minor');
            $table->unsignedBigInteger('bonus_amount_minor')->default('0');
            $table->unsignedBigInteger('deductions_amount_minor')->default('0');
            $table->unsignedBigInteger('net_amount_minor');
            $table->string('currency', 3);
            $table->jsonb('branch_allocations')->nullable();
            $table->timestampTz('computed_at')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->index(['tenant_id', 'payroll_run_id'], 'payroll_lines_run_idx');
            $table->index(['tenant_id', 'staff_id'], 'payroll_lines_staff_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('payroll_lines');
    }
};
