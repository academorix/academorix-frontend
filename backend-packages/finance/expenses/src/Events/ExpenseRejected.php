<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Academorix\Expenses\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * status: submitted → rejected. Reason REQUIRED.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::NotifySubmitterRejected
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
final readonly class ExpenseRejected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $expenseId  ULID
     * @param  string  $rejectedByUserId  ULID
     * @param  string  $rejectionReason  string
     */
    public function __construct(
        public readonly string $expenseId,
        public readonly string $rejectedByUserId,
        public readonly string $rejectionReason,
    ) {
    }
}
