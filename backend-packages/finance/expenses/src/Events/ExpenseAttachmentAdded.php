<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Academorix\Expenses\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Receipt / invoice attached to Expense.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - compliance::VerifyOcrReceipt
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
final readonly class ExpenseAttachmentAdded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $expenseId  ULID
     * @param  string  $attachmentId  ULID
     * @param  string  $fileId  ULID
     * @param  string  $kind  'receipt'|'invoice'|'contract'|'other'
     */
    public function __construct(
        public readonly string $expenseId,
        public readonly string $attachmentId,
        public readonly string $fileId,
        public readonly string $kind,
    ) {
    }
}
