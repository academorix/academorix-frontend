<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Academorix\Expenses\Actions\Tenant;

use Academorix\Expenses\Contracts\Repositories\ExpenseRepositoryInterface;
use Academorix\Expenses\Data\ExpenseData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Get;

/**
 * `GET /api/v1/expenses/{expense}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Get(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
#[AsController]
#[Get('/api/v1/expenses/{expense}')]
final class ShowExpenseAction
{
    public function __construct(
        private readonly ExpenseRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `expense` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): ExpenseData
    {
        return ExpenseData::from($this->repository->findOrFail($id));
    }
}
