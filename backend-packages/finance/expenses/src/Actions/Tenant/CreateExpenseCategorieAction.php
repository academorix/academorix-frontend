<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Academorix\Expenses\Actions\Tenant;

use Academorix\Expenses\Contracts\Repositories\ExpenseCategoryRepositoryInterface;
use Academorix\Expenses\Data\ExpenseCategoryData;
use Academorix\Expenses\Data\Requests\CreateExpenseCategoryRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/expense-categories` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
#[AsAction(name: 'expenses.expense_categories.create')]
#[Post('/api/v1/expense-categories')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateExpenseCategorieAction
{
    use AsController;

    public function __construct(
        private readonly ExpenseCategoryRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `expense-categorie` from the validated request payload.
     *
     * @param  CreateExpenseCategoryRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateExpenseCategoryRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(ExpenseCategoryData::from($model), JsonResponse::HTTP_CREATED);
    }
}
