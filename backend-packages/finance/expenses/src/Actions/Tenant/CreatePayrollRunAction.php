<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Academorix\Expenses\Actions\Tenant;

use Academorix\Expenses\Contracts\Repositories\PayrollRunRepositoryInterface;
use Academorix\Expenses\Data\PayrollRunData;
use Academorix\Expenses\Data\Requests\CreatePayrollRunRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/payroll-runs` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Post(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
#[AsController]
#[Post('/api/v1/payroll-runs')]
final class CreatePayrollRunAction
{
    public function __construct(
        private readonly PayrollRunRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `payroll-run` from the validated request payload.
     *
     * @param  CreatePayrollRunRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreatePayrollRunRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(PayrollRunData::from($model), JsonResponse::HTTP_CREATED);
    }
}
