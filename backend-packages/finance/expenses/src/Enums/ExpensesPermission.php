<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Academorix\Expenses\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Expenses module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum ExpensesPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `expenses.viewAny` — Expenses ViewAny
     */
    #[Label('Expenses ViewAny')]
    #[Description('Expenses ViewAny')]
    case ExpensesViewAny = 'expenses.viewAny';

    /**
     * `expenses.view` — Expenses View
     */
    #[Label('Expenses View')]
    #[Description('Expenses View')]
    case ExpensesView = 'expenses.view';

    /**
     * `expenses.submit` — Expenses Submit
     */
    #[Label('Expenses Submit')]
    #[Description('Expenses Submit')]
    case ExpensesSubmit = 'expenses.submit';

    /**
     * `expenses.approve` — Threshold-gated by entitlement.
     */
    #[Label('Threshold-gated by entitlement')]
    #[Description('Threshold-gated by entitlement. Self-approval blocked.')]
    case ExpensesApprove = 'expenses.approve';

    /**
     * `expenses.reject` — Expenses Reject
     */
    #[Label('Expenses Reject')]
    #[Description('Expenses Reject')]
    case ExpensesReject = 'expenses.reject';

    /**
     * `expenses.mark-paid` — Expenses Mark-paid
     */
    #[Label('Expenses Mark-paid')]
    #[Description('Expenses Mark-paid')]
    case ExpensesMarkPaid = 'expenses.mark-paid';

    /**
     * `expenses.cancel` — Expenses Cancel
     */
    #[Label('Expenses Cancel')]
    #[Description('Expenses Cancel')]
    case ExpensesCancel = 'expenses.cancel';

    /**
     * `expenses.delete` — Draft state only.
     */
    #[Label('Draft state only')]
    #[Description('Draft state only.')]
    case ExpensesDelete = 'expenses.delete';

    /**
     * `expenses.attachments.view` — Expenses Attachments View
     */
    #[Label('Expenses Attachments View')]
    #[Description('Expenses Attachments View')]
    case ExpensesAttachmentsView = 'expenses.attachments.view';

    /**
     * `expenses.attachments.create` — Expenses Attachments Create
     */
    #[Label('Expenses Attachments Create')]
    #[Description('Expenses Attachments Create')]
    case ExpensesAttachmentsCreate = 'expenses.attachments.create';

    /**
     * `expenses.attachments.delete` — Expenses Attachments Delete
     */
    #[Label('Expenses Attachments Delete')]
    #[Description('Expenses Attachments Delete')]
    case ExpensesAttachmentsDelete = 'expenses.attachments.delete';

    /**
     * `expenses.payroll.viewAny` — Expenses Payroll ViewAny
     */
    #[Label('Expenses Payroll ViewAny')]
    #[Description('Expenses Payroll ViewAny')]
    case ExpensesPayrollViewAny = 'expenses.payroll.viewAny';

    /**
     * `expenses.payroll.view` — Expenses Payroll View
     */
    #[Label('Expenses Payroll View')]
    #[Description('Expenses Payroll View')]
    case ExpensesPayrollView = 'expenses.payroll.view';

    /**
     * `expenses.payroll.create` — Expenses Payroll Create
     */
    #[Label('Expenses Payroll Create')]
    #[Description('Expenses Payroll Create')]
    case ExpensesPayrollCreate = 'expenses.payroll.create';

    /**
     * `expenses.payroll.approve` — Batches over $10k require owner.
     */
    #[Label('Batches over $10k require owner')]
    #[Description('Batches over $10k require owner.')]
    case ExpensesPayrollApprove = 'expenses.payroll.approve';

    /**
     * `expenses.vendors.viewAny` — Expenses Vendors ViewAny
     */
    #[Label('Expenses Vendors ViewAny')]
    #[Description('Expenses Vendors ViewAny')]
    case ExpensesVendorsViewAny = 'expenses.vendors.viewAny';

    /**
     * `expenses.vendors.view` — Expenses Vendors View
     */
    #[Label('Expenses Vendors View')]
    #[Description('Expenses Vendors View')]
    case ExpensesVendorsView = 'expenses.vendors.view';

    /**
     * `expenses.vendors.create` — Expenses Vendors Create
     */
    #[Label('Expenses Vendors Create')]
    #[Description('Expenses Vendors Create')]
    case ExpensesVendorsCreate = 'expenses.vendors.create';

    /**
     * `expenses.vendors.update` — Expenses Vendors Update
     */
    #[Label('Expenses Vendors Update')]
    #[Description('Expenses Vendors Update')]
    case ExpensesVendorsUpdate = 'expenses.vendors.update';

    /**
     * `expenses.vendors.archive` — Expenses Vendors Archive
     */
    #[Label('Expenses Vendors Archive')]
    #[Description('Expenses Vendors Archive')]
    case ExpensesVendorsArchive = 'expenses.vendors.archive';

    /**
     * `platform.expenses.audit` — Cross-tenant expense audit.
     */
    #[Label('Cross-tenant expense audit')]
    #[Description('Cross-tenant expense audit. Aggregate totals only; no receipts.')]
    case PlatformExpensesAudit = 'platform.expenses.audit';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::ExpensesViewAny => Guard::Sanctum,
            self::ExpensesView => Guard::Sanctum,
            self::ExpensesSubmit => Guard::Sanctum,
            self::ExpensesApprove => Guard::Sanctum,
            self::ExpensesReject => Guard::Sanctum,
            self::ExpensesMarkPaid => Guard::Sanctum,
            self::ExpensesCancel => Guard::Sanctum,
            self::ExpensesDelete => Guard::Sanctum,
            self::ExpensesAttachmentsView => Guard::Sanctum,
            self::ExpensesAttachmentsCreate => Guard::Sanctum,
            self::ExpensesAttachmentsDelete => Guard::Sanctum,
            self::ExpensesPayrollViewAny => Guard::Sanctum,
            self::ExpensesPayrollView => Guard::Sanctum,
            self::ExpensesPayrollCreate => Guard::Sanctum,
            self::ExpensesPayrollApprove => Guard::Sanctum,
            self::ExpensesVendorsViewAny => Guard::Sanctum,
            self::ExpensesVendorsView => Guard::Sanctum,
            self::ExpensesVendorsCreate => Guard::Sanctum,
            self::ExpensesVendorsUpdate => Guard::Sanctum,
            self::ExpensesVendorsArchive => Guard::Sanctum,
            self::PlatformExpensesAudit => Guard::PlatformAdmin,
        };
    }
}
