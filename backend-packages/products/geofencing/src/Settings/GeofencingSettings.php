<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py products geofencing --force`.

declare(strict_types=1);

namespace Academorix\Geofencing\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * GeofencingSettings — Geofencing.
 *
 * Tenant-wide defaults for point-in-polygon evaluation + retention.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Geofencing
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'geofencing',
    label: 'Geofencing',
    description: 'Tenant-wide defaults for point-in-polygon evaluation + retention.',
    icon: 'map-pin',
    scope: 'tenant',
    permission: 'tenant_locales.viewAny',
    sortOrder: 24,
)]
class GeofencingSettings extends Settings
{

    // ── Defaults ──────────────────────────────────
    #[SettingGroup(label: 'Defaults', description: 'Values used when a branch row doesn\'t specify its own.', icon: 'adjustments-horizontal', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Number, label: 'Default accuracy tolerance (m)', group: 'Defaults', sortOrder: 1, min: 1, max: 5000, helpText: 'Reject GPS readings noisier than this. Consumer sees SKIPPED.')]
    public int $default_accuracy_tolerance_m = 50;

    #[SettingField(controlType: ControlType::Number, label: 'Default radius (m)', group: 'Defaults', sortOrder: 2, min: 1, max: 100000, helpText: 'Used when a branch has no polygon.')]
    public int $default_radius_m = 100;


    // ── Retention ──────────────────────────────────
    #[SettingGroup(label: 'Retention', description: 'How long evaluation audit rows persist. Non-override rows configurable; override rows fixed to 10 years.', icon: 'clock', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Number, label: 'Retention (years)', group: 'Retention', sortOrder: 1, min: 3, max: 25, helpText: 'Minimum 3 years to meet US FLSA. Maximum 25 to cap storage costs.')]
    public int $retention_years = 7;


    // ── Enforcement ──────────────────────────────────
    #[SettingGroup(label: 'Enforcement', description: 'Master toggle for the enforcement UX. When off, evaluations still record on the audit log but consumers ignore the OUTSIDE verdict — useful for shadow-mode deployments.', icon: 'shield-check', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enforce geofence verdicts', group: 'Enforcement', sortOrder: 1)]
    public bool $enforcement_enabled = false;

    #[SettingField(controlType: ControlType::Toggle, label: 'Email tenant admins when an override is requested', group: 'Enforcement', sortOrder: 2)]
    public bool $notify_admins_on_override_request = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'geofencing';
    }
}
