<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py products geofencing --force`.

declare(strict_types=1);

namespace Academorix\Geofencing\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * GeofenceService::evaluate() persisted a row (any leaf: INSIDE / OUTSIDE / SKIPPED / ERROR).
 *
 * ## Consumers
 *
 * - geofencing::LogGeofenceEvaluationForAudit (internal audit trail)
 * - geofencing::GeofenceMetricsRecorder (metric emission)
 * - staff-clock-in-out::StampGeofenceCheckOnClockIn (subject_type='staff_clockin')
 * - athlete-self-check-in::StampGeofenceCheckOnCheckIn (subject_type='attendance_submission')
 * - visitor-auto-log::StampGeofenceCheckOnVisit (subject_type='reception_visit')
 *
 * @category Geofencing
 *
 * @since    0.1.0
 */
final readonly class GeofenceEvaluated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $check  GeofenceCheck (persisted model instance) — carries fenceable_type + fenceable_id + subject_type + subject_id
     * @param  string  $input  EvaluateGeofenceData (originating DTO) — carries fenceable_type + fenceable_id + lat + lng + accuracy_m + subject_type + subject_id
     */
    public function __construct(
        public readonly string $check,
        public readonly string $input,
    ) {
    }
}
