<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Academorix\AthleteEnrollment\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Consent snapshot updated from a prior state where required consents were missing to a state where they're recorded. Distinct from athlete::AthleteConsentRecorded because the enrollment observes it in its own context.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchConsentReceivedNotification (to admin — enrollment can now transition to confirmed)
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
final readonly class EnrollmentConsentReceived implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $enrollmentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  array  $consentSnapshot  object
     * @param  string  $receivedAt  ISO 8601
     */
    public function __construct(
        public readonly string $enrollmentId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly array $consentSnapshot,
        public readonly string $receivedAt,
    ) {
    }
}
