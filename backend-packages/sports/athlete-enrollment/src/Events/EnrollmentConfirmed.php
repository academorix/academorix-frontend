<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Academorix\AthleteEnrollment\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Status transitioned {submitted, payment_pending, consent_pending} → confirmed. All preconditions (payment + consent) met. Consent snapshot captured at this transition.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchEnrollmentConfirmedNotification (to primary guardian + admin + coach)
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EnrollmentConfirmed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $enrollmentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $teamId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $confirmedAt  ISO 8601
     * @param  string  $confirmedByUserId  ULID
     * @param  array  $consentSnapshot  object (from athlete's consent state at this moment)
     */
    public function __construct(
        public readonly string $enrollmentId,
        public readonly string $tenantId,
        public readonly string $teamId,
        public readonly string $athleteId,
        public readonly string $confirmedAt,
        public readonly string $confirmedByUserId,
        public readonly array $consentSnapshot,
    ) {
    }
}
