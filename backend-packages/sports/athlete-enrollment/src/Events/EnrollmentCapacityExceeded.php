<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Academorix\AthleteEnrollment\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Recoverable event. Fires when an enrollment is submitted for a team at capacity + waitlist entitlement is on — the observer moves the enrollment to waitlisted. When waitlist entitlement is off (Small tier), the observer refuses the submit with TEAM_AT_CAPACITY error and this event doesn't fire.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchCapacityExceededNotification (to admin — informational)
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
final readonly class EnrollmentCapacityExceeded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $capacity  integer
     * @param  int  $currentActiveCount  integer
     * @param  string  $waitlistedEnrollmentId  ULID
     */
    public function __construct(
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly int $capacity,
        public readonly int $currentActiveCount,
        public readonly string $waitlistedEnrollmentId,
    ) {
    }
}
