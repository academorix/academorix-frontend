<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Academorix\AthleteEnrollment\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * In the same transaction as EnrollmentActivated, the ConvertEnrollmentToTeamMemberAtomically hook created a teams::TeamMember row. Fires alongside teams::TeamMemberJoined so downstream consumers see both.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - cache::InvalidateTeamRosterCache
 * - cache::InvalidateTeamMemberCache
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
final readonly class EnrollmentTeamMemberCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $enrollmentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $teamId  ULID
     * @param  string  $teamMemberId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $memberType  'athlete'
     */
    public function __construct(
        public readonly string $enrollmentId,
        public readonly string $tenantId,
        public readonly string $teamId,
        public readonly string $teamMemberId,
        public readonly string $athleteId,
        public readonly string $memberType,
    ) {
    }
}
