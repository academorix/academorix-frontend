<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Academorix\AthleteEnrollment\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan enrollment:list` — List every enrollment row.
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'enrollment:list',
    description: 'List every enrollment row.',
)]
final class ListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List every enrollment row', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List every enrollment row completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
