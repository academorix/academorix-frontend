<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Any Event scalar field mutated except the specific-transition fields (status, visibility, starts_at, ends_at, branch_id, entry_fee_cents, min_age, max_age, age_group_ids). Umbrella event for renames, description edits, hero_image_url changes, sort tweaks.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Event
 *
 * @since    0.1.0
 */
final readonly class EventUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedFields  string[]
     * @param  string  $updatedBy  ULID
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $changedFields,
        public readonly string $updatedBy,
    ) {
    }
}
