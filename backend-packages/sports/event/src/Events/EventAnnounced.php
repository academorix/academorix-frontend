<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Event.status transitioned draft -> announced. First point at which the event is visible on public/tenant surfaces. Fires alongside EventStatusChanged.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchEventAnnouncedNotification (opt-in to tenant members)
 *
 * @category Event
 *
 * @since    0.1.0
 */
final readonly class EventAnnounced implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $announcedAt  ISO 8601
     * @param  string  $visibility  string
     * @param  string  $trigger  'manual_admin'
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $announcedAt,
        public readonly string $visibility,
        public readonly string $trigger,
    ) {
    }
}
