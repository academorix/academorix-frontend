<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Event.status transitioned to cancelled (from any pre-completed state). Requires cancellation_reason. Cannot be reversed except by restore-to-draft flow.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — refund + registrant records outlive event)
 * - activity::WriteToActivityLog
 *
 * @category Event
 *
 * @since    0.1.0
 */
final readonly class EventCancelled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $cancelledAt  ISO 8601
     * @param  string  $cancelledBy  ULID
     * @param  string  $cancellationReason  string
     * @param  int  $registeredCountAtCancel  integer
     * @param  string  $auditSeverity  warn
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $cancelledAt,
        public readonly string $cancelledBy,
        public readonly string $cancellationReason,
        public readonly int $registeredCountAtCancel,
        public readonly string $auditSeverity,
    ) {
    }
}
