<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Event.status transitioned completed -> archived OR cancelled -> archived. Soft-delete; the row is hidden from tenant selectors + registration flows but readable via `?include_archived=true`.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Event
 *
 * @since    0.1.0
 */
final readonly class EventArchived implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $archivedAt  ISO 8601
     * @param  string  $archivedBy  ULID
     * @param  string  $reason  string | null
     * @param  string  $hardDeleteScheduledFor  ISO 8601
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $archivedAt,
        public readonly string $archivedBy,
        public readonly string $reason,
        public readonly string $hardDeleteScheduledFor,
    ) {
    }
}
