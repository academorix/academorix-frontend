<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Event module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Event
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum EventPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `events.viewAny` — List events.
     */
    #[Label('List events')]
    #[Description('List events. Row-level visibility (public / tenant_only / participants_only / private) filters what each role actually sees.')]
    case EventsViewAny = 'events.viewAny';

    /**
     * `events.viewAny.tenant` — Events ViewAny Tenant
     */
    #[Label('Events ViewAny Tenant')]
    #[Description('Events ViewAny Tenant')]
    case EventsViewAnyTenant = 'events.viewAny.tenant';

    /**
     * `events.view` — Single-event read.
     */
    #[Label('Single-event read')]
    #[Description('Single-event read. Visibility rules still apply.')]
    case EventsView = 'events.view';

    /**
     * `events.viewPrivate` — Read events with visibility='private'.
     */
    #[Label('Read events with visibility=\'private\'')]
    #[Description('Read events with visibility=\'private\'. Admin + owner only — private events are admin-facing dry runs / internal-only.')]
    case EventsViewPrivate = 'events.viewPrivate';

    /**
     * `events.create` — Consumes 1 unit of the event_slot entitlement per overlapping calendar year.
     */
    #[Label('Consumes 1 unit of the event_slot entitlement per overlapping calendar year')]
    #[Description('Consumes 1 unit of the event_slot entitlement per overlapping calendar year.')]
    case EventsCreate = 'events.create';

    /**
     * `events.update` — Mutable fields depend on status.
     */
    #[Label('Mutable fields depend on status')]
    #[Description('Mutable fields depend on status. See routes.json PATCH endpoint for the immutability matrix.')]
    case EventsUpdate = 'events.update';

    /**
     * `events.dates.shift` — Owner-only.
     */
    #[Label('Owner-only')]
    #[Description('Owner-only. Permits starts_at / ends_at changes on Events with status IN (announced, registration_open, registration_closed, in_progress). Fires EventRescheduled (audit-warn) + cannot-opt-out notification to registered.')]
    case EventsDatesShift = 'events.dates.shift';

    /**
     * `events.venue.change` — Owner-only.
     */
    #[Label('Owner-only')]
    #[Description('Owner-only. Permits branch_id + primary facility change post-registration_open. Fires EventVenueChanged.')]
    case EventsVenueChange = 'events.venue.change';

    /**
     * `events.pricing.change` — Owner-only.
     */
    #[Label('Owner-only')]
    #[Description('Owner-only. Permits entry_fee_cents / late_registration_fee_cents / prize_pool_cents change post-registration_open. Financial-audit-critical — every fire retained 7 years.')]
    case EventsPricingChange = 'events.pricing.change';

    /**
     * `events.eligibility.narrow` — Owner-only.
     */
    #[Label('Owner-only')]
    #[Description('Owner-only. Permits narrowing min_age / max_age / age_group_ids post-registration_open. Fires EventMinorEligibilityCorrected when the narrowing excludes existing registrants.')]
    case EventsEligibilityNarrow = 'events.eligibility.narrow';

    /**
     * `events.delete` — Owner-only.
     */
    #[Label('Owner-only')]
    #[Description('Owner-only. Soft-delete (archive path). Hard-delete via retention purge only.')]
    case EventsDelete = 'events.delete';

    /**
     * `events.announce` — Events Announce
     */
    #[Label('Events Announce')]
    #[Description('Events Announce')]
    case EventsAnnounce = 'events.announce';

    /**
     * `events.open_registration` — Events Open Registration
     */
    #[Label('Events Open Registration')]
    #[Description('Events Open Registration')]
    case EventsOpenRegistration = 'events.open_registration';

    /**
     * `events.close_registration` — Events Close Registration
     */
    #[Label('Events Close Registration')]
    #[Description('Events Close Registration')]
    case EventsCloseRegistration = 'events.close_registration';

    /**
     * `events.start` — Events Start
     */
    #[Label('Events Start')]
    #[Description('Events Start')]
    case EventsStart = 'events.start';

    /**
     * `events.complete` — Events Complete
     */
    #[Label('Events Complete')]
    #[Description('Events Complete')]
    case EventsComplete = 'events.complete';

    /**
     * `events.cancel` — Fires EventCancelled + EventCancellationBroadcast (cannot-opt-out notification to registered).
     */
    #[Label('Fires EventCancelled + EventCancellationBroadcast (cannot-opt-out notification t')]
    #[Description('Fires EventCancelled + EventCancellationBroadcast (cannot-opt-out notification to registered).')]
    case EventsCancel = 'events.cancel';

    /**
     * `events.archive` — Events Archive
     */
    #[Label('Events Archive')]
    #[Description('Events Archive')]
    case EventsArchive = 'events.archive';

    /**
     * `events.restore` — Events Restore
     */
    #[Label('Events Restore')]
    #[Description('Events Restore')]
    case EventsRestore = 'events.restore';

    /**
     * `events.publish` — Set visibility='public'.
     */
    #[Label('Set visibility=\'public\'')]
    #[Description('Set visibility=\'public\'. Gated by event_public_visibility entitlement.')]
    case EventsPublish = 'events.publish';

    /**
     * `events.unpublish` — Events Unpublish
     */
    #[Label('Events Unpublish')]
    #[Description('Events Unpublish')]
    case EventsUnpublish = 'events.unpublish';

    /**
     * `events.facilities.attach` — Events Facilities Attach
     */
    #[Label('Events Facilities Attach')]
    #[Description('Events Facilities Attach')]
    case EventsFacilitiesAttach = 'events.facilities.attach';

    /**
     * `events.facilities.detach` — Events Facilities Detach
     */
    #[Label('Events Facilities Detach')]
    #[Description('Events Facilities Detach')]
    case EventsFacilitiesDetach = 'events.facilities.detach';

    /**
     * `events.facilities.set_primary` — Events Facilities Set Primary
     */
    #[Label('Events Facilities Set Primary')]
    #[Description('Events Facilities Set Primary')]
    case EventsFacilitiesSetPrimary = 'events.facilities.set_primary';

    /**
     * `events.livestream.manage` — Manage livestream_url + livestream_password.
     */
    #[Label('Manage livestream_url + livestream_password')]
    #[Description('Manage livestream_url + livestream_password. Gated by event_livestream entitlement.')]
    case EventsLivestreamManage = 'events.livestream.manage';

    /**
     * `events.enroll.late` — Permission for the downstream enrollment surface (Wave 3+) to accept event registrations in the late-registration window.
     */
    #[Label('Permission for the downstream enrollment surface (Wave 3+) to accept event regis')]
    #[Description('Permission for the downstream enrollment surface (Wave 3+) to accept event registrations in the late-registration window. NOT seeded on the reception role by default — admins explicitly grant it.')]
    case EventsEnrollLate = 'events.enroll.late';

    /**
     * `events.manage` — Umbrella permission for the settings blueprint gate.
     */
    #[Label('Umbrella permission for the settings blueprint gate')]
    #[Description('Umbrella permission for the settings blueprint gate. Admin + owner.')]
    case EventsManage = 'events.manage';

    /**
     * `platform.events.viewAny` — Platform Events ViewAny
     */
    #[Label('Platform Events ViewAny')]
    #[Description('Platform Events ViewAny')]
    case PlatformEventsViewAny = 'platform.events.viewAny';

    /**
     * `platform.events.view` — Platform Events View
     */
    #[Label('Platform Events View')]
    #[Description('Platform Events View')]
    case PlatformEventsView = 'platform.events.view';

    /**
     * `platform.events.force_cancel` — Force-cancel escape hatch.
     */
    #[Label('Force-cancel escape hatch')]
    #[Description('Force-cancel escape hatch. Two-person path — ops must document the incident + get super_admin approval. Fires EventCancelled + EventCancellationBroadcast.')]
    case PlatformEventsForceCancel = 'platform.events.force_cancel';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::EventsViewAny => Guard::Sanctum,
            self::EventsViewAnyTenant => Guard::Sanctum,
            self::EventsView => Guard::Sanctum,
            self::EventsViewPrivate => Guard::Sanctum,
            self::EventsCreate => Guard::Sanctum,
            self::EventsUpdate => Guard::Sanctum,
            self::EventsDatesShift => Guard::Sanctum,
            self::EventsVenueChange => Guard::Sanctum,
            self::EventsPricingChange => Guard::Sanctum,
            self::EventsEligibilityNarrow => Guard::Sanctum,
            self::EventsDelete => Guard::Sanctum,
            self::EventsAnnounce => Guard::Sanctum,
            self::EventsOpenRegistration => Guard::Sanctum,
            self::EventsCloseRegistration => Guard::Sanctum,
            self::EventsStart => Guard::Sanctum,
            self::EventsComplete => Guard::Sanctum,
            self::EventsCancel => Guard::Sanctum,
            self::EventsArchive => Guard::Sanctum,
            self::EventsRestore => Guard::Sanctum,
            self::EventsPublish => Guard::Sanctum,
            self::EventsUnpublish => Guard::Sanctum,
            self::EventsFacilitiesAttach => Guard::Sanctum,
            self::EventsFacilitiesDetach => Guard::Sanctum,
            self::EventsFacilitiesSetPrimary => Guard::Sanctum,
            self::EventsLivestreamManage => Guard::Sanctum,
            self::EventsEnrollLate => Guard::Sanctum,
            self::EventsManage => Guard::Sanctum,
            self::PlatformEventsViewAny => Guard::PlatformAdmin,
            self::PlatformEventsView => Guard::PlatformAdmin,
            self::PlatformEventsForceCancel => Guard::PlatformAdmin,
        };
    }
}
