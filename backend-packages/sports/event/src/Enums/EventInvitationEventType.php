<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `event_type` on `event-invitation`.
 *
 * Polymorphic activity type: events / sessions / matches / training_sessions.
 *
 * @category Event
 *
 * @since    0.1.0
 */
enum EventInvitationEventType: string
{
    use Enum;

    case Events = 'events';
    case Sessions = 'sessions';
    case Matches = 'matches';
    case TrainingSessions = 'training_sessions';
}
