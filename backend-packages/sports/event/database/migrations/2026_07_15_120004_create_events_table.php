<?php

/**
 * @file database/migrations/2026_07_15_120004_create_events_table.php
 *
 * @description
 * Create the `events` table for the `Event` entity.
 * Owned by the event module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports event --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('events', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('organization_id', 64);
            $table->foreign('organization_id')->references('id')->on('organizations')->onDelete('restrict');
            $table->string('branch_id', 64)->nullable();
            $table->foreign('branch_id')->references('id')->on('branches')->onDelete('restrict');
            $table->string('season_id', 64)->nullable();
            $table->foreign('season_id')->references('id')->on('seasons')->onDelete('restrict');
            $table->string('primary_sport_key', 32)->nullable();
            $table->string('name', 128);
            $table->string('slug', 64);
            $table->string('event_type', 24);
            $table->string('format', 24)->nullable();
            $table->text('description')->nullable();
            $table->text('private_notes')->nullable();
            $table->string('visibility', 24)->default('tenant_only');
            $table->string('status', 24)->default('draft');
            $table->timestampTz('starts_at');
            $table->timestampTz('ends_at');
            $table->string('timezone', 64)->nullable();
            $table->timestampTz('registration_opens_at')->nullable();
            $table->timestampTz('registration_closes_at')->nullable();
            $table->integer('max_participants')->nullable();
            $table->integer('max_teams')->nullable();
            $table->integer('min_age')->nullable();
            $table->integer('max_age')->nullable();
            $table->jsonb('age_group_ids')->nullable();
            $table->string('gender_category', 16)->nullable();
            $table->integer('entry_fee_cents')->default(0);
            $table->string('entry_fee_currency', 3)->nullable();
            $table->integer('prize_pool_cents')->nullable();
            $table->string('prize_pool_currency', 3)->nullable();
            $table->string('livestream_url', 512)->nullable();
            $table->string('livestream_password', 128)->nullable();
            $table->string('hero_image_url', 512)->nullable();
            $table->string('registration_form_id', 64)->nullable();
            $table->boolean('allow_late_registration')->default(false);
            $table->integer('late_registration_fee_cents')->default(0);
            $table->text('cancellation_reason')->nullable();
            $table->timestampTz('cancelled_at')->nullable();
            $table->string('cancelled_by_user_id', 64)->nullable();
            $table->foreign('cancelled_by_user_id')->references('id')->on('users')->onDelete('restrict');
            $table->jsonb('sponsors')->nullable();
            $table->string('rules_document_url', 512)->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 64)->nullable();
            $table->string('updated_by', 64)->nullable();
            $table->string('deleted_by', 64)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'organization_id', 'slug'], 'events_scope_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'status'], 'events_status_idx');
            $table->index(['tenant_id', 'starts_at'], 'events_starts_at_idx');
            $table->index(['tenant_id', 'organization_id', 'starts_at'], 'events_org_starts_at_idx');
            $table->index(['tenant_id', 'branch_id', 'starts_at'], 'events_branch_starts_at_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE branch_id IS NOT NULL
            $table->index(['tenant_id', 'season_id'], 'events_season_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE season_id IS NOT NULL
            $table->index(['tenant_id', 'visibility', 'starts_at'], 'events_visibility_starts_at_idx');
            $table->index(['tenant_id', 'primary_sport_key'], 'events_sport_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE primary_sport_key IS NOT NULL
            $table->index(['tenant_id', 'event_type', 'starts_at'], 'events_type_starts_at_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('events');
    }
};
