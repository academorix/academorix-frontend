<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /matches/{match}/cancel. Terminal. Match is preserved for audit; no future rescheduling.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchMatchCancelledNotification
 * - monitoring::MetricsCollector
 *
 * @category Match
 *
 * @since    0.1.0
 */
final readonly class MatchCancelled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $matchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $reason  string | null
     * @param  string  $cancelledByUserId  ULID
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $matchId,
        public readonly string $tenantId,
        public readonly string $reason,
        public readonly string $cancelledByUserId,
        public readonly string $at,
    ) {
    }
}
