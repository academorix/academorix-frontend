<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /matches/{match}/lineup/publish committed. lineup_published_at set. Kicks parent-facing notify + attendance auto-materialisation.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - match::NotifyMatchLineupPublishedJob (dispatches parent + squad-athlete notifications)
 * - match::MaterialiseAttendanceFromLineupJob (creates pre-filled attendance rows from the published squad)
 * - monitoring::MetricsCollector (increments academorix.matches.lineup_published_total)
 *
 * @category Match
 *
 * @since    0.1.0
 */
final readonly class MatchLineupPublished implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $matchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $teamId  ULID | null
     * @param  string  $squadAthleteIds  ULID[] (ath_*)
     * @param  string  $publishedAt  ISO 8601
     * @param  string  $publishedByUserId  ULID
     */
    public function __construct(
        public readonly string $matchId,
        public readonly string $tenantId,
        public readonly string $teamId,
        public readonly string $squadAthleteIds,
        public readonly string $publishedAt,
        public readonly string $publishedByUserId,
    ) {
    }
}
