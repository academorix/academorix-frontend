<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * match_results.motm_athlete_id was set (or changed) while published_at IS NULL. Distinct from MatchResultPublished so the awards module can react without waiting for the full publish.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchMatchMotmAwardedNotification (opt-in to the awarded athlete's family)
 * - monitoring::MetricsCollector
 *
 * @category Match
 *
 * @since    0.1.0
 */
final readonly class MatchMotmAwarded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $matchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $motmAthleteId  ULID (ath_*)
     * @param  string  $awardedByUserId  ULID
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $matchId,
        public readonly string $tenantId,
        public readonly string $motmAthleteId,
        public readonly string $awardedByUserId,
        public readonly string $at,
    ) {
    }
}
