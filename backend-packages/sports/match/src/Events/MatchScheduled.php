<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MatchObserver.creating committed a new matches row. Attaches to a Competition when competition_id IS NOT NULL.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - match::CacheOpponentLogoOnMatchScheduled (writes / bumps the opponent_logos row for the named opponent)
 * - monitoring::MetricsCollector (increments academorix.matches.scheduled_total)
 *
 * @category Match
 *
 * @since    0.1.0
 */
final readonly class MatchScheduled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $matchId  ULID (mat_*)
     * @param  string  $tenantId  ULID
     * @param  string  $seasonId  ULID
     * @param  string  $sportKey  string
     * @param  string  $teamId  ULID | null
     * @param  string  $competitionId  ULID | null
     * @param  string  $branchId  ULID
     * @param  string  $facilityId  ULID | null
     * @param  string  $opponentName  string | null
     * @param  string  $kickoffAt  ISO 8601
     * @param  string  $scheduledAt  ISO 8601
     */
    public function __construct(
        public readonly string $matchId,
        public readonly string $tenantId,
        public readonly string $seasonId,
        public readonly string $sportKey,
        public readonly string $teamId,
        public readonly string $competitionId,
        public readonly string $branchId,
        public readonly string $facilityId,
        public readonly string $opponentName,
        public readonly string $kickoffAt,
        public readonly string $scheduledAt,
    ) {
    }
}
