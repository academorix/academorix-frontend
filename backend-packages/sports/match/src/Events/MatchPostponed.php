<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /matches/{match}/postpone. Match keeps its identity + squad + notes but kickoff_at is re-planned. Not terminal.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchMatchPostponedNotification (families + squad athletes + public site)
 * - monitoring::MetricsCollector
 *
 * @category Match
 *
 * @since    0.1.0
 */
final readonly class MatchPostponed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $matchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $reason  string | null
     * @param  string  $oldKickoffAt  ISO 8601
     * @param  string  $newKickoffAt  ISO 8601 | null (null when re-scheduling is deferred)
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $matchId,
        public readonly string $tenantId,
        public readonly string $reason,
        public readonly string $oldKickoffAt,
        public readonly string $newKickoffAt,
        public readonly string $at,
    ) {
    }
}
