<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Actions\Tenant;

use Academorix\Match\Contracts\Repositories\OpponentLogoRepositoryInterface;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Delete;
use Illuminate\Http\Response;

/**
 * `DELETE /api/v1/opponent-logos/{opponent_logo}` — delete action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Delete(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Match
 *
 * @since    0.1.0
 */
#[AsController]
#[Delete('/api/v1/opponent-logos/{opponent_logo}')]
final class DeleteOpponentLogoAction
{
    public function __construct(
        private readonly OpponentLogoRepositoryInterface $repository,
    ) {
    }

    /**
     * Soft-delete one `opponent-logo` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): Response
    {
        $this->repository->delete($id);

        return response()->noContent();
    }
}
