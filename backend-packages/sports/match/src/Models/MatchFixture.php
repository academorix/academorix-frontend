<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Attributes\WithoutIncrementing;
use Illuminate\Database\Eloquent\Model;
use Academorix\Match\Contracts\Data\MatchFixtureInterface;
use Academorix\Match\Database\Factories\MatchFixtureFactory;
use Academorix\Branch\Concerns\BelongsToBranch;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Match\Policies\MatchFixturePolicy;
use Academorix\Season\Concerns\BelongsToSeason;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Attributes\UsePolicy;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Laravel\Scout\Searchable;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use OwenIt\Auditing\Contracts\Auditable as AuditableContract;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a MatchFixture.
 *
 * Canonical competitive-match row.
 *
 * @category Match
 *
 * @since    0.1.0
 */
#[Table(name: MatchFixtureInterface::TABLE, key: MatchFixtureInterface::PRIMARY_KEY, keyType: MatchFixtureInterface::KEY_TYPE)]
#[Fillable([
    MatchFixtureInterface::ATTR_TENANT_ID,
        MatchFixtureInterface::ATTR_SEASON_ID,
        MatchFixtureInterface::ATTR_SPORT_KEY,
        MatchFixtureInterface::ATTR_TEAM_ID,
        MatchFixtureInterface::ATTR_COMPETITION_ID,
        MatchFixtureInterface::ATTR_BRANCH_ID,
        MatchFixtureInterface::ATTR_FACILITY_ID,
        MatchFixtureInterface::ATTR_OPPONENT_NAME,
        MatchFixtureInterface::ATTR_OPPONENT_LOGO_ID,
        MatchFixtureInterface::ATTR_HOME_OR_AWAY,
        MatchFixtureInterface::ATTR_KICKOFF_AT,
        MatchFixtureInterface::ATTR_STATUS,
        MatchFixtureInterface::ATTR_LINEUP_PUBLISHED_AT,
        MatchFixtureInterface::ATTR_RESULT_SUMMARY_JSON,
        MatchFixtureInterface::ATTR_NOTES_SNAPSHOT,
        MatchFixtureInterface::ATTR_METADATA,
])]
#[UseFactory(MatchFixtureFactory::class)]
#[WithoutIncrementing]
#[UsePolicy(MatchFixturePolicy::class)]
final class MatchFixture extends Model implements MatchFixtureInterface, AuditableContract
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    use BelongsToBranch;
    use BelongsToSeason;
    // TODO(gen): resolve unknown trait `HasMatchParticipants` — add its import + `use` line.
    // TODO(gen): resolve unknown trait `HasMatchSquadEntries` — add its import + `use` line.
    // TODO(gen): resolve unknown trait `HasMatchResults` — add its import + `use` line.
    // TODO(gen): resolve unknown trait `HasMatchEvents` — add its import + `use` line.
    // TODO(gen): resolve unknown trait `HasMatchNotes` — add its import + `use` line.
    use HasMetadata;
    use Userstamps;
    use Auditable;
    use LogsActivity;
    use Filterable;
    use Searchable;
    use SoftDeletes;

    /**
     * Cast map — from the blueprint's `x-eloquent.casts`.
     *
     * @var array<string, string>
     */
    protected $casts = [
        MatchFixtureInterface::ATTR_HOME_OR_AWAY => MatchHomeOrAway::class,
        MatchFixtureInterface::ATTR_STATUS => MatchStatus::class,
        MatchFixtureInterface::ATTR_KICKOFF_AT => 'datetime',
        MatchFixtureInterface::ATTR_LINEUP_PUBLISHED_AT => 'datetime',
        MatchFixtureInterface::ATTR_RESULT_SUMMARY_JSON => 'array',
        MatchFixtureInterface::ATTR_METADATA => 'array',
    ];
}
