<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports private-sessions --force`.

declare(strict_types=1);

namespace Academorix\PrivateSessions\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\PrivateSessions\Contracts\Data\SessionCreditInterface;
use Academorix\PrivateSessions\Database\Factories\SessionCreditFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Foundation\Concerns\HasPrefixedUlid;
use Academorix\Sports\Athlete\Concerns\BelongsToAthlete;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a SessionCredit.
 *
 * Session-pack credit ledger — parent bought 10-pack; each private-session consume atomically decrements via SELECT FOR UPDATE.
 *
 * @category PrivateSessions
 *
 * @since    0.1.0
 */
#[Table(name: SessionCreditInterface::TABLE, keyType: SessionCreditInterface::KEY_TYPE)]
#[Fillable([
    SessionCreditInterface::ATTR_TENANT_ID,
        SessionCreditInterface::ATTR_ATHLETE_ID,
        SessionCreditInterface::ATTR_PURCHASED_BY_USER_ID,
        SessionCreditInterface::ATTR_KIND,
        SessionCreditInterface::ATTR_TOTAL_PURCHASED,
        SessionCreditInterface::ATTR_TOTAL_CONSUMED,
        SessionCreditInterface::ATTR_EXPIRES_AT,
        SessionCreditInterface::ATTR_INVOICE_ID,
        SessionCreditInterface::ATTR_STATUS,
        SessionCreditInterface::ATTR_METADATA,
])]
#[UseFactory(SessionCreditFactory::class)]
final class SessionCredit extends Model implements SessionCreditInterface
{
    use HasFactory;
    use HasPrefixedUlid;
    use BelongsToTenant;
    use BelongsToAthlete;
    use HasMetadata;
    use HasUserstamps;
    use Auditable;
    use HasActivityLog;
    use Filterable;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

}
