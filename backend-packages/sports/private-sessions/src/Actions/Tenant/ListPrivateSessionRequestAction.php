<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports private-sessions --force`.

declare(strict_types=1);

namespace Academorix\PrivateSessions\Actions\Tenant;

use Academorix\PrivateSessions\Contracts\Repositories\PrivateSessionRequestRepositoryInterface;
use Academorix\PrivateSessions\Data\PrivateSessionRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Get;
use Illuminate\Http\Request;
use Illuminate\Pagination\LengthAwarePaginator;
use Spatie\LaravelData\PaginatedDataCollection;

/**
 * `GET /api/v1/private-session-requests` — list action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category PrivateSessions
 *
 * @since    0.1.0
 */
#[AsAction(name: 'private_sessions.private_session_requests.list')]
#[Get('/api/v1/private-session-requests')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ListPrivateSessionRequestAction
{
    use AsController;

    public function __construct(
        private readonly PrivateSessionRequestRepositoryInterface $repository,
    ) {
    }

    /**
     * List `private-session-requests` for the current caller.
     *
     * The `Repository` base picks up `filter[...]` / `sort=...` /
     * `include=...` query parameters via the `#[Filterable]` attribute
     * on the concrete repository — no per-Action wiring needed.
     *
     * @return PaginatedDataCollection<int, PrivateSessionRequestData>  Paginated wire-visible DTOs.
     */
    public function __invoke(Request $request): PaginatedDataCollection
    {
        /** @var LengthAwarePaginator<int, \Academorix\PrivateSessions\Models\PrivateSessionRequest> $page */
        $page = $this->repository->paginate(
            perPage: (int) $request->integer('per_page', 15),
        );

        return PrivateSessionRequestData::collect($page, PaginatedDataCollection::class);
    }
}
