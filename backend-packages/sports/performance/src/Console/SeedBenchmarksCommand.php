<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports performance --force`.

declare(strict_types=1);

namespace Academorix\Performance\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan performance:seed-benchmarks` — Seed benchmarks — performance command.
 *
 * @category Performance
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'performance:seed-benchmarks',
    description: 'Seed benchmarks — performance command.',
)]
final class SeedBenchmarksCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Seed benchmarks — performance command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Seed benchmarks — performance command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
