<?php

/**
 * @file database/migrations/2026_07_15_120001_create_certificates_table.php
 *
 * @description
 * Create the `certificates` table for the `Certificate` entity.
 * Owned by the awards module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports awards --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports awards --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('certificates', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('award_id', 30);
            $table->foreign('award_id')->references('id')->on('awards')->onDelete('cascade');
            $table->string('template_key', 64);
            $table->string('issued_to_name', 200);
            $table->timestampTz('issued_at');
            $table->string('issued_by_user_id', 30)->nullable();
            $table->foreign('issued_by_user_id')->references('id')->on('users')->onDelete('set null');
            $table->string('document_id', 30)->nullable();
            $table->foreign('document_id')->references('id')->on('files')->onDelete('set null');
            $table->string('share_signature', 128)->nullable();
            $table->timestampTz('share_expires_at')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->timestampTz('deleted_at')->nullable();
            $table->unique(['tenant_id', 'award_id'], 'certificates_award_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->unique(['share_signature'], 'certificates_share_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE share_signature IS NOT NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('certificates');
    }
};
