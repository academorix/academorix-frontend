<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports awards --force`.

declare(strict_types=1);

namespace Academorix\Awards\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Awards module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Awards
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum AwardsPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `awards.templates.viewAny` — Awards Templates ViewAny
     */
    #[Label('Awards Templates ViewAny')]
    #[Description('Awards Templates ViewAny')]
    case AwardsTemplatesViewAny = 'awards.templates.viewAny';

    /**
     * `awards.templates.view` — Awards Templates View
     */
    #[Label('Awards Templates View')]
    #[Description('Awards Templates View')]
    case AwardsTemplatesView = 'awards.templates.view';

    /**
     * `awards.templates.create` — Awards Templates Create
     */
    #[Label('Awards Templates Create')]
    #[Description('Awards Templates Create')]
    case AwardsTemplatesCreate = 'awards.templates.create';

    /**
     * `awards.templates.update` — Awards Templates Update
     */
    #[Label('Awards Templates Update')]
    #[Description('Awards Templates Update')]
    case AwardsTemplatesUpdate = 'awards.templates.update';

    /**
     * `awards.templates.delete` — Awards Templates Delete
     */
    #[Label('Awards Templates Delete')]
    #[Description('Awards Templates Delete')]
    case AwardsTemplatesDelete = 'awards.templates.delete';

    /**
     * `awards.viewAny` — Awards ViewAny
     */
    #[Label('Awards ViewAny')]
    #[Description('Awards ViewAny')]
    case AwardsViewAny = 'awards.viewAny';

    /**
     * `awards.view` — Awards View
     */
    #[Label('Awards View')]
    #[Description('Awards View')]
    case AwardsView = 'awards.view';

    /**
     * `awards.issue` — Manual award issuance path.
     */
    #[Label('Manual award issuance path')]
    #[Description('Manual award issuance path. Auto-issuance happens via events.')]
    case AwardsIssue = 'awards.issue';

    /**
     * `awards.revoke` — Reason REQUIRED.
     */
    #[Label('Reason REQUIRED')]
    #[Description('Reason REQUIRED. 7yr audit retention.')]
    case AwardsRevoke = 'awards.revoke';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::AwardsTemplatesViewAny => Guard::Sanctum,
            self::AwardsTemplatesView => Guard::Sanctum,
            self::AwardsTemplatesCreate => Guard::Sanctum,
            self::AwardsTemplatesUpdate => Guard::Sanctum,
            self::AwardsTemplatesDelete => Guard::Sanctum,
            self::AwardsViewAny => Guard::Sanctum,
            self::AwardsView => Guard::Sanctum,
            self::AwardsIssue => Guard::Sanctum,
            self::AwardsRevoke => Guard::Sanctum,
        };
    }
}
