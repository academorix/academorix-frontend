<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports awards --force`.

declare(strict_types=1);

namespace Academorix\Awards\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Award revoked (rare — usually after result amendment). Full audit chain.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention 7yr)
 * - notifications::NotifyAwardRevoked
 *
 * @category Awards
 *
 * @since    0.1.0
 */
final readonly class AwardRevoked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $awardId  ULID
     * @param  string  $revokedByUserId  ULID
     * @param  string  $reason  string
     */
    public function __construct(
        public readonly string $awardId,
        public readonly string $revokedByUserId,
        public readonly string $reason,
    ) {
    }
}
