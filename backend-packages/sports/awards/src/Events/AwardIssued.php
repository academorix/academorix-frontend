<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports awards --force`.

declare(strict_types=1);

namespace Academorix\Awards\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Award issued to athlete. Certificate render queued.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - jobs::RenderCertificateJob
 * - notifications::CelebrateAward
 *
 * @category Awards
 *
 * @since    0.1.0
 */
final readonly class AwardIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $awardId  ULID (awr_*)
     * @param  string  $templateId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $issuedAt  ISO 8601
     * @param  string  $certificateNumber  string
     */
    public function __construct(
        public readonly string $awardId,
        public readonly string $templateId,
        public readonly string $athleteId,
        public readonly string $issuedAt,
        public readonly string $certificateNumber,
    ) {
    }
}
