<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports age-group --force`.

declare(strict_types=1);

namespace Academorix\AgeGroup\Actions\Platform;

use Academorix\AgeGroup\Contracts\Repositories\AgeGroupRepositoryInterface;
use Academorix\AgeGroup\Data\AgeGroupData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Get;

/**
 * `GET /api/v1/platform/age-groups/{age_group}` — show action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category AgeGroup
 *
 * @since    0.1.0
 */
#[AsAction(name: 'age_group.age_groups.show')]
#[Get('/api/v1/platform/age-groups/{age_group}')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class ShowAgeGroupAction
{
    use AsController;

    public function __construct(
        private readonly AgeGroupRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `age-group` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): AgeGroupData
    {
        return AgeGroupData::from($this->repository->findOrFail($id));
    }
}
