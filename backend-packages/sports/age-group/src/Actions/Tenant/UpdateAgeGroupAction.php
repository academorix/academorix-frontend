<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports age-group --force`.

declare(strict_types=1);

namespace Academorix\AgeGroup\Actions\Tenant;

use Academorix\AgeGroup\Contracts\Repositories\AgeGroupRepositoryInterface;
use Academorix\AgeGroup\Data\AgeGroupData;
use Academorix\AgeGroup\Data\Requests\UpdateAgeGroupRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/age-groups/{age_group}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category AgeGroup
 *
 * @since    0.1.0
 */
#[AsAction(name: 'age_group.age_groups.update')]
#[Patch('/api/v1/age-groups/{age_group}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateAgeGroupAction
{
    use AsController;

    public function __construct(
        private readonly AgeGroupRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `age-group` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateAgeGroupRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateAgeGroupRequestData $data): AgeGroupData
    {
        $model = $this->repository->update($id, $data->toArray());

        return AgeGroupData::from($model);
    }
}
