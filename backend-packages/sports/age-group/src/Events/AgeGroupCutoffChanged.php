<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports age-group --force`.

declare(strict_types=1);

namespace Academorix\AgeGroup\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * cutoff_date_kind OR cutoff_month OR cutoff_day mutated. Audit-CRITICAL for the same reasoning as AgeGroupBoundsChanged — changing how age is calculated may reshape eligibility outcomes without touching the bounds themselves. (Example: switching from calendar_year to rolling_from_birthday on the same U12 group re-scores every borderline athlete.)
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - teams::MaybeInvalidateTeamRoster
 * - athlete-enrollment::MaybeInvalidateEnrollment
 *
 * @category AgeGroup
 *
 * @since    0.1.0
 */
final readonly class AgeGroupCutoffChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $ageGroupId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedBy  ULID
     * @param  string  $cutoffDateKind  { before: string, after: string }
     * @param  int  $cutoffMonth  { before: integer|null, after: integer|null }
     * @param  int  $cutoffDay  { before: integer|null, after: integer|null }
     */
    public function __construct(
        public readonly string $ageGroupId,
        public readonly string $tenantId,
        public readonly string $changedBy,
        public readonly string $cutoffDateKind,
        public readonly int $cutoffMonth,
        public readonly int $cutoffDay,
    ) {
    }
}
