<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports age-group --force`.

declare(strict_types=1);

namespace Academorix\AgeGroup\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SeedDefaultAgeGroupsForTenantJob completed for a freshly-provisioned tenant. Fires exactly once per tenant lifetime (unless the tenant loses every default + the reconciler re-seeds — that path fires a fresh event).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - teams::PopulateAgeGroupPickerCacheOnFirstSeed
 *
 * @category AgeGroup
 *
 * @since    0.1.0
 */
final readonly class AgeGroupCatalogSeeded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $ageGroupIds  ULID[] (10 by default — U6..Adult..Masters)
     * @param  string  $defaultAgeGroupId  ULID (the row with is_default=true, usually 'Adult')
     * @param  string  $seededBy  'tenant_provisioning' | 'reconciler' | 'legacy_backfill_command'
     * @param  int  $catalogVersion  integer (bumps when data/default-age-groups.json changes; consumers may compare vs their cached copy)
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $ageGroupIds,
        public readonly string $defaultAgeGroupId,
        public readonly string $seededBy,
        public readonly int $catalogVersion,
    ) {
    }
}
