<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports age-group --force`.

declare(strict_types=1);

namespace Academorix\AgeGroup\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AgeGroupProvisioner completed successfully. Fires exactly once per (tenant_id, organization_id, sport_key, slug) tuple. Emits for both seeded defaults + tenant customs; consumers may filter on `source` in the payload to distinguish.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::MaybeDispatchAgeGroupCreatedNotification (skipped for source='tenant_provisioning' — seeded defaults are silent)
 * - teams::InvalidateAgeGroupPickerCache (Wave 2b — Team edit form caches allowed age groups)
 *
 * @category AgeGroup
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AgeGroupCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $ageGroupId  ULID (age_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $organizationId  ULID (org_*) | null
     * @param  string  $name  string
     * @param  string  $slug  string
     * @param  string  $sportKey  string | null
     * @param  int  $minAgeInclusive  integer (3..100)
     * @param  int  $maxAgeInclusive  integer (3..100)
     * @param  string  $cutoffDateKind  'calendar_year' | 'academic_year' | 'rolling_from_birthday' | 'custom_date'
     * @param  bool  $isYouth  boolean (max_age_inclusive < 18)
     * @param  bool  $isDefault  boolean
     * @param  bool  $isSeeded  boolean
     * @param  string  $createdBy  ULID (usr_*) | null (null = system-provisioned by the seeder)
     * @param  string  $source  'tenant_provisioning' | 'admin_created' | 'legacy_seed'
     */
    public function __construct(
        public readonly string $ageGroupId,
        public readonly string $tenantId,
        public readonly string $organizationId,
        public readonly string $name,
        public readonly string $slug,
        public readonly string $sportKey,
        public readonly int $minAgeInclusive,
        public readonly int $maxAgeInclusive,
        public readonly string $cutoffDateKind,
        public readonly bool $isYouth,
        public readonly bool $isDefault,
        public readonly bool $isSeeded,
        public readonly string $createdBy,
        public readonly string $source,
    ) {
    }
}
