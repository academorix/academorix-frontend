<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports age-group --force`.

declare(strict_types=1);

namespace Academorix\AgeGroup\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AgeGroup soft-deleted OR hard-deleted. Distinct events not needed — the payload carries the mode.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category AgeGroup
 *
 * @since    0.1.0
 */
final readonly class AgeGroupDeleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $ageGroupId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $deletedBy  ULID
     * @param  string  $mode  'soft' | 'hard'
     * @param  bool  $wasDefault  boolean
     * @param  bool  $wasSeeded  boolean
     */
    public function __construct(
        public readonly string $ageGroupId,
        public readonly string $tenantId,
        public readonly string $deletedBy,
        public readonly string $mode,
        public readonly bool $wasDefault,
        public readonly bool $wasSeeded,
    ) {
    }
}
