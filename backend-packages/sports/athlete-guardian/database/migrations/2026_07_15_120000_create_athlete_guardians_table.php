<?php

/**
 * @file database/migrations/2026_07_15_120000_create_athlete_guardians_table.php
 *
 * @description
 * Create the `athlete_guardians` table for the `AthleteGuardian` entity.
 * Owned by the athlete-guardian module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports athlete-guardian --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-guardian --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('athlete_guardians', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('athlete_id', 30);
            $table->foreign('athlete_id')->references('id')->on('athletes')->onDelete('cascade');
            $table->string('user_id', 30);
            $table->foreign('user_id')->references('id')->on('users')->onDelete('restrict');
            $table->string('relationship', 32);
            $table->boolean('is_primary')->default('false');
            $table->boolean('has_legal_custody')->default('false');
            $table->boolean('can_pickup')->default('true');
            $table->boolean('can_receive_communications')->default('true');
            $table->boolean('can_authorise_medical_care')->default('false');
            $table->string('verification_status', 16)->default('pending');
            $table->string('verification_method', 32)->nullable();
            $table->timestampTz('verified_at')->nullable();
            $table->string('verified_by_user_id', 30)->nullable();
            $table->foreign('verified_by_user_id')->references('id')->on('users')->onDelete('set null');
            $table->string('verification_document_file_id', 30)->nullable();
            $table->text('dispute_reason')->nullable();
            $table->timestampTz('dispute_recorded_at')->nullable();
            $table->string('dispute_recorded_by_user_id', 30)->nullable();
            $table->foreign('dispute_recorded_by_user_id')->references('id')->on('users')->onDelete('set null');
            $table->text('revoke_reason')->nullable();
            $table->timestampTz('revoked_at')->nullable();
            $table->string('revoked_by_user_id', 30)->nullable();
            $table->foreign('revoked_by_user_id')->references('id')->on('users')->onDelete('set null');
            $table->text('notes')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'athlete_id', 'user_id'], 'athlete_guardians_athlete_user_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->unique(['tenant_id', 'athlete_id'], 'athlete_guardians_primary_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_primary = true AND deleted_at IS NULL
            $table->index(['tenant_id', 'user_id'], 'athlete_guardians_user_idx');
            $table->index(['tenant_id', 'athlete_id', 'verification_status'], 'athlete_guardians_verification_idx');
            $table->index(['tenant_id', 'verification_status', 'created_at'], 'athlete_guardians_pending_verification_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE verification_status = 'pending' AND deleted_at IS NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('athlete_guardians');
    }
};
