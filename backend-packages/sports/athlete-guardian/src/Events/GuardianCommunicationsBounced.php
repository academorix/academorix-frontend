<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-guardian --force`.

declare(strict_types=1);

namespace Academorix\AthleteGuardian\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Wave 4+ Notifications module detected permanent delivery failure to a guardian's User. Auto-disables can_receive_communications after N permanent bounces (default 3).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchGuardianCommunicationsBouncedNotification (to admin — email failing, please contact guardian)
 *
 * @category AthleteGuardian
 *
 * @since    0.1.0
 */
final readonly class GuardianCommunicationsBounced implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $guardianId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  int  $bounceCount  integer
     * @param  string  $channel  'mail' | 'sms' | 'push'
     * @param  bool  $autoDisabledCommunications  boolean
     */
    public function __construct(
        public readonly string $guardianId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly int $bounceCount,
        public readonly string $channel,
        public readonly bool $autoDisabledCommunications,
    ) {
    }
}
