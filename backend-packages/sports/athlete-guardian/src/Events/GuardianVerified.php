<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-guardian --force`.

declare(strict_types=1);

namespace Academorix\AthleteGuardian\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * verification_status transitioned pending → verified OR disputed → verified. Unlocks consent-recording for the guardian.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchGuardianVerifiedNotification (to guardian + admin)
 *
 * @category AthleteGuardian
 *
 * @since    0.1.0
 */
final readonly class GuardianVerified implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $guardianId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $userId  ULID
     * @param  string  $verificationMethod  'auto_household' | 'hr_manual' | 'silent_acceptance' | 'self_declared'
     * @param  string  $verifiedAt  ISO 8601
     * @param  string  $verifiedByUserId  ULID | null (null for auto / silent methods)
     */
    public function __construct(
        public readonly string $guardianId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $userId,
        public readonly string $verificationMethod,
        public readonly string $verifiedAt,
        public readonly string $verifiedByUserId,
    ) {
    }
}
