<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-guardian --force`.

declare(strict_types=1);

namespace Academorix\AthleteGuardian\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * verification_status transitioned verified → disputed. HR flag — the guardian is under review; consent-recording BLOCKED until resolved.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchGuardianVerificationDisputedNotification (to guardian + admin + safeguarding_officer — cannot-opt-out)
 *
 * @category AthleteGuardian
 *
 * @since    0.1.0
 */
final readonly class GuardianVerificationDisputed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $guardianId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $disputeReason  string
     * @param  string  $disputedAt  ISO 8601
     * @param  string  $disputedByUserId  ULID
     */
    public function __construct(
        public readonly string $guardianId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $disputeReason,
        public readonly string $disputedAt,
        public readonly string $disputedByUserId,
    ) {
    }
}
