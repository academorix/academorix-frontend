<?php

/**
 * @file database/migrations/2026_07_15_120001_create_positions_table.php
 *
 * @description
 * Create the `positions` table for the `Position` entity.
 * Owned by the registry module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports registry --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('positions', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30)->nullable();
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('discipline_id', 30);
            $table->foreign('discipline_id')->references('id')->on('disciplines')->onDelete('restrict');
            $table->string('name', 64);
            $table->string('slug', 64);
            $table->text('description')->nullable();
            $table->boolean('is_field_position')->default(true);
            $table->boolean('is_offensive')->nullable();
            $table->boolean('is_defensive')->nullable();
            $table->integer('typical_jersey_range_min')->nullable();
            $table->integer('typical_jersey_range_max')->nullable();
            $table->integer('sort_order')->default(100);
            $table->boolean('is_active')->default(true);
            $table->boolean('is_platform_seeded')->default(false);
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['discipline_id', 'slug'], 'positions_discipline_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'slug'], 'positions_tenant_slug_idx');
            $table->index(['discipline_id', 'sort_order'], 'positions_picker_idx');
            $table->index(['discipline_id', 'is_field_position'], 'positions_field_filter_idx');
            $table->index(['is_platform_seeded', 'is_active'], 'positions_flags_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('positions');
    }
};
