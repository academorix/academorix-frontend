<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

namespace Academorix\Registry\Actions\Tenant;

use Academorix\Registry\Contracts\Repositories\PositionRepositoryInterface;
use Academorix\Registry\Data\PositionData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Get;

/**
 * `GET /api/v1/positions/{position}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Registry
 *
 * @since    0.1.0
 */
#[AsAction(name: 'registry.positions.show')]
#[Get('/api/v1/positions/{position}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowPositionAction
{
    use AsController;

    public function __construct(
        private readonly PositionRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `position` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): PositionData
    {
        return PositionData::from($this->repository->findOrFail($id));
    }
}
