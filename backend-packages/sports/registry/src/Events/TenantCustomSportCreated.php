<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

namespace Academorix\Registry\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A tenant created a custom Sport (is_platform_seeded=false, tenant_id NOT NULL). Fires in ADDITION to SportCreated so analytics can graph tenant-custom adoption separately from platform seeder activity. Consumed by the sports-registry-adoption dashboard.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchTenantCustomSportCreatedNotification
 *
 * @category Registry
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TenantCustomSportCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sportId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $slug  string
     * @param  string  $category  string
     * @param  string  $createdBy  ULID
     */
    public function __construct(
        public readonly string $sportId,
        public readonly string $tenantId,
        public readonly string $slug,
        public readonly string $category,
        public readonly string $createdBy,
    ) {
    }
}
