<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

namespace Academorix\Registry\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Any Sport field mutated (except is_active flip, which fires SportDeactivated / SportCreated depending on direction). Fires for renames, description edits, color / icon changes, sort_order, gender_typical, age_range_min/max, governing_body edits.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - sports-registry::InvalidateCatalogCache
 *
 * @category Registry
 *
 * @since    0.1.0
 */
final readonly class SportUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sportId  ULID
     * @param  string  $tenantId  ULID | null
     * @param  string  $updatedBy  ULID
     * @param  string  $changedFields  string[] (e.g. ['name', 'description', 'age_range_max'])
     */
    public function __construct(
        public readonly string $sportId,
        public readonly string $tenantId,
        public readonly string $updatedBy,
        public readonly string $changedFields,
    ) {
    }
}
