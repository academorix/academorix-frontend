<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

namespace Academorix\Registry\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SeedPlatformSportsCatalogJob completed successfully. Fires exactly once at platform first-boot when the catalog is empty; also fires on explicit reseed via `sports:registry:reseed-platform-catalog --force`.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - sports-registry::WarmCatalogCache
 *
 * @category Registry
 *
 * @since    0.1.0
 */
final readonly class SportCatalogSeeded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sportIds  ULID[] (30+ platform sport IDs)
     * @param  string  $disciplineIds  ULID[]
     * @param  string  $positionIds  ULID[]
     * @param  int  $catalogVersion  integer (bumps when data/platform-sports-catalog.json changes materially)
     * @param  string  $seededBy  'platform_boot' | 'admin_reseed' | 'legacy_backfill_command'
     * @param  int  $durationMs  integer
     */
    public function __construct(
        public readonly string $sportIds,
        public readonly string $disciplineIds,
        public readonly string $positionIds,
        public readonly int $catalogVersion,
        public readonly string $seededBy,
        public readonly int $durationMs,
    ) {
    }
}
