<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

namespace Academorix\Progress\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AssessmentOrchestrator committed a ProgressAssessment (values validated + snapshot stamped + overall computed).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateAthleteProgressCache
 * - development::EvaluatePromotionCriteria
 * - awards::EvaluateAutoGrantOnAssessment
 * - growth::analytics::TrackAssessmentEvent
 *
 * @category Progress
 *
 * @since    0.1.0
 */
final readonly class ProgressAssessmentRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $assessmentId  ULID (pra_*)
     * @param  string  $tenantId  ULID
     * @param  string  $athleteEnrollmentId  ULID (aen_*)
     * @param  string  $sportKey  string
     * @param  string  $attributeSetSnapshotId  ULID (asg_*)
     * @param  string  $overallScore  number | null
     * @param  string  $overallTier  'bronze' | 'silver' | 'gold' | 'diamond' | null
     * @param  string  $captureDate  date
     * @param  string  $assessedByUserId  ULID
     */
    public function __construct(
        public readonly string $assessmentId,
        public readonly string $tenantId,
        public readonly string $athleteEnrollmentId,
        public readonly string $sportKey,
        public readonly string $attributeSetSnapshotId,
        public readonly string $overallScore,
        public readonly string $overallTier,
        public readonly string $captureDate,
        public readonly string $assessedByUserId,
    ) {
    }
}
