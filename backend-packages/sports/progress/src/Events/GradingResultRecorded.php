<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

namespace Academorix\Progress\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * GradingResult with result IN ('pass', 'fail', 'deferred'). Cascades to BeltRankPromoted on 'pass'.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchGradingResultNotification
 *
 * @category Progress
 *
 * @since    0.1.0
 */
final readonly class GradingResultRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $gradingResultId  ULID (grr_*)
     * @param  string  $gradingEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteEnrollmentId  ULID
     * @param  string  $fromBeltId  ULID (blt_*) | null
     * @param  string  $toBeltId  ULID (blt_*)
     * @param  string  $result  'pass' | 'fail' | 'deferred'
     */
    public function __construct(
        public readonly string $gradingResultId,
        public readonly string $gradingEventId,
        public readonly string $tenantId,
        public readonly string $athleteEnrollmentId,
        public readonly string $fromBeltId,
        public readonly string $toBeltId,
        public readonly string $result,
    ) {
    }
}
