<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

namespace Academorix\Progress\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New GradingEvent committed with status='scheduled'. Fires notifications to candidate athletes + guardians.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchGradingEventScheduledNotification
 *
 * @category Progress
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class GradingEventScheduled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $gradingEventId  ULID (gre_*)
     * @param  string  $tenantId  ULID
     * @param  string  $branchId  ULID (brn_*)
     * @param  string  $sportKey  string
     * @param  string  $scheduledAt  ISO 8601
     * @param  string  $examinerCoachId  ULID (cop_*)
     */
    public function __construct(
        public readonly string $gradingEventId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $sportKey,
        public readonly string $scheduledAt,
        public readonly string $examinerCoachId,
    ) {
    }
}
