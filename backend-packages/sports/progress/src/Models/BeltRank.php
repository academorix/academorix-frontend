<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

namespace Academorix\Progress\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Progress\Contracts\Data\BeltRankInterface;
use Academorix\Progress\Database\Factories\BeltRankFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Foundation\Concerns\HasPrefixedUlid;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;

/**
 * Eloquent model for a BeltRank.
 *
 * Ordered martial-arts belt (or equivalent grading) per sport_key.
 *
 * @category Progress
 *
 * @since    0.1.0
 */
#[Table(name: BeltRankInterface::TABLE, keyType: BeltRankInterface::KEY_TYPE)]
#[Fillable([
    BeltRankInterface::ATTR_TENANT_ID,
        BeltRankInterface::ATTR_SPORT_KEY,
        BeltRankInterface::ATTR_CODE,
        BeltRankInterface::ATTR_LABELS,
        BeltRankInterface::ATTR_COLOR_HEX,
        BeltRankInterface::ATTR_RANK_ORDER,
        BeltRankInterface::ATTR_MIN_TRAINING_HOURS,
        BeltRankInterface::ATTR_METADATA,
])]
#[UseFactory(BeltRankFactory::class)]
final class BeltRank extends Model implements BeltRankInterface
{
    use HasFactory;
    use HasPrefixedUlid;
    use BelongsToTenant;
    use HasMetadata;
    use HasUserstamps;
    use Auditable;
    use Filterable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        BeltRankInterface::ATTR_LABELS => 'array',
        BeltRankInterface::ATTR_METADATA => 'array',
    ];
}
