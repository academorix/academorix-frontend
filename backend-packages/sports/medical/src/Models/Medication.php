<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

namespace Academorix\Medical\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Medical\Contracts\Data\MedicationInterface;
use Academorix\Medical\Database\Factories\MedicationFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Foundation\Concerns\HasPrefixedUlid;
use Academorix\Sports\Athlete\Concerns\BelongsToAthlete;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a Medication.
 *
 * Active medication — name / dosage / frequency / prescribing_provider / expires_at.
 *
 * @category Medical
 *
 * @since    0.1.0
 */
#[Table(name: MedicationInterface::TABLE, keyType: MedicationInterface::KEY_TYPE)]
#[Fillable([
    MedicationInterface::ATTR_TENANT_ID,
        MedicationInterface::ATTR_ATHLETE_ID,
        MedicationInterface::ATTR_NAME_ENCRYPTED,
        MedicationInterface::ATTR_DOSAGE_ENCRYPTED,
        MedicationInterface::ATTR_FREQUENCY,
        MedicationInterface::ATTR_PRESCRIBING_PROVIDER_ENCRYPTED,
        MedicationInterface::ATTR_STARTED_AT,
        MedicationInterface::ATTR_EXPIRES_AT,
        MedicationInterface::ATTR_STATUS,
        MedicationInterface::ATTR_NOTES_ENCRYPTED,
        MedicationInterface::ATTR_METADATA,
])]
#[UseFactory(MedicationFactory::class)]
final class Medication extends Model implements MedicationInterface
{
    use HasFactory;
    use HasPrefixedUlid;
    use BelongsToTenant;
    use BelongsToAthlete;
    use HasMetadata;
    use HasUserstamps;
    use Auditable;
    use HasActivityLog;
    // TODO(gen): resolve unknown trait `EncryptsSensitiveFields` — add its import + use line.
    use Filterable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

}
