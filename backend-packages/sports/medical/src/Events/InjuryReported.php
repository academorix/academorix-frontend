<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

namespace Academorix\Medical\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. New Injury committed with status='reported'. Closes eligibility gate for the athlete on next squad query.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 10 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchInjuryReportedNotification (guardian + medical + coach, cannot-opt-out)
 * - medical::UpdateEligibilityGate
 * - event::InvalidateFutureSquadSelections
 *
 * @category Medical
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class InjuryReported implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $injuryId  ULID (inj_*)
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $bodyPart  string
     * @param  string  $severity  'minor' | 'moderate' | 'severe' | 'critical'
     * @param  string  $onsetAt  ISO 8601
     * @param  string  $reportedByUserId  ULID
     */
    public function __construct(
        public readonly string $injuryId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $bodyPart,
        public readonly string $severity,
        public readonly string $onsetAt,
        public readonly string $reportedByUserId,
    ) {
    }
}
