<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

namespace Academorix\Medical\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New Treatment committed against an active Injury.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 10 years)
 *
 * @category Medical
 *
 * @since    0.1.0
 */
final readonly class TreatmentAdministered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $treatmentId  ULID (trt_*)
     * @param  string  $injuryId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $kind  'physio' | 'medication' | 'rehab' | 'consult' | 'imaging' | 'surgery' | 'other'
     * @param  string  $administeredAt  ISO 8601
     */
    public function __construct(
        public readonly string $treatmentId,
        public readonly string $injuryId,
        public readonly string $tenantId,
        public readonly string $kind,
        public readonly string $administeredAt,
    ) {
    }
}
