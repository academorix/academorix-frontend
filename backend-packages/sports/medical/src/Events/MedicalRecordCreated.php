<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

namespace Academorix\Medical\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * First MedicalRecord committed for an athlete.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 10 years)
 * - activity::WriteToActivityLog
 *
 * @category Medical
 *
 * @since    0.1.0
 */
final readonly class MedicalRecordCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $medicalRecordId  ULID (mrc_*)
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID (ath_*)
     * @param  string  $createdByUserId  ULID
     */
    public function __construct(
        public readonly string $medicalRecordId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $createdByUserId,
    ) {
    }
}
