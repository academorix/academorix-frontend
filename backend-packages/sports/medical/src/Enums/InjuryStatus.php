<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

namespace Academorix\Medical\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `status` on `injury`.
 *
 * Backed enum — one case per allowed value.
 *
 * @category Medical
 *
 * @since    0.1.0
 */
enum InjuryStatus: string
{
    use Enum;

    case Reported = 'reported';
    case UnderTreatment = 'under_treatment';
    case Recovering = 'recovering';
    case Cleared = 'cleared';
}
