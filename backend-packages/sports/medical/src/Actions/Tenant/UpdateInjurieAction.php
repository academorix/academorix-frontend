<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

namespace Academorix\Medical\Actions\Tenant;

use Academorix\Medical\Contracts\Repositories\InjuryRepositoryInterface;
use Academorix\Medical\Data\InjuryData;
use Academorix\Medical\Data\Requests\UpdateInjuryRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/injuries/{injury}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Patch(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Medical
 *
 * @since    0.1.0
 */
#[AsController]
#[Patch('/api/v1/injuries/{injury}')]
final class UpdateInjurieAction
{
    public function __construct(
        private readonly InjuryRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `injurie` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateInjuryRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateInjuryRequestData $data): InjuryData
    {
        $model = $this->repository->update($id, $data->toArray());

        return InjuryData::from($model);
    }
}
