<?php

/**
 * @file database/migrations/2026_07_15_120005_create_treatments_table.php
 *
 * @description
 * Create the `treatments` table for the `Treatment` entity.
 * Owned by the medical module. Every column + index reference
 * goes through {@see Academorix\Medical\Contracts\Data\TreatmentInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

use Academorix\Medical\Contracts\Data\TreatmentInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(TreatmentInterface::TABLE, function (Blueprint $table): void {
            $table->string(TreatmentInterface::ATTR_ID, 30)->primary();
            $table->string(TreatmentInterface::ATTR_TENANT_ID, 30);
            $table->foreign(TreatmentInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(TreatmentInterface::ATTR_INJURY_ID, 30);
            $table->foreign(TreatmentInterface::ATTR_INJURY_ID)->references('id')->on('injuries')->onDelete('cascade');
            $table->timestampTz(TreatmentInterface::ATTR_ADMINISTERED_AT);
            $table->text(TreatmentInterface::ATTR_PROVIDER_NAME_ENCRYPTED)->nullable();
            $table->string(TreatmentInterface::ATTR_KIND, 32);
            $table->text(TreatmentInterface::ATTR_NOTES_ENCRYPTED)->nullable();
            $table->timestampTz(TreatmentInterface::ATTR_FOLLOW_UP_AT)->nullable();
            $table->jsonb(TreatmentInterface::ATTR_METADATA)->nullable();
            $table->timestampTz(TreatmentInterface::ATTR_CREATED_AT);
            $table->timestampTz(TreatmentInterface::ATTR_UPDATED_AT);
            $table->timestampTz(TreatmentInterface::ATTR_DELETED_AT)->nullable();
            $table->index([TreatmentInterface::ATTR_TENANT_ID, TreatmentInterface::ATTR_INJURY_ID, TreatmentInterface::ATTR_ADMINISTERED_AT], 'treatments_injury_time_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(TreatmentInterface::TABLE);
    }
};
