<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Athlete\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AthleteProvisioner committed successfully. Fires exactly once per athlete row. Consumes 1 unit of athlete_slot before commit (observer creating hook).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchAthleteWelcomeNotification (to the created-by admin AND to any primary guardian resolved at create-time — Wave 3b hooks the guardian path in the same commit as athlete-guardian)
 * - cache::InvalidateBranchAthleteListCache
 * - athlete::SetAgeGroupSnapshotOnAthleteCreated (in-band snapshot resolution — the row is unusable in enrollment forms without a snapshot)
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
final readonly class AthleteCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $athleteId  ULID (ath_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $branchId  ULID (brn_*)
     * @param  string  $userId  ULID (usr_*) | null
     * @param  string  $firstName  string
     * @param  string  $lastName  string
     * @param  string  $dateOfBirth  YYYY-MM-DD
     * @param  int  $ageYearsAtCreate  integer
     * @param  bool  $isMinorAtCreate  boolean
     * @param  string  $currentAgeGroupId  ULID (age_*) | null
     * @param  string  $createdByUserId  ULID (usr_*)
     * @param  string  $source  'admin_created' | 'self_serve' | 'trial_converted' | 'import' | 'api'
     */
    public function __construct(
        public readonly string $athleteId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $userId,
        public readonly string $firstName,
        public readonly string $lastName,
        public readonly string $dateOfBirth,
        public readonly int $ageYearsAtCreate,
        public readonly bool $isMinorAtCreate,
        public readonly string $currentAgeGroupId,
        public readonly string $createdByUserId,
        public readonly string $source,
    ) {
    }
}
