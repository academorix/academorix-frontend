<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Athlete\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Athlete.status transitioned active → paused. Distinct from the umbrella AthleteStatusChanged because pause is safeguarding-adjacent and drives dedicated notification cascade + auto-expiry job.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchAthletePausedForSafeguardingNotification (to guardian + safeguarding officer — cannot-opt-out)
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
final readonly class AthletePausedForSafeguarding implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $athleteId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $pausedAt  ISO 8601
     * @param  string  $pausedReason  string
     * @param  string  $expectedReturnAt  YYYY-MM-DD | null
     * @param  string  $pausedByUserId  ULID
     */
    public function __construct(
        public readonly string $athleteId,
        public readonly string $tenantId,
        public readonly string $pausedAt,
        public readonly string $pausedReason,
        public readonly string $expectedReturnAt,
        public readonly string $pausedByUserId,
    ) {
    }
}
