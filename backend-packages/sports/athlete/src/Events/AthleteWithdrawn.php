<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Athlete\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Athlete.status transitioned active/paused → withdrawn. Terminal. Requires withdrawal_reason.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — compliance)
 * - activity::WriteToActivityLog
 * - notifications::DispatchAthleteWithdrawnNotification (to admin + coach; guardian is opt-in — some withdrawals are guardian-initiated)
 * - athlete-enrollment::CancelActiveEnrollmentsOnAthleteWithdrawn (Wave 3+)
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
final readonly class AthleteWithdrawn implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $athleteId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $withdrawalReason  string
     * @param  string  $withdrawalNotes  string | null
     * @param  string  $withdrawnAt  YYYY-MM-DD
     * @param  string  $withdrawnByUserId  ULID
     * @param  bool  $wasGuardianInitiated  boolean
     */
    public function __construct(
        public readonly string $athleteId,
        public readonly string $tenantId,
        public readonly string $withdrawalReason,
        public readonly string $withdrawalNotes,
        public readonly string $withdrawnAt,
        public readonly string $withdrawnByUserId,
        public readonly bool $wasGuardianInitiated,
    ) {
    }
}
