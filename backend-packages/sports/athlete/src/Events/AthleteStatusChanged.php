<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Athlete\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Athlete.status transitioned. Umbrella event; also fires alongside the dedicated status events (AthletePausedForSafeguarding / AthleteGraduated / AthleteWithdrawn / AthleteArchived / AthleteRestored) so listeners can subscribe to the umbrella when the specific reason doesn't matter to them.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateBranchAthleteListCache
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
final readonly class AthleteStatusChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $athleteId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromStatus  string
     * @param  string  $toStatus  string
     * @param  string  $changedByUserId  ULID
     * @param  string  $reason  string | null
     */
    public function __construct(
        public readonly string $athleteId,
        public readonly string $tenantId,
        public readonly string $fromStatus,
        public readonly string $toStatus,
        public readonly string $changedByUserId,
        public readonly string $reason,
    ) {
    }
}
