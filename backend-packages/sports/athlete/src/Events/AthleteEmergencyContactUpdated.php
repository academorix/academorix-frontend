<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Athlete\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Any of emergency_contact_name / emergency_contact_phone / emergency_contact_relationship mutated. Audit-relevant for safety compliance.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — safeguarding)
 * - activity::WriteToActivityLog (WITHOUT the field values — activity feed shows 'emergency contact updated', not the phone number)
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AthleteEmergencyContactUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $athleteId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedByUserId  ULID
     * @param  string  $changedFields  string[] (e.g. ['emergency_contact_phone'] — NEVER the values)
     */
    public function __construct(
        public readonly string $athleteId,
        public readonly string $tenantId,
        public readonly string $changedByUserId,
        public readonly string $changedFields,
    ) {
    }
}
