<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Athlete\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Athlete.consent_photo_release specifically flipped. Distinct event because it drives immediate downstream cascade (marketing collateral, gallery visibility, social-media auto-inclusion — Wave 5+).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchAthletePhotoConsentChangedNotification (to marketing team — cannot-opt-out; ensures marketing pauses use)
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
final readonly class AthletePhotoConsentChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $athleteId  ULID
     * @param  string  $tenantId  ULID
     * @param  bool  $fromValue  boolean
     * @param  bool  $toValue  boolean
     * @param  string  $changedByUserId  ULID
     */
    public function __construct(
        public readonly string $athleteId,
        public readonly string $tenantId,
        public readonly bool $fromValue,
        public readonly bool $toValue,
        public readonly string $changedByUserId,
    ) {
    }
}
