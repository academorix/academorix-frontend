<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Athlete\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Athlete hard-deleted. Fires from PurgeArchivedAthletesJob after 7-year retention window + no downstream references. Also fires on TenantErased cascade.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
final readonly class AthleteDeleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $athleteId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $deletedBy  'retention_purge' | 'tenant_erasure'
     * @param  string  $wasArchivedAt  ISO 8601
     */
    public function __construct(
        public readonly string $athleteId,
        public readonly string $tenantId,
        public readonly string $deletedBy,
        public readonly string $wasArchivedAt,
    ) {
    }
}
