<?php

/**
 * @file database/migrations/2026_07_15_120000_create_athletes_table.php
 *
 * @description
 * Create the `athletes` table for the `Athlete` entity.
 * Owned by the athlete module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports athlete --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('athletes', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('branch_id', 30);
            $table->foreign('branch_id')->references('id')->on('branches')->onDelete('restrict');
            $table->string('user_id', 30)->nullable();
            $table->foreign('user_id')->references('id')->on('users')->onDelete('restrict');
            $table->string('external_id', 64)->nullable();
            $table->string('first_name', 128);
            $table->string('middle_name', 128)->nullable();
            $table->string('last_name', 128);
            $table->string('preferred_name', 128)->nullable();
            $table->date('date_of_birth');
            $table->string('gender', 24)->nullable();
            $table->string('pronouns', 32)->nullable();
            $table->integer('height_cm')->nullable();
            $table->integer('weight_kg')->nullable();
            $table->string('dominant_hand', 16)->nullable();
            $table->string('primary_language', 10)->nullable();
            $table->string('profile_photo_url', 512)->nullable();
            $table->string('current_age_group_id', 30)->nullable();
            $table->foreign('current_age_group_id')->references('id')->on('age_groups')->onDelete('set null');
            $table->timestampTz('current_age_group_snapshot_at')->nullable();
            $table->string('emergency_contact_name', 128)->nullable();
            $table->string('emergency_contact_phone', 20)->nullable();
            $table->string('emergency_contact_relationship', 32)->nullable();
            $table->text('medical_conditions')->nullable();
            $table->text('medical_allergies')->nullable();
            $table->text('medical_medications')->nullable();
            $table->text('medical_notes')->nullable();
            $table->boolean('consent_photo_release')->default('false');
            $table->boolean('consent_medical_disclosure')->default('false');
            $table->boolean('consent_third_party')->default('false');
            $table->timestampTz('consent_recorded_at')->nullable();
            $table->string('consent_recorded_by_user_id', 30)->nullable();
            $table->foreign('consent_recorded_by_user_id')->references('id')->on('users')->onDelete('set null');
            $table->string('status', 16)->default('active');
            $table->text('registration_notes')->nullable();
            $table->string('withdrawal_reason', 32)->nullable();
            $table->date('withdrawn_at')->nullable();
            $table->string('withdrawn_by_user_id', 30)->nullable();
            $table->foreign('withdrawn_by_user_id')->references('id')->on('users')->onDelete('set null');
            $table->date('graduated_at')->nullable();
            $table->timestampTz('paused_at')->nullable();
            $table->string('paused_reason', 64)->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'user_id'], 'athletes_tenant_user_active_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE user_id IS NOT NULL AND deleted_at IS NULL
            $table->unique(['tenant_id', 'external_id'], 'athletes_tenant_external_id_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE external_id IS NOT NULL AND deleted_at IS NULL
            $table->index(['tenant_id', 'branch_id', 'status'], 'athletes_status_idx');
            $table->index(['tenant_id', 'current_age_group_id'], 'athletes_age_group_idx');
            $table->index(['tenant_id', 'last_name', 'first_name'], 'athletes_name_idx');
            $table->index(['tenant_id', 'date_of_birth'], 'athletes_dob_idx');
            $table->index(['tenant_id', 'consent_recorded_at'], 'athletes_consent_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('athletes');
    }
};
