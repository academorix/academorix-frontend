<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Season.status transitioned to in_progress. Fires from automated RollSeasonPhaseJob (when now() reaches start_date OR competition_starts_at when set) OR manual admin start action. Kicks off session scheduling for Wave 3 downstream consumers.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchSeasonStartingSoonNotification (to enrolled athletes + coaches at T-48h; ALSO fired here at T-0 as a start confirmation)
 *
 * @category Season
 *
 * @since    0.1.0
 */
final readonly class SeasonStarted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $startedAt  ISO 8601
     * @param  string  $trigger  'automated_phase_roll' | 'manual_admin'
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly string $startedAt,
        public readonly string $trigger,
    ) {
    }
}
