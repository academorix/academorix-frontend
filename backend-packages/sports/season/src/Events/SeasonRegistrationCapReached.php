<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The enrolled-athlete count for this Season crossed `capacity_target` for the first time. Wave 3 athlete-enrollment consumers fire this from their post-commit hook; this module owns the event class so the notification + audit consumers can subscribe today. Informational only — enrollments continue past capacity_target unless a Wave 3 quota gate blocks them.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Season
 *
 * @since    0.1.0
 */
final readonly class SeasonRegistrationCapReached implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $capacityTarget  integer
     * @param  string  $reachedAt  ISO 8601
     * @param  string  $triggeringEnrollmentId  ULID | null (Wave 3 — will be aen_* once athlete-enrollment ships)
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly int $capacityTarget,
        public readonly string $reachedAt,
        public readonly string $triggeringEnrollmentId,
    ) {
    }
}
