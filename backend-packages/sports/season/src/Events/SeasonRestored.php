<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Archived -> completed. Only within the retention window (default 2555 days = 7 years). Reactivates the row without touching the (retired) enrollments that pointed at it.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Season
 *
 * @since    0.1.0
 */
final readonly class SeasonRestored implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $restoredAt  ISO 8601
     * @param  string  $restoredBy  ULID
     * @param  int  $daysArchived  integer
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly string $restoredAt,
        public readonly string $restoredBy,
        public readonly int $daysArchived,
    ) {
    }
}
