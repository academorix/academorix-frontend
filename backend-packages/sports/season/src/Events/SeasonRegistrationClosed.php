<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Season.status transitioned registration_open -> registration_closed. Fires from CloseExpiredRegistrationsJob (every 15 min when now() reaches registration_closes_at) OR manual admin close action. Does NOT block late enrollments — the late window is independent.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Season
 *
 * @since    0.1.0
 */
final readonly class SeasonRegistrationClosed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $closedAt  ISO 8601
     * @param  string  $lateRegistrationEndsAt  ISO 8601 | null (null = no late window)
     * @param  int  $enrolledCountSnapshot  integer
     * @param  string  $trigger  'automated_phase_roll' | 'manual_admin'
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly string $closedAt,
        public readonly string $lateRegistrationEndsAt,
        public readonly int $enrolledCountSnapshot,
        public readonly string $trigger,
    ) {
    }
}
