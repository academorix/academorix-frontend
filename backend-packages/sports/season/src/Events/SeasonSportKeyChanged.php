<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Season.sport_key mutated. CRITICAL severity: should never happen post-in_progress; if it does, downstream Team + Enrollment rows carrying this Season may drift into inconsistency (a hockey team enrolled to a suddenly-basketball season). Refused post-in_progress by the observer.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years, critical severity)
 * - activity::WriteToActivityLog
 *
 * @category Season
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SeasonSportKeyChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromSportKey  string | null
     * @param  string  $toSportKey  string | null
     * @param  string  $changedBy  ULID
     * @param  string  $auditSeverity  critical
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly string $fromSportKey,
        public readonly string $toSportKey,
        public readonly string $changedBy,
        public readonly string $auditSeverity,
    ) {
    }
}
