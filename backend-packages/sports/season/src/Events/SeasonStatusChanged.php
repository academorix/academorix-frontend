<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Season.status transitioned to any new value. A superset event; the specific-state events below (SeasonStarted, SeasonCompleted, ...) fire alongside for the routing case that cares.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Season
 *
 * @since    0.1.0
 */
final readonly class SeasonStatusChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromStatus  'planned' | 'registration_open' | 'registration_closed' | 'in_progress' | 'playoffs' | 'completed' | 'archived'
     * @param  string  $toStatus  'planned' | 'registration_open' | 'registration_closed' | 'in_progress' | 'playoffs' | 'completed' | 'archived'
     * @param  string  $changedBy  ULID | null (null = automated RollSeasonPhaseJob)
     * @param  string  $trigger  'automated_phase_roll' | 'manual_admin'
     * @param  string  $reason  string | null
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly string $fromStatus,
        public readonly string $toStatus,
        public readonly string $changedBy,
        public readonly string $trigger,
        public readonly string $reason,
    ) {
    }
}
