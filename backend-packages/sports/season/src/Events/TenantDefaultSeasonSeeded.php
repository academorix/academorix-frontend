<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SeedDefaultSeasonForTenantJob completed for an eligible seasonal-business_type tenant. Fires exactly once per tenant lifetime under normal operations (subsequent seasons come from admin action, not from this listener).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Season
 *
 * @since    0.1.0
 */
final readonly class TenantDefaultSeasonSeeded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $seasonId  ULID
     * @param  string  $businessType  'sports_academy' | 'school' | 'federation' | 'club'
     * @param  string  $seededBy  'tenant_provisioning' | 'legacy_backfill_command'
     * @param  int  $seasonType  'annual' | 'academic_year' | 'summer_camp' | 'winter_camp' | 'half_year' | 'quarter' | 'trimester' | 'custom'
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $seasonId,
        public readonly string $businessType,
        public readonly string $seededBy,
        public readonly int $seasonType,
    ) {
    }
}
