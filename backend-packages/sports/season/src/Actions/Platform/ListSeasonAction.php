<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Actions\Platform;

use Academorix\Season\Contracts\Repositories\SeasonRepositoryInterface;
use Academorix\Season\Data\SeasonData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Get;
use Illuminate\Http\Request;
use Illuminate\Pagination\LengthAwarePaginator;
use Spatie\LaravelData\PaginatedDataCollection;

/**
 * `GET /api/v1/platform/seasons` — list action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Season
 *
 * @since    0.1.0
 */
#[AsAction(name: 'season.seasons.list')]
#[Get('/api/v1/platform/seasons')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class ListSeasonAction
{
    use AsController;

    public function __construct(
        private readonly SeasonRepositoryInterface $repository,
    ) {
    }

    /**
     * List `seasons` for the current caller.
     *
     * The `Repository` base picks up `filter[...]` / `sort=...` /
     * `include=...` query parameters via the `#[Filterable]` attribute
     * on the concrete repository — no per-Action wiring needed.
     *
     * @return PaginatedDataCollection<int, SeasonData>  Paginated wire-visible DTOs.
     */
    public function __invoke(Request $request): PaginatedDataCollection
    {
        /** @var LengthAwarePaginator<int, \Academorix\Season\Models\Season> $page */
        $page = $this->repository->paginate(
            perPage: (int) $request->integer('per_page', 15),
        );

        return SeasonData::collect($page, PaginatedDataCollection::class);
    }
}
