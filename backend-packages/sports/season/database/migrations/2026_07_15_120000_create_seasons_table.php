<?php

/**
 * @file database/migrations/2026_07_15_120000_create_seasons_table.php
 *
 * @description
 * Create the `seasons` table for the `Season` entity.
 * Owned by the season module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports season --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('seasons', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('organization_id', 64)->nullable();
            $table->foreign('organization_id')->references('id')->on('organizations')->onDelete('restrict');
            $table->string('branch_id', 64)->nullable();
            $table->foreign('branch_id')->references('id')->on('branches')->onDelete('restrict');
            $table->string('name', 128);
            $table->string('slug', 64);
            $table->string('sport_key', 32)->nullable();
            $table->text('description')->nullable();
            $table->string('season_type', 24)->default('annual');
            $table->date('start_date');
            $table->date('end_date');
            $table->timestampTz('registration_opens_at')->nullable();
            $table->timestampTz('registration_closes_at')->nullable();
            $table->timestampTz('late_registration_ends_at')->nullable();
            $table->timestampTz('competition_starts_at')->nullable();
            $table->boolean('has_playoffs')->default(false);
            $table->timestampTz('playoff_starts_at')->nullable();
            $table->timestampTz('playoff_ends_at')->nullable();
            $table->string('status', 24)->default('planned');
            $table->boolean('is_current')->default(false);
            $table->integer('sort_order')->default(100);
            $table->string('primary_color', 7)->nullable();
            $table->integer('capacity_target')->nullable();
            $table->integer('max_enrollments_per_athlete')->default(1);
            $table->boolean('allows_late_registration')->default(false);
            $table->integer('late_registration_fee_cents')->default(0);
            $table->string('form_id', 64)->nullable();
            $table->foreign('form_id')->references('id')->on('forms')->onDelete('set null');
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 64)->nullable();
            $table->string('updated_by', 64)->nullable();
            $table->string('deleted_by', 64)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'organization_id', 'branch_id', 'slug'], 'seasons_scope_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->unique(['tenant_id', 'organization_id', 'branch_id', 'sport_key'], 'seasons_scope_current_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_current = true AND deleted_at IS NULL
            $table->index(['tenant_id', 'status'], 'seasons_status_idx');
            $table->index(['tenant_id', 'start_date'], 'seasons_start_date_idx');
            $table->index(['tenant_id', 'end_date'], 'seasons_end_date_idx');
            $table->index(['tenant_id', 'is_current'], 'seasons_current_idx');
            $table->index(['tenant_id', 'sport_key'], 'seasons_sport_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE sport_key IS NOT NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('seasons');
    }
};
