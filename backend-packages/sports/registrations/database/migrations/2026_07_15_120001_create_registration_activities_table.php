<?php

/**
 * @file database/migrations/2026_07_15_120001_create_registration_activities_table.php
 *
 * @description
 * Create the `registration_activities` table for the `RegistrationActivity` entity.
 * Owned by the registrations module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports registrations --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registrations --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('registration_activities', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('registration_id', 30);
            $table->foreign('registration_id')->references('id')->on('registrations')->onDelete('cascade');
            $table->string('kind', 32);
            $table->string('actor_user_id', 30)->nullable();
            $table->foreign('actor_user_id')->references('id')->on('users')->onDelete('set null');
            $table->text('body')->nullable();
            $table->timestampTz('occurred_at');
            $table->jsonb('context')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->index(['tenant_id', 'registration_id', 'occurred_at'], 'registration_activities_timeline_idx');
            $table->index(['tenant_id', 'kind', 'occurred_at'], 'registration_activities_kind_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('registration_activities');
    }
};
