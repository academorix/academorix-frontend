<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registrations --force`.

declare(strict_types=1);

namespace Academorix\Registrations\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P0 event. Terminal state. Registration.stage=enrolled. Downstream: Athlete created, AthleteEnrollment created, Invoice raised.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::WelcomeEnrolledNotification
 * - growth::PropagateAttributionOnEnrolled
 * - reporting::UpdateConversionFunnel
 *
 * @category Registrations
 *
 * @since    0.1.0
 */
final readonly class Enrolled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $registrationId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $athleteEnrollmentId  ULID
     * @param  string  $invoiceId  ULID
     * @param  string  $convertedAt  ISO 8601
     */
    public function __construct(
        public readonly string $registrationId,
        public readonly string $athleteId,
        public readonly string $athleteEnrollmentId,
        public readonly string $invoiceId,
        public readonly string $convertedAt,
    ) {
    }
}
