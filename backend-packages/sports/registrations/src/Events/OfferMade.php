<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registrations --force`.

declare(strict_types=1);

namespace Academorix\Registrations\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Offer created. Time-boxed. Signed accept URL emailed.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::EmailOffer
 *
 * @category Registrations
 *
 * @since    0.1.0
 */
final readonly class OfferMade implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $offerId  ULID (off_*)
     * @param  string  $registrationId  ULID
     * @param  string  $expiresAt  ISO 8601
     * @param  int  $offeredAmountMinor  bigint|null
     */
    public function __construct(
        public readonly string $offerId,
        public readonly string $registrationId,
        public readonly string $expiresAt,
        public readonly int $offeredAmountMinor,
    ) {
    }
}
