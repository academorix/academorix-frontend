<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports competition --force`.

declare(strict_types=1);

namespace Academorix\Competition\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CompetitionFixture row created by GenerateFixturesJob (or manual).
 *
 * ## Consumers
 *
 * - event::CreateEventFromFixture
 * - activity::WriteToActivityLog
 *
 * @category Competition
 *
 * @since    0.1.0
 */
final readonly class FixtureScheduled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $fixtureId  ULID (cfx_*)
     * @param  string  $competitionId  ULID
     * @param  string  $homeTeamId  ULID
     * @param  string  $awayTeamId  ULID
     * @param  string  $scheduledAt  ISO 8601
     * @param  string  $facilityId  ULID|null
     */
    public function __construct(
        public readonly string $fixtureId,
        public readonly string $competitionId,
        public readonly string $homeTeamId,
        public readonly string $awayTeamId,
        public readonly string $scheduledAt,
        public readonly string $facilityId,
    ) {
    }
}
