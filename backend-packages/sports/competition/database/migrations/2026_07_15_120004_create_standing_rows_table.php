<?php

/**
 * @file database/migrations/2026_07_15_120004_create_standing_rows_table.php
 *
 * @description
 * Create the `standing_rows` table for the `StandingRow` entity.
 * Owned by the competition module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports competition --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports competition --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('standing_rows', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('competition_id', 30);
            $table->foreign('competition_id')->references('id')->on('competitions')->onDelete('cascade');
            $table->string('team_id', 30);
            $table->foreign('team_id')->references('id')->on('teams')->onDelete('cascade');
            $table->string('group_code', 8)->nullable();
            $table->integer('position')->default('0');
            $table->integer('games_played')->default('0');
            $table->integer('wins')->default('0');
            $table->integer('draws')->default('0');
            $table->integer('losses')->default('0');
            $table->integer('goals_for')->default('0');
            $table->integer('goals_against')->default('0');
            $table->integer('goal_difference')->default('0');
            $table->integer('points')->default('0');
            $table->timestampTz('recomputed_at')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'competition_id', 'team_id'], 'standing_rows_unique');
            $table->index(['tenant_id', 'competition_id', 'position'], 'standing_rows_position_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('standing_rows');
    }
};
