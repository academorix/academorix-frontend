<?php

/**
 * @file database/migrations/2026_07_15_120000_create_formation_slots_table.php
 *
 * @description
 * Create the `formation_slots` table for the `FormationSlot` entity.
 * Owned by the formations module. Every column + index reference
 * goes through {@see Academorix\Formations\Contracts\Data\FormationSlotInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports formations --force`.

declare(strict_types=1);

use Academorix\Formations\Contracts\Data\FormationSlotInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(FormationSlotInterface::TABLE, function (Blueprint $table): void {
            $table->string(FormationSlotInterface::ATTR_ID, 30)->primary();
            $table->string(FormationSlotInterface::ATTR_TENANT_ID, 30)->nullable();
            $table->foreign(FormationSlotInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(FormationSlotInterface::ATTR_FORMATION_ID, 30);
            $table->foreign(FormationSlotInterface::ATTR_FORMATION_ID)->references('id')->on('formations')->onDelete('cascade');
            $table->string(FormationSlotInterface::ATTR_POSITION_CODE, 32);
            $table->decimal(FormationSlotInterface::ATTR_X_COORD, 15, 4);
            $table->decimal(FormationSlotInterface::ATTR_Y_COORD, 15, 4);
            $table->integer(FormationSlotInterface::ATTR_SORT_ORDER)->default('0');
            $table->jsonb(FormationSlotInterface::ATTR_METADATA)->nullable();
            $table->timestampTz(FormationSlotInterface::ATTR_CREATED_AT);
            $table->timestampTz(FormationSlotInterface::ATTR_UPDATED_AT);
            $table->unique([FormationSlotInterface::ATTR_FORMATION_ID, FormationSlotInterface::ATTR_POSITION_CODE], 'formation_slots_position_unique');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(FormationSlotInterface::TABLE);
    }
};
