<?php

/**
 * @file database/migrations/2026_07_15_120000_create_formation_slots_table.php
 *
 * @description
 * Create the `formation_slots` table for the `FormationSlot` entity.
 * Owned by the formations module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports formations --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports formations --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('formation_slots', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30)->nullable();
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('formation_id', 30);
            $table->foreign('formation_id')->references('id')->on('formations')->onDelete('cascade');
            $table->string('position_code', 32);
            $table->decimal('x_coord', 15, 4);
            $table->decimal('y_coord', 15, 4);
            $table->integer('sort_order')->default('0');
            $table->jsonb('metadata')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['formation_id', 'position_code'], 'formation_slots_position_unique');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('formation_slots');
    }
};
