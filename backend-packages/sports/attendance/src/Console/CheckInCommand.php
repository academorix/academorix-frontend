<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan attendance:check-in` — Check in — attendance command.
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'attendance:check-in',
    description: 'Check in — attendance command.',
)]
final class CheckInCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Check in — attendance command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Check in — attendance command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
