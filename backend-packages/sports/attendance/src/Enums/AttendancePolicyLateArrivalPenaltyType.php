<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `late_arrival_penalty_type` on `attendance-policy`.
 *
 * Enum: none / consume_pass_anyway / fractional_pass_consume / notify_only.
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
enum AttendancePolicyLateArrivalPenaltyType: string
{
    use Enum;

    case None = 'none';
    case ConsumePassAnyway = 'consume_pass_anyway';
    case FractionalPassConsume = 'fractional_pass_consume';
    case NotifyOnly = 'notify_only';
}
