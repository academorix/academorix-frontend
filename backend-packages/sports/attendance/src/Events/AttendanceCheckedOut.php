<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * checked_out_at set on an AttendanceRecord. Optional field — only sessions with mandatory check-out or partial-attendance tracking use it.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
final readonly class AttendanceCheckedOut implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attendanceRecordId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $sessionId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $checkedOutAt  ISO 8601
     * @param  string  $leftEarlyReason  string | null
     */
    public function __construct(
        public readonly string $attendanceRecordId,
        public readonly string $tenantId,
        public readonly string $sessionId,
        public readonly string $athleteId,
        public readonly string $checkedOutAt,
        public readonly string $leftEarlyReason,
    ) {
    }
}
