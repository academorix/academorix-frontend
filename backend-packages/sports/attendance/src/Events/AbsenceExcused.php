<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Admin flipped an existing AbsenceRecord.status from unexcused → excused. counted_toward_freeze flipped to false; threshold re-evaluated (may undo a prior freeze cascade).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchAttendanceExcusedNotification (to guardian)
 * - attendance::EvaluateAbsenceThresholdOnAbsenceRecorded (re-run — may undo freeze)
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
final readonly class AbsenceExcused implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $absenceRecordId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $excusedByUserId  ULID
     * @param  string  $excusedAt  ISO 8601
     * @param  string  $absenceReason  string | null
     * @param  string  $documentedUrlSigned  string | null
     */
    public function __construct(
        public readonly string $absenceRecordId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $excusedByUserId,
        public readonly string $excusedAt,
        public readonly string $absenceReason,
        public readonly string $documentedUrlSigned,
    ) {
    }
}
