<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. EvaluateAbsenceThresholdOnAbsenceRecorded hook detected the athlete's unexcused-absence count in the rolling window has crossed policy.max_unexcused_absences_per_period. Cascades to membership freeze.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchAbsenceFreezeThresholdReachedNotification (to guardian + admin — cannot-opt-out)
 * - attendance::FreezeMembershipOnThresholdReached (pauses the membership via membership::MembershipObserver.setStatus('paused'))
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
final readonly class AbsenceFreezeThresholdReached implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $athleteEnrollmentId  ULID
     * @param  string  $membershipId  ULID | null
     * @param  string  $policyId  ULID | null
     * @param  int  $unexcusedCount  integer
     * @param  int  $threshold  integer
     * @param  int  $windowDays  integer
     * @param  string  $triggeredAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $athleteEnrollmentId,
        public readonly string $membershipId,
        public readonly string $policyId,
        public readonly int $unexcusedCount,
        public readonly int $threshold,
        public readonly int $windowDays,
        public readonly string $triggeredAt,
    ) {
    }
}
