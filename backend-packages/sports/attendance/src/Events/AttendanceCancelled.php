<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Admin cancelled an attendance record (accidental check-in, wrong athlete, wrong session). The consumed Pass is refunded (returned to unused pool via Pass.refund()) in the same transaction. Also fires via sports::SessionCancelled cascade — every attendance for the cancelled session is cancelled.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchAttendanceCancelledNotification (to guardian)
 * - cache::InvalidateAthleteAttendanceCache
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
final readonly class AttendanceCancelled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attendanceRecordId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $sessionId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $refundedPassId  ULID | null
     * @param  string  $cancellationReason  string
     * @param  string  $cancelledByUserId  ULID
     */
    public function __construct(
        public readonly string $attendanceRecordId,
        public readonly string $tenantId,
        public readonly string $sessionId,
        public readonly string $athleteId,
        public readonly string $refundedPassId,
        public readonly string $cancellationReason,
        public readonly string $cancelledByUserId,
    ) {
    }
}
