<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Actions\Tenant;

use Academorix\Attendance\Contracts\Repositories\AttendanceRecordRepositoryInterface;
use Academorix\Attendance\Data\AttendanceRecordData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Get;

/**
 * `GET /api/v1/attendance-records/{record}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Get(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsController]
#[Get('/api/v1/attendance-records/{record}')]
final class ShowAttendanceRecordAction
{
    public function __construct(
        private readonly AttendanceRecordRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `attendance-record` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): AttendanceRecordData
    {
        return AttendanceRecordData::from($this->repository->findOrFail($id));
    }
}
