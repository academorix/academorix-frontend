<?php

/**
 * @file database/migrations/2026_07_15_120003_create_late_arrivals_table.php
 *
 * @description
 * Create the `late_arrivals` table for the `LateArrival` entity.
 * Owned by the attendance module. Every column + index reference
 * goes through {@see Academorix\Attendance\Contracts\Data\LateArrivalInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

use Academorix\Attendance\Contracts\Data\LateArrivalInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(LateArrivalInterface::TABLE, function (Blueprint $table): void {
            $table->string(LateArrivalInterface::ATTR_ID, 30)->primary();
            $table->string(LateArrivalInterface::ATTR_TENANT_ID, 30);
            $table->foreign(LateArrivalInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(LateArrivalInterface::ATTR_ATTENDANCE_RECORD_ID, 30);
            $table->foreign(LateArrivalInterface::ATTR_ATTENDANCE_RECORD_ID)->references('id')->on('attendance_records')->onDelete('restrict');
            $table->string(LateArrivalInterface::ATTR_SESSION_ID, 30);
            $table->foreign(LateArrivalInterface::ATTR_SESSION_ID)->references('id')->on('sessions')->onDelete('restrict');
            $table->string(LateArrivalInterface::ATTR_ATHLETE_ID, 30);
            $table->foreign(LateArrivalInterface::ATTR_ATHLETE_ID)->references('id')->on('athletes')->onDelete('restrict');
            $table->integer(LateArrivalInterface::ATTR_MINUTES_LATE);
            $table->string(LateArrivalInterface::ATTR_POLICY_ID, 30)->nullable();
            $table->foreign(LateArrivalInterface::ATTR_POLICY_ID)->references('id')->on('attendance_policies')->onDelete('restrict');
            $table->string(LateArrivalInterface::ATTR_PENALTY_APPLIED, 32)->nullable();
            $table->jsonb(LateArrivalInterface::ATTR_METADATA)->nullable();
            $table->string(LateArrivalInterface::ATTR_CREATED_BY, 30)->nullable();
            $table->string(LateArrivalInterface::ATTR_UPDATED_BY, 30)->nullable();
            $table->timestampTz(LateArrivalInterface::ATTR_CREATED_AT);
            $table->timestampTz(LateArrivalInterface::ATTR_UPDATED_AT);
            $table->index([LateArrivalInterface::ATTR_TENANT_ID, LateArrivalInterface::ATTR_ATHLETE_ID, LateArrivalInterface::ATTR_MINUTES_LATE], 'late_arrivals_by_athlete_idx');
            $table->index([LateArrivalInterface::ATTR_TENANT_ID, LateArrivalInterface::ATTR_SESSION_ID], 'late_arrivals_by_session_idx');
            $table->index([LateArrivalInterface::ATTR_ATTENDANCE_RECORD_ID], 'late_arrivals_by_attendance_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(LateArrivalInterface::TABLE);
    }
};
