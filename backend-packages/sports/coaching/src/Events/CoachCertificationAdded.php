<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Coaching\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A CoachCertification row committed. Schedules the three expiration-cadence checkpoints.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - coaching::ScheduleExpirationNoticesOnCertificationAdded (priority 30)
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
final readonly class CoachCertificationAdded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $certificationId  ULID (ccf_*)
     * @param  string  $tenantId  ULID
     * @param  string  $coachingProfileId  ULID
     * @param  string  $issuingBody  string
     * @param  string  $certificationName  string
     * @param  string  $level  string | null
     * @param  string  $issuedAt  ISO 8601 date
     * @param  string  $expiresAt  ISO 8601 date | null
     * @param  string  $addedByUserId  ULID
     */
    public function __construct(
        public readonly string $certificationId,
        public readonly string $tenantId,
        public readonly string $coachingProfileId,
        public readonly string $issuingBody,
        public readonly string $certificationName,
        public readonly string $level,
        public readonly string $issuedAt,
        public readonly string $expiresAt,
        public readonly string $addedByUserId,
    ) {
    }
}
