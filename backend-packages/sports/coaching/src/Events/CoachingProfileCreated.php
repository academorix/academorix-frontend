<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Coaching\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CoachingProfileRepository committed a new profile. Fires once per profile row.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchCoachingProfileCreatedNotification (to admin — pending verification)
 * - cache::InvalidateStaffCoachingProfileCache
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
final readonly class CoachingProfileCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $profileId  ULID (cop_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $staffId  ULID (stf_*)
     * @param  string  $branchId  ULID (brn_*)
     * @param  string  $primarySportId  ULID (spt_*) | null
     * @param  int  $coachingSinceYear  integer | null
     * @param  string  $createdByUserId  ULID (usr_*)
     */
    public function __construct(
        public readonly string $profileId,
        public readonly string $tenantId,
        public readonly string $staffId,
        public readonly string $branchId,
        public readonly string $primarySportId,
        public readonly int $coachingSinceYear,
        public readonly string $createdByUserId,
    ) {
    }
}
