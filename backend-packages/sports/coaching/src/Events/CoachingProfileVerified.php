<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Coaching\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Admin invoked /verify. verified_by_admin_at set. Unlocks assignment creation.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchCoachingProfileVerifiedNotification (to coach)
 * - cache::InvalidateCoachingBySportCache
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
final readonly class CoachingProfileVerified implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $profileId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $staffId  ULID
     * @param  string  $verifiedByAdminUserId  ULID
     * @param  string  $verifiedAt  ISO 8601
     */
    public function __construct(
        public readonly string $profileId,
        public readonly string $tenantId,
        public readonly string $staffId,
        public readonly string $verifiedByAdminUserId,
        public readonly string $verifiedAt,
    ) {
    }
}
