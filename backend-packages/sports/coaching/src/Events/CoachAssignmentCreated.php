<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Coaching\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A CoachAssignment row committed successfully.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchCoachAssignmentCreatedNotification (to coach)
 * - cache::InvalidateCoachAssignmentsCache
 * - cache::InvalidateAssignableAssignmentsCache
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CoachAssignmentCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $assignmentId  ULID (cas_*)
     * @param  string  $tenantId  ULID
     * @param  string  $coachingProfileId  ULID (cop_*)
     * @param  string  $assignableType  'Session' | 'Team' | 'Event'
     * @param  string  $assignableId  ULID (ses_* | tea_* | evt_*)
     * @param  string  $role  'head_coach' | 'assistant_coach' | 'observer' | 'substitute'
     * @param  string  $startDate  ISO 8601
     * @param  string  $endDate  ISO 8601 | null
     * @param  string  $assignedByUserId  ULID
     */
    public function __construct(
        public readonly string $assignmentId,
        public readonly string $tenantId,
        public readonly string $coachingProfileId,
        public readonly string $assignableType,
        public readonly string $assignableId,
        public readonly string $role,
        public readonly string $startDate,
        public readonly string $endDate,
        public readonly string $assignedByUserId,
    ) {
    }
}
