<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Coaching\Actions\Tenant;

use Academorix\Coaching\Contracts\Repositories\CoachAssignmentRepositoryInterface;
use Academorix\Coaching\Data\CoachAssignmentData;
use Academorix\Coaching\Data\Requests\CreateCoachAssignmentRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/coach-assignments` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Post(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
#[AsController]
#[Post('/api/v1/coach-assignments')]
final class CreateCoachAssignmentAction
{
    public function __construct(
        private readonly CoachAssignmentRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `coach-assignment` from the validated request payload.
     *
     * @param  CreateCoachAssignmentRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateCoachAssignmentRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(CoachAssignmentData::from($model), JsonResponse::HTTP_CREATED);
    }
}
