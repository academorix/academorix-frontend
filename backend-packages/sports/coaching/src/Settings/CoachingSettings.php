<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Coaching\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * CoachingSettings — Coaching.
 *
 * Tenant-wide coaching workflow defaults.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'coaching',
    label: 'Coaching',
    description: 'Tenant-wide coaching workflow defaults.',
    icon: 'user-group',
    scope: 'tenant',
    permission: 'coaching_profiles.viewAny',
    sortOrder: 68,
)]
class CoachingSettings extends Settings
{

    // ── Verification ──────────────────────────────────
    #[SettingGroup(label: 'Verification', icon: 'shield-check', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Require admin verification', group: 'Verification', sortOrder: 1, helpText: 'When on (default), new coaching profiles require admin verify before assignments can be created.')]
    public bool $require_admin_verification = true;

    #[SettingField(controlType: ControlType::Text, label: 'Auto-verify certifications from these issuing bodies', group: 'Verification', sortOrder: 2, helpText: 'Certifications from these issuing bodies auto-verify without admin review. Empty by default; use only for tenant-internal training certs.')]
    public array $auto_verify_low_risk_bodies = [];


    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', icon: 'bell', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Text, label: 'Certification expiration cadence (days before expiry)', group: 'Notifications', sortOrder: 1, helpText: 'Days before expires_at at which to fire the expiring-soon notification. 0 always fires the expired event.')]
    public array $expiration_cadence_days = [90, 30, 7];

    #[SettingField(controlType: ControlType::Number, label: 'Assignment ending-soon window (days)', group: 'Notifications', sortOrder: 2, min: 1, max: 30)]
    public int $assignment_ending_soon_days = 7;


    // ── Assignment ──────────────────────────────────
    #[SettingGroup(label: 'Assignment', icon: 'clipboard-list', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Number, label: 'Max concurrent assignments per coach', group: 'Assignment', sortOrder: 1, min: 0, max: 100, helpText: '0 = unlimited.')]
    public int $max_concurrent_per_coach = 0;

    #[SettingField(controlType: ControlType::Number, label: 'Max assistant coaches per Session/Team/Event', group: 'Assignment', sortOrder: 2, min: 0, max: 10)]
    public int $max_assistants_per_assignable = 3;

    #[SettingField(controlType: ControlType::Toggle, label: 'Allow overlapping assignments by default', group: 'Assignment', sortOrder: 3, helpText: 'When off (default), the conflict detector flags overlapping assignments but does not refuse them. Admins may still override with intent.')]
    public bool $allow_overlapping_by_default = false;


    // ── Rating ──────────────────────────────────
    #[SettingGroup(label: 'Rating', icon: 'star', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Number, label: 'Default rating expiry (years)', group: 'Rating', sortOrder: 1, min: 0, max: 10, helpText: '0 = never expire. Extended retention (Enterprise) defaults to 5.')]
    public int $default_expiry_years = 2;

    #[SettingField(controlType: ControlType::Select, label: 'Default rating scale', group: 'Rating', sortOrder: 2)]
    public string $default_scale = 'stars_1_5';


    // ── Availability ──────────────────────────────────
    #[SettingGroup(label: 'Availability', icon: 'calendar', sortOrder: 5)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Require full-week availability', group: 'Availability', sortOrder: 1, helpText: 'When on, availability must include every day-of-week (Mon-Sun) with at least an empty array. When off, missing days imply unavailable.')]
    public bool $strict_validation = false;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'coaching';
    }
}
