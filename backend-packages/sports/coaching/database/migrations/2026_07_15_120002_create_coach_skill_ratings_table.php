<?php

/**
 * @file database/migrations/2026_07_15_120002_create_coach_skill_ratings_table.php
 *
 * @description
 * Create the `coach_skill_ratings` table for the `CoachSkillRating` entity.
 * Owned by the coaching module. Emitted from the module blueprint —
 * regenerate via `generate-module.py sports coaching --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('coach_skill_ratings', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('coaching_profile_id', 30);
            $table->foreign('coaching_profile_id')->references('id')->on('coaching_profiles')->onDelete('cascade');
            $table->string('ratable_type', 32);
            $table->string('ratable_id', 30);
            $table->string('rating_scale', 16);
            $table->string('rating_value');
            $table->string('rated_by_user_id', 30);
            $table->foreign('rated_by_user_id')->references('id')->on('users')->onDelete('restrict');
            $table->timestampTz('rated_at');
            $table->text('notes')->nullable();
            $table->date('expires_at')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'coaching_profile_id', 'ratable_type', 'ratable_id'], 'coach_skill_ratings_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'coaching_profile_id', 'ratable_type'], 'coach_skill_ratings_profile_type_idx');
            $table->index(['tenant_id', 'expires_at'], 'coach_skill_ratings_expires_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('coach_skill_ratings');
    }
};
