<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Academorix\Session\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SessionProvisioner completed a successful create. Fires exactly once per `sessions.id`.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Session
 *
 * @since    0.1.0
 */
final readonly class SessionScheduled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID (ses_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $branchId  ULID (brn_*)
     * @param  string  $resourceBookingId  ULID (bkg_*)
     * @param  string  $teamId  ULID (tea_*) | null
     * @param  string  $seasonId  ULID (sea_*) | null
     * @param  string  $eventId  ULID (eve_*) | null
     * @param  string  $sessionType  string
     * @param  string  $headCoachId  ULID (coa_*) | null
     * @param  string  $startsAt  ISO 8601 (from resource_booking)
     * @param  string  $endsAt  ISO 8601 (from resource_booking)
     * @param  string  $createdBy  ULID (usr_*) | null
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $resourceBookingId,
        public readonly string $teamId,
        public readonly string $seasonId,
        public readonly string $eventId,
        public readonly string $sessionType,
        public readonly string $headCoachId,
        public readonly string $startsAt,
        public readonly string $endsAt,
        public readonly string $createdBy,
    ) {
    }
}
