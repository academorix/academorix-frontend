<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Academorix\Session\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * head_coach_id set on session for the first time OR assistant_coach added to array.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchSessionCoachAssignedNotification (to the coach — 'you're on this session')
 *
 * @category Session
 *
 * @since    0.1.0
 */
final readonly class SessionCoachAssigned implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $coachId  ULID
     * @param  string  $role  'head' | 'assistant'
     * @param  string  $assignedBy  ULID
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $tenantId,
        public readonly string $coachId,
        public readonly string $role,
        public readonly string $assignedBy,
    ) {
    }
}
