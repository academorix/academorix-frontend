<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Academorix\Session\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Session.resource_booking_id changed to a new booking (which is how time changes flow — Session doesn't store starts_at/ends_at directly; the booking does). AUDIT-WARN — affects athletes + coach.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchSessionRescheduledNotification (cannot-opt-out to registered athletes + coach + guardians for minors)
 *
 * @category Session
 *
 * @since    0.1.0
 */
final readonly class SessionRescheduled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $shiftedBy  ULID
     * @param  string  $oldResourceBookingId  ULID
     * @param  string  $newResourceBookingId  ULID
     * @param  string  $before  {'starts_at': 'ISO 8601', 'ends_at': 'ISO 8601'}
     * @param  string  $after  {'starts_at': 'ISO 8601', 'ends_at': 'ISO 8601'}
     * @param  int  $affectedAthletesCount  integer
     * @param  string  $noticeHoursBeforeOriginal  number
     * @param  string  $auditSeverity  warn
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $tenantId,
        public readonly string $shiftedBy,
        public readonly string $oldResourceBookingId,
        public readonly string $newResourceBookingId,
        public readonly string $before,
        public readonly string $after,
        public readonly int $affectedAthletesCount,
        public readonly string $noticeHoursBeforeOriginal,
        public readonly string $auditSeverity,
    ) {
    }
}
