<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Academorix\Session\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SessionAttendance.status transitioned to absent.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Session
 *
 * @since    0.1.0
 */
final readonly class SessionAthleteMarkedAbsent implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID
     * @param  string  $attendanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $markedAt  ISO 8601
     * @param  string  $markedBy  ULID
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $attendanceId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $markedAt,
        public readonly string $markedBy,
    ) {
    }
}
