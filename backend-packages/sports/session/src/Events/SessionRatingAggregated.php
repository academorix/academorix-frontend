<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Academorix\Session\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AggregateSessionRatingsJob wrote a new session_rating value. Fires at session close (nightly job).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - coaching::UpdateCoachAggregateRating (Wave 2b — the coach.rating aggregator that pulls per-session ratings)
 *
 * @category Session
 *
 * @since    0.1.0
 */
final readonly class SessionRatingAggregated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $sessionRating  number
     * @param  int  $sessionRatingCount  integer
     * @param  string  $headCoachId  ULID | null
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $tenantId,
        public readonly string $sessionRating,
        public readonly int $sessionRatingCount,
        public readonly string $headCoachId,
    ) {
    }
}
