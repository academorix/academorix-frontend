<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Academorix\Session\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Session.status transitioned to cancelled. Requires cancellation_reason.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial + safeguarding record)
 * - activity::WriteToActivityLog
 * - notifications::DispatchSessionCancelledNotification (cannot-opt-out)
 *
 * @category Session
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SessionCancelled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $cancelledAt  ISO 8601
     * @param  string  $cancelledBy  ULID | null (null = automated cancel from under-minimum job)
     * @param  string  $cancellationReason  string
     * @param  string  $noticeHoursBeforeStart  number
     * @param  int  $affectedAthletesCount  integer
     * @param  string  $trigger  'manual_admin' | 'automated_under_minimum' | 'facility_retired' | 'coach_deactivated'
     * @param  string  $auditSeverity  warn
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $tenantId,
        public readonly string $cancelledAt,
        public readonly string $cancelledBy,
        public readonly string $cancellationReason,
        public readonly string $noticeHoursBeforeStart,
        public readonly int $affectedAthletesCount,
        public readonly string $trigger,
        public readonly string $auditSeverity,
    ) {
    }
}
