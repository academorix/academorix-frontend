<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports development --force`.

declare(strict_types=1);

namespace Academorix\Development\Actions\Tenant;

use Academorix\Development\Contracts\Repositories\GoalRepositoryInterface;
use Academorix\Development\Data\GoalData;
use Academorix\Development\Data\Requests\UpdateGoalRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/goals/{goal}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Development
 *
 * @since    0.1.0
 */
#[AsAction(name: 'development.goals.update')]
#[Patch('/api/v1/goals/{goal}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateGoalAction
{
    use AsController;

    public function __construct(
        private readonly GoalRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `goal` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateGoalRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateGoalRequestData $data): GoalData
    {
        $model = $this->repository->update($id, $data->toArray());

        return GoalData::from($model);
    }
}
