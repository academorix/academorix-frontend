<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports development --force`.

declare(strict_types=1);

namespace Academorix\Development\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Reason REQUIRED (rare — usually for RTP-related reasons).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::NotifyDemotion
 *
 * @category Development
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AthleteDemoted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $assignmentId  ULID
     * @param  string  $athleteId  ULID
     * @param  int  $fromStage  int
     * @param  int  $toStage  int
     * @param  string  $demotedByUserId  ULID
     * @param  string  $reason  string
     */
    public function __construct(
        public readonly string $assignmentId,
        public readonly string $athleteId,
        public readonly int $fromStage,
        public readonly int $toStage,
        public readonly string $demotedByUserId,
        public readonly string $reason,
    ) {
    }
}
