<?php

/**
 * @file database/migrations/2026_07_15_120000_create_mail_suppressions_table.php
 *
 * @description
 * Create the `mail_suppressions` table for the `MailSuppression` entity.
 * Owned by the notifications-mail module. Emitted from the module blueprint —
 * regenerate via `generate-module.py notifications notifications-mail --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-mail --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('mail_suppressions', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64)->nullable();
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('email', 320);
            $table->string('email_domain', 255);
            $table->string('reason', 24);
            $table->string('provider', 32)->nullable();
            $table->string('source_delivery_id', 64)->nullable();
            $table->foreign('source_delivery_id')->references('id')->on('notification_deliveries')->onDelete('set null');
            $table->text('bounce_reason')->nullable();
            $table->boolean('is_system')->default(false);
            $table->timestampTz('expires_at')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'email'], 'mail_suppressions_tenant_email_unique');
            $table->index(['email'], 'mail_suppressions_email_index');
            $table->index(['email_domain'], 'mail_suppressions_domain_index');
            $table->index(['reason', 'created_at'], 'mail_suppressions_reason_index');
            $table->index(['expires_at'], 'mail_suppressions_expires_at_index');
            // Partial-index predicate — hand-add via DB::statement: WHERE expires_at IS NOT NULL AND deleted_at IS NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('mail_suppressions');
    }
};
