<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-mail --force`.

declare(strict_types=1);

namespace Academorix\NotificationsMail\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Provider webhook: click event.
 *
 * ## Consumers
 *
 * - notifications::UpdateDeliveryClickedListener
 *
 * @category NotificationsMail
 *
 * @since    0.1.0
 */
final readonly class MailClicked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $notificationId  ULID
     * @param  string  $deliveryId  ULID
     * @param  string  $provider  string
     * @param  string  $clickedUrl  string — hashed for restricted URLs
     * @param  string  $clickedAt  ISO 8601
     */
    public function __construct(
        public readonly string $notificationId,
        public readonly string $deliveryId,
        public readonly string $provider,
        public readonly string $clickedUrl,
        public readonly string $clickedAt,
    ) {
    }
}
