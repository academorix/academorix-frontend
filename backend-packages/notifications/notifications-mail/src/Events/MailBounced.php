<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-mail --force`.

declare(strict_types=1);

namespace Academorix\NotificationsMail\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Provider webhook: bounce event. Hard bounces trigger address suppression; soft bounces trigger retry.
 *
 * ## Consumers
 *
 * - notifications::UpdateDeliveryFailedListener
 * - notifications-mail::SuppressAddressListener
 *
 * @category NotificationsMail
 *
 * @since    0.1.0
 */
final readonly class MailBounced implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $notificationId  ULID
     * @param  string  $deliveryId  ULID
     * @param  string  $provider  string
     * @param  string  $bounceKind  'hard' | 'soft'
     * @param  string  $bounceReason  string
     * @param  string  $recipientEmail  string
     * @param  string  $bouncedAt  ISO 8601
     */
    public function __construct(
        public readonly string $notificationId,
        public readonly string $deliveryId,
        public readonly string $provider,
        public readonly string $bounceKind,
        public readonly string $bounceReason,
        public readonly string $recipientEmail,
        public readonly string $bouncedAt,
    ) {
    }
}
