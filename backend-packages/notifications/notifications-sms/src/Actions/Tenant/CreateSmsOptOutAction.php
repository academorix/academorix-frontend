<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-sms --force`.

declare(strict_types=1);

namespace Academorix\NotificationsSms\Actions\Tenant;

use Academorix\NotificationsSms\Contracts\Repositories\SmsOptOutRepositoryInterface;
use Academorix\NotificationsSms\Data\SmsOptOutData;
use Academorix\NotificationsSms\Data\Requests\CreateSmsOptOutRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/tenant/sms-opt-outs` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Post(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category NotificationsSms
 *
 * @since    0.1.0
 */
#[AsController]
#[Post('/api/v1/tenant/sms-opt-outs')]
final class CreateSmsOptOutAction
{
    public function __construct(
        private readonly SmsOptOutRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `sms-opt-out` from the validated request payload.
     *
     * @param  CreateSmsOptOutRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateSmsOptOutRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(SmsOptOutData::from($model), JsonResponse::HTTP_CREATED);
    }
}
