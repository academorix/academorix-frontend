<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications announcements --force`.

declare(strict_types=1);

namespace Academorix\Announcements\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Announcements module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Announcements
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum AnnouncementsPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `announcements.viewAny` — Announcements ViewAny
     */
    #[Label('Announcements ViewAny')]
    #[Description('Announcements ViewAny')]
    case AnnouncementsViewAny = 'announcements.viewAny';

    /**
     * `announcements.view` — Announcements View
     */
    #[Label('Announcements View')]
    #[Description('Announcements View')]
    case AnnouncementsView = 'announcements.view';

    /**
     * `announcements.create` — Draft an announcement.
     */
    #[Label('Draft an announcement')]
    #[Description('Draft an announcement. Audience must intersect caller\'s scopes.')]
    case AnnouncementsCreate = 'announcements.create';

    /**
     * `announcements.update` — Draft state only.
     */
    #[Label('Draft state only')]
    #[Description('Draft state only.')]
    case AnnouncementsUpdate = 'announcements.update';

    /**
     * `announcements.publish` — Publish.
     */
    #[Label('Publish')]
    #[Description('Publish. Tenant-wide audience requires admin/owner.')]
    case AnnouncementsPublish = 'announcements.publish';

    /**
     * `announcements.retract` — Retract published announcement.
     */
    #[Label('Retract published announcement')]
    #[Description('Retract published announcement.')]
    case AnnouncementsRetract = 'announcements.retract';

    /**
     * `announcements.delete` — Draft state only.
     */
    #[Label('Draft state only')]
    #[Description('Draft state only.')]
    case AnnouncementsDelete = 'announcements.delete';

    /**
     * `announcements.schedule` — Schedule future publish.
     */
    #[Label('Schedule future publish')]
    #[Description('Schedule future publish.')]
    case AnnouncementsSchedule = 'announcements.schedule';

    /**
     * `announcements.channels.sms` — Use SMS channel.
     */
    #[Label('Use SMS channel')]
    #[Description('Use SMS channel. Gated by per-tenant SMS-credit entitlement.')]
    case AnnouncementsChannelsSms = 'announcements.channels.sms';

    /**
     * `announcements.channels.push` — Use push notification channel.
     */
    #[Label('Use push notification channel')]
    #[Description('Use push notification channel.')]
    case AnnouncementsChannelsPush = 'announcements.channels.push';

    /**
     * `announcements.channels.email` — Announcements Channels Email
     */
    #[Label('Announcements Channels Email')]
    #[Description('Announcements Channels Email')]
    case AnnouncementsChannelsEmail = 'announcements.channels.email';

    /**
     * `platform.announcements.viewAny` — Cross-tenant announcement audit.
     */
    #[Label('Cross-tenant announcement audit')]
    #[Description('Cross-tenant announcement audit.')]
    case PlatformAnnouncementsViewAny = 'platform.announcements.viewAny';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::AnnouncementsViewAny => Guard::Sanctum,
            self::AnnouncementsView => Guard::Sanctum,
            self::AnnouncementsCreate => Guard::Sanctum,
            self::AnnouncementsUpdate => Guard::Sanctum,
            self::AnnouncementsPublish => Guard::Sanctum,
            self::AnnouncementsRetract => Guard::Sanctum,
            self::AnnouncementsDelete => Guard::Sanctum,
            self::AnnouncementsSchedule => Guard::Sanctum,
            self::AnnouncementsChannelsSms => Guard::Sanctum,
            self::AnnouncementsChannelsPush => Guard::Sanctum,
            self::AnnouncementsChannelsEmail => Guard::Sanctum,
            self::PlatformAnnouncementsViewAny => Guard::PlatformAdmin,
        };
    }
}
