<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications announcements --force`.

declare(strict_types=1);

namespace Academorix\Announcements\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Delivered to ONE recipient on ONE channel. Fires per (recipient × channel).
 *
 * ## Consumers
 *
 * - realtime::UpdateAnnouncementInbox
 * - notifications::TrackDeliveryStats
 *
 * @category Announcements
 *
 * @since    0.1.0
 */
final readonly class AnnouncementDelivered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $deliveryId  ULID
     * @param  string  $announcementId  ULID
     * @param  string  $recipientUserId  ULID
     * @param  string  $channel  string
     * @param  string  $deliveredAt  ISO 8601
     */
    public function __construct(
        public readonly string $deliveryId,
        public readonly string $announcementId,
        public readonly string $recipientUserId,
        public readonly string $channel,
        public readonly string $deliveredAt,
    ) {
    }
}
