<?php

/**
 * @file database/migrations/2026_07_15_120004_create_notification_templates_table.php
 *
 * @description
 * Create the `notification_templates` table for the `NotificationTemplate` entity.
 * Owned by the notifications module. Emitted from the module blueprint —
 * regenerate via `generate-module.py notifications notifications --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('notification_templates', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64)->nullable();
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('key', 128);
            $table->string('category_slug', 128);
            $table->foreign('category_slug')->references('slug')->on('notification_categories')->onDelete('restrict');
            $table->string('channel', 16);
            $table->string('locale', 8)->default('en');
            $table->integer('version')->default(1);
            $table->string('state', 16)->default('draft');
            $table->boolean('is_system')->default(false);
            $table->string('slug', 200);
            $table->string('subject_template', 500)->nullable();
            $table->text('body_rendered_html');
            $table->string('provider_template_id', 255)->nullable();
            $table->jsonb('variables_schema')->nullable();
            $table->timestampTz('published_at')->nullable();
            $table->integer('sort_order')->default(0);
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'key', 'channel', 'locale', 'version'], 'notification_templates_version_unique');
            $table->index(['tenant_id', 'key', 'channel', 'locale'], 'notification_templates_resolver_index');
            $table->index(['category_slug'], 'notification_templates_category_index');
            $table->index(['state', 'updated_at'], 'notification_templates_state_index');
            $table->unique(['slug'], 'notification_templates_slug_unique');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('notification_templates');
    }
};
