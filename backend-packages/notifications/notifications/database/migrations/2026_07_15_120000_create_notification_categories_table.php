<?php

/**
 * @file database/migrations/2026_07_15_120000_create_notification_categories_table.php
 *
 * @description
 * Create the `notification_categories` table for the `NotificationCategory` entity.
 * Owned by the notifications module. Emitted from the module blueprint —
 * regenerate via `generate-module.py notifications notifications --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('notification_categories', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64)->nullable();
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('slug', 128);
            $table->string('owning_module', 64);
            $table->string('display_name', 200);
            $table->text('description')->nullable();
            $table->jsonb('default_channels')->default('[]');
            $table->string('priority', 16)->default('transactional');
            $table->string('consent_tier', 32)->default('transactional_required');
            $table->boolean('opt_out_allowed')->default(true);
            $table->boolean('is_system')->default(false);
            $table->integer('sort_order')->default(0);
            $table->jsonb('metadata')->nullable();
            $table->jsonb('translations')->default('\'{}\'::jsonb');
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'slug'], 'notification_categories_tenant_slug_unique');
            $table->index(['owning_module'], 'notification_categories_owning_module_index');
            $table->index(['priority', 'consent_tier'], 'notification_categories_priority_index');
            $table->index(['is_system', 'slug'], 'notification_categories_is_system_index');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('notification_categories');
    }
};
