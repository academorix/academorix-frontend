<?php

/**
 * @file database/migrations/2026_07_15_120005_create_notifications_table.php
 *
 * @description
 * Create the `notifications` table for the `Notification` entity.
 * Owned by the notifications module. Emitted from the module blueprint —
 * regenerate via `generate-module.py notifications notifications --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('notifications', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('application_id', 64)->nullable();
            $table->foreign('application_id')->references('id')->on('applications')->onDelete('restrict');
            $table->string('category_slug', 128);
            $table->foreign('category_slug')->references('slug')->on('notification_categories')->onDelete('restrict');
            $table->string('template_id', 64)->nullable();
            $table->foreign('template_id')->references('id')->on('notification_templates')->onDelete('set null');
            $table->string('priority', 16)->default('transactional');
            $table->jsonb('priority_channels_requested');
            $table->string('state', 16)->default('queued');
            $table->string('addressee_type', 16)->default('user');
            $table->string('addressee_id', 64)->nullable();
            $table->string('addressee_email', 320)->nullable();
            $table->string('addressee_phone', 20)->nullable();
            $table->string('addressee_name', 200);
            $table->string('actor_type', 16)->nullable();
            $table->string('actor_id', 64)->nullable();
            $table->string('subject', 300)->nullable();
            $table->jsonb('payload')->default('{}');
            $table->timestampTz('seen_at')->nullable();
            $table->timestampTz('archived_at')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->index(['tenant_id', 'addressee_type', 'addressee_id', 'state', 'created_at'], 'notifications_inbox_index');
            $table->index(['tenant_id', 'category_slug', 'created_at'], 'notifications_category_index');
            $table->index(['tenant_id', 'state', 'created_at'], 'notifications_state_index');
            $table->index(['template_id'], 'notifications_template_id_index');
            $table->index(['addressee_email'], 'notifications_addressee_email_index');
            // Partial-index predicate — hand-add via DB::statement: WHERE addressee_email IS NOT NULL
            $table->index(['addressee_phone'], 'notifications_addressee_phone_index');
            // Partial-index predicate — hand-add via DB::statement: WHERE addressee_phone IS NOT NULL
            $table->index(['created_at'], 'notifications_created_at_index');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('notifications');
    }
};
