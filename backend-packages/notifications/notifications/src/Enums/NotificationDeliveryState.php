<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `state` on `notification-delivery`.
 *
 * Enum: `queued`, `sending`, `sent`, `delivered`, `opened`, `clicked`, `failed`, `permanently_failed`.
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
enum NotificationDeliveryState: string
{
    use Enum;

    case Queued = 'queued';
    case Sending = 'sending';
    case Sent = 'sent';
    case Delivered = 'delivered';
    case Opened = 'opened';
    case Clicked = 'clicked';
    case Failed = 'failed';
    case PermanentlyFailed = 'permanently_failed';
}
