<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Notifications\Contracts\Data\NotificationInterface;
use Academorix\Notifications\Database\Factories\NotificationFactory;
use Academorix\Application\Concerns\BelongsToApplication;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Laravel\Scout\Searchable;
use Mattiverse\Userstamps\Traits\Userstamps;

/**
 * Eloquent model for a Notification.
 *
 * The universal delivery record.
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
#[Table(name: NotificationInterface::TABLE, keyType: NotificationInterface::KEY_TYPE)]
#[Fillable([
    NotificationInterface::ATTR_TENANT_ID,
        NotificationInterface::ATTR_APPLICATION_ID,
        NotificationInterface::ATTR_CATEGORY_SLUG,
        NotificationInterface::ATTR_TEMPLATE_ID,
        NotificationInterface::ATTR_PRIORITY,
        NotificationInterface::ATTR_PRIORITY_CHANNELS_REQUESTED,
        NotificationInterface::ATTR_STATE,
        NotificationInterface::ATTR_ADDRESSEE_TYPE,
        NotificationInterface::ATTR_ADDRESSEE_ID,
        NotificationInterface::ATTR_ADDRESSEE_EMAIL,
        NotificationInterface::ATTR_ADDRESSEE_PHONE,
        NotificationInterface::ATTR_ADDRESSEE_NAME,
        NotificationInterface::ATTR_ACTOR_TYPE,
        NotificationInterface::ATTR_ACTOR_ID,
        NotificationInterface::ATTR_SUBJECT,
        NotificationInterface::ATTR_PAYLOAD,
        NotificationInterface::ATTR_SEEN_AT,
        NotificationInterface::ATTR_ARCHIVED_AT,
        NotificationInterface::ATTR_METADATA,
])]
#[UseFactory(NotificationFactory::class)]
final class Notification extends Model implements NotificationInterface
{
    use HasFactory;
    use HasUlids;
    // TODO(gen): resolve unknown trait `HasStates` — add its import + use line.
    use BelongsToTenant;
    use BelongsToApplication;
    use HasMetadata;
    use HasUserstamps;
    // TODO(gen): resolve unknown trait `HasAuditable` — add its import + use line.
    use Filterable;
    use Searchable;
    // TODO(gen): resolve unknown trait `Exportable` — add its import + use line.
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        NotificationInterface::ATTR_STATE => 'NotificationState',
        NotificationInterface::ATTR_PRIORITY => 'NotificationPriority',
        NotificationInterface::ATTR_PRIORITY_CHANNELS_REQUESTED => 'array',
        NotificationInterface::ATTR_PAYLOAD => 'NotificationPayloadCast',
        NotificationInterface::ATTR_METADATA => 'array',
        NotificationInterface::ATTR_SEEN_AT => 'datetime',
        NotificationInterface::ATTR_ARCHIVED_AT => 'datetime',
    ];
}
