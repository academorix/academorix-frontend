<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Notifications\Contracts\Data\NotificationTemplateInterface;
use Academorix\Notifications\Database\Factories\NotificationTemplateFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Laravel\Scout\Searchable;
use Mattiverse\Userstamps\Traits\Userstamps;

/**
 * Eloquent model for a NotificationTemplate.
 *
 * Versioned reusable template.
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
#[Table(name: NotificationTemplateInterface::TABLE, keyType: NotificationTemplateInterface::KEY_TYPE)]
#[Fillable([
    NotificationTemplateInterface::ATTR_TENANT_ID,
        NotificationTemplateInterface::ATTR_KEY,
        NotificationTemplateInterface::ATTR_CATEGORY_SLUG,
        NotificationTemplateInterface::ATTR_CHANNEL,
        NotificationTemplateInterface::ATTR_LOCALE,
        NotificationTemplateInterface::ATTR_VERSION,
        NotificationTemplateInterface::ATTR_STATE,
        NotificationTemplateInterface::ATTR_IS_SYSTEM,
        NotificationTemplateInterface::ATTR_SLUG,
        NotificationTemplateInterface::ATTR_SUBJECT_TEMPLATE,
        NotificationTemplateInterface::ATTR_BODY_RENDERED_HTML,
        NotificationTemplateInterface::ATTR_PROVIDER_TEMPLATE_ID,
        NotificationTemplateInterface::ATTR_VARIABLES_SCHEMA,
        NotificationTemplateInterface::ATTR_PUBLISHED_AT,
        NotificationTemplateInterface::ATTR_SORT_ORDER,
        NotificationTemplateInterface::ATTR_METADATA,
])]
#[UseFactory(NotificationTemplateFactory::class)]
final class NotificationTemplate extends Model implements NotificationTemplateInterface
{
    use HasFactory;
    use HasUlids;
    // TODO(gen): resolve unknown trait `HasSystemFlag` — add its import + use line.
    // TODO(gen): resolve unknown trait `BelongsToTenantOptional` — add its import + use line.
    use HasMetadata;
    use HasUserstamps;
    // TODO(gen): resolve unknown trait `HasAuditable` — add its import + use line.
    // TODO(gen): resolve unknown trait `Sortable` — add its import + use line.
    use Filterable;
    // TODO(gen): resolve unknown trait `Sluggable` — add its import + use line.
    use Searchable;
    // TODO(gen): resolve unknown trait `Importable` — add its import + use line.
    // TODO(gen): resolve unknown trait `Exportable` — add its import + use line.
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        NotificationTemplateInterface::ATTR_STATE => 'NotificationTemplateState',
        NotificationTemplateInterface::ATTR_IS_SYSTEM => 'boolean',
        NotificationTemplateInterface::ATTR_VARIABLES_SCHEMA => 'array',
        NotificationTemplateInterface::ATTR_METADATA => 'array',
        NotificationTemplateInterface::ATTR_PUBLISHED_AT => 'datetime',
    ];
}
