<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Provider webhook confirms endpoint delivery (mail: recipient MX accepted / rendered / opened depending on provider convention; sms: carrier delivery receipt; push: FCM/APNs success). Emitted from the channel module's IngestProviderWebhookJob.
 *
 * ## Consumers
 *
 * - invitations::MarkInvitationDeliveredListener
 * - compliance::AuditNotificationDeliveredListener
 * - notifications::MetricsCollector
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
final readonly class NotificationDelivered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $notificationId  ULID
     * @param  string  $deliveryId  ULID
     * @param  string  $channel  string
     * @param  string  $provider  string
     * @param  string  $deliveredAt  ISO 8601
     */
    public function __construct(
        public readonly string $notificationId,
        public readonly string $deliveryId,
        public readonly string $channel,
        public readonly string $provider,
        public readonly string $deliveredAt,
    ) {
    }
}
