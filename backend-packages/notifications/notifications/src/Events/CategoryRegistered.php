<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A module's ServiceProvider registered a new category during boot. Emitted once per unique slug per boot. Consumers invalidate their caches.
 *
 * ## Consumers
 *
 * - notifications::CategoryRegistryCacheInvalidator
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
final readonly class CategoryRegistered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $slug  string
     * @param  string  $owningModule  string
     * @param  string  $displayName  string
     * @param  string  $defaultChannels  string[]
     * @param  string  $priority  string
     * @param  string  $consentTier  string
     */
    public function __construct(
        public readonly string $slug,
        public readonly string $owningModule,
        public readonly string $displayName,
        public readonly string $defaultChannels,
        public readonly string $priority,
        public readonly string $consentTier,
    ) {
    }
}
