<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Channel module handed off to its provider AND received a synchronous accept (provider message id captured). Not the same as Delivered — provider may still bounce later.
 *
 * ## Consumers
 *
 * - invitations::MarkInvitationSentListener
 * - auth::MarkAuthNotificationSentListener
 * - compliance::AuditNotificationSentListener
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
final readonly class NotificationSent implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $notificationId  ULID
     * @param  string  $deliveryId  ULID
     * @param  string  $channel  'mail' | 'sms' | 'push' | 'in_app'
     * @param  string  $provider  string — 'mailgun' | 'twilio' | 'fcm' | 'reverb' | ...
     * @param  string  $providerMessageId  string | null
     * @param  string  $sentAt  ISO 8601
     * @param  int  $costMicroUnits  integer | null — populated by SMS + push providers
     */
    public function __construct(
        public readonly string $notificationId,
        public readonly string $deliveryId,
        public readonly string $channel,
        public readonly string $provider,
        public readonly string $providerMessageId,
        public readonly string $sentAt,
        public readonly int $costMicroUnits,
    ) {
    }
}
