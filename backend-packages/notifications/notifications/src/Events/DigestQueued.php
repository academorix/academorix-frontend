<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * DigestScheduler added a notification to a pending digest instead of dispatching immediately.
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
final readonly class DigestQueued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $digestId  ULID
     * @param  string  $notificationId  ULID
     * @param  string  $userId  ULID
     * @param  string  $categorySlug  string
     * @param  string  $channel  string
     * @param  string  $scheduledFor  ISO 8601 — digest delivery window boundary
     */
    public function __construct(
        public readonly string $digestId,
        public readonly string $notificationId,
        public readonly string $userId,
        public readonly string $categorySlug,
        public readonly string $channel,
        public readonly string $scheduledFor,
    ) {
    }
}
