<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Tenant admin (or platform seeder) transitioned a template from draft → published. Emitted for cache invalidation + audit.
 *
 * ## Consumers
 *
 * - notifications::TemplateRegistryCacheInvalidator
 * - compliance::AuditTemplateChangeListener (SOC 2 CC8.1 evidence)
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
final readonly class TemplatePublished implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $templateId  ULID
     * @param  string  $tenantId  ULID | null
     * @param  string  $templateKey  string
     * @param  string  $channel  string
     * @param  string  $locale  string
     * @param  int  $version  integer
     * @param  string  $actorId  ULID
     * @param  string  $publishedAt  ISO 8601
     */
    public function __construct(
        public readonly string $templateId,
        public readonly string $tenantId,
        public readonly string $templateKey,
        public readonly string $channel,
        public readonly string $locale,
        public readonly int $version,
        public readonly string $actorId,
        public readonly string $publishedAt,
    ) {
    }
}
