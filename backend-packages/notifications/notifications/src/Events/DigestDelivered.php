<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ProcessDigestJob rendered + dispatched a digest. Notifications inside the digest are marked digest_delivered_at; downstream consumers can mark items sent-via-digest.
 *
 * ## Consumers
 *
 * - analytics::TrackDigestDeliveryListener
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
final readonly class DigestDelivered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $digestId  ULID
     * @param  string  $userId  ULID
     * @param  string  $categorySlug  string
     * @param  string  $channel  string
     * @param  string  $notificationIds  ULID[]
     * @param  int  $itemCount  integer
     * @param  string  $deliveredAt  ISO 8601
     */
    public function __construct(
        public readonly string $digestId,
        public readonly string $userId,
        public readonly string $categorySlug,
        public readonly string $channel,
        public readonly string $notificationIds,
        public readonly int $itemCount,
        public readonly string $deliveredAt,
    ) {
    }
}
