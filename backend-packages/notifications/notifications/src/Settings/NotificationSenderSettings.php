<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * NotificationSenderSettings — Sender identity.
 *
 * Default sender identity + brand overlay for outbound notifications across channels.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'notifications_sender',
    label: 'Sender identity',
    description: 'Default sender identity + brand overlay for outbound notifications across channels.',
    icon: 'identification',
    scope: 'tenant',
    permission: 'settings.read',
    sortOrder: 10,
)]
class NotificationSenderSettings extends Settings
{

    // ── Sender identity ──────────────────────────────────
    #[SettingGroup(label: 'Sender identity', description: 'Overrides platform default. Per-channel overrides in the respective channel module (mail / sms).', icon: 'envelope', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Text, label: 'From display name', validation: ['required', 'string', 'max:40'], group: 'Sender identity', sortOrder: 1, helpText: 'Kept short (< 40 chars) for provider compatibility.')]
    public string $display_name = 'Academorix';

    #[SettingField(controlType: ControlType::Email, label: 'From email', validation: ['required', 'email', 'max:254'], group: 'Sender identity', sortOrder: 2, helpText: 'Must match a verified sender identity on the mail provider (validated at save time).')]
    public string $from_email = 'no-reply@academorix.app';

    #[SettingField(controlType: ControlType::Email, label: 'Reply-to email', validation: ['nullable', 'email', 'max:254'], group: 'Sender identity', sortOrder: 3, helpText: 'Empty = use From address.')]
    public ?string $reply_to_email = null;

    #[SettingField(controlType: ControlType::Textarea, label: 'Postal address', validation: ['required', 'string', 'max:500'], group: 'Sender identity', sortOrder: 4, helpText: 'Required for CAN-SPAM footer. Rendered into email footer as-is.')]
    public string $postal_address = 'Academorix Ltd, 123 Placeholder St, City, Country';


    // ── Notification branding ──────────────────────────────────
    #[SettingGroup(label: 'Notification branding', description: 'Tenant branding used in the shared React Email Layout + in-app notification card. Overlays tenancy::Branding.', icon: 'swatch', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Url, label: 'Logo URL', validation: ['nullable', 'url', 'max:500'], group: 'Notification branding', sortOrder: 1, helpText: 'Empty = use tenants default.')]
    public ?string $brand_logo_url = null;

    #[SettingField(controlType: ControlType::Color, label: 'Primary colour', validation: ['nullable', 'regex:/^#[0-9a-fA-F]{6}$/'], group: 'Notification branding', sortOrder: 2, helpText: 'Empty = use tenants default.')]
    public ?string $brand_primary_color = null;

    #[SettingField(controlType: ControlType::Textarea, label: 'Supplementary footer text', validation: ['nullable', 'string', 'max:500'], group: 'Notification branding', sortOrder: 3, helpText: 'Optional text below the required CAN-SPAM postal address.')]
    public ?string $brand_footer_text = null;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'notifications_sender';
    }
}
