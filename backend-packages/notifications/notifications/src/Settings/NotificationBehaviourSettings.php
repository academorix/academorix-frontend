<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * NotificationBehaviourSettings — Notification behaviour.
 *
 * Digest defaults + consent defaults + quiet hours defaults.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Notifications
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'notifications_behaviour',
    label: 'Notification behaviour',
    description: 'Digest defaults + consent defaults + quiet hours defaults.',
    icon: 'bell',
    scope: 'tenant',
    permission: 'settings.read',
    sortOrder: 11,
)]
class NotificationBehaviourSettings extends Settings
{

    // ── Digest defaults ──────────────────────────────────
    #[SettingGroup(label: 'Digest defaults', description: 'Platform-default digest behaviour. Users may still override per-category.', icon: 'queue-list', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Default digest mode', validation: ['required', 'string', 'in:immediate,daily,weekly,off'], group: 'Digest defaults', sortOrder: 1, options: ['immediate' => 'Immediate', 'daily' => 'Daily', 'weekly' => 'Weekly', 'off' => 'Off'], helpText: 'Default for new users on non-critical categories.')]
    public string $digest_default_mode = 'immediate';

    #[SettingField(controlType: ControlType::Toggle, label: 'Show digest option in preferences UI', validation: ['boolean'], group: 'Digest defaults', sortOrder: 2, helpText: 'Consumer plans may turn off to reduce UI complexity.')]
    public bool $digest_opt_in_default = true;

    #[SettingField(controlType: ControlType::Select, label: 'Marketing digest mode', validation: ['required', 'string', 'in:immediate,daily,weekly,off'], group: 'Digest defaults', sortOrder: 3, options: ['immediate' => 'Immediate', 'daily' => 'Daily', 'weekly' => 'Weekly', 'off' => 'Off'], helpText: 'Higher default reduces marketing burden.')]
    public string $digest_marketing_default_mode = 'weekly';


    // ── Consent defaults ──────────────────────────────────
    #[SettingGroup(label: 'Consent defaults', description: 'Per-tenant marketing consent defaults governed by tenant\'s jurisdiction.', icon: 'shield-check', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Select, label: 'Marketing consent mode', validation: ['required', 'string', 'in:opt_in,opt_out'], group: 'Consent defaults', sortOrder: 1, options: ['opt_in' => 'Opt-in (explicit enable)', 'opt_out' => 'Opt-out (US-only, per CAN-SPAM §7702)'])]
    public string $consent_marketing_default = 'opt_in';

    #[SettingField(controlType: ControlType::Toggle, label: 'Double opt-in', validation: ['boolean'], group: 'Consent defaults', sortOrder: 2, helpText: 'Require confirmation email before marketing consent is captured. Required for EU/UK tenants.')]
    public bool $consent_double_opt_in = false;

    #[SettingField(controlType: ControlType::Toggle, label: 'Enforce age gate', validation: ['boolean'], group: 'Consent defaults', sortOrder: 3, helpText: 'Block marketing to minors. Recommended on for every tenant.')]
    public bool $consent_age_gate_enabled = true;


    // ── Quiet hours defaults ──────────────────────────────────
    #[SettingGroup(label: 'Quiet hours defaults', description: 'Tenant-wide quiet-hours defaults. Users can override per-user.', icon: 'moon', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enable by default for new users', validation: ['boolean'], group: 'Quiet hours defaults', sortOrder: 1)]
    public bool $quiet_hours_enabled_default = true;

    #[SettingField(controlType: ControlType::Time, label: 'Start', validation: ['required', 'date_format:H:i'], group: 'Quiet hours defaults', sortOrder: 2, helpText: 'Applied in user\'s local timezone.')]
    public string $quiet_hours_start = '22:00';

    #[SettingField(controlType: ControlType::Time, label: 'End', validation: ['required', 'date_format:H:i'], group: 'Quiet hours defaults', sortOrder: 3)]
    public string $quiet_hours_end = '07:00';

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'notifications_behaviour';
    }
}
