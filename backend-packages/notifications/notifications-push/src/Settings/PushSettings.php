<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-push --force`.

declare(strict_types=1);

namespace Academorix\Notifications\Push\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * PushSettings — Push notifications.
 *
 * Provider routing + rich notification features + quiet-hours interaction.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category NotificationsPush
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'push',
    label: 'Push notifications',
    description: 'Provider routing + rich notification features + quiet-hours interaction.',
    icon: 'device-phone-mobile',
    scope: 'tenant',
    permission: 'settings.read',
    sortOrder: 14,
)]
class PushSettings extends Settings
{

    // ── Provider selection ──────────────────────────────────
    #[SettingGroup(label: 'Provider selection', description: 'Tenant-level provider preferences per platform. Platform-specific defaults apply otherwise (iOS → APNs, Android/Web → FCM).', icon: 'arrows-right-left', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'iOS provider', validation: ['required', 'string', 'in:apns,onesignal'], group: 'Provider selection', sortOrder: 1, options: ['apns' => 'Apple APNs', 'onesignal' => 'OneSignal'])]
    public string $provider_ios = 'apns';

    #[SettingField(controlType: ControlType::Select, label: 'Android provider', validation: ['required', 'string', 'in:fcm,onesignal'], group: 'Provider selection', sortOrder: 2, options: ['fcm' => 'Firebase FCM', 'onesignal' => 'OneSignal'])]
    public string $provider_android = 'fcm';

    #[SettingField(controlType: ControlType::Select, label: 'Web provider', validation: ['required', 'string', 'in:fcm,onesignal'], group: 'Provider selection', sortOrder: 3, options: ['fcm' => 'Firebase FCM', 'onesignal' => 'OneSignal'])]
    public string $provider_web = 'fcm';

    #[SettingField(controlType: ControlType::Select, label: 'React Native provider', validation: ['required', 'string', 'in:expo,fcm,apns,onesignal'], group: 'Provider selection', sortOrder: 4, options: ['expo' => 'Expo', 'fcm' => 'FCM', 'apns' => 'APNs', 'onesignal' => 'OneSignal'])]
    public string $provider_react_native = 'expo';


    // ── Rich notifications ──────────────────────────────────
    #[SettingGroup(label: 'Rich notifications', description: 'Enable rich notification features per platform. Requires notifications.push.rich_notifications entitlement.', icon: 'photo', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Images', validation: ['boolean'], group: 'Rich notifications', sortOrder: 1)]
    public bool $rich_images_enabled = false;

    #[SettingField(controlType: ControlType::Toggle, label: 'Action buttons', validation: ['boolean'], group: 'Rich notifications', sortOrder: 2)]
    public bool $rich_action_buttons_enabled = false;

    #[SettingField(controlType: ControlType::Toggle, label: 'Custom sounds', validation: ['boolean'], group: 'Rich notifications', sortOrder: 3)]
    public bool $rich_custom_sounds_enabled = false;


    // ── Quiet hours ──────────────────────────────────
    #[SettingGroup(label: 'Quiet hours', description: 'Push-specific override on top of notifications core\'s quiet_hours settings.', icon: 'moon', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Honour user\'s quiet hours', validation: ['boolean'], group: 'Quiet hours', sortOrder: 1, helpText: 'When on, defer non-critical pushes during user\'s quiet-hours window. Off = always deliver.')]
    public bool $quiet_hours_honor_user_setting = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'push';
    }
}
