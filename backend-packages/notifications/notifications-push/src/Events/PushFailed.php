<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-push --force`.

declare(strict_types=1);

namespace Academorix\NotificationsPush\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SendPushJob provider call failed with non-invalid-token error (transient network, provider 5xx, quota exceeded).
 *
 * ## Consumers
 *
 * - notifications::UpdateDeliveryFailedListener
 *
 * @category NotificationsPush
 *
 * @since    0.1.0
 */
final readonly class PushFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $notificationId  ULID
     * @param  string  $deliveryId  ULID
     * @param  string  $subscriptionId  ULID
     * @param  string  $provider  string
     * @param  string  $errorCode  string
     * @param  string  $errorMessage  string
     * @param  string  $failedAt  ISO 8601
     */
    public function __construct(
        public readonly string $notificationId,
        public readonly string $deliveryId,
        public readonly string $subscriptionId,
        public readonly string $provider,
        public readonly string $errorCode,
        public readonly string $errorMessage,
        public readonly string $failedAt,
    ) {
    }
}
