<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-push --force`.

declare(strict_types=1);

namespace Academorix\NotificationsPush\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New PushSubscription persisted (device token validated + stored).
 *
 * ## Consumers
 *
 * - compliance::AuditListener
 * - notifications-push::WarmSubscriptionCache
 *
 * @category NotificationsPush
 *
 * @since    0.1.0
 */
final readonly class PushSubscriptionRegistered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $subscriptionId  ULID
     * @param  string  $userId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $applicationId  ULID
     * @param  string  $provider  string
     * @param  string  $platform  string
     * @param  string  $deviceName  string | null
     */
    public function __construct(
        public readonly string $subscriptionId,
        public readonly string $userId,
        public readonly string $tenantId,
        public readonly string $applicationId,
        public readonly string $provider,
        public readonly string $platform,
        public readonly string $deviceName,
    ) {
    }
}
