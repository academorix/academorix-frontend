<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-push --force`.

declare(strict_types=1);

namespace Academorix\NotificationsPush\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * User or admin explicitly deleted the subscription (soft delete).
 *
 * ## Consumers
 *
 * - notifications-push::InvalidateSubscriptionCache
 *
 * @category NotificationsPush
 *
 * @since    0.1.0
 */
final readonly class PushSubscriptionRevoked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $subscriptionId  ULID
     * @param  string  $userId  ULID
     * @param  string  $revokedByUserId  ULID
     */
    public function __construct(
        public readonly string $subscriptionId,
        public readonly string $userId,
        public readonly string $revokedByUserId,
    ) {
    }
}
