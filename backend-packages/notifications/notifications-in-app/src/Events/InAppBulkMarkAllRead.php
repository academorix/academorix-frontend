<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-in-app --force`.

declare(strict_types=1);

namespace Academorix\NotificationsInApp\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Bulk mark-all-read operation completed for a caller. Emitted from BulkMarkReadJob on job success — the read events themselves are batched-suppressed (would generate N events for N deliveries; that's noise).
 *
 * @category NotificationsInApp
 *
 * @since    0.1.0
 */
final readonly class InAppBulkMarkAllRead implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $userId  ULID
     * @param  int  $deliveriesMarked  integer
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $userId,
        public readonly int $deliveriesMarked,
    ) {
    }
}
