<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications notifications-in-app --force`.

declare(strict_types=1);

namespace Academorix\NotificationsInApp\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * User clicked dismiss on the bell UI. Sets dismissed_at + fires event once. Distinct from InAppDeliveryRead — a delivery can be read (viewed in a modal) without being dismissed (still visible in the drawer).
 *
 * @category NotificationsInApp
 *
 * @since    0.1.0
 */
final readonly class InAppDeliveryDismissed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $notificationDeliveryId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $userId  ULID
     * @param  string  $categorySlug  string
     * @param  string  $dismissedAt  ISO-8601
     */
    public function __construct(
        public readonly string $notificationDeliveryId,
        public readonly string $tenantId,
        public readonly string $userId,
        public readonly string $categorySlug,
        public readonly string $dismissedAt,
    ) {
    }
}
