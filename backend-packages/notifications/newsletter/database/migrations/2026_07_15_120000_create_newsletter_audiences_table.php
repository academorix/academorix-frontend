<?php

/**
 * @file database/migrations/2026_07_15_120000_create_newsletter_audiences_table.php
 *
 * @description
 * Create the `newsletter_audiences` table for the `NewsletterAudience` entity.
 * Owned by the newsletter module. Emitted from the module blueprint —
 * regenerate via `generate-module.py notifications newsletter --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications newsletter --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('newsletter_audiences', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('newsletter_id', 64);
            $table->foreign('newsletter_id')->references('id')->on('newsletters')->onDelete('cascade');
            $table->string('name', 200);
            $table->string('slug', 100);
            $table->text('description')->nullable();
            $table->jsonb('expression');
            $table->jsonb('cached_subscriber_ids')->nullable();
            $table->timestampTz('cache_refreshed_at')->nullable();
            $table->boolean('is_default')->default(false);
            $table->jsonb('metadata')->nullable();
            $table->jsonb('translations')->default('\'{}\'::jsonb');
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['newsletter_id', 'slug'], 'newsletter_audiences_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->unique(['newsletter_id', 'is_default'], 'newsletter_audiences_default_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_default = true AND deleted_at IS NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('newsletter_audiences');
    }
};
