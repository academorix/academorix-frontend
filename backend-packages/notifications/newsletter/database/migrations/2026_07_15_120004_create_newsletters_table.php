<?php

/**
 * @file database/migrations/2026_07_15_120004_create_newsletters_table.php
 *
 * @description
 * Create the `newsletters` table for the `Newsletter` entity.
 * Owned by the newsletter module. Emitted from the module blueprint —
 * regenerate via `generate-module.py notifications newsletter --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications newsletter --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('newsletters', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('slug', 100);
            $table->string('name', 200);
            $table->text('description')->nullable();
            $table->string('cadence', 24)->default('weekly');
            $table->string('state', 24)->default('draft');
            $table->jsonb('sender_config');
            $table->jsonb('brand')->nullable();
            $table->jsonb('reputation_thresholds');
            $table->boolean('confirmation_required')->default(true);
            $table->integer('confirmation_ttl_hours')->default(720);
            $table->boolean('is_system')->default(false);
            $table->timestampTz('archived_at')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->jsonb('translations')->default('\'{}\'::jsonb');
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'slug'], 'newsletters_tenant_slug_unique');
            $table->index(['tenant_id', 'state'], 'newsletters_tenant_state_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('newsletters');
    }
};
