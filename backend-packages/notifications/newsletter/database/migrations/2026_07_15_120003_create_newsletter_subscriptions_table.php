<?php

/**
 * @file database/migrations/2026_07_15_120003_create_newsletter_subscriptions_table.php
 *
 * @description
 * Create the `newsletter_subscriptions` table for the `NewsletterSubscription` entity.
 * Owned by the newsletter module. Emitted from the module blueprint —
 * regenerate via `generate-module.py notifications newsletter --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications newsletter --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('newsletter_subscriptions', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('newsletter_id', 64);
            $table->foreign('newsletter_id')->references('id')->on('newsletters')->onDelete('cascade');
            $table->string('user_id', 64)->nullable();
            $table->foreign('user_id')->references('id')->on('users')->onDelete('set null');
            $table->string('email', 254);
            $table->string('first_name', 100)->nullable();
            $table->string('last_name', 100)->nullable();
            $table->string('locale', 8)->nullable();
            $table->string('state', 24)->default('pending_confirmation');
            $table->string('source', 32);
            $table->string('confirmation_token', 128)->nullable();
            $table->timestampTz('confirmation_token_expires_at')->nullable();
            $table->string('unsubscribe_token', 128);
            $table->timestampTz('confirmed_at')->nullable();
            $table->timestampTz('unsubscribed_at')->nullable();
            $table->string('unsubscribe_reason', 200)->nullable();
            $table->jsonb('tags')->nullable()->default('\'[]\'::jsonb');
            $table->integer('engagement_score')->default(0);
            $table->timestampTz('last_opened_at')->nullable();
            $table->timestampTz('last_clicked_at')->nullable();
            $table->string('ip_address')->nullable();
            $table->string('user_agent', 512)->nullable();
            $table->jsonb('consent_evidence')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['newsletter_id', 'email'], 'newsletter_subscriptions_email_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['newsletter_id', 'state'], 'newsletter_subscriptions_state_idx');
            $table->index(['newsletter_id', 'engagement_score'], 'newsletter_subscriptions_engagement_idx');
            $table->index(['confirmation_token'], 'newsletter_subscriptions_confirmation_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE confirmation_token IS NOT NULL
            $table->index(['unsubscribe_token'], 'newsletter_subscriptions_unsubscribe_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('newsletter_subscriptions');
    }
};
