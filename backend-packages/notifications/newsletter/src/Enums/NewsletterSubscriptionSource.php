<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications newsletter --force`.

declare(strict_types=1);

namespace Academorix\Newsletter\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `source` on `newsletter-subscription`.
 *
 * Enum: public_form / admin_added / imported / referral / api.
 *
 * @category Newsletter
 *
 * @since    0.1.0
 */
enum NewsletterSubscriptionSource: string
{
    use Enum;

    case PublicForm = 'public_form';
    case AdminAdded = 'admin_added';
    case Imported = 'imported';
    case Referral = 'referral';
    case Api = 'api';
}
