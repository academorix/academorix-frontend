<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications newsletter --force`.

declare(strict_types=1);

namespace Academorix\Newsletter\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `state` on `newsletter`.
 *
 * Enum: draft / active / paused / throttled / archived.
 *
 * @category Newsletter
 *
 * @since    0.1.0
 */
enum NewsletterState: string
{
    use Enum;

    case Draft = 'draft';
    case Active = 'active';
    case Paused = 'paused';
    case Throttled = 'throttled';
    case Archived = 'archived';
}
