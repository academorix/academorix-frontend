<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications newsletter --force`.

declare(strict_types=1);

namespace Academorix\Newsletter\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Newsletter\Contracts\Data\NewsletterInterface;
use Academorix\Newsletter\Database\Factories\NewsletterFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Laravel\Scout\Searchable;
use Mattiverse\Userstamps\Traits\Userstamps;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a Newsletter.
 *
 * A publication.
 *
 * @category Newsletter
 *
 * @since    0.1.0
 */
#[Table(name: NewsletterInterface::TABLE, keyType: NewsletterInterface::KEY_TYPE)]
#[Fillable([
    NewsletterInterface::ATTR_TENANT_ID,
        NewsletterInterface::ATTR_SLUG,
        NewsletterInterface::ATTR_NAME,
        NewsletterInterface::ATTR_DESCRIPTION,
        NewsletterInterface::ATTR_CADENCE,
        NewsletterInterface::ATTR_STATE,
        NewsletterInterface::ATTR_SENDER_CONFIG,
        NewsletterInterface::ATTR_BRAND,
        NewsletterInterface::ATTR_REPUTATION_THRESHOLDS,
        NewsletterInterface::ATTR_CONFIRMATION_REQUIRED,
        NewsletterInterface::ATTR_CONFIRMATION_TTL_HOURS,
        NewsletterInterface::ATTR_IS_SYSTEM,
        NewsletterInterface::ATTR_ARCHIVED_AT,
        NewsletterInterface::ATTR_METADATA,
        NewsletterInterface::ATTR_TRANSLATIONS,
])]
#[UseFactory(NewsletterFactory::class)]
final class Newsletter extends Model implements NewsletterInterface
{
    use HasFactory;
    use HasUlids;
    // TODO(gen): resolve unknown trait `HasSystemFlag` — add its import + use line.
    use BelongsToTenant;
    use HasMetadata;
    use HasUserstamps;
    // TODO(gen): resolve unknown trait `HasVersions` — add its import + use line.
    // TODO(gen): resolve unknown trait `HasAuditable` — add its import + use line.
    // TODO(gen): resolve unknown trait `HasTranslations` — add its import + use line.
    use HasActivityLog;
    use Filterable;
    use Searchable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        NewsletterInterface::ATTR_CADENCE => 'NewsletterCadence',
        NewsletterInterface::ATTR_STATE => 'NewsletterState',
        NewsletterInterface::ATTR_SENDER_CONFIG => 'array',
        NewsletterInterface::ATTR_BRAND => 'array',
        NewsletterInterface::ATTR_REPUTATION_THRESHOLDS => 'array',
        NewsletterInterface::ATTR_IS_SYSTEM => 'boolean',
        NewsletterInterface::ATTR_ARCHIVED_AT => 'datetime',
        NewsletterInterface::ATTR_METADATA => 'array',
        NewsletterInterface::ATTR_TRANSLATIONS => 'array',
    ];
}
