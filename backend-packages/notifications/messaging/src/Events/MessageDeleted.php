<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications messaging --force`.

declare(strict_types=1);

namespace Academorix\Messaging\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Message soft-deleted. Content masked in downstream broadcasts.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - realtime::BroadcastToConversation
 *
 * @category Messaging
 *
 * @since    0.1.0
 */
final readonly class MessageDeleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $messageId  ULID
     * @param  string  $conversationId  ULID
     * @param  string  $deletedByUserId  ULID
     * @param  string  $reason  'self'|'admin'|'safeguarding'|'gdpr_delete'
     */
    public function __construct(
        public readonly string $messageId,
        public readonly string $conversationId,
        public readonly string $deletedByUserId,
        public readonly string $reason,
    ) {
    }
}
