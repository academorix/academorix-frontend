<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications messaging --force`.

declare(strict_types=1);

namespace Academorix\Messaging\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * User marked messages read (client → server). Batched update.
 *
 * ## Consumers
 *
 * - realtime::BroadcastReadReceipt
 *
 * @category Messaging
 *
 * @since    0.1.0
 */
final readonly class MessageReadReceipt implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $conversationId  ULID
     * @param  string  $userId  ULID
     * @param  string  $lastReadMessageId  ULID
     * @param  string  $readAt  ISO 8601
     */
    public function __construct(
        public readonly string $conversationId,
        public readonly string $userId,
        public readonly string $lastReadMessageId,
        public readonly string $readAt,
    ) {
    }
}
