<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications messaging --force`.

declare(strict_types=1);

namespace Academorix\Messaging\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New Message row. HIGH VOLUME. Broadcasts to every conversation member via realtime channel.
 *
 * ## Consumers
 *
 * - realtime::BroadcastToConversation
 * - activity::WriteToActivityLog (sampled)
 * - safeguarding::ScanForFlaggedContent
 * - notifications::PushToOfflineMembers
 *
 * @category Messaging
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MessageSent implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $messageId  ULID (msg_*)
     * @param  string  $conversationId  ULID
     * @param  string  $senderUserId  ULID
     * @param  string  $bodyPreview  string (first 100 chars, PII-redacted)
     * @param  int  $attachmentCount  int
     * @param  string  $sentAt  ISO 8601
     */
    public function __construct(
        public readonly string $messageId,
        public readonly string $conversationId,
        public readonly string $senderUserId,
        public readonly string $bodyPreview,
        public readonly int $attachmentCount,
        public readonly string $sentAt,
    ) {
    }
}
