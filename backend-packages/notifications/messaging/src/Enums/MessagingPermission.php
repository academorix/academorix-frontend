<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py notifications messaging --force`.

declare(strict_types=1);

namespace Academorix\Messaging\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Messaging module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Messaging
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum MessagingPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `messaging.conversations.viewAny` — Messaging Conversations ViewAny
     */
    #[Label('Messaging Conversations ViewAny')]
    #[Description('Messaging Conversations ViewAny')]
    case MessagingConversationsViewAny = 'messaging.conversations.viewAny';

    /**
     * `messaging.conversations.view` — Messaging Conversations View
     */
    #[Label('Messaging Conversations View')]
    #[Description('Messaging Conversations View')]
    case MessagingConversationsView = 'messaging.conversations.view';

    /**
     * `messaging.conversations.create` — Messaging Conversations Create
     */
    #[Label('Messaging Conversations Create')]
    #[Description('Messaging Conversations Create')]
    case MessagingConversationsCreate = 'messaging.conversations.create';

    /**
     * `messaging.conversations.update` — Messaging Conversations Update
     */
    #[Label('Messaging Conversations Update')]
    #[Description('Messaging Conversations Update')]
    case MessagingConversationsUpdate = 'messaging.conversations.update';

    /**
     * `messaging.conversations.archive` — Messaging Conversations Archive
     */
    #[Label('Messaging Conversations Archive')]
    #[Description('Messaging Conversations Archive')]
    case MessagingConversationsArchive = 'messaging.conversations.archive';

    /**
     * `messaging.members.add` — Messaging Members Add
     */
    #[Label('Messaging Members Add')]
    #[Description('Messaging Members Add')]
    case MessagingMembersAdd = 'messaging.members.add';

    /**
     * `messaging.members.remove` — Messaging Members Remove
     */
    #[Label('Messaging Members Remove')]
    #[Description('Messaging Members Remove')]
    case MessagingMembersRemove = 'messaging.members.remove';

    /**
     * `messaging.members.promote` — Messaging Members Promote
     */
    #[Label('Messaging Members Promote')]
    #[Description('Messaging Members Promote')]
    case MessagingMembersPromote = 'messaging.members.promote';

    /**
     * `messaging.messages.view` — Messaging Messages View
     */
    #[Label('Messaging Messages View')]
    #[Description('Messaging Messages View')]
    case MessagingMessagesView = 'messaging.messages.view';

    /**
     * `messaging.messages.send` — Messaging Messages Send
     */
    #[Label('Messaging Messages Send')]
    #[Description('Messaging Messages Send')]
    case MessagingMessagesSend = 'messaging.messages.send';

    /**
     * `messaging.messages.edit` — Own messages only.
     */
    #[Label('Own messages only')]
    #[Description('Own messages only. 24h window from send.')]
    case MessagingMessagesEdit = 'messaging.messages.edit';

    /**
     * `messaging.messages.delete` — Own messages OR conversation admin.
     */
    #[Label('Own messages OR conversation admin')]
    #[Description('Own messages OR conversation admin.')]
    case MessagingMessagesDelete = 'messaging.messages.delete';

    /**
     * `messaging.messages.admin-delete` — Reason REQUIRED.
     */
    #[Label('Reason REQUIRED')]
    #[Description('Reason REQUIRED. Full audit chain (7yr retention).')]
    case MessagingMessagesAdminDelete = 'messaging.messages.admin-delete';

    /**
     * `messaging.reactions.add` — Messaging Reactions Add
     */
    #[Label('Messaging Reactions Add')]
    #[Description('Messaging Reactions Add')]
    case MessagingReactionsAdd = 'messaging.reactions.add';

    /**
     * `messaging.reactions.remove` — Messaging Reactions Remove
     */
    #[Label('Messaging Reactions Remove')]
    #[Description('Messaging Reactions Remove')]
    case MessagingReactionsRemove = 'messaging.reactions.remove';

    /**
     * `messaging.safeguarding.view-all` — Safeguarding officer — reads ALL messages tenant-wide.
     */
    #[Label('Safeguarding officer — reads ALL messages tenant-wide')]
    #[Description('Safeguarding officer — reads ALL messages tenant-wide. Child protection use only.')]
    case MessagingSafeguardingViewAll = 'messaging.safeguarding.view-all';

    /**
     * `platform.messaging.audit` — Cross-tenant safeguarding audit.
     */
    #[Label('Cross-tenant safeguarding audit')]
    #[Description('Cross-tenant safeguarding audit. Metadata + reason only, no message bodies.')]
    case PlatformMessagingAudit = 'platform.messaging.audit';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::MessagingConversationsViewAny => Guard::Sanctum,
            self::MessagingConversationsView => Guard::Sanctum,
            self::MessagingConversationsCreate => Guard::Sanctum,
            self::MessagingConversationsUpdate => Guard::Sanctum,
            self::MessagingConversationsArchive => Guard::Sanctum,
            self::MessagingMembersAdd => Guard::Sanctum,
            self::MessagingMembersRemove => Guard::Sanctum,
            self::MessagingMembersPromote => Guard::Sanctum,
            self::MessagingMessagesView => Guard::Sanctum,
            self::MessagingMessagesSend => Guard::Sanctum,
            self::MessagingMessagesEdit => Guard::Sanctum,
            self::MessagingMessagesDelete => Guard::Sanctum,
            self::MessagingMessagesAdminDelete => Guard::Sanctum,
            self::MessagingReactionsAdd => Guard::Sanctum,
            self::MessagingReactionsRemove => Guard::Sanctum,
            self::MessagingSafeguardingViewAll => Guard::Sanctum,
            self::PlatformMessagingAudit => Guard::PlatformAdmin,
        };
    }
}
