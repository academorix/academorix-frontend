<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability activity --force`.

declare(strict_types=1);

namespace Academorix\Activity\Actions\Platform;

use Academorix\Activity\Contracts\Repositories\ActivityRepositoryInterface;
use Academorix\Activity\Data\ActivityData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Get;

/**
 * `GET /api/v1/platform/activities/{activity}` — show action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Get(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Activity
 *
 * @since    0.1.0
 */
#[AsController]
#[Get('/api/v1/platform/activities/{activity}')]
final class ShowActivitieAction
{
    public function __construct(
        private readonly ActivityRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `activitie` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): ActivityData
    {
        return ActivityData::from($this->repository->findOrFail($id));
    }
}
