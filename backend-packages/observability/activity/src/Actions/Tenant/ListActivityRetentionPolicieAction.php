<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability activity --force`.

declare(strict_types=1);

namespace Academorix\Activity\Actions\Tenant;

use Academorix\Activity\Contracts\Repositories\ActivityRetentionPolicyRepositoryInterface;
use Academorix\Activity\Data\ActivityRetentionPolicyData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Get;
use Illuminate\Http\Request;
use Illuminate\Pagination\LengthAwarePaginator;
use Spatie\LaravelData\PaginatedDataCollection;

/**
 * `GET /api/v1/activity-retention-policies` — list action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Get(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Activity
 *
 * @since    0.1.0
 */
#[AsController]
#[Get('/api/v1/activity-retention-policies')]
final class ListActivityRetentionPolicieAction
{
    public function __construct(
        private readonly ActivityRetentionPolicyRepositoryInterface $repository,
    ) {
    }

    /**
     * List `activity-retention-policies` for the current caller.
     *
     * The `Repository` base picks up `filter[...]` / `sort=...` /
     * `include=...` query parameters via the `#[Filterable]` attribute
     * on the concrete repository — no per-Action wiring needed.
     *
     * @return PaginatedDataCollection<int, ActivityRetentionPolicyData>  Paginated wire-visible DTOs.
     */
    public function __invoke(Request $request): PaginatedDataCollection
    {
        /** @var LengthAwarePaginator<int, \Academorix\Activity\Models\ActivityRetentionPolicy> $page */
        $page = $this->repository->paginate(
            perPage: (int) $request->integer('per_page', 15),
        );

        return ActivityRetentionPolicyData::collect($page, PaginatedDataCollection::class);
    }
}
