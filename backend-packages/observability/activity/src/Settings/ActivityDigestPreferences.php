<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability activity --force`.

declare(strict_types=1);

namespace Academorix\Activity\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * ActivityDigestPreferences — Activity digest emails.
 *
 * Per-user preferences for activity digest emails.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Activity
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'activity_digest_prefs',
    label: 'Activity digest emails',
    description: 'Per-user preferences for activity digest emails. Every user can configure their own.',
    icon: 'envelope',
    scope: 'user',
    sortOrder: 41,
)]
class ActivityDigestPreferences extends Settings
{

    // ── Subscription ──────────────────────────────────
    #[SettingGroup(label: 'Subscription', icon: 'bell', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Digest frequency', group: 'Subscription', sortOrder: 1, options: ['none' => 'Do not send', 'daily' => 'Daily (every morning)', 'weekly' => 'Weekly (Monday morning)'], helpText: 'Daily digests summarise the last 24h; weekly summarises the last 7 days.')]
    public string $digest_cadence = 'none';

    #[SettingField(controlType: ControlType::Multiselect, label: 'Which feeds to include', validation: ['array'], group: 'Subscription', sortOrder: 2, helpText: 'Selected feeds appear in the digest. Empty = every feed you can access.')]
    public array $digest_log_names = [];

    #[SettingField(controlType: ControlType::Toggle, label: 'Include activities I caused', validation: ['boolean'], group: 'Subscription', sortOrder: 3, helpText: 'By default the digest shows only activities other people caused (things happening TO your entities). Turn on to include your own actions too.')]
    public bool $digest_include_own_activity = false;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'activity_digest_prefs';
    }
}
