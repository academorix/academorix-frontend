<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability activity --force`.

declare(strict_types=1);

namespace Academorix\Activity\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Activity module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Activity
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum ActivityPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `activity.viewAny` — List + show activities in the caller's tenant.
     */
    #[Label('List + show activities in the caller\'s tenant')]
    #[Description('List + show activities in the caller\'s tenant. RBAC-narrowed by `activity.enforce_scope` middleware — the caller sees only activities on entities they hold viewing permissions for. Every authenticated tenant user gets this by default (baseline read).')]
    case ActivityViewAny = 'activity.viewAny';

    /**
     * `activity.audit_any` — Enumerate activities across the tenant — call `/api/v1/activities/by-user/{user}` for any actor, or bypass the RBAC-narrowed default on the tenant list.
     */
    #[Label('Enumerate activities across the tenant — call `/api/v1/activities/by-user/{user}')]
    #[Description('Enumerate activities across the tenant — call `/api/v1/activities/by-user/{user}` for any actor, or bypass the RBAC-narrowed default on the tenant list. Admin-level compliance surface.')]
    case ActivityAuditAny = 'activity.audit_any';

    /**
     * `activity.retention.manage` — CRUD on retention policies.
     */
    #[Label('CRUD on retention policies')]
    #[Description('CRUD on retention policies. Gated by `activity_retention_extended` entitlement (Enterprise) for retention_days > 90.')]
    case ActivityRetentionManage = 'activity.retention.manage';

    /**
     * `activity.export` — Trigger an asynchronous CSV export.
     */
    #[Label('Trigger an asynchronous CSV export')]
    #[Description('Trigger an asynchronous CSV export. Gated by `activity_export` entitlement (Medium+) AND this permission.')]
    case ActivityExport = 'activity.export';

    /**
     * `platform.activity.viewAny` — Cross-tenant list.
     */
    #[Label('Cross-tenant list')]
    #[Description('Cross-tenant list. Every read captured in audit context (SOC 2 CC6.1).')]
    case PlatformActivityViewAny = 'platform.activity.viewAny';

    /**
     * `platform.activity.view` — Cross-tenant detail read.
     */
    #[Label('Cross-tenant detail read')]
    #[Description('Cross-tenant detail read.')]
    case PlatformActivityView = 'platform.activity.view';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::ActivityViewAny => Guard::Sanctum,
            self::ActivityAuditAny => Guard::Sanctum,
            self::ActivityRetentionManage => Guard::Sanctum,
            self::ActivityExport => Guard::Sanctum,
            self::PlatformActivityViewAny => Guard::PlatformAdmin,
            self::PlatformActivityView => Guard::PlatformAdmin,
        };
    }
}
