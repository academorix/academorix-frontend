<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability activity --force`.

declare(strict_types=1);

namespace Academorix\Activity\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ActivityObserver@created fires after the vendor row is persisted + tenant_id / application_id are stamped. The primary broadcast + reaction signal — every downstream consumer that wants to react to activity subscribes here.
 *
 * ## Consumers
 *
 * - activity::ActivityBroadcastPublisher (pushes to tenant.{id}.activity + user.{id}.activity)
 * - notifications::QueueActivityDigestForRecipients (opt-in per user — collect for daily/weekly digest email)
 * - growth::analytics::CaptureActivityRecordedFunnelEvent (functional consent, sampled at 5%)
 * - monitoring::EmitActivityRecordedMetric (RED-pattern counter)
 *
 * @category Activity
 *
 * @since    0.1.0
 */
final readonly class ActivityRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $activity  Activity
     * @param  string  $subject  Model|null (resolved via morph map)
     * @param  string  $causer  Model|null (resolved via morph map)
     * @param  string  $logName  string
     * @param  string  $batchUuid  string|null
     */
    public function __construct(
        public readonly string $activity,
        public readonly string $subject,
        public readonly string $causer,
        public readonly string $logName,
        public readonly string $batchUuid,
    ) {
    }
}
