<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability activity --force`.

declare(strict_types=1);

namespace Academorix\Activity\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PurgeExpiredActivitiesJob or ArchiveActivitiesToColdStorageJob finished a sweep. Fires once per job run (not per row deleted / archived) so downstream metrics can aggregate correctly.
 *
 * ## Consumers
 *
 * - notifications::DispatchActivityPurgeCompletedNotification (weekly admin digest)
 * - monitoring::EmitPurgeVolumeMetric
 * - compliance::EmitRetentionCascadeSignal
 *
 * @category Activity
 *
 * @since    0.1.0
 */
final readonly class ActivityPurgeExecuted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  string|null (null = cross-tenant sweep)
     * @param  int  $rowsDeleted  integer
     * @param  int  $rowsArchived  integer
     * @param  int  $durationMs  integer
     * @param  string  $kind  string (purge | archive)
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly int $rowsDeleted,
        public readonly int $rowsArchived,
        public readonly int $durationMs,
        public readonly string $kind,
    ) {
    }
}
