<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability activity --force`.

declare(strict_types=1);

namespace Academorix\Activity\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A user opened the activity feed dashboard OR subscribed to a specific tenant.{tenantId}.activity WebSocket channel. Dispatched by the /api/v1/activities/feed endpoint on successful subscription auth. Sampled at 10% (feed opens are frequent — full-rate capture would dominate analytics volume).
 *
 * ## Consumers
 *
 * - growth::analytics::CaptureFeedEngagementFunnel (functional consent tier — how often do users check the feed?)
 *
 * @category Activity
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ActivityFeedSubscribed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $actor  User
     * @param  string  $tenantId  string
     * @param  string  $channelSubscribed  string (channel name)
     * @param  array  $logNamesFiltered  array<string> (which log_names the caller is subscribing to)
     */
    public function __construct(
        public readonly string $actor,
        public readonly string $tenantId,
        public readonly string $channelSubscribed,
        public readonly array $logNamesFiltered,
    ) {
    }
}
