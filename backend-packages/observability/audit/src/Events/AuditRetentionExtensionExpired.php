<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability audit --force`.

declare(strict_types=1);

namespace Academorix\Audit\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ExpireRetentionOverridesJob detected override_ends_at <= now() on an active policy. Retention_years is RESET to config.audit.retention.default_years (7) and the policy is soft-deleted (deleted_at = override_ends_at).
 *
 * ## Consumers
 *
 * - audit::DispatchExtensionExpiredNotificationListener — informs the compliance officer that the extension window closed and the tenant is back on the default 7y retention
 * - audit::InvalidateRetentionCacheListener
 *
 * @category Audit
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AuditRetentionExtensionExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $auditRetentionPolicyId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $priorRetentionYears  int
     * @param  int  $newRetentionYears  int (= default_years)
     * @param  string  $overrideReason  string
     */
    public function __construct(
        public readonly string $auditRetentionPolicyId,
        public readonly string $tenantId,
        public readonly int $priorRetentionYears,
        public readonly int $newRetentionYears,
        public readonly string $overrideReason,
    ) {
    }
}
