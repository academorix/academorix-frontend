<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Monitoring\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MonitoringIncidentObserver.updated detected status transition to investigating / mitigating / postmortem. Not fired for open / acknowledged / resolved — those have dedicated events.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MonitoringIncidentStatusUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $monitoringIncidentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $previousStatus  string
     * @param  string  $newStatus  'investigating' | 'mitigating' | 'postmortem'
     * @param  string  $updatedByUserId  ULID
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $monitoringIncidentId,
        public readonly string $tenantId,
        public readonly string $previousStatus,
        public readonly string $newStatus,
        public readonly string $updatedByUserId,
        public readonly string $at,
    ) {
    }
}
