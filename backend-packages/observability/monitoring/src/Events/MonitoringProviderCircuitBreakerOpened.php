<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Monitoring\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CRITICAL. consecutive_failure_count on a provider_config reached failure_threshold. Sets circuit_breaker_open_until = now() + open_duration_seconds. Blocks all subsequent notifications until the timer elapses. Every fire is a P1 signal — the tenant's monitoring is telling them their monitoring is broken.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchMonitoringProviderCircuitBreakerOpenedNotification (cannot-opt-out — pages ops + notifies tenant owner + falls back to alternate paging provider)
 * - monitoring::FailoverToAlternateProvider (if this was PagerDuty: fail over to Opsgenie for pending notifications; if Opsgenie: fail over to PagerDuty)
 * - monitoring::MetricsCollector (increments academorix.monitoring.circuit_breakers.opened_total)
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
final readonly class MonitoringProviderCircuitBreakerOpened implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $providerConfigId  ULID
     * @param  int  $consecutiveFailureCount  integer
     * @param  string  $openUntil  ISO 8601
     * @param  string  $lastErrorCode  string | null
     * @param  string  $lastErrorMessage  string | null
     * @param  string  $openedAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $providerConfigId,
        public readonly int $consecutiveFailureCount,
        public readonly string $openUntil,
        public readonly string $lastErrorCode,
        public readonly string $lastErrorMessage,
        public readonly string $openedAt,
    ) {
    }
}
