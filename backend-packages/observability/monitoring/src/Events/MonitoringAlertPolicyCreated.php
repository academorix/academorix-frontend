<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Monitoring\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MonitoringAlertPolicyObserver.created committed a new monitoring_alert_policies row.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::PrimeEvaluatorCache (warms the AlertPolicyEvaluator's per-tenant policy cache)
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
final readonly class MonitoringAlertPolicyCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $monitoringAlertPolicyId  ULID (map_*)
     * @param  string  $tenantId  ULID
     * @param  string  $name  string
     * @param  string  $signalType  string
     * @param  string  $signalSource  string
     * @param  string  $severity  'p1' | 'p2' | 'p3' | 'p4'
     * @param  string  $thresholdOperator  string
     * @param  string  $thresholdValue  number
     * @param  int  $windowSeconds  integer
     * @param  bool  $isEnabled  boolean
     * @param  string  $createdByUserId  ULID
     * @param  string  $createdAt  ISO 8601
     */
    public function __construct(
        public readonly string $monitoringAlertPolicyId,
        public readonly string $tenantId,
        public readonly string $name,
        public readonly string $signalType,
        public readonly string $signalSource,
        public readonly string $severity,
        public readonly string $thresholdOperator,
        public readonly string $thresholdValue,
        public readonly int $windowSeconds,
        public readonly bool $isEnabled,
        public readonly string $createdByUserId,
        public readonly string $createdAt,
    ) {
    }
}
