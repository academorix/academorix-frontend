<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Monitoring\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /providers/{provider}/test invoked OR test_mode=true on a new config. Dispatches a synthetic alert via the provider's sandbox / test endpoint.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchMonitoringProviderTestResultNotification (success or failure)
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
final readonly class MonitoringProviderTestSent implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $providerConfigId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $testOutcome  'succeeded' | 'failed'
     * @param  int  $latencyMs  integer | null
     * @param  string  $sentByUserId  ULID
     * @param  string  $sentAt  ISO 8601
     */
    public function __construct(
        public readonly string $providerConfigId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $testOutcome,
        public readonly int $latencyMs,
        public readonly string $sentByUserId,
        public readonly string $sentAt,
    ) {
    }
}
