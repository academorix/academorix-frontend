<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Monitoring\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MonitoringProviderConfigObserver.created committed a new provider_configs row.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — provider connection is compliance-relevant)
 * - monitoring::MaybeDispatchProviderTest (when test_mode=true: fires MonitoringProviderTestSent via a synthetic alert)
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
final readonly class MonitoringProviderConfigured implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $providerConfigId  ULID (mpr_*)
     * @param  string  $tenantId  ULID
     * @param  string  $provider  'sentry' | 'datadog' | 'pagerduty' | 'opsgenie' | 'prometheus' | 'custom_webhook'
     * @param  bool  $testMode  boolean
     * @param  string  $enabledSeverities  string[]
     * @param  string  $configuredByUserId  ULID
     * @param  string  $configuredAt  ISO 8601
     */
    public function __construct(
        public readonly string $providerConfigId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly bool $testMode,
        public readonly string $enabledSeverities,
        public readonly string $configuredByUserId,
        public readonly string $configuredAt,
    ) {
    }
}
