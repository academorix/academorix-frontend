<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Monitoring\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * MonitoringUserSettings — My monitoring preferences.
 *
 * Per-user preferences for monitoring alerts + digest.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'monitoring_user',
    label: 'My monitoring preferences',
    description: 'Per-user preferences for monitoring alerts + digest.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 25,
)]
class MonitoringUserSettings extends Settings
{

    // ── Alert subscriptions ──────────────────────────────────
    #[SettingGroup(label: 'Alert subscriptions', icon: 'bell', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me on P1 alerts', group: 'Alert subscriptions', sortOrder: 1, helpText: 'P1 alerts are cannot-opt-out for the primary on-call user; this preference only applies when you are NOT the primary on-call.')]
    public bool $receive_p1_alerts = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me on P2 alerts', group: 'Alert subscriptions', sortOrder: 2)]
    public bool $receive_p2_alerts = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me on P3 alerts', group: 'Alert subscriptions', sortOrder: 3)]
    public bool $receive_p3_alerts = false;

    #[SettingField(controlType: ControlType::Text, label: 'Delivery channels', group: 'Alert subscriptions', sortOrder: 4, options: ['mail' => 'Email', 'in_app' => 'In-app', 'push' => 'Push notification', 'sms' => 'SMS (P1 only)'])]
    public array $alert_channels = ['mail', 'in_app', 'push'];


    // ── Weekly digest ──────────────────────────────────
    #[SettingGroup(label: 'Weekly digest', icon: 'chart-bar', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Send me the weekly SLO digest', group: 'Weekly digest', sortOrder: 1, helpText: 'Every Monday 09:00 tenant-local: alert counts by severity, MTTR, top 5 firing policies, top 5 unhealthy checks.')]
    public bool $receive_weekly_digest = false;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'monitoring_user';
    }
}
