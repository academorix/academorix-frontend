<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Monitoring\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * MonitoringSettings — System monitoring.
 *
 * Tenant-wide defaults for monitoring capture + notification routing.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'monitoring',
    label: 'System monitoring',
    description: 'Tenant-wide defaults for monitoring capture + notification routing.',
    icon: 'signal',
    scope: 'tenant',
    permission: 'monitoring.providers.viewAny',
    sortOrder: 20,
)]
class MonitoringSettings extends Settings
{

    // ── Alert defaults ──────────────────────────────────
    #[SettingGroup(label: 'Alert defaults', description: 'Tenant-wide defaults applied to newly-configured alert policies.', icon: 'bell-alert', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Number, label: 'Default consecutive breaches required', group: 'Alert defaults', sortOrder: 1, min: 1, max: 10, helpText: 'How many consecutive threshold breaches before an alert fires. 2 filters transient spikes; 1 fires on every single breach.')]
    public int $default_consecutive_breaches_required = 2;

    #[SettingField(controlType: ControlType::Number, label: 'Default threshold window (seconds)', group: 'Alert defaults', sortOrder: 2, min: 60, max: 3600, helpText: 'How much history the evaluator considers when computing the threshold value. 300s = 5min.')]
    public int $default_window_seconds = 300;


    // ── Suppression windows ──────────────────────────────────
    #[SettingGroup(label: 'Suppression windows', description: 'When to silence non-P1 alerts automatically.', icon: 'moon', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Time, label: 'After-hours start (tenant-local)', group: 'Suppression windows', sortOrder: 1)]
    public string $after_hours_start = '18:00';

    #[SettingField(controlType: ControlType::Time, label: 'After-hours end (tenant-local)', group: 'Suppression windows', sortOrder: 2)]
    public string $after_hours_end = '09:00';

    #[SettingField(controlType: ControlType::Text, label: 'Weekend days', group: 'Suppression windows', sortOrder: 3, options: ['monday' => 'Monday', 'tuesday' => 'Tuesday', 'wednesday' => 'Wednesday', 'thursday' => 'Thursday', 'friday' => 'Friday', 'saturday' => 'Saturday', 'sunday' => 'Sunday'])]
    public array $weekend_days = ['saturday', 'sunday'];

    #[SettingField(controlType: ControlType::Toggle, label: 'Also suppress P1 alerts after hours', group: 'Suppression windows', sortOrder: 4, helpText: 'WARNING. Default off — P1 alerts are pageable regardless of hours. Only enable when your on-call rotation is truly 09:00-18:00.')]
    public bool $suppress_p1_after_hours = false;


    // ── Notification routing ──────────────────────────────────
    #[SettingGroup(label: 'Notification routing', description: 'Where alert + incident notifications are delivered.', icon: 'bell', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Text, label: 'Primary on-call users', group: 'Notification routing', sortOrder: 1, helpText: 'Every user listed receives P1 + P2 MonitoringAlertFiredNotification. Tenant owners are always notified regardless.')]
    public array $primary_oncall_users = [];

    #[SettingField(controlType: ControlType::Text, label: 'Escalation on-call users', group: 'Notification routing', sortOrder: 2, helpText: 'Every user listed receives MonitoringIncidentEscalatedNotification when the primary on-call misses the SLA.')]
    public array $escalation_oncall_users = [];

    #[SettingField(controlType: ControlType::Text, label: 'Postmortem reviewers', group: 'Notification routing', sortOrder: 3, helpText: 'Every user listed receives MonitoringPostmortemRequiredNotification when a P1 incident resolves.')]
    public array $postmortem_reviewer_users = [];

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'monitoring';
    }
}
