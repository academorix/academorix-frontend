<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Monitoring\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `status` on `health-check-run`.
 *
 * Enum: healthy / degraded / unhealthy / timeout / error.
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
enum HealthCheckRunStatus: string
{
    use Enum;

    case Healthy = 'healthy';
    case Degraded = 'degraded';
    case Unhealthy = 'unhealthy';
    case Timeout = 'timeout';
    case Error = 'error';
}
