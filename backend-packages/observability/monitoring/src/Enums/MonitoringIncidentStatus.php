<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Monitoring\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `status` on `monitoring-incident`.
 *
 * Enum: open / acknowledged / investigating / mitigating / resolved / postmortem.
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
enum MonitoringIncidentStatus: string
{
    use Enum;

    case Open = 'open';
    case Acknowledged = 'acknowledged';
    case Investigating = 'investigating';
    case Mitigating = 'mitigating';
    case Resolved = 'resolved';
    case Postmortem = 'postmortem';
}
