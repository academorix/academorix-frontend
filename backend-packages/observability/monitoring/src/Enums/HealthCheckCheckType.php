<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Monitoring\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `check_type` on `health-check`.
 *
 * Enum: db_ping / redis_ping / queue_depth / memory_usage / cpu_usage / disk_usage / http_endpoint / dns_lookup / tls_expiry / custom_script.
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
enum HealthCheckCheckType: string
{
    use Enum;

    case DbPing = 'db_ping';
    case RedisPing = 'redis_ping';
    case QueueDepth = 'queue_depth';
    case MemoryUsage = 'memory_usage';
    case CpuUsage = 'cpu_usage';
    case DiskUsage = 'disk_usage';
    case HttpEndpoint = 'http_endpoint';
    case DnsLookup = 'dns_lookup';
    case TlsExpiry = 'tls_expiry';
    case CustomScript = 'custom_script';
}
