<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py observability monitoring --force`.

declare(strict_types=1);

namespace Academorix\Monitoring\Actions\Tenant;

use Academorix\Monitoring\Contracts\Repositories\HealthCheckRepositoryInterface;
use Academorix\Monitoring\Data\HealthCheckData;
use Academorix\Monitoring\Data\Requests\CreateHealthCheckRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/monitoring/health-checks` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Post(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Monitoring
 *
 * @since    0.1.0
 */
#[AsController]
#[Post('/api/v1/monitoring/health-checks')]
final class CreateHealthCheckAction
{
    public function __construct(
        private readonly HealthCheckRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `health-check` from the validated request payload.
     *
     * @param  CreateHealthCheckRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateHealthCheckRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(HealthCheckData::from($model), JsonResponse::HTTP_CREATED);
    }
}
