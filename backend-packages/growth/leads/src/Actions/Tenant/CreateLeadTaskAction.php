<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

namespace Academorix\Leads\Actions\Tenant;

use Academorix\Leads\Contracts\Repositories\LeadTaskRepositoryInterface;
use Academorix\Leads\Data\LeadTaskData;
use Academorix\Leads\Data\Requests\CreateLeadTaskRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/lead-tasks` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Post(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Leads
 *
 * @since    0.1.0
 */
#[AsController]
#[Post('/api/v1/lead-tasks')]
final class CreateLeadTaskAction
{
    public function __construct(
        private readonly LeadTaskRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `lead-task` from the validated request payload.
     *
     * @param  CreateLeadTaskRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateLeadTaskRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(LeadTaskData::from($model), JsonResponse::HTTP_CREATED);
    }
}
