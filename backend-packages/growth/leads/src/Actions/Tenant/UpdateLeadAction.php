<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

namespace Academorix\Leads\Actions\Tenant;

use Academorix\Leads\Contracts\Repositories\LeadRepositoryInterface;
use Academorix\Leads\Data\LeadData;
use Academorix\Leads\Data\Requests\UpdateLeadRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/leads/{lead}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Patch(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Leads
 *
 * @since    0.1.0
 */
#[AsController]
#[Patch('/api/v1/leads/{lead}')]
final class UpdateLeadAction
{
    public function __construct(
        private readonly LeadRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `lead` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateLeadRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateLeadRequestData $data): LeadData
    {
        $model = $this->repository->update($id, $data->toArray());

        return LeadData::from($model);
    }
}
