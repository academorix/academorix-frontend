<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

namespace Academorix\Leads\Actions\Tenant;

use Academorix\Leads\Contracts\Repositories\LeadTaskRepositoryInterface;
use Academorix\Leads\Data\LeadTaskData;
use Academorix\Leads\Data\Requests\UpdateLeadTaskRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/lead-tasks/{lead_task}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Patch(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Leads
 *
 * @since    0.1.0
 */
#[AsController]
#[Patch('/api/v1/lead-tasks/{lead_task}')]
final class UpdateLeadTaskAction
{
    public function __construct(
        private readonly LeadTaskRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `lead-task` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateLeadTaskRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateLeadTaskRequestData $data): LeadTaskData
    {
        $model = $this->repository->update($id, $data->toArray());

        return LeadTaskData::from($model);
    }
}
