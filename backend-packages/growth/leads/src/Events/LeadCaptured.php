<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

namespace Academorix\Leads\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * LeadObserver.creating committed a new leads row. Precedes attribution snapshot materialisation.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — regulator lookback on marketing conversions)
 * - leads::SnapshotAttributionOnLeadCapture (reads growth::attribution + freezes into leads.attribution_snapshot)
 * - notifications::DispatchLeadCapturedNotification (opt-in to marketing role — new-lead alerts)
 * - monitoring::MetricsCollector (increments academorix.leads.captured_total)
 *
 * @category Leads
 *
 * @since    0.1.0
 */
final readonly class LeadCaptured implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $leadId  ULID (led_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $source  string (web_form / referral / walk_in / call / email / social / trial_signup / import / api)
     * @param  string  $contactEmail  string | null
     * @param  string  $branchId  ULID (brn_*) | null
     * @param  string  $sportKey  string | null
     * @param  string  $capturedAt  ISO 8601
     */
    public function __construct(
        public readonly string $leadId,
        public readonly string $tenantId,
        public readonly string $source,
        public readonly string $contactEmail,
        public readonly string $branchId,
        public readonly string $sportKey,
        public readonly string $capturedAt,
    ) {
    }
}
