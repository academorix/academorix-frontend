<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

namespace Academorix\Leads\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Lead.stage transitions to LOST. lost_reason set at the same time. Terminal.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector (increments academorix.leads.lost_total; feeds loss-reason breakdown dashboard)
 *
 * @category Leads
 *
 * @since    0.1.0
 */
final readonly class LeadLost implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $leadId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromStage  string
     * @param  string  $lostReason  'budget' | 'timing' | 'competitor' | 'not_qualified' | 'ghost' | 'other'
     * @param  string  $ownerId  ULID | null
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $leadId,
        public readonly string $tenantId,
        public readonly string $fromStage,
        public readonly string $lostReason,
        public readonly string $ownerId,
        public readonly string $at,
    ) {
    }
}
