<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

namespace Academorix\Leads\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CRITICAL. Lead.stage transitions to WON. LeadConversionService materialised athletes into sports::registrations + attribution snapshot travels to marketing::MarketingEvent event_type=signup.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial conversion record)
 * - leads::MaterialiseAthletesOnLeadConverted (creates athlete + guardian rows via sports::registrations)
 * - marketing::MarketingEventCapturer (maps to marketing_events.event_type='signup' for ad-network fan-out)
 * - notifications::DispatchLeadConvertedNotification (owner + admin — celebrate + kick off onboarding)
 * - monitoring::MetricsCollector (increments academorix.leads.converted_total; feeds funnel-conversion dashboard)
 *
 * @category Leads
 *
 * @since    0.1.0
 */
final readonly class LeadConverted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $leadId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $ownerId  ULID | null
     * @param  string  $source  string
     * @param  string  $convertedAthleteIds  ULID[] (ath_*)
     * @param  array  $attributionSnapshot  object (frozen at capture — passed through to downstream consumers)
     * @param  int  $estimatedLifetimeValueCents  integer | null
     * @param  string  $currency  string (ISO 4217) | null
     * @param  string  $convertedAt  ISO 8601
     */
    public function __construct(
        public readonly string $leadId,
        public readonly string $tenantId,
        public readonly string $ownerId,
        public readonly string $source,
        public readonly string $convertedAthleteIds,
        public readonly array $attributionSnapshot,
        public readonly int $estimatedLifetimeValueCents,
        public readonly string $currency,
        public readonly string $convertedAt,
    ) {
    }
}
