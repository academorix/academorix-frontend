<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

namespace Academorix\Leads\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `type` on `lead-activity`.
 *
 * Enum: note / email / call / meeting / form_submission / stage_change / assignment / trial_scheduled / trial_attended.
 *
 * @category Leads
 *
 * @since    0.1.0
 */
enum LeadActivityType: string
{
    use Enum;

    case Note = 'note';
    case Email = 'email';
    case Call = 'call';
    case Meeting = 'meeting';
    case FormSubmission = 'form_submission';
    case StageChange = 'stage_change';
    case Assignment = 'assignment';
    case TrialScheduled = 'trial_scheduled';
    case TrialAttended = 'trial_attended';
}
