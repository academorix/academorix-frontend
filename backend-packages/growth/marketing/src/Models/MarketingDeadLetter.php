<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Marketing\Contracts\Data\MarketingDeadLetterInterface;
use Academorix\Marketing\Database\Factories\MarketingDeadLetterFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a MarketingDeadLetter.
 *
 * Max-attempts-exceeded events for manual replay.
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[Table(name: MarketingDeadLetterInterface::TABLE, keyType: MarketingDeadLetterInterface::KEY_TYPE)]
#[Fillable([
    MarketingDeadLetterInterface::ATTR_TENANT_ID,
        MarketingDeadLetterInterface::ATTR_MARKETING_EVENT_ID,
        MarketingDeadLetterInterface::ATTR_PROVIDER,
        MarketingDeadLetterInterface::ATTR_REASON,
        MarketingDeadLetterInterface::ATTR_FINAL_ERROR_DETAILS,
        MarketingDeadLetterInterface::ATTR_REPLAY_ATTEMPTS,
        MarketingDeadLetterInterface::ATTR_LAST_REPLAY_AT,
        MarketingDeadLetterInterface::ATTR_RESOLVED_AT,
        MarketingDeadLetterInterface::ATTR_RESOLVED_BY_USER_ID,
        MarketingDeadLetterInterface::ATTR_RESOLUTION_NOTE,
        MarketingDeadLetterInterface::ATTR_METADATA,
])]
#[UseFactory(MarketingDeadLetterFactory::class)]
final class MarketingDeadLetter extends Model implements MarketingDeadLetterInterface
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    // TODO(gen): resolve unknown trait `BelongsToMarketingEvent` — add its import + use line.
    use HasMetadata;
    use HasUserstamps;
    use Auditable;
    use HasActivityLog;
    use Filterable;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        MarketingDeadLetterInterface::ATTR_PROVIDER => 'MarketingProvider',
        MarketingDeadLetterInterface::ATTR_REASON => 'MarketingDeadLetterReason',
        MarketingDeadLetterInterface::ATTR_FINAL_ERROR_DETAILS => 'array',
        MarketingDeadLetterInterface::ATTR_REPLAY_ATTEMPTS => 'integer',
        MarketingDeadLetterInterface::ATTR_LAST_REPLAY_AT => 'datetime',
        MarketingDeadLetterInterface::ATTR_RESOLVED_AT => 'datetime',
        MarketingDeadLetterInterface::ATTR_METADATA => 'array',
    ];
}
