<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * DispatchToProviderJob started + the pre-dispatch ConsentGate::allows check returned false. Delivery.status = 'suppressed_by_consent'. Never touches the wire. Fires when the user revoked consent between capture and dispatch.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (compliance record — dispatch-time consent-honored suppression)
 * - monitoring::MetricsCollector
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
final readonly class MarketingDeliverySuppressedByConsent implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $marketingDeliveryId  ULID
     * @param  string  $marketingEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $revokedConsentCategory  'marketing' | 'advertising'
     * @param  bool  $revokedBetweenCaptureAndDispatch  boolean (true when consent was valid at capture but revoked by dispatch)
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $marketingDeliveryId,
        public readonly string $marketingEventId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $revokedConsentCategory,
        public readonly bool $revokedBetweenCaptureAndDispatch,
        public readonly string $at,
    ) {
    }
}
