<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Provider config is_active flipped to false OR the row is soft-deleted. Stops all future dispatches; in-flight retries drain but no new attempts.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchMarketingConfigurationChangedNotification
 * - monitoring::MetricsCollector
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MarketingProviderDeactivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $providerConfigId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $deactivatedByUserId  ULID
     * @param  string  $deactivatedAt  ISO 8601
     * @param  string  $reason  'manual' | 'tenant_suspended' | 'entitlement_revoked'
     */
    public function __construct(
        public readonly string $providerConfigId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $deactivatedByUserId,
        public readonly string $deactivatedAt,
        public readonly string $reason,
    ) {
    }
}
