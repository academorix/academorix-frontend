<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MarketingEventObserver.creating committed a new marketing_events row. Fires exactly once per (tenant_id, deduplication_key) tuple. Precedes MarketingEventDispatched by the DispatchMarketingEventJob run.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - marketing::EnqueueDispatchMarketingEventJob (spawns per-provider fan-out)
 * - monitoring::MetricsCollector (increments academorix.marketing.events.captured_total)
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
final readonly class MarketingEventCaptured implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $marketingEventId  ULID (mev_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $eventType  string (25+ enum values)
     * @param  string  $source  'system' | 'api' | 'import' | 'migration' | 'manual'
     * @param  string  $subjectType  string (User / Athlete / Membership / Subscription / Lead)
     * @param  string  $subjectId  ULID (polymorphic)
     * @param  int  $valueAmountCents  integer | null
     * @param  string  $valueCurrency  string (ISO 4217) | null
     * @param  int  $eligibleProviderCount  integer
     * @param  string  $deduplicationKey  string (128 chars)
     * @param  string  $capturedAt  ISO 8601
     */
    public function __construct(
        public readonly string $marketingEventId,
        public readonly string $tenantId,
        public readonly string $eventType,
        public readonly string $source,
        public readonly string $subjectType,
        public readonly string $subjectId,
        public readonly int $valueAmountCents,
        public readonly string $valueCurrency,
        public readonly int $eligibleProviderCount,
        public readonly string $deduplicationKey,
        public readonly string $capturedAt,
    ) {
    }
}
