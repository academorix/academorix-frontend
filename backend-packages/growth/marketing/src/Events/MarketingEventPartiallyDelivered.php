<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * At least one provider succeeded AND at least one failed permanently (dead-lettered). Event.status = 'partially_delivered'. Distinct from MarketingEventFailed (all providers failed).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MarketingEventPartiallyDelivered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $marketingEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $eventType  string
     * @param  string  $deliveredProviders  string[]
     * @param  string  $failedProviders  string[]
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $marketingEventId,
        public readonly string $tenantId,
        public readonly string $eventType,
        public readonly string $deliveredProviders,
        public readonly string $failedProviders,
        public readonly string $at,
    ) {
    }
}
