<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MarketingEventObserver.creating detected that the subject lacks `marketing` or `advertising` consent. Refuses capture. Event.status = 'suppressed_by_consent'. This is the CAPTURE-time gate; distinct from MarketingDeliverySuppressedByConsent (dispatch-time re-check on a captured event).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (compliance record — every suppressed capture is a consent-honored event)
 * - monitoring::MetricsCollector (increments academorix.marketing.events.suppressed_by_consent_total; spike drives MarketingConsentSuppressionSpikeNotification)
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
final readonly class MarketingEventSuppressedByConsent implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $eventType  string
     * @param  string  $subjectType  string
     * @param  string  $subjectId  ULID
     * @param  string  $missingConsentCategories  string[] (subset of ['marketing', 'advertising'])
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $eventType,
        public readonly string $subjectType,
        public readonly string $subjectId,
        public readonly string $missingConsentCategories,
        public readonly string $at,
    ) {
    }
}
