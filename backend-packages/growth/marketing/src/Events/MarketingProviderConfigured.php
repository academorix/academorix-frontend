<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MarketingProviderConfigObserver committed a new provider config row.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — provider connection is a compliance-relevant integration)
 * - notifications::DispatchMarketingConfigurationChangedNotification (audit trail for tenant admins)
 * - marketing::MaybeDispatchProviderTest (fires MarketingProviderTestSent when test_mode=true)
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
final readonly class MarketingProviderConfigured implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $providerConfigId  ULID (mpc_*)
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  bool  $testMode  boolean
     * @param  string  $enabledEventTypes  string[]
     * @param  string  $configuredByUserId  ULID
     * @param  string  $configuredAt  ISO 8601
     */
    public function __construct(
        public readonly string $providerConfigId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly bool $testMode,
        public readonly string $enabledEventTypes,
        public readonly string $configuredByUserId,
        public readonly string $configuredAt,
    ) {
    }
}
