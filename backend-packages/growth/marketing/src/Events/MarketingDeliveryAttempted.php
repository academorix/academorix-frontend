<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * DispatchToProviderJob started an attempt against a provider. Fires ONCE per (marketing_event, provider, attempt_number). Precedes MarketingDeliverySucceeded or MarketingDeliveryFailed.
 *
 * ## Consumers
 *
 * - monitoring::MetricsCollector (increments academorix.marketing.deliveries.attempted_total)
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
final readonly class MarketingDeliveryAttempted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $marketingDeliveryId  ULID (mdv_*)
     * @param  string  $marketingEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string (provider slug)
     * @param  int  $attemptNumber  integer
     * @param  string  $attemptedAt  ISO 8601
     */
    public function __construct(
        public readonly string $marketingDeliveryId,
        public readonly string $marketingEventId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly int $attemptNumber,
        public readonly string $attemptedAt,
    ) {
    }
}
