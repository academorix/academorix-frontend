<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Test-mode dispatch returned 4xx/5xx OR validation failed. Signals a mis-configured credential / wrong pixel_id / invalid webhook_url.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchMarketingProviderTestFailedNotification (cannot-opt-out — the tenant admin needs to fix the config before it goes live)
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
final readonly class MarketingProviderTestFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $providerConfigId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $errorCode  string | null
     * @param  string  $errorMessage  string
     * @param  int  $responseStatusCode  integer | null
     * @param  string  $failedAt  ISO 8601
     */
    public function __construct(
        public readonly string $providerConfigId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $errorCode,
        public readonly string $errorMessage,
        public readonly int $responseStatusCode,
        public readonly string $failedAt,
    ) {
    }
}
