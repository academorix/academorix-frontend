<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Either (a) a replay attempt succeeded (auto-resolved) OR (b) ops manually marked the dead letter resolved via POST /dead-letters/{id}/resolve. Sets resolved_at + resolved_by_user_id + resolution_note.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - monitoring::MetricsCollector
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
final readonly class MarketingDeadLetterResolved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $marketingDeadLetterId  ULID
     * @param  string  $marketingEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $resolvedByUserId  ULID | null (null = auto-resolved on replay success)
     * @param  string  $resolvedAt  ISO 8601
     * @param  string  $resolutionNote  string | null
     * @param  bool  $wasAutoResolved  boolean
     */
    public function __construct(
        public readonly string $marketingDeadLetterId,
        public readonly string $marketingEventId,
        public readonly string $tenantId,
        public readonly string $resolvedByUserId,
        public readonly string $resolvedAt,
        public readonly string $resolutionNote,
        public readonly bool $wasAutoResolved,
    ) {
    }
}
