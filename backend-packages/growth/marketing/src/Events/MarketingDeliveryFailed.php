<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Provider returned 4xx/5xx OR request timed out OR response validation failed. Delivery.status = 'failed'. Increments provider's consecutive_failure_count. Schedules next retry when attempt_number < max_attempts + is_transient.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector
 * - marketing::MaybeOpenCircuitBreaker (checks if consecutive_failure_count >= threshold; opens the breaker)
 * - marketing::MaybeCreateDeadLetter (when attempt_number == max_attempts OR is_transient=false — creates the MarketingDeadLetter row)
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
final readonly class MarketingDeliveryFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $marketingDeliveryId  ULID
     * @param  string  $marketingEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  int  $attemptNumber  integer
     * @param  int  $responseStatusCode  integer | null
     * @param  string  $errorCode  string | null
     * @param  string  $errorMessage  string
     * @param  bool  $isTransient  boolean
     * @param  string  $nextRetryAt  ISO 8601 | null (null = terminal failure, no retry)
     * @param  string  $failedAt  ISO 8601
     */
    public function __construct(
        public readonly string $marketingDeliveryId,
        public readonly string $marketingEventId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly int $attemptNumber,
        public readonly int $responseStatusCode,
        public readonly string $errorCode,
        public readonly string $errorMessage,
        public readonly bool $isTransient,
        public readonly string $nextRetryAt,
        public readonly string $failedAt,
    ) {
    }
}
