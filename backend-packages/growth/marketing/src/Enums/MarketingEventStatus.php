<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `status` on `marketing-event`.
 *
 * Enum: pending / dispatching / delivered / partially_delivered / failed / suppressed_by_consent / superseded.
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
enum MarketingEventStatus: string
{
    use Enum;

    case Pending = 'pending';
    case Dispatching = 'dispatching';
    case Delivered = 'delivered';
    case PartiallyDelivered = 'partially_delivered';
    case Failed = 'failed';
    case SuppressedByConsent = 'suppressed_by_consent';
    case Superseded = 'superseded';
}
