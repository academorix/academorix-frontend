<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Attribution\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MergeIdentifiedAttributionsJob completed a merge: N anonymous profiles merged into 1 identified profile. Enterprise-only (gated by `attribution_merge`).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchAttributionMergeCompletedNotification (opt-in to admin)
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
final readonly class AttributionMerged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $mergedIntoAttributionId  ULID (the identified profile that absorbed the others)
     * @param  string  $tenantId  ULID
     * @param  string  $mergedFromAttributionIds  ULID[] (the anonymous profiles absorbed)
     * @param  int  $mergedTouchpointCount  integer
     * @param  bool  $firstTouchOverwritten  boolean (true when anon profile's first_touch predated identified's)
     * @param  string  $mergedAt  ISO 8601
     * @param  string  $mergedByJobId  ULID
     */
    public function __construct(
        public readonly string $mergedIntoAttributionId,
        public readonly string $tenantId,
        public readonly string $mergedFromAttributionIds,
        public readonly int $mergedTouchpointCount,
        public readonly bool $firstTouchOverwritten,
        public readonly string $mergedAt,
        public readonly string $mergedByJobId,
    ) {
    }
}
