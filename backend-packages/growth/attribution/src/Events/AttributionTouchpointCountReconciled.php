<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Attribution\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReconcileAttributionCountsJob completed a reconciliation pass. Fires only when a mismatch was found + corrected (touchpoint_count or conversion_touchpoint_count drifted from the actual row count).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - observability::MetricsCollector (increments academorix.attribution.count_drift_corrections)
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
final readonly class AttributionTouchpointCountReconciled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attributionId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $wasTouchpointCount  integer
     * @param  int  $actualTouchpointCount  integer
     * @param  int  $wasConversionTouchpointCount  integer
     * @param  int  $actualConversionTouchpointCount  integer
     * @param  string  $reconciledAt  ISO 8601
     */
    public function __construct(
        public readonly string $attributionId,
        public readonly string $tenantId,
        public readonly int $wasTouchpointCount,
        public readonly int $actualTouchpointCount,
        public readonly int $wasConversionTouchpointCount,
        public readonly int $actualConversionTouchpointCount,
        public readonly string $reconciledAt,
    ) {
    }
}
