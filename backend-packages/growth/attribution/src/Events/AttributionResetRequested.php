<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Attribution\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * User (or admin on their behalf) invoked POST /api/v1/attributions/{attribution}/reset. Precedes AttributionResetConfirmed by the 90-day audit hold on touchpoint hard-delete.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — GDPR erasure request)
 * - activity::WriteToActivityLog
 * - notifications::DispatchAttributionResetConfirmationNotification
 * - compliance::LogGdprErasureRequest
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
final readonly class AttributionResetRequested implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attributionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $requestedByUserId  ULID
     * @param  string  $requestedForSubjectType  string
     * @param  string  $requestedForSubjectId  ULID | UUID
     * @param  string  $reason  string | null
     * @param  string  $trigger  'user_request' | 'admin_action' | 'regulator_request' | 'force_reset'
     * @param  string  $touchpointPurgeScheduledFor  ISO 8601 (now + 90d)
     */
    public function __construct(
        public readonly string $attributionId,
        public readonly string $tenantId,
        public readonly string $requestedByUserId,
        public readonly string $requestedForSubjectType,
        public readonly string $requestedForSubjectId,
        public readonly string $reason,
        public readonly string $trigger,
        public readonly string $touchpointPurgeScheduledFor,
    ) {
    }
}
