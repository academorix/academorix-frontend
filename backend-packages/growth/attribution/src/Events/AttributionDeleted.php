<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Attribution\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * attributions.deleted_at set (soft-delete) OR row hard-deleted. Distinct from AttributionReset (reset is user-initiated erasure; delete is admin operational).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
final readonly class AttributionDeleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attributionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $deletedByUserId  ULID
     * @param  string  $deletionKind  'soft' | 'hard'
     * @param  string  $deletedAt  ISO 8601
     */
    public function __construct(
        public readonly string $attributionId,
        public readonly string $tenantId,
        public readonly string $deletedByUserId,
        public readonly string $deletionKind,
        public readonly string $deletedAt,
    ) {
    }
}
