<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Attribution\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AttributionTouchpointObserver committed a new attribution_touchpoints row. Fires exactly once per touchpoint. High-volume event — every attribution-bearing HTTP request produces one.
 *
 * ## Consumers
 *
 * - cache::IncrementAttributionTouchpointCount
 * - analytics::MaybeAttributeCampaignTouchpoint (Wave 5)
 * - referrals::MaybeMatchReferralLink (Wave 5)
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
final readonly class AttributionTouchpointCaptured implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $touchpointId  ULID (atp_*)
     * @param  string  $attributionId  ULID (att_*)
     * @param  string  $tenantId  ULID
     * @param  string  $subjectType  string
     * @param  string  $subjectId  ULID | UUID
     * @param  string  $touchpointType  'page_view' | 'campaign_click' | 'referral_click' | 'direct_visit' | 'email_open' | 'email_click' | 'paid_ad_click' | 'organic_click' | 'social_click' | 'affiliate_click' | 'referral_link_click'
     * @param  string  $source  string
     * @param  string  $medium  string | null
     * @param  string  $campaign  string | null
     * @param  string  $landingPage  string
     * @param  bool  $wasFirstTouch  boolean
     * @param  bool  $wasConversion  boolean
     * @param  string  $sessionId  UUID
     * @param  string  $capturedAt  ISO 8601
     */
    public function __construct(
        public readonly string $touchpointId,
        public readonly string $attributionId,
        public readonly string $tenantId,
        public readonly string $subjectType,
        public readonly string $subjectId,
        public readonly string $touchpointType,
        public readonly string $source,
        public readonly string $medium,
        public readonly string $campaign,
        public readonly string $landingPage,
        public readonly bool $wasFirstTouch,
        public readonly bool $wasConversion,
        public readonly string $sessionId,
        public readonly string $capturedAt,
    ) {
    }
}
