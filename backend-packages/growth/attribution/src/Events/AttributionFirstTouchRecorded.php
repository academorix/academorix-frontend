<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Attribution\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Fires alongside AttributionCreated inside the same afterCommit hook. Distinct event because downstream consumers care specifically about first-touch data for cohort analysis (analytics module) and revenue attribution (finance module Wave 5).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - analytics::TrackFirstTouchCohort (Wave 5)
 * - notifications::MaybeDispatchWelcomeCampaign
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
final readonly class AttributionFirstTouchRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attributionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $subjectType  string
     * @param  string  $subjectId  ULID | UUID
     * @param  string  $source  string
     * @param  string  $medium  string | null
     * @param  string  $campaign  string | null
     * @param  string  $content  string | null
     * @param  string  $term  string | null
     * @param  string  $landingPage  string
     * @param  string  $referrer  string | null
     * @param  array  $clickId  object (jsonb)
     * @param  array  $device  object (jsonb)
     * @param  string  $capturedAt  ISO 8601
     */
    public function __construct(
        public readonly string $attributionId,
        public readonly string $tenantId,
        public readonly string $subjectType,
        public readonly string $subjectId,
        public readonly string $source,
        public readonly string $medium,
        public readonly string $campaign,
        public readonly string $content,
        public readonly string $term,
        public readonly string $landingPage,
        public readonly string $referrer,
        public readonly array $clickId,
        public readonly array $device,
        public readonly string $capturedAt,
    ) {
    }
}
