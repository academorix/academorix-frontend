<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Attribution\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan attribution:audit-consent-suppressions` — Audit consent suppressions — attribution command.
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'attribution:audit-consent-suppressions',
    description: 'Audit consent suppressions — attribution command.',
)]
final class AuditConsentSuppressionsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Audit consent suppressions — attribution command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Audit consent suppressions — attribution command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
