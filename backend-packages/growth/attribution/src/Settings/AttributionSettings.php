<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Attribution\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * AttributionSettings — Attribution.
 *
 * Tenant-wide attribution capture + rollup preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'attribution',
    label: 'Attribution',
    description: 'Tenant-wide attribution capture + rollup preferences.',
    icon: 'chart-bar',
    scope: 'tenant',
    permission: 'attribution.viewAny',
    sortOrder: 60,
)]
class AttributionSettings extends Settings
{

    // ── Capture ──────────────────────────────────
    #[SettingGroup(label: 'Capture', description: 'How attribution is captured at request boundary.', icon: 'cloud-arrow-down', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enable attribution capture', group: 'Capture', sortOrder: 1, helpText: 'When off, all downstream marketing / analytics / referrals events attribute against a null profile. Privacy-conservative deployments may prefer off.')]
    public bool $capture_enabled = true;

    #[SettingField(controlType: ControlType::Number, label: 'Session TTL (hours)', group: 'Capture', sortOrder: 2, min: 1, max: 168, helpText: 'How long a session_id cookie lasts before a fresh one is minted.')]
    public int $session_ttl_hours = 24;

    #[SettingField(controlType: ControlType::Number, label: 'First-touch TTL (days)', group: 'Capture', sortOrder: 3, min: 30, max: 3650, helpText: 'How long the first-touch cookie lasts. After expiry, a new profile is created on next visit.')]
    public int $first_touch_ttl_days = 365;

    #[SettingField(controlType: ControlType::Toggle, label: 'Strip query parameters from referrer URLs', group: 'Capture', sortOrder: 4, helpText: 'Recommended. Prevents accidental PII leakage in referrer strings from third-party sites.')]
    public bool $strip_pii_from_referrer = true;


    // ── Click IDs ──────────────────────────────────
    #[SettingGroup(label: 'Click IDs', description: 'Which ad-network click IDs to capture.', icon: 'cursor-arrow-rays', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Google Ads (gclid)', group: 'Click IDs', sortOrder: 1)]
    public bool $capture_google = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Meta (fbclid)', group: 'Click IDs', sortOrder: 2)]
    public bool $capture_meta = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'TikTok (ttclid)', group: 'Click IDs', sortOrder: 3)]
    public bool $capture_tiktok = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Microsoft Bing (msclkid)', group: 'Click IDs', sortOrder: 4)]
    public bool $capture_microsoft = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Snapchat (sccid)', group: 'Click IDs', sortOrder: 5)]
    public bool $capture_snapchat = true;


    // ── Rollup ──────────────────────────────────
    #[SettingGroup(label: 'Rollup', description: 'Campaign performance rollup configuration.', icon: 'chart-pie', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Select, label: 'Rollup cadence', group: 'Rollup', sortOrder: 1, options: ['disabled' => 'Disabled', 'every-15-min' => 'Every 15 minutes', 'hourly' => 'Hourly'], helpText: 'How often campaign performance is recomputed.')]
    public string $rollup_schedule = 'hourly';

    #[SettingField(controlType: ControlType::Number, label: 'Top campaigns shown in widget', group: 'Rollup', sortOrder: 2, min: 3, max: 20)]
    public int $top_campaigns_widget_count = 5;


    // ── Merge ──────────────────────────────────
    #[SettingGroup(label: 'Merge', description: 'Cross-device profile merging (Enterprise-only).', icon: 'arrow-path-rounded-square', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Number, label: 'Match window (days)', group: 'Merge', sortOrder: 1, min: 1, max: 90, helpText: 'How far back to look for anonymous profiles to absorb into an identified profile.')]
    public int $merge_match_window_days = 30;

    #[SettingField(controlType: ControlType::Select, label: 'First-touch overwrite policy', group: 'Merge', sortOrder: 2, options: ['earlier_wins' => 'Earlier first-touch wins (default)', 'keep_identified' => 'Keep identified profile\'s first-touch', 'manual_review' => 'Flag for admin review'])]
    public string $merge_first_touch_policy = 'earlier_wins';


    // ── Consent + Compliance ──────────────────────────────────
    #[SettingGroup(label: 'Consent + Compliance', description: 'Consent gate + suppression digest configuration.', icon: 'shield-check', sortOrder: 5)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Honor Sec-GPC HTTP header', group: 'Consent + Compliance', sortOrder: 1, helpText: 'Recommended. Global Privacy Control signal treated as consent withdrawal.')]
    public bool $honor_gpc_signal = true;

    #[SettingField(controlType: ControlType::Select, label: 'Consent suppression digest cadence', group: 'Consent + Compliance', sortOrder: 2, options: ['disabled' => 'Disabled', 'hourly' => 'Hourly', 'daily' => 'Daily', 'weekly' => 'Weekly'], helpText: 'How often the compliance-team receives the AttributionSuppressedByConsent digest.')]
    public string $suppression_notification_frequency = 'daily';

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'attribution';
    }
}
