<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Attribution\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Attribution module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum AttributionPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `attribution.viewAny` — See the tenant's attribution profile list.
     */
    #[Label('See the tenant\'s attribution profile list')]
    #[Description('See the tenant\'s attribution profile list. Scoped: marketing sees aggregated (no PII); admin + owner see full rows.')]
    case AttributionViewAny = 'attribution.viewAny';

    /**
     * `attribution.view` — See one attribution profile.
     */
    #[Label('See one attribution profile')]
    #[Description('See one attribution profile. Same scope as viewAny.')]
    case AttributionView = 'attribution.view';

    /**
     * `attribution.view-own` — See own attribution profile via GET /me/attribution.
     */
    #[Label('See own attribution profile via GET /me/attribution')]
    #[Description('See own attribution profile via GET /me/attribution. Seeded to every authenticated role — GDPR Art. 15 right of access.')]
    case AttributionViewOwn = 'attribution.view-own';

    /**
     * `attribution.view-all` — See EVERY attribution profile in the tenant (admin drill-down).
     */
    #[Label('See EVERY attribution profile in the tenant (admin drill-down)')]
    #[Description('See EVERY attribution profile in the tenant (admin drill-down). Distinct from viewAny (which is filterable and marketing-scoped).')]
    case AttributionViewAll = 'attribution.view-all';

    /**
     * `attribution.view-confidential` — See confidential fields (click IDs, ip_hash, user_agent_hash, device_snapshot).
     */
    #[Label('See confidential fields (click IDs, ip_hash, user_agent_hash, device_snapshot)')]
    #[Description('See confidential fields (click IDs, ip_hash, user_agent_hash, device_snapshot). Redacted by default for other roles.')]
    case AttributionViewConfidential = 'attribution.view-confidential';

    /**
     * `attribution.reset` — Invoke the GDPR right-to-erasure reset flow on an attribution profile.
     */
    #[Label('Invoke the GDPR right-to-erasure reset flow on an attribution profile')]
    #[Description('Invoke the GDPR right-to-erasure reset flow on an attribution profile.')]
    case AttributionReset = 'attribution.reset';

    /**
     * `attribution.reset-own` — Reset own attribution profile.
     */
    #[Label('Reset own attribution profile')]
    #[Description('Reset own attribution profile. Seeded to every authenticated role — GDPR Art. 17 right to erasure.')]
    case AttributionResetOwn = 'attribution.reset-own';

    /**
     * `attribution.merge` — Manually merge N anonymous profiles into an identified profile.
     */
    #[Label('Manually merge N anonymous profiles into an identified profile')]
    #[Description('Manually merge N anonymous profiles into an identified profile. Enterprise-only.')]
    case AttributionMerge = 'attribution.merge';

    /**
     * `attribution.import` — Bulk CSV import from external CRMs.
     */
    #[Label('Bulk CSV import from external CRMs')]
    #[Description('Bulk CSV import from external CRMs. Enterprise-only.')]
    case AttributionImport = 'attribution.import';

    /**
     * `attribution.view-rollup` — See the campaign performance rollup dashboards.
     */
    #[Label('See the campaign performance rollup dashboards')]
    #[Description('See the campaign performance rollup dashboards. Aggregated data only — no per-subject PII.')]
    case AttributionViewRollup = 'attribution.view-rollup';

    /**
     * `attribution.view-audit-usage` — See attribution-module operational reports (consent-suppression audit, count-drift reconciler findings).
     */
    #[Label('See attribution-module operational reports (consent-suppression audit, count-dri')]
    #[Description('See attribution-module operational reports (consent-suppression audit, count-drift reconciler findings).')]
    case AttributionViewAuditUsage = 'attribution.view-audit-usage';

    /**
     * `attribution.view-consent-suppressions` — See the consent-suppression audit report (compliance-team specific — DPO role).
     */
    #[Label('See the consent-suppression audit report (compliance-team specific — DPO role)')]
    #[Description('See the consent-suppression audit report (compliance-team specific — DPO role).')]
    case AttributionViewConsentSuppressions = 'attribution.view-consent-suppressions';

    /**
     * `platform.attribution.viewAny` — Platform Attribution ViewAny
     */
    #[Label('Platform Attribution ViewAny')]
    #[Description('Platform Attribution ViewAny')]
    case PlatformAttributionViewAny = 'platform.attribution.viewAny';

    /**
     * `platform.attribution.view` — Platform Attribution View
     */
    #[Label('Platform Attribution View')]
    #[Description('Platform Attribution View')]
    case PlatformAttributionView = 'platform.attribution.view';

    /**
     * `platform.attribution.force-reset` — Support intervention to reset an attribution bypassing every tenant-level guard.
     */
    #[Label('Support intervention to reset an attribution bypassing every tenant-level guard')]
    #[Description('Support intervention to reset an attribution bypassing every tenant-level guard. Regulator-issued erasure requests. Every fire is audit-critical + notifies the tenant admin.')]
    case PlatformAttributionForceReset = 'platform.attribution.force-reset';

    /**
     * `platform.attribution.view-audit-usage` — Cross-tenant attribution operational report for capacity planning + compliance-team review.
     */
    #[Label('Cross-tenant attribution operational report for capacity planning + compliance-t')]
    #[Description('Cross-tenant attribution operational report for capacity planning + compliance-team review.')]
    case PlatformAttributionViewAuditUsage = 'platform.attribution.view-audit-usage';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::AttributionViewAny => Guard::Sanctum,
            self::AttributionView => Guard::Sanctum,
            self::AttributionViewOwn => Guard::Sanctum,
            self::AttributionViewAll => Guard::Sanctum,
            self::AttributionViewConfidential => Guard::Sanctum,
            self::AttributionReset => Guard::Sanctum,
            self::AttributionResetOwn => Guard::Sanctum,
            self::AttributionMerge => Guard::Sanctum,
            self::AttributionImport => Guard::Sanctum,
            self::AttributionViewRollup => Guard::Sanctum,
            self::AttributionViewAuditUsage => Guard::Sanctum,
            self::AttributionViewConsentSuppressions => Guard::Sanctum,
            self::PlatformAttributionViewAny => Guard::PlatformAdmin,
            self::PlatformAttributionView => Guard::PlatformAdmin,
            self::PlatformAttributionForceReset => Guard::PlatformAdmin,
            self::PlatformAttributionViewAuditUsage => Guard::PlatformAdmin,
        };
    }
}
