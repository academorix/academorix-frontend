<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * ReferralsSettings — Referrals.
 *
 * Tenant-wide defaults for referral programs + fraud detection.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'referrals',
    label: 'Referrals',
    description: 'Tenant-wide defaults for referral programs + fraud detection.',
    icon: 'gift',
    scope: 'tenant',
    permission: 'referral_programs.viewAny',
    sortOrder: 45,
)]
class ReferralsSettings extends Settings
{

    // ── Program defaults ──────────────────────────────────
    #[SettingGroup(label: 'Program defaults', description: 'Default values applied to newly-created programs. Overridable per program.', icon: 'cog', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Default reward type', group: 'Program defaults', sortOrder: 1, options: ['percent_discount' => 'Percent discount', 'fixed_amount' => 'Fixed amount', 'free_months' => 'Free months (Medium+)', 'free_sessions' => 'Free sessions (Medium+)', 'credits' => 'Credits (Medium+)', 'point_award' => 'Point award (Medium+)'])]
    public string $default_reward_type = 'percent_discount';

    #[SettingField(controlType: ControlType::Number, label: 'Default reward amount', group: 'Program defaults', sortOrder: 2, min: 1, helpText: 'Interpretation depends on reward_type — percent 1..100; fixed_amount in cents; free_months in months; credits in cents; points in points.')]
    public int $default_reward_amount = 10;

    #[SettingField(controlType: ControlType::Select, label: 'Default trigger event', group: 'Program defaults', sortOrder: 3, options: ['signup' => 'Signup', 'trial_started' => 'Trial started', 'first_payment' => 'First payment', 'subscription_renewal_1' => 'First renewal', 'subscription_renewal_3' => 'Third renewal', 'lifetime_value_threshold_hit' => 'LTV threshold', 'athlete_enrolled' => 'Athlete enrolled', 'event_registered' => 'Event registered'])]
    public string $default_trigger_event = 'first_payment';

    #[SettingField(controlType: ControlType::Select, label: 'Default vesting rule', group: 'Program defaults', sortOrder: 4, options: ['immediate' => 'Immediate', 'trigger_plus_hold_days' => 'Trigger + hold days', 'after_refund_window_closes' => 'After refund window closes', 'n_events_completed' => 'N events completed', 'manual_approval' => 'Manual approval (Enterprise)'])]
    public string $default_vesting_rule = 'trigger_plus_hold_days';

    #[SettingField(controlType: ControlType::Number, label: 'Default vesting hold (days)', group: 'Program defaults', sortOrder: 5, min: 1, max: 365, helpText: 'Used when vesting rule is \'Trigger + hold days\'. Aligns with refund windows.')]
    public int $default_vesting_hold_days = 30;


    // ── Fraud detection ──────────────────────────────────
    #[SettingGroup(label: 'Fraud detection', description: 'Global fraud thresholds. Programs can override per-program.', icon: 'shield-exclamation', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Number, label: 'Hard block threshold', group: 'Fraud detection', sortOrder: 1, min: 0, max: 100, helpText: 'Referrals with fraud_score above this are refused at claim time. 100 = disabled.')]
    public int $fraud_hard_block_threshold = 90;

    #[SettingField(controlType: ControlType::Number, label: 'Warn threshold', group: 'Fraud detection', sortOrder: 2, min: 0, max: 100, helpText: 'Referrals with fraud_score above this create a warn-severity flag but proceed.')]
    public int $fraud_warn_threshold = 40;

    #[SettingField(controlType: ControlType::Number, label: 'Max referrals per referrer per hour', group: 'Fraud detection', sortOrder: 3, min: 1)]
    public int $fraud_max_referrals_per_hour = 3;

    #[SettingField(controlType: ControlType::Toggle, label: 'Block on referrer-referred IP match', group: 'Fraud detection', sortOrder: 4)]
    public bool $fraud_self_ip_match_block = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Block disposable email addresses', group: 'Fraud detection', sortOrder: 5)]
    public bool $fraud_disposable_email_block = true;


    // ── Retention ──────────────────────────────────
    #[SettingGroup(label: 'Retention', icon: 'archive-box', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Select, label: 'Retention window', group: 'Retention', sortOrder: 1, options: ['7' => '7 years (default)', '10' => '10 years (Enterprise)'], helpText: '10 years requires the `referrals_extended_retention` entitlement.')]
    public int $retention_years = 7;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'referrals';
    }
}
