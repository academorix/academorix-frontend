<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * ReferralsUserSettings — My referrals.
 *
 * Per-user invitation + notification preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'referrals_user',
    label: 'My referrals',
    description: 'Per-user invitation + notification preferences.',
    icon: 'user-plus',
    scope: 'user',
    sortOrder: 42,
)]
class ReferralsUserSettings extends Settings
{

    // ── Invitation channels ──────────────────────────────────
    #[SettingGroup(label: 'Invitation channels', icon: 'envelope', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Preferred channel', group: 'Invitation channels', sortOrder: 1, options: ['email' => 'Email', 'sms' => 'SMS (usage charged)'])]
    public string $invitation_channel = 'email';

    #[SettingField(controlType: ControlType::Select, label: 'Invitation template', group: 'Invitation channels', sortOrder: 2, options: ['generic-friend-invite' => 'Generic', 'casual-friend-invite' => 'Casual', 'professional-invite' => 'Professional', 'parent-friend-invite' => 'Parent-to-parent'])]
    public string $invitation_template = 'generic-friend-invite';


    // ── Notification preferences ──────────────────────────────────
    #[SettingGroup(label: 'Notification preferences', icon: 'bell', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me when a friend clicks', group: 'Notification preferences', sortOrder: 1)]
    public bool $notify_on_claim = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me when a friend joins', group: 'Notification preferences', sortOrder: 2)]
    public bool $notify_on_signup = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me when a reward is on the way', group: 'Notification preferences', sortOrder: 3, helpText: 'Reward-received notifications (materialization → paid) are transactional and cannot be turned off.')]
    public bool $notify_on_vested = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'referrals_user';
    }
}
