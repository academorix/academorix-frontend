<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Actions\Tenant;

use Academorix\Referrals\Contracts\Repositories\ReferralCodeRepositoryInterface;
use Academorix\Referrals\Data\ReferralCodeData;
use Academorix\Referrals\Data\Requests\CreateReferralCodeRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/referral-codes` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Post(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsController]
#[Post('/api/v1/referral-codes')]
final class CreateReferralCodeAction
{
    public function __construct(
        private readonly ReferralCodeRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `referral-code` from the validated request payload.
     *
     * @param  CreateReferralCodeRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateReferralCodeRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(ReferralCodeData::from($model), JsonResponse::HTTP_CREATED);
    }
}
