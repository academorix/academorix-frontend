<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReferralReward.status transitioned to clawback. Reverses a materialized-or-paid reward due to refund / chargeback / fraud / program-archived. CRITICAL — every clawback is a financial reversal that Finance module (Wave 4) must also process.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - activity::WriteToActivityLog
 * - notifications::DispatchReferralClawbackNotification (to recipient — cannot-opt-out)
 * - finance::ReverseCreditForClawback (Wave 4 — reverses the Finance credit)
 * - marketing::ReverseReferralConversion (marketing rollups adjust)
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
final readonly class ReferralClawback implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $rewardId  ULID
     * @param  string  $referralId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $clawbackReason  'refund' | 'chargeback' | 'fraud' | 'program_archived'
     * @param  string  $clawbackAt  ISO 8601
     * @param  bool  $wasPaid  boolean
     * @param  bool  $wasMaterialized  boolean
     * @param  int  $reversedAmount  integer
     */
    public function __construct(
        public readonly string $rewardId,
        public readonly string $referralId,
        public readonly string $tenantId,
        public readonly string $clawbackReason,
        public readonly string $clawbackAt,
        public readonly bool $wasPaid,
        public readonly bool $wasMaterialized,
        public readonly int $reversedAmount,
    ) {
    }
}
