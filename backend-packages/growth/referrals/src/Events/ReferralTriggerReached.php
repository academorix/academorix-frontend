<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The program-configured trigger event fired for the referred user (signup / trial_started / first_payment / subscription_renewal_1 / subscription_renewal_3 / lifetime_value_threshold_hit / athlete_enrolled / event_registered). The listener captures it + transitions Referral -> trigger_pending.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - referrals::DetectReferralFraudAtTriggerTime (re-runs FraudDetector before vesting)
 * - observability::MetricsCollector (academorix.referrals.triggers_reached_total)
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
final readonly class ReferralTriggerReached implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $referralId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $programId  ULID
     * @param  string  $triggerEvent  string
     * @param  string  $triggerEventAt  ISO 8601
     */
    public function __construct(
        public readonly string $referralId,
        public readonly string $tenantId,
        public readonly string $programId,
        public readonly string $triggerEvent,
        public readonly string $triggerEventAt,
    ) {
    }
}
