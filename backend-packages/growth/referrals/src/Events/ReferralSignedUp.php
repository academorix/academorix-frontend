<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Referral.status transitioned claimed -> signed_up. The referred user completed the signup flow.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchReferralSignedUpNotification (to referrer — 'your friend joined')
 * - marketing::CaptureReferralSignupAsConversion (fires a marketing event on the ad-network fan-out)
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
final readonly class ReferralSignedUp implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $referralId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $programId  ULID
     * @param  string  $referredType  'user' | 'lead'
     * @param  string  $referredId  ULID
     * @param  string  $referredEmail  string
     * @param  string  $signedUpAt  ISO 8601
     */
    public function __construct(
        public readonly string $referralId,
        public readonly string $tenantId,
        public readonly string $programId,
        public readonly string $referredType,
        public readonly string $referredId,
        public readonly string $referredEmail,
        public readonly string $signedUpAt,
    ) {
    }
}
