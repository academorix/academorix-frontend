<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReferralCode.expires_at passed. Detected by claim-path (ReferralClaimer refuses expired code + fires this event) or by ExpireStaleReferralsJob scanning codes.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
final readonly class ReferralCodeExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $codeId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $code  string
     * @param  string  $expiredAt  ISO 8601
     */
    public function __construct(
        public readonly string $codeId,
        public readonly string $tenantId,
        public readonly string $code,
        public readonly string $expiredAt,
    ) {
    }
}
