<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The invitation URL was hit (redirect endpoint captures the click). Different from ReferralClicked (general referral link) — this is specifically from an emailed / SMS'd invitation.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - attribution::CaptureInvitationClickTouchpoint
 * - observability::MetricsCollector (academorix.referrals.invitations_clicked_total)
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
final readonly class ReferralInvitationClicked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $senderUserId  ULID
     * @param  string  $recipientEmailHash  SHA-256 hex string
     * @param  string  $codeId  ULID
     * @param  string  $clickedAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $senderUserId,
        public readonly string $recipientEmailHash,
        public readonly string $codeId,
        public readonly string $clickedAt,
    ) {
    }
}
