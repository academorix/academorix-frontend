<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * > 50% of remaining program budget consumed in a rolling 24h window. Fired by ReconcileProgramBudgetJob. Ops signal — could be viral traction OR fraud storm.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchProgramBudgetAlertNotification (to admin — includes recent-referral rollup)
 * - observability::MetricsCollector (academorix.referrals.budget_spikes_total)
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
final readonly class ReferralProgramBudgetSpike implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $programId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $consumedIn24hCents  integer
     * @param  int  $remainingBudgetCents  integer
     * @param  string  $spikeRatio  number (0..1)
     * @param  string  $detectedAt  ISO 8601
     */
    public function __construct(
        public readonly string $programId,
        public readonly string $tenantId,
        public readonly int $consumedIn24hCents,
        public readonly int $remainingBudgetCents,
        public readonly string $spikeRatio,
        public readonly string $detectedAt,
    ) {
    }
}
