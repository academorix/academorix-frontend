<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReferralProgram.status transitioned draft -> active OR paused -> active. Program becomes eligible for new referral claims.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchProgramActivatedInternalNotification (admin audit)
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ReferralProgramActivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $programId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $activatedAt  ISO 8601
     * @param  string  $activatedBy  ULID
     * @param  string  $wasStatus  'draft' | 'paused'
     */
    public function __construct(
        public readonly string $programId,
        public readonly string $tenantId,
        public readonly string $activatedAt,
        public readonly string $activatedBy,
        public readonly string $wasStatus,
    ) {
    }
}
