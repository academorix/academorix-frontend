<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A referrer's completed-referral count hit ReferralProgram.max_rewards_per_referrer or max_rewards_per_period. Further claims by this referrer refuse until the period rolls or the program allows more.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchMaxReferralsReachedNotification (to referrer — 'you've hit your cap')
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ReferralMaxReferralsPerReferrerReached implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $referrerId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $programId  ULID
     * @param  string  $capType  'per_referrer_lifetime' | 'per_referrer_period'
     * @param  int  $capValue  integer
     * @param  string  $reachedAt  ISO 8601
     */
    public function __construct(
        public readonly string $referrerId,
        public readonly string $tenantId,
        public readonly string $programId,
        public readonly string $capType,
        public readonly int $capValue,
        public readonly string $reachedAt,
    ) {
    }
}
