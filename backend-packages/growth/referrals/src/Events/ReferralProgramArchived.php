<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReferralProgram.status transitioned to archived. Terminal — cascades: pending referrals cancel + materialized-not-yet-paid rewards clawback.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - referrals::ArchiveReferralsOnProgramArchived
 * - notifications::DispatchProgramArchivedInternalNotification
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
final readonly class ReferralProgramArchived implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $programId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $archivedAt  ISO 8601
     * @param  string  $archivedBy  ULID
     * @param  string  $reason  string | null
     * @param  int  $cascadedReferralCount  integer
     * @param  int  $cascadedClawbackCount  integer
     */
    public function __construct(
        public readonly string $programId,
        public readonly string $tenantId,
        public readonly string $archivedAt,
        public readonly string $archivedBy,
        public readonly string $reason,
        public readonly int $cascadedReferralCount,
        public readonly int $cascadedClawbackCount,
    ) {
    }
}
