<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Referrals\Contracts\Data\ReferralCodeInterface;
use Academorix\Referrals\Database\Factories\ReferralCodeFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;

/**
 * Eloquent model for a ReferralCode.
 *
 * The distributable code that maps inbound clicks to a specific referral program.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[Table(name: ReferralCodeInterface::TABLE, keyType: ReferralCodeInterface::KEY_TYPE)]
#[Fillable([
    ReferralCodeInterface::ATTR_TENANT_ID,
        ReferralCodeInterface::ATTR_REFERRAL_PROGRAM_ID,
        ReferralCodeInterface::ATTR_CODE_TYPE,
        ReferralCodeInterface::ATTR_CODE,
        ReferralCodeInterface::ATTR_OWNER_TYPE,
        ReferralCodeInterface::ATTR_OWNER_USER_ID,
        ReferralCodeInterface::ATTR_CAMPAIGN_NAME,
        ReferralCodeInterface::ATTR_IS_ACTIVE,
        ReferralCodeInterface::ATTR_USAGE_CAP,
        ReferralCodeInterface::ATTR_USAGE_COUNT,
        ReferralCodeInterface::ATTR_EXPIRES_AT,
        ReferralCodeInterface::ATTR_METADATA,
])]
#[UseFactory(ReferralCodeFactory::class)]
final class ReferralCode extends Model implements ReferralCodeInterface
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    // TODO(gen): resolve unknown trait `BelongsToReferralProgram` — add its import + use line.
    use HasMetadata;
    use HasUserstamps;
    use Auditable;
    use Filterable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        ReferralCodeInterface::ATTR_CODE_TYPE => 'ReferralCodeType',
        ReferralCodeInterface::ATTR_OWNER_TYPE => 'ReferralCodeOwnerType',
        ReferralCodeInterface::ATTR_IS_ACTIVE => 'boolean',
        ReferralCodeInterface::ATTR_USAGE_CAP => 'integer',
        ReferralCodeInterface::ATTR_USAGE_COUNT => 'integer',
        ReferralCodeInterface::ATTR_EXPIRES_AT => 'datetime',
        ReferralCodeInterface::ATTR_METADATA => 'array',
    ];
}
