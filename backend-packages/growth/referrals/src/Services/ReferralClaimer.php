<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Services;

use Academorix\Referrals\Contracts\Services\ReferralClaimerInterface;
use Illuminate\Container\Attributes\Scoped;
use Academorix\Referrals\Contracts\Repositories\ReferralCodeRepositoryInterface;

/**
 * Concrete service — ReferralClaimer.
 *
 * Implements {@see ReferralClaimerInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[Scoped]
final class ReferralClaimer implements ReferralClaimerInterface
{
    /**
     * @param  ReferralCodeRepositoryInterface  $referralCodeRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly ReferralCodeRepositoryInterface $referralCodeRepository,
    ) {
    }
}
