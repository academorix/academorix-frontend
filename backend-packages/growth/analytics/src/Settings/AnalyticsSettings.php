<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Academorix\Analytics\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * AnalyticsSettings — Analytics.
 *
 * Server-side analytics capture + fan-out behavior.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'analytics',
    label: 'Analytics',
    description: 'Server-side analytics capture + fan-out behavior.',
    icon: 'chart-bar',
    scope: 'tenant',
    permission: 'analytics.providers.viewAny',
    sortOrder: 20,
)]
class AnalyticsSettings extends Settings
{

    // ── Consent enforcement ──────────────────────────────────
    #[SettingGroup(label: 'Consent enforcement', description: 'How strictly the consent gate treats missing / stale consent.', icon: 'shield-check', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Strict consent mode', validation: ['boolean'], group: 'Consent enforcement', sortOrder: 1, helpText: 'When on, subjects with no explicit consent decision are treated as suppressed (fail-closed). When off, no-decision subjects are captured while awaiting decision (fail-open). GDPR-strict tenants keep this on.')]
    public bool $consent_strict_mode = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Recheck consent at dispatch time', validation: ['boolean'], group: 'Consent enforcement', sortOrder: 2, helpText: 'Default on. Suppresses events whose subject withdrew consent between capture + dispatch. Off = honors the capture-time snapshot only.')]
    public bool $consent_recheck_on_dispatch = true;


    // ── Custom webhook ──────────────────────────────────
    #[SettingGroup(label: 'Custom webhook', description: 'Settings for the custom_webhook provider driver.', icon: 'code-bracket', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Text, label: 'Allowed egress domains', validation: ['nullable', 'array', 'each_url_https'], group: 'Custom webhook', sortOrder: 1, helpText: 'Comma-separated list of https:// domains the custom_webhook driver may POST to. Any provider config pointing outside this list is refused. Anti-exfiltration control.')]
    public ?array $custom_webhook_allowed_egress_domains = [];

    #[SettingField(controlType: ControlType::Select, label: 'HMAC signing algorithm', validation: ['required', 'string', 'in:sha256,sha512'], group: 'Custom webhook', sortOrder: 2, options: ['sha256' => 'SHA-256', 'sha512' => 'SHA-512'])]
    public string $custom_webhook_sign_algorithm = 'sha256';


    // ── Funnel + experiment defaults ──────────────────────────────────
    #[SettingGroup(label: 'Funnel + experiment defaults', description: 'Default window + confidence-level for funnel + experiment queries.', icon: 'funnel', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Number, label: 'Default funnel window (days)', validation: ['integer', 'min:1', 'max:365'], group: 'Funnel + experiment defaults', sortOrder: 1, min: 1, max: 365)]
    public int $funnel_default_window_days = 30;

    #[SettingField(controlType: ControlType::Select, label: 'Statistical confidence level', validation: ['required', 'string', 'in:0.90,0.95,0.99'], group: 'Funnel + experiment defaults', sortOrder: 2, options: ['0.90' => '90%', '0.95' => '95%', '0.99' => '99%'], helpText: 'Used in ExperimentAssignmentReportBuilder for p-value + confidence interval computation.')]
    public string $experiment_confidence_level = '0.95';


    // ── Session recording ──────────────────────────────────
    #[SettingGroup(label: 'Session recording', description: 'PostHog session recording opt-in. Independently requires analytics_session_recording entitlement + subject functional consent.', icon: 'video-camera', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enable session recording', validation: ['boolean'], group: 'Session recording', sortOrder: 1, helpText: 'Master toggle. Requires Enterprise plan + subject-level `functional` consent. Provider config also needs enable_session_recording=true.')]
    public bool $session_recording_enabled = false;

    #[SettingField(controlType: ControlType::Toggle, label: 'Mask all text', validation: ['boolean'], group: 'Session recording', sortOrder: 2, helpText: 'PostHog session-recording setting. On = all text redacted (default; safer). Off = free-form text captured. Never captures inputs with type=password regardless.')]
    public bool $session_recording_mask_all_text = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'analytics';
    }
}
