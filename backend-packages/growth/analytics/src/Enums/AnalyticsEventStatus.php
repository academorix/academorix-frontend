<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Academorix\Analytics\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `status` on `analytics-event`.
 *
 * Enum: pending / dispatching / delivered / partially_delivered / suppressed_by_consent / suppressed_by_sampling / suppressed_by_provider_off.
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
enum AnalyticsEventStatus: string
{
    use Enum;

    case Pending = 'pending';
    case Dispatching = 'dispatching';
    case Delivered = 'delivered';
    case PartiallyDelivered = 'partially_delivered';
    case SuppressedByConsent = 'suppressed_by_consent';
    case SuppressedBySampling = 'suppressed_by_sampling';
    case SuppressedByProviderOff = 'suppressed_by_provider_off';
}
