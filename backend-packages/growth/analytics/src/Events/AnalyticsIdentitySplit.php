<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Academorix\Analytics\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Deidentification — identified_user_id set back to null (typically GDPR erasure).
 *
 * ## Consumers
 *
 * - compliance::AuditDeidentificationListener (Wave 5)
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
final readonly class AnalyticsIdentitySplit implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $priorIdentifiedUserId  ULID
     * @param  string  $reason  'gdpr_erasure' | 'admin_deidentify' | 'account_deletion'
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $tenantId,
        public readonly string $priorIdentifiedUserId,
        public readonly string $reason,
    ) {
    }
}
