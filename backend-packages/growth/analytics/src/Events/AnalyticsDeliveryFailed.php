<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Academorix\Analytics\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * HTTP call returned a non-retryable error OR max attempts exhausted.
 *
 * ## Consumers
 *
 * - analytics::CircuitBreakerFailureListener — records failure into circuit_breaker_state, may transition to open
 * - monitoring::EmitFailureMetricListener — emits academorix.analytics.deliveries.by_state_total counter
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
final readonly class AnalyticsDeliveryFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $deliveryId  ULID
     * @param  string  $analyticsEventId  ULID
     * @param  string  $provider  string
     * @param  int  $attemptNumber  int
     * @param  string  $errorCode  string
     * @param  int  $responseHttpStatus  int | null
     */
    public function __construct(
        public readonly string $deliveryId,
        public readonly string $analyticsEventId,
        public readonly string $provider,
        public readonly int $attemptNumber,
        public readonly string $errorCode,
        public readonly int $responseHttpStatus,
    ) {
    }
}
