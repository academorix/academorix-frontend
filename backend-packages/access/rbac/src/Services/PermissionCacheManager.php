<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Academorix\Rbac\Services;

use Academorix\Rbac\Contracts\Services\PermissionCacheManagerInterface;
use Illuminate\Container\Attributes\Scoped;
use Academorix\Rbac\Contracts\Repositories\ModelHasPermissionsRepositoryInterface;

/**
 * Concrete service — PermissionCacheManager.
 *
 * Implements {@see PermissionCacheManagerInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
#[Scoped]
final class PermissionCacheManager implements PermissionCacheManagerInterface
{
    /**
     * @param  ModelHasPermissionsRepositoryInterface  $modelHasPermissionsRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly ModelHasPermissionsRepositoryInterface $modelHasPermissionsRepository,
    ) {
    }
}
