<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Academorix\Rbac\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * One or more permissions attached to a role via `role_has_permissions`. Emitted ONCE per attach transaction — batches many permissions into one event with a `permission_ids[]` array.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - rbac::PurgeAllPrincipalPermissionCachesForRole
 * - rbac::InvalidateTenantRoleCatalogueCache
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
final readonly class PermissionsAttachedToRole implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $roleId  ULID
     * @param  string  $roleName  string
     * @param  string  $permissionIds  string[]
     * @param  string  $permissionNames  string[]
     * @param  string  $actorId  ULID | null
     */
    public function __construct(
        public readonly string $roleId,
        public readonly string $roleName,
        public readonly string $permissionIds,
        public readonly string $permissionNames,
        public readonly string $actorId,
    ) {
    }
}
