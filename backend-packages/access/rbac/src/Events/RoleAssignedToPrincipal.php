<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Academorix\Rbac\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A `model_has_roles` row was inserted. Applies to User / PlatformUser / ServiceAccount.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteToAuditLog
 * - rbac::InvalidatePrincipalPermissionCache
 * - notifications::DispatchRoleAssignedNotification
 * - observability::EmitRoleAssignedMetric
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
final readonly class RoleAssignedToPrincipal implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $roleId  ULID
     * @param  string  $roleName  string
     * @param  string  $principalType  'User' | 'PlatformUser' | 'ServiceAccount'
     * @param  string  $principalId  ULID
     * @param  string  $tenantId  ULID | null
     * @param  string  $applicationId  ULID | null
     * @param  string  $assignedBy  ULID | null (null = system-seeded assignment on tenant provisioning)
     * @param  string  $expiresAt  ISO-8601 timestamp | null
     * @param  bool  $wasDelegated  boolean
     * @param  string  $assignedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $roleId,
        public readonly string $roleName,
        public readonly string $principalType,
        public readonly string $principalId,
        public readonly string $tenantId,
        public readonly string $applicationId,
        public readonly string $assignedBy,
        public readonly string $expiresAt,
        public readonly bool $wasDelegated,
        public readonly string $assignedAt,
    ) {
    }
}
