<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Academorix\Rbac\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PermissionCacheManager purged one or more cache keys. Fires ONCE per invalidation call (may batch many keys).
 *
 * ## Consumers
 *
 * - observability::EmitPermissionCacheInvalidatedMetric
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
final readonly class PermissionCacheInvalidated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID | null
     * @param  string  $principalType  string | null (null = tenant-wide)
     * @param  string  $principalId  ULID | null
     * @param  int  $cachedKeysPurged  integer
     * @param  string  $trigger  'role_change' | 'permission_change' | 'assignment_change' | 'bulk' | 'broadcast'
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $principalType,
        public readonly string $principalId,
        public readonly int $cachedKeysPurged,
        public readonly string $trigger,
    ) {
    }
}
