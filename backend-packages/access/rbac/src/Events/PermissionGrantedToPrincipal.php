<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Academorix\Rbac\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Direct permission attach via `model_has_permissions` (rare — usually roles carry perms).
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteToAuditLog
 * - rbac::InvalidatePrincipalPermissionCache
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
final readonly class PermissionGrantedToPrincipal implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $permissionId  ULID
     * @param  string  $permissionName  string
     * @param  string  $principalType  string
     * @param  string  $principalId  ULID
     * @param  string  $actorId  ULID
     */
    public function __construct(
        public readonly string $permissionId,
        public readonly string $permissionName,
        public readonly string $principalType,
        public readonly string $principalId,
        public readonly string $actorId,
    ) {
    }
}
