<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Academorix\Rbac\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New row inserted into `permissions`. Fires for both catalogue-loader inserts (created_by null) and tenant-created custom permissions.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitPermissionCreatedMetric
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PermissionCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $permissionId  ULID (`per_...`)
     * @param  string  $tenantId  ULID | null
     * @param  string  $applicationId  ULID | null
     * @param  string  $name  string
     * @param  string  $guardName  'sanctum' | 'platform_admin'
     * @param  string  $moduleSlug  string | null
     * @param  bool  $isSystem  boolean
     * @param  string  $actorId  ULID | null
     */
    public function __construct(
        public readonly string $permissionId,
        public readonly string $tenantId,
        public readonly string $applicationId,
        public readonly string $name,
        public readonly string $guardName,
        public readonly string $moduleSlug,
        public readonly bool $isSystem,
        public readonly string $actorId,
    ) {
    }
}
