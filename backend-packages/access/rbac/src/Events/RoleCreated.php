<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Academorix\Rbac\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New row inserted into `roles`. Fires for both system-seeded rows (created_by null) and admin-created custom rows.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteToAuditLog
 * - observability::EmitRoleCreatedMetric
 * - rbac::InvalidateTenantRoleCatalogueCache
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RoleCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $roleId  ULID (`rol_...`)
     * @param  string  $tenantId  ULID | null
     * @param  string  $applicationId  ULID | null
     * @param  string  $name  string
     * @param  string  $guardName  'sanctum' | 'platform_admin'
     * @param  bool  $isSystem  boolean
     * @param  string  $actorId  ULID | null
     * @param  string  $createdAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $roleId,
        public readonly string $tenantId,
        public readonly string $applicationId,
        public readonly string $name,
        public readonly string $guardName,
        public readonly bool $isSystem,
        public readonly string $actorId,
        public readonly string $createdAt,
    ) {
    }
}
