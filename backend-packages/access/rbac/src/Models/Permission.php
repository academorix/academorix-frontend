<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Academorix\Rbac\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Rbac\Contracts\Data\PermissionInterface;
use Academorix\Rbac\Database\Factories\PermissionFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Laravel\Scout\Searchable;
use Mattiverse\Userstamps\Traits\Userstamps;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a Permission.
 *
 * Spatie's `permissions` table augmented with Academorix scope columns (application_id, tenant_id, is_system, description, module_slug, sort_order).
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
#[Table(name: PermissionInterface::TABLE, keyType: PermissionInterface::KEY_TYPE)]
#[Fillable([
    PermissionInterface::ATTR_APPLICATION_ID,
        PermissionInterface::ATTR_TENANT_ID,
        PermissionInterface::ATTR_NAME,
        PermissionInterface::ATTR_GUARD_NAME,
        PermissionInterface::ATTR_IS_SYSTEM,
        PermissionInterface::ATTR_DESCRIPTION,
        PermissionInterface::ATTR_MODULE_SLUG,
        PermissionInterface::ATTR_SORT_ORDER,
        PermissionInterface::ATTR_METADATA,
])]
#[UseFactory(PermissionFactory::class)]
final class Permission extends Model implements PermissionInterface
{
    use HasFactory;
    use HasUlids;
    // TODO(gen): resolve unknown trait `HasSystemFlag` — add its import + use line.
    use HasMetadata;
    use HasUserstamps;
    use HasActivityLog;
    use Filterable;
    use Searchable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        PermissionInterface::ATTR_IS_SYSTEM => 'boolean',
        PermissionInterface::ATTR_SORT_ORDER => 'integer',
        PermissionInterface::ATTR_METADATA => 'array',
    ];
}
