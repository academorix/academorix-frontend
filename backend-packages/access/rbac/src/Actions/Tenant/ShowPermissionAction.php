<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access rbac --force`.

declare(strict_types=1);

namespace Academorix\Rbac\Actions\Tenant;

use Academorix\Rbac\Contracts\Repositories\PermissionRepositoryInterface;
use Academorix\Rbac\Data\PermissionData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Get;

/**
 * `GET /api/v1/rbac/permissions/{permission}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Rbac
 *
 * @since    0.1.0
 */
#[AsAction(name: 'rbac.permissions.show')]
#[Get('/api/v1/rbac/permissions/{permission}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowPermissionAction
{
    use AsController;

    public function __construct(
        private readonly PermissionRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `permission` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): PermissionData
    {
        return PermissionData::from($this->repository->findOrFail($id));
    }
}
