<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access invitations --force`.

declare(strict_types=1);

namespace Academorix\Invitations\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `event` on `invitation-event`.
 *
 * Enum: `sent`, `delivered`, `opened`, `clicked`, `accepted`, `declined`, `expired`, `revoked`, `bounced`, `resent`, `preflight_failed`, `send_failed`.
 *
 * @category Invitations
 *
 * @since    0.1.0
 */
enum InvitationEventEvent: string
{
    use Enum;

    case Sent = 'sent';
    case Delivered = 'delivered';
    case Opened = 'opened';
    case Clicked = 'clicked';
    case Accepted = 'accepted';
    case Declined = 'declined';
    case Expired = 'expired';
    case Revoked = 'revoked';
    case Bounced = 'bounced';
    case Resent = 'resent';
    case PreflightFailed = 'preflight_failed';
    case SendFailed = 'send_failed';
}
