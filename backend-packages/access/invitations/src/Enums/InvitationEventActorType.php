<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access invitations --force`.

declare(strict_types=1);

namespace Academorix\Invitations\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `actor_type` on `invitation-event`.
 *
 * Polymorphic actor key when the event was user-initiated (`user`, `service_account`, `system`, `invitee`, `mail_transport`, `webhook`).
 *
 * @category Invitations
 *
 * @since    0.1.0
 */
enum InvitationEventActorType: string
{
    use Enum;

    case User = 'user';
    case ServiceAccount = 'service_account';
    case System = 'system';
    case Invitee = 'invitee';
    case MailTransport = 'mail_transport';
    case Webhook = 'webhook';
}
