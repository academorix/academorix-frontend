<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Academorix\Delegation\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * DelegationUserSettings — My delegations.
 *
 * Per-user delegation notification preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'delegation_user',
    label: 'My delegations',
    description: 'Per-user delegation notification preferences.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 40,
)]
class DelegationUserSettings extends Settings
{

    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', icon: 'bell', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me when I receive a delegation', group: 'Notifications', sortOrder: 1, helpText: 'You are always notified for delegation.activated (regulatory — cannot be disabled) but you can turn off the informational delegation.created notice here.')]
    public bool $notify_on_inbound_delegation = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me 24h before my outbound delegation expires', group: 'Notifications', sortOrder: 2)]
    public bool $notify_on_outbound_expiring = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'delegation_user';
    }
}
