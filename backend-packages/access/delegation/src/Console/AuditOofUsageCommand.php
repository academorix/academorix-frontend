<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Academorix\Delegation\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan delegations:audit-oof-usage` — Audit oof usage — delegations command.
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'delegations:audit-oof-usage',
    description: 'Audit oof usage — delegations command.',
)]
final class AuditOofUsageCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Audit oof usage — delegations command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Audit oof usage — delegations command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
