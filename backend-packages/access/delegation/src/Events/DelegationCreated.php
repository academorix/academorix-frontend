<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Academorix\Delegation\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * RoleDelegationObserver::created hook after the row is inserted + all validators passed. Fires exactly once per delegation, ever.
 *
 * ## Consumers
 *
 * - notifications::DispatchDelegationCreatedNotification
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRow
 * - observability::EmitDelegationCreatedMetric
 * - analytics::TrackDelegationCreated
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
final readonly class DelegationCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $delegationId  ULID (dlg_)
     * @param  string  $tenantId  ULID (ten_)
     * @param  string  $applicationId  ULID (app_)
     * @param  string  $delegatorId  ULID (usr_)
     * @param  string  $delegateId  ULID (usr_)
     * @param  string  $roleId  ULID (rol_) | null (null = all-roles OOF)
     * @param  string  $delegationType  'oof' | 'handover' | 'emergency'
     * @param  string  $startsAt  ISO-8601 timestamp
     * @param  string  $endsAt  ISO-8601 timestamp
     * @param  string  $reason  string (min 20)
     * @param  string  $createdAt  ISO-8601 timestamp
     * @param  string  $actorId  ULID (the caller — always the delegator on POST /me/delegations)
     */
    public function __construct(
        public readonly string $delegationId,
        public readonly string $tenantId,
        public readonly string $applicationId,
        public readonly string $delegatorId,
        public readonly string $delegateId,
        public readonly string $roleId,
        public readonly string $delegationType,
        public readonly string $startsAt,
        public readonly string $endsAt,
        public readonly string $reason,
        public readonly string $createdAt,
        public readonly string $actorId,
    ) {
    }
}
