<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Academorix\Delegation\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ImpersonationSessionObserver::updated on ended_at transitioning from null to non-null. Fires for end_trigger IN (self_service, ttl_expired). NOT for admin_revocation / security_lockdown — those fire the dedicated ForcedTerminate event instead.
 *
 * ## Consumers
 *
 * - notifications::DispatchImpersonationSessionEndedNotification
 * - audit::WriteHeavyAuditRow
 * - activity::WriteToActivityLog
 * - observability::EmitImpersonationEndedMetric
 * - analytics::TrackImpersonationEnded
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ImpersonationSessionEnded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID (imp_)
     * @param  string  $impersonatorPlatformUserId  ULID (plu_) | null (if hard-deleted post-90d)
     * @param  string  $impersonatedUserId  ULID (usr_)
     * @param  string  $targetTenantId  ULID (ten_)
     * @param  string  $startedAt  ISO-8601 timestamp
     * @param  string  $endedAt  ISO-8601 timestamp
     * @param  int  $durationSeconds  integer
     * @param  string  $endTrigger  'self_service' | 'ttl_expired'
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $impersonatorPlatformUserId,
        public readonly string $impersonatedUserId,
        public readonly string $targetTenantId,
        public readonly string $startedAt,
        public readonly string $endedAt,
        public readonly int $durationSeconds,
        public readonly string $endTrigger,
    ) {
    }
}
