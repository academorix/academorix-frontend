<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Academorix\Delegation\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A super_admin extended the session's ends_at (rare — the tenant must hold `impersonation_extended_ttl` entitlement + only super_admin can invoke). ImpersonationSessionObserver::updated on ends_at increase.
 *
 * ## Consumers
 *
 * - notifications::DispatchImpersonationExtendedNotification
 * - audit::WriteHeavyAuditRow
 * - activity::WriteToActivityLog
 * - observability::EmitImpersonationExtendedMetric
 * - monitoring::AlertOnImpersonationExtended
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ImpersonationSessionExtended implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID (imp_)
     * @param  string  $previousEndsAt  ISO-8601 timestamp
     * @param  string  $newEndsAt  ISO-8601 timestamp
     * @param  string  $extendedBy  ULID (plu_) — the super_admin approving
     * @param  string  $extensionReason  string (min 40)
     * @param  string  $extendedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $previousEndsAt,
        public readonly string $newEndsAt,
        public readonly string $extendedBy,
        public readonly string $extensionReason,
        public readonly string $extendedAt,
    ) {
    }
}
