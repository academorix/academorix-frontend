<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access delegation --force`.

declare(strict_types=1);

namespace Academorix\Delegation\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * RoleDelegationObserver::creating rejected a POST because the delegator already has an ACTIVE delegation of the same role to the same delegate with a window that overlaps the requested one. Critical severity — signals a UX bug in the tenant admin surface OR an abuse attempt. No row is created; the event fires so security + product can investigate.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRow
 * - observability::EmitDelegationSelfOverlapMetric
 * - monitoring::AlertSecurityOnOverlap
 *
 * @category Delegation
 *
 * @since    0.1.0
 */
final readonly class DelegationSelfOverlapDetected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID (ten_)
     * @param  string  $attemptedDelegatorId  ULID (usr_)
     * @param  string  $attemptedDelegateId  ULID (usr_)
     * @param  string  $attemptedRoleId  ULID (rol_) | null
     * @param  string  $attemptedStartsAt  ISO-8601 timestamp
     * @param  string  $attemptedEndsAt  ISO-8601 timestamp
     * @param  string  $existingDelegationId  ULID (dlg_) — the overlapping row that caused refusal
     * @param  string  $detectedAt  ISO-8601 timestamp
     * @param  string  $attemptedActorId  ULID (usr_)
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $attemptedDelegatorId,
        public readonly string $attemptedDelegateId,
        public readonly string $attemptedRoleId,
        public readonly string $attemptedStartsAt,
        public readonly string $attemptedEndsAt,
        public readonly string $existingDelegationId,
        public readonly string $detectedAt,
        public readonly string $attemptedActorId,
    ) {
    }
}
