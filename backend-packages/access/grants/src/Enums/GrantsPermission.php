<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Academorix\Grants\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Grants module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Grants
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum GrantsPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `grants.viewAny` — List + show grants in the caller's tenant.
     */
    #[Label('List + show grants in the caller\'s tenant')]
    #[Description('List + show grants in the caller\'s tenant. Scoped by default to grants where subject_id = auth_user (users see their own grants); widened for admins with `grants.manage`.')]
    case GrantsViewAny = 'grants.viewAny';

    /**
     * `grants.manage` — Full grant CRUD scope.
     */
    #[Label('Full grant CRUD scope')]
    #[Description('Full grant CRUD scope. Includes issue, patch, revoke, view any grant in the tenant (not just own).')]
    case GrantsManage = 'grants.manage';

    /**
     * `grants.issue` — Issue a grant.
     */
    #[Label('Issue a grant')]
    #[Description('Issue a grant. The grantor must ALSO hold every permission being granted (privilege-escalation guard — enforced by observer + issuer regardless of this permission). Members with grants.issue can share access on resources they own (e.g. a coach sharing view on athletes they manage).')]
    case GrantsIssue = 'grants.issue';

    /**
     * `grants.revoke` — Revoke a grant the caller issued (granted_by = auth_user).
     */
    #[Label('Revoke a grant the caller issued (granted_by = auth_user)')]
    #[Description('Revoke a grant the caller issued (granted_by = auth_user). Broader revocation (any grant in the tenant) requires `grants.manage`.')]
    case GrantsRevoke = 'grants.revoke';

    /**
     * `grants.audit_any` — Enumerate grants across the tenant — call `/grants/subject/{id}` for any principal or `/grants/resource/{type}/{id}` for any resource.
     */
    #[Label('Enumerate grants across the tenant — call `/grants/subject/{id}` for any princip')]
    #[Description('Enumerate grants across the tenant — call `/grants/subject/{id}` for any principal or `/grants/resource/{type}/{id}` for any resource. Admin-level compliance surface.')]
    case GrantsAuditAny = 'grants.audit_any';

    /**
     * `grants.bulk_provision` — Upload a CSV of grants for bulk issue.
     */
    #[Label('Upload a CSV of grants for bulk issue')]
    #[Description('Upload a CSV of grants for bulk issue. Gated by the `bulk_grant_provision` entitlement (medium plan+) AND this permission.')]
    case GrantsBulkProvision = 'grants.bulk_provision';

    /**
     * `grants.deny` — Issue a grant with decision='deny' (an inviolable block).
     */
    #[Label('Issue a grant with decision=\'deny\' (an inviolable block)')]
    #[Description('Issue a grant with decision=\'deny\' (an inviolable block). Rarer than allow — split into its own permission because denies short-circuit everything and are audit-critical. Gated by the `deny_grants` entitlement (enterprise) AND this permission.')]
    case GrantsDeny = 'grants.deny';

    /**
     * `grants.grant_across_role` — Rare enterprise workflow: grant a permission the grantor DOES NOT hold in their own role, via an approval flow.
     */
    #[Label('Rare enterprise workflow: grant a permission the grantor DOES NOT hold in their')]
    #[Description('Rare enterprise workflow: grant a permission the grantor DOES NOT hold in their own role, via an approval flow. Requires this permission AND the `cross_role_grants` entitlement AND a matching workflow/approvals template. Without it, the privilege-escalation guard refuses.')]
    case GrantsGrantAcrossRole = 'grants.grant_across_role';

    /**
     * `platform.grants.viewAny` — Cross-tenant list.
     */
    #[Label('Cross-tenant list')]
    #[Description('Cross-tenant list. Every read captured in audit context (SOC 2 CC6.1).')]
    case PlatformGrantsViewAny = 'platform.grants.viewAny';

    /**
     * `platform.grants.view` — Cross-tenant detail read.
     */
    #[Label('Cross-tenant detail read')]
    #[Description('Cross-tenant detail read.')]
    case PlatformGrantsView = 'platform.grants.view';

    /**
     * `platform.grants.audit_report` — Generate cross-tenant compliance audit reports.
     */
    #[Label('Generate cross-tenant compliance audit reports')]
    #[Description('Generate cross-tenant compliance audit reports. Enforced by the `grant_audit_report` entitlement AND this permission.')]
    case PlatformGrantsAuditReport = 'platform.grants.audit_report';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::GrantsViewAny => Guard::Sanctum,
            self::GrantsManage => Guard::Sanctum,
            self::GrantsIssue => Guard::Sanctum,
            self::GrantsRevoke => Guard::Sanctum,
            self::GrantsAuditAny => Guard::Sanctum,
            self::GrantsBulkProvision => Guard::Sanctum,
            self::GrantsDeny => Guard::Sanctum,
            self::GrantsGrantAcrossRole => Guard::Sanctum,
            self::PlatformGrantsViewAny => Guard::PlatformAdmin,
            self::PlatformGrantsView => Guard::PlatformAdmin,
            self::PlatformGrantsAuditReport => Guard::PlatformAdmin,
        };
    }
}
