<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Academorix\Grants\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * GrantSettings — Access grants.
 *
 * Tenant-wide preferences for the share-access feature.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Grants
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'grants',
    label: 'Access grants',
    description: 'Tenant-wide preferences for the share-access feature. Owner + admin only.',
    icon: 'shield-check',
    scope: 'tenant',
    permission: 'grants.manage',
    sortOrder: 30,
)]
class GrantSettings extends Settings
{

    // ── Expiration defaults ──────────────────────────────────
    #[SettingGroup(label: 'Expiration defaults', description: 'How long grants stay valid by default. Callers may override per-grant within these limits.', icon: 'clock', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Number, label: 'Default expiry (days)', validation: ['required', 'integer', 'min:1', 'max:365'], group: 'Expiration defaults', sortOrder: 1, min: 1, max: 365, helpText: 'Default `expires_at` on new grants. Users can override per-grant within the max_ttl_days ceiling.')]
    public int $default_ttl_days = 30;

    #[SettingField(controlType: ControlType::Number, label: 'Maximum expiry (days)', validation: ['required', 'integer', 'min:1', 'max:365'], group: 'Expiration defaults', sortOrder: 2, min: 1, max: 365, helpText: 'Hard ceiling. Enterprise tenants with the permanent-grants entitlement can bypass by leaving expiry empty.')]
    public int $max_ttl_days = 365;

    #[SettingField(controlType: ControlType::Number, label: 'Warn subject before expiry (hours)', validation: ['required', 'integer', 'min:1', 'max:168'], group: 'Expiration defaults', sortOrder: 3, min: 1, max: 168, helpText: 'How many hours before a grant expires the subject + grantor receive a warning notification. Zero disables the warning.')]
    public int $warn_before_hours = 24;


    // ── Reason requirements ──────────────────────────────────
    #[SettingGroup(label: 'Reason requirements', description: 'The audit trail\'s promise: every grant carries a meaningful reason.', icon: 'document-text', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Number, label: 'Minimum reason length (characters)', validation: ['required', 'integer', 'min:0', 'max:200'], group: 'Reason requirements', sortOrder: 1, min: 0, max: 200, helpText: 'Reason field must be at least this many characters (after trim). 20 is the compromise between meaningful and non-intrusive.')]
    public int $min_reason_length = 20;

    #[SettingField(controlType: ControlType::Toggle, label: 'Block placeholder reasons', validation: ['boolean'], group: 'Reason requirements', sortOrder: 2, helpText: 'Reject reasons matching common placeholders like \'test\', \'temp\', \'because\'.')]
    public bool $block_placeholder_reasons = true;


    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', icon: 'bell-alert', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify subject when they receive a grant', validation: ['boolean'], group: 'Notifications', sortOrder: 1)]
    public bool $notify_subject_on_issue = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify subject when a grant is revoked', validation: ['boolean'], group: 'Notifications', sortOrder: 2)]
    public bool $notify_subject_on_revoke = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify grantor when a grant they issued is expiring', validation: ['boolean'], group: 'Notifications', sortOrder: 3, helpText: 'By default only the subject sees the expiry warning. Enable this for grantors who want to renew proactively.')]
    public bool $notify_grantor_on_expiring = false;

    #[SettingField(controlType: ControlType::Toggle, label: 'Alert admins when a deny grant blocks an access attempt', validation: ['boolean'], group: 'Notifications', sortOrder: 4, helpText: 'Fires DeniedAccessAttemptNotification when a subject with a deny grant tries the denied action. Noisy on high-traffic tenants — off by default.')]
    public bool $notify_admins_on_deny_attempt = false;


    // ── Bulk provisioning ──────────────────────────────────
    #[SettingGroup(label: 'Bulk provisioning', description: 'Preferences for the CSV bulk-issue feature.', icon: 'queue-list', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Text, label: 'Default source tag', validation: ['nullable', 'string', 'max:64'], group: 'Bulk provisioning', sortOrder: 1, helpText: 'Grants uploaded via CSV get this string as `source_reference` prefix, useful when running multiple batches in a day.')]
    public ?string $bulk_default_source = 'bulk_provision';

    #[SettingField(controlType: ControlType::Toggle, label: 'Require a reason on every CSV row', validation: ['boolean'], group: 'Bulk provisioning', sortOrder: 2, helpText: 'When off, missing per-row reasons fall back to the batch-level reason.')]
    public bool $bulk_require_reason_per_row = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'grants';
    }
}
