<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Academorix\Grants\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * GrantsReviewSettings — Access reviews.
 *
 * Preferences for the periodic access-review workflow (ISO 27001 A.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Grants
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'grants_review',
    label: 'Access reviews',
    description: 'Preferences for the periodic access-review workflow (ISO 27001 A.9.2.5). Compliance officer role.',
    icon: 'clipboard-document-check',
    scope: 'tenant',
    permission: 'grants.audit_any',
    sortOrder: 32,
)]
class GrantsReviewSettings extends Settings
{

    // ── Review cadence ──────────────────────────────────
    #[SettingGroup(label: 'Review cadence', icon: 'calendar', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'How often should reviews run?', group: 'Review cadence', sortOrder: 1, options: ['monthly' => 'Monthly', 'quarterly' => 'Quarterly (ISO 27001 recommendation)', 'semi_annually' => 'Every 6 months', 'annually' => 'Annually'])]
    public string $review_frequency = 'quarterly';

    #[SettingField(controlType: ControlType::Select, label: 'Which grants require review?', group: 'Review cadence', sortOrder: 2, options: ['all_active' => 'Every active grant', 'long_lived' => 'Grants older than 90 days', 'permanent_only' => 'Permanent grants (no expiry)', 'deny_only' => 'Deny grants only'], helpText: 'Long-lived + permanent grants are the highest-risk category — reviewing everything is time-consuming.')]
    public string $review_scope = 'long_lived';


    // ── Reviewer routing ──────────────────────────────────
    #[SettingGroup(label: 'Reviewer routing', icon: 'users', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Select, label: 'Primary reviewer role', group: 'Reviewer routing', sortOrder: 1, helpText: 'Every user with this role receives a review-batch notification at the start of each review cycle.')]
    public string $primary_reviewer_role = 'compliance_officer';

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'grants_review';
    }
}
