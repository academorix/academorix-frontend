<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Academorix\Grants\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Someone tried to issue a grant where subject.tenant_id !== resource.tenant_id !== grantor.tenant_id — a tenant-isolation breach attempt. Refused by observer with GRANT_CROSS_TENANT (422). FIRES IMMEDIATELY (pre-refusal).
 *
 * ## Consumers
 *
 * - audit::WriteHighSeverityAuditRow (severity=critical)
 * - compliance::EmitSecurityIncidentSignal
 * - notifications::DispatchTenantIsolationBreachNotification (to platform ops — this is a platform-level security signal)
 *
 * @category Grants
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class GrantCrossTenantAttempt implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attemptedGrantor  User
     * @param  string  $grantorTenantId  string
     * @param  string  $attemptedSubjectId  string
     * @param  string  $attemptedSubjectTenantId  string|null
     * @param  string  $attemptedResourceType  string
     * @param  string  $attemptedResourceId  string
     * @param  string  $attemptedResourceTenantId  string|null
     */
    public function __construct(
        public readonly string $attemptedGrantor,
        public readonly string $grantorTenantId,
        public readonly string $attemptedSubjectId,
        public readonly string $attemptedSubjectTenantId,
        public readonly string $attemptedResourceType,
        public readonly string $attemptedResourceId,
        public readonly string $attemptedResourceTenantId,
    ) {
    }
}
