<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Academorix\Grants\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A deny grant was revoked (lifting an inviolable block). Also audit-critical — someone re-enabled access that was explicitly denied.
 *
 * ## Consumers
 *
 * - audit::WriteHighSeverityAuditRow (severity=critical)
 * - compliance::EmitAccessReviewSignal
 *
 * @category Grants
 *
 * @since    0.1.0
 */
final readonly class GrantDenialRevoked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $grant  AccessGrant
     * @param  string  $revoker  User|null
     * @param  string  $reason  string
     */
    public function __construct(
        public readonly string $grant,
        public readonly string $revoker,
        public readonly string $reason,
    ) {
    }
}
