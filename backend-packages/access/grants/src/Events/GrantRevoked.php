<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Academorix\Grants\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Explicit revocation via DELETE /grants/{id} or grantor action. Terminal.
 *
 * ## Consumers
 *
 * - grants::InvalidateGrantEvaluationCache
 * - notifications::DispatchGrantRevokedNotification (to subject)
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRow
 *
 * @category Grants
 *
 * @since    0.1.0
 */
final readonly class GrantRevoked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $grant  AccessGrant
     * @param  string  $revoker  User|null (null for system revokes — reconcile orphaned)
     * @param  string  $reason  string (required — the revoked_reason column)
     * @param  bool  $isSystemRevoke  boolean (true when triggered by ReconcileOrphanedGrantsJob)
     */
    public function __construct(
        public readonly string $grant,
        public readonly string $revoker,
        public readonly string $reason,
        public readonly bool $isSystemRevoke,
    ) {
    }
}
