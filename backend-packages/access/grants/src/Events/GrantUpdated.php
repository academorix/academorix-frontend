<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Academorix\Grants\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PATCH /grants/{id} completed — only `expires_at` + `reason` are mutable (never subject/resource/permissions/decision after issue). Fires only when a real change happened.
 *
 * @category Grants
 *
 * @since    0.1.0
 */
final readonly class GrantUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $grant  AccessGrant
     * @param  string  $actor  User
     * @param  array  $dirtyFields  array<string>
     * @param  array  $priorValues  array<string, mixed>
     */
    public function __construct(
        public readonly string $grant,
        public readonly string $actor,
        public readonly array $dirtyFields,
        public readonly array $priorValues,
    ) {
    }
}
