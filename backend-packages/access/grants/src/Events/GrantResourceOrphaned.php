<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Academorix\Grants\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReconcileOrphanedGrantsJob discovered a grant whose resource_id no longer resolves (resource hard-deleted out-of-band). The reconciler auto-revokes the grant with revoked_reason='orphaned_resource' before firing this event.
 *
 * ## Consumers
 *
 * - notifications::DispatchGrantRevokedNotification (with orphan-specific copy)
 * - compliance::EmitDataIntegrityAlert
 *
 * @category Grants
 *
 * @since    0.1.0
 */
final readonly class GrantResourceOrphaned implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $grant  AccessGrant (already in revoked state)
     * @param  string  $orphanedResourceType  string
     * @param  string  $orphanedResourceId  string
     */
    public function __construct(
        public readonly string $grant,
        public readonly string $orphanedResourceType,
        public readonly string $orphanedResourceId,
    ) {
    }
}
