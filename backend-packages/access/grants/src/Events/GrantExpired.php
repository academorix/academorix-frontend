<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Academorix\Grants\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PurgeExpiredGrantsJob observed `expires_at <= now()` on a non-terminal grant. Transitions the row to revoked with revoked_reason='expired'. Terminal — no listener can reverse.
 *
 * ## Consumers
 *
 * - grants::InvalidateGrantEvaluationCache
 * - notifications::DispatchGrantExpiredNotification (to subject; to grantor if opt-in setting enabled)
 *
 * @category Grants
 *
 * @since    0.1.0
 */
final readonly class GrantExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $grant  AccessGrant
     * @param  string  $expiredAt  datetime (the actual expires_at wall-clock)
     */
    public function __construct(
        public readonly string $grant,
        public readonly string $expiredAt,
    ) {
    }
}
