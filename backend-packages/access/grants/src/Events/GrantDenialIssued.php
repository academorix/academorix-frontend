<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py access grants --force`.

declare(strict_types=1);

namespace Academorix\Grants\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A grant with decision='deny' was created. FIRES IMMEDIATELY (not afterCommit) so audit captures every attempt even if the outer transaction rolls back.
 *
 * ## Consumers
 *
 * - audit::WriteHighSeverityAuditRow (severity=critical — denies are inviolable, every one is notable)
 * - compliance::EmitAccessReviewSignal (SOC 2 CC6.3 evidence)
 * - notifications::MaybeDispatchDeniedAccessAdminNotification (opt-in per settings)
 *
 * @category Grants
 *
 * @since    0.1.0
 */
final readonly class GrantDenialIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $grant  AccessGrant
     * @param  string  $grantor  User|null
     * @param  string  $subject  Model
     * @param  string  $resource  Model
     * @param  array  $permissionsDenied  array<string>
     * @param  string  $reason  string
     */
    public function __construct(
        public readonly string $grant,
        public readonly string $grantor,
        public readonly string $subject,
        public readonly string $resource,
        public readonly array $permissionsDenied,
        public readonly string $reason,
    ) {
    }
}
