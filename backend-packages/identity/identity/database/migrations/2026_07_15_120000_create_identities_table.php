<?php

/**
 * @file database/migrations/2026_07_15_120000_create_identities_table.php
 *
 * @description
 * Create the `identities` table for the `Identity` entity.
 * Owned by the identity module. Every column + index reference
 * goes through {@see Academorix\Identity\Contracts\Data\IdentityInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity identity --force`.

declare(strict_types=1);

use Academorix\Identity\Contracts\Data\IdentityInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(IdentityInterface::TABLE, function (Blueprint $table): void {
            $table->string(IdentityInterface::ATTR_ID, 64)->primary();
            $table->string(IdentityInterface::ATTR_EMAIL);
            $table->text(IdentityInterface::ATTR_PASSWORD_HASH);
            $table->text(IdentityInterface::ATTR_MFA_SECRET_ENCRYPTED)->nullable();
            $table->jsonb(IdentityInterface::ATTR_MFA_RECOVERY_CODES_HASHED)->nullable();
            $table->timestampTz(IdentityInterface::ATTR_EMAIL_VERIFIED_AT)->nullable();
            $table->text(IdentityInterface::ATTR_EMAIL_VERIFICATION_TOKEN_HASH)->nullable();
            $table->timestampTz(IdentityInterface::ATTR_EMAIL_VERIFICATION_EXPIRES_AT)->nullable();
            $table->jsonb(IdentityInterface::ATTR_PASSWORD_HISTORY_HASHED)->default('\'[]\'::jsonb');
            $table->timestampTz(IdentityInterface::ATTR_PASSWORD_CHANGED_AT)->default('now()');
            $table->timestampTz(IdentityInterface::ATTR_LAST_LOGIN_AT)->nullable();
            $table->timestampTz(IdentityInterface::ATTR_LAST_FAILED_AT)->nullable();
            $table->timestampTz(IdentityInterface::ATTR_LOCKED_UNTIL)->nullable();
            $table->integer(IdentityInterface::ATTR_FAILED_ATTEMPTS_COUNT)->default(0);
            $table->date(IdentityInterface::ATTR_DOB)->nullable();
            $table->jsonb(IdentityInterface::ATTR_METADATA)->nullable();
            $table->uuid(IdentityInterface::ATTR_CREATED_BY)->nullable();
            $table->uuid(IdentityInterface::ATTR_UPDATED_BY)->nullable();
            $table->uuid(IdentityInterface::ATTR_DELETED_BY)->nullable();
            $table->timestampTz(IdentityInterface::ATTR_DELETED_AT)->nullable();
            $table->timestampTz(IdentityInterface::ATTR_CREATED_AT);
            $table->timestampTz(IdentityInterface::ATTR_UPDATED_AT);

        });

        DB::statement('CREATE UNIQUE INDEX identities_email_unique ON ' . IdentityInterface::TABLE . ' (' . IdentityInterface::ATTR_EMAIL . ') WHERE deleted_at IS NULL');
        DB::statement('CREATE INDEX identities_locked_idx ON ' . IdentityInterface::TABLE . ' (' . IdentityInterface::ATTR_LOCKED_UNTIL . ') WHERE locked_until IS NOT NULL');
        DB::statement('CREATE INDEX identities_unverified_idx ON ' . IdentityInterface::TABLE . ' (' . IdentityInterface::ATTR_EMAIL_VERIFIED_AT . ') WHERE email_verified_at IS NULL AND deleted_at IS NULL');
        DB::statement('CREATE INDEX identities_erasure_idx ON ' . IdentityInterface::TABLE . ' (' . IdentityInterface::ATTR_DELETED_AT . ') WHERE deleted_at IS NOT NULL');
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(IdentityInterface::TABLE);
    }
};
