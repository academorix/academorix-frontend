<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity identity --force`.

declare(strict_types=1);

namespace Academorix\Identity\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AutoUnlockExpiredLockoutsJob cleared an expired lockout OR platform-admin invoked `identity:unlock`. failed_attempts_count reset to 0 in the same transaction.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitIdentityUnlockedMetric
 *
 * @category Identity
 *
 * @since    0.1.0
 */
final readonly class IdentityUnlocked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $unlockedAt  ISO-8601 timestamp
     * @param  string  $trigger  'auto_expiry' | 'admin_action'
     * @param  string  $actorId  ULID | null
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $unlockedAt,
        public readonly string $trigger,
        public readonly string $actorId,
    ) {
    }
}
