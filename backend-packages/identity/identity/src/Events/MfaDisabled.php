<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity identity --force`.

declare(strict_types=1);

namespace Academorix\Identity\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Identity.mfa_secret_encrypted transitioned to NULL. Recovery codes purged in the same transaction. Requires re-authentication + explicit consent — never fires as a side-effect.
 *
 * ## Consumers
 *
 * - notifications::DispatchMfaDisabledNotification
 * - activity::WriteToActivityLog
 * - observability::EmitMfaDisabledMetric
 *
 * @category Identity
 *
 * @since    0.1.0
 */
final readonly class MfaDisabled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $disabledAt  ISO-8601 timestamp
     * @param  string  $trigger  'self_service' | 'admin_action' | 'account_recovery'
     * @param  string  $actorId  ULID | null
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $disabledAt,
        public readonly string $trigger,
        public readonly string $actorId,
    ) {
    }
}
