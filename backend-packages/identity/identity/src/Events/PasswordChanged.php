<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity identity --force`.

declare(strict_types=1);

namespace Academorix\Identity\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Identity.password_hash mutated (via voluntary change OR forced reset). Password added to password_history_hashed rolling window in the same transaction.
 *
 * ## Consumers
 *
 * - notifications::DispatchPasswordChangedConfirmationNotification
 * - auth::RevokeAllActiveSessions
 * - activity::WriteToActivityLog
 * - observability::EmitPasswordChangedMetric
 * - analytics::TrackPasswordChanged
 *
 * @category Identity
 *
 * @since    0.1.0
 */
final readonly class PasswordChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $changedAt  ISO-8601 timestamp
     * @param  string  $trigger  'voluntary' | 'forced_reset' | 'admin_reset' | 'breach_detected'
     * @param  string  $actorId  ULID | null (null = self-service; non-null for admin/system actions)
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $changedAt,
        public readonly string $trigger,
        public readonly string $actorId,
    ) {
    }
}
