<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity identity --force`.

declare(strict_types=1);

namespace Academorix\Identity\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Identity.email_verified_at flipped from NULL to now(). Idempotent — re-verifying a verified identity is a no-op.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitEmailVerifiedMetric
 * - user::MaybePromoteUserStatusFromPending
 *
 * @category Identity
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class IdentityEmailVerified implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $verifiedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $verifiedAt,
    ) {
    }
}
