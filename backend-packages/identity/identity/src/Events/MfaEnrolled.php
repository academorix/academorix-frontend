<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity identity --force`.

declare(strict_types=1);

namespace Academorix\Identity\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Identity.mfa_secret_encrypted transitioned from NULL to a KMS-encrypted value AND the enrolment was confirmed by a valid TOTP challenge. Recovery codes generated + hashed in the same transaction.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitMfaEnrolledMetric
 * - analytics::TrackMfaEnrolled
 *
 * @category Identity
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MfaEnrolled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $enrolledAt  ISO-8601 timestamp
     * @param  int  $recoveryCodesCount  integer (10)
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $enrolledAt,
        public readonly int $recoveryCodesCount,
    ) {
    }
}
