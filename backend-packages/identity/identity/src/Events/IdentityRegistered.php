<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity identity --force`.

declare(strict_types=1);

namespace Academorix\Identity\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New Identity row inserted after passing compliance-floor + email-uniqueness checks. Fires exactly once per Identity, ever.
 *
 * ## Consumers
 *
 * - auth::DispatchEmailVerificationChallenge
 * - activity::WriteToActivityLog
 * - observability::EmitIdentityCreatedMetric
 * - analytics::TrackIdentityRegistered
 *
 * @category Identity
 *
 * @since    0.1.0
 */
final readonly class IdentityRegistered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  int  $emailFingerprint  string (SHA-256 of normalised email — never the plaintext)
     * @param  string  $regionCountryCode  string (ISO-3166-2)
     * @param  bool  $isMinor  boolean (resolved by CompliantAgePolicy at creation time)
     * @param  string  $registeredAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $identityId,
        public readonly int $emailFingerprint,
        public readonly string $regionCountryCode,
        public readonly bool $isMinor,
        public readonly string $registeredAt,
    ) {
    }
}
