<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity identity --force`.

declare(strict_types=1);

namespace Academorix\Identity\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Identity soft-deleted (GDPR erasure request received) OR hard-deleted (hold-period expired). Two dispatches per full erasure lifecycle.
 *
 * ## Consumers
 *
 * - user::CascadeUserErasureForIdentity
 * - activity::WriteToActivityLog
 * - compliance::RecordErasureEvent
 * - observability::EmitIdentityErasedMetric
 *
 * @category Identity
 *
 * @since    0.1.0
 */
final readonly class IdentityErased implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $erasurePhase  'soft_delete' | 'hard_delete'
     * @param  string  $erasedAt  ISO-8601 timestamp
     * @param  string  $actorId  ULID | null
     * @param  string  $reason  string (free-form justification captured at request time)
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $erasurePhase,
        public readonly string $erasedAt,
        public readonly string $actorId,
        public readonly string $reason,
    ) {
    }
}
