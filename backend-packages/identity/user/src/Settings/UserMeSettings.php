<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Academorix\User\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * UserMeSettings — My profile.
 *
 * Per-user preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category User
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'user_me',
    label: 'My profile',
    description: 'Per-user preferences.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 4,
)]
class UserMeSettings extends Settings
{

    // ── Locale + time ──────────────────────────────────
    #[SettingGroup(label: 'Locale + time', icon: 'clock', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Preferred language', group: 'Locale + time', sortOrder: 1, helpText: 'Overrides the tenant default for this user.')]
    public ?string $preferred_locale = null;

    #[SettingField(controlType: ControlType::Select, label: 'Time zone', group: 'Locale + time', sortOrder: 2)]
    public ?string $timezone = null;


    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', icon: 'bell', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Email me when I\'m added to a tenant', group: 'Notifications', sortOrder: 1)]
    public bool $notify_added_to_tenant = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Email me when my profile changes', group: 'Notifications', sortOrder: 2, helpText: 'Recommended — helps you catch account-takeover attempts.')]
    public bool $notify_profile_updated = false;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'user_me';
    }
}
