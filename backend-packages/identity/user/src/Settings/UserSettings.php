<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Academorix\User\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * UserSettings — Users.
 *
 * Tenant-wide policy for user provisioning + lifecycle.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category User
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'user',
    label: 'Users',
    description: 'Tenant-wide policy for user provisioning + lifecycle.',
    icon: 'user-group',
    scope: 'tenant',
    permission: 'users.manage',
    sortOrder: 20,
)]
class UserSettings extends Settings
{

    // ── Registration ──────────────────────────────────
    #[SettingGroup(label: 'Registration', description: 'Who can create new users, and under what conditions.', icon: 'user-plus', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Allow self-registration', group: 'Registration', sortOrder: 1, helpText: 'When on, anyone with the invitation-free sign-up URL can create an account.')]
    public bool $self_registration_enabled = false;

    #[SettingField(controlType: ControlType::Toggle, label: 'Require invitation', group: 'Registration', sortOrder: 2, helpText: 'Users must accept an invitation to register.')]
    public bool $requires_invitation = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Require admin approval', group: 'Registration', sortOrder: 3, helpText: 'New users land in pending status until an admin activates them.')]
    public bool $requires_admin_approval = false;

    #[SettingField(controlType: ControlType::Select, label: 'Default role for new members', group: 'Registration', sortOrder: 4)]
    public string $default_role_slug = 'member';


    // ── Profile requirements ──────────────────────────────────
    #[SettingGroup(label: 'Profile requirements', description: 'Which fields are mandatory on a new profile.', icon: 'identification', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Require full name', group: 'Profile requirements', sortOrder: 1)]
    public bool $require_first_last_name = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Require phone number', group: 'Profile requirements', sortOrder: 2)]
    public bool $require_phone = false;

    #[SettingField(controlType: ControlType::Toggle, label: 'Allow profile picture upload', group: 'Profile requirements', sortOrder: 3)]
    public bool $allow_avatar_upload = true;

    #[SettingField(controlType: ControlType::Number, label: 'Maximum profile picture size (KB)', group: 'Profile requirements', sortOrder: 4, min: 128, max: 8192, helpText: 'Upper bound. Per-tier cap clamps this down.')]
    public int $max_avatar_size_kb = 2048;


    // ── Privacy + retention ──────────────────────────────────
    #[SettingGroup(label: 'Privacy + retention', description: 'Minor visibility + erasure timelines. Compliance floors may tighten these values automatically.', icon: 'shield-check', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Select, label: 'Who sees minor profile PII', group: 'Privacy + retention', sortOrder: 1, options: ['guardian_only' => 'Guardians + admins with child-PII permission', 'tenant_admin' => 'Every tenant admin', 'any_authenticated' => 'Any authenticated caller in the tenant'], helpText: 'COPPA + regional GDPR floors may force `guardian_only`.')]
    public string $minor_data_visibility = 'guardian_only';

    #[SettingField(controlType: ControlType::Number, label: 'Age of majority (years)', group: 'Privacy + retention', sortOrder: 2, min: 16, max: 21, helpText: 'Age at which guardianship auto-terminates + medical/financial data transfers to the athlete\'s own consent.')]
    public int $adult_transition_age = 18;

    #[SettingField(controlType: ControlType::Number, label: 'Days to hold erased users before purge', group: 'Privacy + retention', sortOrder: 3, min: 30, max: 90, helpText: 'Soft-deleted rows survive this window — restorable during it. Compliance floor: 30 days.')]
    public int $erasure_hold_days = 30;


    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', icon: 'bell-alert', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Confirmation email when profile is updated', group: 'Notifications', sortOrder: 1, helpText: 'Helps catch account-takeover attempts.')]
    public bool $notify_profile_updated = false;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'user';
    }
}
