<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Academorix\User\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * User called POST /me/tenants/{tenant}/switch. Session state updated; next API call carries the new tenant in `X-Tenant-Id`.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 *
 * @category User
 *
 * @since    0.1.0
 */
final readonly class UserSwitchedActiveTenant implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $fromTenantId  ULID
     * @param  string  $toTenantId  ULID
     * @param  string  $sessionId  string (Sanctum PAT id)
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $fromTenantId,
        public readonly string $toTenantId,
        public readonly string $sessionId,
    ) {
    }
}
