<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Academorix\User\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TenantMember row soft-deleted OR hard-deleted. Sanctum PATs scoped to that (User, Tenant) pair are revoked in-process.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - auth::RevokeTokensScopedToMembership
 * - notifications::DispatchUserRemovedFromTenantNotification
 *
 * @category User
 *
 * @since    0.1.0
 */
final readonly class UserRemovedFromTenant implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $membershipId  ULID
     * @param  string  $removedBy  ULID
     * @param  string  $reason  string | null
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $tenantId,
        public readonly string $membershipId,
        public readonly string $removedBy,
        public readonly string $reason,
    ) {
    }
}
