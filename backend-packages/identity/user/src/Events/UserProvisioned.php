<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Academorix\User\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ProvisionUser action completed. Fires exactly once per (identity_id, application_id, tenant_id) triple.
 *
 * ## Consumers
 *
 * - user::AssignInitialRole
 * - user::SeedDefaultTenantMembership
 * - notifications::DispatchUserProvisionedNotification
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - access::SeedDefaultPermissions
 * - auth::MaybeIssueEmailVerification
 *
 * @category User
 *
 * @since    0.1.0
 */
final readonly class UserProvisioned implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID (usr_*)
     * @param  string  $identityId  ULID (idn_*)
     * @param  string  $applicationId  ULID (app_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $profileId  ULID (prf_*)
     * @param  string  $initialRoleId  ULID (rol_*) | null
     * @param  string  $provisionedBy  ULID (usr_*) | null (null = self-registration / system)
     * @param  string  $source  'invitation' | 'self_registration' | 'admin_created' | 'sso_grant' | 'import'
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $identityId,
        public readonly string $applicationId,
        public readonly string $tenantId,
        public readonly string $profileId,
        public readonly string $initialRoleId,
        public readonly string $provisionedBy,
        public readonly string $source,
    ) {
    }
}
