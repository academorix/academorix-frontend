<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Academorix\User\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PurgeErasedUsersJob removed the row after the hold window. Cascade delete of TenantMember rows completes atomically before dispatch.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — the audit outlives the row)
 * - activity::WriteToActivityLog
 *
 * @category User
 *
 * @since    0.1.0
 */
final readonly class UserHardDeleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID (the row is gone; the ULID is preserved in the audit trail)
     * @param  string  $tenantId  ULID
     * @param  string  $identityId  ULID (Identity may live on — other Users per Application)
     * @param  string  $profileId  ULID
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $tenantId,
        public readonly string $identityId,
        public readonly string $profileId,
    ) {
    }
}
