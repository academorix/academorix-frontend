<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Academorix\User\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Profile.avatar_url set OR cleared. Fired independently of ProfileUpdated because avatar changes trigger a media-pruning listener.
 *
 * ## Consumers
 *
 * - media::PruneOldAvatar (deletes the previous S3 object after 24h grace window)
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category User
 *
 * @since    0.1.0
 */
final readonly class AvatarUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $profileId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $oldAvatarUrl  string | null
     * @param  string  $newAvatarUrl  string | null
     * @param  string  $updatedBy  ULID
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $profileId,
        public readonly string $tenantId,
        public readonly string $oldAvatarUrl,
        public readonly string $newAvatarUrl,
        public readonly string $updatedBy,
    ) {
    }
}
