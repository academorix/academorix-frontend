<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Academorix\User\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * User.deleted_at set (soft delete). Distinct from UserHardDeleted — the row survives in the hold window.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - auth::RevokeAllTokens
 *
 * @category User
 *
 * @since    0.1.0
 */
final readonly class UserSoftDeleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $deletedBy  ULID
     * @param  string  $erasureHoldEndsAt  ISO 8601 timestamp
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $tenantId,
        public readonly string $deletedBy,
        public readonly string $erasureHoldEndsAt,
    ) {
    }
}
