<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

namespace Academorix\User\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Any Profile field mutated (except avatar_url — that fires AvatarUpdated).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::MaybeDispatchProfileUpdatedConfirmation (opt-in on `notify_profile_updated` preference)
 *
 * @category User
 *
 * @since    0.1.0
 */
final readonly class ProfileUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $profileId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $updatedBy  ULID (may equal user_id for self-update)
     * @param  string  $changedFields  string[] (e.g. ['first_name', 'phone_e164'])
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $profileId,
        public readonly string $tenantId,
        public readonly string $updatedBy,
        public readonly string $changedFields,
    ) {
    }
}
