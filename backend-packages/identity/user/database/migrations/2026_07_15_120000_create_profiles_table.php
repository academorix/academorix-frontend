<?php

/**
 * @file database/migrations/2026_07_15_120000_create_profiles_table.php
 *
 * @description
 * Create the `profiles` table for the `Profile` entity.
 * Owned by the user module. Every column + index reference
 * goes through {@see Academorix\User\Contracts\Data\ProfileInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity user --force`.

declare(strict_types=1);

use Academorix\User\Contracts\Data\ProfileInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(ProfileInterface::TABLE, function (Blueprint $table): void {
            $table->string(ProfileInterface::ATTR_ID, 64)->primary();
            $table->string(ProfileInterface::ATTR_TENANT_ID, 64);
            $table->foreign(ProfileInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(ProfileInterface::ATTR_FIRST_NAME, 100)->nullable();
            $table->string(ProfileInterface::ATTR_LAST_NAME, 100)->nullable();
            $table->string(ProfileInterface::ATTR_DISPLAY_NAME, 100)->nullable();
            $table->string(ProfileInterface::ATTR_AVATAR_URL, 500)->nullable();
            $table->string(ProfileInterface::ATTR_PHONE_E164, 20)->nullable();
            $table->string(ProfileInterface::ATTR_LOCALE, 12)->nullable();
            $table->string(ProfileInterface::ATTR_TIMEZONE, 64);
            $table->date(ProfileInterface::ATTR_DOB)->nullable();
            $table->jsonb(ProfileInterface::ATTR_METADATA)->nullable();
            $table->uuid(ProfileInterface::ATTR_CREATED_BY)->nullable();
            $table->uuid(ProfileInterface::ATTR_UPDATED_BY)->nullable();
            $table->uuid(ProfileInterface::ATTR_DELETED_BY)->nullable();
            $table->timestampTz(ProfileInterface::ATTR_DELETED_AT)->nullable();
            $table->timestampTz(ProfileInterface::ATTR_CREATED_AT);
            $table->timestampTz(ProfileInterface::ATTR_UPDATED_AT);
            $table->index([ProfileInterface::ATTR_TENANT_ID, ProfileInterface::ATTR_LAST_NAME, ProfileInterface::ATTR_FIRST_NAME], 'profiles_tenant_name_idx');
            $table->index([ProfileInterface::ATTR_TENANT_ID, ProfileInterface::ATTR_PHONE_E164], 'profiles_tenant_phone_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(ProfileInterface::TABLE);
    }
};
