<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity platform-user --force`.

declare(strict_types=1);

namespace Academorix\PlatformUser\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PlatformUser.manager_id mutated from a non-null value to a different non-null value, or to null (orphaning to top-of-tree), or from null to a non-null value (adopting into a chain).
 *
 * ## Consumers
 *
 * - notifications::DispatchPlatformUserManagerReassignedNotification
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRow
 * - observability::EmitPlatformUserManagerReassignedMetric
 *
 * @category PlatformUser
 *
 * @since    0.1.0
 */
final readonly class PlatformUserManagerReassigned implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $platformUserId  ULID
     * @param  string  $previousManagerId  ULID | null
     * @param  string  $newManagerId  ULID | null
     * @param  string  $reassignedAt  ISO-8601 timestamp
     * @param  string  $actorId  ULID (usually a super_admin during a re-org)
     */
    public function __construct(
        public readonly string $platformUserId,
        public readonly string $previousManagerId,
        public readonly string $newManagerId,
        public readonly string $reassignedAt,
        public readonly string $actorId,
    ) {
    }
}
