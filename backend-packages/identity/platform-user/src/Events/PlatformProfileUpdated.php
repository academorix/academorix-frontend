<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity platform-user --force`.

declare(strict_types=1);

namespace Academorix\PlatformUser\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * One or more columns on the PlatformProfile row mutated. Fires once per save regardless of the number of changed columns. Includes the changed field names so listeners can filter (e.g. `avatar_updated` listener only reacts when `avatar_url` is in the changed set).
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRow
 * - observability::EmitPlatformProfileUpdatedMetric
 *
 * @category PlatformUser
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PlatformProfileUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $platformUserId  ULID
     * @param  string  $platformProfileId  ULID
     * @param  string  $changedFields  string[] (subset of: first_name, last_name, display_name, avatar_url, phone_e164, slack_handle, github_handle, timezone)
     * @param  string  $updatedAt  ISO-8601 timestamp
     * @param  string  $actorId  ULID (the PlatformUser themselves for self-service, OR the super_admin for admin-driven writes)
     */
    public function __construct(
        public readonly string $platformUserId,
        public readonly string $platformProfileId,
        public readonly string $changedFields,
        public readonly string $updatedAt,
        public readonly string $actorId,
    ) {
    }
}
