<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity platform-user --force`.

declare(strict_types=1);

namespace Academorix\PlatformUser\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PlatformUser.status transitioned from `pending` to `active` (usually via the onboarding checklist completing) OR from `suspended` back to `active` via `restore`.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRow
 * - observability::EmitPlatformUserActivatedMetric
 *
 * @category PlatformUser
 *
 * @since    0.1.0
 */
final readonly class PlatformUserActivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $platformUserId  ULID
     * @param  string  $previousStatus  'pending' | 'suspended'
     * @param  string  $activatedAt  ISO-8601 timestamp
     * @param  string  $actorId  ULID
     */
    public function __construct(
        public readonly string $platformUserId,
        public readonly string $previousStatus,
        public readonly string $activatedAt,
        public readonly string $actorId,
    ) {
    }
}
