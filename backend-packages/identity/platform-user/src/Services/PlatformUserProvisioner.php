<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity platform-user --force`.

declare(strict_types=1);

namespace Academorix\PlatformUser\Services;

use Academorix\PlatformUser\Contracts\Services\PlatformUserProvisionerInterface;
use Illuminate\Container\Attributes\Scoped;
use Academorix\PlatformUser\Contracts\Repositories\PlatformProfileRepositoryInterface;

/**
 * Concrete service — PlatformUserProvisioner.
 *
 * Implements {@see PlatformUserProvisionerInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category PlatformUser
 *
 * @since    0.1.0
 */
#[Scoped]
final class PlatformUserProvisioner implements PlatformUserProvisionerInterface
{
    /**
     * @param  PlatformProfileRepositoryInterface  $platformProfileRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly PlatformProfileRepositoryInterface $platformProfileRepository,
    ) {
    }
}
