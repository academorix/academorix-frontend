<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity platform-user --force`.

declare(strict_types=1);

namespace Academorix\PlatformUser\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the PlatformUser module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category PlatformUser
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum PlatformUserPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `platform.platform_users.viewAny` — List platform staff.
     */
    #[Label('List platform staff')]
    #[Description('List platform staff. Redacted list — no PII, no offboarding_reason.')]
    case PlatformPlatformUsersViewAny = 'platform.platform_users.viewAny';

    /**
     * `platform.platform_users.view` — Read a specific platform staff row.
     */
    #[Label('Read a specific platform staff row')]
    #[Description('Read a specific platform staff row. Redacted per PII gate.')]
    case PlatformPlatformUsersView = 'platform.platform_users.view';

    /**
     * `platform.platform_users.view.pii` — Unmutes email + phone + Slack/GitHub handles on the response.
     */
    #[Label('Unmutes email + phone + Slack/GitHub handles on the response')]
    #[Description('Unmutes email + phone + Slack/GitHub handles on the response. Every unmute writes an audit meta-row.')]
    case PlatformPlatformUsersViewPii = 'platform.platform_users.view.pii';

    /**
     * `platform.platform_users.view.hr_context` — Unmutes offboarding_reason + suspension_reason (free-form HR-sensitive text).
     */
    #[Label('Unmutes offboarding_reason + suspension_reason (free-form HR-sensitive text)')]
    #[Description('Unmutes offboarding_reason + suspension_reason (free-form HR-sensitive text).')]
    case PlatformPlatformUsersViewHrContext = 'platform.platform_users.view.hr_context';

    /**
     * `platform.platform_users.view.audit_trail` — Read the 90-day audit-trail summary for a specific PlatformUser as actor.
     */
    #[Label('Read the 90-day audit-trail summary for a specific PlatformUser as actor')]
    #[Description('Read the 90-day audit-trail summary for a specific PlatformUser as actor.')]
    case PlatformPlatformUsersViewAuditTrail = 'platform.platform_users.view.audit_trail';

    /**
     * `platform.platform_users.hire` — Create new PlatformUser rows via the hire flow.
     */
    #[Label('Create new PlatformUser rows via the hire flow')]
    #[Description('Create new PlatformUser rows via the hire flow.')]
    case PlatformPlatformUsersHire = 'platform.platform_users.hire';

    /**
     * `platform.platform_users.update` — Update employment metadata (department, employment_type, job_title, manager_id, hire_date).
     */
    #[Label('Update employment metadata (department, employment_type, job_title, manager_id,')]
    #[Description('Update employment metadata (department, employment_type, job_title, manager_id, hire_date).')]
    case PlatformPlatformUsersUpdate = 'platform.platform_users.update';

    /**
     * `platform.platform_users.suspend` — Temporarily suspend a PlatformUser.
     */
    #[Label('Temporarily suspend a PlatformUser')]
    #[Description('Temporarily suspend a PlatformUser.')]
    case PlatformPlatformUsersSuspend = 'platform.platform_users.suspend';

    /**
     * `platform.platform_users.restore` — Reverse a suspend.
     */
    #[Label('Reverse a suspend')]
    #[Description('Reverse a suspend.')]
    case PlatformPlatformUsersRestore = 'platform.platform_users.restore';

    /**
     * `platform.platform_users.offboard` — Terminal offboarding.
     */
    #[Label('Terminal offboarding')]
    #[Description('Terminal offboarding. Kicks off the 24h access-revocation SLA + 90-day retention hold.')]
    case PlatformPlatformUsersOffboard = 'platform.platform_users.offboard';

    /**
     * `platform.platform_users.reassign_manager` — Reassign a PlatformUser's manager.
     */
    #[Label('Reassign a PlatformUser\'s manager')]
    #[Description('Reassign a PlatformUser\'s manager. Enforces cycle detection + depth cap.')]
    case PlatformPlatformUsersReassignManager = 'platform.platform_users.reassign_manager';

    /**
     * `platform.platform_profiles.view.self` — Read own PlatformProfile row (self-service).
     */
    #[Label('Read own PlatformProfile row (self-service)')]
    #[Description('Read own PlatformProfile row (self-service).')]
    case PlatformPlatformProfilesViewSelf = 'platform.platform_profiles.view.self';

    /**
     * `platform.platform_profiles.update.self` — Update own PlatformProfile fields (except first_name + last_name, which are HR-controlled).
     */
    #[Label('Update own PlatformProfile fields (except first_name + last_name, which are HR-c')]
    #[Description('Update own PlatformProfile fields (except first_name + last_name, which are HR-controlled).')]
    case PlatformPlatformProfilesUpdateSelf = 'platform.platform_profiles.update.self';

    /**
     * `platform.platform_profiles.view.pii` — Unmute PII fields (phone_e164, slack_handle, github_handle) on ANOTHER PlatformUser's profile.
     */
    #[Label('Unmute PII fields (phone_e164, slack_handle, github_handle) on ANOTHER PlatformU')]
    #[Description('Unmute PII fields (phone_e164, slack_handle, github_handle) on ANOTHER PlatformUser\'s profile.')]
    case PlatformPlatformProfilesViewPii = 'platform.platform_profiles.view.pii';

    /**
     * `platform.on_call.view` — Read the on-call rotation resolver.
     */
    #[Label('Read the on-call rotation resolver')]
    #[Description('Read the on-call rotation resolver. Unmutes phone_e164 on the resolution response (specifically — not the general view.pii). Also seeded on the on_call_router_service_account for internal escalation tooling.')]
    case PlatformOnCallView = 'platform.on_call.view';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::PlatformPlatformUsersViewAny => Guard::PlatformAdmin,
            self::PlatformPlatformUsersView => Guard::PlatformAdmin,
            self::PlatformPlatformUsersViewPii => Guard::PlatformAdmin,
            self::PlatformPlatformUsersViewHrContext => Guard::PlatformAdmin,
            self::PlatformPlatformUsersViewAuditTrail => Guard::PlatformAdmin,
            self::PlatformPlatformUsersHire => Guard::PlatformAdmin,
            self::PlatformPlatformUsersUpdate => Guard::PlatformAdmin,
            self::PlatformPlatformUsersSuspend => Guard::PlatformAdmin,
            self::PlatformPlatformUsersRestore => Guard::PlatformAdmin,
            self::PlatformPlatformUsersOffboard => Guard::PlatformAdmin,
            self::PlatformPlatformUsersReassignManager => Guard::PlatformAdmin,
            self::PlatformPlatformProfilesViewSelf => Guard::PlatformAdmin,
            self::PlatformPlatformProfilesUpdateSelf => Guard::PlatformAdmin,
            self::PlatformPlatformProfilesViewPii => Guard::PlatformAdmin,
            self::PlatformOnCallView => Guard::PlatformAdmin,
        };
    }
}
