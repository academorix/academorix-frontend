<?php

/**
 * @file database/migrations/2026_07_15_120000_create_platform_profiles_table.php
 *
 * @description
 * Create the `platform_profiles` table for the `PlatformProfile` entity.
 * Owned by the platform-user module. Emitted from the module blueprint —
 * regenerate via `generate-module.py identity platform-user --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity platform-user --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('platform_profiles', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('first_name', 128);
            $table->string('last_name', 128);
            $table->string('display_name', 128)->nullable();
            $table->text('avatar_url')->nullable();
            $table->string('phone_e164', 20);
            $table->string('slack_handle', 64)->nullable();
            $table->string('github_handle', 64)->nullable();
            $table->string('timezone', 64);
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->index(['last_name', 'first_name'], 'platform_profiles_name_idx');
            $table->index(['slack_handle'], 'platform_profiles_slack_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE slack_handle IS NOT NULL
            $table->index(['github_handle'], 'platform_profiles_github_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE github_handle IS NOT NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('platform_profiles');
    }
};
