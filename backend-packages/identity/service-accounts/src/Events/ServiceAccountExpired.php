<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity service-accounts --force`.

declare(strict_types=1);

namespace Academorix\ServiceAccounts\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PurgeExpiredServiceAccountsJob detected an SA past expires_at + grace_days_after_expiry with no rotation. The job soft-disables the SA in the same transaction; this event fires after commit.
 *
 * ## Consumers
 *
 * - notifications::DispatchServiceAccountExpiredNotification
 * - activity::WriteToActivityLog
 * - observability::EmitServiceAccountExpiredMetric
 * - analytics::TrackServiceAccountExpired
 *
 * @category ServiceAccounts
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ServiceAccountExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $serviceAccountId  ULID
     * @param  string  $applicationId  ULID
     * @param  string  $expiresAt  ISO-8601 timestamp (past)
     * @param  int  $daysPastExpiry  integer
     * @param  int  $daysSinceLastUse  integer | null
     * @param  string  $detectedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $serviceAccountId,
        public readonly string $applicationId,
        public readonly string $expiresAt,
        public readonly int $daysPastExpiry,
        public readonly int $daysSinceLastUse,
        public readonly string $detectedAt,
    ) {
    }
}
