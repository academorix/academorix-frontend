<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity service-accounts --force`.

declare(strict_types=1);

namespace Academorix\ServiceAccounts\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * secret_hash mutated. Also updates secret_rotated_at + expires_at in the same transaction. Fires whether the trigger was manual (operator ran service-account:rotate) or scheduled (future RotateServiceAccountSecretJob).
 *
 * ## Consumers
 *
 * - cache::InvalidateServiceAccountLookup
 * - activity::WriteToActivityLog
 * - observability::EmitServiceAccountRotatedMetric
 * - analytics::TrackServiceAccountSecretRotated
 *
 * @category ServiceAccounts
 *
 * @since    0.1.0
 */
final readonly class ServiceAccountSecretRotated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $serviceAccountId  ULID
     * @param  string  $applicationId  ULID
     * @param  string  $trigger  'manual' | 'scheduled' | 'incident' | 'expired_grace'
     * @param  string  $actorPlatformUserId  ULID | null
     * @param  int  $daysSinceLastRotation  integer
     * @param  string  $oldExpiresAt  ISO-8601 timestamp
     * @param  string  $newExpiresAt  ISO-8601 timestamp
     * @param  string  $rotatedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $serviceAccountId,
        public readonly string $applicationId,
        public readonly string $trigger,
        public readonly string $actorPlatformUserId,
        public readonly int $daysSinceLastRotation,
        public readonly string $oldExpiresAt,
        public readonly string $newExpiresAt,
        public readonly string $rotatedAt,
    ) {
    }
}
