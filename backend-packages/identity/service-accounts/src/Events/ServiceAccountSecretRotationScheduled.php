<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity service-accounts --force`.

declare(strict_types=1);

namespace Academorix\ServiceAccounts\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * NotifyRotationDueJob detected an SA whose expires_at falls inside the warning window (default 14d). Fires once per SA per week the SA is inside the window — debounced so the notification volume matches the weekly job cadence.
 *
 * ## Consumers
 *
 * - notifications::DispatchServiceAccountSecretRotationDueNotification
 * - activity::WriteToActivityLog
 * - analytics::TrackServiceAccountRotationDueNotified
 *
 * @category ServiceAccounts
 *
 * @since    0.1.0
 */
final readonly class ServiceAccountSecretRotationScheduled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $serviceAccountId  ULID
     * @param  string  $applicationId  ULID
     * @param  int  $daysUntilExpiry  integer
     * @param  string  $expiresAt  ISO-8601 timestamp
     * @param  string  $notifiedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $serviceAccountId,
        public readonly string $applicationId,
        public readonly int $daysUntilExpiry,
        public readonly string $expiresAt,
        public readonly string $notifiedAt,
    ) {
    }
}
