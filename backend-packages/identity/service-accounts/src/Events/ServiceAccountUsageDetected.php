<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity service-accounts --force`.

declare(strict_types=1);

namespace Academorix\ServiceAccounts\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The FIRST request after a > `security.dormant_threshold_days` (default 30) silence. Distinct from the periodic Dormant Detected — this signals a wake-up which is compromise-adjacent. Debounced so subsequent requests within the same 24h window do NOT re-fire.
 *
 * ## Consumers
 *
 * - notifications::DispatchServiceAccountUsageDetectedNotification
 * - activity::WriteToActivityLog
 * - observability::EmitServiceAccountUsageAfterDormancyMetric
 * - analytics::TrackServiceAccountUsageDetectedAfterDormancy
 *
 * @category ServiceAccounts
 *
 * @since    0.1.0
 */
final readonly class ServiceAccountUsageDetected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $serviceAccountId  ULID
     * @param  string  $applicationId  ULID
     * @param  int  $daysSinceLastUse  integer
     * @param  string  $requestIpPrefix24  string
     * @param  string  $userAgentHash  string (SHA-256 of full UA truncated to 8 chars for label cardinality)
     * @param  string  $detectedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $serviceAccountId,
        public readonly string $applicationId,
        public readonly int $daysSinceLastUse,
        public readonly string $requestIpPrefix24,
        public readonly string $userAgentHash,
        public readonly string $detectedAt,
    ) {
    }
}
