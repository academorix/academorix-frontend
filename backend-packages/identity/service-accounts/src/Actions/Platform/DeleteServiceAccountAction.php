<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity service-accounts --force`.

declare(strict_types=1);

namespace Academorix\ServiceAccounts\Actions\Platform;

use Academorix\ServiceAccounts\Contracts\Repositories\ServiceAccountRepositoryInterface;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Delete;
use Illuminate\Http\Response;

/**
 * `DELETE /api/v1/platform/service-accounts/{serviceAccount}` — delete action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Delete(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category ServiceAccounts
 *
 * @since    0.1.0
 */
#[AsController]
#[Delete('/api/v1/platform/service-accounts/{serviceAccount}')]
final class DeleteServiceAccountAction
{
    public function __construct(
        private readonly ServiceAccountRepositoryInterface $repository,
    ) {
    }

    /**
     * Soft-delete one `service-account` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): Response
    {
        $this->repository->delete($id);

        return response()->noContent();
    }
}
