<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity service-accounts --force`.

declare(strict_types=1);

namespace Academorix\ServiceAccounts\Actions\Platform;

use Academorix\ServiceAccounts\Contracts\Repositories\ServiceAccountRepositoryInterface;
use Academorix\ServiceAccounts\Data\ServiceAccountData;
use Academorix\ServiceAccounts\Data\Requests\CreateServiceAccountRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/platform/service-accounts` — create action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Post(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category ServiceAccounts
 *
 * @since    0.1.0
 */
#[AsController]
#[Post('/api/v1/platform/service-accounts')]
final class CreateServiceAccountAction
{
    public function __construct(
        private readonly ServiceAccountRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `service-account` from the validated request payload.
     *
     * @param  CreateServiceAccountRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateServiceAccountRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(ServiceAccountData::from($model), JsonResponse::HTTP_CREATED);
    }
}
