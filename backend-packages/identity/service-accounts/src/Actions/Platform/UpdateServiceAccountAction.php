<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity service-accounts --force`.

declare(strict_types=1);

namespace Academorix\ServiceAccounts\Actions\Platform;

use Academorix\ServiceAccounts\Contracts\Repositories\ServiceAccountRepositoryInterface;
use Academorix\ServiceAccounts\Data\ServiceAccountData;
use Academorix\ServiceAccounts\Data\Requests\UpdateServiceAccountRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/platform/service-accounts/{serviceAccount}` — update action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Patch(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category ServiceAccounts
 *
 * @since    0.1.0
 */
#[AsController]
#[Patch('/api/v1/platform/service-accounts/{serviceAccount}')]
final class UpdateServiceAccountAction
{
    public function __construct(
        private readonly ServiceAccountRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `service-account` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateServiceAccountRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateServiceAccountRequestData $data): ServiceAccountData
    {
        $model = $this->repository->update($id, $data->toArray());

        return ServiceAccountData::from($model);
    }
}
