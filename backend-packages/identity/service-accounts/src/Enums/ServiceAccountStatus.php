<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity service-accounts --force`.

declare(strict_types=1);

namespace Academorix\ServiceAccounts\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `status` on `service-account`.
 *
 * Computed categorical status derived from is_enabled + expires_at + last_used_at.
 *
 * @category ServiceAccounts
 *
 * @since    0.1.0
 */
enum ServiceAccountStatus: string
{
    use Enum;

    case Pending = 'pending';
    case Active = 'active';
    case Disabled = 'disabled';
    case Expired = 'expired';
}
