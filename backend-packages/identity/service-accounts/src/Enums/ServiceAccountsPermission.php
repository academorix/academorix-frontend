<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity service-accounts --force`.

declare(strict_types=1);

namespace Academorix\ServiceAccounts\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the ServiceAccounts module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category ServiceAccounts
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum ServiceAccountsPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `platform.service_accounts.viewAny` — List SAs.
     */
    #[Label('List SAs')]
    #[Description('List SAs. Redacted list — no secret material, no last_used_ip.')]
    case PlatformServiceAccountsViewAny = 'platform.service_accounts.viewAny';

    /**
     * `platform.service_accounts.view` — Show one SA.
     */
    #[Label('Show one SA')]
    #[Description('Show one SA. Includes signer_kid + expiry + status. No secret material.')]
    case PlatformServiceAccountsView = 'platform.service_accounts.view';

    /**
     * `platform.service_accounts.view.audit_trail` — Read the audit-usage report (usage histogram, IP-tuple diversity, jti-reuse detections, last 100 audit rows).
     */
    #[Label('Read the audit-usage report (usage histogram, IP-tuple diversity, jti-reuse dete')]
    #[Description('Read the audit-usage report (usage histogram, IP-tuple diversity, jti-reuse detections, last 100 audit rows).')]
    case PlatformServiceAccountsViewAuditTrail = 'platform.service_accounts.view.audit_trail';

    /**
     * `platform.service_accounts.create` — Provision a new SA.
     */
    #[Label('Provision a new SA')]
    #[Description('Provision a new SA. Requires --reason. Returns the plaintext secret ONCE.')]
    case PlatformServiceAccountsCreate = 'platform.service_accounts.create';

    /**
     * `platform.service_accounts.update` — Update mutable fields (name, description, is_enabled, expires_at, metadata).
     */
    #[Label('Update mutable fields (name, description, is_enabled, expires_at, metadata)')]
    #[Description('Update mutable fields (name, description, is_enabled, expires_at, metadata). application_id + tenant_id + signer_kid are immutable regardless of this permission.')]
    case PlatformServiceAccountsUpdate = 'platform.service_accounts.update';

    /**
     * `platform.service_accounts.rotate` — Rotate the SA secret.
     */
    #[Label('Rotate the SA secret')]
    #[Description('Rotate the SA secret. Returns the plaintext new secret ONCE.')]
    case PlatformServiceAccountsRotate = 'platform.service_accounts.rotate';

    /**
     * `platform.service_accounts.disable` — Set is_enabled=false.
     */
    #[Label('Set is_enabled=false')]
    #[Description('Set is_enabled=false. Existing JWTs remain valid until natural expiry.')]
    case PlatformServiceAccountsDisable = 'platform.service_accounts.disable';

    /**
     * `platform.service_accounts.enable` — Set is_enabled=true.
     */
    #[Label('Set is_enabled=true')]
    #[Description('Set is_enabled=true. Refuses when expires_at has elapsed.')]
    case PlatformServiceAccountsEnable = 'platform.service_accounts.enable';

    /**
     * `platform.service_accounts.delete` — Soft-delete.
     */
    #[Label('Soft-delete')]
    #[Description('Soft-delete. Starts the retention hold clock. Actual hard-delete via PurgeExpiredServiceAccountsJob.')]
    case PlatformServiceAccountsDelete = 'platform.service_accounts.delete';

    /**
     * `platform.service_accounts.issue_test_jwt` — Issue a test JWT for troubleshooting.
     */
    #[Label('Issue a test JWT for troubleshooting')]
    #[Description('Issue a test JWT for troubleshooting. Uses `service-account:test-jwt` command OR the /issue-jwt HTTP endpoint. Every issue is audit-logged.')]
    case PlatformServiceAccountsIssueTestJwt = 'platform.service_accounts.issue_test_jwt';

    /**
     * `platform.service_accounts.view.metadata` — Read the SA's metadata JSONB (IP allowlist, ops notes, deployment hints).
     */
    #[Label('Read the SA\'s metadata JSONB (IP allowlist, ops notes, deployment hints)')]
    #[Description('Read the SA\'s metadata JSONB (IP allowlist, ops notes, deployment hints). Metadata may contain sensitive integration details; separate permission from view.')]
    case PlatformServiceAccountsViewMetadata = 'platform.service_accounts.view.metadata';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::PlatformServiceAccountsViewAny => Guard::PlatformAdmin,
            self::PlatformServiceAccountsView => Guard::PlatformAdmin,
            self::PlatformServiceAccountsViewAuditTrail => Guard::PlatformAdmin,
            self::PlatformServiceAccountsCreate => Guard::PlatformAdmin,
            self::PlatformServiceAccountsUpdate => Guard::PlatformAdmin,
            self::PlatformServiceAccountsRotate => Guard::PlatformAdmin,
            self::PlatformServiceAccountsDisable => Guard::PlatformAdmin,
            self::PlatformServiceAccountsEnable => Guard::PlatformAdmin,
            self::PlatformServiceAccountsDelete => Guard::PlatformAdmin,
            self::PlatformServiceAccountsIssueTestJwt => Guard::PlatformAdmin,
            self::PlatformServiceAccountsViewMetadata => Guard::PlatformAdmin,
        };
    }
}
