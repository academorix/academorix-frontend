<?php

/**
 * @file database/migrations/2026_07_15_120002_create_tenant_link_requests_table.php
 *
 * @description
 * Create the `tenant_link_requests` table for the `TenantLinkRequest` entity.
 * Owned by the people module. Emitted from the module blueprint —
 * regenerate via `generate-module.py identity people --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity people --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('tenant_link_requests', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('person_identity_id', 30);
            $table->foreign('person_identity_id')->references('id')->on('person_identities')->onDelete('cascade');
            $table->string('requesting_tenant_id', 30);
            $table->string('purpose', 32);
            $table->string('status', 16)->default('requested');
            $table->string('signature', 128);
            $table->timestampTz('requested_at');
            $table->timestampTz('approved_at')->nullable();
            $table->timestampTz('declined_at')->nullable();
            $table->timestampTz('revoked_at')->nullable();
            $table->timestampTz('expires_at');
            $table->string('requester_user_id', 30)->nullable();
            $table->jsonb('shared_fields')->nullable();
            $table->string('consenting_guardian_person_identity_id', 30)->nullable();
            $table->foreign('consenting_guardian_person_identity_id')->references('id')->on('person_identities')->onDelete('set null');
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->index(['person_identity_id', 'requesting_tenant_id', 'status'], 'tenant_link_requests_person_tenant_idx');
            $table->unique(['signature'], 'tenant_link_requests_signature_unique');
            $table->index(['expires_at'], 'tenant_link_requests_expiry_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE status = 'requested'
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('tenant_link_requests');
    }
};
