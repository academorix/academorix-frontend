<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity people --force`.

declare(strict_types=1);

namespace Academorix\People\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan people:audit-lookups` — Audit lookups — people command.
 *
 * @category People
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'people:audit-lookups',
    description: 'Audit lookups — people command.',
)]
final class AuditLookupsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Audit lookups — people command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Audit lookups — people command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
