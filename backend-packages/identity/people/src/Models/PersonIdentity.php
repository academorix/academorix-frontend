<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity people --force`.

declare(strict_types=1);

namespace Academorix\People\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\People\Contracts\Data\PersonIdentityInterface;
use Academorix\People\Database\Factories\PersonIdentityFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Foundation\Concerns\HasPrefixedUlid;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;

/**
 * Eloquent model for a PersonIdentity.
 *
 * CENTRAL-plane global identity carrying the Academorix ID.
 *
 * @category People
 *
 * @since    0.1.0
 */
#[Table(name: PersonIdentityInterface::TABLE, keyType: PersonIdentityInterface::KEY_TYPE)]
#[Fillable([
    PersonIdentityInterface::ATTR_ACADEMORIX_ID,
        PersonIdentityInterface::ATTR_LEGAL_NAME,
        PersonIdentityInterface::ATTR_DATE_OF_BIRTH,
        PersonIdentityInterface::ATTR_NATIONALITY,
        PersonIdentityInterface::ATTR_VERIFICATION_STATUS,
        PersonIdentityInterface::ATTR_VERIFIED_AT,
        PersonIdentityInterface::ATTR_VERIFIED_BY_PLATFORM_USER_ID,
        PersonIdentityInterface::ATTR_FROZEN_AT,
        PersonIdentityInterface::ATTR_FROZEN_REASON,
        PersonIdentityInterface::ATTR_METADATA,
])]
#[UseFactory(PersonIdentityFactory::class)]
final class PersonIdentity extends Model implements PersonIdentityInterface
{
    use HasFactory;
    use HasPrefixedUlid;
    use HasMetadata;
    use HasUserstamps;
    use Auditable;
    use Filterable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        PersonIdentityInterface::ATTR_VERIFICATION_STATUS => 'PersonVerificationStatus',
        PersonIdentityInterface::ATTR_DATE_OF_BIRTH => 'date',
        PersonIdentityInterface::ATTR_VERIFIED_AT => 'datetime',
        PersonIdentityInterface::ATTR_FROZEN_AT => 'datetime',
        PersonIdentityInterface::ATTR_METADATA => 'array',
    ];
}
