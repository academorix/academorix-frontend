<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity people --force`.

declare(strict_types=1);

namespace Academorix\People\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New PersonGuardianLink committed between two PersonIdentities. Cross-tenant portable — every tenant with either identity linked sees the guardian relationship on next resolution.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchGuardianLinkAddedNotification (guardian + minor owners)
 *
 * @category People
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class GuardianLinkAdded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $guardianLinkId  ULID (pgl_*)
     * @param  string  $minorPersonIdentityId  ULID
     * @param  string  $guardianPersonIdentityId  ULID
     * @param  string  $relationship  'parent' | 'step_parent' | 'legal_guardian' | 'grandparent' | 'other'
     */
    public function __construct(
        public readonly string $guardianLinkId,
        public readonly string $minorPersonIdentityId,
        public readonly string $guardianPersonIdentityId,
        public readonly string $relationship,
    ) {
    }
}
