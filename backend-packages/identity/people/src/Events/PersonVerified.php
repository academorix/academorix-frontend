<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity people --force`.

declare(strict_types=1);

namespace Academorix\People\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PersonIdentity.verification_status flipped to 'verified' by platform staff. All active tenant link references gain verified=true in their next resolution.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (central retention: 10 years)
 * - notifications::DispatchPersonVerifiedNotification (person owner)
 * - people::PropagateVerificationJob (all active tenant references)
 *
 * @category People
 *
 * @since    0.1.0
 */
final readonly class PersonVerified implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $personIdentityId  ULID
     * @param  string  $verifiedAt  ISO 8601
     * @param  string  $verifiedByPlatformUserId  ULID (pun_*)
     * @param  string  $verificationMethod  'passport_document' | 'national_id' | 'attested_by_original_tenant' | 'admin_manual'
     */
    public function __construct(
        public readonly string $personIdentityId,
        public readonly string $verifiedAt,
        public readonly string $verifiedByPlatformUserId,
        public readonly string $verificationMethod,
    ) {
    }
}
