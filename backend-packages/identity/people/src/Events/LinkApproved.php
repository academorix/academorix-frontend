<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity people --force`.

declare(strict_types=1);

namespace Academorix\People\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Guardian approved a TenantLinkRequest + selected shareable fields. Cascades to the requesting tenant's local Athlete/Staff prefill.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (central retention: 7 years)
 * - notifications::DispatchLinkApprovedNotification (requester + guardian)
 * - people::PropagateLinkPrefillToTenant (async cross-tenant push of consented fields)
 *
 * @category People
 *
 * @since    0.1.0
 */
final readonly class LinkApproved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantLinkRequestId  ULID
     * @param  string  $personIdentityId  ULID
     * @param  string  $requestingTenantId  ULID
     * @param  string  $consentingGuardianPersonIdentityId  ULID | null (null when the person is an adult approving for themselves)
     * @param  string  $sharedFields  string[]
     * @param  string  $approvedAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantLinkRequestId,
        public readonly string $personIdentityId,
        public readonly string $requestingTenantId,
        public readonly string $consentingGuardianPersonIdentityId,
        public readonly string $sharedFields,
        public readonly string $approvedAt,
    ) {
    }
}
