<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity people --force`.

declare(strict_types=1);

namespace Academorix\People\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Guardian revoked a previously-approved link. Stops future sync but preserves what the receiving tenant already created (per GDPR + local retention).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (central retention: 10 years)
 * - notifications::DispatchLinkRevokedNotification (requesting tenant admin + guardian, cannot-opt-out)
 * - people::MarkTenantReferenceStaleJob (async signal to tenant DB — local data preserved)
 *
 * @category People
 *
 * @since    0.1.0
 */
final readonly class LinkRevoked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantLinkRequestId  ULID
     * @param  string  $personIdentityId  ULID
     * @param  string  $affectedTenantId  ULID
     * @param  string  $revokedAt  ISO 8601
     * @param  string  $revokedByPersonIdentityId  ULID
     * @param  string  $revokedReason  string | null
     */
    public function __construct(
        public readonly string $tenantLinkRequestId,
        public readonly string $personIdentityId,
        public readonly string $affectedTenantId,
        public readonly string $revokedAt,
        public readonly string $revokedByPersonIdentityId,
        public readonly string $revokedReason,
    ) {
    }
}
