<?php

/**
 * @file database/migrations/2026_07_15_120008_create_jwt_payloads_table.php
 *
 * @description
 * Create the `jwt_payloads` table for the `JwtPayload` entity.
 * Owned by the auth module. Every column + index reference
 * goes through {@see Academorix\Auth\Contracts\Data\JwtPayloadInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

use Academorix\Auth\Contracts\Data\JwtPayloadInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(JwtPayloadInterface::TABLE, function (Blueprint $table): void {
            $table->string(JwtPayloadInterface::ATTR_ISS);
            $table->string(JwtPayloadInterface::ATTR_AUD);
            $table->string(JwtPayloadInterface::ATTR_SUB);
            $table->string(JwtPayloadInterface::ATTR_APP);
            $table->string(JwtPayloadInterface::ATTR_TID)->nullable();
            $table->string(JwtPayloadInterface::ATTR_SCO)->nullable();
            $table->string(JwtPayloadInterface::ATTR_ROLES)->nullable();
            $table->string(JwtPayloadInterface::ATTR_PERMISSIONS)->nullable();
            $table->string(JwtPayloadInterface::ATTR_KID);
            $table->string(JwtPayloadInterface::ATTR_IAT);
            $table->string(JwtPayloadInterface::ATTR_EXP);
            $table->string(JwtPayloadInterface::ATTR_JTI);
            $table->string(JwtPayloadInterface::ATTR_PURPOSE);

        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(JwtPayloadInterface::TABLE);
    }
};
