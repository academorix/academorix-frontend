<?php

/**
 * @file database/migrations/2026_07_15_120003_create_auth_jwt_signing_keies_table.php
 *
 * @description
 * Create the `auth_jwt_signing_keies` table for the `AuthJwtSigningKey` entity.
 * Owned by the auth module. Emitted from the module blueprint —
 * regenerate via `generate-module.py identity auth --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('auth_jwt_signing_keies', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('kid');
            $table->string('application_id', 30)->nullable();
            $table->string('algorithm')->default('HS256');
            $table->text('secret_encrypted');
            $table->boolean('is_active')->default(false);
            $table->timestampTz('issued_at')->default('now()');
            $table->timestampTz('rotated_at')->nullable();
            $table->timestampTz('retired_at')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['kid'], 'auth_jwt_signing_keys_kid_unique');
            $table->unique(['application_id'], 'auth_jwt_signing_keys_one_active_per_app');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_active = true AND retired_at IS NULL
            $table->index(['retired_at'], 'auth_jwt_signing_keys_retired_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE retired_at IS NOT NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('auth_jwt_signing_keies');
    }
};
