<?php

/**
 * @file database/migrations/2026_07_15_120006_create_auth_refresh_tokens_table.php
 *
 * @description
 * Create the `auth_refresh_tokens` table for the `AuthRefreshToken` entity.
 * Owned by the auth module. Emitted from the module blueprint —
 * regenerate via `generate-module.py identity auth --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('auth_refresh_tokens', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tokenable_type');
            $table->string('tokenable_id', 30);
            $table->text('token_hash');
            $table->uuid('family_id');
            $table->unsignedBigInteger('personal_access_token_id');
            $table->timestampTz('issued_at')->default('now()');
            $table->timestampTz('expires_at');
            $table->timestampTz('consumed_at')->nullable();
            $table->timestampTz('revoked_at')->nullable();
            $table->text('ip_address_hash')->nullable();
            $table->text('user_agent_hash')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['token_hash'], 'auth_refresh_tokens_hash_unique');
            $table->index(['family_id'], 'auth_refresh_tokens_family_idx');
            $table->index(['tokenable_type', 'tokenable_id'], 'auth_refresh_tokens_tokenable_idx');
            $table->index(['expires_at'], 'auth_refresh_tokens_active_expiry_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE consumed_at IS NULL AND revoked_at IS NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('auth_refresh_tokens');
    }
};
