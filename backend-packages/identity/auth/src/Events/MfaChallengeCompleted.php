<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MFA challenge verified successfully. Challenge consumed atomically → Sanctum PAT issued.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitMfaChallengeCompletedMetric
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class MfaChallengeCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $challengeId  ULID
     * @param  string  $challengeType  'totp' | 'webauthn' | 'recovery_code'
     * @param  int  $attemptsTaken  integer (1–3)
     * @param  string  $completedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $challengeId,
        public readonly string $challengeType,
        public readonly int $attemptsTaken,
        public readonly string $completedAt,
    ) {
    }
}
