<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Auth\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Refresh token exchanged for a new PAT + new refresh token. Old refresh consumed + new one issued inside the family_id chain.
 *
 * ## Consumers
 *
 * - observability::EmitRefreshRotationMetric
 *
 * @category Auth
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RefreshTokenRotated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $familyId  ULID
     * @param  string  $oldRefreshId  ULID
     * @param  string  $newRefreshId  ULID
     * @param  int  $newTokenId  integer
     * @param  string  $rotatedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $familyId,
        public readonly string $oldRefreshId,
        public readonly string $newRefreshId,
        public readonly int $newTokenId,
        public readonly string $rotatedAt,
    ) {
    }
}
