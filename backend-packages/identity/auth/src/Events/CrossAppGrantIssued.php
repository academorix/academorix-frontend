<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /auth/cross-app/grant succeeded — short-lived (300s) grant token issued to the caller for exchange on the target Application.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitCrossAppGrantIssuedMetric
 * - analytics::TrackCrossAppSsoStart
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class CrossAppGrantIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $grantId  ULID
     * @param  string  $sourceApplicationId  ULID
     * @param  string  $targetApplicationId  ULID
     * @param  string  $issuedAt  ISO-8601 timestamp
     * @param  string  $expiresAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $grantId,
        public readonly string $sourceApplicationId,
        public readonly string $targetApplicationId,
        public readonly string $issuedAt,
        public readonly string $expiresAt,
    ) {
    }
}
