<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Login flow detected MFA is enrolled → dispatched MFA challenge (returns 202 with challenge_id). The pending-login context (application_id, tenant_id, requested_scope) is snapshotted into the challenge row's metadata.
 *
 * ## Consumers
 *
 * - observability::EmitMfaChallengeIssuedMetric
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class MfaChallengeIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $challengeId  ULID
     * @param  string  $challengeType  'totp' | 'webauthn' | 'recovery_code'
     * @param  string  $expiresAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $challengeId,
        public readonly string $challengeType,
        public readonly string $expiresAt,
    ) {
    }
}
