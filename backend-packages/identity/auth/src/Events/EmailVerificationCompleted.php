<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * EmailVerification token exchanged for identity.email_verified_at = now(). Token consumed atomically. Fires alongside identity::IdentityEmailVerified — auth OWNS the endpoint, identity OWNS the substrate mutation.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class EmailVerificationCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $verificationTokenId  ULID
     * @param  string  $completedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $verificationTokenId,
        public readonly string $completedAt,
    ) {
    }
}
