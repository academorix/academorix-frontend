<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New EmailVerification token issued (registration OR email-change flow OR explicit resend). Debounced 60s per identity_id.
 *
 * ## Consumers
 *
 * - notifications::DispatchEmailVerificationLinkNotification
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class EmailVerificationRequested implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $verificationTokenId  ULID
     * @param  string  $tokenExpiresAt  ISO-8601 timestamp
     * @param  string  $reason  'registration' | 'email_change' | 'resend'
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $verificationTokenId,
        public readonly string $tokenExpiresAt,
        public readonly string $reason,
    ) {
    }
}
