<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /auth/password/forgot passed rate-limit + resolved a real Identity. NEVER fired when email is unknown (enumeration safety). This is the AUTH surface's version of identity::PasswordResetRequested — fires alongside identity's event, since auth OWNS the endpoint.
 *
 * ## Consumers
 *
 * - notifications::DispatchPasswordResetLinkNotification
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class PasswordResetInitiated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $resetTokenId  ULID
     * @param  string  $tokenExpiresAt  ISO-8601 timestamp
     * @param  string  $requestIpHash  string
     * @param  string  $userAgentHash  string
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $resetTokenId,
        public readonly string $tokenExpiresAt,
        public readonly string $requestIpHash,
        public readonly string $userAgentHash,
    ) {
    }
}
