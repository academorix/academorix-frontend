<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Successful password + optional MFA verification → Sanctum PAT issued. Fires exactly once per interactive login (never on refresh, never on cross-app grant exchange).
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitLoginSuccessMetric
 * - analytics::TrackLogin
 * - auth::MaybeDispatchNewDeviceNotification
 * - auth::UpdateUserLastLoginAt
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class UserLoggedIn implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID (`usr_...` OR `plu_...`)
     * @param  string  $identityId  ULID
     * @param  string  $guard  'sanctum' | 'platform_admin'
     * @param  string  $applicationId  ULID | null (null for platform_admin)
     * @param  string  $tenantId  ULID | null (null for platform_admin)
     * @param  int  $tokenId  integer (Sanctum PAT id)
     * @param  bool  $mfaVerified  boolean
     * @param  string  $deviceFingerprint  string (SHA-256; empty when detection is off)
     * @param  string  $loggedInAt  ISO-8601 timestamp
     * @param  int  $requestIpHash  string (SHA-256 of IP; plaintext never emitted)
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $identityId,
        public readonly string $guard,
        public readonly string $applicationId,
        public readonly string $tenantId,
        public readonly int $tokenId,
        public readonly bool $mfaVerified,
        public readonly string $deviceFingerprint,
        public readonly string $loggedInAt,
        public readonly int $requestIpHash,
    ) {
    }
}
