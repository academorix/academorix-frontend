<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /auth/cross-app/exchange consumed a valid grant → target-Application PAT issued. Grant marked consumed atomically.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitCrossAppGrantExchangedMetric
 * - analytics::TrackCrossAppSsoComplete
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class CrossAppGrantExchanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $grantId  ULID
     * @param  string  $targetUserId  ULID (the User row resolved on the target Application)
     * @param  string  $targetApplicationId  ULID
     * @param  int  $newTokenId  integer
     * @param  string  $exchangedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $grantId,
        public readonly string $targetUserId,
        public readonly string $targetApplicationId,
        public readonly int $newTokenId,
        public readonly string $exchangedAt,
    ) {
    }
}
