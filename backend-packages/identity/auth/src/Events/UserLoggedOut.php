<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /auth/logout OR platform equivalent. Sanctum PAT revoked + paired refresh token consumed in the same transaction.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitLogoutMetric
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class UserLoggedOut implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $userId  ULID
     * @param  string  $identityId  ULID
     * @param  string  $guard  'sanctum' | 'platform_admin'
     * @param  int  $tokenId  integer
     * @param  string  $trigger  'self_service' | 'idle_timeout' | 'admin_revocation'
     * @param  string  $loggedOutAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $userId,
        public readonly string $identityId,
        public readonly string $guard,
        public readonly int $tokenId,
        public readonly string $trigger,
        public readonly string $loggedOutAt,
    ) {
    }
}
