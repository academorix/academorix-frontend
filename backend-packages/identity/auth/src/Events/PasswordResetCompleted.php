<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Auth\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PasswordReset token exchanged for a new password. Token consumed atomically + all sessions revoked in the same transaction.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitPasswordResetCompletedMetric
 *
 * @category Auth
 *
 * @since    0.1.0
 */
final readonly class PasswordResetCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $resetTokenId  ULID
     * @param  string  $completedAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $resetTokenId,
        public readonly string $completedAt,
    ) {
    }
}
