<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Auth\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `purpose` on `jwt-payload`.
 *
 * Distinguishes the JWT's issuance context.
 *
 * @category Auth
 *
 * @since    0.1.0
 */
enum JwtPayloadPurpose: string
{
    use Enum;

    case UserSession = 'user_session';
    case ServiceAccount = 'service_account';
    case Impersonation = 'impersonation';
}
