<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity auth --force`.

declare(strict_types=1);

namespace Academorix\Auth\Services;

use Academorix\Auth\Contracts\Services\JwtVerifierInterface;
use Illuminate\Container\Attributes\Scoped;

/**
 * Concrete service — JwtVerifier.
 *
 * Implements {@see JwtVerifierInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If the
 * service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Auth
 *
 * @since    0.1.0
 */
#[Scoped]
final class JwtVerifier implements JwtVerifierInterface
{
    // TODO(gen): implement the interface methods.
}
