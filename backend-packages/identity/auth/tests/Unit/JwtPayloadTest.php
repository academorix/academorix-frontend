<?php

declare(strict_types=1);

use Academorix\Auth\Models\JwtPayload;

it('is a placeholder — replace with actual JwtPayload unit tests', function (): void {
    expect(JwtPayload::class)->toBeString();
});
