<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity mfa --force`.

declare(strict_types=1);

namespace Academorix\Mfa\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /api/v1/me/mfa/webauthn/register succeeded — attestation verified, credential row inserted into webauthn_credentials.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitWebauthnCredentialRegisteredMetric
 * - analytics::TrackWebauthnCredentialRegistered
 *
 * @category Mfa
 *
 * @since    0.1.0
 */
final readonly class WebauthnCredentialRegistered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $credentialId  ULID (`wac_...`)
     * @param  string  $deviceName  string (60 chars max)
     * @param  string  $attestationFormat  'packed' | 'fido-u2f' | 'android-key' | 'apple' | 'none' | 'tpm'
     * @param  int  $transports  string[] (subset of usb / nfc / ble / internal / hybrid)
     * @param  bool  $isPlatformAuthenticator  boolean
     * @param  bool  $isSyncedPasskey  boolean
     * @param  string  $registeredAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $credentialId,
        public readonly string $deviceName,
        public readonly string $attestationFormat,
        public readonly int $transports,
        public readonly bool $isPlatformAuthenticator,
        public readonly bool $isSyncedPasskey,
        public readonly string $registeredAt,
    ) {
    }
}
