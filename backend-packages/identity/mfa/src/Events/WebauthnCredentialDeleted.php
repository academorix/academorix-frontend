<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity mfa --force`.

declare(strict_types=1);

namespace Academorix\Mfa\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * DELETE /api/v1/me/mfa/webauthn/credentials/{id} OR the 90-day retention window elapsed on a disabled credential (via CleanupDisabledWebauthnCredentialsJob).
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitWebauthnCredentialDeletedMetric
 *
 * @category Mfa
 *
 * @since    0.1.0
 */
final readonly class WebauthnCredentialDeleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $credentialId  ULID
     * @param  string  $deviceName  string
     * @param  string  $deletedAt  ISO-8601 timestamp
     * @param  string  $actorId  ULID | null (null = automatic retention purge)
     * @param  string  $trigger  'user_action' | 'retention_purge' | 'admin_action' | 'identity_erasure_cascade'
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $credentialId,
        public readonly string $deviceName,
        public readonly string $deletedAt,
        public readonly string $actorId,
        public readonly string $trigger,
    ) {
    }
}
