<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity mfa --force`.

declare(strict_types=1);

namespace Academorix\Mfa\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /api/v1/me/mfa/totp/enroll/preview returned a fresh TOTP secret to the caller. The secret is held ephemerally in Redis under a signed envelope; the caller must confirm (round 2) within 5 minutes.
 *
 * ## Consumers
 *
 * - observability::EmitMfaEnrollmentPreviewMetric
 * - analytics::TrackMfaEnrollmentStarted
 *
 * @category Mfa
 *
 * @since    0.1.0
 */
final readonly class MfaEnrollmentPreviewIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $previewId  string (ephemeral ID of the Redis envelope — used to correlate with the confirm event; NEVER the raw secret)
     * @param  string  $issuedAt  ISO-8601 timestamp
     * @param  string  $expiresAt  ISO-8601 timestamp
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $previewId,
        public readonly string $issuedAt,
        public readonly string $expiresAt,
    ) {
    }
}
