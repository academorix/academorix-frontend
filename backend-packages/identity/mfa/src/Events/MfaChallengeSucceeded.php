<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity mfa --force`.

declare(strict_types=1);

namespace Academorix\Mfa\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /api/v1/me/mfa/challenge/{challenge_id}/verify succeeded. Challenge row consumed atomically; Sanctum PAT issued by identity/auth in the same transaction as the challenge consumption.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - observability::EmitMfaChallengeSucceededMetric
 * - analytics::TrackMfaChallengeSucceeded
 *
 * @category Mfa
 *
 * @since    0.1.0
 */
final readonly class MfaChallengeSucceeded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID
     * @param  string  $challengeId  ULID
     * @param  string  $method  'totp' | 'webauthn' | 'recovery_code'
     * @param  string  $credentialId  ULID | null (WebauthnCredential.id when method=webauthn; null otherwise)
     * @param  string  $succeededAt  ISO-8601 timestamp
     * @param  int  $durationMs  integer (issued_at → verified — signals user friction if too high, or automation if too low)
     */
    public function __construct(
        public readonly string $identityId,
        public readonly string $challengeId,
        public readonly string $method,
        public readonly string $credentialId,
        public readonly string $succeededAt,
        public readonly int $durationMs,
    ) {
    }
}
