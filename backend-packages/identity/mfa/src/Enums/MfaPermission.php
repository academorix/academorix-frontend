<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity mfa --force`.

declare(strict_types=1);

namespace Academorix\Mfa\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Mfa module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Mfa
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum MfaPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `mfa.viewOwn` — View own MFA posture.
     */
    #[Label('View own MFA posture')]
    #[Description('View own MFA posture. Baseline permission required to render the /me/mfa panel.')]
    case MfaViewOwn = 'mfa.viewOwn';

    /**
     * `mfa.enrollTotp` — Enrol a TOTP secret on own Identity.
     */
    #[Label('Enrol a TOTP secret on own Identity')]
    #[Description('Enrol a TOTP secret on own Identity.')]
    case MfaEnrollTotp = 'mfa.enrollTotp';

    /**
     * `mfa.disableTotp` — Disable the TOTP factor on own Identity.
     */
    #[Label('Disable the TOTP factor on own Identity')]
    #[Description('Disable the TOTP factor on own Identity. Gated by `mfa.require_recent_challenge` middleware.')]
    case MfaDisableTotp = 'mfa.disableTotp';

    /**
     * `mfa.registerWebauthn` — Register a new WebAuthn credential on own Identity.
     */
    #[Label('Register a new WebAuthn credential on own Identity')]
    #[Description('Register a new WebAuthn credential on own Identity.')]
    case MfaRegisterWebauthn = 'mfa.registerWebauthn';

    /**
     * `mfa.manageWebauthn` — Umbrella permission covering the caller's own WebAuthn credential CRUD (rename / disable / delete).
     */
    #[Label('Umbrella permission covering the caller\'s own WebAuthn credential CRUD (rename /')]
    #[Description('Umbrella permission covering the caller\'s own WebAuthn credential CRUD (rename / disable / delete). Actual enforcement is delegated per-credential via WebauthnCredentialPolicy (which reads `mfa.webauthn_credentials.update` / `.delete`). Seeded alongside so a role that carries this umbrella permission automatically inherits every child.')]
    case MfaManageWebauthn = 'mfa.manageWebauthn';

    /**
     * `mfa.webauthn_credentials.viewAny` — List own registered WebAuthn credentials.
     */
    #[Label('List own registered WebAuthn credentials')]
    #[Description('List own registered WebAuthn credentials.')]
    case MfaWebauthnCredentialsViewAny = 'mfa.webauthn_credentials.viewAny';

    /**
     * `mfa.webauthn_credentials.view` — View one of own registered WebAuthn credentials.
     */
    #[Label('View one of own registered WebAuthn credentials')]
    #[Description('View one of own registered WebAuthn credentials.')]
    case MfaWebauthnCredentialsView = 'mfa.webauthn_credentials.view';

    /**
     * `mfa.webauthn_credentials.update` — Rename or disable one of own registered WebAuthn credentials.
     */
    #[Label('Rename or disable one of own registered WebAuthn credentials')]
    #[Description('Rename or disable one of own registered WebAuthn credentials. Gated by `mfa.require_recent_challenge`.')]
    case MfaWebauthnCredentialsUpdate = 'mfa.webauthn_credentials.update';

    /**
     * `mfa.webauthn_credentials.delete` — Hard-delete one of own registered WebAuthn credentials.
     */
    #[Label('Hard-delete one of own registered WebAuthn credentials')]
    #[Description('Hard-delete one of own registered WebAuthn credentials. Gated by `mfa.require_recent_challenge`.')]
    case MfaWebauthnCredentialsDelete = 'mfa.webauthn_credentials.delete';

    /**
     * `mfa.regenerateRecoveryCodes` — Regenerate own recovery-code set.
     */
    #[Label('Regenerate own recovery-code set')]
    #[Description('Regenerate own recovery-code set. Gated by `mfa.require_recent_challenge`.')]
    case MfaRegenerateRecoveryCodes = 'mfa.regenerateRecoveryCodes';

    /**
     * `mfa.downloadRecoveryCodes` — Re-download current recovery-code plaintext.
     */
    #[Label('Re-download current recovery-code plaintext')]
    #[Description('Re-download current recovery-code plaintext. Audit-logged as data-egress. Gated by `mfa.require_recent_challenge`.')]
    case MfaDownloadRecoveryCodes = 'mfa.downloadRecoveryCodes';

    /**
     * `mfa.policy.manage` — Manage tenant-level MFA policy settings (required_for_roles, grace_days, step-up TTL, WebAuthn policy).
     */
    #[Label('Manage tenant-level MFA policy settings (required_for_roles, grace_days, step-up')]
    #[Description('Manage tenant-level MFA policy settings (required_for_roles, grace_days, step-up TTL, WebAuthn policy). Admin + owner only.')]
    case MfaPolicyManage = 'mfa.policy.manage';

    /**
     * `platform.mfa.methods.view` — View a user's MFA posture across every Application via the shared Identity.
     */
    #[Label('View a user\'s MFA posture across every Application via the shared Identity')]
    #[Description('View a user\'s MFA posture across every Application via the shared Identity. Redacted response.')]
    case PlatformMfaMethodsView = 'platform.mfa.methods.view';

    /**
     * `platform.mfa.force_disable` — Emergency force-disable MFA for a user.
     */
    #[Label('Emergency force-disable MFA for a user')]
    #[Description('Emergency force-disable MFA for a user. Audit-critical. Requires justification.')]
    case PlatformMfaForceDisable = 'platform.mfa.force_disable';

    /**
     * `platform.mfa.regenerate_recovery_codes` — Regenerate recovery codes on behalf of a user + email them.
     */
    #[Label('Regenerate recovery codes on behalf of a user + email them')]
    #[Description('Regenerate recovery codes on behalf of a user + email them. Audit-warn.')]
    case PlatformMfaRegenerateRecoveryCodes = 'platform.mfa.regenerate_recovery_codes';

    /**
     * `platform.mfa.webauthn_credentials.viewAny` — List a user's registered WebAuthn credentials for audit or incident-response.
     */
    #[Label('List a user\'s registered WebAuthn credentials for audit or incident-response')]
    #[Description('List a user\'s registered WebAuthn credentials for audit or incident-response.')]
    case PlatformMfaWebauthnCredentialsViewAny = 'platform.mfa.webauthn_credentials.viewAny';

    /**
     * `platform.mfa.webauthn_credentials.disable` — Disable one of a user's WebAuthn credentials during an incident (reported stolen laptop).
     */
    #[Label('Disable one of a user\'s WebAuthn credentials during an incident (reported stolen')]
    #[Description('Disable one of a user\'s WebAuthn credentials during an incident (reported stolen laptop). Audit-critical.')]
    case PlatformMfaWebauthnCredentialsDisable = 'platform.mfa.webauthn_credentials.disable';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::MfaViewOwn => Guard::Sanctum,
            self::MfaEnrollTotp => Guard::Sanctum,
            self::MfaDisableTotp => Guard::Sanctum,
            self::MfaRegisterWebauthn => Guard::Sanctum,
            self::MfaManageWebauthn => Guard::Sanctum,
            self::MfaWebauthnCredentialsViewAny => Guard::Sanctum,
            self::MfaWebauthnCredentialsView => Guard::Sanctum,
            self::MfaWebauthnCredentialsUpdate => Guard::Sanctum,
            self::MfaWebauthnCredentialsDelete => Guard::Sanctum,
            self::MfaRegenerateRecoveryCodes => Guard::Sanctum,
            self::MfaDownloadRecoveryCodes => Guard::Sanctum,
            self::MfaPolicyManage => Guard::Sanctum,
            self::PlatformMfaMethodsView => Guard::PlatformAdmin,
            self::PlatformMfaForceDisable => Guard::PlatformAdmin,
            self::PlatformMfaRegenerateRecoveryCodes => Guard::PlatformAdmin,
            self::PlatformMfaWebauthnCredentialsViewAny => Guard::PlatformAdmin,
            self::PlatformMfaWebauthnCredentialsDisable => Guard::PlatformAdmin,
        };
    }
}
