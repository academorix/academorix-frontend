<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity mfa --force`.

declare(strict_types=1);

namespace Academorix\Mfa\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `method` on `mfa-challenge`.
 *
 * Enum: totp / webauthn / recovery_code — dispatches to the right challenger at verify time.
 *
 * @category Mfa
 *
 * @since    0.1.0
 */
enum MfaChallengeMethod: string
{
    use Enum;

    case Totp = 'totp';
    case Webauthn = 'webauthn';
    case RecoveryCode = 'recovery_code';
}
