<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py identity mfa --force`.

declare(strict_types=1);

namespace Academorix\Mfa\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * MfaSettings — Two-factor authentication.
 *
 * Tenant-wide MFA policy.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Mfa
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'mfa',
    label: 'Two-factor authentication',
    description: 'Tenant-wide MFA policy. Compliance floor: the tenant can only TIGHTEN these values, never loosen below the platform floor.',
    icon: 'shield-check',
    scope: 'tenant',
    permission: 'mfa.policy.manage',
    sortOrder: 20,
)]
class MfaSettings extends Settings
{

    // ── Required roles ──────────────────────────────────
    #[SettingGroup(label: 'Required roles', description: 'Users holding any listed role must have MFA enrolled. Grace period gives newly-elevated users time to enrol before enforcement.', icon: 'user-group', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Multiselect, label: 'Additional roles requiring MFA', group: 'Required roles', sortOrder: 1, helpText: 'Platform floor: owner, admin, billing_manager, super_admin (always required). Add more roles here to widen enforcement.')]
    public ?array $additional_required_roles = null;

    #[SettingField(controlType: ControlType::Number, label: 'Grace period (days)', group: 'Required roles', sortOrder: 2, min: 0, max: 7, helpText: 'How long a newly-elevated user has to enrol MFA before enforcement. Platform max: 7 days. Set to 0 for immediate enforcement.')]
    public int $grace_days_after_enrollment = 7;


    // ── Step-up verification ──────────────────────────────────
    #[SettingGroup(label: 'Step-up verification', description: 'Require a fresh MFA challenge before sensitive actions.', icon: 'lock-closed', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enable step-up verification', group: 'Step-up verification', sortOrder: 1)]
    public bool $step_up_enabled = false;

    #[SettingField(controlType: ControlType::Select, label: 'Freshness window', group: 'Step-up verification', sortOrder: 2, options: ['1' => '1 minute (most strict)', '5' => '5 minutes (recommended)', '15' => '15 minutes', '30' => '30 minutes'], helpText: 'How long after an MFA challenge a sensitive action is allowed without re-challenging.')]
    public int $step_up_ttl_minutes = 5;


    // ── WebAuthn policy ──────────────────────────────────
    #[SettingGroup(label: 'WebAuthn policy', description: 'Fine-tune the WebAuthn behaviour for your tenant.', icon: 'key', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Select, label: 'User-verification policy', group: 'WebAuthn policy', sortOrder: 1, options: ['required' => 'Required (single-step MFA — passkey-first)', 'preferred' => 'Preferred (recommended)', 'discouraged' => 'Discouraged (classic one-tap)'], helpText: 'Whether the authenticator MUST verify the user (PIN, biometric). Required makes WebAuthn a full MFA on its own.')]
    public string $webauthn_user_verification = 'preferred';

    #[SettingField(controlType: ControlType::Toggle, label: 'Require passkey-style credentials (resident keys)', group: 'WebAuthn policy', sortOrder: 2, helpText: 'On = only passkeys accepted (no classic YubiKeys). Off = accept both roaming keys and passkeys.')]
    public bool $webauthn_require_resident_key = false;

    #[SettingField(controlType: ControlType::Number, label: 'Maximum credentials per user', group: 'WebAuthn policy', sortOrder: 3, min: 1, max: 10, helpText: 'Platform max: 10. Free tier caps at 1 (upgrade to team+ for multiple).')]
    public int $webauthn_max_credentials = 10;


    // ── Recovery codes ──────────────────────────────────
    #[SettingGroup(label: 'Recovery codes', icon: 'key', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Number, label: 'Low-code alert threshold', group: 'Recovery codes', sortOrder: 1, min: 1, max: 10, helpText: 'Send a reminder when a user\'s remaining recovery-code count falls below this value.')]
    public int $recovery_codes_low_water_threshold = 3;


    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', icon: 'bell-alert', sortOrder: 5)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Send suspicious-activity alerts', group: 'Notifications', sortOrder: 1, helpText: 'Emails users when their MFA sees rapid failed challenges from novel IPs.')]
    public bool $notify_suspicious_activity = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Send low-recovery-codes reminders', group: 'Notifications', sortOrder: 2)]
    public bool $notify_low_recovery_codes = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'mfa';
    }
}
