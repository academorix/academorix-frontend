<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared versioning --force`.

declare(strict_types=1);

namespace Academorix\Versioning\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Versioning\Contracts\Data\ApiVersionInterface;
use Academorix\Versioning\Database\Factories\ApiVersionFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Laravel\Scout\Searchable;
use Mattiverse\Userstamps\Traits\Userstamps;

/**
 * Eloquent model for a ApiVersion.
 *
 * One row per registered API version.
 *
 * @category Versioning
 *
 * @since    0.1.0
 */
#[Table(name: ApiVersionInterface::TABLE, keyType: ApiVersionInterface::KEY_TYPE)]
#[Fillable([
    ApiVersionInterface::ATTR_SLUG,
        ApiVersionInterface::ATTR_NAME,
        ApiVersionInterface::ATTR_DESCRIPTION,
        ApiVersionInterface::ATTR_SCHEME,
        ApiVersionInterface::ATTR_STATE,
        ApiVersionInterface::ATTR_RELEASED_AT,
        ApiVersionInterface::ATTR_DEPRECATED_AT,
        ApiVersionInterface::ATTR_SUNSET_AT,
        ApiVersionInterface::ATTR_SUNSET_NOTICE_URL,
        ApiVersionInterface::ATTR_MIGRATION_TARGET_SLUG,
        ApiVersionInterface::ATTR_IS_DEFAULT,
        ApiVersionInterface::ATTR_IS_SYSTEM,
        ApiVersionInterface::ATTR_SORT_ORDER,
        ApiVersionInterface::ATTR_METADATA,
])]
#[UseFactory(ApiVersionFactory::class)]
final class ApiVersion extends Model implements ApiVersionInterface
{
    use HasFactory;
    use HasUlids;
    // TODO(gen): resolve unknown trait `HasStates` — add its import + use line.
    // TODO(gen): resolve unknown trait `HasSystemFlag` — add its import + use line.
    use HasMetadata;
    use HasUserstamps;
    // TODO(gen): resolve unknown trait `HasAuditable` — add its import + use line.
    // TODO(gen): resolve unknown trait `HasVersions` — add its import + use line.
    // TODO(gen): resolve unknown trait `Sortable` — add its import + use line.
    use Filterable;
    // TODO(gen): resolve unknown trait `Sluggable` — add its import + use line.
    use Searchable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        ApiVersionInterface::ATTR_STATE => 'ApiVersionState',
        ApiVersionInterface::ATTR_SCHEME => 'VersionScheme',
        ApiVersionInterface::ATTR_IS_DEFAULT => 'boolean',
        ApiVersionInterface::ATTR_IS_SYSTEM => 'boolean',
        ApiVersionInterface::ATTR_RELEASED_AT => 'datetime',
        ApiVersionInterface::ATTR_DEPRECATED_AT => 'datetime',
        ApiVersionInterface::ATTR_SUNSET_AT => 'datetime',
        ApiVersionInterface::ATTR_METADATA => 'array',
    ];
}
