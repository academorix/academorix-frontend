<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared versioning --force`.

declare(strict_types=1);

namespace Academorix\Versioning\Actions\Platform;

use Academorix\Versioning\Contracts\Repositories\DeprecationNoticeRepositoryInterface;
use Academorix\Versioning\Data\DeprecationNoticeData;
use Academorix\Versioning\Data\Requests\CreateDeprecationNoticeRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/platform/versioning/deprecation-notices` — create action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Post(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Versioning
 *
 * @since    0.1.0
 */
#[AsController]
#[Post('/api/v1/platform/versioning/deprecation-notices')]
final class CreateDeprecationNoticeAction
{
    public function __construct(
        private readonly DeprecationNoticeRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `deprecation-notice` from the validated request payload.
     *
     * @param  CreateDeprecationNoticeRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateDeprecationNoticeRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(DeprecationNoticeData::from($model), JsonResponse::HTTP_CREATED);
    }
}
