<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared versioning --force`.

declare(strict_types=1);

namespace Academorix\Versioning\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ApiVersion state transitions released → deprecated. Fires once per version.
 *
 * ## Consumers
 *
 * - webhook::TrackVersionDeprecatedForSubscriptions
 * - notifications::PublishDeprecationNoticesListener
 * - compliance::AuditApiVersionDeprecatedListener
 *
 * @category Versioning
 *
 * @since    0.1.0
 */
final readonly class ApiVersionDeprecated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $apiVersionId  ULID
     * @param  string  $slug  string
     * @param  string  $deprecatedAt  ISO 8601
     * @param  string  $sunsetAt  ISO 8601 | null
     * @param  string  $migrationTargetSlug  string | null
     * @param  string  $migrationGuideUrl  string | null
     * @param  int  $affectedSubscriptionCount  integer — how many WebhookSubscriptions reference this slug at deprecation time
     * @param  string  $actorId  ULID | null
     */
    public function __construct(
        public readonly string $apiVersionId,
        public readonly string $slug,
        public readonly string $deprecatedAt,
        public readonly string $sunsetAt,
        public readonly string $migrationTargetSlug,
        public readonly string $migrationGuideUrl,
        public readonly int $affectedSubscriptionCount,
        public readonly string $actorId,
    ) {
    }
}
