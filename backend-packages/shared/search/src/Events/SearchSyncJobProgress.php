<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared search --force`.

declare(strict_types=1);

namespace Academorix\Search\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Fires every `config('search.progress.every_rows')` rows during a chunk. NOT audit-worthy — broadcast only.
 *
 * ## Consumers
 *
 * - SPA progress bar
 *
 * @category Search
 *
 * @since    0.1.0
 */
final readonly class SearchSyncJobProgress implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $searchSyncJobId  ULID
     * @param  int  $progressPercent  integer 0–100
     * @param  string  $counters  { total, indexed, updated, skipped, failed, deleted }
     */
    public function __construct(
        public readonly string $searchSyncJobId,
        public readonly int $progressPercent,
        public readonly string $counters,
    ) {
    }
}
