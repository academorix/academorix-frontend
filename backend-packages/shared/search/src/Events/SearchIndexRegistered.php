<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared search --force`.

declare(strict_types=1);

namespace Academorix\Search\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New SearchIndex row inserted. Fires from SearchIndexObserver.created (afterCommit).
 *
 * ## Consumers
 *
 * - activity::HasActivityLog observer on SearchIndex → activity_log row
 * - audit::HasAudit observer on SearchIndex → audits row
 * - observability::MetricsCollector — index_count gauge bump
 *
 * @category Search
 *
 * @since    0.1.0
 */
final readonly class SearchIndexRegistered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $searchIndexId  ULID (sidx_)
     * @param  string  $tenantId  ULID (ten_) | null
     * @param  string  $modelClass  string
     * @param  string  $engine  'meilisearch' | 'postgres-fts' | 'pgvector' | 'elasticsearch'
     * @param  string  $indexName  string
     * @param  string  $language  string | null
     */
    public function __construct(
        public readonly string $searchIndexId,
        public readonly string $tenantId,
        public readonly string $modelClass,
        public readonly string $engine,
        public readonly string $indexName,
        public readonly string $language,
    ) {
    }
}
