<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared search --force`.

declare(strict_types=1);

namespace Academorix\Search\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SwapIndexAliasJob successfully repointed the live alias. Fires afterCommit on the SearchIndex.current_version + live_alias update.
 *
 * ## Consumers
 *
 * - activity::HasActivityLog
 * - audit::HasAudit
 * - PurgeIndexModelJob dispatched with swap_grace_days delay for the old version
 *
 * @category Search
 *
 * @since    0.1.0
 */
final readonly class SearchIndexAliasSwapped implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $searchIndexId  ULID
     * @param  int  $fromVersion  integer
     * @param  int  $toVersion  integer
     * @param  string  $engine  string
     * @param  string  $indexName  string
     * @param  string  $swappedAt  ISO 8601
     */
    public function __construct(
        public readonly string $searchIndexId,
        public readonly int $fromVersion,
        public readonly int $toVersion,
        public readonly string $engine,
        public readonly string $indexName,
        public readonly string $swappedAt,
    ) {
    }
}
