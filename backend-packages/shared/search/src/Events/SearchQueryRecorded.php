<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared search --force`.

declare(strict_types=1);

namespace Academorix\Search\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * One kind=query analytics event was persisted. Fires from RecordSearchAnalyticsEventJob (fire-and-forget — not afterCommit).
 *
 * ## Consumers
 *
 * - observability::MetricsCollector — query counter increment
 *
 * @category Search
 *
 * @since    0.1.0
 */
final readonly class SearchQueryRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $searchAnalyticsEventId  ULID (sae_)
     * @param  string  $tenantId  ULID | null
     * @param  string  $engine  string
     * @param  array  $indexNames  array<string>
     * @param  int  $resultCount  integer
     * @param  int  $tookMs  integer
     */
    public function __construct(
        public readonly string $searchAnalyticsEventId,
        public readonly string $tenantId,
        public readonly string $engine,
        public readonly array $indexNames,
        public readonly int $resultCount,
        public readonly int $tookMs,
    ) {
    }
}
