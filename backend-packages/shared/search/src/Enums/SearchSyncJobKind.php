<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared search --force`.

declare(strict_types=1);

namespace Academorix\Search\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `kind` on `search-sync-job`.
 *
 * Enum: reindex / backfill / flush / alias_swap / single_document.
 *
 * @category Search
 *
 * @since    0.1.0
 */
enum SearchSyncJobKind: string
{
    use Enum;

    case Reindex = 'reindex';
    case Backfill = 'backfill';
    case Flush = 'flush';
    case AliasSwap = 'alias_swap';
    case SingleDocument = 'single_document';
}
