<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared search --force`.

declare(strict_types=1);

namespace Academorix\Search\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `engine` on `search-index`.
 *
 * Enum: meilisearch / postgres-fts / pgvector / elasticsearch.
 *
 * @category Search
 *
 * @since    0.1.0
 */
enum SearchIndexEngine: string
{
    use Enum;

    case Meilisearch = 'meilisearch';
    case PostgresFts = 'postgres-fts';
    case Pgvector = 'pgvector';
    case Elasticsearch = 'elasticsearch';
}
