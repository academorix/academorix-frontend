<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared search --force`.

declare(strict_types=1);

namespace Academorix\Search\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * UserSearchSettings — Search — my preferences.
 *
 * Per-user override of the tenant defaults.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Search
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'search_user',
    label: 'Search — my preferences',
    description: 'Per-user override of the tenant defaults.',
    icon: 'user-circle',
    scope: 'user',
    permission: 'settings.read',
    sortOrder: 25,
)]
class UserSearchSettings extends Settings
{

    // ── Query defaults ──────────────────────────────────
    #[SettingGroup(label: 'Query defaults', icon: 'adjustments-horizontal', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Results per page', validation: ['required', 'in:10,20,50,100'], group: 'Query defaults', sortOrder: 1, options: ['10' => '10', '20' => '20', '50' => '50', '100' => '100'])]
    public int $default_result_count = 20;

    #[SettingField(controlType: ControlType::Select, label: 'Preferred engine (override)', validation: ['nullable'], group: 'Query defaults', sortOrder: 2, options: ['meilisearch' => 'Meilisearch', 'postgres-fts' => 'Postgres FTS', 'pgvector' => 'pgvector'], helpText: 'Empty = follow tenant default. Only applies to models where more than one engine is available.')]
    public ?string $preferred_engine = null;


    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', icon: 'bell', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Text, label: 'How I want to be notified about my search sync jobs', validation: ['nullable', 'array', 'min:1'], group: 'Notifications', sortOrder: 1, options: ['mail' => 'Email', 'broadcast' => 'Real-time (in-app toast)', 'database' => 'In-app inbox', 'push' => 'Mobile push', 'sms' => 'SMS'], helpText: 'Empty = follow tenant default.')]
    public ?string $notify_channels = null;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'search_user';
    }
}
