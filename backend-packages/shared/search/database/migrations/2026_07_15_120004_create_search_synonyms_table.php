<?php

/**
 * @file database/migrations/2026_07_15_120004_create_search_synonyms_table.php
 *
 * @description
 * Create the `search_synonyms` table for the `SearchSynonym` entity.
 * Owned by the search module. Emitted from the module blueprint —
 * regenerate via `generate-module.py shared search --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared search --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('search_synonyms', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64)->nullable();
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('search_index_id', 64)->nullable();
            $table->foreign('search_index_id')->references('id')->on('search_indexes')->onDelete('cascade');
            $table->string('language', 16)->default('en');
            $table->string('kind', 16)->default('equivalent');
            $table->jsonb('terms');
            $table->string('one_way_source', 128)->nullable();
            $table->jsonb('one_way_targets')->nullable();
            $table->text('description')->nullable();
            $table->boolean('is_active')->default('true');
            $table->boolean('is_system')->default('false');
            $table->string('source', 32)->default('tenant_admin');
            $table->jsonb('metadata')->nullable();
            $table->string('created_by_type', 128)->nullable();
            $table->string('created_by_id', 64)->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->timestampTz('deleted_at')->nullable();
            $table->index(['tenant_id', 'language', 'is_active'], 'search_synonyms_tenant_lang_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['search_index_id'], 'search_synonyms_index_idx');
            $table->index(['is_system', 'language'], 'search_synonyms_system_scan_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_system = true
            $table->index(['tenant_id', 'created_at'], 'search_synonyms_tenant_time_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('search_synonyms');
    }
};
