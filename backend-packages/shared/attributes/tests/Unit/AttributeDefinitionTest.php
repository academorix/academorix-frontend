<?php

declare(strict_types=1);

use Academorix\Attributes\Models\AttributeDefinition;

it('is a placeholder — replace with actual AttributeDefinition unit tests', function (): void {
    expect(AttributeDefinition::class)->toBeString();
});
