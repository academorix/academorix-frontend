<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared attributes --force`.

declare(strict_types=1);

namespace Academorix\Attributes\Actions\Tenant;

use Academorix\Attributes\Contracts\Repositories\AttributeSetRepositoryInterface;
use Academorix\Attributes\Data\AttributeSetData;
use Academorix\Attributes\Data\Requests\CreateAttributeSetRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/attribute-sets` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Post(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Attributes
 *
 * @since    0.1.0
 */
#[AsController]
#[Post('/api/v1/attribute-sets')]
final class CreateAttributeSetAction
{
    public function __construct(
        private readonly AttributeSetRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `attribute-set` from the validated request payload.
     *
     * @param  CreateAttributeSetRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateAttributeSetRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(AttributeSetData::from($model), JsonResponse::HTTP_CREATED);
    }
}
