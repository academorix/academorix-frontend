<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared attributes --force`.

declare(strict_types=1);

namespace Academorix\Attributes\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `widget` on `attribute-definition`.
 *
 * Rendering hint: select / slider / number / date / input / switch / textarea / radio.
 *
 * @category Attributes
 *
 * @since    0.1.0
 */
enum AttributeDefinitionWidget: string
{
    use Enum;

    case Select = 'select';
    case Slider = 'slider';
    case Number = 'number';
    case Date = 'date';
    case Input = 'input';
    case Switch = 'switch';
    case Textarea = 'textarea';
    case Radio = 'radio';
}
