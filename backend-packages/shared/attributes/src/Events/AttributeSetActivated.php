<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared attributes --force`.

declare(strict_types=1);

namespace Academorix\Attributes\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AttributeSet.is_active flipped false → true. Refused when it would collide with another active set at the same (entity_type, discriminator_value).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateHostResolverCache
 *
 * @category Attributes
 *
 * @since    0.1.0
 */
final readonly class AttributeSetActivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $setId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $entityType  string
     * @param  string  $discriminatorValue  string
     * @param  string  $activatedAt  ISO 8601
     * @param  string  $activatedByUserId  ULID
     */
    public function __construct(
        public readonly string $setId,
        public readonly string $tenantId,
        public readonly string $entityType,
        public readonly string $discriminatorValue,
        public readonly string $activatedAt,
        public readonly string $activatedByUserId,
    ) {
    }
}
