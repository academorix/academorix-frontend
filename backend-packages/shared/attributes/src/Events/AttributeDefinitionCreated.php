<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared attributes --force`.

declare(strict_types=1);

namespace Academorix\Attributes\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New AttributeDefinition committed via CreateAttributeDefinitionAction.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateDefinitionCacheOnDefinitionCreated
 *
 * @category Attributes
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AttributeDefinitionCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $definitionId  ULID (ads_*)
     * @param  string  $tenantId  ULID
     * @param  string  $code  string
     * @param  int  $type  'integer' | 'decimal' | 'select' | 'boolean' | 'date' | 'text' | 'slider' | 'percentage'
     * @param  string  $widget  string
     * @param  string  $createdByUserId  ULID (usr_*)
     */
    public function __construct(
        public readonly string $definitionId,
        public readonly string $tenantId,
        public readonly string $code,
        public readonly int $type,
        public readonly string $widget,
        public readonly string $createdByUserId,
    ) {
    }
}
