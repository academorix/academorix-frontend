<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared attributes --force`.

declare(strict_types=1);

namespace Academorix\Attributes\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AttributeSet.is_active flipped true → false. Fallback to next-lower version if any.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateHostResolverCache
 *
 * @category Attributes
 *
 * @since    0.1.0
 */
final readonly class AttributeSetDeactivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $setId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $deactivatedAt  ISO 8601
     * @param  string  $deactivatedByUserId  ULID
     */
    public function __construct(
        public readonly string $setId,
        public readonly string $tenantId,
        public readonly string $deactivatedAt,
        public readonly string $deactivatedByUserId,
    ) {
    }
}
