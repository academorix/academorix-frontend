<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared transfer --force`.

declare(strict_types=1);

namespace Academorix\Transfer\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * TransferSettings — Data transfer.
 *
 * Tenant-wide defaults for the import / export engine.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Transfer
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'transfer',
    label: 'Data transfer',
    description: 'Tenant-wide defaults for the import / export engine.',
    icon: 'arrow-path',
    scope: 'tenant',
    permission: 'settings.read',
    sortOrder: 60,
)]
class TransferSettings extends Settings
{

    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', description: 'Default channels for job-completion notifications when the initiator hasn\'t set a preference.', icon: 'bell', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Text, label: 'Default channels', validation: ['required', 'array', 'min:1'], group: 'Notifications', sortOrder: 1, options: ['mail' => 'Email', 'broadcast' => 'Real-time (in-app toast)', 'database' => 'In-app inbox', 'push' => 'Mobile push', 'sms' => 'SMS'])]
    public array $default_notify_channels = ['database', 'broadcast'];


    // ── Import defaults ──────────────────────────────────
    #[SettingGroup(label: 'Import defaults', description: 'Fallback behaviour when a request omits the parameter.', icon: 'arrow-down-tray', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Select, label: 'Default mode', validation: ['required', 'in:append,upsert,upsert,replace,delete'], group: 'Import defaults', sortOrder: 1, options: ['append' => 'Append (insert new only)', 'upsert' => 'Upsert (insert or update)', 'replace' => 'Replace (destructive)', 'delete' => 'Delete matched rows'])]
    public string $default_mode = 'upsert';

    #[SettingField(controlType: ControlType::Text, label: 'Stop on first error', group: 'Import defaults', sortOrder: 2)]
    public bool $stop_on_error = false;

    #[SettingField(controlType: ControlType::Number, label: 'Max errors before halt', validation: ['integer', 'min:0'], group: 'Import defaults', sortOrder: 3, helpText: '0 = unlimited.')]
    public int $max_errors = 0;

    #[SettingField(controlType: ControlType::Text, label: 'Auto-delete uploaded file after success', group: 'Import defaults', sortOrder: 4)]
    public bool $auto_delete_source = true;


    // ── Export defaults ──────────────────────────────────
    #[SettingGroup(label: 'Export defaults', icon: 'arrow-up-tray', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Select, label: 'Default format', validation: ['required', 'in:csv,xlsx,json,pdf'], group: 'Export defaults', sortOrder: 1, options: ['csv' => 'CSV', 'xlsx' => 'Excel (XLSX)', 'json' => 'JSON', 'pdf' => 'PDF'])]
    public string $default_format = 'xlsx';

    #[SettingField(controlType: ControlType::Text, label: 'Auto-size columns (XLSX)', group: 'Export defaults', sortOrder: 2)]
    public bool $auto_size_xlsx = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'transfer';
    }
}
