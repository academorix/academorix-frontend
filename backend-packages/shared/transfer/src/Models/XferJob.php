<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared transfer --force`.

declare(strict_types=1);

namespace Academorix\Transfer\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Transfer\Contracts\Data\XferJobInterface;
use Academorix\Transfer\Database\Factories\XferJobFactory;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\SoftDeletes;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a XferJob.
 *
 * The persisted domain record for a single import / export / sample operation.
 *
 * @category Transfer
 *
 * @since    0.1.0
 */
#[Table(name: XferJobInterface::TABLE, keyType: XferJobInterface::KEY_TYPE)]
#[Fillable([
    XferJobInterface::ATTR_TENANT_ID,
        XferJobInterface::ATTR_CAUSER_TYPE,
        XferJobInterface::ATTR_CAUSER_ID,
        XferJobInterface::ATTR_KIND,
        XferJobInterface::ATTR_ENTITY,
        XferJobInterface::ATTR_MODE,
        XferJobInterface::ATTR_FORMAT,
        XferJobInterface::ATTR_STATUS,
        XferJobInterface::ATTR_COUNTERS,
        XferJobInterface::ATTR_PARAMS,
        XferJobInterface::ATTR_FILTERS,
        XferJobInterface::ATTR_MAPPING_PROFILE_ID,
        XferJobInterface::ATTR_SOURCE_FILENAME,
        XferJobInterface::ATTR_SOURCE_FILE_SIZE_BYTES,
        XferJobInterface::ATTR_SOURCE_FILE_HASH,
        XferJobInterface::ATTR_SOURCE_ARTIFACT_ID,
        XferJobInterface::ATTR_RESULT_ARTIFACT_ID,
        XferJobInterface::ATTR_ERRORS_ARTIFACT_ID,
        XferJobInterface::ATTR_NOTIFY_CHANNELS,
        XferJobInterface::ATTR_NOTIFY_LOCALE,
        XferJobInterface::ATTR_RETENTION_TIER,
        XferJobInterface::ATTR_QUEUE_CONNECTION,
        XferJobInterface::ATTR_QUEUE_NAME,
        XferJobInterface::ATTR_LARAVEL_QUEUE_BATCH_ID,
        XferJobInterface::ATTR_SHARDS_TOTAL,
        XferJobInterface::ATTR_SHARDS_COMPLETED,
        XferJobInterface::ATTR_PROGRESS_PERCENT,
        XferJobInterface::ATTR_STARTED_AT,
        XferJobInterface::ATTR_FINISHED_AT,
        XferJobInterface::ATTR_LAST_PROGRESS_AT,
        XferJobInterface::ATTR_ARTIFACT_PURGED_AT,
        XferJobInterface::ATTR_LAST_ERROR_MESSAGE,
        XferJobInterface::ATTR_LAST_ERROR_CODE,
        XferJobInterface::ATTR_CANCELLED_BY_TYPE,
        XferJobInterface::ATTR_CANCELLED_BY_ID,
        XferJobInterface::ATTR_CANCEL_REASON,
])]
#[UseFactory(XferJobFactory::class)]
final class XferJob extends Model implements XferJobInterface
{
    use HasUlids;
    use BelongsToTenant;
    use HasActivityLog;
    // TODO(gen): resolve unknown trait `HasAudit` — add its import + use line.
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        XferJobInterface::ATTR_KIND => 'Academorix\Transfer\Enums\XferJobKind',
        XferJobInterface::ATTR_MODE => 'Academorix\Transfer\Enums\ImportMode',
        XferJobInterface::ATTR_FORMAT => 'string',
        XferJobInterface::ATTR_STATUS => 'Academorix\Transfer\Enums\XferJobStatus',
        XferJobInterface::ATTR_COUNTERS => 'Academorix\Transfer\Casts\XferJobCountersCast',
        XferJobInterface::ATTR_PARAMS => 'array',
        XferJobInterface::ATTR_FILTERS => 'array',
        XferJobInterface::ATTR_NOTIFY_CHANNELS => 'Academorix\Transfer\Casts\NotifyChannelListCast',
        XferJobInterface::ATTR_RETENTION_TIER => 'string',
        XferJobInterface::ATTR_SHARDS_TOTAL => 'integer',
        XferJobInterface::ATTR_SHARDS_COMPLETED => 'integer',
        XferJobInterface::ATTR_PROGRESS_PERCENT => 'integer',
        XferJobInterface::ATTR_SOURCE_FILE_SIZE_BYTES => 'integer',
        XferJobInterface::ATTR_STARTED_AT => 'immutable_datetime',
        XferJobInterface::ATTR_FINISHED_AT => 'immutable_datetime',
        XferJobInterface::ATTR_LAST_PROGRESS_AT => 'immutable_datetime',
        XferJobInterface::ATTR_ARTIFACT_PURGED_AT => 'immutable_datetime',
    ];
}
