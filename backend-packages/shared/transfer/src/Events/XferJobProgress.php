<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared transfer --force`.

declare(strict_types=1);

namespace Academorix\Transfer\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Fires every `config('transfer.progress_every_rows')` rows during a chunk, or on every shard completion. NOT audit-worthy — broadcast only.
 *
 * ## Consumers
 *
 * - SPA progress bar
 *
 * @category Transfer
 *
 * @since    0.1.0
 */
final readonly class XferJobProgress implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $xferJobId  ULID
     * @param  int  $progressPercent  integer 0–100
     * @param  string  $counters  { total, created, updated, skipped, failed, deleted }
     */
    public function __construct(
        public readonly string $xferJobId,
        public readonly int $progressPercent,
        public readonly string $counters,
    ) {
    }
}
