<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared transfer --force`.

declare(strict_types=1);

namespace Academorix\Transfer\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * One XferShard transitioned to succeeded or failed. Fires from XferShardObserver.updated. Coordinator job aggregates counters into the parent XferJob.
 *
 * ## Consumers
 *
 * - ImportCoordinatorJob / ExportCoordinatorJob — aggregates + updates parent progress
 * - SPA — detailed progress
 *
 * @category Transfer
 *
 * @since    0.1.0
 */
final readonly class XferJobShardCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $xferJobId  ULID
     * @param  string  $xferShardId  ULID (xshd_)
     * @param  int  $shardIndex  integer
     * @param  string  $shardStatus  'succeeded' | 'failed'
     * @param  array  $shardCounters  counters object
     */
    public function __construct(
        public readonly string $xferJobId,
        public readonly string $xferShardId,
        public readonly int $shardIndex,
        public readonly string $shardStatus,
        public readonly array $shardCounters,
    ) {
    }
}
