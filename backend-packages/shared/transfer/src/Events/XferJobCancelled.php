<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared transfer --force`.

declare(strict_types=1);

namespace Academorix\Transfer\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * XferJob transitioned to cancelled either by user request (POST /jobs/{id}/cancel) or stale sweep (queued/running with no progress > 24h). Fires from XferJobObserver.updating.
 *
 * ## Consumers
 *
 * - activity::HasActivityLog
 * - audit::HasAudit
 *
 * @category Transfer
 *
 * @since    0.1.0
 */
final readonly class XferJobCancelled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $xferJobId  ULID
     * @param  string  $cancelledByType  string | null
     * @param  string  $cancelledById  string | null
     * @param  string  $cancelReason  string | null
     * @param  string  $finishedAt  ISO 8601
     */
    public function __construct(
        public readonly string $xferJobId,
        public readonly string $cancelledByType,
        public readonly string $cancelledById,
        public readonly string $cancelReason,
        public readonly string $finishedAt,
    ) {
    }
}
