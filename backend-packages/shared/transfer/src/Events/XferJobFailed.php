<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared transfer --force`.

declare(strict_types=1);

namespace Academorix\Transfer\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * XferJob transitioned to failed (unrecoverable). Fires from AbstractDynamicImport/Export::failed(Throwable) handler.
 *
 * ## Consumers
 *
 * - activity::HasActivityLog
 * - audit::HasAudit
 * - SendXferJobNotificationJob (chained; sends XferJobFailedNotification)
 * - observability::MetricsCollector
 *
 * @category Transfer
 *
 * @since    0.1.0
 */
final readonly class XferJobFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $xferJobId  ULID
     * @param  string  $errorCode  string
     * @param  string  $errorMessage  string (redacted of restricted-tier row values)
     * @param  string  $finishedAt  ISO 8601
     */
    public function __construct(
        public readonly string $xferJobId,
        public readonly string $errorCode,
        public readonly string $errorMessage,
        public readonly string $finishedAt,
    ) {
    }
}
