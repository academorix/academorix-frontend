<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared offline-sync --force`.

declare(strict_types=1);

namespace Academorix\OfflineSync\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Op conflicted with server state. Resolution strategy applied per aggregate.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::AlertOnConflictRate
 *
 * @category OfflineSync
 *
 * @since    0.1.0
 */
final readonly class SyncConflictDetected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $opId  ULID
     * @param  string  $deviceId  ULID
     * @param  string  $aggregateType  string
     * @param  string  $aggregateId  ULID
     * @param  string  $resolution  'last_writer_wins'|'merge'|'client_reject'|'user_prompt'
     */
    public function __construct(
        public readonly string $opId,
        public readonly string $deviceId,
        public readonly string $aggregateType,
        public readonly string $aggregateId,
        public readonly string $resolution,
    ) {
    }
}
