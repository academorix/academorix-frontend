<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared offline-sync --force`.

declare(strict_types=1);

namespace Academorix\OfflineSync\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan offline-sync:reset-device` — Reset device — offline sync command.
 *
 * @category OfflineSync
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'offline-sync:reset-device',
    description: 'Reset device — offline sync command.',
)]
final class ResetDeviceCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Reset device — offline sync command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Reset device — offline sync command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
