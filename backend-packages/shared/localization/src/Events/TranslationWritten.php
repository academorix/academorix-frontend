<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared localization --force`.

declare(strict_types=1);

namespace Academorix\Localization\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * `HasTranslations::setTranslation()` wrote a new (locale, field) tuple into a model's `translations` JSONB column. Fires on both first-time writes AND updates. Distinct from `TranslationOverridden` (which is for platform-level UI-string overrides).
 *
 * ## Consumers
 *
 * - localization::BackfillMissingLocalesViaAutoTranslate
 * - activity::WriteToActivityLog
 *
 * @category Localization
 *
 * @since    0.1.0
 */
final readonly class TranslationWritten implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID | null
     * @param  string  $modelType  string (fully qualified class)
     * @param  string  $modelId  string (ULID or PK)
     * @param  string  $field  string (the property annotated with #[Translatable])
     * @param  string  $localeCode  string (BCP-47)
     * @param  string  $actorId  ULID | null (null = system write, e.g. auto-translate)
     * @param  bool  $isNewLocale  boolean (true = first write for this locale on this row)
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $modelType,
        public readonly string $modelId,
        public readonly string $field,
        public readonly string $localeCode,
        public readonly string $actorId,
        public readonly bool $isNewLocale,
    ) {
    }
}
