<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared localization --force`.

declare(strict_types=1);

namespace Academorix\Localization\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * LocalizationUserSettings — My language.
 *
 * Per-user locale preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Localization
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'localization_user',
    label: 'My language',
    description: 'Per-user locale preferences.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 5,
)]
class LocalizationUserSettings extends Settings
{

    // ── Language preferences ──────────────────────────────────
    #[SettingGroup(label: 'Language preferences', icon: 'language', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Preferred locale', group: 'Language preferences', sortOrder: 1, helpText: 'Overrides the tenant default for this user.')]
    public ?string $preferred_locale_code = null;

    #[SettingField(controlType: ControlType::Toggle, label: 'Show language names in native script', group: 'Language preferences', sortOrder: 2)]
    public bool $use_native_names_in_picker = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'localization_user';
    }
}
