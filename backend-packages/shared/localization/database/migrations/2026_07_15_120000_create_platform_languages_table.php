<?php

/**
 * @file database/migrations/2026_07_15_120000_create_platform_languages_table.php
 *
 * @description
 * Create the `platform_languages` table for the `PlatformLanguage` entity.
 * Owned by the localization module. Emitted from the module blueprint —
 * regenerate via `generate-module.py shared localization --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py shared localization --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('platform_languages', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->unsignedBigInteger('geography_language_id');
            $table->foreign('geography_language_id')->references('id')->on('languages')->onDelete('restrict');
            $table->unsignedBigInteger('geography_country_id')->nullable();
            $table->foreign('geography_country_id')->references('id')->on('countries')->onDelete('restrict');
            $table->string('bcp47_code', 12);
            $table->string('script_code', 4)->nullable();
            $table->boolean('is_active_on_platform')->default(true);
            $table->boolean('is_beta')->default(false);
            $table->boolean('is_system')->default(true);
            $table->integer('sort_order')->default(100);
            $table->text('notes')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['bcp47_code'], 'platform_languages_bcp47_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->unique(['geography_language_id', 'geography_country_id', 'script_code'], 'platform_languages_composite_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['is_active_on_platform', 'sort_order'], 'platform_languages_picker_idx');
            $table->index(['geography_language_id'], 'platform_languages_geo_language_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('platform_languages');
    }
};
