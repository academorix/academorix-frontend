<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Approvals\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * ApprovalUserSettings — My approvals.
 *
 * Personal approval-inbox preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'approvals_user',
    label: 'My approvals',
    description: 'Personal approval-inbox preferences.',
    icon: 'inbox',
    scope: 'user',
    sortOrder: 25,
)]
class ApprovalUserSettings extends Settings
{

    // ── Notification preferences ──────────────────────────────────
    #[SettingGroup(label: 'Notification preferences', icon: 'bell', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Text, label: 'Reminder channels', group: 'Notification preferences', sortOrder: 1, options: ['mail' => 'Email', 'in_app' => 'In-app', 'push' => 'Push notification'])]
    public array $reminder_channels = ['mail', 'in_app', 'push'];

    #[SettingField(controlType: ControlType::Select, label: 'Pending approvals digest', group: 'Notification preferences', sortOrder: 2, options: ['off' => 'Off (real-time only)', 'daily' => 'Daily at 9am', 'twice_daily' => 'Twice daily (9am + 3pm)'], helpText: 'In addition to real-time notifications, receive a periodic digest of your pending approvals.')]
    public string $digest_frequency = 'off';


    // ── Inbox display ──────────────────────────────────
    #[SettingGroup(label: 'Inbox display', icon: 'view-columns', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Select, label: 'Default sort', group: 'Inbox display', sortOrder: 1, options: ['expires_at' => 'Expiring soonest first', '-requested_at' => 'Newest first', 'action_key' => 'By action type'])]
    public string $default_inbox_sort = 'expires_at';

    #[SettingField(controlType: ControlType::Toggle, label: 'Show \'delegate\' badge on delegated approvals', group: 'Inbox display', sortOrder: 2, helpText: 'When on, approvals you were substituted into via delegation show a badge on the row.')]
    public bool $show_delegation_badge = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'approvals_user';
    }
}
