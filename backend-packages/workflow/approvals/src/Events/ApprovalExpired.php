<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Approvals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The ExpireOverdueApprovalsJob sweeper found an instance past expires_at with status='pending'. Final status depends on template.on_timeout: expire → status='expired'; approve → treats as unanimous approve + fires ApprovalGranted; escalate → new approvers added + expires_at extended.
 *
 * ## Consumers
 *
 * - notifications::NotifyRequesterOfDecisionJob → ApprovalExpiredNotification
 * - notifications::NotifyPendingApproversJob → ApprovalExpiredNotification
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord
 * - observability::MetricsCollector
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final readonly class ApprovalExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalInstanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $actionKey  string
     * @param  string  $requesterId  ULID
     * @param  string  $expiredAt  ISO-8601
     * @param  string  $onTimeoutBehaviour  'expire' | 'approve' | 'escalate'
     * @param  string  $finalStatus  'expired' | 'approved'
     */
    public function __construct(
        public readonly string $approvalInstanceId,
        public readonly string $tenantId,
        public readonly string $actionKey,
        public readonly string $requesterId,
        public readonly string $expiredAt,
        public readonly string $onTimeoutBehaviour,
        public readonly string $finalStatus,
    ) {
    }
}
