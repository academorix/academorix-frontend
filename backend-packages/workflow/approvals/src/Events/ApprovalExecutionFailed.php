<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Approvals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ApprovalInstanceExecutor dispatched the underlying action but the action threw. The instance is in a WEDGED state: status='approved' but executed_at IS NULL. Human intervention required — retry via `approvals:remediate-failed-executions`.
 *
 * ## Consumers
 *
 * - notifications::NotifyPlatformOpsOfExecutionFailure → ApprovalExecutionFailedNotification
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord (P0)
 * - observability::MetricsCollector
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final readonly class ApprovalExecutionFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalInstanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $actionKey  string
     * @param  string  $errorClass  string (FQCN of the exception)
     * @param  string  $errorMessage  string (redacted — no secrets)
     * @param  string  $errorTraceFingerprint  string (SHA-256 of stack trace; correlate to Sentry)
     * @param  string  $failedAt  ISO-8601
     * @param  int  $attemptCount  integer
     */
    public function __construct(
        public readonly string $approvalInstanceId,
        public readonly string $tenantId,
        public readonly string $actionKey,
        public readonly string $errorClass,
        public readonly string $errorMessage,
        public readonly string $errorTraceFingerprint,
        public readonly string $failedAt,
        public readonly int $attemptCount,
    ) {
    }
}
