<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Approvals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Per-requirement fanout of ApprovalRequested. Fires ONCE per requirement at instance-create time after the ApproverSelectorEngine materialised `resolved_approvers`. Consumers use this to build per-approver notification recipients without re-querying.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final readonly class ApprovalRequirementResolved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalRequirementId  ULID (`apr_...`)
     * @param  string  $approvalInstanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $templateApproverGroupId  ULID
     * @param  string  $groupName  string
     * @param  int  $sortOrder  integer
     * @param  string  $resolvedApprovers  ULID[]
     * @param  string  $quorumType  'all' | 'any' | 'n_of_m'
     * @param  int  $quorumN  integer | null
     * @param  bool  $startsPending  boolean (false = requirement starts as blocked pending a lower sort_order group)
     */
    public function __construct(
        public readonly string $approvalRequirementId,
        public readonly string $approvalInstanceId,
        public readonly string $tenantId,
        public readonly string $templateApproverGroupId,
        public readonly string $groupName,
        public readonly int $sortOrder,
        public readonly string $resolvedApprovers,
        public readonly string $quorumType,
        public readonly int $quorumN,
        public readonly bool $startsPending,
    ) {
    }
}
