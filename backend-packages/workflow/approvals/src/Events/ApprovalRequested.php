<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Approvals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The `approve` middleware intercepted a decorated action + resolved a matching template + created an `approval_instances` row with status='pending'. This is the fanout signal for NotifyApproversJob.
 *
 * ## Consumers
 *
 * - notifications::NotifyApproversJob (dispatches ApprovalRequestedNotification to each resolved approver)
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord
 * - observability::MetricsCollector (increments academorix.approvals.instances.total)
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final readonly class ApprovalRequested implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalInstanceId  ULID (`api_...`)
     * @param  string  $tenantId  ULID
     * @param  string  $applicationId  ULID
     * @param  string  $actionKey  string
     * @param  string  $approvalTemplateId  ULID
     * @param  int  $templateVersion  integer
     * @param  string  $requesterId  ULID
     * @param  string  $subjectType  string | null
     * @param  string  $subjectId  ULID | null
     * @param  string  $requestedAt  ISO-8601
     * @param  string  $expiresAt  ISO-8601
     * @param  int  $requirementCount  integer (how many groups matched)
     * @param  int  $totalResolvedApproverCount  integer (union across all groups; delegation-substituted)
     */
    public function __construct(
        public readonly string $approvalInstanceId,
        public readonly string $tenantId,
        public readonly string $applicationId,
        public readonly string $actionKey,
        public readonly string $approvalTemplateId,
        public readonly int $templateVersion,
        public readonly string $requesterId,
        public readonly string $subjectType,
        public readonly string $subjectId,
        public readonly string $requestedAt,
        public readonly string $expiresAt,
        public readonly int $requirementCount,
        public readonly int $totalResolvedApproverCount,
    ) {
    }
}
