<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Approvals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Every requirement has status='satisfied'. Instance transitions pending→approved. THIS IS THE ONLY EVENT DISPATCHED SYNCHRONOUSLY — the ApprovalInstanceExecutor listener runs in the same request context to preserve the caller's transaction. Downstream listeners (notifications, activity) queue as normal.
 *
 * ## Consumers
 *
 * - approvals::ApprovalInstanceExecutor (dispatches the underlying action with scoped('approvals.executing') flag)
 * - notifications::NotifyRequesterOfDecisionJob → ApprovalGrantedNotification
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord (P0)
 * - observability::MetricsCollector
 * - broadcast::PublishToApprovalChannels
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final readonly class ApprovalGranted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalInstanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $applicationId  ULID
     * @param  string  $actionKey  string
     * @param  string  $requesterId  ULID
     * @param  string  $finalDeciderId  ULID (the approver whose decision tipped the last requirement to satisfied)
     * @param  string  $decidedAt  ISO-8601
     * @param  int  $requirementCount  integer
     * @param  int  $decisionCount  integer (total approves + rejects that happened; usually equals requirement_count when all groups quorum=all)
     */
    public function __construct(
        public readonly string $approvalInstanceId,
        public readonly string $tenantId,
        public readonly string $applicationId,
        public readonly string $actionKey,
        public readonly string $requesterId,
        public readonly string $finalDeciderId,
        public readonly string $decidedAt,
        public readonly int $requirementCount,
        public readonly int $decisionCount,
    ) {
    }
}
