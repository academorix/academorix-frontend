<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Approvals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Boot-time discovery pass upserted a new `approvable_actions` row from an #[AsApprovableAction] decorator on an action class. Fires once per new registration per boot. Not fired on unchanged rows.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog (platform-level activity feed — new capability visible)
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final readonly class ApprovableActionRegistered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvableActionId  ULID (`aac_...`)
     * @param  string  $actionKey  string
     * @param  string  $namespace  string
     * @param  string  $actionClassFqcn  string
     * @param  string  $registeredAt  ISO-8601
     */
    public function __construct(
        public readonly string $approvableActionId,
        public readonly string $actionKey,
        public readonly string $namespace,
        public readonly string $actionClassFqcn,
        public readonly string $registeredAt,
    ) {
    }
}
