<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Approvals\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Tenant admin flipped is_active=true on an inactive template row. The one-is-active invariant enforced by ApprovalTemplateObserver deactivates any peer row with the same (tenant, application, action_key, name).
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ApprovalTemplateActivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalTemplateId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $actionKey  string
     * @param  string  $name  string
     * @param  int  $version  integer
     * @param  string  $deactivatedPeerId  ULID | null (the row previously carrying is_active=true for the same key)
     * @param  string  $actorUserId  ULID
     */
    public function __construct(
        public readonly string $approvalTemplateId,
        public readonly string $tenantId,
        public readonly string $actionKey,
        public readonly string $name,
        public readonly int $version,
        public readonly string $deactivatedPeerId,
        public readonly string $actorUserId,
    ) {
    }
}
