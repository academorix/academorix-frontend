<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Approvals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A group was added/edited/removed on a template. Batched — one event per admin edit session (multiple group mutations coalesce to one event with `changed_groups[]`). ALWAYS accompanied by ApprovalTemplateVersionBumped in the same transaction.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final readonly class ApprovalTemplateGroupsUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalTemplateId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $addedGroupIds  ULID[]
     * @param  string  $updatedGroupIds  ULID[]
     * @param  string  $removedGroupIds  ULID[]
     * @param  string  $editorUserId  ULID
     */
    public function __construct(
        public readonly string $approvalTemplateId,
        public readonly string $tenantId,
        public readonly string $addedGroupIds,
        public readonly string $updatedGroupIds,
        public readonly string $removedGroupIds,
        public readonly string $editorUserId,
    ) {
    }
}
