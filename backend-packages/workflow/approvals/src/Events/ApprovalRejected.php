<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Approvals\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * At least one requirement failed. Instance transitions pending→rejected. Distinct from ApprovalExpired (past expires_at) and ApprovalCancelled (requester withdraw).
 *
 * ## Consumers
 *
 * - notifications::NotifyRequesterOfDecisionJob → ApprovalRejectedNotification
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord (P0)
 * - observability::MetricsCollector
 * - broadcast::PublishToApprovalChannels
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ApprovalRejected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalInstanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $applicationId  ULID
     * @param  string  $actionKey  string
     * @param  string  $requesterId  ULID
     * @param  string  $finalDeciderId  ULID (the approver whose reject tipped the flow)
     * @param  string  $rejectionReason  string (comment from the rejecting decision)
     * @param  string  $rejectedAt  ISO-8601
     */
    public function __construct(
        public readonly string $approvalInstanceId,
        public readonly string $tenantId,
        public readonly string $applicationId,
        public readonly string $actionKey,
        public readonly string $requesterId,
        public readonly string $finalDeciderId,
        public readonly string $rejectionReason,
        public readonly string $rejectedAt,
    ) {
    }
}
