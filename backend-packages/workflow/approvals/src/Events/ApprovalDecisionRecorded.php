<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Approvals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * An approver POSTed approve OR reject and the ApprovalDecisionRecorder wrote a new `approval_decisions` row. Fires BEFORE quorum evaluation runs.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord
 * - observability::MetricsCollector
 * - broadcast::PublishToApprovalChannels (real-time approver-inbox refresh)
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final readonly class ApprovalDecisionRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalDecisionId  ULID (`apd_...`)
     * @param  string  $approvalRequirementId  ULID
     * @param  string  $approvalInstanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $approverId  ULID
     * @param  string  $delegatedFromId  ULID | null
     * @param  string  $decision  'approve' | 'reject'
     * @param  string  $decidedAt  ISO-8601
     */
    public function __construct(
        public readonly string $approvalDecisionId,
        public readonly string $approvalRequirementId,
        public readonly string $approvalInstanceId,
        public readonly string $tenantId,
        public readonly string $approverId,
        public readonly string $delegatedFromId,
        public readonly string $decision,
        public readonly string $decidedAt,
    ) {
    }
}
