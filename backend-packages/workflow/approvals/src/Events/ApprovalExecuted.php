<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

namespace Academorix\Approvals\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ApprovalInstanceExecutor successfully dispatched the underlying action + captured its return value. Instance transitions approved→executed AND `executed_at` is set. THIS IS THE HANDOFF SIGNAL to downstream cross-module bridges (access/grants materialization, billing charge processing, identity impersonation extension).
 *
 * ## Consumers
 *
 * - access/requests::AccessRequestGrantMaterializer (when action_key='access.grants.create')
 * - activity::WriteToActivityLog
 * - audit::WriteAuditRecord (P0 — this is the moment tenant state actually changed)
 * - observability::MetricsCollector
 * - broadcast::PublishToApprovalChannels
 *
 * @category Approvals
 *
 * @since    0.1.0
 */
final readonly class ApprovalExecuted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $approvalInstanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $applicationId  ULID
     * @param  string  $actionKey  string
     * @param  string  $requesterId  ULID
     * @param  string  $subjectType  string | null
     * @param  string  $subjectId  ULID | null
     * @param  string  $executedAt  ISO-8601
     * @param  string  $executionResultSummary  Record<string, mixed> (the action's return value's PUBLIC-tier projection — never the full payload)
     */
    public function __construct(
        public readonly string $approvalInstanceId,
        public readonly string $tenantId,
        public readonly string $applicationId,
        public readonly string $actionKey,
        public readonly string $requesterId,
        public readonly string $subjectType,
        public readonly string $subjectId,
        public readonly string $executedAt,
        public readonly string $executionResultSummary,
    ) {
    }
}
