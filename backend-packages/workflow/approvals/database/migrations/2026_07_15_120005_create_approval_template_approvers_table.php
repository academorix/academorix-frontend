<?php

/**
 * @file database/migrations/2026_07_15_120005_create_approval_template_approvers_table.php
 *
 * @description
 * Create the `approval_template_approvers` table for the `ApprovalTemplateApprover` entity.
 * Owned by the approvals module. Emitted from the module blueprint —
 * regenerate via `generate-module.py workflow approvals --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('approval_template_approvers', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('approval_template_id', 64);
            $table->foreign('approval_template_id')->references('id')->on('approval_templates')->onDelete('cascade');
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('group_name', 100);
            $table->integer('sort_order')->default(0);
            $table->text('selector_expression');
            $table->string('quorum_type', 16)->default('any');
            $table->integer('quorum_n')->nullable();
            $table->boolean('allows_self_approve')->default(false);
            $table->text('description')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->index(['approval_template_id', 'sort_order'], 'approval_template_approvers_template_sort_idx');
            $table->index(['tenant_id'], 'approval_template_approvers_tenant_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('approval_template_approvers');
    }
};
