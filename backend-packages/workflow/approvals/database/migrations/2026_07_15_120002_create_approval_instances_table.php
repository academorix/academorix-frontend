<?php

/**
 * @file database/migrations/2026_07_15_120002_create_approval_instances_table.php
 *
 * @description
 * Create the `approval_instances` table for the `ApprovalInstance` entity.
 * Owned by the approvals module. Emitted from the module blueprint —
 * regenerate via `generate-module.py workflow approvals --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow approvals --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('approval_instances', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('application_id', 64);
            $table->foreign('application_id')->references('id')->on('applications')->onDelete('restrict');
            $table->string('action_key', 200);
            $table->foreign('action_key')->references('action_key')->on('approvable_actions')->onDelete('restrict');
            $table->string('approval_template_id', 64);
            $table->foreign('approval_template_id')->references('id')->on('approval_templates')->onDelete('restrict');
            $table->integer('template_version');
            $table->string('requester_id', 64);
            $table->foreign('requester_id')->references('id')->on('users')->onDelete('restrict');
            $table->string('status', 16)->default('pending');
            $table->string('subject_type', 200)->nullable();
            $table->string('subject_id', 64)->nullable();
            $table->jsonb('context_json')->default('\'{}\'::jsonb');
            $table->jsonb('payload_json')->default('\'{}\'::jsonb');
            $table->string('requester_ip_hash', 64)->nullable();
            $table->timestampTz('requested_at');
            $table->timestampTz('expires_at');
            $table->timestampTz('decided_at')->nullable();
            $table->timestampTz('executed_at')->nullable();
            $table->timestampTz('rejected_at')->nullable();
            $table->timestampTz('cancelled_at')->nullable();
            $table->string('final_decider_id', 64)->nullable();
            $table->foreign('final_decider_id')->references('id')->on('users')->onDelete('restrict');
            $table->text('rejection_reason')->nullable();
            $table->jsonb('execution_result_json')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->index(['tenant_id', 'status', 'expires_at'], 'approval_instances_pending_sweep_idx');
            $table->index(['tenant_id', 'requester_id', 'requested_at'], 'approval_instances_requester_recent_idx');
            $table->index(['subject_type', 'subject_id', 'status'], 'approval_instances_subject_idx');
            $table->index(['approval_template_id'], 'approval_instances_template_idx');
            $table->index(['tenant_id', 'action_key', 'status'], 'approval_instances_reporting_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('approval_instances');
    }
};
