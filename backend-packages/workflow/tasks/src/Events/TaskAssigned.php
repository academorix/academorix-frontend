<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py workflow tasks --force`.

declare(strict_types=1);

namespace Academorix\Tasks\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New TaskAssignment row committed.
 *
 * ## Consumers
 *
 * - notifications::DispatchTaskAssignedNotificationListener
 *
 * @category Tasks
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TaskAssigned implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $taskId  ULID
     * @param  string  $assignmentId  ULID (tas_*)
     * @param  string  $userId  ULID
     * @param  string  $role  string
     * @param  string  $assignedBy  ULID | null
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $taskId,
        public readonly string $assignmentId,
        public readonly string $userId,
        public readonly string $role,
        public readonly string $assignedBy,
        public readonly string $at,
    ) {
    }
}
