<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py billing subscription --force`.

declare(strict_types=1);

namespace Academorix\Subscription\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan subscription:advance-dunning` — Advance dunning — subscription command.
 *
 * @category Subscription
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'subscription:advance-dunning',
    description: 'Advance dunning — subscription command.',
)]
final class AdvanceDunningCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Advance dunning — subscription command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Advance dunning — subscription command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
