<?php

declare(strict_types=1);

use Academorix\Subscription\Models\Subscription;

it('is a placeholder — replace with actual Subscription unit tests', function (): void {
    expect(Subscription::class)->toBeString();
});
