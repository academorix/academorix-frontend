<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py billing entitlements --force`.

declare(strict_types=1);

namespace Academorix\Entitlements\Actions\CentralAdmin;

use Academorix\Entitlements\Contracts\Repositories\EntitlementRepositoryInterface;
use Academorix\Entitlements\Data\EntitlementData;
use Academorix\Entitlements\Data\Requests\UpdateEntitlementRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/platform/entitlements/{tenant}/{key}` — update action (central_admin audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Patch(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Entitlements
 *
 * @since    0.1.0
 */
#[AsController]
#[Patch('/api/v1/platform/entitlements/{tenant}/{key}')]
final class UpdateEntitlementAction
{
    public function __construct(
        private readonly EntitlementRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `entitlement` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateEntitlementRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateEntitlementRequestData $data): EntitlementData
    {
        $model = $this->repository->update($id, $data->toArray());

        return EntitlementData::from($model);
    }
}
