<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py billing entitlements --force`.

declare(strict_types=1);

namespace Academorix\Entitlements\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * EntitlementsSettings — Quotas + usage.
 *
 * Tenant-facing quota notification preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Entitlements
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'entitlements',
    label: 'Quotas + usage',
    description: 'Tenant-facing quota notification preferences.',
    icon: 'chart-bar-square',
    scope: 'tenant',
    permission: 'entitlements.viewAny',
    sortOrder: 23,
)]
class EntitlementsSettings extends Settings
{

    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', description: 'Which quota events send tenant-owner notifications.', icon: 'bell-alert', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify when approaching cap (80%)', validation: ['boolean'], group: 'Notifications', sortOrder: 1)]
    public bool $notify_approaching_cap = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify when cap exceeded', validation: ['boolean'], group: 'Notifications', sortOrder: 2, helpText: 'Cannot disable — exceeded caps block operations, owner must know.')]
    public bool $notify_exceeded_cap = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify on period reset', validation: ['boolean'], group: 'Notifications', sortOrder: 3, helpText: 'Off by default (noisy).')]
    public bool $notify_period_reset = false;


    // ── Dashboard preferences ──────────────────────────────────
    #[SettingGroup(label: 'Dashboard preferences', icon: 'adjustments-horizontal', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Select, label: 'Usage chart default period', validation: ['required', 'string', 'in:day,week,month,quarter'], group: 'Dashboard preferences', sortOrder: 1, options: ['day' => 'Last 24 hours', 'week' => 'Last 7 days', 'month' => 'Last 30 days', 'quarter' => 'Last 90 days'])]
    public string $dashboard_default_period = 'month';

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'entitlements';
    }
}
