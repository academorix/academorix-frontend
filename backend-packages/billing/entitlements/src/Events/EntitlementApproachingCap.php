<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py billing entitlements --force`.

declare(strict_types=1);

namespace Academorix\Entitlements\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * value_used crossed the warn threshold (default 80% of value_cap). Fires once per period.
 *
 * ## Consumers
 *
 * - notifications::DispatchQuotaApproachingNotification
 *
 * @category Entitlements
 *
 * @since    0.1.0
 */
final readonly class EntitlementApproachingCap implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $entitlementId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $key  string
     * @param  string  $usageRatio  number
     */
    public function __construct(
        public readonly string $entitlementId,
        public readonly string $tenantId,
        public readonly string $key,
        public readonly string $usageRatio,
    ) {
    }
}
