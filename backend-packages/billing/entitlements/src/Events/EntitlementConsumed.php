<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py billing entitlements --force`.

declare(strict_types=1);

namespace Academorix\Entitlements\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Enforcer::consume() succeeded. Fires afterCommit on the EntitlementUsage row.
 *
 * ## Consumers
 *
 * - telemetry::MetricsCollector
 *
 * @category Entitlements
 *
 * @since    0.1.0
 */
final readonly class EntitlementConsumed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $entitlementId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $key  string
     * @param  int  $amount  integer
     * @param  int  $newValueUsed  integer
     * @param  string  $correlationId  string | null
     */
    public function __construct(
        public readonly string $entitlementId,
        public readonly string $tenantId,
        public readonly string $key,
        public readonly int $amount,
        public readonly int $newValueUsed,
        public readonly string $correlationId,
    ) {
    }
}
