<?php

/**
 * @file database/migrations/2026_07_15_120000_create_consent_categories_table.php
 *
 * @description
 * Create the `consent_categories` table for the `ConsentCategory` entity.
 * Owned by the compliance module. Emitted from the module blueprint —
 * regenerate via `generate-module.py compliance compliance --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py compliance compliance --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('consent_categories', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30)->nullable();
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('key', 64);
            $table->string('display_name', 128);
            $table->text('description')->nullable();
            $table->boolean('is_default_granted')->default(false);
            $table->boolean('requires_guardian')->default(false);
            $table->boolean('is_system')->default(false);
            $table->jsonb('allowed_channels')->default('\'[]\'::jsonb');
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'key'], 'consent_categories_tenant_key_uidx');
            $table->index(['is_system', 'tenant_id'], 'consent_categories_system_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('consent_categories');
    }
};
