<?php

/**
 * @file database/migrations/2026_07_15_120007_create_subprocessors_table.php
 *
 * @description
 * Create the `subprocessors` table for the `Subprocessor` entity.
 * Owned by the compliance module. Emitted from the module blueprint —
 * regenerate via `generate-module.py compliance compliance --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py compliance compliance --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('subprocessors', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('key', 64);
            $table->string('name', 128);
            $table->string('role', 32);
            $table->text('purpose');
            $table->text('description')->nullable();
            $table->jsonb('data_processed')->default('\'[]\'::jsonb');
            $table->jsonb('location')->default('\'[]\'::jsonb');
            $table->string('legal_basis', 64)->default('contract');
            $table->string('dpa_url', 500)->nullable();
            $table->string('dpa_version', 32)->nullable();
            $table->string('contact_email', 320)->nullable();
            $table->boolean('is_active')->default(true);
            $table->boolean('is_system')->default(false);
            $table->integer('version')->default(1);
            $table->string('previous_version_id', 30)->nullable();
            $table->timestampTz('first_engaged_at');
            $table->string('last_updated_by', 64)->nullable();
            $table->foreign('last_updated_by')->references('id')->on('users')->onDelete('set null');
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['key'], 'subprocessors_key_uidx');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['is_active', 'role'], 'subprocessors_active_role_idx');
            $table->index(['version'], 'subprocessors_version_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('subprocessors');
    }
};
