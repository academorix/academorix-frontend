<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py compliance compliance --force`.

declare(strict_types=1);

namespace Academorix\Compliance\Actions\Tenant;

use Academorix\Compliance\Contracts\Repositories\SafeguardingIncidentRepositoryInterface;
use Academorix\Compliance\Data\SafeguardingIncidentData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Get;
use Illuminate\Http\Request;
use Illuminate\Pagination\LengthAwarePaginator;
use Spatie\LaravelData\PaginatedDataCollection;

/**
 * `GET /api/v1/compliance/safeguarding-incidents` — list action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Get(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Compliance
 *
 * @since    0.1.0
 */
#[AsController]
#[Get('/api/v1/compliance/safeguarding-incidents')]
final class ListSafeguardingIncidentAction
{
    public function __construct(
        private readonly SafeguardingIncidentRepositoryInterface $repository,
    ) {
    }

    /**
     * List `safeguarding-incidents` for the current caller.
     *
     * The `Repository` base picks up `filter[...]` / `sort=...` /
     * `include=...` query parameters via the `#[Filterable]` attribute
     * on the concrete repository — no per-Action wiring needed.
     *
     * @return PaginatedDataCollection<int, SafeguardingIncidentData>  Paginated wire-visible DTOs.
     */
    public function __invoke(Request $request): PaginatedDataCollection
    {
        /** @var LengthAwarePaginator<int, \Academorix\Compliance\Models\SafeguardingIncident> $page */
        $page = $this->repository->paginate(
            perPage: (int) $request->integer('per_page', 15),
        );

        return SafeguardingIncidentData::collect($page, PaginatedDataCollection::class);
    }
}
