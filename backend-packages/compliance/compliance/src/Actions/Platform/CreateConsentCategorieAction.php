<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py compliance compliance --force`.

declare(strict_types=1);

namespace Academorix\Compliance\Actions\Platform;

use Academorix\Compliance\Contracts\Repositories\ConsentCategoryRepositoryInterface;
use Academorix\Compliance\Data\ConsentCategoryData;
use Academorix\Compliance\Data\Requests\CreateConsentCategoryRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/platform/compliance/consent-categories` — create action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Post(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Compliance
 *
 * @since    0.1.0
 */
#[AsController]
#[Post('/api/v1/platform/compliance/consent-categories')]
final class CreateConsentCategorieAction
{
    public function __construct(
        private readonly ConsentCategoryRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `consent-categorie` from the validated request payload.
     *
     * @param  CreateConsentCategoryRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateConsentCategoryRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(ConsentCategoryData::from($model), JsonResponse::HTTP_CREATED);
    }
}
