<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py compliance compliance --force`.

declare(strict_types=1);

namespace Academorix\Compliance\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Compliance\Contracts\Data\DsarInterface;
use Academorix\Compliance\Database\Factories\DsarFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Mattiverse\Userstamps\Traits\Userstamps;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a Dsar.
 *
 * Data-Subject-Access-Request state machine.
 *
 * @category Compliance
 *
 * @since    0.1.0
 */
#[Table(name: DsarInterface::TABLE, keyType: DsarInterface::KEY_TYPE)]
#[Fillable([
    DsarInterface::ATTR_TENANT_ID,
        DsarInterface::ATTR_SUBJECT_TYPE,
        DsarInterface::ATTR_SUBJECT_ID,
        DsarInterface::ATTR_SUBJECT_EMAIL,
        DsarInterface::ATTR_ACTION,
        DsarInterface::ATTR_STATE,
        DsarInterface::ATTR_VERIFIED_AT,
        DsarInterface::ATTR_ASSIGNED_REVIEWER_ID,
        DsarInterface::ATTR_SUBMITTED_AT,
        DsarInterface::ATTR_SLA_DAYS,
        DsarInterface::ATTR_SLA_DEADLINE,
        DsarInterface::ATTR_COLLECTED_AT,
        DsarInterface::ATTR_ASSEMBLED_AT,
        DsarInterface::ATTR_DELIVERED_AT,
        DsarInterface::ATTR_REJECTED_AT,
        DsarInterface::ATTR_REJECTION_REASON,
        DsarInterface::ATTR_EXTENSION_REASON,
        DsarInterface::ATTR_NOTES,
        DsarInterface::ATTR_REQUEST_METADATA,
        DsarInterface::ATTR_SIGNATURE,
        DsarInterface::ATTR_METADATA,
])]
#[UseFactory(DsarFactory::class)]
final class Dsar extends Model implements DsarInterface
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    use HasMetadata;
    use HasUserstamps;
    // TODO(gen): resolve unknown trait `HasAuditable` — add its import + use line.
    use HasActivityLog;
    use Filterable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        DsarInterface::ATTR_ACTION => 'DsarAction',
        DsarInterface::ATTR_STATE => 'DsarState',
        DsarInterface::ATTR_VERIFIED_AT => 'datetime',
        DsarInterface::ATTR_SUBMITTED_AT => 'datetime',
        DsarInterface::ATTR_SLA_DEADLINE => 'datetime',
        DsarInterface::ATTR_COLLECTED_AT => 'datetime',
        DsarInterface::ATTR_ASSEMBLED_AT => 'datetime',
        DsarInterface::ATTR_DELIVERED_AT => 'datetime',
        DsarInterface::ATTR_REJECTED_AT => 'datetime',
        DsarInterface::ATTR_NOTES => 'array',
        DsarInterface::ATTR_REQUEST_METADATA => 'array',
        DsarInterface::ATTR_METADATA => 'array',
    ];
}
