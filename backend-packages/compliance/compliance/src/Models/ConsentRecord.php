<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py compliance compliance --force`.

declare(strict_types=1);

namespace Academorix\Compliance\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Compliance\Contracts\Data\ConsentRecordInterface;
use Academorix\Compliance\Database\Factories\ConsentRecordFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Mattiverse\Userstamps\Traits\Userstamps;

/**
 * Eloquent model for a ConsentRecord.
 *
 * Immutable subject × category × decision snapshot.
 *
 * @category Compliance
 *
 * @since    0.1.0
 */
#[Table(name: ConsentRecordInterface::TABLE, keyType: ConsentRecordInterface::KEY_TYPE)]
#[Fillable([
    ConsentRecordInterface::ATTR_TENANT_ID,
        ConsentRecordInterface::ATTR_SUBJECT_TYPE,
        ConsentRecordInterface::ATTR_SUBJECT_ID,
        ConsentRecordInterface::ATTR_CONSENT_CATEGORY_ID,
        ConsentRecordInterface::ATTR_DECISION,
        ConsentRecordInterface::ATTR_SOURCE,
        ConsentRecordInterface::ATTR_GUARDIAN_USER_ID,
        ConsentRecordInterface::ATTR_EVIDENCE,
        ConsentRecordInterface::ATTR_METADATA,
])]
#[UseFactory(ConsentRecordFactory::class)]
final class ConsentRecord extends Model implements ConsentRecordInterface
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    use HasMetadata;
    use HasUserstamps;
    // TODO(gen): resolve unknown trait `HasAuditable` — add its import + use line.
    use Filterable;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        ConsentRecordInterface::ATTR_DECISION => 'ConsentDecision',
        ConsentRecordInterface::ATTR_SOURCE => 'array',
        ConsentRecordInterface::ATTR_EVIDENCE => 'array',
        ConsentRecordInterface::ATTR_METADATA => 'array',
    ];
}
