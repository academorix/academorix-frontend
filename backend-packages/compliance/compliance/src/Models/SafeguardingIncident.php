<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py compliance compliance --force`.

declare(strict_types=1);

namespace Academorix\Compliance\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Compliance\Contracts\Data\SafeguardingIncidentInterface;
use Academorix\Compliance\Database\Factories\SafeguardingIncidentFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Mattiverse\Userstamps\Traits\Userstamps;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a SafeguardingIncident.
 *
 * Minor-safeguarding report inbox.
 *
 * @category Compliance
 *
 * @since    0.1.0
 */
#[Table(name: SafeguardingIncidentInterface::TABLE, keyType: SafeguardingIncidentInterface::KEY_TYPE)]
#[Fillable([
    SafeguardingIncidentInterface::ATTR_TENANT_ID,
        SafeguardingIncidentInterface::ATTR_SUBJECT_TYPE,
        SafeguardingIncidentInterface::ATTR_SUBJECT_ID,
        SafeguardingIncidentInterface::ATTR_SEVERITY,
        SafeguardingIncidentInterface::ATTR_STATE,
        SafeguardingIncidentInterface::ATTR_REPORTED_BY_USER_ID,
        SafeguardingIncidentInterface::ATTR_ASSIGNED_TO_USER_ID,
        SafeguardingIncidentInterface::ATTR_REPORTED_AT,
        SafeguardingIncidentInterface::ATTR_TRIAGED_AT,
        SafeguardingIncidentInterface::ATTR_INVESTIGATING_AT,
        SafeguardingIncidentInterface::ATTR_ESCALATED_AT,
        SafeguardingIncidentInterface::ATTR_RESOLVED_AT,
        SafeguardingIncidentInterface::ATTR_CLOSED_AT,
        SafeguardingIncidentInterface::ATTR_SLA_DEADLINE,
        SafeguardingIncidentInterface::ATTR_DESCRIPTION,
        SafeguardingIncidentInterface::ATTR_KEYWORDS_MATCHED,
        SafeguardingIncidentInterface::ATTR_NOTES,
        SafeguardingIncidentInterface::ATTR_EXTERNAL_REFERRAL,
        SafeguardingIncidentInterface::ATTR_PENDING_EXTERNAL_REFERRAL,
        SafeguardingIncidentInterface::ATTR_METADATA,
])]
#[UseFactory(SafeguardingIncidentFactory::class)]
final class SafeguardingIncident extends Model implements SafeguardingIncidentInterface
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    use HasMetadata;
    use HasUserstamps;
    // TODO(gen): resolve unknown trait `HasAuditable` — add its import + use line.
    use HasActivityLog;
    use Filterable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        SafeguardingIncidentInterface::ATTR_SEVERITY => 'SafeguardingSeverity',
        SafeguardingIncidentInterface::ATTR_STATE => 'SafeguardingIncidentState',
        SafeguardingIncidentInterface::ATTR_REPORTED_AT => 'datetime',
        SafeguardingIncidentInterface::ATTR_TRIAGED_AT => 'datetime',
        SafeguardingIncidentInterface::ATTR_INVESTIGATING_AT => 'datetime',
        SafeguardingIncidentInterface::ATTR_ESCALATED_AT => 'datetime',
        SafeguardingIncidentInterface::ATTR_RESOLVED_AT => 'datetime',
        SafeguardingIncidentInterface::ATTR_CLOSED_AT => 'datetime',
        SafeguardingIncidentInterface::ATTR_SLA_DEADLINE => 'datetime',
        SafeguardingIncidentInterface::ATTR_KEYWORDS_MATCHED => 'array',
        SafeguardingIncidentInterface::ATTR_NOTES => 'array',
        SafeguardingIncidentInterface::ATTR_EXTERNAL_REFERRAL => 'array',
        SafeguardingIncidentInterface::ATTR_PENDING_EXTERNAL_REFERRAL => 'boolean',
        SafeguardingIncidentInterface::ATTR_METADATA => 'array',
    ];
}
