<?php

/**
 * @file database/migrations/2026_07_15_120000_create_regions_table.php
 *
 * @description
 * Create the `regions` table for the `Region` entity.
 * Owned by the region module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform region --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('regions', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('name', 128);
            $table->string('slug', 64);
            $table->string('country_code', 2)->nullable();
            $table->string('subdivision_code', 8)->nullable();
            $table->string('currency', 3);
            $table->string('currency_symbol', 8)->default('');
            $table->string('timezone', 64);
            $table->string('locale', 16);
            $table->boolean('rtl')->default(false);
            $table->jsonb('weekend_days')->default('["saturday","sunday"]');
            $table->jsonb('tax_config')->default('{}');
            $table->integer('fiscal_year_start_month')->default(1);
            $table->string('status', 16)->default('active');
            $table->boolean('is_default')->default(false);
            $table->integer('sort_order')->default(100);
            $table->text('notes')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->string('deleted_by', 30)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'slug'], 'regions_tenant_slug_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->unique(['tenant_id'], 'regions_tenant_default_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_default = true AND deleted_at IS NULL
            $table->index(['tenant_id', 'status', 'sort_order'], 'regions_tenant_status_sort_idx');
            $table->index(['tenant_id', 'country_code'], 'regions_tenant_country_idx');
            $table->index(['tenant_id', 'currency'], 'regions_tenant_currency_idx');
            $table->index(['deleted_at'], 'regions_deleted_at_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('regions');
    }
};
