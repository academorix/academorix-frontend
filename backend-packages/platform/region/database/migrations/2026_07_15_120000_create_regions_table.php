<?php

/**
 * @file database/migrations/2026_07_15_120000_create_regions_table.php
 *
 * @description
 * Create the `regions` table for the `Region` entity.
 * Owned by the region module. Every column + index reference
 * goes through {@see Academorix\Region\Contracts\Data\RegionInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

use Academorix\Region\Contracts\Data\RegionInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(RegionInterface::TABLE, function (Blueprint $table): void {
            $table->string(RegionInterface::ATTR_ID, 30)->primary();
            $table->string(RegionInterface::ATTR_TENANT_ID, 30);
            $table->foreign(RegionInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(RegionInterface::ATTR_NAME, 128);
            $table->string(RegionInterface::ATTR_SLUG, 64);
            $table->string(RegionInterface::ATTR_COUNTRY_CODE, 2)->nullable();
            $table->string(RegionInterface::ATTR_SUBDIVISION_CODE, 8)->nullable();
            $table->string(RegionInterface::ATTR_CURRENCY, 3);
            $table->string(RegionInterface::ATTR_CURRENCY_SYMBOL, 8)->default('');
            $table->string(RegionInterface::ATTR_TIMEZONE, 64);
            $table->string(RegionInterface::ATTR_LOCALE, 16);
            $table->boolean(RegionInterface::ATTR_RTL)->default(false);
            $table->jsonb(RegionInterface::ATTR_WEEKEND_DAYS)->default('["saturday","sunday"]');
            $table->jsonb(RegionInterface::ATTR_TAX_CONFIG)->default('{}');
            $table->integer(RegionInterface::ATTR_FISCAL_YEAR_START_MONTH)->default(1);
            $table->string(RegionInterface::ATTR_STATUS, 16)->default('active');
            $table->boolean(RegionInterface::ATTR_IS_DEFAULT)->default(false);
            $table->integer(RegionInterface::ATTR_SORT_ORDER)->default(100);
            $table->text(RegionInterface::ATTR_NOTES)->nullable();
            $table->jsonb(RegionInterface::ATTR_METADATA)->nullable();
            $table->string(RegionInterface::ATTR_CREATED_BY, 30)->nullable();
            $table->string(RegionInterface::ATTR_UPDATED_BY, 30)->nullable();
            $table->string(RegionInterface::ATTR_DELETED_BY, 30)->nullable();
            $table->timestampTz(RegionInterface::ATTR_DELETED_AT)->nullable();
            $table->timestampTz(RegionInterface::ATTR_CREATED_AT);
            $table->timestampTz(RegionInterface::ATTR_UPDATED_AT);
            $table->index([RegionInterface::ATTR_TENANT_ID, RegionInterface::ATTR_STATUS, RegionInterface::ATTR_SORT_ORDER], 'regions_tenant_status_sort_idx');
            $table->index([RegionInterface::ATTR_TENANT_ID, RegionInterface::ATTR_COUNTRY_CODE], 'regions_tenant_country_idx');
            $table->index([RegionInterface::ATTR_TENANT_ID, RegionInterface::ATTR_CURRENCY], 'regions_tenant_currency_idx');
            $table->index([RegionInterface::ATTR_DELETED_AT], 'regions_deleted_at_idx');
        });

        DB::statement('CREATE UNIQUE INDEX regions_tenant_slug_unique ON ' . RegionInterface::TABLE . ' (' . RegionInterface::ATTR_TENANT_ID . ', ' . RegionInterface::ATTR_SLUG . ') WHERE deleted_at IS NULL');
        DB::statement('CREATE UNIQUE INDEX regions_tenant_default_unique ON ' . RegionInterface::TABLE . ' (' . RegionInterface::ATTR_TENANT_ID . ') WHERE is_default = true AND deleted_at IS NULL');
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(RegionInterface::TABLE);
    }
};
