<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Academorix\Region\Services;

use Academorix\Region\Contracts\Services\TaxConfigResolverInterface;
use Illuminate\Container\Attributes\Scoped;
use Academorix\Region\Contracts\Repositories\RegionRepositoryInterface;

/**
 * Concrete service — TaxConfigResolver.
 *
 * Implements {@see TaxConfigResolverInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Region
 *
 * @since    0.1.0
 */
#[Scoped]
final class TaxConfigResolver implements TaxConfigResolverInterface
{
    /**
     * @param  RegionRepositoryInterface  $regionRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly RegionRepositoryInterface $regionRepository,
    ) {
    }
}
