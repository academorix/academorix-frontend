<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Academorix\Region\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan region:create` — Create a new region.
 *
 * @category Region
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'region:create',
    description: 'Create a new region.',
)]
final class CreateCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Create a new region', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Create a new region completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
