<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Academorix\Region\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Region module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Region
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum RegionPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `regions.viewAny` — Every tenant role reads the region list — regions are UI reference data (every currency + timezone + tax dropdown reads them).
     */
    #[Label('Every tenant role reads the region list — regions are UI reference data (every c')]
    #[Description('Every tenant role reads the region list — regions are UI reference data (every currency + timezone + tax dropdown reads them).')]
    case RegionsViewAny = 'regions.viewAny';

    /**
     * `regions.viewAny.tenant` — Regions ViewAny Tenant
     */
    #[Label('Regions ViewAny Tenant')]
    #[Description('Regions ViewAny Tenant')]
    case RegionsViewAnyTenant = 'regions.viewAny.tenant';

    /**
     * `regions.view` — Single-row read.
     */
    #[Label('Single-row read')]
    #[Description('Single-row read. tax_config.registration_number is redacted unless the caller ALSO carries `regions.view.tax_registration`.')]
    case RegionsView = 'regions.view';

    /**
     * `regions.view.tax_registration` — Unredacted read of `tax_config.
     */
    #[Label('Unredacted read of `tax_config')]
    #[Description('Unredacted read of `tax_config.registration_number` (confidential-tier). Admin + owner only.')]
    case RegionsViewTaxRegistration = 'regions.view.tax_registration';

    /**
     * `regions.create` — Consumes 1 unit of the region_slot entitlement.
     */
    #[Label('Consumes 1 unit of the region_slot entitlement')]
    #[Description('Consumes 1 unit of the region_slot entitlement.')]
    case RegionsCreate = 'regions.create';

    /**
     * `regions.update` — Mutable fields: name, slug, timezone, locale, weekend_days, sort_order, subdivision_code, fiscal_year_start_month.
     */
    #[Label('Mutable fields: name, slug, timezone, locale, weekend_days, sort_order, subdivis')]
    #[Description('Mutable fields: name, slug, timezone, locale, weekend_days, sort_order, subdivision_code, fiscal_year_start_month. tax_config edits require the higher `regions.update.tax_config` permission.')]
    case RegionsUpdate = 'regions.update';

    /**
     * `regions.update.tax_config` — Separated from base update — tax_config mutations produce financial-record audit rows (7-year retention).
     */
    #[Label('Separated from base update — tax_config mutations produce financial-record audit')]
    #[Description('Separated from base update — tax_config mutations produce financial-record audit rows (7-year retention). Admin + owner.')]
    case RegionsUpdateTaxConfig = 'regions.update.tax_config';

    /**
     * `regions.set_default` — Flip is_default.
     */
    #[Label('Flip is_default')]
    #[Description('Flip is_default. Owner-only — the default region drives invoice calculations for legacy rows.')]
    case RegionsSetDefault = 'regions.set_default';

    /**
     * `regions.pause` — Status → paused.
     */
    #[Label('Status → paused')]
    #[Description('Status → paused. Refused when is_default=true.')]
    case RegionsPause = 'regions.pause';

    /**
     * `regions.resume` — Regions Resume
     */
    #[Label('Regions Resume')]
    #[Description('Regions Resume')]
    case RegionsResume = 'regions.resume';

    /**
     * `regions.archive` — Refused when is_default=true OR when inbound rows exist.
     */
    #[Label('Refused when is_default=true OR when inbound rows exist')]
    #[Description('Refused when is_default=true OR when inbound rows exist.')]
    case RegionsArchive = 'regions.archive';

    /**
     * `regions.restore` — Reverse of archive.
     */
    #[Label('Reverse of archive')]
    #[Description('Reverse of archive. Only within the 730-day retention window.')]
    case RegionsRestore = 'regions.restore';

    /**
     * `regions.delete` — Owner only.
     */
    #[Label('Owner only')]
    #[Description('Owner only. Same guards as archive — plus the hard-delete happens only via PurgeArchivedRegionsJob after retention window.')]
    case RegionsDelete = 'regions.delete';

    /**
     * `regions.manage` — Umbrella permission for the settings blueprint gate.
     */
    #[Label('Umbrella permission for the settings blueprint gate')]
    #[Description('Umbrella permission for the settings blueprint gate. Admin + owner.')]
    case RegionsManage = 'regions.manage';

    /**
     * `platform.regions.viewAny` — Platform Regions ViewAny
     */
    #[Label('Platform Regions ViewAny')]
    #[Description('Platform Regions ViewAny')]
    case PlatformRegionsViewAny = 'platform.regions.viewAny';

    /**
     * `platform.regions.view` — Platform Regions View
     */
    #[Label('Platform Regions View')]
    #[Description('Platform Regions View')]
    case PlatformRegionsView = 'platform.regions.view';

    /**
     * `platform.regions.view.tax_registration` — Support-tier does NOT carry unredacted tax registration reads.
     */
    #[Label('Support-tier does NOT carry unredacted tax registration reads')]
    #[Description('Support-tier does NOT carry unredacted tax registration reads.')]
    case PlatformRegionsViewTaxRegistration = 'platform.regions.view.tax_registration';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::RegionsViewAny => Guard::Sanctum,
            self::RegionsViewAnyTenant => Guard::Sanctum,
            self::RegionsView => Guard::Sanctum,
            self::RegionsViewTaxRegistration => Guard::Sanctum,
            self::RegionsCreate => Guard::Sanctum,
            self::RegionsUpdate => Guard::Sanctum,
            self::RegionsUpdateTaxConfig => Guard::Sanctum,
            self::RegionsSetDefault => Guard::Sanctum,
            self::RegionsPause => Guard::Sanctum,
            self::RegionsResume => Guard::Sanctum,
            self::RegionsArchive => Guard::Sanctum,
            self::RegionsRestore => Guard::Sanctum,
            self::RegionsDelete => Guard::Sanctum,
            self::RegionsManage => Guard::Sanctum,
            self::PlatformRegionsViewAny => Guard::PlatformAdmin,
            self::PlatformRegionsView => Guard::PlatformAdmin,
            self::PlatformRegionsViewTaxRegistration => Guard::PlatformAdmin,
        };
    }
}
