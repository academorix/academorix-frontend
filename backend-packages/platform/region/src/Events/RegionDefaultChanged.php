<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Academorix\Region\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The is_default flag flipped. Fires with the previous default's ID + the new default's ID. Audit-critical: the default region determines invoice calculations for regionless legacy rows.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchRegionDefaultChangedNotification (cannot-opt-out — financial calculation impact)
 * - finance::RecomputeDefaultAttributionForLegacyRows (Wave 4 — recomputes attribution for the small set of legacy rows that lack region_id)
 *
 * @category Region
 *
 * @since    0.1.0
 */
final readonly class RegionDefaultChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $fromRegionId  ULID | null (null when this is the tenant's first default — TenantDefaultRegionSeeded fires that path)
     * @param  string  $toRegionId  ULID
     * @param  string  $changedBy  ULID
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $fromRegionId,
        public readonly string $toRegionId,
        public readonly string $changedBy,
    ) {
    }
}
