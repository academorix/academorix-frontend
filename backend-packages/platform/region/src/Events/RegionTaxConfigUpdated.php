<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Academorix\Region\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Region.tax_config JSONB mutated. Any sub-field change (default_rate_percent, rates, name, inclusive, registration_number, fiscal_year_start_month) fires this event. Financial-record retention 7 years.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - activity::WriteToActivityLog
 * - finance::InvalidateTaxCalculationCache (Wave 4)
 * - finance::MaybeReplayFutureInvoices (Wave 4 — only when the caller carries `region_time_travel` entitlement)
 *
 * @category Region
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RegionTaxConfigUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $regionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $updatedBy  ULID
     * @param  array  $before  object (previous tax_config snapshot)
     * @param  array  $after  object (new tax_config snapshot)
     * @param  string  $changedFields  string[] (dotted paths, e.g. ['default_rate_percent', 'rates[0].rate_percent'])
     */
    public function __construct(
        public readonly string $regionId,
        public readonly string $tenantId,
        public readonly string $updatedBy,
        public readonly array $before,
        public readonly array $after,
        public readonly string $changedFields,
    ) {
    }
}
