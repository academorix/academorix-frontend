<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Academorix\Region\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Region soft-deleted OR hard-deleted. Distinct events not needed — the payload carries the mode.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Region
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RegionDeleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $regionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $deletedBy  ULID
     * @param  string  $mode  'soft' | 'hard'
     * @param  bool  $wasDefault  boolean (true means the tenant lost its default region — a system-invariant violation; the reconciler will alert)
     */
    public function __construct(
        public readonly string $regionId,
        public readonly string $tenantId,
        public readonly string $deletedBy,
        public readonly string $mode,
        public readonly bool $wasDefault,
    ) {
    }
}
