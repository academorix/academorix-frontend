<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Academorix\Region\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Any Region field mutated except status (RegionStatusChanged), is_default (RegionDefaultChanged), and tax_config (RegionTaxConfigUpdated). Fires for renames, timezone changes, locale changes, sort_order, weekend_days, subdivision_code.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Region
 *
 * @since    0.1.0
 */
final readonly class RegionUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $regionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $updatedBy  ULID
     * @param  string  $changedFields  string[] (e.g. ['name', 'timezone', 'weekend_days'])
     */
    public function __construct(
        public readonly string $regionId,
        public readonly string $tenantId,
        public readonly string $updatedBy,
        public readonly string $changedFields,
    ) {
    }
}
