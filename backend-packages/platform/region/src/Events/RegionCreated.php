<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Academorix\Region\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * RegionProvisioner completed successfully. Fires exactly once per (tenant_id, slug) pair.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchRegionCreatedNotification
 * - branch::InvalidateRegionListCache (Wave 2b — Branch caches allowed regions for its dropdowns)
 *
 * @category Region
 *
 * @since    0.1.0
 */
final readonly class RegionCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $regionId  ULID (reg_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $countryCode  string (ISO 3166-1 alpha-2)
     * @param  string  $currency  string (ISO 4217)
     * @param  string  $timezone  string (IANA identifier)
     * @param  string  $locale  string (BCP-47)
     * @param  bool  $isDefault  boolean
     * @param  string  $createdBy  ULID (usr_*) | null (null = system-provisioned on TenantProvisioned)
     * @param  string  $source  'tenant_provision' | 'admin_created' | 'legacy_seed'
     */
    public function __construct(
        public readonly string $regionId,
        public readonly string $tenantId,
        public readonly string $countryCode,
        public readonly string $currency,
        public readonly string $timezone,
        public readonly string $locale,
        public readonly bool $isDefault,
        public readonly string $createdBy,
        public readonly string $source,
    ) {
    }
}
