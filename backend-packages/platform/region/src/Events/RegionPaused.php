<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Academorix\Region\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Region.status transitioned active → paused. New bookings + new memberships referencing this Region are refused; existing rows survive unchanged.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchRegionPausedNotification (cannot-opt-out — new-booking block is ops-critical)
 * - branch::InvalidateRegionAvailabilityCache
 * - facility::InvalidateBookingWindowCache (Wave 2b)
 * - finance::PauseNewMembershipsInRegion (Wave 4)
 *
 * @category Region
 *
 * @since    0.1.0
 */
final readonly class RegionPaused implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $regionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $pausedBy  ULID
     * @param  string  $reason  string | null
     * @param  string  $expectedResumeAt  ISO 8601 timestamp | null
     */
    public function __construct(
        public readonly string $regionId,
        public readonly string $tenantId,
        public readonly string $pausedBy,
        public readonly string $reason,
        public readonly string $expectedResumeAt,
    ) {
    }
}
