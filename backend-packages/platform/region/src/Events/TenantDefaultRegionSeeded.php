<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Academorix\Region\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SeedDefaultRegionForTenantJob completed for a freshly-provisioned tenant. Fires exactly once per tenant lifetime (unless the tenant loses its default region + the reconciler re-seeds — that path fires a fresh event).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - branch::PopulateAllowedRegionsCacheOnFirstSeed
 *
 * @category Region
 *
 * @since    0.1.0
 */
final readonly class TenantDefaultRegionSeeded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $regionId  ULID
     * @param  string  $countryCode  string (resolved from tenant's registered country)
     * @param  string  $seededBy  'tenant_provisioning' | 'reconciler' | 'legacy_backfill_command'
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $regionId,
        public readonly string $countryCode,
        public readonly string $seededBy,
    ) {
    }
}
