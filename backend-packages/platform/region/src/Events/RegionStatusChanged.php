<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform region --force`.

declare(strict_types=1);

namespace Academorix\Region\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Region.status transitioned. Umbrella event fired alongside the specific transition event (RegionPaused / RegionResumed / RegionArchived / RegionRestored). Downstream consumers subscribe to whichever level of specificity they need.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Region
 *
 * @since    0.1.0
 */
final readonly class RegionStatusChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $regionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromStatus  'active' | 'paused' | 'archived'
     * @param  string  $toStatus  'active' | 'paused' | 'archived'
     * @param  string  $changedBy  ULID
     * @param  int  $reason  string | null (free-form ops note; internal-tier)
     */
    public function __construct(
        public readonly string $regionId,
        public readonly string $tenantId,
        public readonly string $fromStatus,
        public readonly string $toStatus,
        public readonly string $changedBy,
        public readonly int $reason,
    ) {
    }
}
