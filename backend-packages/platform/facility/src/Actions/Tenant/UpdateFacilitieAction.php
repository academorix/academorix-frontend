<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Actions\Tenant;

use Academorix\Facility\Contracts\Repositories\FacilityRepositoryInterface;
use Academorix\Facility\Data\FacilityData;
use Academorix\Facility\Data\Requests\UpdateFacilityRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/facilities/{facility}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Patch(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsController]
#[Patch('/api/v1/facilities/{facility}')]
final class UpdateFacilitieAction
{
    public function __construct(
        private readonly FacilityRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `facilitie` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateFacilityRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateFacilityRequestData $data): FacilityData
    {
        $model = $this->repository->update($id, $data->toArray());

        return FacilityData::from($model);
    }
}
