<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Actions\Tenant;

use Academorix\Facility\Contracts\Repositories\DayPassRepositoryInterface;
use Academorix\Facility\Data\DayPassData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Get;

/**
 * `GET /api/v1/day-passes/{dayPass}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Get(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsController]
#[Get('/api/v1/day-passes/{dayPass}')]
final class ShowDayPasseAction
{
    public function __construct(
        private readonly DayPassRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `day-passe` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): DayPassData
    {
        return DayPassData::from($this->repository->findOrFail($id));
    }
}
