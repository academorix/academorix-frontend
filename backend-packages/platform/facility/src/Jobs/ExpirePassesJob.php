<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Jobs;

use Illuminate\Bus\Queueable;
use Illuminate\Contracts\Queue\ShouldQueue;
use Illuminate\Foundation\Bus\Dispatchable;
use Illuminate\Queue\Attributes\Queue;
use Illuminate\Queue\Attributes\Timeout;
use Illuminate\Queue\Attributes\Tries;
use Illuminate\Queue\InteractsWithQueue;
use Illuminate\Queue\SerializesModels;

/**
 * Queued job — ExpirePassesJob.
 *
 * TODO(gen): describe the job's trigger, payload, and idempotency
 * contract. Emitted as a skeleton; hand-implement the `handle()` body.
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[Queue('facility')]
#[Timeout(120)]
#[Tries(3)]
final class ExpirePassesJob implements ShouldQueue
{
    use Dispatchable;
    use InteractsWithQueue;
    use Queueable;
    use SerializesModels;

    /**
     * Execute the job.
     *
     * TODO(gen): implement the job body.
     */
    public function handle(): void
    {
        // Hand-implement.
    }

    /**
     * Handle a job failure.
     */
    public function failed(\Throwable $e): void
    {
        // TODO(gen): compensate or notify. Never `throw` from here.
    }
}
