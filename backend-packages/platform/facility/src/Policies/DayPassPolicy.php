<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Policies;

use Academorix\User\Models\User;
use Academorix\Facility\Models\DayPass;

/**
 * Authorization policy for {@see \Academorix\Facility\Models\DayPass}.
 *
 * Wired via `#[UsePolicy]` on the Model.
 *
 * @category Facility
 *
 * @since    0.1.0
 */
final class DayPassPolicy
{
    /**
     * reception + branch_manager + admin see day_passes for their scoped branch(es); a user can see their own past passes via a separate my-passes endpoint
     *
     * @return bool
     */
    public function viewAny(User $user, DayPass $model): bool
    {
        return $user->can('day_passes.viewAny');
    }
    /**
     * issuer + issued_to_user (if named) + reception in same branch
     *
     * @return bool
     */
    public function view(User $user, DayPass $model): bool
    {
        return $user->can('day_passes.view');
    }
    /**
     * requires facilities_advanced entitlement (Enterprise-only)
     *
     * @return bool
     */
    public function issue(User $user, DayPass $model): bool
    {
        return $user->can('day_passes.issue');
    }
    /**
     * consume ability.
     *
     * @return bool
     */
    public function consume(User $user, DayPass $model): bool
    {
        return $user->can('day_passes.consume');
    }
    /**
     * must be within 30-day refund window per observer
     *
     * @return bool
     */
    public function refund(User $user, DayPass $model): bool
    {
        return $user->can('day_passes.refund');
    }
}
