<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Booker or receptionist called the check-in action. Sets checked_in_at on the booking. Distinct from BookingCompleted (check-in is at start-time; completion is at end-time + 15 min).
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class BookingCheckedIn implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $bookingId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $facilityId  ULID
     * @param  string  $checkedInAt  ISO 8601
     * @param  string  $checkedInByUserId  ULID
     */
    public function __construct(
        public readonly string $bookingId,
        public readonly string $tenantId,
        public readonly string $facilityId,
        public readonly string $checkedInAt,
        public readonly string $checkedInByUserId,
    ) {
    }
}
