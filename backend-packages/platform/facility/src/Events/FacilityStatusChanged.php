<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Facility.status transitioned. Values: active / maintenance / retired. `active → maintenance` is temporary; `active → retired` is terminal (fires FacilityRetired in parallel).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - facility::CancelFutureBookingsOnMaintenance (Wave 2b — dispatched only on maintenance transition; retired path is a separate flow)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class FacilityStatusChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $facilityId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $branchId  ULID
     * @param  int  $fromStatus  'active' | 'maintenance' | 'retired'
     * @param  int  $toStatus  'active' | 'maintenance' | 'retired'
     * @param  string  $changedBy  ULID
     * @param  string  $reason  string | null
     */
    public function __construct(
        public readonly string $facilityId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly int $fromStatus,
        public readonly int $toStatus,
        public readonly string $changedBy,
        public readonly string $reason,
    ) {
    }
}
