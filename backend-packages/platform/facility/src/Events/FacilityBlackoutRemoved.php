<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A blackout window was removed from Facility.blackout_dates. Does NOT restore cancelled bookings — those are gone.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class FacilityBlackoutRemoved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $facilityId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $blackoutId  string
     * @param  string  $removedBy  ULID
     * @param  string  $wasStartsAt  ISO 8601
     * @param  string  $wasEndsAt  ISO 8601
     */
    public function __construct(
        public readonly string $facilityId,
        public readonly string $tenantId,
        public readonly string $blackoutId,
        public readonly string $removedBy,
        public readonly string $wasStartsAt,
        public readonly string $wasEndsAt,
    ) {
    }
}
