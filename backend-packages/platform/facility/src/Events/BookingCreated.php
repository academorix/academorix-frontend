<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ResourceBookingObserver committed a new resource_bookings row. Fires ONCE per booking. Precedes BookingConfirmed for pending-status bookings; for auto-confirmed bookings the two events fire in the same transaction.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchBookingConfirmedNotification (when status='confirmed')
 * - notifications::DispatchBookingReminderNotification (schedules the 24h and 1h reminders)
 * - finance::AttributePendingRevenue (Wave 4 — for status='confirmed' + price > 0)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
final readonly class BookingCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $bookingId  ULID (bkg_*)
     * @param  string  $tenantId  ULID
     * @param  string  $facilityId  ULID
     * @param  string  $bookedByUserId  ULID
     * @param  string  $bookedForUserId  ULID | null
     * @param  string  $bookedForTeamId  ULID | null (Wave 3+)
     * @param  string  $startsAt  ISO 8601
     * @param  string  $endsAt  ISO 8601
     * @param  int  $durationMinutes  integer
     * @param  string  $status  'pending' | 'confirmed'
     * @param  int  $priceAmountCents  integer
     * @param  string  $priceCurrency  string (ISO 4217)
     * @param  string  $paymentStatus  'none' | 'pending' | 'paid'
     * @param  int  $attendeesCount  integer
     * @param  string  $purpose  string | null
     */
    public function __construct(
        public readonly string $bookingId,
        public readonly string $tenantId,
        public readonly string $facilityId,
        public readonly string $bookedByUserId,
        public readonly string $bookedForUserId,
        public readonly string $bookedForTeamId,
        public readonly string $startsAt,
        public readonly string $endsAt,
        public readonly int $durationMinutes,
        public readonly string $status,
        public readonly int $priceAmountCents,
        public readonly string $priceCurrency,
        public readonly string $paymentStatus,
        public readonly int $attendeesCount,
        public readonly string $purpose,
    ) {
    }
}
