<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Facility.availability_json mutated. Distinct from FacilityBlackout* (recurring weekly hours vs one-off windows). Observer refuses saves whose new availability window is broader than the parent Branch.opening_hours.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateFacilityAvailabilityCache
 *
 * @category Facility
 *
 * @since    0.1.0
 */
final readonly class FacilityAvailabilityChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $facilityId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $updatedBy  ULID
     * @param  array  $before  object (previous availability_json)
     * @param  array  $after  object (new availability_json)
     */
    public function __construct(
        public readonly string $facilityId,
        public readonly string $tenantId,
        public readonly string $updatedBy,
        public readonly array $before,
        public readonly array $after,
    ) {
    }
}
