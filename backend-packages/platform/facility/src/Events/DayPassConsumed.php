<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * DayPass.consumed_at set from null → now(). Fires on turnstile scan or receptionist manual confirmation.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Facility
 *
 * @since    0.1.0
 */
final readonly class DayPassConsumed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $dayPassId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $branchId  ULID
     * @param  string  $consumedAt  ISO 8601
     * @param  string  $consumedByUserId  ULID | null (null = automated turnstile scan)
     */
    public function __construct(
        public readonly string $dayPassId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $consumedAt,
        public readonly string $consumedByUserId,
    ) {
    }
}
