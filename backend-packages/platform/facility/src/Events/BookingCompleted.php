<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ResourceBooking.status transitioned confirmed → completed. Fired ONLY by the automated `MarkBookingCompletedJob` when ends_at + 15 min has passed AND the booking wasn't cancelled or no-show. Never fires from a manual admin action (observer refuses manual transitions to completed).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - finance::RecognizeBookingRevenue (Wave 4 — moves pending revenue to recognised)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
final readonly class BookingCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $bookingId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $facilityId  ULID
     * @param  string  $completedAt  ISO 8601
     * @param  bool  $wasCheckedIn  boolean
     */
    public function __construct(
        public readonly string $bookingId,
        public readonly string $tenantId,
        public readonly string $facilityId,
        public readonly string $completedAt,
        public readonly bool $wasCheckedIn,
    ) {
    }
}
