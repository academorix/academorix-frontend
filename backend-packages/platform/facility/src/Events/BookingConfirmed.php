<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ResourceBooking.status transitioned pending → confirmed. Fires when the payment/gate approval flow (Wave 4 Finance approval, or immediate on free bookings) resolves. Also fires directly at BookingCreated time for auto-confirmed bookings — the two events are dispatched in the same afterCommit hook, in that order.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchBookingConfirmedNotification
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class BookingConfirmed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $bookingId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $facilityId  ULID
     * @param  string  $confirmedAt  ISO 8601
     * @param  int  $wasPendingForSeconds  integer (created_at → confirmed_at latency)
     */
    public function __construct(
        public readonly string $bookingId,
        public readonly string $tenantId,
        public readonly string $facilityId,
        public readonly string $confirmedAt,
        public readonly int $wasPendingForSeconds,
    ) {
    }
}
