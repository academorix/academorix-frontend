<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ResourceBooking.status transitioned to `no_show`. Fired by the automated `MarkNoShowJob` when starts_at + 15 min has passed with no check-in AND the booking is still confirmed. Also fired by manual admin action.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchBookingNoShowRecordedNotification (to the booker for accountability)
 * - finance::MaybeRetainPayment (Wave 4 — most tenants keep the full charge on no-show)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
final readonly class BookingMarkedNoShow implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $bookingId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $facilityId  ULID
     * @param  string  $startsAt  ISO 8601
     * @param  string  $markedAt  ISO 8601
     * @param  string  $markedByUserId  ULID | null (null = automated job)
     * @param  string  $trigger  'automated_job' | 'manual_admin'
     */
    public function __construct(
        public readonly string $bookingId,
        public readonly string $tenantId,
        public readonly string $facilityId,
        public readonly string $startsAt,
        public readonly string $markedAt,
        public readonly string $markedByUserId,
        public readonly string $trigger,
    ) {
    }
}
