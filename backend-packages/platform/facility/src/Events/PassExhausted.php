<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Pass.used_credits reached Pass.total_credits. Fires from PassObserver.updated when the increment crosses the threshold. NOT fired on subsequent increments (there won't be any — the observer refuses over-consumption).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchPassExhaustedNotification (opt-in to the pass owner)
 * - finance::MaybeAutoRenew (Wave 4 — for renewing memberships)
 *
 * @category Facility
 *
 * @since    0.1.0
 */
final readonly class PassExhausted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $passId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $totalCredits  integer
     * @param  string  $exhaustedAt  ISO 8601
     */
    public function __construct(
        public readonly string $passId,
        public readonly string $tenantId,
        public readonly int $totalCredits,
        public readonly string $exhaustedAt,
    ) {
    }
}
