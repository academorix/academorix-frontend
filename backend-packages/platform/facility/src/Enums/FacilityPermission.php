<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Facility module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Facility
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum FacilityPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `facilities.viewAny` — Facilities ViewAny
     */
    #[Label('Facilities ViewAny')]
    #[Description('Facilities ViewAny')]
    case FacilitiesViewAny = 'facilities.viewAny';

    /**
     * `facilities.view` — Facilities View
     */
    #[Label('Facilities View')]
    #[Description('Facilities View')]
    case FacilitiesView = 'facilities.view';

    /**
     * `facilities.create` — Facilities Create
     */
    #[Label('Facilities Create')]
    #[Description('Facilities Create')]
    case FacilitiesCreate = 'facilities.create';

    /**
     * `facilities.update` — Facilities Update
     */
    #[Label('Facilities Update')]
    #[Description('Facilities Update')]
    case FacilitiesUpdate = 'facilities.update';

    /**
     * `facilities.delete` — Facilities Delete
     */
    #[Label('Facilities Delete')]
    #[Description('Facilities Delete')]
    case FacilitiesDelete = 'facilities.delete';

    /**
     * `facilities.retire` — Transition facility to retired status — terminal, cascades future bookings when combined with --cascade flag.
     */
    #[Label('Transition facility to retired status — terminal, cascades future bookings when')]
    #[Description('Transition facility to retired status — terminal, cascades future bookings when combined with --cascade flag.')]
    case FacilitiesRetire = 'facilities.retire';

    /**
     * `facilities.manage-blackouts` — Facilities Manage-blackouts
     */
    #[Label('Facilities Manage-blackouts')]
    #[Description('Facilities Manage-blackouts')]
    case FacilitiesManageBlackouts = 'facilities.manage-blackouts';

    /**
     * `facilities.manage-pricing` — Edit pricing_json.
     */
    #[Label('Edit pricing_json')]
    #[Description('Edit pricing_json. Financial-audit-critical; scoped to admins + owners.')]
    case FacilitiesManagePricing = 'facilities.manage-pricing';

    /**
     * `facilities.manage-availability` — Facilities Manage-availability
     */
    #[Label('Facilities Manage-availability')]
    #[Description('Facilities Manage-availability')]
    case FacilitiesManageAvailability = 'facilities.manage-availability';

    /**
     * `facilities.view-audit-usage` — See utilisation reports (hourly occupancy, revenue per facility).
     */
    #[Label('See utilisation reports (hourly occupancy, revenue per facility)')]
    #[Description('See utilisation reports (hourly occupancy, revenue per facility).')]
    case FacilitiesViewAuditUsage = 'facilities.view-audit-usage';

    /**
     * `bookings.viewAny` — See bookings owned by the caller.
     */
    #[Label('See bookings owned by the caller')]
    #[Description('See bookings owned by the caller. Admins additionally see all bookings via the ResourceBookingPolicy scope hook.')]
    case BookingsViewAny = 'bookings.viewAny';

    /**
     * `bookings.view-all` — See EVERY booking in the tenant (staff / support tooling).
     */
    #[Label('See EVERY booking in the tenant (staff / support tooling)')]
    #[Description('See EVERY booking in the tenant (staff / support tooling). Distinct from bookings.viewAny (own-only).')]
    case BookingsViewAll = 'bookings.view-all';

    /**
     * `bookings.view` — Bookings View
     */
    #[Label('Bookings View')]
    #[Description('Bookings View')]
    case BookingsView = 'bookings.view';

    /**
     * `bookings.create` — Book a facility.
     */
    #[Label('Book a facility')]
    #[Description('Book a facility. Scoped by ResolveScope middleware — a coach with scope \'branch:X\' can only book facilities in branch X.')]
    case BookingsCreate = 'bookings.create';

    /**
     * `bookings.create.for-other` — Book a facility on behalf of ANOTHER user (booked_for_user_id != booked_by_user_id).
     */
    #[Label('Book a facility on behalf of ANOTHER user (booked_for_user_id != booked_by_user_')]
    #[Description('Book a facility on behalf of ANOTHER user (booked_for_user_id != booked_by_user_id). Parents booking for children, coaches for team members, receptionists for walk-ins.')]
    case BookingsCreateForOther = 'bookings.create.for-other';

    /**
     * `bookings.cancel` — Cancel a booking owned by the caller.
     */
    #[Label('Cancel a booking owned by the caller')]
    #[Description('Cancel a booking owned by the caller.')]
    case BookingsCancel = 'bookings.cancel';

    /**
     * `bookings.cancel-any` — Cancel any booking, including bypassing the cancellation_notice_hours window.
     */
    #[Label('Cancel any booking, including bypassing the cancellation_notice_hours window')]
    #[Description('Cancel any booking, including bypassing the cancellation_notice_hours window.')]
    case BookingsCancelAny = 'bookings.cancel-any';

    /**
     * `bookings.reschedule` — Bookings Reschedule
     */
    #[Label('Bookings Reschedule')]
    #[Description('Bookings Reschedule')]
    case BookingsReschedule = 'bookings.reschedule';

    /**
     * `bookings.mark-no-show` — Manually mark a booking as no-show before the automated job runs.
     */
    #[Label('Manually mark a booking as no-show before the automated job runs')]
    #[Description('Manually mark a booking as no-show before the automated job runs.')]
    case BookingsMarkNoShow = 'bookings.mark-no-show';

    /**
     * `bookings.check-in` — Set checked_in_at on a booking.
     */
    #[Label('Set checked_in_at on a booking')]
    #[Description('Set checked_in_at on a booking. Reception staff + admins.')]
    case BookingsCheckIn = 'bookings.check-in';

    /**
     * `bookings.complete` — Reserved: manual completion is refused by the observer; this permission exists so downstream Wave 4 refund flows can grant it to specific ops staff.
     */
    #[Label('Reserved: manual completion is refused by the observer; this permission exists s')]
    #[Description('Reserved: manual completion is refused by the observer; this permission exists so downstream Wave 4 refund flows can grant it to specific ops staff.')]
    case BookingsComplete = 'bookings.complete';

    /**
     * `day_passes.viewAny` — Day Passes ViewAny
     */
    #[Label('Day Passes ViewAny')]
    #[Description('Day Passes ViewAny')]
    case DayPassesViewAny = 'day_passes.viewAny';

    /**
     * `day_passes.view` — Day Passes View
     */
    #[Label('Day Passes View')]
    #[Description('Day Passes View')]
    case DayPassesView = 'day_passes.view';

    /**
     * `day_passes.issue` — POS surface.
     */
    #[Label('POS surface')]
    #[Description('POS surface. Reception staff sell day passes.')]
    case DayPassesIssue = 'day_passes.issue';

    /**
     * `day_passes.consume` — Turnstile / receptionist marks a day pass consumed.
     */
    #[Label('Turnstile / receptionist marks a day pass consumed')]
    #[Description('Turnstile / receptionist marks a day pass consumed.')]
    case DayPassesConsume = 'day_passes.consume';

    /**
     * `day_passes.refund` — Reverse a day-pass sale within the 30-day refund window.
     */
    #[Label('Reverse a day-pass sale within the 30-day refund window')]
    #[Description('Reverse a day-pass sale within the 30-day refund window.')]
    case DayPassesRefund = 'day_passes.refund';

    /**
     * `passes.viewAny` — Passes ViewAny
     */
    #[Label('Passes ViewAny')]
    #[Description('Passes ViewAny')]
    case PassesViewAny = 'passes.viewAny';

    /**
     * `passes.view` — Passes View
     */
    #[Label('Passes View')]
    #[Description('Passes View')]
    case PassesView = 'passes.view';

    /**
     * `passes.create` — Manually issue a pass (Wave 4 will auto-issue on Membership provisioning; manual creates cover ad-hoc cases).
     */
    #[Label('Manually issue a pass (Wave 4 will auto-issue on Membership provisioning; manual')]
    #[Description('Manually issue a pass (Wave 4 will auto-issue on Membership provisioning; manual creates cover ad-hoc cases).')]
    case PassesCreate = 'passes.create';

    /**
     * `passes.update` — Passes Update
     */
    #[Label('Passes Update')]
    #[Description('Passes Update')]
    case PassesUpdate = 'passes.update';

    /**
     * `passes.transfer` — Change assignable_user_ids on a transferable pass.
     */
    #[Label('Change assignable_user_ids on a transferable pass')]
    #[Description('Change assignable_user_ids on a transferable pass.')]
    case PassesTransfer = 'passes.transfer';

    /**
     * `passes.revoke` — Terminate a pass early (soft-delete + revocation_reason).
     */
    #[Label('Terminate a pass early (soft-delete + revocation_reason)')]
    #[Description('Terminate a pass early (soft-delete + revocation_reason).')]
    case PassesRevoke = 'passes.revoke';

    /**
     * `platform.facilities.viewAny` — Platform Facilities ViewAny
     */
    #[Label('Platform Facilities ViewAny')]
    #[Description('Platform Facilities ViewAny')]
    case PlatformFacilitiesViewAny = 'platform.facilities.viewAny';

    /**
     * `platform.facilities.view` — Platform Facilities View
     */
    #[Label('Platform Facilities View')]
    #[Description('Platform Facilities View')]
    case PlatformFacilitiesView = 'platform.facilities.view';

    /**
     * `platform.facilities.view-audit-usage` — Cross-tenant utilisation report for support tooling + capacity planning.
     */
    #[Label('Cross-tenant utilisation report for support tooling + capacity planning')]
    #[Description('Cross-tenant utilisation report for support tooling + capacity planning.')]
    case PlatformFacilitiesViewAuditUsage = 'platform.facilities.view-audit-usage';

    /**
     * `platform.bookings.viewAny` — Platform Bookings ViewAny
     */
    #[Label('Platform Bookings ViewAny')]
    #[Description('Platform Bookings ViewAny')]
    case PlatformBookingsViewAny = 'platform.bookings.viewAny';

    /**
     * `platform.bookings.force-cancel` — Support intervention to cancel a booking bypassing every tenant-level guard.
     */
    #[Label('Support intervention to cancel a booking bypassing every tenant-level guard')]
    #[Description('Support intervention to cancel a booking bypassing every tenant-level guard. Every fire is audit-critical + notifies the tenant admin.')]
    case PlatformBookingsForceCancel = 'platform.bookings.force-cancel';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::FacilitiesViewAny => Guard::Sanctum,
            self::FacilitiesView => Guard::Sanctum,
            self::FacilitiesCreate => Guard::Sanctum,
            self::FacilitiesUpdate => Guard::Sanctum,
            self::FacilitiesDelete => Guard::Sanctum,
            self::FacilitiesRetire => Guard::Sanctum,
            self::FacilitiesManageBlackouts => Guard::Sanctum,
            self::FacilitiesManagePricing => Guard::Sanctum,
            self::FacilitiesManageAvailability => Guard::Sanctum,
            self::FacilitiesViewAuditUsage => Guard::Sanctum,
            self::BookingsViewAny => Guard::Sanctum,
            self::BookingsViewAll => Guard::Sanctum,
            self::BookingsView => Guard::Sanctum,
            self::BookingsCreate => Guard::Sanctum,
            self::BookingsCreateForOther => Guard::Sanctum,
            self::BookingsCancel => Guard::Sanctum,
            self::BookingsCancelAny => Guard::Sanctum,
            self::BookingsReschedule => Guard::Sanctum,
            self::BookingsMarkNoShow => Guard::Sanctum,
            self::BookingsCheckIn => Guard::Sanctum,
            self::BookingsComplete => Guard::Sanctum,
            self::DayPassesViewAny => Guard::Sanctum,
            self::DayPassesView => Guard::Sanctum,
            self::DayPassesIssue => Guard::Sanctum,
            self::DayPassesConsume => Guard::Sanctum,
            self::DayPassesRefund => Guard::Sanctum,
            self::PassesViewAny => Guard::Sanctum,
            self::PassesView => Guard::Sanctum,
            self::PassesCreate => Guard::Sanctum,
            self::PassesUpdate => Guard::Sanctum,
            self::PassesTransfer => Guard::Sanctum,
            self::PassesRevoke => Guard::Sanctum,
            self::PlatformFacilitiesViewAny => Guard::PlatformAdmin,
            self::PlatformFacilitiesView => Guard::PlatformAdmin,
            self::PlatformFacilitiesViewAuditUsage => Guard::PlatformAdmin,
            self::PlatformBookingsViewAny => Guard::PlatformAdmin,
            self::PlatformBookingsForceCancel => Guard::PlatformAdmin,
        };
    }
}
