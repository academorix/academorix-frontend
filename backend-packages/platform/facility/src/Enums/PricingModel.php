<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

namespace Academorix\Facility\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `model` on `pricing`.
 *
 * Backed enum — one case per allowed value.
 *
 * @category Facility
 *
 * @since    0.1.0
 */
enum PricingModel: string
{
    use Enum;

    case PerHour = 'per_hour';
    case PerSlot = 'per_slot';
    case PerPerson = 'per_person';
    case IncludedInMembership = 'included_in_membership';
}
