<?php

/**
 * @file database/migrations/2026_07_15_120005_create_resource_bookings_table.php
 *
 * @description
 * Create the `resource_bookings` table for the `ResourceBooking` entity.
 * Owned by the facility module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform facility --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform facility --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('resource_bookings', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('facility_id', 64);
            $table->foreign('facility_id')->references('id')->on('facilities')->onDelete('restrict');
            $table->string('booked_by_user_id', 64);
            $table->foreign('booked_by_user_id')->references('id')->on('users')->onDelete('restrict');
            $table->string('booked_for_user_id', 64)->nullable();
            $table->foreign('booked_for_user_id')->references('id')->on('users')->onDelete('restrict');
            $table->string('booked_for_team_id', 64)->nullable();
            $table->string('purpose', 500)->nullable();
            $table->timestampTz('starts_at');
            $table->timestampTz('ends_at');
            $table->integer('duration_minutes');
            $table->string('status', 16)->default('pending');
            $table->text('cancellation_reason')->nullable();
            $table->timestampTz('cancelled_at')->nullable();
            $table->string('cancelled_by_user_id', 64)->nullable();
            $table->foreign('cancelled_by_user_id')->references('id')->on('users')->onDelete('restrict');
            $table->timestampTz('checked_in_at')->nullable();
            $table->timestampTz('completed_at')->nullable();
            $table->integer('price_amount_cents')->default(0);
            $table->string('price_currency', 3);
            $table->string('payment_status', 16)->default('none');
            $table->integer('attendees_count')->default(1);
            $table->text('notes')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 64)->nullable();
            $table->string('updated_by', 64)->nullable();
            $table->string('deleted_by', 64)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->index(['facility_id', 'starts_at', 'ends_at'], 'resource_bookings_overlap_idx');
            // Partial-index predicate — hand-add via DB::statement: WHERE status IN ('pending','confirmed') AND deleted_at IS NULL
            $table->index(['tenant_id', 'booked_by_user_id', 'starts_at'], 'resource_bookings_by_booker_idx');
            $table->index(['tenant_id', 'booked_for_user_id', 'starts_at'], 'resource_bookings_by_bookedfor_idx');
            $table->index(['tenant_id', 'status', 'starts_at'], 'resource_bookings_status_idx');
            $table->index(['tenant_id', 'created_at'], 'resource_bookings_recent_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('resource_bookings');
    }
};
