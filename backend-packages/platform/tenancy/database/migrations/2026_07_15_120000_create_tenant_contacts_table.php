<?php

/**
 * @file database/migrations/2026_07_15_120000_create_tenant_contacts_table.php
 *
 * @description
 * Create the `tenant_contacts` table for the `TenantContact` entity.
 * Owned by the tenancy module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform tenancy --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform tenancy --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('tenant_contacts', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('kind', 16);
            $table->string('name', 200);
            $table->string('email', 320);
            $table->string('phone', 32)->nullable();
            $table->string('job_title', 200)->nullable();
            $table->jsonb('address')->nullable();
            $table->text('notes')->nullable();
            $table->boolean('is_primary')->default(false);
            $table->timestampTz('verified_at')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'kind', 'is_primary'], 'tenant_contacts_primary_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_primary = TRUE AND deleted_at IS NULL
            $table->index(['tenant_id', 'kind'], 'tenant_contacts_tenant_kind_index');
            $table->index(['email'], 'tenant_contacts_email_index');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('tenant_contacts');
    }
};
