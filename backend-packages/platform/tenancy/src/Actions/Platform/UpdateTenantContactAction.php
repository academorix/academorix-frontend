<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform tenancy --force`.

declare(strict_types=1);

namespace Academorix\Tenancy\Actions\Platform;

use Academorix\Tenancy\Contracts\Repositories\TenantContactRepositoryInterface;
use Academorix\Tenancy\Data\TenantContactData;
use Academorix\Tenancy\Data\Requests\UpdateTenantContactRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/tenant-contacts/{id}` — update action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Patch(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Tenancy
 *
 * @since    0.1.0
 */
#[AsController]
#[Patch('/api/v1/tenant-contacts/{id}')]
final class UpdateTenantContactAction
{
    public function __construct(
        private readonly TenantContactRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `tenant-contact` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateTenantContactRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateTenantContactRequestData $data): TenantContactData
    {
        $model = $this->repository->update($id, $data->toArray());

        return TenantContactData::from($model);
    }
}
