<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform tenancy --force`.

declare(strict_types=1);

namespace Academorix\Tenancy\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Tenancy\Contracts\Data\TenantInterface;
use Academorix\Tenancy\Database\Factories\TenantFactory;
use Academorix\Application\Concerns\BelongsToApplication;
use Academorix\Foundation\Concerns\HasMetadata;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Mattiverse\Userstamps\Traits\Userstamps;

/**
 * Eloquent model for a Tenant.
 *
 * The tenant / customer record.
 *
 * @category Tenancy
 *
 * @since    0.1.0
 */
#[Table(name: TenantInterface::TABLE, keyType: TenantInterface::KEY_TYPE)]
#[Fillable([
    TenantInterface::ATTR_APPLICATION_ID,
        TenantInterface::ATTR_PRIMARY_BRANDING_ID,
        TenantInterface::ATTR_SLUG,
        TenantInterface::ATTR_NAME,
        TenantInterface::ATTR_BUSINESS_TYPE,
        TenantInterface::ATTR_STATUS,
        TenantInterface::ATTR_BRANDING,
        TenantInterface::ATTR_PROVISIONING,
        TenantInterface::ATTR_METADATA,
        TenantInterface::ATTR_IS_DEFAULT,
        TenantInterface::ATTR_IS_SYSTEM,
        TenantInterface::ATTR_TRIAL_ENDS_AT,
        TenantInterface::ATTR_SUSPENDED_AT,
        TenantInterface::ATTR_GRACE_ENDS_AT,
])]
#[UseFactory(TenantFactory::class)]
final class Tenant extends Model implements TenantInterface
{
    use HasFactory;
    use HasUlids;
    use BelongsToApplication;
    // TODO(gen): resolve unknown trait `HasSystemFlag` — add its import + use line.
    use HasMetadata;
    use HasUserstamps;
    // TODO(gen): resolve unknown trait `HasAuditable` — add its import + use line.
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        TenantInterface::ATTR_BRANDING => 'array',
        TenantInterface::ATTR_PROVISIONING => 'array',
        TenantInterface::ATTR_METADATA => 'array',
        TenantInterface::ATTR_IS_DEFAULT => 'boolean',
        TenantInterface::ATTR_IS_SYSTEM => 'boolean',
        TenantInterface::ATTR_TRIAL_ENDS_AT => 'datetime',
        TenantInterface::ATTR_SUSPENDED_AT => 'datetime',
        TenantInterface::ATTR_GRACE_ENDS_AT => 'datetime',
    ];
}
