<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform tenancy --force`.

declare(strict_types=1);

namespace Academorix\Tenancy\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * TenantContactSettings — Contacts.
 *
 * Named-contact + notification preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Tenancy
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'tenant_contacts',
    label: 'Contacts',
    description: 'Named-contact + notification preferences.',
    icon: 'user-group',
    scope: 'tenant',
    permission: 'tenants.manage_contacts',
    sortOrder: 4,
)]
class TenantContactSettings extends Settings
{

    // ── DPO policy ──────────────────────────────────
    #[SettingGroup(label: 'DPO policy', description: 'GDPR Art. 37 — recommended for tenants processing EU data subjects.', icon: 'shield-check', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Require verified DPO before activation', validation: ['boolean'], group: 'DPO policy', sortOrder: 1)]
    public bool $require_dpo_before_activation = false;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me when a contact\'s verification expires', validation: ['boolean'], group: 'DPO policy', sortOrder: 2)]
    public bool $notify_expiring_verifications = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'tenant_contacts';
    }
}
