<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform tenancy --force`.

declare(strict_types=1);

namespace Academorix\Tenancy\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * TenantDomainSettings — Custom domains.
 *
 * Custom domain preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Tenancy
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'tenant_domains',
    label: 'Custom domains',
    description: 'Custom domain preferences.',
    icon: 'globe-alt',
    scope: 'tenant',
    permission: 'tenants.manage_domains',
    sortOrder: 2,
)]
class TenantDomainSettings extends Settings
{

    // ── Domain policy ──────────────────────────────────
    #[SettingGroup(label: 'Domain policy', icon: 'shield-check', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Allow custom domains', validation: ['boolean'], group: 'Domain policy', sortOrder: 1)]
    public bool $allow_custom_domains = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Enforce HTTPS', validation: ['boolean'], group: 'Domain policy', sortOrder: 2, helpText: 'Redirect http:// to https:// for custom domains.')]
    public bool $enforce_https = true;

    #[SettingField(controlType: ControlType::Number, label: 'Re-verify interval (hours)', validation: ['required', 'integer', 'min:1', 'max:168'], group: 'Domain policy', sortOrder: 3, min: 1, max: 168)]
    public int $auto_verify_interval_hours = 24;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'tenant_domains';
    }
}
