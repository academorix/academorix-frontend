<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform tenancy --force`.

declare(strict_types=1);

namespace Academorix\Tenancy\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * TenantSettings — Tenant.
 *
 * Basic tenant preferences — timezone, locale, currency, formatting.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Tenancy
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'tenant',
    label: 'Tenant',
    description: 'Basic tenant preferences — timezone, locale, currency, formatting.',
    icon: 'building-office-2',
    scope: 'tenant',
    permission: 'tenants.manage_own_settings',
    sortOrder: 1,
)]
class TenantSettings extends Settings
{

    // ── Locale + region ──────────────────────────────────
    #[SettingGroup(label: 'Locale + region', description: 'Applied to every session, report, and schedule surface.', icon: 'globe-europe-africa', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Timezone, label: 'Timezone', validation: ['required', 'timezone'], group: 'Locale + region', sortOrder: 1, helpText: 'IANA identifier. Source of truth: Tenant.provisioning.timezone.')]
    public string $timezone = 'UTC';

    #[SettingField(controlType: ControlType::Locale, label: 'Language', validation: ['required', 'string', 'max:8'], group: 'Locale + region', sortOrder: 2)]
    public string $locale = 'en';

    #[SettingField(controlType: ControlType::Currency, label: 'Currency', validation: ['required', 'string', 'size:3'], group: 'Locale + region', sortOrder: 3)]
    public string $currency = 'USD';


    // ── Formatting ──────────────────────────────────
    #[SettingGroup(label: 'Formatting', icon: 'calendar-days', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Select, label: 'First day of week', validation: ['required', 'string', 'in:monday,sunday,saturday'], group: 'Formatting', sortOrder: 1, options: ['monday' => 'Monday', 'sunday' => 'Sunday', 'saturday' => 'Saturday'])]
    public string $first_day_of_week = 'monday';

    #[SettingField(controlType: ControlType::Select, label: 'Date format', validation: ['required', 'string', 'in:iso,us,eu,long'], group: 'Formatting', sortOrder: 2, options: ['iso' => 'ISO (2026-07-14)', 'us' => 'US (07/14/2026)', 'eu' => 'EU (14/07/2026)', 'long' => 'Long (14 July 2026)'])]
    public string $date_format = 'iso';

    #[SettingField(controlType: ControlType::Select, label: 'Time format', validation: ['required', 'string', 'in:24h,12h'], group: 'Formatting', sortOrder: 3, options: ['24h' => '24-hour', '12h' => '12-hour'])]
    public string $time_format = '24h';

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'tenant';
    }
}
