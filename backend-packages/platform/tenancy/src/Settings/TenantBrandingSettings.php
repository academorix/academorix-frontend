<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform tenancy --force`.

declare(strict_types=1);

namespace Academorix\Tenancy\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * TenantBrandingSettings — Branding.
 *
 * Branding profile policy.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Tenancy
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'tenant_branding',
    label: 'Branding',
    description: 'Branding profile policy.',
    icon: 'swatch',
    scope: 'tenant',
    permission: 'tenants.manage_branding',
    sortOrder: 3,
)]
class TenantBrandingSettings extends Settings
{

    // ── Profiles ──────────────────────────────────
    #[SettingGroup(label: 'Profiles', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Per-domain branding profiles', validation: ['boolean'], group: 'Profiles', sortOrder: 1, helpText: 'Ship distinct branding for each custom domain (v2).')]
    public bool $allow_per_domain_profiles = false;


    // ── Accessibility ──────────────────────────────────
    #[SettingGroup(label: 'Accessibility', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enforce WCAG 2.2 AA contrast', validation: ['boolean'], group: 'Accessibility', sortOrder: 1, helpText: 'Reject brand colour combinations that fail SC 1.4.11.')]
    public bool $enforce_contrast = false;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'tenant_branding';
    }
}
