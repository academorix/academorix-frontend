<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform ai --force`.

declare(strict_types=1);

namespace Academorix\Ai\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `status` on `ai-conversation`.
 *
 * Backed enum — one case per allowed value.
 *
 * @category Ai
 *
 * @since    0.1.0
 */
enum AiConversationStatus: string
{
    use Enum;

    case Active = 'active';
    case Archived = 'archived';
    case Flagged = 'flagged';
    case Deleted = 'deleted';
}
