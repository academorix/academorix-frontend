<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform ai --force`.

declare(strict_types=1);

namespace Academorix\Ai\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AiToolCall created for a tool the run invoked. RBAC re-checked BEFORE this event fires.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::TrackToolLatency
 *
 * @category Ai
 *
 * @since    0.1.0
 */
final readonly class ToolCalled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $toolCallId  ULID (atc_*)
     * @param  string  $runId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $toolName  string
     * @param  string  $argsHash  string (sha256)
     */
    public function __construct(
        public readonly string $toolCallId,
        public readonly string $runId,
        public readonly string $tenantId,
        public readonly string $toolName,
        public readonly string $argsHash,
    ) {
    }
}
