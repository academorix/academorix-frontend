<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform ai --force`.

declare(strict_types=1);

namespace Academorix\Ai\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New AiConversation committed on first user message.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - growth::analytics::TrackAiConversation
 *
 * @category Ai
 *
 * @since    0.1.0
 */
final readonly class ConversationStarted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $conversationId  ULID (aic_*)
     * @param  string  $tenantId  ULID
     * @param  string  $userId  ULID (usr_*)
     * @param  string  $persona  string
     */
    public function __construct(
        public readonly string $conversationId,
        public readonly string $tenantId,
        public readonly string $userId,
        public readonly string $persona,
    ) {
    }
}
