<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform ai --force`.

declare(strict_types=1);

namespace Academorix\Ai\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. SensitiveDataGate refused a prompt because it would have leaked medical / safeguarding / financial / PII to an LLM without explicit consent. Fail-closed.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 10 years)
 * - activity::WriteToActivityLog
 * - notifications::AlertPlatformAdminOnSensitiveBlock
 * - compliance::RaiseSafeguardingSignalIfMinor
 *
 * @category Ai
 *
 * @since    0.1.0
 */
final readonly class SensitiveDataBlocked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $runId  ULID | null
     * @param  string  $conversationId  ULID | null
     * @param  string  $tenantId  ULID
     * @param  string  $userId  ULID
     * @param  string  $persona  string
     * @param  string  $sensitivityClass  'medical' | 'safeguarding' | 'financial' | 'pii'
     * @param  string  $blockedAt  ISO 8601
     * @param  string  $reasonCode  string
     */
    public function __construct(
        public readonly string $runId,
        public readonly string $conversationId,
        public readonly string $tenantId,
        public readonly string $userId,
        public readonly string $persona,
        public readonly string $sensitivityClass,
        public readonly string $blockedAt,
        public readonly string $reasonCode,
    ) {
    }
}
