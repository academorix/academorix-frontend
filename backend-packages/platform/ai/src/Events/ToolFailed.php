<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform ai --force`.

declare(strict_types=1);

namespace Academorix\Ai\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AiToolCall.status flipped to 'failed' or 'refused'. Refusal signals a RBAC/sensitivity gate blocked the tool from executing.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 *
 * @category Ai
 *
 * @since    0.1.0
 */
final readonly class ToolFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $toolCallId  ULID
     * @param  string  $runId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $toolName  string
     * @param  string  $status  'failed' | 'refused'
     * @param  string  $reason  string
     */
    public function __construct(
        public readonly string $toolCallId,
        public readonly string $runId,
        public readonly string $tenantId,
        public readonly string $toolName,
        public readonly string $status,
        public readonly string $reason,
    ) {
    }
}
