<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform ai --force`.

declare(strict_types=1);

namespace Academorix\Ai\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Tenant's monthly AI token pool crossed 0 via entitlements gate. Further AI calls refused with 429 until pool refills next cycle OR admin upgrades.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchTokenPoolExhaustedNotification (tenant admin + owner, cannot-opt-out)
 * - entitlements::LockAiFeature
 *
 * @category Ai
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TokenPoolExhausted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $poolKey  'ai.token_pool.month'
     * @param  string  $cycleStartedAt  ISO 8601
     * @param  string  $cycleEndsAt  ISO 8601
     * @param  int  $tokensConsumed  integer
     * @param  int  $tokensAllowed  integer
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $poolKey,
        public readonly string $cycleStartedAt,
        public readonly string $cycleEndsAt,
        public readonly int $tokensConsumed,
        public readonly int $tokensAllowed,
    ) {
    }
}
