<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Model;
use Academorix\Teams\Contracts\Data\EventTeamInterface;
use Academorix\Teams\Database\Factories\EventTeamFactory;
use Academorix\Foundation\Concerns\Filterable;
use Academorix\Foundation\Concerns\HasMetadata;
use Academorix\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a EventTeam.
 *
 * Team's participation in an Event (Wave 3).
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[Table(name: EventTeamInterface::TABLE, keyType: EventTeamInterface::KEY_TYPE)]
#[Fillable([
    EventTeamInterface::ATTR_TENANT_ID,
        EventTeamInterface::ATTR_TEAM_ID,
        EventTeamInterface::ATTR_EVENT_ID,
        EventTeamInterface::ATTR_PARTICIPATION_TYPE,
        EventTeamInterface::ATTR_REGISTERED_AT,
        EventTeamInterface::ATTR_WITHDREW_AT,
        EventTeamInterface::ATTR_WITHDRAWAL_REASON,
        EventTeamInterface::ATTR_CHECKED_IN_AT,
        EventTeamInterface::ATTR_RESULT,
        EventTeamInterface::ATTR_ATTENDANCE_COUNT,
        EventTeamInterface::ATTR_METADATA,
])]
#[UseFactory(EventTeamFactory::class)]
final class EventTeam extends Model implements EventTeamInterface
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    // TODO(gen): resolve unknown trait `BelongsToTeam` — add its import + use line.
    use HasMetadata;
    use HasUserstamps;
    use Auditable;
    use HasActivityLog;
    use Filterable;
    use SoftDeletes;

    /**
     * The primary key IS a string (prefixed ULID); disable auto-increment.
     *
     * @var bool
     */
    public $incrementing = false;

    /**
     * Cast map — from the blueprint's x-eloquent.casts.
     *
     * @var array<string, string>
     */
    protected $casts = [
        EventTeamInterface::ATTR_PARTICIPATION_TYPE => 'EventTeamParticipationType',
        EventTeamInterface::ATTR_RESULT => 'EventResult',
        EventTeamInterface::ATTR_ATTENDANCE_COUNT => 'integer',
        EventTeamInterface::ATTR_REGISTERED_AT => 'datetime',
        EventTeamInterface::ATTR_WITHDREW_AT => 'datetime',
        EventTeamInterface::ATTR_CHECKED_IN_AT => 'datetime',
    ];
}
