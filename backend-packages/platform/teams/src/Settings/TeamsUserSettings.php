<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * TeamsUserSettings — My teams.
 *
 * Per-user team preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'teams_user',
    label: 'My teams',
    description: 'Per-user team preferences.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 35,
)]
class TeamsUserSettings extends Settings
{

    // ── Team notifications ──────────────────────────────────
    #[SettingGroup(label: 'Team notifications', icon: 'bell', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me when registration opens for teams I\'m eligible for', group: 'Team notifications', sortOrder: 1)]
    public bool $receive_team_registration_notifications = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me when someone joins or leaves my team', group: 'Team notifications', sortOrder: 2, helpText: 'Only applies to teams you coach or captain.')]
    public bool $receive_team_member_updates = true;

    #[SettingField(controlType: ControlType::Text, label: 'Team notification channels', group: 'Team notifications', sortOrder: 3, options: ['mail' => 'Email', 'push' => 'Push notification', 'in_app' => 'In-app', 'sms' => 'SMS (usage charged)'])]
    public array $notification_channels = ['in_app', 'push'];


    // ── Preferences ──────────────────────────────────
    #[SettingGroup(label: 'Preferences', icon: 'adjustments-horizontal', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Text, label: 'My default team', group: 'Preferences', sortOrder: 1, helpText: 'Pre-selected on the team dashboard when you have multiple.')]
    public ?string $default_team_id = null;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'teams_user';
    }
}
