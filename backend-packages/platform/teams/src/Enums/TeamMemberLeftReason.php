<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `left_reason` on `team-member`.
 *
 * Enum: transferred / graduated / withdrew / dropped / injury / disciplinary / team_archived / other.
 *
 * @category Teams
 *
 * @since    0.1.0
 */
enum TeamMemberLeftReason: string
{
    use Enum;

    case Transferred = 'transferred';
    case Graduated = 'graduated';
    case Withdrew = 'withdrew';
    case Dropped = 'dropped';
    case Injury = 'injury';
    case Disciplinary = 'disciplinary';
    case TeamArchived = 'team_archived';
    case Other = 'other';
}
