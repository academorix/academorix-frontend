<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Services;

use Academorix\Teams\Contracts\Services\TeamProvisionerInterface;
use Illuminate\Container\Attributes\Scoped;

/**
 * Concrete service — TeamProvisioner.
 *
 * Implements {@see TeamProvisionerInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If the
 * service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[Scoped]
final class TeamProvisioner implements TeamProvisionerInterface
{
    // TODO(gen): implement the interface methods.
}
