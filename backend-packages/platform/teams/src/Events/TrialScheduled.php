<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TrialRepository committed a new team_trials row.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchTrialScheduledNotification (to trialist + coach)
 * - teams::ScheduleTrialistReminders (dispatches NotifyTrialistPreTrialJob at 24h + 1h before starts_at)
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TrialScheduled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $trialId  ULID (ttr_*)
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $trialistType  'athlete' | 'user' | 'anonymous'
     * @param  string  $trialistId  ULID | null
     * @param  string  $trialType  'individual' | 'group' | 'one_day' | 'multi_session'
     * @param  int  $sessionCount  int
     * @param  string  $startsAt  ISO 8601
     * @param  string  $endsAt  ISO 8601
     * @param  string  $scheduledBy  ULID
     */
    public function __construct(
        public readonly string $trialId,
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $trialistType,
        public readonly string $trialistId,
        public readonly string $trialType,
        public readonly int $sessionCount,
        public readonly string $startsAt,
        public readonly string $endsAt,
        public readonly string $scheduledBy,
    ) {
    }
}
