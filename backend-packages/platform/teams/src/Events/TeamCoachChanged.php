<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Team.coach_id changed. Fires with `is_minor_only` flag when team.min_age < 18 OR max_age < 18 — CHILD-SAFETY-SENSITIVE. Downstream: notifies old + new coach; when is_minor_only=true, additionally notifies the branch's safeguarding lead + fires critical-severity audit.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (severity=critical when is_minor_only=true)
 * - activity::WriteToActivityLog
 * - notifications::DispatchTeamCoachChangedNotification (to old + new coach; safeguarding lead when is_minor_only=true)
 *
 * @category Teams
 *
 * @since    0.1.0
 */
final readonly class TeamCoachChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $wasCoachId  ULID | null
     * @param  string  $newCoachId  ULID | null
     * @param  string  $changedBy  ULID
     * @param  bool  $isMinorOnly  boolean (team.min_age < 18 OR team.max_age < 18 — hooks the child-safety notification chain)
     */
    public function __construct(
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $wasCoachId,
        public readonly string $newCoachId,
        public readonly string $changedBy,
        public readonly bool $isMinorOnly,
    ) {
    }
}
