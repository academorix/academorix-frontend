<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Team.status transitioned. Values: active / paused / archived. Fires alongside TeamArchived / TeamRestored on specific transitions.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateBranchTeamListCache
 *
 * @category Teams
 *
 * @since    0.1.0
 */
final readonly class TeamStatusChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromStatus  'active' | 'paused' | 'archived'
     * @param  string  $toStatus  'active' | 'paused' | 'archived'
     * @param  string  $changedBy  ULID
     * @param  string  $reason  string | null
     */
    public function __construct(
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $fromStatus,
        public readonly string $toStatus,
        public readonly string $changedBy,
        public readonly string $reason,
    ) {
    }
}
