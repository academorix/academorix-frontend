<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TeamMember.status transitioned but not to `inactive` OR `graduated` (those fire TeamMemberLeft). Non-terminal transitions: injured / suspended / recovery-to-active.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TeamMemberStatusChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamMemberId  ULID
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $wasStatus  string
     * @param  string  $newStatus  string
     * @param  string  $changedBy  ULID
     */
    public function __construct(
        public readonly string $teamMemberId,
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $wasStatus,
        public readonly string $newStatus,
        public readonly string $changedBy,
    ) {
    }
}
