<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * event_teams.withdrew_at set from null → now(). Cascades: cancel attendance for members.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchEventTeamWithdrewNotification (to members)
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EventTeamWithdrew implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventTeamId  ULID
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $withdrewAt  ISO 8601
     * @param  string  $withdrewBy  ULID | null
     * @param  string  $withdrawalReason  string | null
     */
    public function __construct(
        public readonly string $eventTeamId,
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $withdrewAt,
        public readonly string $withdrewBy,
        public readonly string $withdrawalReason,
    ) {
    }
}
