<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Any Team scalar field mutated except status (TeamStatusChanged), capacity/capacity_waitlist (TeamCapacityChanged), schedule_pattern (TeamScheduleChanged), coach_id (TeamCoachChanged), registration_opens_at/closes_at (TeamRegistrationOpened / TeamRegistrationClosed). Umbrella event for renames, description edits, colour/logo changes, sort_order tweaks, min_age/max_age edits.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Teams
 *
 * @since    0.1.0
 */
final readonly class TeamUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedFields  string[]
     * @param  string  $updatedBy  ULID
     */
    public function __construct(
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $changedFields,
        public readonly string $updatedBy,
    ) {
    }
}
