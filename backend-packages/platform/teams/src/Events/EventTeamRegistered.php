<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * EventTeamRepository committed a new event_teams row.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EventTeamRegistered implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventTeamId  ULID (evt_*)
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $eventId  ULID | null (Wave 3 FK, nullable pre-Wave-3)
     * @param  string  $participationType  'home' | 'away' | 'host' | 'guest' | 'participant'
     * @param  string  $registeredAt  ISO 8601
     * @param  string  $registeredBy  ULID
     */
    public function __construct(
        public readonly string $eventTeamId,
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $eventId,
        public readonly string $participationType,
        public readonly string $registeredAt,
        public readonly string $registeredBy,
    ) {
    }
}
