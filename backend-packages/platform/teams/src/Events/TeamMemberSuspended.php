<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TeamMember.status transitioned to `suspended` (disciplinary). Fires alongside TeamMemberStatusChanged.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (severity=warn)
 * - activity::WriteToActivityLog
 *
 * @category Teams
 *
 * @since    0.1.0
 */
final readonly class TeamMemberSuspended implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamMemberId  ULID
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $suspendedAt  ISO 8601
     * @param  string  $suspendedBy  ULID
     * @param  string  $reason  string | null
     */
    public function __construct(
        public readonly string $teamMemberId,
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $suspendedAt,
        public readonly string $suspendedBy,
        public readonly string $reason,
    ) {
    }
}
