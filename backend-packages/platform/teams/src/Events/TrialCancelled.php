<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TeamTrial.status transitioned to `cancelled` from scheduled or in_progress. Voluntary cancel by trialist OR admin.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchTrialCancelledNotification (to trialist + coach)
 *
 * @category Teams
 *
 * @since    0.1.0
 */
final readonly class TrialCancelled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $trialId  ULID
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $cancelledAt  ISO 8601
     * @param  string  $cancelledBy  ULID | null
     * @param  string  $cancellationReason  string | null
     */
    public function __construct(
        public readonly string $trialId,
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $cancelledAt,
        public readonly string $cancelledBy,
        public readonly string $cancellationReason,
    ) {
    }
}
