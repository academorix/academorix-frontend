<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * event_teams.result JSONB written. Refused when the team has withdrew_at set OR participation_type='participant' AND result claims a position (invalid combination — clarified in valid_result_shape rule).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Teams
 *
 * @since    0.1.0
 */
final readonly class EventTeamResultRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventTeamId  ULID
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  array  $result  object (sport-specific, validated against valid_result_shape rule)
     * @param  string  $recordedAt  ISO 8601
     * @param  string  $recordedBy  ULID
     */
    public function __construct(
        public readonly string $eventTeamId,
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly array $result,
        public readonly string $recordedAt,
        public readonly string $recordedBy,
    ) {
    }
}
