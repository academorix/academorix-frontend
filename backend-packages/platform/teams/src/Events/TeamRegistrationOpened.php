<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Team.registration_opens_at reached the current moment (auto-flipped by OpenClosePendingRegistrationsJob) OR admin manually set an already-past registration_opens_at.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchTeamRegistrationOpeningNotification (to eligible members based on age + gender)
 *
 * @category Teams
 *
 * @since    0.1.0
 */
final readonly class TeamRegistrationOpened implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $opensAt  ISO 8601
     * @param  string  $closesAt  ISO 8601 | null
     * @param  int  $capacity  int | null
     */
    public function __construct(
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $opensAt,
        public readonly string $closesAt,
        public readonly int $capacity,
    ) {
    }
}
