<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Team.status transitioned to `archived` OR soft-delete with --cascade-members flag. Terminal.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - teams::CascadeMembersOnTeamArchived (cascades: transitions every active member to inactive with left_reason='team_archived')
 * - notifications::DispatchTeamMemberLeftNotification (per cascaded member)
 *
 * @category Teams
 *
 * @since    0.1.0
 */
final readonly class TeamArchived implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $archivedAt  ISO 8601
     * @param  string  $archivedBy  ULID
     * @param  string  $reason  string | null
     * @param  int  $cascadedMemberCount  int
     * @param  string  $hardDeleteScheduledFor  ISO 8601
     */
    public function __construct(
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $archivedAt,
        public readonly string $archivedBy,
        public readonly string $reason,
        public readonly int $cascadedMemberCount,
        public readonly string $hardDeleteScheduledFor,
    ) {
    }
}
