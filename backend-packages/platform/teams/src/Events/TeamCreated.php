<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform teams --force`.

declare(strict_types=1);

namespace Academorix\Teams\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TeamRepository finished a successful create. Fires exactly once per (tenant_id, branch_id, slug) tuple.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateBranchTeamListCache
 *
 * @category Teams
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TeamCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $teamId  ULID (tea_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $branchId  ULID (brn_*)
     * @param  string  $organizationId  ULID (org_*)
     * @param  string  $name  string
     * @param  string  $slug  string
     * @param  string  $teamType  string (squad / class / cohort / group / crew / other)
     * @param  string  $sportKey  string | null
     * @param  string  $competitiveLevel  string | null
     * @param  string  $genderCategory  string | null
     * @param  int  $minAge  int | null
     * @param  int  $maxAge  int | null
     * @param  int  $capacity  int | null
     * @param  string  $coachId  ULID | null (Wave 2b Coach FK)
     * @param  string  $createdBy  ULID (usr_*)
     */
    public function __construct(
        public readonly string $teamId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $organizationId,
        public readonly string $name,
        public readonly string $slug,
        public readonly string $teamType,
        public readonly string $sportKey,
        public readonly string $competitiveLevel,
        public readonly string $genderCategory,
        public readonly int $minAge,
        public readonly int $maxAge,
        public readonly int $capacity,
        public readonly string $coachId,
        public readonly string $createdBy,
    ) {
    }
}
