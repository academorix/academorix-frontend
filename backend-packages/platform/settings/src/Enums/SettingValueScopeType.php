<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform settings --force`.

declare(strict_types=1);

namespace Academorix\Settings\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `scope_type` on `setting-value`.
 *
 * Enum: system, tenant, user (org, branch reserved for scope module wave).
 *
 * @category Settings
 *
 * @since    0.1.0
 */
enum SettingValueScopeType: string
{
    use Enum;

    case System = 'system';
    case Tenant = 'tenant';
    case User = 'user';
}
