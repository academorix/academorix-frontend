<?php

/**
 * @file database/migrations/2026_07_15_120000_create_setting_values_table.php
 *
 * @description
 * Create the `setting_values` table for the `SettingValue` entity.
 * Owned by the settings module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform settings --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform settings --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('setting_values', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('scope_type', 16);
            $table->string('scope_id', 64)->nullable();
            $table->string('group', 64);
            $table->string('name', 100);
            $table->jsonb('payload')->nullable();
            $table->boolean('is_sensitive')->default(false);
            $table->boolean('is_locked')->default(false);
            $table->boolean('encrypted_at_rest')->default(false);
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['scope_type', 'scope_id', 'group', 'name'], 'setting_values_scope_group_name_unique');
            $table->index(['group', 'name'], 'setting_values_group_name_idx');
            $table->index(['scope_type', 'scope_id'], 'setting_values_scope_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('setting_values');
    }
};
