<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform public-site --force`.

declare(strict_types=1);

namespace Academorix\PublicSite\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Reusable block updated. All pages containing it are re-rendered.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - jobs::RerenderPagesJob
 * - cache::InvalidateCdn
 *
 * @category PublicSite
 *
 * @since    0.1.0
 */
final readonly class BlockUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $blockId  ULID (bkc_*)
     * @param  string  $updatedByUserId  ULID
     * @param  int  $affectedPageCount  int
     */
    public function __construct(
        public readonly string $blockId,
        public readonly string $updatedByUserId,
        public readonly int $affectedPageCount,
    ) {
    }
}
