<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform public-site --force`.

declare(strict_types=1);

namespace Academorix\PublicSite\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Form on public site submitted (contact / trial-request / newsletter).
 *
 * ## Consumers
 *
 * - registrations::MaybeCreateLead
 * - notifications::NotifyOwner
 * - activity::WriteToActivityLog
 *
 * @category PublicSite
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PublicFormSubmitted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $formSlug  string
     * @param  string  $tenantId  ULID
     * @param  string  $capturedAt  ISO 8601
     * @param  string  $ipAddressMasked  string
     */
    public function __construct(
        public readonly string $formSlug,
        public readonly string $tenantId,
        public readonly string $capturedAt,
        public readonly string $ipAddressMasked,
    ) {
    }
}
