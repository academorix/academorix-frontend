<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform public-site --force`.

declare(strict_types=1);

namespace Academorix\PublicSite\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `kind` on `content-block`.
 *
 * hero / text / gallery / cta / testimonials / results_widget / roster_widget / registration_form.
 *
 * @category PublicSite
 *
 * @since    0.1.0
 */
enum ContentBlockKind: string
{
    use Enum;

    case Hero = 'hero';
    case Text = 'text';
    case Gallery = 'gallery';
    case Cta = 'cta';
    case Testimonials = 'testimonials';
    case ResultsWidget = 'results_widget';
    case RosterWidget = 'roster_widget';
    case RegistrationForm = 'registration_form';
}
