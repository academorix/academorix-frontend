<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform organization --force`.

declare(strict_types=1);

namespace Academorix\Organization\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Soft-delete + status=archived transition. Refused when is_default / has descendants / referenced by branches — the event fires only after all guards pass.
 *
 * ## Consumers
 *
 * - entitlements::ReleaseOrganizationSlot
 * - activity::WriteToActivityLog
 * - audit::WriteCriticalAuditRow
 *
 * @category Organization
 *
 * @since    0.1.0
 */
final readonly class OrganizationArchived implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $organization  Organization
     * @param  string  $reason  string | null
     * @param  string  $actorId  string | null
     */
    public function __construct(
        public readonly string $organization,
        public readonly string $reason,
        public readonly string $actorId,
    ) {
    }
}
