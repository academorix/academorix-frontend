<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform organization --force`.

declare(strict_types=1);

namespace Academorix\Organization\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The tenant's `is_default=true` Organization changed. Fires when `is_default` flips on the NEW default (the auto-demotion of the OLD default is part of the same event; no separate demotion event).
 *
 * ## Consumers
 *
 * - organization::InvalidateDefaultCache
 * - activity::WriteToActivityLog
 * - audit::WriteCriticalAuditRow
 * - notifications::DispatchOrganizationDefaultChangedNotification
 *
 * @category Organization
 *
 * @since    0.1.0
 */
final readonly class OrganizationDefaultChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  string
     * @param  string  $fromOrganizationId  string | null
     * @param  string  $toOrganizationId  string
     * @param  string  $actorId  string | null
     * @param  string  $source  'action' | 'reconciler' | 'seeder'
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $fromOrganizationId,
        public readonly string $toOrganizationId,
        public readonly string $actorId,
        public readonly string $source,
    ) {
    }
}
