<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform organization --force`.

declare(strict_types=1);

namespace Academorix\Organization\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Hard-delete via PurgeArchivedOrganizationsJob (after the 730-day archive hold + zero descendants + zero branch references). Distinct from OrganizationArchived — that is the soft-delete signal; this is the terminal-purge signal.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - audit::WriteCriticalAuditRow
 *
 * @category Organization
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class OrganizationDeleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  string
     * @param  string  $organizationId  string
     * @param  string  $slug  string
     * @param  string  $name  string
     * @param  string  $archivedAt  datetime
     * @param  string  $deletedAt  datetime
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $organizationId,
        public readonly string $slug,
        public readonly string $name,
        public readonly string $archivedAt,
        public readonly string $deletedAt,
    ) {
    }
}
