<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform organization --force`.

declare(strict_types=1);

namespace Academorix\Organization\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * One of `logo_url`, `primary_color`, `secondary_color`, `description` changed. Requires `organization_branding` entitlement to have been active at write time.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - branding::CascadeBrandingResolution
 *
 * @category Organization
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class OrganizationBrandingUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $organization  Organization
     * @param  string  $changedFields  string[]
     * @param  array  $oldValues  object
     * @param  string  $actorId  string | null
     */
    public function __construct(
        public readonly string $organization,
        public readonly string $changedFields,
        public readonly array $oldValues,
        public readonly string $actorId,
    ) {
    }
}
