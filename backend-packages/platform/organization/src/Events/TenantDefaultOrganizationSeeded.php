<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform organization --force`.

declare(strict_types=1);

namespace Academorix\Organization\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SeedDefaultOrganizationForTenantJob committed the tenant's default row. Fires exactly once per tenant. Distinct from OrganizationCreated (which fires for the same row) — this event has extra context tying the seed to the provisioning transaction.
 *
 * ## Consumers
 *
 * - branch::MayAutoSeedDefaultBranch (planned)
 * - activity::WriteToActivityLog
 *
 * @category Organization
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TenantDefaultOrganizationSeeded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  string
     * @param  string  $organizationId  string
     * @param  string  $seededFrom  'tenant_provisioning' | 'reconciler_repair'
     * @param  string  $inferredCategory  string
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $organizationId,
        public readonly string $seededFrom,
        public readonly string $inferredCategory,
    ) {
    }
}
