<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform organization --force`.

declare(strict_types=1);

namespace Academorix\Organization\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * OrganizationSettings — Organizations.
 *
 * Tenant-wide Organization behaviour preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Organization
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'organization',
    label: 'Organizations',
    description: 'Tenant-wide Organization behaviour preferences. Distinct from per-Organization branding (which lives on each row).',
    icon: 'building-office-2',
    scope: 'tenant',
    permission: 'organizations.viewAny.tenant',
    sortOrder: 20,
)]
class OrganizationSettings extends Settings
{

    // ── Defaults ──────────────────────────────────
    #[SettingGroup(label: 'Defaults', description: 'Behaviour when the caller has not pinned a specific Organization.', icon: 'adjustments-horizontal', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Auto-resolve default organization', group: 'Defaults', sortOrder: 1, helpText: 'When on, requests without an X-Organization-Id header fall back to the tenant\'s default org. When off, missing header is a hard 400 error.')]
    public bool $auto_resolve_default_on_missing_header = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Ask users to pick an organization at login', group: 'Defaults', sortOrder: 2, helpText: 'When on and the tenant has more than one Organization, the login flow shows a picker instead of routing to the default.')]
    public bool $prompt_for_organization_on_login = false;


    // ── Hierarchy ──────────────────────────────────
    #[SettingGroup(label: 'Hierarchy', description: 'Nesting behaviour for tenants on Enterprise tier.', icon: 'square-3-stack-3d', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Allow moving organizations after create', group: 'Hierarchy', sortOrder: 1, helpText: 'When off, parent_id becomes IMMUTABLE — an organization can only be created with a parent, never moved. Recommended for compliance-heavy tenants.')]
    public bool $allow_reparent_after_create = true;

    #[SettingField(controlType: ControlType::Number, label: 'Max nesting depth', group: 'Hierarchy', sortOrder: 2, min: 1, max: 10, helpText: 'Hard cap on tree_depth. Existing rows deeper than the new cap remain until reparented.')]
    public int $max_hierarchy_depth = 5;


    // ── Branding cascade ──────────────────────────────────
    #[SettingGroup(label: 'Branding cascade', description: 'How Organizations without their own branding inherit from the tenant.', icon: 'swatch', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Inherit branding from parent organization', group: 'Branding cascade', sortOrder: 1, helpText: 'When on, a child Organization without its own logo/colors inherits from its nearest ancestor. When off, the tenant-level branding is used directly.')]
    public bool $cascade_branding_from_parent = true;


    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', icon: 'bell-alert', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify admins when a new organization is created', group: 'Notifications', sortOrder: 1)]
    public bool $notify_admins_on_create = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify admins when an organization is reparented', group: 'Notifications', sortOrder: 2)]
    public bool $notify_admins_on_reparent = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify admins when the default organization changes', group: 'Notifications', sortOrder: 3, helpText: 'Cannot be turned off — default-changes are transactional (they shift every downstream default-org filter).')]
    public bool $notify_admins_on_default_change = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'organization';
    }
}
