<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform reception --force`.

declare(strict_types=1);

namespace Academorix\Reception\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Walk-in converted to registration + athlete on the spot.
 *
 * ## Consumers
 *
 * - registrations::CreateFromWalkIn
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Reception
 *
 * @since    0.1.0
 */
final readonly class WalkInEnrolled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $visitorId  ULID
     * @param  string  $registrationId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $convertedByUserId  ULID
     */
    public function __construct(
        public readonly string $visitorId,
        public readonly string $registrationId,
        public readonly string $athleteId,
        public readonly string $convertedByUserId,
    ) {
    }
}
