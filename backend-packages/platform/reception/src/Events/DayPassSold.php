<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform reception --force`.

declare(strict_types=1);

namespace Academorix\Reception\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * One-time day pass purchased at reception. Payment cleared via facilities/DayPass.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::EmailDayPassReceipt
 * - credentials::IssueTemporaryCredential
 *
 * @category Reception
 *
 * @since    0.1.0
 */
final readonly class DayPassSold implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $dayPassId  ULID
     * @param  string  $visitorId  ULID|null
     * @param  int  $amountMinor  bigint
     * @param  string  $currency  string(3)
     * @param  string  $soldByUserId  ULID
     */
    public function __construct(
        public readonly string $dayPassId,
        public readonly string $visitorId,
        public readonly int $amountMinor,
        public readonly string $currency,
        public readonly string $soldByUserId,
    ) {
    }
}
