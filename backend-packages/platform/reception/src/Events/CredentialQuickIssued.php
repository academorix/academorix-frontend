<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform reception --force`.

declare(strict_types=1);

namespace Academorix\Reception\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Emergency credential issued at front desk (replacement, guest pass).
 *
 * ## Consumers
 *
 * - credentials::IssueCredential
 * - audit::WriteToAuditLog
 *
 * @category Reception
 *
 * @since    0.1.0
 */
final readonly class CredentialQuickIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $credentialId  ULID
     * @param  string  $holderId  ULID
     * @param  string  $reason  'replacement'|'guest'|'emergency'
     * @param  string  $issuedByUserId  ULID
     */
    public function __construct(
        public readonly string $credentialId,
        public readonly string $holderId,
        public readonly string $reason,
        public readonly string $issuedByUserId,
    ) {
    }
}
