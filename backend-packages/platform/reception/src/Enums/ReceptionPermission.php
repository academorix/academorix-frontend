<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform reception --force`.

declare(strict_types=1);

namespace Academorix\Reception\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Reception module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Reception
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum ReceptionPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `reception.visitors.viewAny` — Reception Visitors ViewAny
     */
    #[Label('Reception Visitors ViewAny')]
    #[Description('Reception Visitors ViewAny')]
    case ReceptionVisitorsViewAny = 'reception.visitors.viewAny';

    /**
     * `reception.visitors.view` — Reception Visitors View
     */
    #[Label('Reception Visitors View')]
    #[Description('Reception Visitors View')]
    case ReceptionVisitorsView = 'reception.visitors.view';

    /**
     * `reception.visitors.check-in` — Reception Visitors Check-in
     */
    #[Label('Reception Visitors Check-in')]
    #[Description('Reception Visitors Check-in')]
    case ReceptionVisitorsCheckIn = 'reception.visitors.check-in';

    /**
     * `reception.visitors.check-out` — Reception Visitors Check-out
     */
    #[Label('Reception Visitors Check-out')]
    #[Description('Reception Visitors Check-out')]
    case ReceptionVisitorsCheckOut = 'reception.visitors.check-out';

    /**
     * `reception.visitors.convert` — Convert walk-in to registration + athlete.
     */
    #[Label('Convert walk-in to registration + athlete')]
    #[Description('Convert walk-in to registration + athlete.')]
    case ReceptionVisitorsConvert = 'reception.visitors.convert';

    /**
     * `reception.shifts.viewAny` — Reception Shifts ViewAny
     */
    #[Label('Reception Shifts ViewAny')]
    #[Description('Reception Shifts ViewAny')]
    case ReceptionShiftsViewAny = 'reception.shifts.viewAny';

    /**
     * `reception.shifts.view` — Reception Shifts View
     */
    #[Label('Reception Shifts View')]
    #[Description('Reception Shifts View')]
    case ReceptionShiftsView = 'reception.shifts.view';

    /**
     * `reception.shifts.start` — Reception Shifts Start
     */
    #[Label('Reception Shifts Start')]
    #[Description('Reception Shifts Start')]
    case ReceptionShiftsStart = 'reception.shifts.start';

    /**
     * `reception.shifts.end` — Reception Shifts End
     */
    #[Label('Reception Shifts End')]
    #[Description('Reception Shifts End')]
    case ReceptionShiftsEnd = 'reception.shifts.end';

    /**
     * `reception.shifts.reconcile` — Cash drawer reconciliation.
     */
    #[Label('Cash drawer reconciliation')]
    #[Description('Cash drawer reconciliation. Alerts on discrepancy.')]
    case ReceptionShiftsReconcile = 'reception.shifts.reconcile';

    /**
     * `reception.dashboard.realtime` — Realtime reception dashboard access.
     */
    #[Label('Realtime reception dashboard access')]
    #[Description('Realtime reception dashboard access.')]
    case ReceptionDashboardRealtime = 'reception.dashboard.realtime';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::ReceptionVisitorsViewAny => Guard::Sanctum,
            self::ReceptionVisitorsView => Guard::Sanctum,
            self::ReceptionVisitorsCheckIn => Guard::Sanctum,
            self::ReceptionVisitorsCheckOut => Guard::Sanctum,
            self::ReceptionVisitorsConvert => Guard::Sanctum,
            self::ReceptionShiftsViewAny => Guard::Sanctum,
            self::ReceptionShiftsView => Guard::Sanctum,
            self::ReceptionShiftsStart => Guard::Sanctum,
            self::ReceptionShiftsEnd => Guard::Sanctum,
            self::ReceptionShiftsReconcile => Guard::Sanctum,
            self::ReceptionDashboardRealtime => Guard::Sanctum,
        };
    }
}
