<?php

/**
 * @file database/migrations/2026_07_15_120003_create_integration_providers_table.php
 *
 * @description
 * Create the `integration_providers` table for the `IntegrationProvider` entity.
 * Owned by the integrations module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform integrations --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform integrations --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('integration_providers', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('slug', 64);
            $table->string('name', 100);
            $table->string('kind', 32);
            $table->string('auth_kind', 16);
            $table->jsonb('scopes')->nullable();
            $table->jsonb('webhook_events')->nullable();
            $table->jsonb('config_schema')->nullable();
            $table->string('docs_url', 500)->nullable();
            $table->string('logo_url', 500)->nullable();
            $table->string('brand_color', 7)->nullable();
            $table->boolean('is_system')->default('true');
            $table->boolean('enabled')->default('true');
            $table->string('min_plan_tier', 16)->nullable();
            $table->integer('sort_order')->default('0');
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 30)->nullable();
            $table->string('updated_by', 30)->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['slug'], 'integration_providers_slug_unique');
            $table->index(['kind', 'enabled', 'sort_order'], 'integration_providers_kind_enabled_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('integration_providers');
    }
};
