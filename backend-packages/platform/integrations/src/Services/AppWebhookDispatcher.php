<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform integrations --force`.

declare(strict_types=1);

namespace Academorix\Integrations\Services;

use Academorix\Integrations\Contracts\Services\AppWebhookDispatcherInterface;
use Illuminate\Container\Attributes\Scoped;

/**
 * Concrete service — AppWebhookDispatcher.
 *
 * Implements {@see AppWebhookDispatcherInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If the
 * service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Integrations
 *
 * @since    0.1.0
 */
#[Scoped]
final class AppWebhookDispatcher implements AppWebhookDispatcherInterface
{
    // TODO(gen): implement the interface methods.
}
