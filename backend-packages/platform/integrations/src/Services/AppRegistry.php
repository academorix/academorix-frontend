<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform integrations --force`.

declare(strict_types=1);

namespace Academorix\Integrations\Services;

use Academorix\Integrations\Contracts\Services\AppRegistryInterface;
use Illuminate\Container\Attributes\Scoped;
use Academorix\Integrations\Contracts\Repositories\AppInstallationRepositoryInterface;

/**
 * Concrete service — AppRegistry.
 *
 * Implements {@see AppRegistryInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Integrations
 *
 * @since    0.1.0
 */
#[Scoped]
final class AppRegistry implements AppRegistryInterface
{
    /**
     * @param  AppInstallationRepositoryInterface  $appInstallationRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly AppInstallationRepositoryInterface $appInstallationRepository,
    ) {
    }
}
