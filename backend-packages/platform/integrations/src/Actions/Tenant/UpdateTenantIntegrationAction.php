<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform integrations --force`.

declare(strict_types=1);

namespace Academorix\Integrations\Actions\Tenant;

use Academorix\Integrations\Contracts\Repositories\TenantIntegrationRepositoryInterface;
use Academorix\Integrations\Data\TenantIntegrationData;
use Academorix\Integrations\Data\Requests\UpdateTenantIntegrationRequestData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/tenant-integrations/{integration}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Patch(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Integrations
 *
 * @since    0.1.0
 */
#[AsController]
#[Patch('/api/v1/tenant-integrations/{integration}')]
final class UpdateTenantIntegrationAction
{
    public function __construct(
        private readonly TenantIntegrationRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `tenant-integration` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateTenantIntegrationRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateTenantIntegrationRequestData $data): TenantIntegrationData
    {
        $model = $this->repository->update($id, $data->toArray());

        return TenantIntegrationData::from($model);
    }
}
