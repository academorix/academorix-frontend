<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform integrations --force`.

declare(strict_types=1);

namespace Academorix\Integrations\Actions\Tenant;

use Academorix\Integrations\Contracts\Repositories\AppInstallationRepositoryInterface;
use Academorix\Integrations\Data\AppInstallationData;
use Academorix\Routing\Attributes\AsController;
use Academorix\Routing\Attributes\Get;

/**
 * `GET /api/v1/app-installations/{install}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsController]` + `#[Get(...)]`
 * attributes from `Academorix\Routing`. Discovered by the routing package's
 * boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Integrations
 *
 * @since    0.1.0
 */
#[AsController]
#[Get('/api/v1/app-installations/{install}')]
final class ShowAppInstallationAction
{
    public function __construct(
        private readonly AppInstallationRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `app-installation` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): AppInstallationData
    {
        return AppInstallationData::from($this->repository->findOrFail($id));
    }
}
