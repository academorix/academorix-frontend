<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform integrations --force`.

declare(strict_types=1);

namespace Academorix\Integrations\Actions\Platform;

use Academorix\Integrations\Contracts\Repositories\AppWebhookSubscriptionRepositoryInterface;
use Academorix\Integrations\Data\AppWebhookSubscriptionData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Get;
use Illuminate\Http\Request;
use Illuminate\Pagination\LengthAwarePaginator;
use Spatie\LaravelData\PaginatedDataCollection;

/**
 * `GET /api/v1/platform/app-webhook-subscriptions` — list action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Integrations
 *
 * @since    0.1.0
 */
#[AsAction(name: 'integrations.app_webhook_subscriptions.list')]
#[Get('/api/v1/platform/app-webhook-subscriptions')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class ListAppWebhookSubscriptionAction
{
    use AsController;

    public function __construct(
        private readonly AppWebhookSubscriptionRepositoryInterface $repository,
    ) {
    }

    /**
     * List `app-webhook-subscriptions` for the current caller.
     *
     * The `Repository` base picks up `filter[...]` / `sort=...` /
     * `include=...` query parameters via the `#[Filterable]` attribute
     * on the concrete repository — no per-Action wiring needed.
     *
     * @return PaginatedDataCollection<int, AppWebhookSubscriptionData>  Paginated wire-visible DTOs.
     */
    public function __invoke(Request $request): PaginatedDataCollection
    {
        /** @var LengthAwarePaginator<int, \Academorix\Integrations\Models\AppWebhookSubscription> $page */
        $page = $this->repository->paginate(
            perPage: (int) $request->integer('per_page', 15),
        );

        return AppWebhookSubscriptionData::collect($page, PaginatedDataCollection::class);
    }
}
