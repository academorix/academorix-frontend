<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform integrations --force`.

declare(strict_types=1);

namespace Academorix\Integrations\Actions\Platform;

use Academorix\Integrations\Contracts\Repositories\AppRepositoryInterface;
use Academorix\Integrations\Data\AppData;
use Academorix\Integrations\Data\Requests\CreateAppRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/platform/apps` — create action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Integrations
 *
 * @since    0.1.0
 */
#[AsAction(name: 'integrations.apps.create')]
#[Post('/api/v1/platform/apps')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class CreateAppAction
{
    use AsController;

    public function __construct(
        private readonly AppRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `app` from the validated request payload.
     *
     * @param  CreateAppRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateAppRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(AppData::from($model), JsonResponse::HTTP_CREATED);
    }
}
