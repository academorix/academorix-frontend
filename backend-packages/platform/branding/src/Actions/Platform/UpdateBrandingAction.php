<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branding --force`.

declare(strict_types=1);

namespace Academorix\Branding\Actions\Platform;

use Academorix\Branding\Contracts\Repositories\BrandingRepositoryInterface;
use Academorix\Branding\Data\BrandingData;
use Academorix\Branding\Data\Requests\UpdateBrandingRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/brandings/{id}` — update action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Branding
 *
 * @since    0.1.0
 */
#[AsAction(name: 'branding.brandings.update')]
#[Patch('/api/v1/brandings/{id}')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class UpdateBrandingAction
{
    use AsController;

    public function __construct(
        private readonly BrandingRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `branding` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateBrandingRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateBrandingRequestData $data): BrandingData
    {
        $model = $this->repository->update($id, $data->toArray());

        return BrandingData::from($model);
    }
}
