<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform credentials --force`.

declare(strict_types=1);

namespace Academorix\Credentials\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * status: active → revoked. Gate scans against the credential now fail.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - cache::InvalidateCredentialCache
 * - notifications::NotifyCredentialRevoked
 *
 * @category Credentials
 *
 * @since    0.1.0
 */
final readonly class CredentialRevoked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $credentialId  ULID
     * @param  string  $revokedByUserId  ULID
     * @param  string  $reason  'lost'|'stolen'|'expired'|'holder_left'|'security'|'other'
     * @param  string  $reasonNote  string|null
     */
    public function __construct(
        public readonly string $credentialId,
        public readonly string $revokedByUserId,
        public readonly string $reason,
        public readonly string $reasonNote,
    ) {
    }
}
