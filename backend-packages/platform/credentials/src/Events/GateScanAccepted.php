<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform credentials --force`.

declare(strict_types=1);

namespace Academorix\Credentials\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * HIGH VOLUME. Valid credential scanned at gate. Forwarded to attendance module.
 *
 * ## Consumers
 *
 * - attendance::ForwardScanToAttendance
 * - activity::WriteToActivityLog (sampled 10%)
 * - realtime::BroadcastGateActivity
 *
 * @category Credentials
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class GateScanAccepted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $scanId  ULID (gts_*)
     * @param  string  $credentialId  ULID
     * @param  string  $gateId  ULID
     * @param  string  $scannedAt  ISO 8601
     * @param  string  $deviceId  ULID
     */
    public function __construct(
        public readonly string $scanId,
        public readonly string $credentialId,
        public readonly string $gateId,
        public readonly string $scannedAt,
        public readonly string $deviceId,
    ) {
    }
}
