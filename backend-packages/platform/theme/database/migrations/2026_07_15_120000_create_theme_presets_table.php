<?php

/**
 * @file database/migrations/2026_07_15_120000_create_theme_presets_table.php
 *
 * @description
 * Create the `theme_presets` table for the `ThemePreset` entity.
 * Owned by the theme module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform theme --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('theme_presets', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64)->nullable();
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('slug', 64);
            $table->string('name', 128);
            $table->text('description')->nullable();
            $table->string('mode', 16);
            $table->string('category', 32)->default('tenant_custom');
            $table->jsonb('tokens');
            $table->string('preview_thumbnail_url', 512)->nullable();
            $table->boolean('is_active')->default(true);
            $table->string('created_by_user_id', 64)->nullable();
            $table->foreign('created_by_user_id')->references('id')->on('users')->onDelete('restrict');
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->uuid('deleted_by')->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            // TODO(gen): hand-implement functional index — columns=[COALESCE(tenant_id, 'platform'), slug] UNIQUE WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'mode', 'category'], 'theme_presets_tenant_mode_category_index');
            $table->index(['tenant_id', 'is_active'], 'theme_presets_tenant_active_index');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('theme_presets');
    }
};
