<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Academorix\Theme\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Theme.is_active transitions false → true (via ThemeObserver::updating). Fires AFTER the prior active theme (same mode) has been flipped to is_active=false in the same DB txn.
 *
 * ## Consumers
 *
 * - theme::RegenerateCssOnThemeActivated
 * - notifications::SendThemeActivatedNotification
 * - activity::WriteThemeActivatedToActivity
 * - audit::WriteThemeActivatedToAudit
 *
 * @category Theme
 *
 * @since    0.1.0
 */
final readonly class ThemeActivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $themeId  thm_ULID
     * @param  string  $tenantId  ten_ULID
     * @param  string  $mode  'light' | 'dark'
     * @param  string  $selectedPresetId  tps_ULID
     * @param  array  $tokensSnapshotHash  SHA-256 of the frozen tokens jsonb — for cache-key derivation
     * @param  string  $activatedAt  ISO 8601
     * @param  string  $priorThemeId  thm_ULID | null — the theme that was deactivated in the same transaction
     */
    public function __construct(
        public readonly string $themeId,
        public readonly string $tenantId,
        public readonly string $mode,
        public readonly string $selectedPresetId,
        public readonly array $tokensSnapshotHash,
        public readonly string $activatedAt,
        public readonly string $priorThemeId,
    ) {
    }
}
