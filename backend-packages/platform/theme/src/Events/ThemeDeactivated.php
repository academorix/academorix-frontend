<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Academorix\Theme\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Theme.is_active transitions true → false. Typically fires alongside a sibling ThemeActivated for the replacement theme in the same DB txn.
 *
 * ## Consumers
 *
 * - activity::WriteThemeDeactivatedToActivity
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ThemeDeactivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $themeId  thm_ULID
     * @param  string  $tenantId  ten_ULID
     * @param  string  $mode  'light' | 'dark'
     * @param  string  $deactivatedAt  ISO 8601
     */
    public function __construct(
        public readonly string $themeId,
        public readonly string $tenantId,
        public readonly string $mode,
        public readonly string $deactivatedAt,
    ) {
    }
}
