<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Academorix\Theme\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Fires post-RegenerateRenderedCssJob completion. Downstream consumers (CDN purge, SSR cache) key off this to bust their own caches.
 *
 * ## Consumers
 *
 * - observability::MetricsCollector
 * - storage::PurgeCdnCache (planned)
 *
 * @category Theme
 *
 * @since    0.1.0
 */
final readonly class ThemeCacheInvalidated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ten_ULID
     * @param  string  $themeId  thm_ULID
     * @param  string  $compiledCssUrl  string — signed URL to the newly-rendered CSS
     * @param  string  $compiledCssHash  SHA-256
     * @param  string  $invalidatedAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $themeId,
        public readonly string $compiledCssUrl,
        public readonly string $compiledCssHash,
        public readonly string $invalidatedAt,
    ) {
    }
}
