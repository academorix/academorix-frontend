<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Academorix\Theme\Data\Requests;

use Spatie\LaravelData\Attributes\MapInputName;
use Spatie\LaravelData\Data;
use Spatie\LaravelData\Mappers\SnakeCaseMapper;
use Spatie\LaravelData\Attributes\Validation\Max;
use Spatie\LaravelData\Attributes\Validation\Min;
use Spatie\LaravelData\Attributes\Validation\Regex;
use Spatie\LaravelData\Attributes\Validation\StringType;

/**
 * Server-side create request DTO for `ThemeTokenOverride`.
 *
 * Consumed by the corresponding Action; validated on construction
 * through Spatie Data's property-attribute pipeline (no `rules()`
 * method). See `.kiro/steering/data-first.md`.
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[MapInputName(SnakeCaseMapper::class)]
final class CreateThemeTokenOverrideRequestData extends Data
{
    public function __construct(
        #[StringType, Regex('/^thm_[0-9A-HJKMNP-TV-Z]{26}$/')]
        public string $themeId,

        #[StringType, Max(128), Regex('/^(color|spacing|typography|radius|shadow|motion|opacity|z_index)(\\.[a-z0-9_-]+)+$/')]
        public string $tokenKey,

        #[StringType, Min(1), Max(256)]
        public string $tokenValue,

        #[StringType]
        public string $tokenType,

        #[StringType, Max(256)]
        public ?string $reason = null,

        #[StringType]
        public string $appliedAt,
    ) {
    }
}
