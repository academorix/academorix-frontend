<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Academorix\Theme\Data;

use Spatie\LaravelData\Attributes\MapInputName;
use Spatie\LaravelData\Data;
use Spatie\LaravelData\Mappers\SnakeCaseMapper;

/**
 * Server-side output DTO for {@see \Academorix\Theme\Models\ThemeTokenOverride}.
 *
 * Consumed by controllers + services on the SERVER side. This is
 * distinct from the wire SDK's DTO (which sits under
 * `Academorix\PlatformThemeSdk\Data`) — the wire DTO travels across the HTTP
 * boundary; this one stays in-process.
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[MapInputName(SnakeCaseMapper::class)]
final class ThemeTokenOverrideData extends Data
{
    public function __construct(
        public string $id,
        public string $tenantId,
        public string $themeId,
        public string $tokenKey,
        public string $tokenValue,
        public string $tokenType,
        public string $appliedAt,
        public string $createdAt,
        public string $updatedAt,
        public ?string $reason = null,
        public ?string $deletedAt = null,
    ) {
    }
}
