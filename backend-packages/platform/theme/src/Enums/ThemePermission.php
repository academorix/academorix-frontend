<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform theme --force`.

declare(strict_types=1);

namespace Academorix\Theme\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Theme module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Theme
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum ThemePermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `theme.read` — Read themes, presets, and token overrides for the tenant.
     */
    #[Label('Read themes, presets, and token overrides for the tenant')]
    #[Description('Read themes, presets, and token overrides for the tenant. Also gates the /me/theme-preference read.')]
    case ThemeRead = 'theme.read';

    /**
     * `theme.manage` — CRUD on themes, presets, and token overrides.
     */
    #[Label('CRUD on themes, presets, and token overrides')]
    #[Description('CRUD on themes, presets, and token overrides. Design-role privilege — typically held by tenant owner + admin + explicit design-role users.')]
    case ThemeManage = 'theme.manage';

    /**
     * `theme.activate` — Flip a theme's is_active false → true.
     */
    #[Label('Flip a theme\'s is_active false → true')]
    #[Description('Flip a theme\'s is_active false → true. Separate from theme.manage because activation triggers CSS regen + notification fanout — some organisations restrict this to the owner even if design-role holds theme.manage.')]
    case ThemeActivate = 'theme.activate';

    /**
     * `platform.theme.read` — Read platform-provided presets + cross-tenant custom preset usage stats.
     */
    #[Label('Read platform-provided presets + cross-tenant custom preset usage stats')]
    #[Description('Read platform-provided presets + cross-tenant custom preset usage stats.')]
    case PlatformThemeRead = 'platform.theme.read';

    /**
     * `platform.theme.manage-presets` — CRUD on platform-provided ThemePreset rows (tenant_id = NULL).
     */
    #[Label('CRUD on platform-provided ThemePreset rows (tenant_id = NULL)')]
    #[Description('CRUD on platform-provided ThemePreset rows (tenant_id = NULL). Academorix staff only. Every write triggers MigrateThemeTokensOnPresetUpdateJob for tenants referencing the preset.')]
    case PlatformThemeManagePresets = 'platform.theme.manage-presets';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::ThemeRead => Guard::Sanctum,
            self::ThemeManage => Guard::Sanctum,
            self::ThemeActivate => Guard::Sanctum,
            self::PlatformThemeRead => Guard::PlatformAdmin,
            self::PlatformThemeManagePresets => Guard::PlatformAdmin,
        };
    }
}
