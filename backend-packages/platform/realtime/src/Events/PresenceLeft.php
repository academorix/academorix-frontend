<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform realtime --force`.

declare(strict_types=1);

namespace Academorix\Realtime\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * User left a presence channel (explicit unsubscribe OR disconnect). High volume.
 *
 * ## Consumers
 *
 * - cache::UpdatePresenceRoster
 *
 * @category Realtime
 *
 * @since    0.1.0
 */
final readonly class PresenceLeft implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $presenceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $channelId  ULID
     * @param  string  $userId  ULID
     */
    public function __construct(
        public readonly string $presenceId,
        public readonly string $tenantId,
        public readonly string $channelId,
        public readonly string $userId,
    ) {
    }
}
