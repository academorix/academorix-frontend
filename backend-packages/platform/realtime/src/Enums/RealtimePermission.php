<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform realtime --force`.

declare(strict_types=1);

namespace Academorix\Realtime\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Realtime module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Realtime
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum RealtimePermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `realtime.channels.viewAny` — Realtime Channels ViewAny
     */
    #[Label('Realtime Channels ViewAny')]
    #[Description('Realtime Channels ViewAny')]
    case RealtimeChannelsViewAny = 'realtime.channels.viewAny';

    /**
     * `realtime.channels.view` — Realtime Channels View
     */
    #[Label('Realtime Channels View')]
    #[Description('Realtime Channels View')]
    case RealtimeChannelsView = 'realtime.channels.view';

    /**
     * `realtime.subscriptions.viewAny` — Realtime Subscriptions ViewAny
     */
    #[Label('Realtime Subscriptions ViewAny')]
    #[Description('Realtime Subscriptions ViewAny')]
    case RealtimeSubscriptionsViewAny = 'realtime.subscriptions.viewAny';

    /**
     * `realtime.subscriptions.view` — Realtime Subscriptions View
     */
    #[Label('Realtime Subscriptions View')]
    #[Description('Realtime Subscriptions View')]
    case RealtimeSubscriptionsView = 'realtime.subscriptions.view';

    /**
     * `realtime.presence.read` — Read the presence roster on a presence channel.
     */
    #[Label('Read the presence roster on a presence channel')]
    #[Description('Read the presence roster on a presence channel.')]
    case RealtimePresenceRead = 'realtime.presence.read';

    /**
     * `platform.realtime.channels.viewAny` — Platform Realtime Channels ViewAny
     */
    #[Label('Platform Realtime Channels ViewAny')]
    #[Description('Platform Realtime Channels ViewAny')]
    case PlatformRealtimeChannelsViewAny = 'platform.realtime.channels.viewAny';

    /**
     * `platform.realtime.subscriptions.viewAny` — Cross-tenant broadcast health monitoring.
     */
    #[Label('Cross-tenant broadcast health monitoring')]
    #[Description('Cross-tenant broadcast health monitoring.')]
    case PlatformRealtimeSubscriptionsViewAny = 'platform.realtime.subscriptions.viewAny';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::RealtimeChannelsViewAny => Guard::Sanctum,
            self::RealtimeChannelsView => Guard::Sanctum,
            self::RealtimeSubscriptionsViewAny => Guard::Sanctum,
            self::RealtimeSubscriptionsView => Guard::Sanctum,
            self::RealtimePresenceRead => Guard::Sanctum,
            self::PlatformRealtimeChannelsViewAny => Guard::PlatformAdmin,
            self::PlatformRealtimeSubscriptionsViewAny => Guard::PlatformAdmin,
        };
    }
}
