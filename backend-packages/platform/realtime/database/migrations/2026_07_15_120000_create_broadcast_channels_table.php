<?php

/**
 * @file database/migrations/2026_07_15_120000_create_broadcast_channels_table.php
 *
 * @description
 * Create the `broadcast_channels` table for the `BroadcastChannel` entity.
 * Owned by the realtime module. Every column + index reference
 * goes through {@see Academorix\Realtime\Contracts\Data\BroadcastChannelInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform realtime --force`.

declare(strict_types=1);

use Academorix\Realtime\Contracts\Data\BroadcastChannelInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(BroadcastChannelInterface::TABLE, function (Blueprint $table): void {
            $table->string(BroadcastChannelInterface::ATTR_ID, 30)->primary();
            $table->string(BroadcastChannelInterface::ATTR_TENANT_ID, 30);
            $table->foreign(BroadcastChannelInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(BroadcastChannelInterface::ATTR_NAMESPACE, 200);
            $table->string(BroadcastChannelInterface::ATTR_CHANNEL_TYPE, 16);
            $table->string(BroadcastChannelInterface::ATTR_AUTHORIZE_CALLBACK, 200)->nullable();
            $table->jsonb(BroadcastChannelInterface::ATTR_METADATA)->nullable();
            $table->string(BroadcastChannelInterface::ATTR_CREATED_BY, 30)->nullable();
            $table->string(BroadcastChannelInterface::ATTR_UPDATED_BY, 30)->nullable();
            $table->timestampTz(BroadcastChannelInterface::ATTR_DELETED_AT)->nullable();
            $table->timestampTz(BroadcastChannelInterface::ATTR_CREATED_AT);
            $table->timestampTz(BroadcastChannelInterface::ATTR_UPDATED_AT);

        });

        DB::statement('CREATE UNIQUE INDEX broadcast_channels_unique ON ' . BroadcastChannelInterface::TABLE . ' (' . BroadcastChannelInterface::ATTR_TENANT_ID . ', ' . BroadcastChannelInterface::ATTR_NAMESPACE . ') WHERE deleted_at IS NULL');
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(BroadcastChannelInterface::TABLE);
    }
};
