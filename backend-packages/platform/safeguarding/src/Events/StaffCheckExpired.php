<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform safeguarding --force`.

declare(strict_types=1);

namespace Academorix\Safeguarding\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P0 event. StaffCheck expired. Staff MUST BE STOOD DOWN from athlete-facing duties per policy.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::AlertOwner
 * - jobs::AutoRevokeAthleteFacingRolesJob
 *
 * @category Safeguarding
 *
 * @since    0.1.0
 */
final readonly class StaffCheckExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $checkId  ULID
     * @param  string  $staffUserId  ULID
     * @param  string  $kind  string
     * @param  string  $expiredOn  date
     */
    public function __construct(
        public readonly string $checkId,
        public readonly string $staffUserId,
        public readonly string $kind,
        public readonly string $expiredOn,
    ) {
    }
}
