<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform safeguarding --force`.

declare(strict_types=1);

namespace Academorix\Safeguarding\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New StaffCheck row (DBS / background check / medical / qualification).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Safeguarding
 *
 * @since    0.1.0
 */
final readonly class StaffCheckCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $checkId  ULID (skc_*)
     * @param  string  $tenantId  ULID
     * @param  string  $staffUserId  ULID
     * @param  string  $kind  'dbs_enhanced'|'dbs_standard'|'background'|'medical'|'qualification'|'first_aid'
     * @param  string  $expiresAt  date|null
     */
    public function __construct(
        public readonly string $checkId,
        public readonly string $tenantId,
        public readonly string $staffUserId,
        public readonly string $kind,
        public readonly string $expiresAt,
    ) {
    }
}
