<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform storage --force`.

declare(strict_types=1);

namespace Academorix\Storage\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `virus_scan_state` on `file`.
 *
 * Enum: pending / scanning / clean / infected / scan_failed / quarantined.
 *
 * @category Storage
 *
 * @since    0.1.0
 */
enum FileVirusScanState: string
{
    use Enum;

    case Pending = 'pending';
    case Scanning = 'scanning';
    case Clean = 'clean';
    case Infected = 'infected';
    case ScanFailed = 'scan_failed';
    case Quarantined = 'quarantined';
}
