<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform storage --force`.

declare(strict_types=1);

namespace Academorix\Storage\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Post-upload, the tenant's storage.bytes.consumed usage crossed the 80% threshold (configurable per plan).
 *
 * ## Consumers
 *
 * - notifications::DispatchStorageQuotaApproachingNotification
 *
 * @category Storage
 *
 * @since    0.1.0
 */
final readonly class StorageQuotaApproaching implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  int  $usedBytes  integer
     * @param  int  $capBytes  integer
     * @param  string  $usedRatio  number
     * @param  string  $threshold  number
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly int $usedBytes,
        public readonly int $capBytes,
        public readonly string $usedRatio,
        public readonly string $threshold,
    ) {
    }
}
