<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform storage --force`.

declare(strict_types=1);

namespace Academorix\Storage\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * File hard-deleted (row + blob refcount decremented; on refcount=0 the physical byte is removed).
 *
 * ## Consumers
 *
 * - storage::DecrementBlobRefcount
 * - storage::RevokeAllSignedUrlsForFile
 * - activity::WriteToActivityLog
 * - audit::WriteAuditEntry
 * - search::RemoveFileFromIndex
 * - compliance::RecordErasureForDsar
 *
 * @category Storage
 *
 * @since    0.1.0
 */
final readonly class FileErased implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $fileId  fil_ULID
     * @param  string  $tenantId  ULID
     * @param  string  $sha256  string (64 hex)
     * @param  int  $sizeBytesFreed  integer
     * @param  string  $erasedBy  ULID | null
     * @param  string  $erasureReason  'user_delete' | 'tenant_erased' | 'admin_takedown' | 'dsar_erasure' | 'retention_purge' | 'virus_purge'
     */
    public function __construct(
        public readonly string $fileId,
        public readonly string $tenantId,
        public readonly string $sha256,
        public readonly int $sizeBytesFreed,
        public readonly string $erasedBy,
        public readonly string $erasureReason,
    ) {
    }
}
