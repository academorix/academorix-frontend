<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform storage --force`.

declare(strict_types=1);

namespace Academorix\Storage\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A signed URL was revoked — either by admin bulk-revoke, by one_time_use consumption, by sweep after expiration, or by file erasure.
 *
 * ## Consumers
 *
 * - audit::WriteAuditEntry
 *
 * @category Storage
 *
 * @since    0.1.0
 */
final readonly class SignedUrlRevoked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $signedUrlAuditId  sua_ULID
     * @param  string  $fileId  fil_ULID
     * @param  string  $tenantId  ULID
     * @param  string  $revokedBy  ULID | null
     * @param  string  $revokedReason  string
     */
    public function __construct(
        public readonly string $signedUrlAuditId,
        public readonly string $fileId,
        public readonly string $tenantId,
        public readonly string $revokedBy,
        public readonly string $revokedReason,
    ) {
    }
}
