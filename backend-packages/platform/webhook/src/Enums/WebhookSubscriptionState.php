<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform webhook --force`.

declare(strict_types=1);

namespace Academorix\Webhook\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `state` on `webhook-subscription`.
 *
 * spatie state: active, paused (manual), disabled (auto — 410 / consecutive_failure_threshold).
 *
 * @category Webhook
 *
 * @since    0.1.0
 */
enum WebhookSubscriptionState: string
{
    use Enum;

    case Active = 'active';
    case Paused = 'paused';
    case Disabled = 'disabled';
}
