<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform domains --force`.

declare(strict_types=1);

namespace Academorix\Domains\Actions\Tenant;

use Academorix\Domains\Contracts\Repositories\DomainRepositoryInterface;
use Academorix\Domains\Data\DomainData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Get;

/**
 * `GET /api/v1/tenant/domains/{id}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Domains
 *
 * @since    0.1.0
 */
#[AsAction(name: 'domains.show')]
#[Get('/api/v1/tenant/domains/{id}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowDomainAction
{
    use AsController;

    public function __construct(
        private readonly DomainRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `domain` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): DomainData
    {
        return DomainData::from($this->repository->findOrFail($id));
    }
}
