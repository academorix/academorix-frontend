<?php

/**
 * @file database/migrations/2026_07_15_120000_create_domain_records_table.php
 *
 * @description
 * Create the `domain_records` table for the `DomainRecord` entity.
 * Owned by the domains module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform domains --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform domains --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('domain_records', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('domain_id', 64);
            $table->foreign('domain_id')->references('id')->on('domains')->onDelete('cascade');
            $table->string('kind', 8);
            $table->string('name', 253);
            $table->text('expected_value');
            $table->text('last_seen_value')->nullable();
            $table->integer('ttl_seconds')->nullable();
            $table->integer('priority')->nullable();
            $table->string('status', 16)->default('unknown');
            $table->timestampTz('last_check_at')->nullable();
            $table->timestampTz('last_matched_at')->nullable();
            $table->text('last_error')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->uuid('created_by')->nullable();
            $table->uuid('updated_by')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['domain_id', 'kind', 'name'], 'domain_records_unique');
            $table->index(['tenant_id', 'domain_id'], 'domain_records_tenant_domain_index');
            $table->index(['status', 'last_check_at'], 'domain_records_status_last_check_index');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('domain_records');
    }
};
