<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

namespace Academorix\Branch\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Any mutable field on Branch was updated. Distinct from the specific-field events below (BranchAddressUpdated, BranchCoordinatesUpdated, …) — this one fires for everything else (name, description, phone, email, website, sort_order, logo_url).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Branch
 *
 * @since    0.1.0
 */
final readonly class BranchUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $branchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $updatedBy  ULID
     * @param  string  $changedFields  string[] (e.g. ['name', 'phone', 'sort_order'])
     */
    public function __construct(
        public readonly string $branchId,
        public readonly string $tenantId,
        public readonly string $updatedBy,
        public readonly string $changedFields,
    ) {
    }
}
