<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

namespace Academorix\Branch\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Branch.status transitioned closed → active. Refused when a successor branch was created under the same (tenant, organization) after the close event.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Branch
 *
 * @since    0.1.0
 */
final readonly class BranchReopened implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $branchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $reopenedBy  ULID
     * @param  int  $daysClosed  integer
     * @param  string  $previousClosedDate  ISO 8601 date
     */
    public function __construct(
        public readonly string $branchId,
        public readonly string $tenantId,
        public readonly string $reopenedBy,
        public readonly int $daysClosed,
        public readonly string $previousClosedDate,
    ) {
    }
}
