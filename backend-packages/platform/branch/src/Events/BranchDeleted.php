<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

namespace Academorix\Branch\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Hard-delete completed. Wave 2 refuses public hard-delete (closed is terminal); this event only fires on the two-person platform-support CLI path.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — the audit outlives the row)
 * - activity::WriteToActivityLog
 *
 * @category Branch
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class BranchDeleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $branchId  ULID (the row is gone; the ULID is preserved in the audit trail)
     * @param  string  $tenantId  ULID
     * @param  string  $organizationId  ULID
     * @param  string  $regionId  ULID
     * @param  string  $deletedBy  ULID (platform-support operator)
     * @param  int  $cascadeSummary  { facilities: int, teams: int, sessions: int, bookings: int } — for post-mortem
     */
    public function __construct(
        public readonly string $branchId,
        public readonly string $tenantId,
        public readonly string $organizationId,
        public readonly string $regionId,
        public readonly string $deletedBy,
        public readonly int $cascadeSummary,
    ) {
    }
}
