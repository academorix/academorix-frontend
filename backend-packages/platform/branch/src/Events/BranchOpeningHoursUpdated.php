<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

namespace Academorix\Branch\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Branch.opening_hours was updated. Distinct from BranchUpdated because the notification consumers care about member-facing hours changes specifically.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::MaybeDispatchBranchOpeningHoursChangedNotification (to members with active memberships referencing this branch, opt-in)
 *
 * @category Branch
 *
 * @since    0.1.0
 */
final readonly class BranchOpeningHoursUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $branchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $updatedBy  ULID
     * @param  string  $daysTouched  string[] (['monday', 'saturday', 'exceptions', ...])
     */
    public function __construct(
        public readonly string $branchId,
        public readonly string $tenantId,
        public readonly string $updatedBy,
        public readonly string $daysTouched,
    ) {
    }
}
