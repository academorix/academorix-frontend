<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

namespace Academorix\Branch\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Branch.address was updated. Distinct from BranchUpdated because it optionally triggers a re-geocode when the tenant has `branch_geocoding` entitlement + coordinates.source === 'geocoded' (i.e. the coordinates were auto-derived; a manual pin drop stays pinned).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - branch::MaybeDispatchGeocodeAddressJob
 *
 * @category Branch
 *
 * @since    0.1.0
 */
final readonly class BranchAddressUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $branchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $updatedBy  ULID
     * @param  string  $changedAddressFields  string[] (['line1', 'city', 'postal_code', ...])
     */
    public function __construct(
        public readonly string $branchId,
        public readonly string $tenantId,
        public readonly string $updatedBy,
        public readonly string $changedAddressFields,
    ) {
    }
}
