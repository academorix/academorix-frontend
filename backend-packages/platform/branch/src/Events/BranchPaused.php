<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

namespace Academorix\Branch\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Branch.status transitioned active → paused. New bookings + registrations refused via the enforce_operating_hours middleware.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchBranchOpeningHoursChangedNotification (paused counts as a hours change to members with active memberships)
 *
 * @category Branch
 *
 * @since    0.1.0
 */
final readonly class BranchPaused implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $branchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $pausedBy  ULID
     * @param  string  $reason  string | null
     * @param  string  $resumeScheduledFor  ISO 8601 timestamp | null (null = indefinite)
     */
    public function __construct(
        public readonly string $branchId,
        public readonly string $tenantId,
        public readonly string $pausedBy,
        public readonly string $reason,
        public readonly string $resumeScheduledFor,
    ) {
    }
}
