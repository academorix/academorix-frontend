<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

namespace Academorix\Branch\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Branch.status transitioned to closed. Refused when active downstream rows reference the branch — this event only fires after that check passes.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchBranchClosedNotification (transactional, no opt-out)
 * - branch::DispatchNotifyStaffOfBranchClosureJob
 *
 * @category Branch
 *
 * @since    0.1.0
 */
final readonly class BranchClosed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $branchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $closedBy  ULID
     * @param  string  $reason  string
     * @param  string  $closedDate  ISO 8601 date
     * @param  string  $successorBranchId  ULID | null (when the tenant designated a replacement venue)
     */
    public function __construct(
        public readonly string $branchId,
        public readonly string $tenantId,
        public readonly string $closedBy,
        public readonly string $reason,
        public readonly string $closedDate,
        public readonly string $successorBranchId,
    ) {
    }
}
