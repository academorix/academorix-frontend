<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

namespace Academorix\Branch\Services;

use Academorix\Branch\Contracts\Services\CapacityGateInterface;
use Illuminate\Container\Attributes\Scoped;
use Academorix\Branch\Contracts\Repositories\BranchRepositoryInterface;

/**
 * Concrete service — CapacityGate.
 *
 * Implements {@see CapacityGateInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Branch
 *
 * @since    0.1.0
 */
#[Scoped]
final class CapacityGate implements CapacityGateInterface
{
    /**
     * @param  BranchRepositoryInterface  $branchRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly BranchRepositoryInterface $branchRepository,
    ) {
    }
}
