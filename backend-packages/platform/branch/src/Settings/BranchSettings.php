<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform branch --force`.

declare(strict_types=1);

namespace Academorix\Branch\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * BranchSettings — Branches.
 *
 * Tenant-wide policy for branch provisioning + operational-hours enforcement + geocoding preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Branch
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'branch',
    label: 'Branches',
    description: 'Tenant-wide policy for branch provisioning + operational-hours enforcement + geocoding preferences.',
    icon: 'building-storefront',
    scope: 'tenant',
    permission: 'branches.viewAny',
    sortOrder: 25,
)]
class BranchSettings extends Settings
{

    // ── Provisioning defaults ──────────────────────────────────
    #[SettingGroup(label: 'Provisioning defaults', description: 'Defaults applied when a new Branch is created.', icon: 'plus-circle', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Auto-create a default branch with each organization', group: 'Provisioning defaults', sortOrder: 1, helpText: 'When on, creating a new Organization automatically provisions a default Branch under it. Off by default — most tenants want explicit control.')]
    public bool $seed_default_on_organization_create = false;

    #[SettingField(controlType: ControlType::Select, label: 'Initial status for new branches', group: 'Provisioning defaults', sortOrder: 2, options: ['active' => 'Active (default)', 'paused' => 'Paused (requires explicit resume)'])]
    public string $default_status = 'active';

    #[SettingField(controlType: ControlType::Toggle, label: 'Require full address on creation', group: 'Provisioning defaults', sortOrder: 3)]
    public bool $require_address_on_create = false;


    // ── Geocoding ──────────────────────────────────
    #[SettingGroup(label: 'Geocoding', description: 'Automatic address→coordinates via a mapping subprocessor. Requires the geocoding entitlement.', icon: 'globe-alt', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Select, label: 'Provider', group: 'Geocoding', sortOrder: 1, options: ['google_maps' => 'Google Maps (broad coverage, higher cost)', 'mapbox' => 'Mapbox (developer-friendly, moderate cost)', 'openstreetmap' => 'OpenStreetMap / Nominatim (free, rate-limited, community data)'])]
    public string $geocoding_provider = 'google_maps';

    #[SettingField(controlType: ControlType::Slider, label: 'Minimum confidence to accept a geocoded match', group: 'Geocoding', sortOrder: 2, min: 0.5, max: 1.0, helpText: 'Below this, the result is refused + coordinates stay at their prior value.')]
    public float $geocoding_quality_threshold = 0.75;

    #[SettingField(controlType: ControlType::Toggle, label: 'Automatically re-geocode when address changes', group: 'Geocoding', sortOrder: 3, helpText: 'Off = address edits do not touch coordinates unless the admin explicitly triggers a re-geocode.')]
    public bool $auto_regeocode_on_address_change = true;


    // ── Operating hours ──────────────────────────────────
    #[SettingGroup(label: 'Operating hours', description: 'Defaults for opening_hours + enforcement policy.', icon: 'clock', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Time, label: 'Default opening time', group: 'Operating hours', sortOrder: 1)]
    public string $default_opening = '09:00';

    #[SettingField(controlType: ControlType::Time, label: 'Default closing time', group: 'Operating hours', sortOrder: 2)]
    public string $default_closing = '18:00';

    #[SettingField(controlType: ControlType::Toggle, label: 'Weekend closed by default', group: 'Operating hours', sortOrder: 3)]
    public bool $default_weekend_closed = false;

    #[SettingField(controlType: ControlType::Toggle, label: 'Refuse bookings outside opening hours', group: 'Operating hours', sortOrder: 4, helpText: 'When on, booking-adjacent write routes refuse with 403 outside the branch\'s opening_hours + exceptions.')]
    public bool $enforce_operating_hours = true;


    // ── Capacity ──────────────────────────────────
    #[SettingGroup(label: 'Capacity', description: 'Fire-safety cap enforcement.', icon: 'users', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enforce capacity on bookings', group: 'Capacity', sortOrder: 1, helpText: 'When on, booking-adjacent routes refuse when the branch\'s fire-safety cap would be exceeded. Off = capacity is display-only.')]
    public bool $capacity_gating_enabled = false;


    // ── Closure notifications ──────────────────────────────────
    #[SettingGroup(label: 'Closure notifications', description: 'Who gets notified when a branch closes.', icon: 'bell-alert', sortOrder: 5)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify every staff member assigned to the branch', group: 'Closure notifications', sortOrder: 1)]
    public bool $notify_all_staff_on_closure = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify members with active memberships', group: 'Closure notifications', sortOrder: 2)]
    public bool $notify_members_on_closure = true;

    #[SettingField(controlType: ControlType::Number, label: 'Minimum days between closure decision and closed_date', group: 'Closure notifications', sortOrder: 3, min: 0, max: 90, helpText: 'Regional labor-notice laws may force this higher. When the value is 0, closure is immediate.')]
    public int $closure_notice_days = 7;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'branch';
    }
}
