<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform reporting --force`.

declare(strict_types=1);

namespace Academorix\Reporting\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Dashboard shared with another user / role. New viewers gain implicit read access to underlying modules' data.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::NotifyDashboardShared
 *
 * @category Reporting
 *
 * @since    0.1.0
 */
final readonly class DashboardShared implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $dashboardId  ULID
     * @param  string  $sharedByUserId  ULID
     * @param  array  $recipientIds  array<ULID>
     * @param  string  $recipientType  'user'|'role'|'team'
     */
    public function __construct(
        public readonly string $dashboardId,
        public readonly string $sharedByUserId,
        public readonly array $recipientIds,
        public readonly string $recipientType,
    ) {
    }
}
