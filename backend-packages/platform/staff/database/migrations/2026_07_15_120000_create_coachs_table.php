<?php

/**
 * @file database/migrations/2026_07_15_120000_create_coachs_table.php
 *
 * @description
 * Create the `coachs` table for the `Coach` entity.
 * Owned by the staff module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform staff --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('coachs', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('staff_id', 64);
            $table->foreign('staff_id')->references('id')->on('staff')->onDelete('restrict');
            $table->string('sport_key', 32);
            $table->jsonb('additional_sports')->default('\'[]\'::jsonb');
            $table->string('specialization', 128)->nullable();
            $table->string('coaching_level', 16);
            $table->jsonb('certifications')->default('\'[]\'::jsonb');
            $table->decimal('rating', 15, 4)->nullable();
            $table->integer('rating_count')->default(0);
            $table->text('bio')->nullable();
            $table->string('head_shot_url', 512)->nullable();
            $table->jsonb('availability_pattern')->nullable();
            $table->integer('session_capacity_per_week')->nullable();
            $table->jsonb('languages')->default('\'[]\'::jsonb');
            $table->string('public_email', 255)->nullable();
            $table->string('public_phone', 20)->nullable();
            $table->string('status', 16)->default('active');
            $table->date('coach_started_at')->nullable();
            $table->date('coach_ended_at')->nullable();
            $table->text('notes')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 64)->nullable();
            $table->string('updated_by', 64)->nullable();
            $table->string('deleted_by', 64)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['staff_id'], 'coaches_staff_active_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->index(['tenant_id', 'sport_key', 'status'], 'coaches_sport_status_idx');
            $table->index(['tenant_id', 'coaching_level'], 'coaches_level_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('coachs');
    }
};
