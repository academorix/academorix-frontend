<?php

/**
 * @file database/migrations/2026_07_15_120001_create_staffs_table.php
 *
 * @description
 * Create the `staffs` table for the `Staff` entity.
 * Owned by the staff module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform staff --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('staffs', function (Blueprint $table): void {
            $table->string('id', 64)->primary();
            $table->string('tenant_id', 64);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('branch_id', 64);
            $table->foreign('branch_id')->references('id')->on('branches')->onDelete('restrict');
            $table->string('user_id', 64);
            $table->foreign('user_id')->references('id')->on('users')->onDelete('restrict');
            $table->string('employee_number', 32)->nullable();
            $table->string('employment_type', 16);
            $table->string('employment_status', 16)->default('pre_hire');
            $table->string('position', 128);
            $table->string('department', 64)->nullable();
            $table->boolean('is_branch_manager')->default('false');
            $table->string('reports_to_staff_id', 64)->nullable();
            $table->foreign('reports_to_staff_id')->references('id')->on('staff')->onDelete('set null');
            $table->date('started_at');
            $table->date('ended_at')->nullable();
            $table->string('termination_reason', 32)->nullable();
            $table->text('termination_notes')->nullable();
            $table->date('probation_ends_at')->nullable();
            $table->integer('weekly_hours')->nullable();
            $table->string('compensation_type', 16)->nullable();
            $table->integer('compensation_amount_cents')->nullable();
            $table->string('compensation_currency', 3)->nullable();
            $table->string('pay_frequency', 16)->nullable();
            $table->string('emergency_contact_name', 128)->nullable();
            $table->string('emergency_contact_phone', 20)->nullable();
            $table->string('emergency_contact_relationship', 32)->nullable();
            $table->string('tax_id_last4', 4)->nullable();
            $table->text('notes')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->string('created_by', 64)->nullable();
            $table->string('updated_by', 64)->nullable();
            $table->string('deleted_by', 64)->nullable();
            $table->timestampTz('deleted_at')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->unique(['tenant_id', 'user_id'], 'staff_tenant_user_active_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
            $table->unique(['tenant_id', 'employee_number'], 'staff_tenant_employee_number_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE employee_number IS NOT NULL AND deleted_at IS NULL
            $table->unique(['branch_id', 'is_branch_manager'], 'staff_branch_manager_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE is_branch_manager = true AND employment_status = 'active' AND deleted_at IS NULL
            $table->index(['tenant_id', 'branch_id', 'employment_status'], 'staff_status_idx');
            $table->index(['tenant_id', 'user_id'], 'staff_user_idx');
            $table->index(['tenant_id', 'position'], 'staff_position_idx');
            $table->index(['tenant_id', 'reports_to_staff_id'], 'staff_reports_to_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('staffs');
    }
};
