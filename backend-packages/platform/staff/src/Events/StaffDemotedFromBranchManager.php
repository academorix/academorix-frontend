<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Staff.is_branch_manager transitioned true → false. Distinct from StaffOffboarded — a demotion keeps the Staff row active.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateBranchManagerCache
 *
 * @category Staff
 *
 * @since    0.1.0
 */
final readonly class StaffDemotedFromBranchManager implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $staffId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $branchId  ULID
     * @param  string  $demotedByUserId  ULID
     * @param  string  $reason  string | null
     */
    public function __construct(
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $demotedByUserId,
        public readonly string $reason,
    ) {
    }
}
