<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Staff.employment_status transitioned on_leave → active. Cascades: resume linked Coach if it was paused as a side-effect of the leave.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchStaffReturnedFromLeaveNotification
 *
 * @category Staff
 *
 * @since    0.1.0
 */
final readonly class StaffReturnedFromLeave implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $staffId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $returnedAt  YYYY-MM-DD
     * @param  string  $returnedByUserId  ULID
     */
    public function __construct(
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $returnedAt,
        public readonly string $returnedByUserId,
    ) {
    }
}
