<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Any Staff scalar field mutated except employment_status (dedicated transition events), position (StaffPositionChanged), department (StaffDepartmentChanged), branch_id (StaffBranchChanged), is_branch_manager (StaffPromotedToBranchManager / StaffDemotedFromBranchManager), and compensation_* (CompensationChanged). Umbrella event for name-of-record changes, employee_number edits, weekly_hours tweaks, reports_to reassignments, probation_ends_at edits.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Staff
 *
 * @since    0.1.0
 */
final readonly class StaffUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $staffId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedFields  string[]
     * @param  string  $updatedBy  ULID (usr_*)
     */
    public function __construct(
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $changedFields,
        public readonly string $updatedBy,
    ) {
    }
}
