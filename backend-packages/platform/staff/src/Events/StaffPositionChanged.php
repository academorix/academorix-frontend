<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Staff.position mutated. A dedicated event because position changes drive downstream notifications (the branch manager wants to know when their receptionist is promoted to assistant coach).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchPositionChangedNotification
 *
 * @category Staff
 *
 * @since    0.1.0
 */
final readonly class StaffPositionChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $staffId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromPosition  string
     * @param  string  $toPosition  string
     * @param  string  $changedByUserId  ULID
     */
    public function __construct(
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $fromPosition,
        public readonly string $toPosition,
        public readonly string $changedByUserId,
    ) {
    }
}
