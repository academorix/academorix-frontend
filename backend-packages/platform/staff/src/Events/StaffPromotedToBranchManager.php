<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Staff.is_branch_manager transitioned false → true. Observer refuses when another active manager exists for the same branch AND does NOT auto-demote — the observer requires the caller to explicitly demote the previous manager first (avoids silent org-chart rewrites).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchStaffPromotedToBranchManagerNotification (to the branch's staff — cannot-opt-out)
 * - cache::InvalidateBranchManagerCache
 *
 * @category Staff
 *
 * @since    0.1.0
 */
final readonly class StaffPromotedToBranchManager implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $staffId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $branchId  ULID
     * @param  string  $promotedByUserId  ULID
     * @param  string  $effectiveAt  YYYY-MM-DD
     */
    public function __construct(
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $promotedByUserId,
        public readonly string $effectiveAt,
    ) {
    }
}
