<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Coach.sport_key mutated. Rare — a coach's primary sport is typically career-defining. When it happens, notifies the branch manager + invalidates the sport-directory cache.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateSportCoachDirectoryCache
 *
 * @category Staff
 *
 * @since    0.1.0
 */
final readonly class CoachSportChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $coachId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromSportKey  string
     * @param  string  $toSportKey  string
     * @param  string  $changedByUserId  ULID
     */
    public function __construct(
        public readonly string $coachId,
        public readonly string $tenantId,
        public readonly string $fromSportKey,
        public readonly string $toSportKey,
        public readonly string $changedByUserId,
    ) {
    }
}
