<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AggregateCoachRatingsJob recomputed Coach.rating + rating_count from the last batch of session-rating rows (Wave 3+ scheduling module). AGGREGATED event — fires once per job invocation per Coach, not per individual rating write.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateBranchCoachDirectoryCache
 * - notifications::DispatchCoachRatingUpdatedNotification (opt-in — only when the delta is > 0.5 stars)
 *
 * @category Staff
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CoachRatingUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $coachId  ULID
     * @param  string  $tenantId  ULID
     * @param  float  $fromRating  decimal | null
     * @param  float  $toRating  decimal
     * @param  int  $fromRatingCount  integer
     * @param  int  $toRatingCount  integer
     */
    public function __construct(
        public readonly string $coachId,
        public readonly string $tenantId,
        public readonly float $fromRating,
        public readonly float $toRating,
        public readonly int $fromRatingCount,
        public readonly int $toRatingCount,
    ) {
    }
}
