<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Staff.employment_status transitioned offboarding → offboarded. Terminal. The Staff row is soft-deleted; ended_at is set; the ReleaseSlot side-effect fires on the same transaction (releases 1 unit of staff_slot entitlement).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — employment record)
 * - activity::WriteToActivityLog
 * - notifications::DispatchStaffOffboardedNotification (to team members if the staff was a coach — cannot-opt-out)
 * - staff::CascadeCoachStatusOnStaffOffboarding (archives the linked Coach)
 * - user::UnblockUserArchiveWhenLastStaffOffboarded (removes the block that prevents User archive while active Staff exists)
 *
 * @category Staff
 *
 * @since    0.1.0
 */
final readonly class StaffOffboarded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $staffId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $branchId  ULID
     * @param  string  $userId  ULID
     * @param  string  $endedAt  YYYY-MM-DD
     * @param  string  $terminationReason  string (resigned / terminated / retired / contract_ended / seasonal_end / other)
     * @param  string  $terminationNotes  string | null
     * @param  string  $offboardedByUserId  ULID
     * @param  int  $daysOfService  integer (started_at → ended_at)
     */
    public function __construct(
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $userId,
        public readonly string $endedAt,
        public readonly string $terminationReason,
        public readonly string $terminationNotes,
        public readonly string $offboardedByUserId,
        public readonly int $daysOfService,
    ) {
    }
}
