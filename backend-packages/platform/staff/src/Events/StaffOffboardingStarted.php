<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Staff.employment_status transitioned active/on_leave/suspended → offboarding. Intermediate state — the actual offboard (with cascade to TeamMember + Coach + ShiftAssignment) happens on the offboarding → offboarded transition. This event is the SIGNAL that HR should start the cascade cleanup + reminder dispatch.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchStaffOffboardingScheduledNotification (to HR + branch manager)
 * - notifications::ScheduleOffboardingReminderCascade (1-day / 3-day / 7-day reminders if the offboarding lingers)
 *
 * @category Staff
 *
 * @since    0.1.0
 */
final readonly class StaffOffboardingStarted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $staffId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $plannedEndedAt  YYYY-MM-DD
     * @param  string  $terminationReason  string
     * @param  string  $initiatedByUserId  ULID
     */
    public function __construct(
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $plannedEndedAt,
        public readonly string $terminationReason,
        public readonly string $initiatedByUserId,
    ) {
    }
}
