<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Staff.employment_status transitioned active → on_leave. Non-terminal. Consumers pause the coach's future session assignments + shift assignments.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchStaffPutOnLeaveNotification (to branch manager + HR)
 * - staff::CascadeCoachStatusOnStaffOffboarding (pauses linked Coach — the same listener handles leave + offboard since the coach-side effect is identical)
 *
 * @category Staff
 *
 * @since    0.1.0
 */
final readonly class StaffPutOnLeave implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $staffId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $leaveType  string (medical / parental / sabbatical / bereavement / other)
     * @param  string  $expectedReturnAt  YYYY-MM-DD | null
     * @param  string  $putOnLeaveByUserId  ULID
     */
    public function __construct(
        public readonly string $staffId,
        public readonly string $tenantId,
        public readonly string $leaveType,
        public readonly string $expectedReturnAt,
        public readonly string $putOnLeaveByUserId,
    ) {
    }
}
