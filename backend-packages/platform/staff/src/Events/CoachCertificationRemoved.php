<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A certification entry was removed from Coach.certifications. Fires per certification removed.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Staff
 *
 * @since    0.1.0
 */
final readonly class CoachCertificationRemoved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $coachId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $certification  {'organization': 'string', 'certification': 'string', 'certified_at': 'YYYY-MM-DD', 'expires_at': 'YYYY-MM-DD | null'}
     * @param  string  $removedByUserId  ULID
     * @param  string  $reason  string | null
     */
    public function __construct(
        public readonly string $coachId,
        public readonly string $tenantId,
        public readonly string $certification,
        public readonly string $removedByUserId,
        public readonly string $reason,
    ) {
    }
}
