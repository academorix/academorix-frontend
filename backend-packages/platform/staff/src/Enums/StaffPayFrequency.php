<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `pay_frequency` on `staff`.
 *
 * Enum: hourly / weekly / biweekly / monthly / per_session.
 *
 * @category Staff
 *
 * @since    0.1.0
 */
enum StaffPayFrequency: string
{
    use Enum;

    case Hourly = 'hourly';
    case Weekly = 'weekly';
    case Biweekly = 'biweekly';
    case Monthly = 'monthly';
    case PerSession = 'per_session';
}
