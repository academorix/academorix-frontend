<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform staff --force`.

declare(strict_types=1);

namespace Academorix\Staff\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `termination_reason` on `staff`.
 *
 * Enum: resigned / terminated / retired / contract_ended / seasonal_end / other.
 *
 * @category Staff
 *
 * @since    0.1.0
 */
enum StaffTerminationReason: string
{
    use Enum;

    case Resigned = 'resigned';
    case Terminated = 'terminated';
    case Retired = 'retired';
    case ContractEnded = 'contract_ended';
    case SeasonalEnd = 'seasonal_end';
    case Other = 'other';
}
