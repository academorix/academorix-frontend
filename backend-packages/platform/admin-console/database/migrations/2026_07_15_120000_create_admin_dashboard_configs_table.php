<?php

/**
 * @file database/migrations/2026_07_15_120000_create_admin_dashboard_configs_table.php
 *
 * @description
 * Create the `admin_dashboard_configs` table for the `AdminDashboardConfig` entity.
 * Owned by the admin-console module. Emitted from the module blueprint —
 * regenerate via `generate-module.py platform admin-console --force`.
 */
// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform admin-console --force`.

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create('admin_dashboard_configs', function (Blueprint $table): void {
            $table->string('id', 30)->primary();
            $table->string('tenant_id', 30);
            $table->foreign('tenant_id')->references('id')->on('tenants')->onDelete('cascade');
            $table->string('user_id', 30)->nullable();
            $table->foreign('user_id')->references('id')->on('users')->onDelete('cascade');
            $table->jsonb('layout')->default('{}');
            $table->jsonb('enabled_widgets')->default('[]');
            $table->string('theme_preference', 16)->nullable();
            $table->jsonb('recent_actions_cache')->nullable();
            $table->jsonb('metadata')->nullable();
            $table->timestampTz('created_at');
            $table->timestampTz('updated_at');
            $table->timestampTz('deleted_at')->nullable();
            $table->unique(['tenant_id', 'user_id'], 'admin_dashboard_configs_unique');
            // Partial-index predicate — hand-add via DB::statement: WHERE deleted_at IS NULL
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists('admin_dashboard_configs');
    }
};
