<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform forms --force`.

declare(strict_types=1);

namespace Academorix\Forms\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Forms module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Forms
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum FormsPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `forms.viewAny` — Forms ViewAny
     */
    #[Label('Forms ViewAny')]
    #[Description('Forms ViewAny')]
    case FormsViewAny = 'forms.viewAny';

    /**
     * `forms.view` — Forms View
     */
    #[Label('Forms View')]
    #[Description('Forms View')]
    case FormsView = 'forms.view';

    /**
     * `forms.create` — Forms Create
     */
    #[Label('Forms Create')]
    #[Description('Forms Create')]
    case FormsCreate = 'forms.create';

    /**
     * `forms.update` — Metadata + settings + handoff wiring.
     */
    #[Label('Metadata + settings + handoff wiring')]
    #[Description('Metadata + settings + handoff wiring. Schema changes must go through a new FormVersion.')]
    case FormsUpdate = 'forms.update';

    /**
     * `forms.publish` — Forms Publish
     */
    #[Label('Forms Publish')]
    #[Description('Forms Publish')]
    case FormsPublish = 'forms.publish';

    /**
     * `forms.archive` — Forms Archive
     */
    #[Label('Forms Archive')]
    #[Description('Forms Archive')]
    case FormsArchive = 'forms.archive';

    /**
     * `forms.delete` — Archived form with 0 submissions only.
     */
    #[Label('Archived form with 0 submissions only')]
    #[Description('Archived form with 0 submissions only.')]
    case FormsDelete = 'forms.delete';

    /**
     * `form-versions.viewAny` — Form-versions ViewAny
     */
    #[Label('Form-versions ViewAny')]
    #[Description('Form-versions ViewAny')]
    case FormVersionsViewAny = 'form-versions.viewAny';

    /**
     * `form-versions.view` — Form-versions View
     */
    #[Label('Form-versions View')]
    #[Description('Form-versions View')]
    case FormVersionsView = 'form-versions.view';

    /**
     * `form-versions.create` — Form-versions Create
     */
    #[Label('Form-versions Create')]
    #[Description('Form-versions Create')]
    case FormVersionsCreate = 'form-versions.create';

    /**
     * `form-versions.publish` — Form-versions Publish
     */
    #[Label('Form-versions Publish')]
    #[Description('Form-versions Publish')]
    case FormVersionsPublish = 'form-versions.publish';

    /**
     * `form-versions.rollback` — Break-glass revert.
     */
    #[Label('Break-glass revert')]
    #[Description('Break-glass revert. Reason REQUIRED. Full audit chain, 7yr retention.')]
    case FormVersionsRollback = 'form-versions.rollback';

    /**
     * `form-submissions.viewAny` — Form-submissions ViewAny
     */
    #[Label('Form-submissions ViewAny')]
    #[Description('Form-submissions ViewAny')]
    case FormSubmissionsViewAny = 'form-submissions.viewAny';

    /**
     * `form-submissions.view` — Sensitivity-gated: medical fields blur unless viewer has medical.
     */
    #[Label('Sensitivity-gated: medical fields blur unless viewer has medical')]
    #[Description('Sensitivity-gated: medical fields blur unless viewer has medical.viewAny.')]
    case FormSubmissionsView = 'form-submissions.view';

    /**
     * `form-submissions.download` — Full-body download.
     */
    #[Label('Full-body download')]
    #[Description('Full-body download. Logs to audit trail.')]
    case FormSubmissionsDownload = 'form-submissions.download';

    /**
     * `form-submissions.export` — Bulk PII export.
     */
    #[Label('Bulk PII export')]
    #[Description('Bulk PII export. AlertOnBulkPiiExport fires above threshold.')]
    case FormSubmissionsExport = 'form-submissions.export';

    /**
     * `form-submissions.delete` — GDPR erasure path — replaces answers with tombstone.
     */
    #[Label('GDPR erasure path — replaces answers with tombstone')]
    #[Description('GDPR erasure path — replaces answers with tombstone. Full audit chain.')]
    case FormSubmissionsDelete = 'form-submissions.delete';

    /**
     * `platform.forms.audit` — Cross-tenant form-schema audit + submission volume analytics.
     */
    #[Label('Cross-tenant form-schema audit + submission volume analytics')]
    #[Description('Cross-tenant form-schema audit + submission volume analytics.')]
    case PlatformFormsAudit = 'platform.forms.audit';

    /**
     * `platform.forms.gdpr-erasure` — Force GDPR erasure across a tenant's submissions.
     */
    #[Label('Force GDPR erasure across a tenant\'s submissions')]
    #[Description('Force GDPR erasure across a tenant\'s submissions.')]
    case PlatformFormsGdprErasure = 'platform.forms.gdpr-erasure';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::FormsViewAny => Guard::Sanctum,
            self::FormsView => Guard::Sanctum,
            self::FormsCreate => Guard::Sanctum,
            self::FormsUpdate => Guard::Sanctum,
            self::FormsPublish => Guard::Sanctum,
            self::FormsArchive => Guard::Sanctum,
            self::FormsDelete => Guard::Sanctum,
            self::FormVersionsViewAny => Guard::Sanctum,
            self::FormVersionsView => Guard::Sanctum,
            self::FormVersionsCreate => Guard::Sanctum,
            self::FormVersionsPublish => Guard::Sanctum,
            self::FormVersionsRollback => Guard::Sanctum,
            self::FormSubmissionsViewAny => Guard::Sanctum,
            self::FormSubmissionsView => Guard::Sanctum,
            self::FormSubmissionsDownload => Guard::Sanctum,
            self::FormSubmissionsExport => Guard::Sanctum,
            self::FormSubmissionsDelete => Guard::Sanctum,
            self::PlatformFormsAudit => Guard::PlatformAdmin,
            self::PlatformFormsGdprErasure => Guard::PlatformAdmin,
        };
    }
}
