<?php

// // AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform forms --force`.

declare(strict_types=1);

namespace Academorix\Forms\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P0 event. Handoff handler threw. Retried per exponential backoff; alerts admin after N failures.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention 7yr)
 * - notifications::AlertHandoffFailed
 *
 * @category Forms
 *
 * @since    0.1.0
 */
final readonly class FormHandoffFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $submissionId  ULID
     * @param  string  $handoffSlug  string
     * @param  int  $attempt  int
     * @param  string  $error  string
     */
    public function __construct(
        public readonly string $submissionId,
        public readonly string $handoffSlug,
        public readonly int $attempt,
        public readonly string $error,
    ) {
    }
}
