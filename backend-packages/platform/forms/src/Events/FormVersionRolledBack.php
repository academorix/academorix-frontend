<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform forms --force`.

declare(strict_types=1);

namespace Academorix\Forms\Events;

use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. published_version_id swapped to a prior version. Reason REQUIRED.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention 7yr)
 * - cache::InvalidatePublicFormCache
 * - notifications::AlertFormRollback
 *
 * @category Forms
 *
 * @since    0.1.0
 */
final readonly class FormVersionRolledBack implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $formId  ULID
     * @param  string  $previousVersionId  ULID
     * @param  string  $newVersionId  ULID
     * @param  string  $rolledBackByUserId  ULID
     * @param  string  $reason  string
     */
    public function __construct(
        public readonly string $formId,
        public readonly string $previousVersionId,
        public readonly string $newVersionId,
        public readonly string $rolledBackByUserId,
        public readonly string $reason,
    ) {
    }
}
