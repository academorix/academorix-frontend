<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform forms --force`.

declare(strict_types=1);

namespace Academorix\Forms\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan forms:submissions:list` — Submissions:list — forms command.
 *
 * @category Forms
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'forms:submissions:list',
    description: 'Submissions:list — forms command.',
)]
final class SubmissionsListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Submissions:list — forms command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Submissions:list — forms command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
