/**
 * @file eslint.config.mjs
 * @description ESLint flat config for @academorix/core. Extends the workspace
 * base config; adds package-local overrides only if needed.
 */

import baseConfig from "@academorix/eslint-config";

export default [...baseConfig];
