/**
 * @file eslint.config.mjs
 * @description ESLint flat config for @academorix/notifications. Ships React
 * hooks + service-worker helpers, so we extend the workspace React base.
 */

import reactConfig from "@academorix/eslint-config/react";

export default [...reactConfig];
