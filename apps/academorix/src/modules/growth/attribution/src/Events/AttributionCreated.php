<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Stackra\Attribution\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AttributionRepository committed a new attributions row. Fires exactly once per (tenant_id, subject_type, subject_id) tuple. First-touch data is populated in the same transaction.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateTenantAttributionCount
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AttributionCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attributionId  ULID (att_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $subjectType  'user' | 'athlete' | 'lead' | 'anonymous_session'
     * @param  string  $subjectId  ULID | UUID (session_id when anonymous)
     * @param  string  $firstTouchSource  string
     * @param  string  $firstTouchMedium  string | null
     * @param  string  $firstTouchCampaign  string | null
     * @param  string  $firstTouchLandingPage  string
     * @param  string  $sessionId  UUID
     * @param  string  $lifecycleState  'anonymous' | 'identified'
     */
    public function __construct(
        public readonly string $attributionId,
        public readonly string $tenantId,
        public readonly string $subjectType,
        public readonly string $subjectId,
        public readonly string $firstTouchSource,
        public readonly string $firstTouchMedium,
        public readonly string $firstTouchCampaign,
        public readonly string $firstTouchLandingPage,
        public readonly string $sessionId,
        public readonly string $lifecycleState,
    ) {
    }
}
