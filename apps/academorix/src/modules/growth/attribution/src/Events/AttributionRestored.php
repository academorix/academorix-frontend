<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Attribution\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * attributions.lifecycle_state transitioned 'churned' → 'identified' (or 'anonymous' → 'identified' when a churned anonymous re-visits). Fires when a churned subject re-engages via an attribution-bearing request.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - analytics::TrackReactivation (Wave 5)
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AttributionRestored implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attributionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $wasLifecycleState  'churned'
     * @param  string  $newLifecycleState  'anonymous' | 'identified'
     * @param  string  $restoredAt  ISO 8601
     * @param  int  $daysSinceChurn  integer
     */
    public function __construct(
        public readonly string $attributionId,
        public readonly string $tenantId,
        public readonly string $wasLifecycleState,
        public readonly string $newLifecycleState,
        public readonly string $restoredAt,
        public readonly int $daysSinceChurn,
    ) {
    }
}
