<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Stackra\Attribution\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * attributions.subject_type transitioned 'anonymous_session' → 'user' | 'athlete' | 'lead'. Fires when an anonymous browser signs up or logs in. This is the pivotal cross-device merge trigger.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - attribution::MergeAnonymousAttributionsOnSubjectIdentified (dispatches MergeIdentifiedAttributionsJob on Enterprise tenants)
 * - marketing::AttributeSignupConversion (Wave 5)
 * - analytics::TrackIdentifyEvent (Wave 5)
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AttributionSubjectIdentified implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attributionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $wasSubjectType  'anonymous_session'
     * @param  string  $newSubjectType  'user' | 'athlete' | 'lead'
     * @param  string  $wasSubjectId  UUID (session_id)
     * @param  string  $newSubjectId  ULID
     * @param  string  $identifiedAt  ISO 8601
     */
    public function __construct(
        public readonly string $attributionId,
        public readonly string $tenantId,
        public readonly string $wasSubjectType,
        public readonly string $newSubjectType,
        public readonly string $wasSubjectId,
        public readonly string $newSubjectId,
        public readonly string $identifiedAt,
    ) {
    }
}
