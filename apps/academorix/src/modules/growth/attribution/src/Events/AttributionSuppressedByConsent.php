<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Attribution\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AttributionMiddleware attempted to persist an attribution row / touchpoint but ConsentGate::allows('marketing') returned false. Fires per suppressed request. High-volume signal — batched daily by AttributionSuppressedByConsentNotification.
 *
 * ## Consumers
 *
 * - compliance::LogConsentSuppression
 * - observability::MetricsCollector (increments stackra.attribution.consent_suppressions_total)
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AttributionSuppressedByConsent implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $sessionId  UUID
     * @param  string  $hashedIp  string (SHA-256)
     * @param  string  $attemptedSource  string | null
     * @param  string  $attemptedCampaign  string | null
     * @param  array  $consentState  object (jsonb — the state that caused refusal)
     * @param  string  $suppressedAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $sessionId,
        public readonly string $hashedIp,
        public readonly string $attemptedSource,
        public readonly string $attemptedCampaign,
        public readonly array $consentState,
        public readonly string $suppressedAt,
    ) {
    }
}
