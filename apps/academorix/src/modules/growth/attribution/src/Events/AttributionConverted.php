<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Attribution\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * attributions.lifecycle_state transitioned 'identified' → 'converted'. Dispatched by a downstream listener when the first purchase / subscription / paid conversion fires (finance module Wave 5 owns the conversion definition).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - activity::WriteToActivityLog
 * - marketing::ForwardConversionToAdNetworks (Wave 5)
 * - analytics::TrackConversionEvent (Wave 5)
 * - referrals::MaybeCreditReferrer (Wave 5)
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AttributionConverted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attributionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $subjectType  string
     * @param  string  $subjectId  ULID
     * @param  string  $convertedAt  ISO 8601
     * @param  string  $conversionSourceEvent  string (fully-qualified event class name that triggered the conversion)
     * @param  int  $conversionValueCents  integer | null (Wave 5 — attributes revenue)
     */
    public function __construct(
        public readonly string $attributionId,
        public readonly string $tenantId,
        public readonly string $subjectType,
        public readonly string $subjectId,
        public readonly string $convertedAt,
        public readonly string $conversionSourceEvent,
        public readonly int $conversionValueCents,
    ) {
    }
}
