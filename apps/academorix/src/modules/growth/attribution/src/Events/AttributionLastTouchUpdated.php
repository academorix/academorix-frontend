<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Stackra\Attribution\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * attributions.last_touch_* fields mutated on an existing row. Fires on every attribution-bearing subsequent request (utm_* changed, new click ID, new referrer). Bookend to AttributionFirstTouchRecorded.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - cache::InvalidateAttributionCurrentSnapshot
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AttributionLastTouchUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attributionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $wasSource  string
     * @param  string  $newSource  string
     * @param  string  $wasCampaign  string | null
     * @param  string  $newCampaign  string | null
     * @param  string  $capturedAt  ISO 8601
     */
    public function __construct(
        public readonly string $attributionId,
        public readonly string $tenantId,
        public readonly string $wasSource,
        public readonly string $newSource,
        public readonly string $wasCampaign,
        public readonly string $newCampaign,
        public readonly string $capturedAt,
    ) {
    }
}
