<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Attribution\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan attribution:capture-manual` — Capture manual — attribution command.
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'attribution:capture-manual',
    description: 'Capture manual — attribution command.',
)]
final class CaptureManualCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Capture manual — attribution command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Capture manual — attribution command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
