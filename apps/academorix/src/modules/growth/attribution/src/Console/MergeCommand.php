<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Stackra\Attribution\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan attribution:merge` — Merge — attribution command.
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'attribution:merge',
    description: 'Merge — attribution command.',
)]
final class MergeCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Merge — attribution command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Merge — attribution command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
