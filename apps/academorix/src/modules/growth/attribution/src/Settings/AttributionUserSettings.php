<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth attribution --force`.

declare(strict_types=1);

namespace Academorix\Attribution\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * AttributionUserSettings — My attribution.
 *
 * Personal attribution preferences.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Attribution
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'attribution_user',
    label: 'My attribution',
    description: 'Personal attribution preferences.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 65,
)]
class AttributionUserSettings extends Settings
{

    // ── Visibility ──────────────────────────────────
    #[SettingGroup(label: 'Visibility', icon: 'eye', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Show attribution widget on my dashboard', group: 'Visibility', sortOrder: 1, helpText: 'Off by default — most users don\'t need this. Referrers and marketing power-users may prefer on.')]
    public bool $show_attribution_widget = false;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'attribution_user';
    }
}
