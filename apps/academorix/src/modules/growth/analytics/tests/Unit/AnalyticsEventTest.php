<?php

declare(strict_types=1);

use Stackra\Analytics\Models\AnalyticsEvent;

it('is a placeholder — replace with actual AnalyticsEvent unit tests', function (): void {
    expect(AnalyticsEvent::class)->toBeString();
});
