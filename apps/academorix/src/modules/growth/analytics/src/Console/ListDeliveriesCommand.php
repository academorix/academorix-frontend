<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Academorix\Analytics\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan analytics:list-deliveries` — List deliveries — analytics command.
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'analytics:list-deliveries',
    description: 'List deliveries — analytics command.',
)]
final class ListDeliveriesCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List deliveries — analytics command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List deliveries — analytics command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
