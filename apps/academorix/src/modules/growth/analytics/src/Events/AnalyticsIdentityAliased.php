<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Academorix\Analytics\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Two AnalyticsIdentity rows merged via alias() into one canonical identity. Fires on the SURVIVING row.
 *
 * ## Consumers
 *
 * - analytics::MergeAliasedEventsListener
 * - notifications::DispatchMergeCompletedNotificationListener
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AnalyticsIdentityAliased implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $identityId  ULID (surviving)
     * @param  array  $mergedFrom  array<ULID> (merged-in rows, now soft-deleted)
     * @param  string  $tenantId  ULID
     */
    public function __construct(
        public readonly string $identityId,
        public readonly array $mergedFrom,
        public readonly string $tenantId,
    ) {
    }
}
