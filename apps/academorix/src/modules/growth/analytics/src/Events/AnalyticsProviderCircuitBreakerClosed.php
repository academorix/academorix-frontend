<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Stackra\Analytics\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Half-open probe succeeded close_after_successes times — state → closed.
 *
 * ## Consumers
 *
 * - notifications::DispatchCircuitBreakerResolutionListener — resolution notification
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AnalyticsProviderCircuitBreakerClosed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $providerConfigId  ULID
     * @param  string  $provider  string
     * @param  string  $closedAt  ISO 8601
     * @param  int  $outageDurationSeconds  int
     */
    public function __construct(
        public readonly string $providerConfigId,
        public readonly string $provider,
        public readonly string $closedAt,
        public readonly int $outageDurationSeconds,
    ) {
    }
}
