<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Academorix\Analytics\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * FlushBatchedProviderJob attempted but the provider returned a non-partial failure (e.g. auth error).
 *
 * ## Consumers
 *
 * - notifications::DispatchBatchBackpressureListener (when batch queue depth exceeds threshold)
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AnalyticsBatchFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $batchId  ULID
     * @param  string  $provider  string
     * @param  int  $eventCount  int
     * @param  string  $errorCode  string
     */
    public function __construct(
        public readonly string $batchId,
        public readonly string $provider,
        public readonly int $eventCount,
        public readonly string $errorCode,
    ) {
    }
}
