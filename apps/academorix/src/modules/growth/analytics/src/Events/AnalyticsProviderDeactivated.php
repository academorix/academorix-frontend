<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Stackra\Analytics\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AnalyticsProviderConfigObserver — is_active flipped false OR soft-deleted.
 *
 * ## Consumers
 *
 * - analytics::InvalidateFanoutCacheListener
 * - analytics::DrainBatchBufferListener — flushes any queued events for this provider before dropping them
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AnalyticsProviderDeactivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $providerConfigId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $reason  'admin_toggle' | 'soft_delete' | 'circuit_breaker_permanent'
     */
    public function __construct(
        public readonly string $providerConfigId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $reason,
    ) {
    }
}
