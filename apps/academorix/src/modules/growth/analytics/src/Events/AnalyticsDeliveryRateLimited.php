<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Academorix\Analytics\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * HTTP call returned 429 with a Retry-After header.
 *
 * ## Consumers
 *
 * - analytics::AdjustBackoffListener — extends next_retry_at per Retry-After hint
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AnalyticsDeliveryRateLimited implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $deliveryId  ULID
     * @param  string  $provider  string
     * @param  int  $retryAfterSeconds  int | null
     */
    public function __construct(
        public readonly string $deliveryId,
        public readonly string $provider,
        public readonly int $retryAfterSeconds,
    ) {
    }
}
