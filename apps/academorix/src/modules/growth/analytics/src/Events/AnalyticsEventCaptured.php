<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Academorix\Analytics\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AnalyticsEventObserver.created — event row inserted into analytics_events.
 *
 * ## Consumers
 *
 * - analytics::DispatchAnalyticsEventListener — dispatches DispatchAnalyticsEventJob
 * - marketing::CorrelateFunnelListener (Wave 5) — correlates funnel steps to marketing attribution when category='funnel_step'
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AnalyticsEventCaptured implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $analyticsEventId  ULID (ane_)
     * @param  string  $tenantId  ULID (ten_)
     * @param  string  $eventName  string
     * @param  string  $eventKind  string
     * @param  string  $category  string
     * @param  string  $subjectType  string
     * @param  string  $subjectId  string
     * @param  int  $eligibleProviderCount  int
     * @param  string  $capturedAt  ISO 8601
     */
    public function __construct(
        public readonly string $analyticsEventId,
        public readonly string $tenantId,
        public readonly string $eventName,
        public readonly string $eventKind,
        public readonly string $category,
        public readonly string $subjectType,
        public readonly string $subjectId,
        public readonly int $eligibleProviderCount,
        public readonly string $capturedAt,
    ) {
    }
}
