<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Academorix\Analytics\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AnalyticsEvent with category='experiment' AND event_name='experiment_variant_assigned' captured. Records the subject's variant assignment for a running experiment.
 *
 * ## Consumers
 *
 * - analytics::UpdateExperimentAssignmentIndexListener — writes a materialised (experiment_key, subject_id, variant) row for the ExperimentAssignmentReportBuilder
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AnalyticsExperimentAssigned implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $analyticsEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $experimentKey  string
     * @param  string  $variant  string
     * @param  string  $subjectType  string
     * @param  string  $subjectId  string
     */
    public function __construct(
        public readonly string $analyticsEventId,
        public readonly string $tenantId,
        public readonly string $experimentKey,
        public readonly string $variant,
        public readonly string $subjectType,
        public readonly string $subjectId,
    ) {
    }
}
