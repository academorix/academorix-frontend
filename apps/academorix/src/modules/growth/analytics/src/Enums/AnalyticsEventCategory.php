<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Stackra\Analytics\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `category` on `analytics-event`.
 *
 * Enum: feature_use / funnel_step / cta / error / performance / experiment / lifecycle / session.
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
enum AnalyticsEventCategory: string
{
    use Enum;

    case FeatureUse = 'feature_use';
    case FunnelStep = 'funnel_step';
    case Cta = 'cta';
    case Error = 'error';
    case Performance = 'performance';
    case Experiment = 'experiment';
    case Lifecycle = 'lifecycle';
    case Session = 'session';
}
