<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth analytics --force`.

declare(strict_types=1);

namespace Stackra\Analytics\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Analytics module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Analytics
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum AnalyticsPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `analytics.events.viewAny` — List analytics events in tenant.
     */
    #[Label('List analytics events in tenant')]
    #[Description('List analytics events in tenant.')]
    case AnalyticsEventsViewAny = 'analytics.events.viewAny';

    /**
     * `analytics.events.view` — View a single analytics event.
     */
    #[Label('View a single analytics event')]
    #[Description('View a single analytics event.')]
    case AnalyticsEventsView = 'analytics.events.view';

    /**
     * `analytics.deliveries.viewAny` — List delivery log across events.
     */
    #[Label('List delivery log across events')]
    #[Description('List delivery log across events.')]
    case AnalyticsDeliveriesViewAny = 'analytics.deliveries.viewAny';

    /**
     * `analytics.deliveries.view` — View a single delivery row.
     */
    #[Label('View a single delivery row')]
    #[Description('View a single delivery row.')]
    case AnalyticsDeliveriesView = 'analytics.deliveries.view';

    /**
     * `analytics.providers.viewAny` — List AnalyticsProviderConfig rows.
     */
    #[Label('List AnalyticsProviderConfig rows')]
    #[Description('List AnalyticsProviderConfig rows.')]
    case AnalyticsProvidersViewAny = 'analytics.providers.viewAny';

    /**
     * `analytics.providers.view` — View a single provider config (config credentials hidden).
     */
    #[Label('View a single provider config (config credentials hidden)')]
    #[Description('View a single provider config (config credentials hidden).')]
    case AnalyticsProvidersView = 'analytics.providers.view';

    /**
     * `analytics.providers.create` — Create a new AnalyticsProviderConfig.
     */
    #[Label('Create a new AnalyticsProviderConfig')]
    #[Description('Create a new AnalyticsProviderConfig.')]
    case AnalyticsProvidersCreate = 'analytics.providers.create';

    /**
     * `analytics.providers.update` — Update provider config (credentials, enabled_event_types, sampling_rate, batch_config).
     */
    #[Label('Update provider config (credentials, enabled_event_types, sampling_rate, batch_c')]
    #[Description('Update provider config (credentials, enabled_event_types, sampling_rate, batch_config).')]
    case AnalyticsProvidersUpdate = 'analytics.providers.update';

    /**
     * `analytics.providers.delete` — Soft-delete a provider config.
     */
    #[Label('Soft-delete a provider config')]
    #[Description('Soft-delete a provider config.')]
    case AnalyticsProvidersDelete = 'analytics.providers.delete';

    /**
     * `analytics.providers.test` — Dispatch a test event to a provider config.
     */
    #[Label('Dispatch a test event to a provider config')]
    #[Description('Dispatch a test event to a provider config.')]
    case AnalyticsProvidersTest = 'analytics.providers.test';

    /**
     * `analytics.providers.reset_circuit_breaker` — Manually reset a provider's circuit breaker.
     */
    #[Label('Manually reset a provider\'s circuit breaker')]
    #[Description('Manually reset a provider\'s circuit breaker.')]
    case AnalyticsProvidersResetCircuitBreaker = 'analytics.providers.reset_circuit_breaker';

    /**
     * `analytics.identities.viewAny` — List AnalyticsIdentity rows.
     */
    #[Label('List AnalyticsIdentity rows')]
    #[Description('List AnalyticsIdentity rows.')]
    case AnalyticsIdentitiesViewAny = 'analytics.identities.viewAny';

    /**
     * `analytics.identities.merge` — Merge one identity into another.
     */
    #[Label('Merge one identity into another')]
    #[Description('Merge one identity into another. Requires analytics_identity_merge entitlement.')]
    case AnalyticsIdentitiesMerge = 'analytics.identities.merge';

    /**
     * `analytics.identities.identify` — Server-side identify — associate an anonymous_id with a user_id.
     */
    #[Label('Server-side identify — associate an anonymous_id with a user_id')]
    #[Description('Server-side identify — associate an anonymous_id with a user_id.')]
    case AnalyticsIdentitiesIdentify = 'analytics.identities.identify';

    /**
     * `analytics.funnels.view` — Access funnel reports.
     */
    #[Label('Access funnel reports')]
    #[Description('Access funnel reports.')]
    case AnalyticsFunnelsView = 'analytics.funnels.view';

    /**
     * `analytics.experiments.view` — Access experiment results.
     */
    #[Label('Access experiment results')]
    #[Description('Access experiment results.')]
    case AnalyticsExperimentsView = 'analytics.experiments.view';

    /**
     * `analytics.circuit_breakers.viewAny` — Cross-tenant circuit-breaker dashboard for on-call.
     */
    #[Label('Cross-tenant circuit-breaker dashboard for on-call')]
    #[Description('Cross-tenant circuit-breaker dashboard for on-call.')]
    case AnalyticsCircuitBreakersViewAny = 'analytics.circuit_breakers.viewAny';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::AnalyticsEventsViewAny => Guard::Sanctum,
            self::AnalyticsEventsView => Guard::Sanctum,
            self::AnalyticsDeliveriesViewAny => Guard::Sanctum,
            self::AnalyticsDeliveriesView => Guard::Sanctum,
            self::AnalyticsProvidersViewAny => Guard::Sanctum,
            self::AnalyticsProvidersView => Guard::Sanctum,
            self::AnalyticsProvidersCreate => Guard::Sanctum,
            self::AnalyticsProvidersUpdate => Guard::Sanctum,
            self::AnalyticsProvidersDelete => Guard::Sanctum,
            self::AnalyticsProvidersTest => Guard::Sanctum,
            self::AnalyticsProvidersResetCircuitBreaker => Guard::Sanctum,
            self::AnalyticsIdentitiesViewAny => Guard::Sanctum,
            self::AnalyticsIdentitiesMerge => Guard::Sanctum,
            self::AnalyticsIdentitiesIdentify => Guard::Sanctum,
            self::AnalyticsFunnelsView => Guard::Sanctum,
            self::AnalyticsExperimentsView => Guard::Sanctum,
            self::AnalyticsCircuitBreakersViewAny => Guard::PlatformAdmin,
        };
    }
}
