<?php

declare(strict_types=1);

use Stackra\Marketing\Models\MarketingEvent;

it('is a placeholder — replace with actual MarketingEvent unit tests', function (): void {
    expect(MarketingEvent::class)->toBeString();
});
