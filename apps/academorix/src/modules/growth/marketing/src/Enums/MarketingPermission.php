<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Marketing module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum MarketingPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `marketing.events.viewAny` — Marketing Events ViewAny
     */
    #[Label('Marketing Events ViewAny')]
    #[Description('Marketing Events ViewAny')]
    case MarketingEventsViewAny = 'marketing.events.viewAny';

    /**
     * `marketing.events.view` — Marketing Events View
     */
    #[Label('Marketing Events View')]
    #[Description('Marketing Events View')]
    case MarketingEventsView = 'marketing.events.view';

    /**
     * `marketing.events.dispatch` — Manual re-dispatch of an existing event.
     */
    #[Label('Manual re-dispatch of an existing event')]
    #[Description('Manual re-dispatch of an existing event. Support tool.')]
    case MarketingEventsDispatch = 'marketing.events.dispatch';

    /**
     * `marketing.deliveries.viewAny` — Marketing Deliveries ViewAny
     */
    #[Label('Marketing Deliveries ViewAny')]
    #[Description('Marketing Deliveries ViewAny')]
    case MarketingDeliveriesViewAny = 'marketing.deliveries.viewAny';

    /**
     * `marketing.deliveries.view` — Marketing Deliveries View
     */
    #[Label('Marketing Deliveries View')]
    #[Description('Marketing Deliveries View')]
    case MarketingDeliveriesView = 'marketing.deliveries.view';

    /**
     * `marketing.providers.viewAny` — Marketing Providers ViewAny
     */
    #[Label('Marketing Providers ViewAny')]
    #[Description('Marketing Providers ViewAny')]
    case MarketingProvidersViewAny = 'marketing.providers.viewAny';

    /**
     * `marketing.providers.view` — Marketing Providers View
     */
    #[Label('Marketing Providers View')]
    #[Description('Marketing Providers View')]
    case MarketingProvidersView = 'marketing.providers.view';

    /**
     * `marketing.providers.create` — Configure a new marketing provider.
     */
    #[Label('Configure a new marketing provider')]
    #[Description('Configure a new marketing provider. Credentials are financial-critical.')]
    case MarketingProvidersCreate = 'marketing.providers.create';

    /**
     * `marketing.providers.update` — Marketing Providers Update
     */
    #[Label('Marketing Providers Update')]
    #[Description('Marketing Providers Update')]
    case MarketingProvidersUpdate = 'marketing.providers.update';

    /**
     * `marketing.providers.delete` — Marketing Providers Delete
     */
    #[Label('Marketing Providers Delete')]
    #[Description('Marketing Providers Delete')]
    case MarketingProvidersDelete = 'marketing.providers.delete';

    /**
     * `marketing.providers.test` — Send a synthetic test event via a provider's sandbox endpoint.
     */
    #[Label('Send a synthetic test event via a provider\'s sandbox endpoint')]
    #[Description('Send a synthetic test event via a provider\'s sandbox endpoint.')]
    case MarketingProvidersTest = 'marketing.providers.test';

    /**
     * `marketing.providers.reset-circuit-breaker` — Manually reset an OPEN circuit-breaker.
     */
    #[Label('Manually reset an OPEN circuit-breaker')]
    #[Description('Manually reset an OPEN circuit-breaker. Every reset writes an audit row + increments circuit_breaker_reset_count.')]
    case MarketingProvidersResetCircuitBreaker = 'marketing.providers.reset-circuit-breaker';

    /**
     * `marketing.dead_letters.viewAny` — Marketing Dead Letters ViewAny
     */
    #[Label('Marketing Dead Letters ViewAny')]
    #[Description('Marketing Dead Letters ViewAny')]
    case MarketingDeadLettersViewAny = 'marketing.dead_letters.viewAny';

    /**
     * `marketing.dead_letters.view` — Marketing Dead Letters View
     */
    #[Label('Marketing Dead Letters View')]
    #[Description('Marketing Dead Letters View')]
    case MarketingDeadLettersView = 'marketing.dead_letters.view';

    /**
     * `marketing.dead_letters.replay` — Enterprise-only via marketing_dead_letter_replay entitlement.
     */
    #[Label('Enterprise-only via marketing_dead_letter_replay entitlement')]
    #[Description('Enterprise-only via marketing_dead_letter_replay entitlement.')]
    case MarketingDeadLettersReplay = 'marketing.dead_letters.replay';

    /**
     * `marketing.dead_letters.resolve` — Marketing Dead Letters Resolve
     */
    #[Label('Marketing Dead Letters Resolve')]
    #[Description('Marketing Dead Letters Resolve')]
    case MarketingDeadLettersResolve = 'marketing.dead_letters.resolve';

    /**
     * `marketing.roas.view` — Access the ROAS dashboard.
     */
    #[Label('Access the ROAS dashboard')]
    #[Description('Access the ROAS dashboard. Medium+ via marketing_roas_report entitlement.')]
    case MarketingRoasView = 'marketing.roas.view';

    /**
     * `marketing.reports.consent-suppressions.view` — Compliance surface: view consent-suppressed events over a date range.
     */
    #[Label('Compliance surface: view consent-suppressed events over a date range')]
    #[Description('Compliance surface: view consent-suppressed events over a date range.')]
    case MarketingReportsConsentSuppressionsView = 'marketing.reports.consent-suppressions.view';

    /**
     * `platform.marketing.events.viewAny` — Platform Marketing Events ViewAny
     */
    #[Label('Platform Marketing Events ViewAny')]
    #[Description('Platform Marketing Events ViewAny')]
    case PlatformMarketingEventsViewAny = 'platform.marketing.events.viewAny';

    /**
     * `platform.marketing.events.view` — Platform Marketing Events View
     */
    #[Label('Platform Marketing Events View')]
    #[Description('Platform Marketing Events View')]
    case PlatformMarketingEventsView = 'platform.marketing.events.view';

    /**
     * `platform.marketing.deliveries.viewAny` — Platform Marketing Deliveries ViewAny
     */
    #[Label('Platform Marketing Deliveries ViewAny')]
    #[Description('Platform Marketing Deliveries ViewAny')]
    case PlatformMarketingDeliveriesViewAny = 'platform.marketing.deliveries.viewAny';

    /**
     * `platform.marketing.providers.viewAny` — Cross-tenant provider config read (redacted — no credentials).
     */
    #[Label('Cross-tenant provider config read (redacted — no credentials)')]
    #[Description('Cross-tenant provider config read (redacted — no credentials).')]
    case PlatformMarketingProvidersViewAny = 'platform.marketing.providers.viewAny';

    /**
     * `platform.marketing.dead_letters.viewAny` — Platform Marketing Dead Letters ViewAny
     */
    #[Label('Platform Marketing Dead Letters ViewAny')]
    #[Description('Platform Marketing Dead Letters ViewAny')]
    case PlatformMarketingDeadLettersViewAny = 'platform.marketing.dead_letters.viewAny';

    /**
     * `platform.marketing.dead_letters.replay` — Cross-tenant support-intervention dead-letter replay.
     */
    #[Label('Cross-tenant support-intervention dead-letter replay')]
    #[Description('Cross-tenant support-intervention dead-letter replay. Every fire notifies the tenant admin + writes audit with 7-year retention.')]
    case PlatformMarketingDeadLettersReplay = 'platform.marketing.dead_letters.replay';

    /**
     * `platform.marketing.circuit_breakers.viewAny` — Cross-tenant view of OPEN circuit-breakers.
     */
    #[Label('Cross-tenant view of OPEN circuit-breakers')]
    #[Description('Cross-tenant view of OPEN circuit-breakers. Powers the ops health dashboard.')]
    case PlatformMarketingCircuitBreakersViewAny = 'platform.marketing.circuit_breakers.viewAny';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::MarketingEventsViewAny => Guard::Sanctum,
            self::MarketingEventsView => Guard::Sanctum,
            self::MarketingEventsDispatch => Guard::Sanctum,
            self::MarketingDeliveriesViewAny => Guard::Sanctum,
            self::MarketingDeliveriesView => Guard::Sanctum,
            self::MarketingProvidersViewAny => Guard::Sanctum,
            self::MarketingProvidersView => Guard::Sanctum,
            self::MarketingProvidersCreate => Guard::Sanctum,
            self::MarketingProvidersUpdate => Guard::Sanctum,
            self::MarketingProvidersDelete => Guard::Sanctum,
            self::MarketingProvidersTest => Guard::Sanctum,
            self::MarketingProvidersResetCircuitBreaker => Guard::Sanctum,
            self::MarketingDeadLettersViewAny => Guard::Sanctum,
            self::MarketingDeadLettersView => Guard::Sanctum,
            self::MarketingDeadLettersReplay => Guard::Sanctum,
            self::MarketingDeadLettersResolve => Guard::Sanctum,
            self::MarketingRoasView => Guard::Sanctum,
            self::MarketingReportsConsentSuppressionsView => Guard::Sanctum,
            self::PlatformMarketingEventsViewAny => Guard::PlatformAdmin,
            self::PlatformMarketingEventsView => Guard::PlatformAdmin,
            self::PlatformMarketingDeliveriesViewAny => Guard::PlatformAdmin,
            self::PlatformMarketingProvidersViewAny => Guard::PlatformAdmin,
            self::PlatformMarketingDeadLettersViewAny => Guard::PlatformAdmin,
            self::PlatformMarketingDeadLettersReplay => Guard::PlatformAdmin,
            self::PlatformMarketingCircuitBreakersViewAny => Guard::PlatformAdmin,
        };
    }
}
