<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan marketing:resolve-dead-letter` — Resolve dead letter — marketing command.
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'marketing:resolve-dead-letter',
    description: 'Resolve dead letter — marketing command.',
)]
final class ResolveDeadLetterCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Resolve dead letter — marketing command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Resolve dead letter — marketing command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
