<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Provider returned 2xx AND the response passed provider-specific validation (e.g. Meta's `events_received == 1`). Delivery.status = 'succeeded'. Resets provider's consecutive_failure_count to 0.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector (increments stackra.marketing.deliveries.succeeded_total)
 * - marketing::MaybeCloseCircuitBreaker (checks if in half-open state; closes on success)
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MarketingDeliverySucceeded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $marketingDeliveryId  ULID
     * @param  string  $marketingEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  int  $attemptNumber  integer
     * @param  int  $responseStatusCode  integer
     * @param  string  $providerEventId  string | null
     * @param  int  $latencyMs  integer
     * @param  string  $succeededAt  ISO 8601
     */
    public function __construct(
        public readonly string $marketingDeliveryId,
        public readonly string $marketingEventId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly int $attemptNumber,
        public readonly int $responseStatusCode,
        public readonly string $providerEventId,
        public readonly int $latencyMs,
        public readonly string $succeededAt,
    ) {
    }
}
