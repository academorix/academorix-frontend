<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Stackra\Marketing\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CRITICAL. A delivery exhausted its retry budget OR failed permanently (auth_failure, invalid_payload). DeadLetterHandler created the MarketingDeadLetter row. Every fire is a P1 — the event is a paid conversion signal that never reached the ad network.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial audit)
 * - notifications::DispatchMarketingDeadLetterCreatedNotification (cannot-opt-out — pages on-call for financial-critical failures)
 * - monitoring::MetricsCollector (increments stackra.marketing.dead_letters.created_total)
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MarketingDeadLetterCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $marketingDeadLetterId  ULID (mdl_*)
     * @param  string  $marketingEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $reason  'max_attempts_exceeded' | 'provider_permanent_failure' | 'circuit_breaker_max_opens' | 'invalid_payload' | 'auth_failure'
     * @param  int  $finalAttemptNumber  integer
     * @param  string  $finalErrorCode  string | null
     * @param  string  $finalErrorMessage  string
     * @param  string  $createdAt  ISO 8601
     */
    public function __construct(
        public readonly string $marketingDeadLetterId,
        public readonly string $marketingEventId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $reason,
        public readonly int $finalAttemptNumber,
        public readonly string $finalErrorCode,
        public readonly string $finalErrorMessage,
        public readonly string $createdAt,
    ) {
    }
}
