<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Stackra\Marketing\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * DispatchToProviderJob started + the pre-dispatch CircuitBreakerGate check found the (tenant, provider) circuit is OPEN. Delivery.status = 'suppressed_by_circuit_breaker'. Never touches the wire. Retries scheduled when the circuit half-opens.
 *
 * ## Consumers
 *
 * - monitoring::MetricsCollector
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MarketingDeliverySuppressedByCircuitBreaker implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $marketingDeliveryId  ULID
     * @param  string  $marketingEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $circuitBreakerOpenUntil  ISO 8601
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $marketingDeliveryId,
        public readonly string $marketingEventId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $circuitBreakerOpenUntil,
        public readonly string $at,
    ) {
    }
}
