<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Stackra\Marketing\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Provider recovered — half-open probe succeeded OR ops manually reset via marketing:reset-circuit-breaker command / POST /providers/{provider}/reset-circuit-breaker. Resets consecutive_failure_count to 0 + increments circuit_breaker_reset_count when the close was manual.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MarketingProviderCircuitBreakerClosed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $providerConfigId  ULID
     * @param  string  $closedAt  ISO 8601
     * @param  bool  $wasAutoClose  boolean (true = half-open probe succeeded; false = manual reset)
     * @param  string  $closedByUserId  ULID | null (null = auto-close; set = manual reset)
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $providerConfigId,
        public readonly string $closedAt,
        public readonly bool $wasAutoClose,
        public readonly string $closedByUserId,
    ) {
    }
}
