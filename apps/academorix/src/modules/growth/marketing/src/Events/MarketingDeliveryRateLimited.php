<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Provider returned a 429 or the pre-dispatch RateLimitGate refused the send. Delivery.status = 'rate_limited'. Schedules a retry with the provider's `Retry-After` header (or the configured rate_limit backoff).
 *
 * ## Consumers
 *
 * - monitoring::MetricsCollector (increments stackra.marketing.deliveries.rate_limited_total; spike indicates tenant ceiling breached)
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MarketingDeliveryRateLimited implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $marketingDeliveryId  ULID
     * @param  string  $marketingEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  int  $retryAfterSeconds  integer
     * @param  string  $nextRetryAt  ISO 8601
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $marketingDeliveryId,
        public readonly string $marketingEventId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly int $retryAfterSeconds,
        public readonly string $nextRetryAt,
        public readonly string $at,
    ) {
    }
}
