<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Academorix\Marketing\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Every eligible provider dead-lettered. Event.status = 'failed'. Terminal.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — every failed conversion is a compliance-relevant financial signal)
 * - notifications::DispatchMarketingHighFailureRateNotification (when > 10% failure rate over 15min window)
 * - monitoring::MetricsCollector (increments stackra.marketing.events.failed_total)
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MarketingEventFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $marketingEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $eventType  string
     * @param  string  $failedProviders  string[]
     * @param  string  $failedAt  ISO 8601
     */
    public function __construct(
        public readonly string $marketingEventId,
        public readonly string $tenantId,
        public readonly string $eventType,
        public readonly string $failedProviders,
        public readonly string $failedAt,
    ) {
    }
}
