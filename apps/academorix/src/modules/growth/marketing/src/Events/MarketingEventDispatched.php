<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Stackra\Marketing\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MarketingEventDispatcher enqueued one DispatchToProviderJob per eligible provider. Fires ONCE per event. Distinct from MarketingDeliveryAttempted (per-provider-per-attempt).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MarketingEventDispatched implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $marketingEventId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $providerCount  integer
     * @param  string  $providers  string[] (list of provider slugs enqueued)
     * @param  string  $dispatchedAt  ISO 8601
     */
    public function __construct(
        public readonly string $marketingEventId,
        public readonly string $tenantId,
        public readonly int $providerCount,
        public readonly string $providers,
        public readonly string $dispatchedAt,
    ) {
    }
}
