<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Stackra\Marketing\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * MarketingSettings — Marketing conversion tracking.
 *
 * Tenant-wide defaults for marketing capture + dispatch.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'marketing',
    label: 'Marketing conversion tracking',
    description: 'Tenant-wide defaults for marketing capture + dispatch.',
    icon: 'megaphone',
    scope: 'tenant',
    permission: 'marketing.providers.viewAny',
    sortOrder: 40,
)]
class MarketingSettings extends Settings
{

    // ── Capture defaults ──────────────────────────────────
    #[SettingGroup(label: 'Capture defaults', description: 'Tenant-wide defaults applied to newly-configured providers.', icon: 'arrow-down-tray', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'New providers start in test mode', group: 'Capture defaults', sortOrder: 1, helpText: 'Newly-configured providers default to test_mode=true so credentials are validated before going live.')]
    public bool $default_test_mode = true;

    #[SettingField(controlType: ControlType::Number, label: 'Default max retry attempts', group: 'Capture defaults', sortOrder: 2, min: 1, max: 10)]
    public int $default_retry_max_attempts = 6;


    // ── Notifications routing ──────────────────────────────────
    #[SettingGroup(label: 'Notifications routing', description: 'Where dead-letter + circuit-breaker alerts are delivered.', icon: 'bell-alert', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Text, label: 'Notify these users on dead letters', group: 'Notifications routing', sortOrder: 1, helpText: 'Every user listed receives MarketingDeadLetterCreatedNotification. Tenant owners are always notified regardless.')]
    public array $dead_letter_notify_users = [];

    #[SettingField(controlType: ControlType::Text, label: 'Notify these users on circuit-breaker opens', group: 'Notifications routing', sortOrder: 2)]
    public array $circuit_breaker_notify_users = [];

    #[SettingField(controlType: ControlType::Text, label: 'Slack channel for alerts', group: 'Notifications routing', sortOrder: 3, helpText: 'Enterprise-only. Marketing alerts route to this Slack channel via the notifications module\'s Slack driver.')]
    public ?string $slack_channel = null;


    // ── Consent policy ──────────────────────────────────
    #[SettingGroup(label: 'Consent policy', description: 'Consent categories required to capture + dispatch marketing events.', icon: 'check-badge', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Text, label: 'Required consent categories', group: 'Consent policy', sortOrder: 1, options: ['marketing' => 'Marketing communications', 'advertising' => 'Advertising tracking', 'personalized_advertising' => 'Personalised advertising'], helpText: 'Compliance-critical. Regulators may require you to keep both marketing + advertising set.')]
    public array $required_consent_categories = ['marketing', 'advertising'];

    #[SettingField(controlType: ControlType::Toggle, label: 'Re-check consent at dispatch time', group: 'Consent policy', sortOrder: 2, helpText: 'When enabled, consent revoked between capture and dispatch is honoured (in-flight events are suppressed). Recommended: leave enabled.')]
    public bool $recheck_consent_on_dispatch = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'marketing';
    }
}
