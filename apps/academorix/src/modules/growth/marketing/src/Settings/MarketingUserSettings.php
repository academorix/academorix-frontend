<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Stackra\Marketing\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * MarketingUserSettings — My marketing preferences.
 *
 * Per-user preferences for marketing reports + alerts.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'marketing_user',
    label: 'My marketing preferences',
    description: 'Per-user preferences for marketing reports + alerts.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 35,
)]
class MarketingUserSettings extends Settings
{

    // ── ROAS report subscription ──────────────────────────────────
    #[SettingGroup(label: 'ROAS report subscription', icon: 'chart-bar', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Send me the monthly ROAS report', group: 'ROAS report subscription', sortOrder: 1)]
    public bool $receive_monthly_roas_report = true;

    #[SettingField(controlType: ControlType::Text, label: 'Delivery channels', group: 'ROAS report subscription', sortOrder: 2, options: ['mail' => 'Email', 'in_app' => 'In-app'])]
    public array $roas_report_channels = ['mail'];


    // ── Alert subscriptions ──────────────────────────────────
    #[SettingGroup(label: 'Alert subscriptions', icon: 'bell', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me on marketing dead letters', group: 'Alert subscriptions', sortOrder: 1, helpText: 'This preference only takes effect if the tenant admin listed you in dead_letter_notify_users. Owners always receive.')]
    public bool $receive_dead_letter_alerts = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me on circuit-breaker events', group: 'Alert subscriptions', sortOrder: 2)]
    public bool $receive_circuit_breaker_alerts = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'marketing_user';
    }
}
