<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth marketing --force`.

declare(strict_types=1);

namespace Stackra\Marketing\Attributes;

use Attribute;

/**
 * PHP attribute — AsMarketingProvider.
 *
 * TODO(gen): document the attribute's target + discovery pipeline.
 * Follow the pattern of `#[AsAiTool]` / `#[LoggableActivity]` /
 * `#[AsRepository]` — one attribute per compile-time-declarative
 * concern, scanned at boot via Foundation's `DiscoversAttributes`
 * seam, hydrated into a registry, consumed by a runner.
 *
 * @category Marketing
 *
 * @since    0.1.0
 */
#[Attribute(Attribute::TARGET_CLASS)]
final readonly class AsMarketingProvider
{
    // TODO(gen): declare the attribute's constructor parameters, if any.
}
