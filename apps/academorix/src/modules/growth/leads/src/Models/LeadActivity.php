<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

namespace Academorix\Leads\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Attributes\WithoutIncrementing;
use Illuminate\Database\Eloquent\Model;
use Academorix\Leads\Contracts\Data\LeadActivityInterface;
use Academorix\Leads\Database\Factories\LeadActivityFactory;
use Stackra\Foundation\Concerns\Filterable;
use Stackra\Foundation\Concerns\HasMetadata;
use Academorix\Leads\Enums\LeadActivityType;
use Academorix\Leads\Policies\LeadActivityPolicy;
use Stackra\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Attributes\UsePolicy;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use OwenIt\Auditing\Contracts\Auditable as AuditableContract;

/**
 * Eloquent model for a LeadActivity.
 *
 * Append-only timeline entry per Lead.
 *
 * @category Leads
 *
 * @since    0.1.0
 */
#[Table(name: LeadActivityInterface::TABLE, key: LeadActivityInterface::PRIMARY_KEY, keyType: LeadActivityInterface::KEY_TYPE)]
#[Fillable([
    LeadActivityInterface::ATTR_TENANT_ID,
        LeadActivityInterface::ATTR_LEAD_ID,
        LeadActivityInterface::ATTR_TYPE,
        LeadActivityInterface::ATTR_BODY,
        LeadActivityInterface::ATTR_METADATA,
        LeadActivityInterface::ATTR_ACTOR_ID,
        LeadActivityInterface::ATTR_OCCURRED_AT,
])]
#[UseFactory(LeadActivityFactory::class)]
#[WithoutIncrementing]
#[UsePolicy(LeadActivityPolicy::class)]
final class LeadActivity extends Model implements LeadActivityInterface, AuditableContract
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    // TODO(gen): resolve unknown trait `BelongsToLead` — add its import + `use` line.
    use HasMetadata;
    use Userstamps;
    use Auditable;
    use Filterable;

    /**
     * Cast map — from the blueprint's `x-eloquent.casts`.
     *
     * @var array<string, string>
     */
    protected $casts = [
        LeadActivityInterface::ATTR_TYPE => LeadActivityType::class,
        LeadActivityInterface::ATTR_METADATA => 'array',
        LeadActivityInterface::ATTR_OCCURRED_AT => 'datetime',
    ];
}
