<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

namespace Academorix\Leads\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Lead.owner_id changed OR the leads:reassign-stale command flushed stale-lead reassignment. Every fire writes an assignment activity via the observer.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchLeadAssignedNotification (opt-in to the new owner)
 * - monitoring::MetricsCollector
 *
 * @category Leads
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class LeadReassigned implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $leadId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $fromOwnerId  ULID | null
     * @param  string  $toOwnerId  ULID | null
     * @param  string  $reason  'manual' | 'stale_reassignment' | 'onboarding'
     * @param  string  $actorId  ULID | null (null = system-generated by the stale-reassignment job)
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $leadId,
        public readonly string $tenantId,
        public readonly string $fromOwnerId,
        public readonly string $toOwnerId,
        public readonly string $reason,
        public readonly string $actorId,
        public readonly string $at,
    ) {
    }
}
