<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

namespace Stackra\Leads\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Lead.stage transitions QUALIFIED -> TRIAL. Typically fired when the family accepts a trial-session invitation.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchLeadTrialBookedNotification (opt-in to family + assigned owner)
 * - monitoring::MetricsCollector
 *
 * @category Leads
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class LeadTrialBooked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $leadId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $ownerId  ULID | null
     * @param  string  $trialScheduledAt  ISO 8601 | null (from the trial_scheduled activity when known)
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $leadId,
        public readonly string $tenantId,
        public readonly string $ownerId,
        public readonly string $trialScheduledAt,
        public readonly string $at,
    ) {
    }
}
