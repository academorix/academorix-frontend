<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

namespace Stackra\Leads\Policies;

use Stackra\User\Models\User;
use Stackra\Leads\Models\LeadActivity;

/**
 * Authorization policy for {@see \Stackra\Leads\Models\LeadActivity}.
 *
 * Wired via `#[UsePolicy]` on the Model.
 *
 * @category Leads
 *
 * @since    0.1.0
 */
final class LeadActivityPolicy
{
    /**
     * inherits the parent Lead's viewAny scope; coach role narrowed to own-athlete leads.
     *
     * @return bool
     */
    public function viewAny(User $user, LeadActivity $model): bool
    {
        return $user->can('lead-activities.viewAny');
    }
    /**
     * view ability.
     *
     * @return bool
     */
    public function view(User $user, LeadActivity $model): bool
    {
        return $user->can('lead-activities.view');
    }
    /**
     * manual touchpoints only; system-generated stage_change / assignment activities are inserted by observers + skip policy checks.
     *
     * @return bool
     */
    public function create(User $user, LeadActivity $model): bool
    {
        return $user->can('lead-activities.create');
    }
    /**
     * activities are APPEND-ONLY (body / metadata polish handled via a dedicated `patchNote` action).
     *
     * @return bool
     */
    public function update(User $user, LeadActivity $model): bool
    {
        return $user->can('NEVER');
    }
    /**
     * activities cascade-delete only when the parent Lead is hard-deleted.
     *
     * @return bool
     */
    public function delete(User $user, LeadActivity $model): bool
    {
        return $user->can('NEVER');
    }
}
