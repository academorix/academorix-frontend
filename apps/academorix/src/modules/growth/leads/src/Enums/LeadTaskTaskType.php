<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

namespace Academorix\Leads\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `task_type` on `lead-task`.
 *
 * Enum: call / email / meeting / follow_up / send_document.
 *
 * @category Leads
 *
 * @since    0.1.0
 */
enum LeadTaskTaskType: string
{
    use Enum;

    case Call = 'call';
    case Email = 'email';
    case Meeting = 'meeting';
    case FollowUp = 'follow_up';
    case SendDocument = 'send_document';
}
