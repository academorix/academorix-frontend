<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth leads --force`.

declare(strict_types=1);

namespace Academorix\Leads\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `source` on `lead`.
 *
 * Enum: web_form / referral / walk_in / call / email / social / trial_signup / import / api.
 *
 * @category Leads
 *
 * @since    0.1.0
 */
enum LeadSource: string
{
    use Enum;

    case WebForm = 'web_form';
    case Referral = 'referral';
    case WalkIn = 'walk_in';
    case Call = 'call';
    case Email = 'email';
    case Social = 'social';
    case TrialSignup = 'trial_signup';
    case Import = 'import';
    case Api = 'api';
}
