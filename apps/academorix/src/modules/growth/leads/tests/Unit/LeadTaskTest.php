<?php

declare(strict_types=1);

use Stackra\Leads\Models\LeadTask;

it('is a placeholder — replace with actual LeadTask unit tests', function (): void {
    expect(LeadTask::class)->toBeString();
});
