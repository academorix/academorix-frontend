<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Attributes\WithoutIncrementing;
use Illuminate\Database\Eloquent\Model;
use Academorix\Referrals\Contracts\Data\ReferralInterface;
use Academorix\Referrals\Database\Factories\ReferralFactory;
use Stackra\Foundation\Concerns\Filterable;
use Stackra\Foundation\Concerns\HasMetadata;
use Academorix\Referrals\Enums\ReferralCancellationReason;
use Academorix\Referrals\Enums\ReferralStatus;
use Academorix\Referrals\Policies\ReferralPolicy;
use Stackra\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Attributes\UsePolicy;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Wildside\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use OwenIt\Auditing\Contracts\Auditable as AuditableContract;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a Referral.
 *
 * The tracked referrer × referred × program × code instance.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[Table(name: ReferralInterface::TABLE, key: ReferralInterface::PRIMARY_KEY, keyType: ReferralInterface::KEY_TYPE)]
#[Fillable([
    ReferralInterface::ATTR_TENANT_ID,
        ReferralInterface::ATTR_REFERRAL_PROGRAM_ID,
        ReferralInterface::ATTR_REFERRAL_CODE_ID,
        ReferralInterface::ATTR_REFERRER_TYPE,
        ReferralInterface::ATTR_REFERRER_ID,
        ReferralInterface::ATTR_REFERRED_TYPE,
        ReferralInterface::ATTR_REFERRED_ID,
        ReferralInterface::ATTR_REFERRED_EMAIL,
        ReferralInterface::ATTR_ATTRIBUTION_SNAPSHOT,
        ReferralInterface::ATTR_DEVICE_SNAPSHOT,
        ReferralInterface::ATTR_STATUS,
        ReferralInterface::ATTR_CLAIMED_AT,
        ReferralInterface::ATTR_SIGNED_UP_AT,
        ReferralInterface::ATTR_TRIGGER_EVENT_AT,
        ReferralInterface::ATTR_VESTED_AT,
        ReferralInterface::ATTR_REWARDED_AT,
        ReferralInterface::ATTR_CANCELLED_AT,
        ReferralInterface::ATTR_CANCELLATION_REASON,
        ReferralInterface::ATTR_FRAUD_SCORE,
        ReferralInterface::ATTR_METADATA,
])]
#[UseFactory(ReferralFactory::class)]
#[WithoutIncrementing]
#[UsePolicy(ReferralPolicy::class)]
final class Referral extends Model implements ReferralInterface, AuditableContract
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    // TODO(gen): resolve unknown trait `BelongsToReferralProgram` — add its import + `use` line.
    // TODO(gen): resolve unknown trait `HasReferralRewards` — add its import + `use` line.
    // TODO(gen): resolve unknown trait `HasReferralFraudFlags` — add its import + `use` line.
    use HasMetadata;
    use Userstamps;
    use Auditable;
    use LogsActivity;
    use Filterable;
    use SoftDeletes;

    /**
     * Cast map — from the blueprint's `x-eloquent.casts`.
     *
     * @var array<string, string>
     */
    protected $casts = [
        ReferralInterface::ATTR_STATUS => ReferralStatus::class,
        ReferralInterface::ATTR_CANCELLATION_REASON => ReferralCancellationReason::class,
        ReferralInterface::ATTR_ATTRIBUTION_SNAPSHOT => 'array',
        ReferralInterface::ATTR_DEVICE_SNAPSHOT => 'array',
        ReferralInterface::ATTR_FRAUD_SCORE => 'integer',
        ReferralInterface::ATTR_CLAIMED_AT => 'datetime',
        ReferralInterface::ATTR_SIGNED_UP_AT => 'datetime',
        ReferralInterface::ATTR_TRIGGER_EVENT_AT => 'datetime',
        ReferralInterface::ATTR_VESTED_AT => 'datetime',
        ReferralInterface::ATTR_REWARDED_AT => 'datetime',
        ReferralInterface::ATTR_CANCELLED_AT => 'datetime',
        ReferralInterface::ATTR_METADATA => 'array',
    ];
}
