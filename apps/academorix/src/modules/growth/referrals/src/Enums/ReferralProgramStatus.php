<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Stackra\Referrals\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `status` on `referral-program`.
 *
 * Enum: draft / active / paused / archived.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
enum ReferralProgramStatus: string
{
    use Enum;

    case Draft = 'draft';
    case Active = 'active';
    case Paused = 'paused';
    case Archived = 'archived';
}
