<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan referral:list` — List every referral row.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'referral:list',
    description: 'List every referral row.',
)]
final class ListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List every referral row', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List every referral row completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
