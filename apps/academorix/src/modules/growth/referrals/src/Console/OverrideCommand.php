<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan referral-fraud:override` — Override — referral fraud command.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'referral-fraud:override',
    description: 'Override — referral fraud command.',
)]
final class OverrideCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Override — referral fraud command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Override — referral fraud command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
