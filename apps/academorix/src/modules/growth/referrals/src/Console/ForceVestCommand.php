<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan referral:force-vest` — Force vest — referral command.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'referral:force-vest',
    description: 'Force vest — referral command.',
)]
final class ForceVestCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Force vest — referral command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Force vest — referral command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
