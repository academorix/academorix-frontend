<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Stackra\Referrals\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan referral-program:pause` — Pause — referral program command.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'referral-program:pause',
    description: 'Pause — referral program command.',
)]
final class PauseCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Pause — referral program command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Pause — referral program command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
