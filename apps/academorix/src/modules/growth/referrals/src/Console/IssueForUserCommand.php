<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan referral-code:issue-for-user` — Issue for user — referral code command.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'referral-code:issue-for-user',
    description: 'Issue for user — referral code command.',
)]
final class IssueForUserCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Issue for user — referral code command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Issue for user — referral code command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
