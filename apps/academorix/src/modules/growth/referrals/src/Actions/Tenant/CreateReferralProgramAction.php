<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Stackra\Referrals\Actions\Tenant;

use Stackra\Referrals\Contracts\Repositories\ReferralProgramRepositoryInterface;
use Stackra\Referrals\Data\ReferralProgramData;
use Stackra\Referrals\Data\Requests\CreateReferralProgramRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/referral-programs` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsAction(name: 'referrals.referral_programs.create')]
#[Post('/api/v1/referral-programs')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateReferralProgramAction
{
    use AsController;

    public function __construct(
        private readonly ReferralProgramRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `referral-program` from the validated request payload.
     *
     * @param  CreateReferralProgramRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateReferralProgramRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(ReferralProgramData::from($model), JsonResponse::HTTP_CREATED);
    }
}
