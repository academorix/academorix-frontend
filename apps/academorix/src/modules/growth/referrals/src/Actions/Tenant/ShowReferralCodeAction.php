<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Stackra\Referrals\Actions\Tenant;

use Stackra\Referrals\Contracts\Repositories\ReferralCodeRepositoryInterface;
use Stackra\Referrals\Data\ReferralCodeData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/referral-codes/{code}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsAction(name: 'referrals.referral_codes.show')]
#[Get('/api/v1/referral-codes/{code}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowReferralCodeAction
{
    use AsController;

    public function __construct(
        private readonly ReferralCodeRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `referral-code` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): ReferralCodeData
    {
        return ReferralCodeData::from($this->repository->findOrFail($id));
    }
}
