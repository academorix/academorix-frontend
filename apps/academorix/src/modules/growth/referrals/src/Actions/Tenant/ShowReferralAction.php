<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Actions\Tenant;

use Academorix\Referrals\Contracts\Repositories\ReferralRepositoryInterface;
use Academorix\Referrals\Data\ReferralData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Get;

/**
 * `GET /api/v1/referrals/{referral}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsAction(name: 'referrals.show')]
#[Get('/api/v1/referrals/{referral}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowReferralAction
{
    use AsController;

    public function __construct(
        private readonly ReferralRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `referral` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): ReferralData
    {
        return ReferralData::from($this->repository->findOrFail($id));
    }
}
