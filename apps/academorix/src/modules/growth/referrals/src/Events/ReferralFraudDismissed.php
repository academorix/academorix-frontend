<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Admin reviewed a flag with disposition = false_positive. Fires alongside ReferralFraudFlagReviewed for consumer convenience (a listener may want only dismissals).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ReferralFraudDismissed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $flagId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $referralId  ULID
     * @param  string  $dismissedAt  ISO 8601
     * @param  string  $dismissedByUserId  ULID
     * @param  string  $reason  string | null
     */
    public function __construct(
        public readonly string $flagId,
        public readonly string $tenantId,
        public readonly string $referralId,
        public readonly string $dismissedAt,
        public readonly string $dismissedByUserId,
        public readonly string $reason,
    ) {
    }
}
