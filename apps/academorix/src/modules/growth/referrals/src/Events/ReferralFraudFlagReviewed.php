<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Stackra\Referrals\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Admin reviewed a fraud flag — disposition set to confirmed_fraud / false_positive / manual_override_approved.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — fraud audit trail)
 * - activity::WriteToActivityLog
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ReferralFraudFlagReviewed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $flagId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $referralId  ULID
     * @param  string  $reviewedAt  ISO 8601
     * @param  string  $reviewedByUserId  ULID
     * @param  string  $disposition  'confirmed_fraud' | 'false_positive' | 'manual_override_approved'
     */
    public function __construct(
        public readonly string $flagId,
        public readonly string $tenantId,
        public readonly string $referralId,
        public readonly string $reviewedAt,
        public readonly string $reviewedByUserId,
        public readonly string $disposition,
    ) {
    }
}
