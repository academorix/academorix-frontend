<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Stackra\Referrals\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReferralClaimer created a Referral row in status 'claimed' — the referred user landed on the signup page with the referral cookie set. Fires ONCE per referrer-referred pair.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - referrals::SnapshotAttributionOnReferralClaimed
 * - notifications::DispatchReferralClaimedNotification (to referrer — 'your friend clicked')
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ReferralClaimed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $referralId  ULID (ref_*)
     * @param  string  $tenantId  ULID
     * @param  string  $programId  ULID
     * @param  string  $codeId  ULID | null
     * @param  string  $referrerType  'user' | 'anonymous_lead'
     * @param  string  $referrerId  ULID
     * @param  string  $claimedAt  ISO 8601
     * @param  array  $attributionSnapshot  object (frozen AttributionSnapshotBuilder output)
     * @param  int  $fraudScore  integer (0..100)
     */
    public function __construct(
        public readonly string $referralId,
        public readonly string $tenantId,
        public readonly string $programId,
        public readonly string $codeId,
        public readonly string $referrerType,
        public readonly string $referrerId,
        public readonly string $claimedAt,
        public readonly array $attributionSnapshot,
        public readonly int $fraudScore,
    ) {
    }
}
