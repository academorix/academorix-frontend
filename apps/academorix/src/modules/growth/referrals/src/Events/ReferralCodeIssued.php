<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Stackra\Referrals\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReferralCodeRepository committed a new referral_codes row. Fires exactly once per code. Distinguishes user_scoped (auto-generated per user) from campaign (admin-generated) from opaque (system-generated for A/B test cohorts).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateProgramCodeListCache
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ReferralCodeIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $codeId  ULID (rcd_*)
     * @param  string  $tenantId  ULID
     * @param  string  $programId  ULID
     * @param  string  $code  string (the visible code)
     * @param  string  $codeType  'user_scoped' | 'campaign' | 'opaque'
     * @param  string  $ownerType  'user' | 'campaign' | 'anonymous' | null
     * @param  string  $ownerUserId  ULID | null
     * @param  string  $campaignName  string | null
     * @param  int  $usageCap  integer | null
     * @param  string  $expiresAt  ISO 8601 | null
     */
    public function __construct(
        public readonly string $codeId,
        public readonly string $tenantId,
        public readonly string $programId,
        public readonly string $code,
        public readonly string $codeType,
        public readonly string $ownerType,
        public readonly string $ownerUserId,
        public readonly string $campaignName,
        public readonly int $usageCap,
        public readonly string $expiresAt,
    ) {
    }
}
