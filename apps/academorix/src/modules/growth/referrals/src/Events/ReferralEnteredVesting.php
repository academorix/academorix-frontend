<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Stackra\Referrals\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Referral.status transitioned trigger_pending -> vesting. VestingScheduler computed vesting_completes_at based on program config. For immediate-vesting programs, this fires in the same transaction as ReferralVested.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ReferralEnteredVesting implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $referralId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $vestingStartsAt  ISO 8601
     * @param  string  $vestingCompletesAt  ISO 8601
     */
    public function __construct(
        public readonly string $referralId,
        public readonly string $tenantId,
        public readonly string $vestingStartsAt,
        public readonly string $vestingCompletesAt,
    ) {
    }
}
