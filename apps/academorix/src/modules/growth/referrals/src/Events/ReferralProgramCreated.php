<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Stackra\Referrals\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReferralProgramRepository finished a successful create. Fires exactly once per (tenant_id, slug) tuple.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial-audit-adjacent)
 * - activity::WriteToActivityLog
 * - cache::InvalidateTenantProgramListCache
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ReferralProgramCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $programId  ULID (rpg_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $name  string
     * @param  string  $slug  string
     * @param  float  $side  'single_sided' | 'double_sided'
     * @param  string  $rewardTypeReferrer  string
     * @param  int  $rewardAmountReferrer  integer
     * @param  string  $triggerEvent  string
     * @param  string  $vestingRule  string
     * @param  string  $createdBy  ULID (usr_*)
     */
    public function __construct(
        public readonly string $programId,
        public readonly string $tenantId,
        public readonly string $name,
        public readonly string $slug,
        public readonly float $side,
        public readonly string $rewardTypeReferrer,
        public readonly int $rewardAmountReferrer,
        public readonly string $triggerEvent,
        public readonly string $vestingRule,
        public readonly string $createdBy,
    ) {
    }
}
