<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A user called POST /api/v1/me/invitations to send an invitation email/SMS. Fires per invitation recipient.
 *
 * ## Consumers
 *
 * - activity::WriteToActivityLog
 * - notifications::DispatchInvitationEmail (delivers the invitation to the recipient)
 * - observability::MetricsCollector (stackra.referrals.invitations_sent_total)
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ReferralInvitationSent implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $senderUserId  ULID
     * @param  string  $recipientEmail  string (hashed for analytics label)
     * @param  string  $channel  'email' | 'sms'
     * @param  string  $codeId  ULID (rcd_*)
     * @param  string  $templateSlug  string
     * @param  string  $sentAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $senderUserId,
        public readonly string $recipientEmail,
        public readonly string $channel,
        public readonly string $codeId,
        public readonly string $templateSlug,
        public readonly string $sentAt,
    ) {
    }
}
