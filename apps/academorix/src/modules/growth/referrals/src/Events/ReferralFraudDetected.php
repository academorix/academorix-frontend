<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReferralFraudFlagObserver.created — a new fraud flag was written to the DB. CRITICAL when severity IN ('block', 'critical'). Every critical fire pages the admin.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — fraud audit trail)
 * - activity::WriteToActivityLog
 * - notifications::DispatchReferralFraudFlaggedNotification (to admin — cannot-opt-out for critical severity)
 * - observability::MetricsCollector (stackra.referrals.fraud_flags_created_total labelled by flag_type + severity)
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ReferralFraudDetected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $flagId  ULID (rfr_*)
     * @param  string  $referralId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $flagType  string
     * @param  string  $severity  'warn' | 'block' | 'critical'
     * @param  int  $confidence  integer (0..100)
     * @param  string  $detectedAt  ISO 8601
     * @param  array  $details  object
     */
    public function __construct(
        public readonly string $flagId,
        public readonly string $referralId,
        public readonly string $tenantId,
        public readonly string $flagType,
        public readonly string $severity,
        public readonly int $confidence,
        public readonly string $detectedAt,
        public readonly array $details,
    ) {
    }
}
