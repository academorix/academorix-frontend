<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReferralReward.status transitioned vested -> materialized — MaterializeVestedRewardsJob successfully created a Finance credit and set finance_credit_id.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - activity::WriteToActivityLog
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ReferralRewardMaterialized implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $rewardId  ULID (rrw_*)
     * @param  string  $referralId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $recipientRole  'referrer' | 'referred'
     * @param  string  $rewardType  string
     * @param  int  $rewardAmount  integer
     * @param  string  $rewardCurrency  string | null (ISO 4217)
     * @param  string  $financeCreditId  UUID (Wave 4 finance FK)
     * @param  string  $materializedAt  ISO 8601
     */
    public function __construct(
        public readonly string $rewardId,
        public readonly string $referralId,
        public readonly string $tenantId,
        public readonly string $recipientRole,
        public readonly string $rewardType,
        public readonly int $rewardAmount,
        public readonly string $rewardCurrency,
        public readonly string $financeCreditId,
        public readonly string $materializedAt,
    ) {
    }
}
