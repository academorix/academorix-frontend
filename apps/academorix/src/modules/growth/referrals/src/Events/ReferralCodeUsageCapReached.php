<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py growth referrals --force`.

declare(strict_types=1);

namespace Academorix\Referrals\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ReferralCode.usage_count reached ReferralCode.usage_cap. Fires from ReferralCodeObserver.updated when the increment crosses the threshold.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchCodeCapReachedNotification (to campaign owner + admin)
 *
 * @category Referrals
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ReferralCodeUsageCapReached implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $codeId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $code  string
     * @param  int  $usageCap  integer
     */
    public function __construct(
        public readonly string $codeId,
        public readonly string $tenantId,
        public readonly string $code,
        public readonly int $usageCap,
    ) {
    }
}
