<?php

declare(strict_types=1);

use Stackra\Referrals\Models\Referral;

it('is a placeholder — replace with actual Referral unit tests', function (): void {
    expect(Referral::class)->toBeString();
});
