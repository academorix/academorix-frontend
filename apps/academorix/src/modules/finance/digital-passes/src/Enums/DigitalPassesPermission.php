<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance digital-passes --force`.

declare(strict_types=1);

namespace Academorix\DigitalPasses\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the DigitalPasses module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category DigitalPasses
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum DigitalPassesPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `wallet-passes.viewAny` — Wallet-passes ViewAny
     */
    #[Label('Wallet-passes ViewAny')]
    #[Description('Wallet-passes ViewAny')]
    case WalletPassesViewAny = 'wallet-passes.viewAny';

    /**
     * `wallet-passes.view` — Wallet-passes View
     */
    #[Label('Wallet-passes View')]
    #[Description('Wallet-passes View')]
    case WalletPassesView = 'wallet-passes.view';

    /**
     * `wallet-passes.issue` — Wallet-passes Issue
     */
    #[Label('Wallet-passes Issue')]
    #[Description('Wallet-passes Issue')]
    case WalletPassesIssue = 'wallet-passes.issue';

    /**
     * `wallet-passes.regenerate` — Wallet-passes Regenerate
     */
    #[Label('Wallet-passes Regenerate')]
    #[Description('Wallet-passes Regenerate')]
    case WalletPassesRegenerate = 'wallet-passes.regenerate';

    /**
     * `wallet-passes.revoke` — Wallet-passes Revoke
     */
    #[Label('Wallet-passes Revoke')]
    #[Description('Wallet-passes Revoke')]
    case WalletPassesRevoke = 'wallet-passes.revoke';

    /**
     * `wallet-passes.download` — Wallet-passes Download
     */
    #[Label('Wallet-passes Download')]
    #[Description('Wallet-passes Download')]
    case WalletPassesDownload = 'wallet-passes.download';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::WalletPassesViewAny => Guard::Sanctum,
            self::WalletPassesView => Guard::Sanctum,
            self::WalletPassesIssue => Guard::Sanctum,
            self::WalletPassesRegenerate => Guard::Sanctum,
            self::WalletPassesRevoke => Guard::Sanctum,
            self::WalletPassesDownload => Guard::Sanctum,
        };
    }
}
