<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance digital-passes --force`.

declare(strict_types=1);

namespace Academorix\DigitalPasses\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan passes:regenerate-all` — Regenerate all — passes command.
 *
 * @category DigitalPasses
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'passes:regenerate-all',
    description: 'Regenerate all — passes command.',
)]
final class RegenerateAllCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Regenerate all — passes command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Regenerate all — passes command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
