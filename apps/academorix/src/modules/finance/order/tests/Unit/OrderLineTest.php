<?php

declare(strict_types=1);

use Stackra\Order\Models\OrderLine;

it('is a placeholder — replace with actual OrderLine unit tests', function (): void {
    expect(OrderLine::class)->toBeString();
});
