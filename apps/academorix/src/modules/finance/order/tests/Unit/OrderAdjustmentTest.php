<?php

declare(strict_types=1);

use Stackra\Order\Models\OrderAdjustment;

it('is a placeholder — replace with actual OrderAdjustment unit tests', function (): void {
    expect(OrderAdjustment::class)->toBeString();
});
