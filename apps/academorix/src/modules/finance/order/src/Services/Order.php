<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance order --force`.

declare(strict_types=1);

namespace Stackra\Order\Services;

use Stackra\Order\Contracts\Services\OrderInterface;
use Illuminate\Container\Attributes\Scoped;
use Stackra\Order\Contracts\Repositories\OrderAdjustmentRepositoryInterface;

/**
 * Concrete service — Order.
 *
 * Implements {@see OrderInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Order
 *
 * @since    0.1.0
 */
#[Scoped]
final class Order implements OrderInterface
{
    /**
     * @param  OrderAdjustmentRepositoryInterface  $orderAdjustmentRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly OrderAdjustmentRepositoryInterface $orderAdjustmentRepository,
    ) {
    }
}
