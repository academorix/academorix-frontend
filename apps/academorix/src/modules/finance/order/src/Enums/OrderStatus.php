<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance order --force`.

declare(strict_types=1);

namespace Stackra\Order\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `status` on `order`.
 *
 * Enum: draft / pending / partially_paid / paid / abandoned / cancelled / refunded / fulfilled.
 *
 * @category Order
 *
 * @since    0.1.0
 */
enum OrderStatus: string
{
    use Enum;

    case Draft = 'draft';
    case Pending = 'pending';
    case PartiallyPaid = 'partially_paid';
    case Paid = 'paid';
    case Abandoned = 'abandoned';
    case Cancelled = 'cancelled';
    case Refunded = 'refunded';
    case Fulfilled = 'fulfilled';
}
