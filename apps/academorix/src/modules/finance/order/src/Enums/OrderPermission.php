<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance order --force`.

declare(strict_types=1);

namespace Academorix\Order\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Order module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Order
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum OrderPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `orders.viewAny` — Orders ViewAny
     */
    #[Label('Orders ViewAny')]
    #[Description('Orders ViewAny')]
    case OrdersViewAny = 'orders.viewAny';

    /**
     * `orders.view` — Orders View
     */
    #[Label('Orders View')]
    #[Description('Orders View')]
    case OrdersView = 'orders.view';

    /**
     * `orders.create` — Orders Create
     */
    #[Label('Orders Create')]
    #[Description('Orders Create')]
    case OrdersCreate = 'orders.create';

    /**
     * `orders.update` — Modify draft/pending order lines + adjustments.
     */
    #[Label('Modify draft/pending order lines + adjustments')]
    #[Description('Modify draft/pending order lines + adjustments.')]
    case OrdersUpdate = 'orders.update';

    /**
     * `orders.delete` — Soft-delete a draft order.
     */
    #[Label('Soft-delete a draft order')]
    #[Description('Soft-delete a draft order.')]
    case OrdersDelete = 'orders.delete';

    /**
     * `orders.checkout` — Transition draft->pending and initiate payment.
     */
    #[Label('Transition draft->pending and initiate payment')]
    #[Description('Transition draft->pending and initiate payment.')]
    case OrdersCheckout = 'orders.checkout';

    /**
     * `orders.cancel` — Cancel pending/paid orders.
     */
    #[Label('Cancel pending/paid orders')]
    #[Description('Cancel pending/paid orders.')]
    case OrdersCancel = 'orders.cancel';

    /**
     * `orders.refund` — Refund a paid order (routes through finance/refund).
     */
    #[Label('Refund a paid order (routes through finance/refund)')]
    #[Description('Refund a paid order (routes through finance/refund).')]
    case OrdersRefund = 'orders.refund';

    /**
     * `orders.recalculate` — Force-recalculate totals (e.
     */
    #[Label('Force-recalculate totals (e')]
    #[Description('Force-recalculate totals (e.g. after price changes).')]
    case OrdersRecalculate = 'orders.recalculate';

    /**
     * `order-lines.create` — Order-lines Create
     */
    #[Label('Order-lines Create')]
    #[Description('Order-lines Create')]
    case OrderLinesCreate = 'order-lines.create';

    /**
     * `order-lines.update` — Order-lines Update
     */
    #[Label('Order-lines Update')]
    #[Description('Order-lines Update')]
    case OrderLinesUpdate = 'order-lines.update';

    /**
     * `order-lines.delete` — Order-lines Delete
     */
    #[Label('Order-lines Delete')]
    #[Description('Order-lines Delete')]
    case OrderLinesDelete = 'order-lines.delete';

    /**
     * `order-adjustments.create` — Apply coupon / credit / manual discount.
     */
    #[Label('Apply coupon / credit / manual discount')]
    #[Description('Apply coupon / credit / manual discount.')]
    case OrderAdjustmentsCreate = 'order-adjustments.create';

    /**
     * `order-adjustments.delete` — Order-adjustments Delete
     */
    #[Label('Order-adjustments Delete')]
    #[Description('Order-adjustments Delete')]
    case OrderAdjustmentsDelete = 'order-adjustments.delete';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::OrdersViewAny => Guard::Sanctum,
            self::OrdersView => Guard::Sanctum,
            self::OrdersCreate => Guard::Sanctum,
            self::OrdersUpdate => Guard::Sanctum,
            self::OrdersDelete => Guard::Sanctum,
            self::OrdersCheckout => Guard::Sanctum,
            self::OrdersCancel => Guard::Sanctum,
            self::OrdersRefund => Guard::Sanctum,
            self::OrdersRecalculate => Guard::Sanctum,
            self::OrderLinesCreate => Guard::Sanctum,
            self::OrderLinesUpdate => Guard::Sanctum,
            self::OrderLinesDelete => Guard::Sanctum,
            self::OrderAdjustmentsCreate => Guard::Sanctum,
            self::OrderAdjustmentsDelete => Guard::Sanctum,
        };
    }
}
