<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance order --force`.

declare(strict_types=1);

namespace Academorix\Order\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan order:expire-abandoned` — Expire abandoned — order command.
 *
 * @category Order
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'order:expire-abandoned',
    description: 'Expire abandoned — order command.',
)]
final class ExpireAbandonedCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Expire abandoned — order command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Expire abandoned — order command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
