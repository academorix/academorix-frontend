<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance order --force`.

declare(strict_types=1);

namespace Academorix\Order\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan order:cancel` — Cancel — order command.
 *
 * @category Order
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'order:cancel',
    description: 'Cancel — order command.',
)]
final class CancelCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Cancel — order command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Cancel — order command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
