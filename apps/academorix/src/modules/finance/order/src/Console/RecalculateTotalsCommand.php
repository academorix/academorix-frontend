<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance order --force`.

declare(strict_types=1);

namespace Stackra\Order\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan order:recalculate-totals` — Recalculate totals — order command.
 *
 * @category Order
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'order:recalculate-totals',
    description: 'Recalculate totals — order command.',
)]
final class RecalculateTotalsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Recalculate totals — order command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Recalculate totals — order command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
