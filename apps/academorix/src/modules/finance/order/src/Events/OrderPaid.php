<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance order --force`.

declare(strict_types=1);

namespace Stackra\Order\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Order status -> paid. Sum of successful payments >= total. LOAD-BEARING EVENT.
 *
 * ## Consumers
 *
 * - invoice::GenerateInvoiceFromOrderListener
 * - wallet::CommitWalletHoldsListener
 * - coupon::FinalizeCouponRedemptionListener
 * - membership::ProvisionMembershipListener
 * - entitlements::GrantEntitlementsListener
 * - notifications::DispatchOrderPaidNotificationListener
 * - audit::WriteToAuditLog
 *
 * @category Order
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class OrderPaid implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $orderId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $totalMinor  integer
     * @param  int  $amountPaidMinor  integer
     * @param  string  $paidAt  ISO 8601
     */
    public function __construct(
        public readonly string $orderId,
        public readonly string $tenantId,
        public readonly int $totalMinor,
        public readonly int $amountPaidMinor,
        public readonly string $paidAt,
    ) {
    }
}
