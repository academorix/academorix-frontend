<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Stackra\Membership\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * MembershipRenewal succeeded. Membership.next_renewal_at extended by billing_interval; passes issued for the new period; consecutive_failed_renewal_count reset. Lifetime value increment.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchMembershipRenewalSucceededNotification (transactional)
 * - membership::ExtendPeriodOnRenewalSucceeded (dispatches IssuePassesForPeriodJob for new period)
 * - growth::analytics::TrackMembershipRenewal
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MembershipRenewed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $membershipId  ULID
     * @param  string  $renewalId  ULID (mbn_*)
     * @param  string  $tenantId  ULID
     * @param  string  $renewedAt  ISO 8601
     * @param  string  $periodStartsAt  ISO 8601
     * @param  string  $periodEndsAt  ISO 8601
     * @param  int  $amountChargedCents  integer
     * @param  string  $newNextRenewalAt  ISO 8601
     */
    public function __construct(
        public readonly string $membershipId,
        public readonly string $renewalId,
        public readonly string $tenantId,
        public readonly string $renewedAt,
        public readonly string $periodStartsAt,
        public readonly string $periodEndsAt,
        public readonly int $amountChargedCents,
        public readonly string $newNextRenewalAt,
    ) {
    }
}
