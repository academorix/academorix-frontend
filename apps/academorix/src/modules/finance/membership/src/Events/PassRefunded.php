<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Stackra\Membership\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Admin returned a pass (e.g. session cancelled). Pass returns to available; membership pass count restored.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchPassRefundedNotification
 * - cache::InvalidatePassCache
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PassRefunded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $passId  ULID
     * @param  string  $membershipId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $refundedAt  ISO 8601
     * @param  string  $refundReason  string
     * @param  string  $refundedByUserId  ULID
     */
    public function __construct(
        public readonly string $passId,
        public readonly string $membershipId,
        public readonly string $tenantId,
        public readonly string $refundedAt,
        public readonly string $refundReason,
        public readonly string $refundedByUserId,
    ) {
    }
}
