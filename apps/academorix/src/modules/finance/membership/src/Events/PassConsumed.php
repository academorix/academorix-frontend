<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Session check-in consumed a pass. Refused if pass already consumed OR outside valid window.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidatePassCache
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PassConsumed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $passId  ULID
     * @param  string  $membershipId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $consumedAt  ISO 8601
     * @param  string  $consumedBySessionId  ULID (ses_*) | null
     */
    public function __construct(
        public readonly string $passId,
        public readonly string $membershipId,
        public readonly string $tenantId,
        public readonly string $consumedAt,
        public readonly string $consumedBySessionId,
    ) {
    }
}
