<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Stackra\Membership\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Medium+. Membership created with plan.trial_days > 0. First period is free; billing starts at trial end. NotifyTrialEndingJob fires 3 days before trial end.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchMembershipTrialStartedNotification
 * - growth::analytics::TrackTrialStart
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MembershipTrialStarted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $membershipId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $trialStartsAt  ISO 8601
     * @param  string  $trialEndsAt  ISO 8601
     * @param  int  $trialDays  integer
     */
    public function __construct(
        public readonly string $membershipId,
        public readonly string $tenantId,
        public readonly string $trialStartsAt,
        public readonly string $trialEndsAt,
        public readonly int $trialDays,
    ) {
    }
}
