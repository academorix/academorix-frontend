<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Stackra\Membership\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Admin resumed a paused membership. next_renewal_at recomputed from resume date + remaining period.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchMembershipResumedNotification
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MembershipResumed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $membershipId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $resumedAt  ISO 8601
     * @param  string  $resumedByUserId  ULID
     * @param  string  $newNextRenewalAt  ISO 8601 | null
     */
    public function __construct(
        public readonly string $membershipId,
        public readonly string $tenantId,
        public readonly string $resumedAt,
        public readonly string $resumedByUserId,
        public readonly string $newNextRenewalAt,
    ) {
    }
}
