<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * MembershipSettings — Memberships.
 *
 * Tenant-wide membership workflow settings.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'membership',
    label: 'Memberships',
    description: 'Tenant-wide membership workflow settings.',
    icon: 'credit-card',
    scope: 'tenant',
    permission: 'memberships.viewAny',
    sortOrder: 52,
)]
class MembershipSettings extends Settings
{

    // ── Billing ──────────────────────────────────
    #[SettingGroup(label: 'Billing', icon: 'banknotes', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Default currency', group: 'Billing', sortOrder: 1)]
    public string $default_currency = 'USD';

    #[SettingField(controlType: ControlType::Toggle, label: 'Reapply coupons on renewals (subject to coupon rules)', group: 'Billing', sortOrder: 2)]
    public bool $reuse_coupon_on_renewal = true;


    // ── Renewals + dunning ──────────────────────────────────
    #[SettingGroup(label: 'Renewals + dunning', icon: 'arrow-path', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Number, label: 'Days before renewal to notify', group: 'Renewals + dunning', sortOrder: 1, min: 1, max: 30)]
    public int $renewal_upcoming_notification_days = 7;

    #[SettingField(controlType: ControlType::Number, label: 'Hours after failed payment for first reminder', group: 'Renewals + dunning', sortOrder: 2, min: 1, max: 168)]
    public int $dunning_first_reminder_hours = 24;

    #[SettingField(controlType: ControlType::Number, label: 'Hours for second reminder', group: 'Renewals + dunning', sortOrder: 3, min: 1, max: 336)]
    public int $dunning_second_reminder_hours = 72;

    #[SettingField(controlType: ControlType::Number, label: 'Hours for third reminder', group: 'Renewals + dunning', sortOrder: 4, min: 1, max: 672)]
    public int $dunning_third_reminder_hours = 168;

    #[SettingField(controlType: ControlType::Number, label: 'Attempt at which SMS dunning kicks in', group: 'Renewals + dunning', sortOrder: 5, min: 1, max: 4)]
    public int $dunning_sms_from_attempt = 3;


    // ── Cancellation ──────────────────────────────────
    #[SettingGroup(label: 'Cancellation', icon: 'x-mark', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Select, label: 'Default cancellation flow', group: 'Cancellation', sortOrder: 1, options: ['soft_period_end' => 'Soft (ends at period)', 'hard_immediate' => 'Hard (immediate + prorated refund)'])]
    public string $default_cancellation_kind = 'soft_period_end';

    #[SettingField(controlType: ControlType::Number, label: 'Minimum length for cancellation reason', group: 'Cancellation', sortOrder: 2, min: 0, max: 50)]
    public int $cancellation_reason_min_length = 3;

    #[SettingField(controlType: ControlType::Number, label: 'Buyer\'s remorse window (hours)', group: 'Cancellation', sortOrder: 3, min: 0, max: 168, helpText: 'Hours after signup during which customer can hard-cancel without admin approval (regardless of tier).')]
    public int $self_serve_buyers_remorse_hours = 24;


    // ── Passes ──────────────────────────────────
    #[SettingGroup(label: 'Passes', icon: 'ticket', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Number, label: 'Days before pass expiry to notify customer', group: 'Passes', sortOrder: 1, min: 0, max: 30)]
    public int $pass_expiring_notification_days = 7;


    // ── Statements ──────────────────────────────────
    #[SettingGroup(label: 'Statements', icon: 'document-chart-bar', sortOrder: 5)]
    #[SettingField(controlType: ControlType::Number, label: 'Day of month for monthly statement generation', group: 'Statements', sortOrder: 1, min: 1, max: 28)]
    public int $monthly_statement_day_of_month = 1;

    #[SettingField(controlType: ControlType::Select, label: 'Statement delivery', group: 'Statements', sortOrder: 2, options: ['email' => 'Email PDF', 'in_app_only' => 'In-app only', 'email_and_in_app' => 'Email + in-app'])]
    public string $monthly_statement_delivery_channel = 'email';

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'membership';
    }
}
