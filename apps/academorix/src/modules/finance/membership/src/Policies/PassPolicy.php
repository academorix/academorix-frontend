<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Policies;

use Stackra\User\Models\User;
use Academorix\Membership\Models\Pass;

/**
 * Authorization policy for {@see \Academorix\Membership\Models\Pass}.
 *
 * Wired via `#[UsePolicy]` on the Model.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
final class PassPolicy
{
    /**
     * reception + coach + finance_officer + admin.
     *
     * @return bool
     */
    public function viewAny(User $user, Pass $model): bool
    {
        return $user->can('passes.viewAny');
    }
    /**
     * guardians + adult athletes see their family's passes
     *
     * @return bool
     */
    public function view(User $user, Pass $model): bool
    {
        return $user->can('passes.view.own');
    }
    /**
     * reception + coach + kiosk + session module's service account.
     *
     * @return bool
     */
    public function consume(User $user, Pass $model): bool
    {
        return $user->can('passes.consume');
    }
    /**
     * reception + finance_officer + admin + owner.
     *
     * @return bool
     */
    public function refund(User $user, Pass $model): bool
    {
        return $user->can('passes.refund');
    }
}
