<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `pass_type` on `pass`.
 *
 * Enum: standard / trial / bonus / carryover / event / promotional.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
enum PassPassType: string
{
    use Enum;

    case Standard = 'standard';
    case Trial = 'trial';
    case Bonus = 'bonus';
    case Carryover = 'carryover';
    case Event = 'event';
    case Promotional = 'promotional';
}
