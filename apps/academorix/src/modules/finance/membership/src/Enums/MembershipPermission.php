<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Membership module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum MembershipPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `memberships.viewAny` — Memberships ViewAny
     */
    #[Label('Memberships ViewAny')]
    #[Description('Memberships ViewAny')]
    case MembershipsViewAny = 'memberships.viewAny';

    /**
     * `memberships.viewAny.own` — Guardian + adult-athlete scoped listing.
     */
    #[Label('Guardian + adult-athlete scoped listing')]
    #[Description('Guardian + adult-athlete scoped listing.')]
    case MembershipsViewAnyOwn = 'memberships.viewAny.own';

    /**
     * `memberships.view` — Memberships View
     */
    #[Label('Memberships View')]
    #[Description('Memberships View')]
    case MembershipsView = 'memberships.view';

    /**
     * `memberships.view.own` — Memberships View Own
     */
    #[Label('Memberships View Own')]
    #[Description('Memberships View Own')]
    case MembershipsViewOwn = 'memberships.view.own';

    /**
     * `memberships.create` — Memberships Create
     */
    #[Label('Memberships Create')]
    #[Description('Memberships Create')]
    case MembershipsCreate = 'memberships.create';

    /**
     * `memberships.create.own` — Guardians + adult athletes may self-subscribe.
     */
    #[Label('Guardians + adult athletes may self-subscribe')]
    #[Description('Guardians + adult athletes may self-subscribe.')]
    case MembershipsCreateOwn = 'memberships.create.own';

    /**
     * `memberships.pause` — Freeze billing + pass consumption — resumable.
     */
    #[Label('Freeze billing + pass consumption — resumable')]
    #[Description('Freeze billing + pass consumption — resumable.')]
    case MembershipsPause = 'memberships.pause';

    /**
     * `memberships.pause.own` — Guardians can pause their own memberships (limited hardship pause).
     */
    #[Label('Guardians can pause their own memberships (limited hardship pause)')]
    #[Description('Guardians can pause their own memberships (limited hardship pause).')]
    case MembershipsPauseOwn = 'memberships.pause.own';

    /**
     * `memberships.resume` — Memberships Resume
     */
    #[Label('Memberships Resume')]
    #[Description('Memberships Resume')]
    case MembershipsResume = 'memberships.resume';

    /**
     * `memberships.resume.own` — Memberships Resume Own
     */
    #[Label('Memberships Resume Own')]
    #[Description('Memberships Resume Own')]
    case MembershipsResumeOwn = 'memberships.resume.own';

    /**
     * `memberships.cancel` — Soft or hard cancel.
     */
    #[Label('Soft or hard cancel')]
    #[Description('Soft or hard cancel. Hard requires membership_hard_cancel_prorated_refund entitlement.')]
    case MembershipsCancel = 'memberships.cancel';

    /**
     * `memberships.cancel.own` — Guardians + adult athletes may cancel own memberships (soft-cancel only).
     */
    #[Label('Guardians + adult athletes may cancel own memberships (soft-cancel only)')]
    #[Description('Guardians + adult athletes may cancel own memberships (soft-cancel only).')]
    case MembershipsCancelOwn = 'memberships.cancel.own';

    /**
     * `memberships.reissue-passes` — Admin issues bonus/promotional passes outside per-period grant.
     */
    #[Label('Admin issues bonus/promotional passes outside per-period grant')]
    #[Description('Admin issues bonus/promotional passes outside per-period grant. Audit-critical.')]
    case MembershipsReissuePasses = 'memberships.reissue-passes';

    /**
     * `memberships.report` — MRR + churn + dunning-recovery dashboards.
     */
    #[Label('MRR + churn + dunning-recovery dashboards')]
    #[Description('MRR + churn + dunning-recovery dashboards. Business-sensitive.')]
    case MembershipsReport = 'memberships.report';

    /**
     * `memberships.audit.refund` — Read refund history + investigate chargebacks.
     */
    #[Label('Read refund history + investigate chargebacks')]
    #[Description('Read refund history + investigate chargebacks. Compliance-critical.')]
    case MembershipsAuditRefund = 'memberships.audit.refund';

    /**
     * `memberships.audit.chargeback` — Read chargeback history for a membership.
     */
    #[Label('Read chargeback history for a membership')]
    #[Description('Read chargeback history for a membership. Admin + owner + finance_officer.')]
    case MembershipsAuditChargeback = 'memberships.audit.chargeback';

    /**
     * `membership_plans.viewAny` — The plan catalog is customer-readable for signup flow.
     */
    #[Label('The plan catalog is customer-readable for signup flow')]
    #[Description('The plan catalog is customer-readable for signup flow.')]
    case MembershipPlansViewAny = 'membership_plans.viewAny';

    /**
     * `membership_plans.view` — Membership Plans View
     */
    #[Label('Membership Plans View')]
    #[Description('Membership Plans View')]
    case MembershipPlansView = 'membership_plans.view';

    /**
     * `membership_plans.create` — Membership Plans Create
     */
    #[Label('Membership Plans Create')]
    #[Description('Membership Plans Create')]
    case MembershipPlansCreate = 'membership_plans.create';

    /**
     * `membership_plans.update` — Membership Plans Update
     */
    #[Label('Membership Plans Update')]
    #[Description('Membership Plans Update')]
    case MembershipPlansUpdate = 'membership_plans.update';

    /**
     * `membership_plans.archive` — Membership Plans Archive
     */
    #[Label('Membership Plans Archive')]
    #[Description('Membership Plans Archive')]
    case MembershipPlansArchive = 'membership_plans.archive';

    /**
     * `membership_renewals.viewAny` — Read renewal history for staff analytics.
     */
    #[Label('Read renewal history for staff analytics')]
    #[Description('Read renewal history for staff analytics.')]
    case MembershipRenewalsViewAny = 'membership_renewals.viewAny';

    /**
     * `membership_renewals.view.own` — Membership Renewals View Own
     */
    #[Label('Membership Renewals View Own')]
    #[Description('Membership Renewals View Own')]
    case MembershipRenewalsViewOwn = 'membership_renewals.view.own';

    /**
     * `passes.viewAny` — Passes ViewAny
     */
    #[Label('Passes ViewAny')]
    #[Description('Passes ViewAny')]
    case PassesViewAny = 'passes.viewAny';

    /**
     * `passes.view.own` — Passes View Own
     */
    #[Label('Passes View Own')]
    #[Description('Passes View Own')]
    case PassesViewOwn = 'passes.view.own';

    /**
     * `passes.consume` — Consume a pass at session check-in.
     */
    #[Label('Consume a pass at session check-in')]
    #[Description('Consume a pass at session check-in. Held by the reception + coach + kiosk roles + session module\'s internal service account.')]
    case PassesConsume = 'passes.consume';

    /**
     * `passes.refund` — Return a consumed pass (session cancelled).
     */
    #[Label('Return a consumed pass (session cancelled)')]
    #[Description('Return a consumed pass (session cancelled). Admin oversight required.')]
    case PassesRefund = 'passes.refund';

    /**
     * `platform.memberships.viewAny` — Platform Memberships ViewAny
     */
    #[Label('Platform Memberships ViewAny')]
    #[Description('Platform Memberships ViewAny')]
    case PlatformMembershipsViewAny = 'platform.memberships.viewAny';

    /**
     * `platform.memberships.view` — Platform Memberships View
     */
    #[Label('Platform Memberships View')]
    #[Description('Platform Memberships View')]
    case PlatformMembershipsView = 'platform.memberships.view';

    /**
     * `platform.memberships.audit.refund` — Cross-tenant refund investigation.
     */
    #[Label('Cross-tenant refund investigation')]
    #[Description('Cross-tenant refund investigation. Super_admin + ops only.')]
    case PlatformMembershipsAuditRefund = 'platform.memberships.audit.refund';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::MembershipsViewAny => Guard::Sanctum,
            self::MembershipsViewAnyOwn => Guard::Sanctum,
            self::MembershipsView => Guard::Sanctum,
            self::MembershipsViewOwn => Guard::Sanctum,
            self::MembershipsCreate => Guard::Sanctum,
            self::MembershipsCreateOwn => Guard::Sanctum,
            self::MembershipsPause => Guard::Sanctum,
            self::MembershipsPauseOwn => Guard::Sanctum,
            self::MembershipsResume => Guard::Sanctum,
            self::MembershipsResumeOwn => Guard::Sanctum,
            self::MembershipsCancel => Guard::Sanctum,
            self::MembershipsCancelOwn => Guard::Sanctum,
            self::MembershipsReissuePasses => Guard::Sanctum,
            self::MembershipsReport => Guard::Sanctum,
            self::MembershipsAuditRefund => Guard::Sanctum,
            self::MembershipsAuditChargeback => Guard::Sanctum,
            self::MembershipPlansViewAny => Guard::Sanctum,
            self::MembershipPlansView => Guard::Sanctum,
            self::MembershipPlansCreate => Guard::Sanctum,
            self::MembershipPlansUpdate => Guard::Sanctum,
            self::MembershipPlansArchive => Guard::Sanctum,
            self::MembershipRenewalsViewAny => Guard::Sanctum,
            self::MembershipRenewalsViewOwn => Guard::Sanctum,
            self::PassesViewAny => Guard::Sanctum,
            self::PassesViewOwn => Guard::Sanctum,
            self::PassesConsume => Guard::Sanctum,
            self::PassesRefund => Guard::Sanctum,
            self::PlatformMembershipsViewAny => Guard::PlatformAdmin,
            self::PlatformMembershipsView => Guard::PlatformAdmin,
            self::PlatformMembershipsAuditRefund => Guard::PlatformAdmin,
        };
    }
}
