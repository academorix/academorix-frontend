<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `status` on `membership-renewal`.
 *
 * Enum: pending / attempting / succeeded / failed_transient / failed_permanent / cancelled.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
enum MembershipRenewalStatus: string
{
    use Enum;

    case Pending = 'pending';
    case Attempting = 'attempting';
    case Succeeded = 'succeeded';
    case FailedTransient = 'failed_transient';
    case FailedPermanent = 'failed_permanent';
    case Cancelled = 'cancelled';
}
