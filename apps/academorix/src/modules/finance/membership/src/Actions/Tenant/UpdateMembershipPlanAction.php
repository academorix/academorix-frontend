<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Stackra\Membership\Actions\Tenant;

use Stackra\Membership\Contracts\Repositories\MembershipPlanRepositoryInterface;
use Stackra\Membership\Data\MembershipPlanData;
use Stackra\Membership\Data\Requests\UpdateMembershipPlanRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/membership-plans/{plan}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsAction(name: 'membership.membership_plans.update')]
#[Patch('/api/v1/membership-plans/{plan}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateMembershipPlanAction
{
    use AsController;

    public function __construct(
        private readonly MembershipPlanRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `membership-plan` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateMembershipPlanRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateMembershipPlanRequestData $data): MembershipPlanData
    {
        $model = $this->repository->update($id, $data->toArray());

        return MembershipPlanData::from($model);
    }
}
