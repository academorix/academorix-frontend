<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Stackra\Membership\Actions\Platform;

use Stackra\Membership\Contracts\Repositories\MembershipRepositoryInterface;
use Stackra\Membership\Data\MembershipData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/platform/memberships/{membership}` — show action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsAction(name: 'membership.memberships.show')]
#[Get('/api/v1/platform/memberships/{membership}')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class ShowMembershipAction
{
    use AsController;

    public function __construct(
        private readonly MembershipRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `membership` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): MembershipData
    {
        return MembershipData::from($this->repository->findOrFail($id));
    }
}
