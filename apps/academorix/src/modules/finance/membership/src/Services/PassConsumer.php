<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Stackra\Membership\Services;

use Stackra\Membership\Contracts\Services\PassConsumerInterface;
use Illuminate\Container\Attributes\Scoped;
use Stackra\Membership\Contracts\Repositories\MembershipPlanRepositoryInterface;

/**
 * Concrete service — PassConsumer.
 *
 * Implements {@see PassConsumerInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[Scoped]
final class PassConsumer implements PassConsumerInterface
{
    /**
     * @param  MembershipPlanRepositoryInterface  $membershipPlanRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly MembershipPlanRepositoryInterface $membershipPlanRepository,
    ) {
    }
}
