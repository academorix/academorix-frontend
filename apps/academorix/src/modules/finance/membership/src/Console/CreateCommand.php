<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Stackra\Membership\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan membership-plan:create` — Create a new membership plan.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'membership-plan:create',
    description: 'Create a new membership plan.',
)]
final class CreateCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Create a new membership plan', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Create a new membership plan completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
