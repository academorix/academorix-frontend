<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan membership-plan:archive` — Archive — membership plan command.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'membership-plan:archive',
    description: 'Archive — membership plan command.',
)]
final class ArchiveCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Archive — membership plan command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Archive — membership plan command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
