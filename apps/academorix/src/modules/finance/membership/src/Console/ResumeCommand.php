<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan membership:resume` — Resume — membership command.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'membership:resume',
    description: 'Resume — membership command.',
)]
final class ResumeCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Resume — membership command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Resume — membership command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
