<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance membership --force`.

declare(strict_types=1);

namespace Academorix\Membership\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan membership:renew` — Renew — membership command.
 *
 * @category Membership
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'membership:renew',
    description: 'Renew — membership command.',
)]
final class RenewCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Renew — membership command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Renew — membership command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
