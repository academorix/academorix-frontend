<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Manual capture attempt failed. Authorization hold may have expired or provider rejected. The intent stays in requires_capture state until retry-limit exhausted or manually cancelled.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchPaymentFailedNotification
 * - monitoring::MetricsCollector
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PaymentCaptureFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $paymentIntentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $errorCode  string | null
     * @param  string  $errorMessage  string
     * @param  int  $attemptNumber  integer
     * @param  string  $failedAt  ISO 8601
     */
    public function __construct(
        public readonly string $paymentIntentId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $errorCode,
        public readonly string $errorMessage,
        public readonly int $attemptNumber,
        public readonly string $failedAt,
    ) {
    }
}
