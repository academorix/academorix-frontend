<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Stackra\Payment\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PaymentDisputeObserver detected status transitioned to a terminal state (won / lost / charge_refunded / warning_closed). closed_at populated.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - monitoring::MetricsCollector
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PaymentDisputeClosed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $paymentDisputeId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $outcome  'won' | 'lost' | 'accepted' | 'warning_closed_without_dispute'
     * @param  string  $closedAt  ISO 8601
     */
    public function __construct(
        public readonly string $paymentDisputeId,
        public readonly string $tenantId,
        public readonly string $outcome,
        public readonly string $closedAt,
    ) {
    }
}
