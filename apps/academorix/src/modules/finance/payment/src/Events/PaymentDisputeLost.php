<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Stackra\Payment\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Dispute outcome = 'lost'. Merchant lost; funds permanently reversed. Triggers EscalateDisputeToChargebackJob → creates Chargeback row via chargeback module.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial audit)
 * - notifications::DispatchPaymentDisputeLostNotification (admin — negative news + follow-up)
 * - payment::EnqueueEscalateDisputeToChargebackJob
 * - monitoring::MetricsCollector
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PaymentDisputeLost implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $paymentDisputeId  ULID
     * @param  string  $paymentId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $amountCents  integer
     * @param  string  $outcomeReason  string | null
     * @param  string  $lostAt  ISO 8601
     */
    public function __construct(
        public readonly string $paymentDisputeId,
        public readonly string $paymentId,
        public readonly string $tenantId,
        public readonly int $amountCents,
        public readonly string $outcomeReason,
        public readonly string $lostAt,
    ) {
    }
}
