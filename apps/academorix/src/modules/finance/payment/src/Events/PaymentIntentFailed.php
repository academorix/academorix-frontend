<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Status transitioned to 'failed'. Provider rejected the confirmation. Terminal. last_error_code + last_error_type populated.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchPaymentFailedNotification (customer — reason + try-again CTA)
 * - monitoring::MetricsCollector
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PaymentIntentFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $paymentIntentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceId  ULID
     * @param  string  $lastErrorCode  string | null
     * @param  string  $lastErrorMessage  string | null
     * @param  string  $lastErrorType  string | null
     * @param  string  $failedAt  ISO 8601
     */
    public function __construct(
        public readonly string $paymentIntentId,
        public readonly string $tenantId,
        public readonly string $invoiceId,
        public readonly string $lastErrorCode,
        public readonly string $lastErrorMessage,
        public readonly string $lastErrorType,
        public readonly string $failedAt,
    ) {
    }
}
