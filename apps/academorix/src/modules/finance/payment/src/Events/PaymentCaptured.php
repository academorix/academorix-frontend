<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Stackra\Payment\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PRIMARY SUCCESS SIGNAL. PaymentObserver.created after the payments row commits. Fires the three atomic side effects: TransactionRecorder.record + Invoice.mark_paid + growth::marketing::CaptureMembershipPurchasedMarketingEvent (when invoice.membership_id is set).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record; 10y Enterprise)
 * - transaction::RecordPaymentTransaction (double-entry: debit cash, credit accounts_receivable — same DB txn)
 * - invoice::MarkInvoicePaidOnPaymentCaptured (state update: paid or partially_paid — same DB txn)
 * - growth::marketing::CaptureMembershipPurchasedMarketingEvent (attribution — via #[AsDomainEventMapper] when invoice.membership_id is set)
 * - notifications::DispatchPaymentSucceededNotification (customer + admin — receipt)
 * - monitoring::MetricsCollector (increments stackra.payment.captured_total)
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PaymentCaptured implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $paymentId  ULID (pay_*)
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceId  ULID
     * @param  string  $paymentIntentId  ULID
     * @param  string  $paymentMethodId  ULID | null
     * @param  string  $provider  string
     * @param  int  $amountCents  integer
     * @param  string  $currency  string
     * @param  int  $feeCents  integer
     * @param  int  $netCents  integer
     * @param  string  $paymentMethodType  string
     * @param  string  $capturedAt  ISO 8601
     */
    public function __construct(
        public readonly string $paymentId,
        public readonly string $tenantId,
        public readonly string $invoiceId,
        public readonly string $paymentIntentId,
        public readonly string $paymentMethodId,
        public readonly string $provider,
        public readonly int $amountCents,
        public readonly string $currency,
        public readonly int $feeCents,
        public readonly int $netCents,
        public readonly string $paymentMethodType,
        public readonly string $capturedAt,
    ) {
    }
}
