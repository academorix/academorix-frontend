<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Stackra\Payment\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Status transitioned to 'cancelled'. Customer or system cancelled. cancellation_reason populated.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PaymentIntentCancelled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $paymentIntentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $cancellationReason  string
     * @param  string  $cancelledAt  ISO 8601
     */
    public function __construct(
        public readonly string $paymentIntentId,
        public readonly string $tenantId,
        public readonly string $cancellationReason,
        public readonly string $cancelledAt,
    ) {
    }
}
