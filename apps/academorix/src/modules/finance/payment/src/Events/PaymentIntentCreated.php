<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Stackra\Payment\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * PaymentIntentObserver.created after the payment_intents row commits. status='initialized'. Fires once per intent.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial audit)
 * - monitoring::MetricsCollector (increments stackra.payment.intents.created_total)
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PaymentIntentCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $paymentIntentId  ULID (pit_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $invoiceId  ULID (ivc_*)
     * @param  string  $provider  string
     * @param  int  $amountCents  integer
     * @param  string  $currency  string (ISO 4217)
     * @param  string  $captureMethod  'automatic' | 'manual'
     * @param  string  $confirmationMethod  'automatic' | 'manual'
     * @param  string  $createdAt  ISO 8601
     */
    public function __construct(
        public readonly string $paymentIntentId,
        public readonly string $tenantId,
        public readonly string $invoiceId,
        public readonly string $provider,
        public readonly int $amountCents,
        public readonly string $currency,
        public readonly string $captureMethod,
        public readonly string $confirmationMethod,
        public readonly string $createdAt,
    ) {
    }
}
