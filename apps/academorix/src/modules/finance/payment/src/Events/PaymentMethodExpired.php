<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Stackra\Payment\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SyncPaymentMethodExpiryJob detected the card's exp_month + exp_year has passed. is_active flipped false with deactivated_reason='card_expired'.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchPaymentMethodExpiringNotification (customer — 30/7 days out) + expiration notification
 * - monitoring::MetricsCollector
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PaymentMethodExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $paymentMethodId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $customerType  string
     * @param  string  $customerId  ULID
     * @param  string  $expiredAt  ISO 8601
     */
    public function __construct(
        public readonly string $paymentMethodId,
        public readonly string $tenantId,
        public readonly string $customerType,
        public readonly string $customerId,
        public readonly string $expiredAt,
    ) {
    }
}
