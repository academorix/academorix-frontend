<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Stackra\Payment\Actions\Tenant;

use Stackra\Payment\Contracts\Repositories\PaymentMethodRepositoryInterface;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Delete;
use Illuminate\Http\Response;

/**
 * `DELETE /api/v1/payment-methods/{method}` — delete action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Delete(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsAction(name: 'payment.payment_methods.delete')]
#[Delete('/api/v1/payment-methods/{method}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class DeletePaymentMethodAction
{
    use AsController;

    public function __construct(
        private readonly PaymentMethodRepositoryInterface $repository,
    ) {
    }

    /**
     * Soft-delete one `payment-method` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): Response
    {
        $this->repository->delete($id);

        return response()->noContent();
    }
}
