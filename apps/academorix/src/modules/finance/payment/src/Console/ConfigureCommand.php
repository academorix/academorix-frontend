<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan payment-provider:configure` — Configure — payment provider command.
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'payment-provider:configure',
    description: 'Configure — payment provider command.',
)]
final class ConfigureCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Configure — payment provider command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Configure — payment provider command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
