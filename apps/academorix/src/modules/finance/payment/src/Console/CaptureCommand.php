<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Academorix\Payment\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan payment:capture` — Capture — payment command.
 *
 * @category Payment
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'payment:capture',
    description: 'Capture — payment command.',
)]
final class CaptureCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Capture — payment command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Capture — payment command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
