<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Stackra\Payment\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `provider` on `payment-method`.
 *
 * Enum: stripe / paddle / adyen / square / paypal / custom_webhook.
 *
 * @category Payment
 *
 * @since    0.1.0
 */
enum PaymentMethodProvider: string
{
    use Enum;

    case Stripe = 'stripe';
    case Paddle = 'paddle';
    case Adyen = 'adyen';
    case Square = 'square';
    case Paypal = 'paypal';
    case CustomWebhook = 'custom_webhook';
}
