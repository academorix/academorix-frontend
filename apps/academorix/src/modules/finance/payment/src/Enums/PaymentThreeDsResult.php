<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Stackra\Payment\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `three_ds_result` on `payment`.
 *
 * Enum: not_attempted / authenticated / rejected / not_supported / bypassed.
 *
 * @category Payment
 *
 * @since    0.1.0
 */
enum PaymentThreeDsResult: string
{
    use Enum;

    case NotAttempted = 'not_attempted';
    case Authenticated = 'authenticated';
    case Rejected = 'rejected';
    case NotSupported = 'not_supported';
    case Bypassed = 'bypassed';
}
