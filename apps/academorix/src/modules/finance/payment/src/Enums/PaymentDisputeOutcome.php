<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Stackra\Payment\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `outcome` on `payment-dispute`.
 *
 * Enum: won / lost / accepted / warning_closed_without_dispute.
 *
 * @category Payment
 *
 * @since    0.1.0
 */
enum PaymentDisputeOutcome: string
{
    use Enum;

    case Won = 'won';
    case Lost = 'lost';
    case Accepted = 'accepted';
    case WarningClosedWithoutDispute = 'warning_closed_without_dispute';
}
