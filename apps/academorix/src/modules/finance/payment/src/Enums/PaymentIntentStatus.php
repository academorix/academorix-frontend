<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Stackra\Payment\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `status` on `payment-intent`.
 *
 * Enum: initialized / requires_action / requires_payment_method / requires_confirmation / requires_capture / processing / succeeded / cancelled / failed.
 *
 * @category Payment
 *
 * @since    0.1.0
 */
enum PaymentIntentStatus: string
{
    use Enum;

    case Initialized = 'initialized';
    case RequiresAction = 'requires_action';
    case RequiresPaymentMethod = 'requires_payment_method';
    case RequiresConfirmation = 'requires_confirmation';
    case RequiresCapture = 'requires_capture';
    case Processing = 'processing';
    case Succeeded = 'succeeded';
    case Cancelled = 'cancelled';
    case Failed = 'failed';
}
