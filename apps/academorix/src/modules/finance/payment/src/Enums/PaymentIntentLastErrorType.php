<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payment --force`.

declare(strict_types=1);

namespace Stackra\Payment\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `last_error_type` on `payment-intent`.
 *
 * Enum: authentication_error / card_error / api_error / idempotency_error / rate_limit_error / validation_error.
 *
 * @category Payment
 *
 * @since    0.1.0
 */
enum PaymentIntentLastErrorType: string
{
    use Enum;

    case AuthenticationError = 'authentication_error';
    case CardError = 'card_error';
    case ApiError = 'api_error';
    case IdempotencyError = 'idempotency_error';
    case RateLimitError = 'rate_limit_error';
    case ValidationError = 'validation_error';
}
