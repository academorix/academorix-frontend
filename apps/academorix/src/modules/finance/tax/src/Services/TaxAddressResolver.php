<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Stackra\Tax\Services;

use Stackra\Tax\Contracts\Services\TaxAddressResolverInterface;
use Illuminate\Container\Attributes\Scoped;
use Stackra\Tax\Contracts\Repositories\TaxCalculationRepositoryInterface;

/**
 * Concrete service — TaxAddressResolver.
 *
 * Implements {@see TaxAddressResolverInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[Scoped]
final class TaxAddressResolver implements TaxAddressResolverInterface
{
    /**
     * @param  TaxCalculationRepositoryInterface  $taxCalculationRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly TaxCalculationRepositoryInterface $taxCalculationRepository,
    ) {
    }
}
