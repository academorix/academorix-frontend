<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `provider` on `tax-calculation`.
 *
 * Enum: taxjar / avalara / stripe_tax / native_calculator / custom_webhook.
 *
 * @category Tax
 *
 * @since    0.1.0
 */
enum TaxCalculationProvider: string
{
    use Enum;

    case Taxjar = 'taxjar';
    case Avalara = 'avalara';
    case StripeTax = 'stripe_tax';
    case NativeCalculator = 'native_calculator';
    case CustomWebhook = 'custom_webhook';
}
