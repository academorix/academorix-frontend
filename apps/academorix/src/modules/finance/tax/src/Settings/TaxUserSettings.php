<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * TaxUserSettings — My tax preferences.
 *
 * Per-user preferences for tax reports + alerts.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'tax_user',
    label: 'My tax preferences',
    description: 'Per-user preferences for tax reports + alerts.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 42,
)]
class TaxUserSettings extends Settings
{

    // ── Annual report subscription ──────────────────────────────────
    #[SettingGroup(label: 'Annual report subscription', icon: 'document-chart-bar', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Send me the annual tax report', group: 'Annual report subscription', sortOrder: 1)]
    public bool $receive_annual_report = true;

    #[SettingField(controlType: ControlType::Select, label: 'Preferred format', group: 'Annual report subscription', sortOrder: 2, options: ['pdf' => 'PDF', 'csv' => 'CSV', 'json' => 'JSON'])]
    public string $annual_report_format = 'pdf';


    // ── Alert subscriptions ──────────────────────────────────
    #[SettingGroup(label: 'Alert subscriptions', icon: 'bell', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me on tax provider circuit-breaker events', group: 'Alert subscriptions', sortOrder: 1, helpText: 'This preference only takes effect if the tenant admin listed you in circuit_breaker_notify_users. Owners always receive.')]
    public bool $receive_circuit_breaker_alerts = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me on calculation drift detection', group: 'Alert subscriptions', sortOrder: 2)]
    public bool $receive_drift_alerts = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'tax_user';
    }
}
