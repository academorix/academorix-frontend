<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * TaxSettings — Tax computation.
 *
 * Tenant-wide defaults for tax rate resolution + address preference + notification routing.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'tax',
    label: 'Tax computation',
    description: 'Tenant-wide defaults for tax rate resolution + address preference + notification routing.',
    icon: 'calculator',
    scope: 'tenant',
    permission: 'tax.providers.viewAny',
    sortOrder: 42,
)]
class TaxSettings extends Settings
{

    // ── Address preference ──────────────────────────────────
    #[SettingGroup(label: 'Address preference', description: 'Which customer address to use for jurisdiction resolution.', icon: 'map-pin', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Prefer shipping address over billing', group: 'Address preference', sortOrder: 1, helpText: 'For tenants selling physical goods, use the shipping address for jurisdiction resolution. For digital-only tenants, use billing.')]
    public bool $prefer_shipping_over_billing = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Fall back to tenant region when address missing', group: 'Address preference', sortOrder: 2, helpText: 'When the customer has no address on file, use the tenant\'s default_region for jurisdiction resolution. Disable to REFUSE calculations for address-less customers (fail-safe).')]
    public bool $fall_back_to_tenant_region = true;


    // ── Rate resolution defaults ──────────────────────────────────
    #[SettingGroup(label: 'Rate resolution defaults', description: 'Behaviour when no rate matches (rare — indicates seed data gap).', icon: 'adjustments-horizontal', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Block calculation when no rate matches', group: 'Rate resolution defaults', sortOrder: 1, helpText: 'Recommended. Default 0% tax when no rate matches is a compliance risk — the tenant should explicitly opt in by disabling this.')]
    public bool $block_when_no_rate = true;


    // ── Notifications routing ──────────────────────────────────
    #[SettingGroup(label: 'Notifications routing', description: 'Where tax pipeline alerts are delivered.', icon: 'bell-alert', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Text, label: 'Notify these users on circuit-breaker opens', group: 'Notifications routing', sortOrder: 1, helpText: 'Every user listed receives TaxProviderCircuitBreakerOpenedNotification. Tenant owners are always notified regardless.')]
    public array $circuit_breaker_notify_users = [];

    #[SettingField(controlType: ControlType::Text, label: 'Notify these users on calculation drift detection', group: 'Notifications routing', sortOrder: 2)]
    public array $drift_detection_notify_users = [];

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify tenant admins on exemption expiry', group: 'Notifications routing', sortOrder: 3, helpText: 'In addition to notifying the customer (always).')]
    public bool $exemption_expiry_notify_admins = true;


    // ── Retention ──────────────────────────────────
    #[SettingGroup(label: 'Retention', description: 'How long tax records are retained.', icon: 'archive-box', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Toggle, label: '10-year calculation retention', group: 'Retention', sortOrder: 1, helpText: 'Enterprise-only. Extends tax_calculations retention from 7 years to 10 years for stricter regulator regimes.')]
    public bool $extended_retention_enabled = false;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'tax';
    }
}
