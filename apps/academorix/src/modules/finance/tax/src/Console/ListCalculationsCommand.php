<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Stackra\Tax\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan tax:list-calculations` — List calculations — tax command.
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'tax:list-calculations',
    description: 'List calculations — tax command.',
)]
final class ListCalculationsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List calculations — tax command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List calculations — tax command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
