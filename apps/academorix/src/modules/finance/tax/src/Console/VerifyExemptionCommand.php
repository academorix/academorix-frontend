<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Stackra\Tax\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan tax:verify-exemption` — Verify exemption — tax command.
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'tax:verify-exemption',
    description: 'Verify exemption — tax command.',
)]
final class VerifyExemptionCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Verify exemption — tax command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Verify exemption — tax command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
