<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Stackra\Tax\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan tax:describe-calculation` — Describe calculation — tax command.
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'tax:describe-calculation',
    description: 'Describe calculation — tax command.',
)]
final class DescribeCalculationCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Describe calculation — tax command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Describe calculation — tax command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
