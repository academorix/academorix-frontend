<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan tax:configure-provider` — Configure provider — tax command.
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'tax:configure-provider',
    description: 'Configure provider — tax command.',
)]
final class ConfigureProviderCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Configure provider — tax command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Configure provider — tax command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
