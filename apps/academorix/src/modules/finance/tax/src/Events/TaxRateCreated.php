<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TaxRateObserver.creating committed a new tax_rates row. Fires exactly once per (tenant_id, tax_jurisdiction_id, rate_type, effective_from) tuple.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - tax::InvalidateRateCache
 * - monitoring::MetricsCollector
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TaxRateCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $taxRateId  ULID (txr_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $taxJurisdictionId  ULID (txj_*)
     * @param  string  $rateType  string (9 enum values)
     * @param  float  $percentage  decimal 5,4
     * @param  string  $appliesTo  string (5 enum values)
     * @param  string  $effectiveFrom  date
     * @param  string  $effectiveTo  date | null
     * @param  string  $source  string (manual / provider_synced / import / migration)
     */
    public function __construct(
        public readonly string $taxRateId,
        public readonly string $tenantId,
        public readonly string $taxJurisdictionId,
        public readonly string $rateType,
        public readonly float $percentage,
        public readonly string $appliesTo,
        public readonly string $effectiveFrom,
        public readonly string $effectiveTo,
        public readonly string $source,
    ) {
    }
}
