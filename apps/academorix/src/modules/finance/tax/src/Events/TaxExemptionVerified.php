<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * VerifyTaxExemptionJob succeeded OR admin manually approved via /verify. verification_status transitions pending → verified. Sets verified_at + verified_by_user_id.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchTaxExemptionVerifiedNotification (informs the customer/tenant admin)
 * - tax::InvalidateExemptionCache
 * - monitoring::MetricsCollector
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TaxExemptionVerified implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $taxExemptionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $verifiedByUserId  ULID | null (null = automatic registry cross-check)
     * @param  string  $verificationMethod  string (manual / automatic_registry_check)
     * @param  string  $verifiedAt  ISO 8601
     */
    public function __construct(
        public readonly string $taxExemptionId,
        public readonly string $tenantId,
        public readonly string $verifiedByUserId,
        public readonly string $verificationMethod,
        public readonly string $verifiedAt,
    ) {
    }
}
