<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Stackra\Tax\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * VerifyTaxExemptionJob failed cross-check OR admin manually rejected via /reject. verification_status transitions pending → rejected. Requires --reason.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchTaxExemptionRejectedNotification (informs the customer/tenant admin with reason)
 * - monitoring::MetricsCollector
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TaxExemptionRejected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $taxExemptionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $rejectedByUserId  ULID | null
     * @param  string  $rejectionReason  string (invalid_certificate / expired_at_creation / jurisdiction_mismatch / fraudulent / other)
     * @param  string  $rejectionNote  string | null
     * @param  string  $rejectedAt  ISO 8601
     */
    public function __construct(
        public readonly string $taxExemptionId,
        public readonly string $tenantId,
        public readonly string $rejectedByUserId,
        public readonly string $rejectionReason,
        public readonly string $rejectionNote,
        public readonly string $rejectedAt,
    ) {
    }
}
