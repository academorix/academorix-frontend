<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Stackra\Tax\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TaxJurisdictionObserver.creating committed a new jurisdiction.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - tax::InvalidateJurisdictionCache
 * - monitoring::MetricsCollector
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TaxJurisdictionCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $taxJurisdictionId  ULID (txj_*)
     * @param  string  $tenantId  ULID | null (null = platform reference)
     * @param  string  $countryCode  string (ISO 3166-1)
     * @param  string  $stateCode  string | null (ISO 3166-2)
     * @param  string  $city  string | null
     * @param  string  $postalCodePattern  string | null
     * @param  string  $taxType  string (5 enum values)
     * @param  string  $nexusType  string | null
     */
    public function __construct(
        public readonly string $taxJurisdictionId,
        public readonly string $tenantId,
        public readonly string $countryCode,
        public readonly string $stateCode,
        public readonly string $city,
        public readonly string $postalCodePattern,
        public readonly string $taxType,
        public readonly string $nexusType,
    ) {
    }
}
