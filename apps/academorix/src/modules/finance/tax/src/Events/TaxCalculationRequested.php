<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance tax --force`.

declare(strict_types=1);

namespace Academorix\Tax\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * TaxCalculator::calculate() invoked. Fires BEFORE the provider call. Distinct from TaxCalculationCompleted (per-provider-per-attempt success). Reserved for provider-latency observability + drift detection instrumentation.
 *
 * ## Consumers
 *
 * - monitoring::MetricsCollector (starts calculation-duration histogram)
 *
 * @category Tax
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TaxCalculationRequested implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $correlationId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceLineId  ULID | null (null when calculation is a real-time preview via POST /calculate)
     * @param  int  $sourceAmountCents  integer
     * @param  string  $sourceCurrency  string (ISO 4217)
     * @param  string  $provider  string
     * @param  string  $requestedAt  ISO 8601
     */
    public function __construct(
        public readonly string $correlationId,
        public readonly string $tenantId,
        public readonly string $invoiceLineId,
        public readonly int $sourceAmountCents,
        public readonly string $sourceCurrency,
        public readonly string $provider,
        public readonly string $requestedAt,
    ) {
    }
}
