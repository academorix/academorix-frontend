<?php

declare(strict_types=1);

use Stackra\Tax\Models\TaxCalculation;

it('is a placeholder — replace with actual TaxCalculation unit tests', function (): void {
    expect(TaxCalculation::class)->toBeString();
});
