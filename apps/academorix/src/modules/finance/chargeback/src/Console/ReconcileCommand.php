<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance chargeback --force`.

declare(strict_types=1);

namespace Academorix\Chargeback\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan chargeback:reconcile` — Reconcile the chargeback data.
 *
 * @category Chargeback
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'chargeback:reconcile',
    description: 'Reconcile the chargeback data.',
)]
final class ReconcileCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Reconcile the chargeback data', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Reconcile the chargeback data completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
