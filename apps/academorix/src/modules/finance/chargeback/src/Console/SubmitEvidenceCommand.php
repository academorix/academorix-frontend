<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance chargeback --force`.

declare(strict_types=1);

namespace Stackra\Chargeback\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan chargeback:submit-evidence` — Submit evidence — chargeback command.
 *
 * @category Chargeback
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'chargeback:submit-evidence',
    description: 'Submit evidence — chargeback command.',
)]
final class SubmitEvidenceCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Submit evidence — chargeback command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Submit evidence — chargeback command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
