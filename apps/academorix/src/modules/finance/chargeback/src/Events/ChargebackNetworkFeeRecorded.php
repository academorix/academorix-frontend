<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance chargeback --force`.

declare(strict_types=1);

namespace Stackra\Chargeback\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * FinalizeChargebackJob step 2 completed — the network fee ($15-25 typical) recorded as a secondary offsetting Transaction. Fires alongside ChargebackTransactionRecorded (step 1) but for the network fee entry specifically.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - activity::WriteToActivityLog
 * - observability::MetricsCollector (stackra.chargeback.network_fees_total_cents counter — sum of fee_cents)
 *
 * @category Chargeback
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ChargebackNetworkFeeRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $chargebackId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $feeTransactionId  ULID (trx_*)
     * @param  string  $network  string
     * @param  int  $feeCents  integer
     * @param  string  $currency  string
     * @param  string  $recordedAt  ISO 8601
     */
    public function __construct(
        public readonly string $chargebackId,
        public readonly string $tenantId,
        public readonly string $feeTransactionId,
        public readonly string $network,
        public readonly int $feeCents,
        public readonly string $currency,
        public readonly string $recordedAt,
    ) {
    }
}
