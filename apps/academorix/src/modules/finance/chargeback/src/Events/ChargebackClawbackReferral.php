<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance chargeback --force`.

declare(strict_types=1);

namespace Academorix\Chargeback\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ChargebackLost fired + ClawbackCoordinator detected that the disputed invoice was linked to a materialized ReferralReward. Cascades to growth::referrals for reward reversal.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - referrals::ProcessReferralClawbackOnChargeback (Wave 5 — reverses the reward + notifies the referrer with a 'chargeback loss' reason)
 *
 * @category Chargeback
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ChargebackClawbackReferral implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $chargebackId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceId  ULID
     * @param  string  $referralRewardId  ULID (rrw_*)
     * @param  int  $rewardType  'percent_discount' | 'fixed_amount' | 'credit' | 'points'
     * @param  int  $rewardAmountCents  integer
     * @param  string  $requestedAt  ISO 8601
     */
    public function __construct(
        public readonly string $chargebackId,
        public readonly string $tenantId,
        public readonly string $invoiceId,
        public readonly string $referralRewardId,
        public readonly int $rewardType,
        public readonly int $rewardAmountCents,
        public readonly string $requestedAt,
    ) {
    }
}
