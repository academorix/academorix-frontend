<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance chargeback --force`.

declare(strict_types=1);

namespace Stackra\Chargeback\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * ChargebackSettings — Chargebacks.
 *
 * Tenant-wide chargeback workflow configuration.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Chargeback
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'chargeback',
    label: 'Chargebacks',
    description: 'Tenant-wide chargeback workflow configuration.',
    icon: 'shield-exclamation',
    scope: 'tenant',
    permission: 'chargebacks.viewAny',
    sortOrder: 59,
)]
class ChargebackSettings extends Settings
{

    // ── Rate monitoring ──────────────────────────────────
    #[SettingGroup(label: 'Rate monitoring', description: 'Thresholds for VDMP/MDMP compliance monitoring.', icon: 'chart-bar', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enable rate monitoring', group: 'Rate monitoring', sortOrder: 1, helpText: 'Monitor rolling 60-day chargeback rate per network. Alerts when rate crosses VDMP/MDMP threshold. Recommended ON — merchant-account termination risk if unmonitored.')]
    public bool $rate_monitoring_enabled = true;

    #[SettingField(controlType: ControlType::Number, label: 'Visa VDMP threshold override (percent)', group: 'Rate monitoring', sortOrder: 2, min: 0.1, max: 5.0, helpText: 'Override the default 0.9% Visa VDMP threshold. Enterprise tenants may enforce stricter internal thresholds (0.5% early-warning). Cannot exceed 5.0% (would be effectively disabled).')]
    public float $custom_threshold_visa_percent = 0.9;

    #[SettingField(controlType: ControlType::Number, label: 'Mastercard MDMP threshold override (percent)', group: 'Rate monitoring', sortOrder: 3, min: 0.1, max: 5.0)]
    public float $custom_threshold_mastercard_percent = 1.5;


    // ── Rapid Dispute Resolution (RDR) ──────────────────────────────────
    #[SettingGroup(label: 'Rapid Dispute Resolution (RDR)', description: 'Visa RDR auto-refund configuration.', icon: 'arrow-uturn-left', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enable RDR', group: 'Rapid Dispute Resolution (RDR)', sortOrder: 1, helpText: 'Auto-refund below threshold to close pre-chargeback state. No network fee. No rate impact. Visa-only.')]
    public bool $rdr_enabled = true;

    #[SettingField(controlType: ControlType::Number, label: 'RDR auto-refund threshold (cents)', group: 'Rapid Dispute Resolution (RDR)', sortOrder: 2, min: 100, max: 100000, helpText: 'Chargebacks below this amount become RDR-eligible for auto-refund. Default 5000 = $50. Small tenants may raise to $100; enterprise may lower to $25.')]
    public int $rdr_auto_refund_threshold_cents = 5000;

    #[SettingField(controlType: ControlType::Number, label: 'Min customer purchase history days', group: 'Rapid Dispute Resolution (RDR)', sortOrder: 3, min: 0, max: 365, helpText: 'New customers below this history threshold are NOT offered RDR (higher fraud risk).')]
    public int $rdr_min_customer_purchase_history_days = 30;


    // ── Evidence collection ──────────────────────────────────
    #[SettingGroup(label: 'Evidence collection', description: 'Auto-collection + deadline reminder defaults.', icon: 'document-check', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Auto-collect evidence on chargeback filed', group: 'Evidence collection', sortOrder: 1, helpText: 'Pre-populates the evidence bundle from invoice + membership + notifications + support modules. Admin reviews + potentially edits before submission.')]
    public bool $evidence_auto_collect_on_create = true;

    #[SettingField(controlType: ControlType::Text, label: 'Deadline reminder cadence (days before)', group: 'Evidence collection', sortOrder: 2, options: ['14' => '14 days before', '7' => '7 days before', '3' => '3 days before', '1' => '1 day before'], helpText: 'Days-before-deadline at which ChargebackEvidenceOverdue notifications fire.')]
    public array $evidence_deadline_reminder_cadence_days = [7, 3, 1];


    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', icon: 'bell', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Text, label: 'Admin notification channels on chargeback filed', group: 'Notifications', sortOrder: 1, options: ['mail' => 'Email', 'in_app' => 'In-app', 'push' => 'Push notification', 'sms' => 'SMS'], helpText: 'P0 event — SMS included by default for immediate awareness. Tenant may drop SMS via override.')]
    public array $notify_admins_on_filed_channels = ['mail', 'in_app', 'push', 'sms'];

    #[SettingField(controlType: ControlType::Text, label: 'Additional admin notification recipients', group: 'Notifications', sortOrder: 2, helpText: 'Users to always notify on chargeback events. Overrides the default (any user holding chargebacks.viewAny permission).')]
    public ?string $notification_recipients = null;


    // ── Retention ──────────────────────────────────
    #[SettingGroup(label: 'Retention', icon: 'archive-box', sortOrder: 5)]
    #[SettingField(controlType: ControlType::Select, label: 'Retention window', group: 'Retention', sortOrder: 1, options: ['7' => '7 years (default)', '10' => '10 years (Enterprise)'], helpText: '10 years requires the chargeback_extended_retention entitlement.')]
    public int $retention_years = 7;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'chargeback';
    }
}
