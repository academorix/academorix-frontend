<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance dunning --force`.

declare(strict_types=1);

namespace Stackra\Dunning\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `status` on `dunning-run`.
 *
 * Enum: active / paused / resolved / exhausted / cancelled.
 *
 * @category Dunning
 *
 * @since    0.1.0
 */
enum DunningRunStatus: string
{
    use Enum;

    case Active = 'active';
    case Paused = 'paused';
    case Resolved = 'resolved';
    case Exhausted = 'exhausted';
    case Cancelled = 'cancelled';
}
