<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance dunning --force`.

declare(strict_types=1);

namespace Academorix\Dunning\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Dunning module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Dunning
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum DunningPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `dunning-plans.viewAny` — Dunning-plans ViewAny
     */
    #[Label('Dunning-plans ViewAny')]
    #[Description('Dunning-plans ViewAny')]
    case DunningPlansViewAny = 'dunning-plans.viewAny';

    /**
     * `dunning-plans.view` — Dunning-plans View
     */
    #[Label('Dunning-plans View')]
    #[Description('Dunning-plans View')]
    case DunningPlansView = 'dunning-plans.view';

    /**
     * `dunning-plans.create` — Dunning-plans Create
     */
    #[Label('Dunning-plans Create')]
    #[Description('Dunning-plans Create')]
    case DunningPlansCreate = 'dunning-plans.create';

    /**
     * `dunning-plans.update` — Dunning-plans Update
     */
    #[Label('Dunning-plans Update')]
    #[Description('Dunning-plans Update')]
    case DunningPlansUpdate = 'dunning-plans.update';

    /**
     * `dunning-plans.delete` — Refuses when plan is referenced by active runs.
     */
    #[Label('Refuses when plan is referenced by active runs')]
    #[Description('Refuses when plan is referenced by active runs.')]
    case DunningPlansDelete = 'dunning-plans.delete';

    /**
     * `dunning-runs.viewAny` — Guardian narrowed to own subject.
     */
    #[Label('Guardian narrowed to own subject')]
    #[Description('Guardian narrowed to own subject.')]
    case DunningRunsViewAny = 'dunning-runs.viewAny';

    /**
     * `dunning-runs.view` — Dunning-runs View
     */
    #[Label('Dunning-runs View')]
    #[Description('Dunning-runs View')]
    case DunningRunsView = 'dunning-runs.view';

    /**
     * `dunning-runs.pause` — Manually pause an active run (support case).
     */
    #[Label('Manually pause an active run (support case)')]
    #[Description('Manually pause an active run (support case).')]
    case DunningRunsPause = 'dunning-runs.pause';

    /**
     * `dunning-runs.resume` — Dunning-runs Resume
     */
    #[Label('Dunning-runs Resume')]
    #[Description('Dunning-runs Resume')]
    case DunningRunsResume = 'dunning-runs.resume';

    /**
     * `dunning-runs.cancel` — Cancel a run without exhausting (e.
     */
    #[Label('Cancel a run without exhausting (e')]
    #[Description('Cancel a run without exhausting (e.g. refund issued).')]
    case DunningRunsCancel = 'dunning-runs.cancel';

    /**
     * `dunning-runs.retry-now` — Force-fire the next retry ahead of schedule.
     */
    #[Label('Force-fire the next retry ahead of schedule')]
    #[Description('Force-fire the next retry ahead of schedule.')]
    case DunningRunsRetryNow = 'dunning-runs.retry-now';

    /**
     * `dunning-events.viewAny` — Dunning-events ViewAny
     */
    #[Label('Dunning-events ViewAny')]
    #[Description('Dunning-events ViewAny')]
    case DunningEventsViewAny = 'dunning-events.viewAny';

    /**
     * `dunning-events.view` — Dunning-events View
     */
    #[Label('Dunning-events View')]
    #[Description('Dunning-events View')]
    case DunningEventsView = 'dunning-events.view';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::DunningPlansViewAny => Guard::Sanctum,
            self::DunningPlansView => Guard::Sanctum,
            self::DunningPlansCreate => Guard::Sanctum,
            self::DunningPlansUpdate => Guard::Sanctum,
            self::DunningPlansDelete => Guard::Sanctum,
            self::DunningRunsViewAny => Guard::Sanctum,
            self::DunningRunsView => Guard::Sanctum,
            self::DunningRunsPause => Guard::Sanctum,
            self::DunningRunsResume => Guard::Sanctum,
            self::DunningRunsCancel => Guard::Sanctum,
            self::DunningRunsRetryNow => Guard::Sanctum,
            self::DunningEventsViewAny => Guard::Sanctum,
            self::DunningEventsView => Guard::Sanctum,
        };
    }
}
