<?php

declare(strict_types=1);

use Academorix\Gateway\Models\GatewayWebhookEvent;

it('is a placeholder — replace with actual GatewayWebhookEvent unit tests', function (): void {
    expect(GatewayWebhookEvent::class)->toBeString();
});
