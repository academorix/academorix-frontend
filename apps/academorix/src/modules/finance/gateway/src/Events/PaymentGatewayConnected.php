<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance gateway --force`.

declare(strict_types=1);

namespace Stackra\Gateway\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New PaymentGatewayConfig row committed with is_active=true.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchPaymentGatewayConnectedNotification
 *
 * @category Gateway
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PaymentGatewayConnected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $gatewayConfigId  ULID (pgc_*)
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $mode  'direct' | 'connect'
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $gatewayConfigId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $mode,
        public readonly string $at,
    ) {
    }
}
