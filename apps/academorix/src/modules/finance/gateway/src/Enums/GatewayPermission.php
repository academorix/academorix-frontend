<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance gateway --force`.

declare(strict_types=1);

namespace Academorix\Gateway\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Gateway module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Gateway
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum GatewayPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `gateways.viewAny` — Gateways ViewAny
     */
    #[Label('Gateways ViewAny')]
    #[Description('Gateways ViewAny')]
    case GatewaysViewAny = 'gateways.viewAny';

    /**
     * `gateways.view` — Credentials redacted from response.
     */
    #[Label('Credentials redacted from response')]
    #[Description('Credentials redacted from response. Only display_name, provider, mode, is_active surface.')]
    case GatewaysView = 'gateways.view';

    /**
     * `gateways.create` — Configure a new gateway.
     */
    #[Label('Configure a new gateway')]
    #[Description('Configure a new gateway. Credentials are financial-critical.')]
    case GatewaysCreate = 'gateways.create';

    /**
     * `gateways.update` — Gateways Update
     */
    #[Label('Gateways Update')]
    #[Description('Gateways Update')]
    case GatewaysUpdate = 'gateways.update';

    /**
     * `gateways.delete` — Gateways Delete
     */
    #[Label('Gateways Delete')]
    #[Description('Gateways Delete')]
    case GatewaysDelete = 'gateways.delete';

    /**
     * `gateways.test` — Send a synthetic test transaction via the provider's sandbox endpoint.
     */
    #[Label('Send a synthetic test transaction via the provider\'s sandbox endpoint')]
    #[Description('Send a synthetic test transaction via the provider\'s sandbox endpoint.')]
    case GatewaysTest = 'gateways.test';

    /**
     * `gateways.connect-oauth` — Initiate Stripe Connect / OAuth flow to link a provider account.
     */
    #[Label('Initiate Stripe Connect / OAuth flow to link a provider account')]
    #[Description('Initiate Stripe Connect / OAuth flow to link a provider account.')]
    case GatewaysConnectOauth = 'gateways.connect-oauth';

    /**
     * `gateway-webhooks.viewAny` — Gateway-webhooks ViewAny
     */
    #[Label('Gateway-webhooks ViewAny')]
    #[Description('Gateway-webhooks ViewAny')]
    case GatewayWebhooksViewAny = 'gateway-webhooks.viewAny';

    /**
     * `gateway-webhooks.view` — Gateway-webhooks View
     */
    #[Label('Gateway-webhooks View')]
    #[Description('Gateway-webhooks View')]
    case GatewayWebhooksView = 'gateway-webhooks.view';

    /**
     * `gateway-webhooks.replay` — Manually replay a failed webhook.
     */
    #[Label('Manually replay a failed webhook')]
    #[Description('Manually replay a failed webhook.')]
    case GatewayWebhooksReplay = 'gateway-webhooks.replay';

    /**
     * `platform.gateways.viewAny` — Cross-tenant gateway config (credentials REDACTED).
     */
    #[Label('Cross-tenant gateway config (credentials REDACTED)')]
    #[Description('Cross-tenant gateway config (credentials REDACTED).')]
    case PlatformGatewaysViewAny = 'platform.gateways.viewAny';

    /**
     * `platform.gateway-webhooks.viewAny` — Platform Gateway-webhooks ViewAny
     */
    #[Label('Platform Gateway-webhooks ViewAny')]
    #[Description('Platform Gateway-webhooks ViewAny')]
    case PlatformGatewayWebhooksViewAny = 'platform.gateway-webhooks.viewAny';

    /**
     * `platform.gateway-webhooks.replay` — Cross-tenant support-intervention webhook replay.
     */
    #[Label('Cross-tenant support-intervention webhook replay')]
    #[Description('Cross-tenant support-intervention webhook replay. Every fire notifies the tenant admin + writes audit.')]
    case PlatformGatewayWebhooksReplay = 'platform.gateway-webhooks.replay';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::GatewaysViewAny => Guard::Sanctum,
            self::GatewaysView => Guard::Sanctum,
            self::GatewaysCreate => Guard::Sanctum,
            self::GatewaysUpdate => Guard::Sanctum,
            self::GatewaysDelete => Guard::Sanctum,
            self::GatewaysTest => Guard::Sanctum,
            self::GatewaysConnectOauth => Guard::Sanctum,
            self::GatewayWebhooksViewAny => Guard::Sanctum,
            self::GatewayWebhooksView => Guard::Sanctum,
            self::GatewayWebhooksReplay => Guard::Sanctum,
            self::PlatformGatewaysViewAny => Guard::PlatformAdmin,
            self::PlatformGatewayWebhooksViewAny => Guard::PlatformAdmin,
            self::PlatformGatewayWebhooksReplay => Guard::PlatformAdmin,
        };
    }
}
