<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance gateway --force`.

declare(strict_types=1);

namespace Academorix\Gateway\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `provider` on `gateway-webhook-event`.
 *
 * Backed enum — one case per allowed value.
 *
 * @category Gateway
 *
 * @since    0.1.0
 */
enum GatewayWebhookEventProvider: string
{
    use Enum;

    case Stripe = 'stripe';
    case Paddle = 'paddle';
    case CheckoutCom = 'checkout_com';
    case Square = 'square';
    case Razorpay = 'razorpay';
    case Custom = 'custom';
}
