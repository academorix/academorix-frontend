<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance gateway --force`.

declare(strict_types=1);

namespace Academorix\Gateway\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `mode` on `payment-gateway-config`.
 *
 * Enum: direct (tenant's own account) / connect (Stripe Connect marketplace flow).
 *
 * @category Gateway
 *
 * @since    0.1.0
 */
enum PaymentGatewayConfigMode: string
{
    use Enum;

    case Direct = 'direct';
    case Connect = 'connect';
}
