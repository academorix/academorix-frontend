<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance gateway --force`.

declare(strict_types=1);

namespace Academorix\Gateway\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `provider` on `payment-gateway-config`.
 *
 * Enum: stripe / paddle / checkout_com / square / razorpay / custom.
 *
 * @category Gateway
 *
 * @since    0.1.0
 */
enum PaymentGatewayConfigProvider: string
{
    use Enum;

    case Stripe = 'stripe';
    case Paddle = 'paddle';
    case CheckoutCom = 'checkout_com';
    case Square = 'square';
    case Razorpay = 'razorpay';
    case Custom = 'custom';
}
