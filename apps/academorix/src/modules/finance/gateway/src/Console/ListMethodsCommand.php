<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance gateway --force`.

declare(strict_types=1);

namespace Academorix\Gateway\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan gateway:list-methods` — List methods — gateway command.
 *
 * @category Gateway
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'gateway:list-methods',
    description: 'List methods — gateway command.',
)]
final class ListMethodsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List methods — gateway command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List methods — gateway command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
