<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance gateway --force`.

declare(strict_types=1);

namespace Stackra\Gateway\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan gateway:replay-webhook` — Replay webhook — gateway command.
 *
 * @category Gateway
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'gateway:replay-webhook',
    description: 'Replay webhook — gateway command.',
)]
final class ReplayWebhookCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Replay webhook — gateway command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Replay webhook — gateway command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
