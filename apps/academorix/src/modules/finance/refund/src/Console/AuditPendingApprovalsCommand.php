<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Refund\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan refund:audit-pending-approvals` — Audit pending approvals — refund command.
 *
 * @category Refund
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'refund:audit-pending-approvals',
    description: 'Audit pending approvals — refund command.',
)]
final class AuditPendingApprovalsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Audit pending approvals — refund command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Audit pending approvals — refund command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
