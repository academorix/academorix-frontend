<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Refund\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan refund:report` — Report on the refund data.
 *
 * @category Refund
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'refund:report',
    description: 'Report on the refund data.',
)]
final class ReportCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Report on the refund data', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Report on the refund data completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
