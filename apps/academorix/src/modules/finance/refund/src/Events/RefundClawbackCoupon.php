<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Refund\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * RefundSucceeded fired + ClawbackCoordinator detected that the refunded invoice had a redeemed coupon. Cascades to coupon module for redemption reversal.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - coupon::ProcessCouponClawbackOnInvoiceRefunded (reverses redemption + decrements coupon.usage_count + fires CouponClawbackCompleted)
 *
 * @category Refund
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RefundClawbackCoupon implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $refundId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceId  ULID
     * @param  string  $couponRedemptionId  ULID (crd_*)
     * @param  string  $couponId  ULID (cpn_*)
     * @param  string  $requestedAt  ISO 8601
     */
    public function __construct(
        public readonly string $refundId,
        public readonly string $tenantId,
        public readonly string $invoiceId,
        public readonly string $couponRedemptionId,
        public readonly string $couponId,
        public readonly string $requestedAt,
    ) {
    }
}
