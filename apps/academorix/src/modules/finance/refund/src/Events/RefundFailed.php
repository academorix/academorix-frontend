<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Refund\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Refund.status transitioned processing -> failed. Provider returned a non-recoverable error (original charge already refunded, provider account closed, source no longer valid). Terminal. Customer + admin notified. Payment untouched.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - activity::WriteToActivityLog
 * - notifications::DispatchRefundFailedNotification (customer + admin)
 * - observability::MetricsCollector (stackra.refund.failed_total labelled by failure_reason_category)
 *
 * @category Refund
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RefundFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $refundId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $paymentId  ULID
     * @param  int  $amountCents  integer
     * @param  string  $currency  string
     * @param  string  $provider  string
     * @param  string  $failureReason  string (256)
     * @param  string  $providerErrorCode  string | null
     * @param  string  $failedAt  ISO 8601
     */
    public function __construct(
        public readonly string $refundId,
        public readonly string $tenantId,
        public readonly string $paymentId,
        public readonly int $amountCents,
        public readonly string $currency,
        public readonly string $provider,
        public readonly string $failureReason,
        public readonly string $providerErrorCode,
        public readonly string $failedAt,
    ) {
    }
}
