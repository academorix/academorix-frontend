<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Refund\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Refund.status transitioned approved -> processing. InitiateProviderRefundJob dispatched the provider-side createRefund call + received an async 'processing' response.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchRefundProcessedNotification (customer — money on the way, ~5-10 days for card refunds)
 *
 * @category Refund
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RefundProcessing implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $refundId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  'stripe' | 'paddle' | 'adyen' | 'square' | 'paypal' | 'custom_webhook'
     * @param  string  $providerReferenceId  string | null
     * @param  string  $processingAt  ISO 8601
     * @param  string  $estimatedDeliveryAt  ISO 8601 | null (provider-communicated when available)
     */
    public function __construct(
        public readonly string $refundId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $providerReferenceId,
        public readonly string $processingAt,
        public readonly string $estimatedDeliveryAt,
    ) {
    }
}
