<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Refund\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Refund.status transitioned awaiting_approval -> rejected. Approver clicked reject. Terminal state — no provider dispatch.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial-audit)
 * - activity::WriteToActivityLog
 * - notifications::DispatchRefundRejectedNotification (customer — with reason)
 *
 * @category Refund
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RefundRejected implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $refundId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $rejectedByUserId  ULID
     * @param  string  $rejectedAt  ISO 8601
     * @param  string  $rejectionReason  string (256)
     */
    public function __construct(
        public readonly string $refundId,
        public readonly string $tenantId,
        public readonly string $rejectedByUserId,
        public readonly string $rejectedAt,
        public readonly string $rejectionReason,
    ) {
    }
}
