<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Refund\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * InitiateProviderRefundJob successfully called the provider's createRefund method and received a 2xx response. Distinct from RefundProcessing (which fires on the async 'processing' state after the call) — this fires immediately after the HTTP call succeeds.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - observability::MetricsCollector (academorix.refund.provider_dispatch_total labelled by provider)
 *
 * @category Refund
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class RefundNotifiedProvider implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $refundId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $provider  string
     * @param  string  $providerReferenceId  string
     * @param  int  $providerResponseCode  integer
     * @param  string  $notifiedAt  ISO 8601
     */
    public function __construct(
        public readonly string $refundId,
        public readonly string $tenantId,
        public readonly string $provider,
        public readonly string $providerReferenceId,
        public readonly int $providerResponseCode,
        public readonly string $notifiedAt,
    ) {
    }
}
