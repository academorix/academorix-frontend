<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Academorix\Refund\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Refund module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Refund
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum RefundPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `refunds.viewAny` — Refunds ViewAny
     */
    #[Label('Refunds ViewAny')]
    #[Description('Refunds ViewAny')]
    case RefundsViewAny = 'refunds.viewAny';

    /**
     * `refunds.view` — Refunds View
     */
    #[Label('Refunds View')]
    #[Description('Refunds View')]
    case RefundsView = 'refunds.view';

    /**
     * `refunds.create` — Admin-initiated refund path (POST /api/v1/refunds).
     */
    #[Label('Admin-initiated refund path (POST /api/v1/refunds)')]
    #[Description('Admin-initiated refund path (POST /api/v1/refunds). Distinct from refunds.request (customer path).')]
    case RefundsCreate = 'refunds.create';

    /**
     * `refunds.request` — Customer-initiated refund path (POST /api/v1/refunds/mine).
     */
    #[Label('Customer-initiated refund path (POST /api/v1/refunds/mine)')]
    #[Description('Customer-initiated refund path (POST /api/v1/refunds/mine). Requires `refund_customer_self_service` entitlement. Customer-allowed reasons only.')]
    case RefundsRequest = 'refunds.request';

    /**
     * `refunds.approve` — Approve OR reject a refund.
     */
    #[Label('Approve OR reject a refund')]
    #[Description('Approve OR reject a refund. Segregation of duties — REFUSED when caller is initiator (SOX §404). Same permission covers both approve + reject actions.')]
    case RefundsApprove = 'refunds.approve';

    /**
     * `refunds.cancel` — Cancel a pending refund.
     */
    #[Label('Cancel a pending refund')]
    #[Description('Cancel a pending refund. Customer role scoped to own refunds via policy; admin roles can cancel any tenant refund still in awaiting_approval / approved.')]
    case RefundsCancel = 'refunds.cancel';

    /**
     * `refunds.force-fail` — Admin escape hatch — mark a stuck provider_error refund as failed after manual provider-side investigation.
     */
    #[Label('Admin escape hatch — mark a stuck provider_error refund as failed after manual p')]
    #[Description('Admin escape hatch — mark a stuck provider_error refund as failed after manual provider-side investigation. High-privilege — only admin + owner.')]
    case RefundsForceFail = 'refunds.force-fail';

    /**
     * `refunds.reconcile` — Dispatches ReconcileRefundsJob.
     */
    #[Label('Dispatches ReconcileRefundsJob')]
    #[Description('Dispatches ReconcileRefundsJob. Sweeps stuck refunds after provider outage recovery.')]
    case RefundsReconcile = 'refunds.reconcile';

    /**
     * `refunds.view-report` — Read the refund volume report.
     */
    #[Label('Read the refund volume report')]
    #[Description('Read the refund volume report. Marketing role holds for ROAS analysis (refund rate feeds attribution modelling).')]
    case RefundsViewReport = 'refunds.view-report';

    /**
     * `refunds.view-financials` — Read the confidential financial fields: amount_cents, currency, provider_fee_reversal_cents, exchange_rate.
     */
    #[Label('Read the confidential financial fields: amount_cents, currency, provider_fee_rev')]
    #[Description('Read the confidential financial fields: amount_cents, currency, provider_fee_reversal_cents, exchange_rate. Redactor strips these when the caller lacks this permission.')]
    case RefundsViewFinancials = 'refunds.view-financials';

    /**
     * `platform.refunds.viewAny` — Platform Refunds ViewAny
     */
    #[Label('Platform Refunds ViewAny')]
    #[Description('Platform Refunds ViewAny')]
    case PlatformRefundsViewAny = 'platform.refunds.viewAny';

    /**
     * `platform.refunds.view` — Platform Refunds View
     */
    #[Label('Platform Refunds View')]
    #[Description('Platform Refunds View')]
    case PlatformRefundsView = 'platform.refunds.view';

    /**
     * `platform.refunds.pii.view` — Cross-tenant view WITH customer PII (customer_id / customer name / email unmasked).
     */
    #[Label('Cross-tenant view WITH customer PII (customer_id / customer name / email unmaske')]
    #[Description('Cross-tenant view WITH customer PII (customer_id / customer name / email unmasked). Reserved for regulator-facing investigations. Default view hashes PII.')]
    case PlatformRefundsPiiView = 'platform.refunds.pii.view';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::RefundsViewAny => Guard::Sanctum,
            self::RefundsView => Guard::Sanctum,
            self::RefundsCreate => Guard::Sanctum,
            self::RefundsRequest => Guard::Sanctum,
            self::RefundsApprove => Guard::Sanctum,
            self::RefundsCancel => Guard::Sanctum,
            self::RefundsForceFail => Guard::Sanctum,
            self::RefundsReconcile => Guard::Sanctum,
            self::RefundsViewReport => Guard::Sanctum,
            self::RefundsViewFinancials => Guard::Sanctum,
            self::PlatformRefundsViewAny => Guard::PlatformAdmin,
            self::PlatformRefundsView => Guard::PlatformAdmin,
            self::PlatformRefundsPiiView => Guard::PlatformAdmin,
        };
    }
}
