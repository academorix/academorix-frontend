<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance refund --force`.

declare(strict_types=1);

namespace Stackra\Refund\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `provider` on `refund`.
 *
 * Enum: stripe / paddle / adyen / square / paypal / custom_webhook.
 *
 * @category Refund
 *
 * @since    0.1.0
 */
enum RefundProvider: string
{
    use Enum;

    case Stripe = 'stripe';
    case Paddle = 'paddle';
    case Adyen = 'adyen';
    case Square = 'square';
    case Paypal = 'paypal';
    case CustomWebhook = 'custom_webhook';
}
