<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance wallet --force`.

declare(strict_types=1);

namespace Academorix\Wallet\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan wallet:issue-credit` — Issue credit — wallet command.
 *
 * @category Wallet
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'wallet:issue-credit',
    description: 'Issue credit — wallet command.',
)]
final class IssueCreditCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Issue credit — wallet command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Issue credit — wallet command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
