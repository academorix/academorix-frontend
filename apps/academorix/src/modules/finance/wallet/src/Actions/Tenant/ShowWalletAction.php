<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance wallet --force`.

declare(strict_types=1);

namespace Academorix\Wallet\Actions\Tenant;

use Academorix\Wallet\Contracts\Repositories\WalletRepositoryInterface;
use Academorix\Wallet\Data\WalletData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/wallets/{wallet}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Wallet
 *
 * @since    0.1.0
 */
#[AsAction(name: 'wallet.wallets.show')]
#[Get('/api/v1/wallets/{wallet}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowWalletAction
{
    use AsController;

    public function __construct(
        private readonly WalletRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `wallet` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): WalletData
    {
        return WalletData::from($this->repository->findOrFail($id));
    }
}
