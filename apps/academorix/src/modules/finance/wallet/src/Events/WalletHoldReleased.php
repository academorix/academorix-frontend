<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance wallet --force`.

declare(strict_types=1);

namespace Stackra\Wallet\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Hold status -> released or expired. Reserved balance decreases.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 *
 * @category Wallet
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class WalletHoldReleased implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $holdId  ULID
     * @param  string  $walletId  ULID
     * @param  string  $reason  abandoned | cancelled | ttl_expired | manual
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $holdId,
        public readonly string $walletId,
        public readonly string $reason,
        public readonly string $at,
    ) {
    }
}
