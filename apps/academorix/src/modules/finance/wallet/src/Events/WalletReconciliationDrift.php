<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance wallet --force`.

declare(strict_types=1);

namespace Academorix\Wallet\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Reconciliation found a difference between local balance and upstream source. INCIDENT SIGNAL.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchWalletDriftNotificationListener
 * - monitoring::MetricsCollector
 *
 * @category Wallet
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class WalletReconciliationDrift implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $walletId  ULID
     * @param  int  $localBalanceMinor  integer
     * @param  int  $upstreamBalanceMinor  integer
     * @param  int  $driftMinor  integer
     * @param  string  $detectedAt  ISO 8601
     */
    public function __construct(
        public readonly string $walletId,
        public readonly int $localBalanceMinor,
        public readonly int $upstreamBalanceMinor,
        public readonly int $driftMinor,
        public readonly string $detectedAt,
    ) {
    }
}
