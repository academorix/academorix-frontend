<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance wallet --force`.

declare(strict_types=1);

namespace Academorix\Wallet\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * points_redeem transaction committed. Fires from redemption flow.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 *
 * @category Wallet
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PointsRedeemed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $walletId  ULID
     * @param  string  $transactionId  ULID
     * @param  int  $amountMinor  integer
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $walletId,
        public readonly string $transactionId,
        public readonly int $amountMinor,
        public readonly string $at,
    ) {
    }
}
