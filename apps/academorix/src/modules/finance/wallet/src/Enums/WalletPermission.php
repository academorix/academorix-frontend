<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance wallet --force`.

declare(strict_types=1);

namespace Academorix\Wallet\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Wallet module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Wallet
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum WalletPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `wallets.viewAny` — Wallets ViewAny
     */
    #[Label('Wallets ViewAny')]
    #[Description('Wallets ViewAny')]
    case WalletsViewAny = 'wallets.viewAny';

    /**
     * `wallets.view` — Wallets View
     */
    #[Label('Wallets View')]
    #[Description('Wallets View')]
    case WalletsView = 'wallets.view';

    /**
     * `wallets.adjust` — Manual debit/credit against a wallet.
     */
    #[Label('Manual debit/credit against a wallet')]
    #[Description('Manual debit/credit against a wallet. Financial-critical.')]
    case WalletsAdjust = 'wallets.adjust';

    /**
     * `wallets.reconcile` — Trigger reconciliation against upstream source.
     */
    #[Label('Trigger reconciliation against upstream source')]
    #[Description('Trigger reconciliation against upstream source.')]
    case WalletsReconcile = 'wallets.reconcile';

    /**
     * `wallet-transactions.viewAny` — Wallet-transactions ViewAny
     */
    #[Label('Wallet-transactions ViewAny')]
    #[Description('Wallet-transactions ViewAny')]
    case WalletTransactionsViewAny = 'wallet-transactions.viewAny';

    /**
     * `wallet-transactions.view` — Wallet-transactions View
     */
    #[Label('Wallet-transactions View')]
    #[Description('Wallet-transactions View')]
    case WalletTransactionsView = 'wallet-transactions.view';

    /**
     * `wallet-holds.viewAny` — Wallet-holds ViewAny
     */
    #[Label('Wallet-holds ViewAny')]
    #[Description('Wallet-holds ViewAny')]
    case WalletHoldsViewAny = 'wallet-holds.viewAny';

    /**
     * `wallet-holds.view` — Wallet-holds View
     */
    #[Label('Wallet-holds View')]
    #[Description('Wallet-holds View')]
    case WalletHoldsView = 'wallet-holds.view';

    /**
     * `wallet-holds.release` — Manually release a stuck hold.
     */
    #[Label('Manually release a stuck hold')]
    #[Description('Manually release a stuck hold.')]
    case WalletHoldsRelease = 'wallet-holds.release';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::WalletsViewAny => Guard::Sanctum,
            self::WalletsView => Guard::Sanctum,
            self::WalletsAdjust => Guard::Sanctum,
            self::WalletsReconcile => Guard::Sanctum,
            self::WalletTransactionsViewAny => Guard::Sanctum,
            self::WalletTransactionsView => Guard::Sanctum,
            self::WalletHoldsViewAny => Guard::Sanctum,
            self::WalletHoldsView => Guard::Sanctum,
            self::WalletHoldsRelease => Guard::Sanctum,
        };
    }
}
