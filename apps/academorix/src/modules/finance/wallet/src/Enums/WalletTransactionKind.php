<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance wallet --force`.

declare(strict_types=1);

namespace Stackra\Wallet\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `kind` on `wallet-transaction`.
 *
 * Enum: credit / debit / adjustment_credit / adjustment_debit / points_earn / points_redeem / points_expire / points_grant / reconciliation.
 *
 * @category Wallet
 *
 * @since    0.1.0
 */
enum WalletTransactionKind: string
{
    use Enum;

    case Credit = 'credit';
    case Debit = 'debit';
    case AdjustmentCredit = 'adjustment_credit';
    case AdjustmentDebit = 'adjustment_debit';
    case PointsEarn = 'points_earn';
    case PointsRedeem = 'points_redeem';
    case PointsExpire = 'points_expire';
    case PointsGrant = 'points_grant';
    case Reconciliation = 'reconciliation';
}
