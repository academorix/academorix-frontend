<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance transaction --force`.

declare(strict_types=1);

namespace Stackra\Transaction\Services;

use Stackra\Transaction\Contracts\Services\ExchangeRateResolverInterface;
use Illuminate\Container\Attributes\Scoped;
use Stackra\Transaction\Contracts\Repositories\TransactionLedgerEntryRepositoryInterface;

/**
 * Concrete service — ExchangeRateResolver.
 *
 * Implements {@see ExchangeRateResolverInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Transaction
 *
 * @since    0.1.0
 */
#[Scoped]
final class ExchangeRateResolver implements ExchangeRateResolverInterface
{
    /**
     * @param  TransactionLedgerEntryRepositoryInterface  $transactionLedgerEntryRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly TransactionLedgerEntryRepositoryInterface $transactionLedgerEntryRepository,
    ) {
    }
}
