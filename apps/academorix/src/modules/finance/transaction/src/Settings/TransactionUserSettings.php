<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance transaction --force`.

declare(strict_types=1);

namespace Academorix\Transaction\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * TransactionUserSettings — My ledger preferences.
 *
 * Per-user preferences for the ledger surface.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Transaction
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'transaction_user',
    label: 'My ledger preferences',
    description: 'Per-user preferences for the ledger surface.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 45,
)]
class TransactionUserSettings extends Settings
{

    // ── Monthly ledger report subscription ──────────────────────────────────
    #[SettingGroup(label: 'Monthly ledger report subscription', icon: 'chart-bar', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Send me the monthly General Ledger report', group: 'Monthly ledger report subscription', sortOrder: 1)]
    public bool $receive_monthly_ledger_report = true;

    #[SettingField(controlType: ControlType::Text, label: 'Preferred format', group: 'Monthly ledger report subscription', sortOrder: 2, options: ['csv' => 'CSV (Excel-friendly)', 'jsonl' => 'JSONL (programmatic)', 'both' => 'Both'])]
    public string $monthly_report_format = 'csv';


    // ── Alert subscriptions ──────────────────────────────────
    #[SettingGroup(label: 'Alert subscriptions', icon: 'bell', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me on ledger imbalances', group: 'Alert subscriptions', sortOrder: 1, helpText: 'This preference only takes effect if the tenant admin listed you in imbalance_notify_users. Owners + admins always receive.')]
    public bool $receive_imbalance_alerts = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me on account balance thresholds', group: 'Alert subscriptions', sortOrder: 2)]
    public bool $receive_threshold_alerts = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me on ledger amendments', group: 'Alert subscriptions', sortOrder: 3)]
    public bool $receive_amendment_alerts = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'transaction_user';
    }
}
