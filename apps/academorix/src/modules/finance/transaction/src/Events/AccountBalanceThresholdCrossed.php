<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance transaction --force`.

declare(strict_types=1);

namespace Stackra\Transaction\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The AccountBalanceCalculator computed a per-account running balance that crossed a config-defined threshold. Example: accounts_receivable > $50k (collection risk); cash < $1k (liquidity risk); refunds_owed > $10k (needs disbursement). Not a P0 — signals ops to review.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchAccountBalanceAlertNotification (finance role + admin)
 * - monitoring::MetricsCollector
 *
 * @category Transaction
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AccountBalanceThresholdCrossed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $account  string (LedgerAccount value)
     * @param  string  $currency  string
     * @param  int  $currentBalanceCents  integer (signed)
     * @param  string  $thresholdKind  'high_watermark' | 'low_watermark'
     * @param  int  $thresholdCents  integer
     * @param  string  $crossedAt  ISO 8601
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $account,
        public readonly string $currency,
        public readonly int $currentBalanceCents,
        public readonly string $thresholdKind,
        public readonly int $thresholdCents,
        public readonly string $crossedAt,
    ) {
    }
}
