<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance transaction --force`.

declare(strict_types=1);

namespace Academorix\Transaction\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Transaction module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Transaction
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum TransactionPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `transaction.viewAny` — Transaction ViewAny
     */
    #[Label('Transaction ViewAny')]
    #[Description('Transaction ViewAny')]
    case TransactionViewAny = 'transaction.viewAny';

    /**
     * `transaction.view` — Transaction View
     */
    #[Label('Transaction View')]
    #[Description('Transaction View')]
    case TransactionView = 'transaction.view';

    /**
     * `transaction.reverse` — Manual ledger correction.
     */
    #[Label('Manual ledger correction')]
    #[Description('Manual ledger correction. Creates a NEW offsetting transaction with reversal_reason=\'accounting_adjustment\'. Requires a written note + writes to audits with 7-year retention. Regulator-critical — admin ONLY (finance role does NOT hold).')]
    case TransactionReverse = 'transaction.reverse';

    /**
     * `transaction.ledger_entries.viewAny` — Transaction Ledger Entries ViewAny
     */
    #[Label('Transaction Ledger Entries ViewAny')]
    #[Description('Transaction Ledger Entries ViewAny')]
    case TransactionLedgerEntriesViewAny = 'transaction.ledger_entries.viewAny';

    /**
     * `transaction.ledger_entries.view` — Transaction Ledger Entries View
     */
    #[Label('Transaction Ledger Entries View')]
    #[Description('Transaction Ledger Entries View')]
    case TransactionLedgerEntriesView = 'transaction.ledger_entries.view';

    /**
     * `transaction.ledger.account_balance.view` — View running balance for a specific ledger account.
     */
    #[Label('View running balance for a specific ledger account')]
    #[Description('View running balance for a specific ledger account.')]
    case TransactionLedgerAccountBalanceView = 'transaction.ledger.account_balance.view';

    /**
     * `transaction.ledger.report.view` — General Ledger report.
     */
    #[Label('General Ledger report')]
    #[Description('General Ledger report. Medium+ via transaction_advanced_reporting entitlement.')]
    case TransactionLedgerReportView = 'transaction.ledger.report.view';

    /**
     * `transaction.ledger.reconciliation.view` — View the nightly reconciliation-run audit trail + any detected imbalances.
     */
    #[Label('View the nightly reconciliation-run audit trail + any detected imbalances')]
    #[Description('View the nightly reconciliation-run audit trail + any detected imbalances.')]
    case TransactionLedgerReconciliationView = 'transaction.ledger.reconciliation.view';

    /**
     * `platform.transaction.viewAny` — Platform Transaction ViewAny
     */
    #[Label('Platform Transaction ViewAny')]
    #[Description('Platform Transaction ViewAny')]
    case PlatformTransactionViewAny = 'platform.transaction.viewAny';

    /**
     * `platform.transaction.view` — Platform Transaction View
     */
    #[Label('Platform Transaction View')]
    #[Description('Platform Transaction View')]
    case PlatformTransactionView = 'platform.transaction.view';

    /**
     * `platform.transaction.ledger_entries.viewAny` — Cross-tenant ledger-entry read for support-tool investigations + audit walk-throughs.
     */
    #[Label('Cross-tenant ledger-entry read for support-tool investigations + audit walk-thro')]
    #[Description('Cross-tenant ledger-entry read for support-tool investigations + audit walk-throughs.')]
    case PlatformTransactionLedgerEntriesViewAny = 'platform.transaction.ledger_entries.viewAny';

    /**
     * `platform.transaction.ledger.reconcile` — Force a reconciliation run cross-tenant.
     */
    #[Label('Force a reconciliation run cross-tenant')]
    #[Description('Force a reconciliation run cross-tenant. Used during regulator audits + escalated imbalance investigations. Every fire notifies the tenant + writes an audit row with 7-year retention.')]
    case PlatformTransactionLedgerReconcile = 'platform.transaction.ledger.reconcile';

    /**
     * `platform.transaction.ledger.imbalances.view` — Cross-tenant view of any detected ledger imbalances.
     */
    #[Label('Cross-tenant view of any detected ledger imbalances')]
    #[Description('Cross-tenant view of any detected ledger imbalances. Feeds the ops finance-health dashboard.')]
    case PlatformTransactionLedgerImbalancesView = 'platform.transaction.ledger.imbalances.view';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::TransactionViewAny => Guard::Sanctum,
            self::TransactionView => Guard::Sanctum,
            self::TransactionReverse => Guard::Sanctum,
            self::TransactionLedgerEntriesViewAny => Guard::Sanctum,
            self::TransactionLedgerEntriesView => Guard::Sanctum,
            self::TransactionLedgerAccountBalanceView => Guard::Sanctum,
            self::TransactionLedgerReportView => Guard::Sanctum,
            self::TransactionLedgerReconciliationView => Guard::Sanctum,
            self::PlatformTransactionViewAny => Guard::PlatformAdmin,
            self::PlatformTransactionView => Guard::PlatformAdmin,
            self::PlatformTransactionLedgerEntriesViewAny => Guard::PlatformAdmin,
            self::PlatformTransactionLedgerReconcile => Guard::PlatformAdmin,
            self::PlatformTransactionLedgerImbalancesView => Guard::PlatformAdmin,
        };
    }
}
