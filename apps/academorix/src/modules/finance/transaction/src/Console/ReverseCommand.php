<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance transaction --force`.

declare(strict_types=1);

namespace Academorix\Transaction\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan transaction:reverse` — Reverse — transaction command.
 *
 * @category Transaction
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'transaction:reverse',
    description: 'Reverse — transaction command.',
)]
final class ReverseCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Reverse — transaction command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Reverse — transaction command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
