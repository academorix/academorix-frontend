<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance transaction --force`.

declare(strict_types=1);

namespace Stackra\Transaction\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Attributes\WithoutIncrementing;
use Illuminate\Database\Eloquent\Model;
use Stackra\Transaction\Contracts\Data\TransactionInterface;
use Stackra\Transaction\Database\Factories\TransactionFactory;
use Stackra\Foundation\Concerns\Filterable;
use Stackra\Foundation\Concerns\HasMetadata;
use Stackra\Tenancy\Concerns\BelongsToTenant;
use Stackra\Transaction\Enums\TransactionKind;
use Stackra\Transaction\Enums\TransactionReversalReason;
use Stackra\Transaction\Enums\TransactionStatus;
use Stackra\Transaction\Policies\TransactionPolicy;
use Illuminate\Database\Eloquent\Attributes\UsePolicy;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Laravel\Scout\Searchable;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use OwenIt\Auditing\Contracts\Auditable as AuditableContract;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a Transaction.
 *
 * The money-movement ledger root record.
 *
 * @category Transaction
 *
 * @since    0.1.0
 */
#[Table(name: TransactionInterface::TABLE, key: TransactionInterface::PRIMARY_KEY, keyType: TransactionInterface::KEY_TYPE)]
#[Fillable([
    TransactionInterface::ATTR_TENANT_ID,
        TransactionInterface::ATTR_TRANSACTION_NUMBER,
        TransactionInterface::ATTR_KIND,
        TransactionInterface::ATTR_REFERENCE_TYPE,
        TransactionInterface::ATTR_REFERENCE_ID,
        TransactionInterface::ATTR_AMOUNT_CENTS,
        TransactionInterface::ATTR_CURRENCY,
        TransactionInterface::ATTR_BASE_CURRENCY,
        TransactionInterface::ATTR_BASE_AMOUNT_CENTS,
        TransactionInterface::ATTR_EXCHANGE_RATE_TO_BASE,
        TransactionInterface::ATTR_STATUS,
        TransactionInterface::ATTR_EFFECTIVE_DATE,
        TransactionInterface::ATTR_POSTED_AT,
        TransactionInterface::ATTR_SOURCE_TRANSACTION_ID,
        TransactionInterface::ATTR_REVERSAL_REASON,
        TransactionInterface::ATTR_DESCRIPTION,
        TransactionInterface::ATTR_METADATA,
        TransactionInterface::ATTR_CREATED_BY_USER_ID,
])]
#[UseFactory(TransactionFactory::class)]
#[WithoutIncrementing]
#[UsePolicy(TransactionPolicy::class)]
final class Transaction extends Model implements TransactionInterface, AuditableContract
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    // TODO(gen): resolve unknown trait `HasLedgerEntries` — add its import + `use` line.
    use HasMetadata;
    use Userstamps;
    use Auditable;
    use LogsActivity;
    use Filterable;
    use Searchable;

    /**
     * Cast map — from the blueprint's `x-eloquent.casts`.
     *
     * @var array<string, string>
     */
    protected $casts = [
        TransactionInterface::ATTR_KIND => TransactionKind::class,
        TransactionInterface::ATTR_STATUS => TransactionStatus::class,
        TransactionInterface::ATTR_REVERSAL_REASON => TransactionReversalReason::class,
        TransactionInterface::ATTR_AMOUNT_CENTS => 'integer',
        TransactionInterface::ATTR_BASE_AMOUNT_CENTS => 'integer',
        TransactionInterface::ATTR_EXCHANGE_RATE_TO_BASE => 'decimal:10',
        TransactionInterface::ATTR_METADATA => 'array',
        TransactionInterface::ATTR_EFFECTIVE_DATE => 'date',
        TransactionInterface::ATTR_POSTED_AT => 'datetime',
    ];
}
