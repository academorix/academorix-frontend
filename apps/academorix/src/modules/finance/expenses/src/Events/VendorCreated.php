<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Stackra\Expenses\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New Vendor row (money-out counterparty).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - compliance::VerifyVendorTaxId
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class VendorCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $vendorId  ULID (ven_*)
     * @param  string  $tenantId  ULID
     * @param  string  $name  string
     * @param  string  $taxId  string|null
     */
    public function __construct(
        public readonly string $vendorId,
        public readonly string $tenantId,
        public readonly string $name,
        public readonly string $taxId,
    ) {
    }
}
