<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Academorix\Expenses\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * status: submitted → approved. Pending payout.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::NotifySubmitter
 * - reporting::UpdateCashFlowProjection
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ExpenseApproved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $expenseId  ULID
     * @param  string  $approvedByUserId  ULID
     * @param  string  $approvedAt  ISO 8601
     * @param  string  $approvalNotes  string|null
     */
    public function __construct(
        public readonly string $expenseId,
        public readonly string $approvedByUserId,
        public readonly string $approvedAt,
        public readonly string $approvalNotes,
    ) {
    }
}
