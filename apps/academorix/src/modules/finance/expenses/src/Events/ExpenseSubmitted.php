<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Academorix\Expenses\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New Expense row. status: draft → submitted. Awaiting approval.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::NotifyApprovers
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ExpenseSubmitted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $expenseId  ULID (exp_*)
     * @param  string  $tenantId  ULID
     * @param  string  $submitterUserId  ULID
     * @param  int  $amountMinor  bigint
     * @param  string  $currency  string(3)
     * @param  string  $category  string
     * @param  string  $vendorId  ULID|null
     */
    public function __construct(
        public readonly string $expenseId,
        public readonly string $tenantId,
        public readonly string $submitterUserId,
        public readonly int $amountMinor,
        public readonly string $currency,
        public readonly string $category,
        public readonly string $vendorId,
    ) {
    }
}
