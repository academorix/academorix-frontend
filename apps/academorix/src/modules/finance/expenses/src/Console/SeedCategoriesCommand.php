<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Academorix\Expenses\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan expenses:seed-categories` — Seed categories — expenses command.
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'expenses:seed-categories',
    description: 'Seed categories — expenses command.',
)]
final class SeedCategoriesCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Seed categories — expenses command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Seed categories — expenses command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
