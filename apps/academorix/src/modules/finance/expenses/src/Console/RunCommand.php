<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Stackra\Expenses\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan payroll:run` — Run — payroll command.
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'payroll:run',
    description: 'Run — payroll command.',
)]
final class RunCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Run — payroll command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Run — payroll command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
