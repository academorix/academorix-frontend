<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Academorix\Expenses\Actions\Tenant;

use Academorix\Expenses\Contracts\Repositories\PayrollRunRepositoryInterface;
use Academorix\Expenses\Data\PayrollRunData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;
use Illuminate\Http\Request;
use Illuminate\Pagination\LengthAwarePaginator;
use Spatie\LaravelData\PaginatedDataCollection;

/**
 * `GET /api/v1/payroll-runs` — list action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
#[AsAction(name: 'expenses.payroll_runs.list')]
#[Get('/api/v1/payroll-runs')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ListPayrollRunAction
{
    use AsController;

    public function __construct(
        private readonly PayrollRunRepositoryInterface $repository,
    ) {
    }

    /**
     * List `payroll-runs` for the current caller.
     *
     * The `Repository` base picks up `filter[...]` / `sort=...` /
     * `include=...` query parameters via the `#[Filterable]` attribute
     * on the concrete repository — no per-Action wiring needed.
     *
     * @return PaginatedDataCollection<int, PayrollRunData>  Paginated wire-visible DTOs.
     */
    public function __invoke(Request $request): PaginatedDataCollection
    {
        /** @var LengthAwarePaginator<int, \Academorix\Expenses\Models\PayrollRun> $page */
        $page = $this->repository->paginate(
            perPage: (int) $request->integer('per_page', 15),
        );

        return PayrollRunData::collect($page, PaginatedDataCollection::class);
    }
}
