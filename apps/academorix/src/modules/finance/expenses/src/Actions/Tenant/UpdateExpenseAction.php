<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Stackra\Expenses\Actions\Tenant;

use Stackra\Expenses\Contracts\Repositories\ExpenseRepositoryInterface;
use Stackra\Expenses\Data\ExpenseData;
use Stackra\Expenses\Data\Requests\UpdateExpenseRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/expenses/{expense}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
#[AsAction(name: 'expenses.update')]
#[Patch('/api/v1/expenses/{expense}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateExpenseAction
{
    use AsController;

    public function __construct(
        private readonly ExpenseRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `expense` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateExpenseRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateExpenseRequestData $data): ExpenseData
    {
        $model = $this->repository->update($id, $data->toArray());

        return ExpenseData::from($model);
    }
}
