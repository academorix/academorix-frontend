<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

namespace Stackra\Expenses\Services;

use Stackra\Expenses\Contracts\Services\AccountingSyncerInterface;
use Illuminate\Container\Attributes\Scoped;
use Stackra\Expenses\Contracts\Repositories\BudgetRepositoryInterface;

/**
 * Concrete service — AccountingSyncer.
 *
 * Implements {@see AccountingSyncerInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Expenses
 *
 * @since    0.1.0
 */
#[Scoped]
final class AccountingSyncer implements AccountingSyncerInterface
{
    /**
     * @param  BudgetRepositoryInterface  $budgetRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly BudgetRepositoryInterface $budgetRepository,
    ) {
    }
}
