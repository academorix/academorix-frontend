<?php

/**
 * @file database/migrations/2026_07_15_120005_create_payroll_runs_table.php
 *
 * @description
 * Create the `payroll_runs` table for the `PayrollRun` entity.
 * Owned by the expenses module. Every column + index reference
 * goes through {@see Stackra\Expenses\Contracts\Data\PayrollRunInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance expenses --force`.

declare(strict_types=1);

use Stackra\Expenses\Contracts\Data\PayrollRunInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(PayrollRunInterface::TABLE, function (Blueprint $table): void {
            $table->string(PayrollRunInterface::ATTR_ID, 30)->primary();
            $table->string(PayrollRunInterface::ATTR_TENANT_ID, 30);
            $table->foreign(PayrollRunInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->date(PayrollRunInterface::ATTR_PERIOD_START);
            $table->date(PayrollRunInterface::ATTR_PERIOD_END);
            $table->string(PayrollRunInterface::ATTR_STATUS, 16)->default('draft');
            $table->unsignedBigInteger(PayrollRunInterface::ATTR_TOTAL_AMOUNT_MINOR)->default('0');
            $table->string(PayrollRunInterface::ATTR_CURRENCY, 3);
            $table->integer(PayrollRunInterface::ATTR_LINE_COUNT)->default('0');
            $table->timestampTz(PayrollRunInterface::ATTR_COMPUTED_AT)->nullable();
            $table->timestampTz(PayrollRunInterface::ATTR_APPROVED_AT)->nullable();
            $table->string(PayrollRunInterface::ATTR_APPROVED_BY_USER_ID, 30)->nullable();
            $table->foreign(PayrollRunInterface::ATTR_APPROVED_BY_USER_ID)->references('id')->on('users')->onDelete('set null');
            $table->timestampTz(PayrollRunInterface::ATTR_PAID_AT)->nullable();
            $table->text(PayrollRunInterface::ATTR_NOTES)->nullable();
            $table->jsonb(PayrollRunInterface::ATTR_METADATA)->nullable();
            $table->timestampTz(PayrollRunInterface::ATTR_CREATED_AT);
            $table->timestampTz(PayrollRunInterface::ATTR_UPDATED_AT);
            $table->timestampTz(PayrollRunInterface::ATTR_DELETED_AT)->nullable();
            $table->index([PayrollRunInterface::ATTR_TENANT_ID, PayrollRunInterface::ATTR_PERIOD_START, PayrollRunInterface::ATTR_PERIOD_END], 'payroll_runs_period_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(PayrollRunInterface::TABLE);
    }
};
