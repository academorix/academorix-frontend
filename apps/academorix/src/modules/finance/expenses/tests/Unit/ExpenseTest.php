<?php

declare(strict_types=1);

use Stackra\Expenses\Models\Expense;

it('is a placeholder — replace with actual Expense unit tests', function (): void {
    expect(Expense::class)->toBeString();
});
