<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance marketplace-fee --force`.

declare(strict_types=1);

namespace Academorix\MarketplaceFee\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan marketplace-fee:list-schedules` — List schedules — marketplace fee command.
 *
 * @category MarketplaceFee
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'marketplace-fee:list-schedules',
    description: 'List schedules — marketplace fee command.',
)]
final class ListSchedulesCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List schedules — marketplace fee command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List schedules — marketplace fee command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
