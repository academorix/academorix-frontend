<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance marketplace-fee --force`.

declare(strict_types=1);

namespace Stackra\MarketplaceFee\Services;

use Stackra\MarketplaceFee\Contracts\Services\FeeCalculatorInterface;
use Illuminate\Container\Attributes\Scoped;
use Stackra\MarketplaceFee\Contracts\Repositories\FeeApplicationRepositoryInterface;

/**
 * Concrete service — FeeCalculator.
 *
 * Implements {@see FeeCalculatorInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category MarketplaceFee
 *
 * @since    0.1.0
 */
#[Scoped]
final class FeeCalculator implements FeeCalculatorInterface
{
    /**
     * @param  FeeApplicationRepositoryInterface  $feeApplicationRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly FeeApplicationRepositoryInterface $feeApplicationRepository,
    ) {
    }
}
