<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance marketplace-fee --force`.

declare(strict_types=1);

namespace Stackra\MarketplaceFee\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New FeePayout roll-up committed. Includes N fee_applications.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 *
 * @category MarketplaceFee
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class FeePayoutCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $feePayoutId  ULID (fpo_*)
     * @param  string  $tenantId  ULID
     * @param  string  $applicationId  ULID
     * @param  string  $periodStart  ISO 8601
     * @param  string  $periodEnd  ISO 8601
     * @param  int  $totalFeeMinor  integer
     * @param  int  $applicationCount  integer
     */
    public function __construct(
        public readonly string $feePayoutId,
        public readonly string $tenantId,
        public readonly string $applicationId,
        public readonly string $periodStart,
        public readonly string $periodEnd,
        public readonly int $totalFeeMinor,
        public readonly int $applicationCount,
    ) {
    }
}
