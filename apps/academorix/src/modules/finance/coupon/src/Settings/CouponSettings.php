<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * CouponSettings — Coupons.
 *
 * Tenant-wide defaults for coupon issuance + validation.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'coupon',
    label: 'Coupons',
    description: 'Tenant-wide defaults for coupon issuance + validation.',
    icon: 'ticket',
    scope: 'tenant',
    permission: 'coupons.viewAny',
    sortOrder: 55,
)]
class CouponSettings extends Settings
{

    // ── Coupon defaults ──────────────────────────────────
    #[SettingGroup(label: 'Coupon defaults', description: 'Default values applied to newly-created coupons. Overridable per coupon.', icon: 'cog', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Select, label: 'Default discount type', group: 'Coupon defaults', sortOrder: 1, options: ['percent' => 'Percent discount', 'fixed_amount' => 'Fixed amount', 'free_period' => 'Free period (Medium+)', 'first_month_free' => 'First month free (Medium+)', 'bogo' => 'Buy one, get one (Medium+)', 'free_shipping' => 'Free shipping'])]
    public string $default_discount_type = 'percent';

    #[SettingField(controlType: ControlType::Number, label: 'Default per-customer limit', group: 'Coupon defaults', sortOrder: 2, min: 1, helpText: 'How many times a single customer can redeem the coupon. 1 = one-shot per customer.')]
    public int $default_per_customer_limit = 1;

    #[SettingField(controlType: ControlType::Toggle, label: 'Default: stackable', group: 'Coupon defaults', sortOrder: 3, helpText: 'Allow multiple coupons per invoice by default. Requires Enterprise `coupon_stackable`.')]
    public bool $default_stackable = false;


    // ── Rate limits ──────────────────────────────────
    #[SettingGroup(label: 'Rate limits', description: 'Preview + validate endpoint rate limits (protects against brute-force enumeration).', icon: 'shield-check', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Number, label: 'Max preview/validate calls per IP per hour', group: 'Rate limits', sortOrder: 1, min: 1, max: 100, helpText: '10 is a safe default. Raise for high-traffic tenants; lower for high-risk industries.')]
    public int $rate_limit_ip_per_hour = 20;

    #[SettingField(controlType: ControlType::Number, label: 'Max preview/validate calls per authenticated user per hour', group: 'Rate limits', sortOrder: 2, min: 1)]
    public int $rate_limit_user_per_hour = 60;


    // ── Reporting ──────────────────────────────────
    #[SettingGroup(label: 'Reporting', icon: 'chart-bar', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Text, label: 'Weekly report recipients', group: 'Reporting', sortOrder: 1, helpText: 'Users to notify when the weekly coupon usage report is generated. Empty = no notification (report still generated + available via GET /coupons/report).')]
    public ?string $weekly_report_recipients = null;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify admins on every redemption', group: 'Reporting', sortOrder: 2, helpText: 'Opt-in real-time push. Useful for high-value / limited campaigns. Off by default (most tenants don\'t want per-redemption pings).')]
    public bool $notify_on_every_redemption = false;


    // ── Retention ──────────────────────────────────
    #[SettingGroup(label: 'Retention', icon: 'archive-box', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Select, label: 'Retention window', group: 'Retention', sortOrder: 1, options: ['7' => '7 years (default)', '10' => '10 years (Enterprise)'], helpText: '10 years requires the `coupon_extended_retention` entitlement.')]
    public int $retention_years = 7;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'coupon';
    }
}
