<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `discount_type` on `coupon`.
 *
 * Enum: percent / fixed_amount / free_period / first_month_free / bogo / free_shipping.
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
enum CouponDiscountType: string
{
    use Enum;

    case Percent = 'percent';
    case FixedAmount = 'fixed_amount';
    case FreePeriod = 'free_period';
    case FirstMonthFree = 'first_month_free';
    case Bogo = 'bogo';
    case FreeShipping = 'free_shipping';
}
