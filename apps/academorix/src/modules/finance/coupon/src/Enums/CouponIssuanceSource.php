<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `issuance_source` on `coupon`.
 *
 * Enum: manual / referral / marketing_campaign / import / api.
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
enum CouponIssuanceSource: string
{
    use Enum;

    case Manual = 'manual';
    case Referral = 'referral';
    case MarketingCampaign = 'marketing_campaign';
    case Import = 'import';
    case Api = 'api';
}
