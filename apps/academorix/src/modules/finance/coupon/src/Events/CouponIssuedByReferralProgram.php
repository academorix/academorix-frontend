<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Stackra\Coupon\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CouponIssuer::issueForReferral() successfully created a Coupon row with issuance_source='referral'. Called by growth::referrals when a ReferralReward.status transitioned to vested with a discount-type reward.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - referrals::LinkCouponToReward (records the coupon_id back onto the referral_rewards row for cross-lookup)
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CouponIssuedByReferralProgram implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $couponId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $code  string
     * @param  string  $referralProgramId  ULID (rpg_*)
     * @param  string  $referralId  ULID (ref_*)
     * @param  string  $referralRewardId  ULID (rrw_*)
     * @param  string  $recipientRole  'referrer' | 'referred'
     * @param  string  $recipientUserId  ULID
     * @param  string  $discountType  string
     * @param  string  $discountAmount  number
     * @param  string  $issuedAt  ISO 8601
     */
    public function __construct(
        public readonly string $couponId,
        public readonly string $tenantId,
        public readonly string $code,
        public readonly string $referralProgramId,
        public readonly string $referralId,
        public readonly string $referralRewardId,
        public readonly string $recipientRole,
        public readonly string $recipientUserId,
        public readonly string $discountType,
        public readonly string $discountAmount,
        public readonly string $issuedAt,
    ) {
    }
}
