<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CouponRedeemer::redeem() or CouponValidator::canRedeem() rejected the redemption attempt. Fires with the specific refusal reason. Audit-critical — supports customer support triage.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog (staff-visible for support triage)
 * - observability::MetricsCollector (stackra.coupons.redemption_refusals_total labelled by refusal_reason)
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CouponRedemptionRefused implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $couponId  ULID | null (null when code doesn't exist)
     * @param  string  $tenantId  ULID
     * @param  string  $code  string
     * @param  string  $customerType  string | null
     * @param  string  $customerId  ULID | null
     * @param  string  $attemptedAppliedToType  string | null
     * @param  string  $attemptedAppliedToId  ULID | null
     * @param  string  $refusalReason  'COUPON_NOT_FOUND' | 'COUPON_INACTIVE' | 'COUPON_EXPIRED' | 'COUPON_NOT_YET_VALID' | 'COUPON_USAGE_CAP_REACHED' | 'COUPON_CUSTOMER_LIMIT_REACHED' | 'COUPON_APPLICABILITY_MISMATCH' | 'COUPON_MINIMUM_ORDER_NOT_MET' | 'COUPON_ALREADY_REDEEMED_ON_TARGET' | 'COUPON_CURRENCY_MISMATCH'
     * @param  string  $refusedAt  ISO 8601
     */
    public function __construct(
        public readonly string $couponId,
        public readonly string $tenantId,
        public readonly string $code,
        public readonly string $customerType,
        public readonly string $customerId,
        public readonly string $attemptedAppliedToType,
        public readonly string $attemptedAppliedToId,
        public readonly string $refusalReason,
        public readonly string $refusedAt,
    ) {
    }
}
