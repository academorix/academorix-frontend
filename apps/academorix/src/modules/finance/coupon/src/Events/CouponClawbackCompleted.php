<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ProcessCouponClawbackJob finished — CouponRedemption.reversed_at set + Coupon.usage_count decremented atomically.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - activity::WriteToActivityLog
 * - notifications::DispatchCouponClawbackNotification (customer + admin — cannot-opt-out)
 * - marketing::ReverseCouponRedemptionMarketingEvent (only when the redemption's issuance_source triggered a marketing event)
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CouponClawbackCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $redemptionId  ULID
     * @param  string  $couponId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $customerType  string
     * @param  string  $customerId  ULID
     * @param  string  $clawbackReason  'refund' | 'chargeback' | 'fraud' | 'manual_admin'
     * @param  int  $reversedAmountCents  integer
     * @param  string  $completedAt  ISO 8601
     */
    public function __construct(
        public readonly string $redemptionId,
        public readonly string $couponId,
        public readonly string $tenantId,
        public readonly string $customerType,
        public readonly string $customerId,
        public readonly string $clawbackReason,
        public readonly int $reversedAmountCents,
        public readonly string $completedAt,
    ) {
    }
}
