<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CouponIssuer::issueManually() or CouponIssuer::bulkIssue() successfully created one or more Coupon rows with issuance_source IN ('manual', 'marketing_campaign', 'import', 'api'). Fires per created row.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CouponIssuedManually implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $couponId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $code  string
     * @param  string  $issuanceSource  'manual' | 'marketing_campaign' | 'import' | 'api'
     * @param  array  $issuanceContext  object | null
     * @param  string  $campaignName  string | null
     * @param  string  $createdBy  ULID
     * @param  string  $issuedAt  ISO 8601
     */
    public function __construct(
        public readonly string $couponId,
        public readonly string $tenantId,
        public readonly string $code,
        public readonly string $issuanceSource,
        public readonly array $issuanceContext,
        public readonly string $campaignName,
        public readonly string $createdBy,
        public readonly string $issuedAt,
    ) {
    }
}
