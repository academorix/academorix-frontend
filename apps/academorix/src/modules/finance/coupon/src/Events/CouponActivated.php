<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Stackra\Coupon\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Coupon.is_active transitioned false -> true. Common paths: manual reactivation before expiry OR admin-initiated activate after a `deactivate` reversal.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CouponActivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $couponId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $activatedAt  ISO 8601
     * @param  string  $activatedBy  ULID
     */
    public function __construct(
        public readonly string $couponId,
        public readonly string $tenantId,
        public readonly string $activatedAt,
        public readonly string $activatedBy,
    ) {
    }
}
