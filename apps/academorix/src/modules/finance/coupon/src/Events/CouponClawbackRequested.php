<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * finance::RefundIssued or finance::ChargebackFiled fires for an invoice that carried a CouponRedemption. ProcessCouponClawbackJob is dispatched. P1 signal — every clawback is a financial reversal.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial record)
 * - activity::WriteToActivityLog
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CouponClawbackRequested implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $redemptionId  ULID
     * @param  string  $couponId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $clawbackReason  'refund' | 'chargeback' | 'fraud' | 'manual_admin'
     * @param  string  $triggeringEventId  ULID (finance event that initiated the clawback)
     * @param  string  $requestedAt  ISO 8601
     */
    public function __construct(
        public readonly string $redemptionId,
        public readonly string $couponId,
        public readonly string $tenantId,
        public readonly string $clawbackReason,
        public readonly string $triggeringEventId,
        public readonly string $requestedAt,
    ) {
    }
}
