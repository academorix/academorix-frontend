<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Academorix\Coupon\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan coupon:report` — Report on the coupon data.
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'coupon:report',
    description: 'Report on the coupon data.',
)]
final class ReportCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Report on the coupon data', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Report on the coupon data completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
