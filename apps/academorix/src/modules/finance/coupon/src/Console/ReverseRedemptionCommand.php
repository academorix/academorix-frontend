<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance coupon --force`.

declare(strict_types=1);

namespace Stackra\Coupon\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan coupon:reverse-redemption` — Reverse redemption — coupon command.
 *
 * @category Coupon
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'coupon:reverse-redemption',
    description: 'Reverse redemption — coupon command.',
)]
final class ReverseRedemptionCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Reverse redemption — coupon command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Reverse redemption — coupon command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
