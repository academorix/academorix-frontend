<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payout --force`.

declare(strict_types=1);

namespace Stackra\Payout\Actions\Tenant;

use Stackra\Payout\Contracts\Repositories\PayoutRepositoryInterface;
use Stackra\Payout\Data\PayoutData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/payouts/{payout}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Payout
 *
 * @since    0.1.0
 */
#[AsAction(name: 'payout.payouts.show')]
#[Get('/api/v1/payouts/{payout}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowPayoutAction
{
    use AsController;

    public function __construct(
        private readonly PayoutRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `payout` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): PayoutData
    {
        return PayoutData::from($this->repository->findOrFail($id));
    }
}
