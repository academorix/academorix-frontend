<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payout --force`.

declare(strict_types=1);

namespace Stackra\Payout\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Payout.status transitions -> paid. Triggers reconciliation job.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - payout::ReconcilePayoutListener
 * - notifications::DispatchPayoutPaidNotificationListener
 *
 * @category Payout
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PayoutPaid implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $payoutId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $netMinor  integer
     * @param  string  $paidAt  ISO 8601
     */
    public function __construct(
        public readonly string $payoutId,
        public readonly string $tenantId,
        public readonly int $netMinor,
        public readonly string $paidAt,
    ) {
    }
}
