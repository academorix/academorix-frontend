<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payout --force`.

declare(strict_types=1);

namespace Stackra\Payout\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `status` on `payout`.
 *
 * Enum: pending / in_transit / paid / failed / cancelled.
 *
 * @category Payout
 *
 * @since    0.1.0
 */
enum PayoutStatus: string
{
    use Enum;

    case Pending = 'pending';
    case InTransit = 'in_transit';
    case Paid = 'paid';
    case Failed = 'failed';
    case Cancelled = 'cancelled';
}
