<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payout --force`.

declare(strict_types=1);

namespace Academorix\Payout\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Payout module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Payout
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum PayoutPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `payouts.viewAny` — Payouts ViewAny
     */
    #[Label('Payouts ViewAny')]
    #[Description('Payouts ViewAny')]
    case PayoutsViewAny = 'payouts.viewAny';

    /**
     * `payouts.view` — Payouts View
     */
    #[Label('Payouts View')]
    #[Description('Payouts View')]
    case PayoutsView = 'payouts.view';

    /**
     * `payouts.sync` — Force re-pull from provider.
     */
    #[Label('Force re-pull from provider')]
    #[Description('Force re-pull from provider.')]
    case PayoutsSync = 'payouts.sync';

    /**
     * `payout-items.viewAny` — Payout-items ViewAny
     */
    #[Label('Payout-items ViewAny')]
    #[Description('Payout-items ViewAny')]
    case PayoutItemsViewAny = 'payout-items.viewAny';

    /**
     * `payout-items.view` — Payout-items View
     */
    #[Label('Payout-items View')]
    #[Description('Payout-items View')]
    case PayoutItemsView = 'payout-items.view';

    /**
     * `payout-reconciliations.viewAny` — Payout-reconciliations ViewAny
     */
    #[Label('Payout-reconciliations ViewAny')]
    #[Description('Payout-reconciliations ViewAny')]
    case PayoutReconciliationsViewAny = 'payout-reconciliations.viewAny';

    /**
     * `payout-reconciliations.view` — Payout-reconciliations View
     */
    #[Label('Payout-reconciliations View')]
    #[Description('Payout-reconciliations View')]
    case PayoutReconciliationsView = 'payout-reconciliations.view';

    /**
     * `payout-reconciliations.resolve` — Mark a drift reconciliation as resolved (audit-logged).
     */
    #[Label('Mark a drift reconciliation as resolved (audit-logged)')]
    #[Description('Mark a drift reconciliation as resolved (audit-logged).')]
    case PayoutReconciliationsResolve = 'payout-reconciliations.resolve';

    /**
     * `platform.payouts.viewAny` — Cross-tenant payout audit.
     */
    #[Label('Cross-tenant payout audit')]
    #[Description('Cross-tenant payout audit.')]
    case PlatformPayoutsViewAny = 'platform.payouts.viewAny';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::PayoutsViewAny => Guard::Sanctum,
            self::PayoutsView => Guard::Sanctum,
            self::PayoutsSync => Guard::Sanctum,
            self::PayoutItemsViewAny => Guard::Sanctum,
            self::PayoutItemsView => Guard::Sanctum,
            self::PayoutReconciliationsViewAny => Guard::Sanctum,
            self::PayoutReconciliationsView => Guard::Sanctum,
            self::PayoutReconciliationsResolve => Guard::Sanctum,
            self::PlatformPayoutsViewAny => Guard::PlatformAdmin,
        };
    }
}
