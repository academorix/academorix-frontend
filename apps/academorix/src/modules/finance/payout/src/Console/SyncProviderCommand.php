<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payout --force`.

declare(strict_types=1);

namespace Academorix\Payout\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan payout:sync-provider` — Sync provider — payout command.
 *
 * @category Payout
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'payout:sync-provider',
    description: 'Sync provider — payout command.',
)]
final class SyncProviderCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Sync provider — payout command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Sync provider — payout command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
