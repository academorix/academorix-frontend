<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance payout --force`.

declare(strict_types=1);

namespace Academorix\Payout\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan payout:show` — Show one payout.
 *
 * @category Payout
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'payout:show',
    description: 'Show one payout.',
)]
final class ShowCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Show one payout', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Show one payout completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
