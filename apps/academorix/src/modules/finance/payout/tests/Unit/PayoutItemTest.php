<?php

declare(strict_types=1);

use Academorix\Payout\Models\PayoutItem;

it('is a placeholder — replace with actual PayoutItem unit tests', function (): void {
    expect(PayoutItem::class)->toBeString();
});
