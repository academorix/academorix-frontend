<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Enterprise-only. EscalateDunningToCollectionsJob handed off to a third-party collections agency after N days past due (default 60). Sets invoices.collection_status = 'with_collections'.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchInvoiceSentToCollectionsNotification (customer + admin)
 * - monitoring::MetricsCollector
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class InvoiceSentToCollections implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $invoiceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceNumber  string
     * @param  int  $amountDueCents  integer
     * @param  string  $currency  string
     * @param  int  $daysPastDue  integer
     * @param  string  $collectionsAgency  string (agency slug)
     * @param  string  $sentAt  ISO 8601
     */
    public function __construct(
        public readonly string $invoiceId,
        public readonly string $tenantId,
        public readonly string $invoiceNumber,
        public readonly int $amountDueCents,
        public readonly string $currency,
        public readonly int $daysPastDue,
        public readonly string $collectionsAgency,
        public readonly string $sentAt,
    ) {
    }
}
