<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CreditNoteObserver.updating detected status transition draft → issued. PDF ready + delivered. Sets credit_notes.status='issued'.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchCreditNoteIssuedNotification (customer)
 * - monitoring::MetricsCollector
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CreditNoteIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $creditNoteId  ULID
     * @param  string  $creditNoteNumber  string
     * @param  string  $tenantId  ULID
     * @param  string  $originalInvoiceId  ULID
     * @param  int  $amountCents  integer
     * @param  string  $currency  string
     * @param  string  $issuedAt  ISO 8601
     */
    public function __construct(
        public readonly string $creditNoteId,
        public readonly string $creditNoteNumber,
        public readonly string $tenantId,
        public readonly string $originalInvoiceId,
        public readonly int $amountCents,
        public readonly string $currency,
        public readonly string $issuedAt,
    ) {
    }
}
