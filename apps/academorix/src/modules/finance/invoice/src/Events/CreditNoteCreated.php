<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Stackra\Invoice\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CreditNoteObserver.creating committed a new credit_notes row. Reason + amount + original_invoice reference all set. Precedes CreditNoteIssued after PDF generation.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — financial audit)
 * - activity::WriteToActivityLog
 * - invoice::EnqueueGenerateCreditNotePDFJob
 * - monitoring::MetricsCollector
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CreditNoteCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $creditNoteId  ULID (crn_*)
     * @param  string  $creditNoteNumber  string (CN-2026-000001)
     * @param  string  $tenantId  ULID
     * @param  string  $originalInvoiceId  ULID (ivc_*)
     * @param  string  $reason  string (refund / adjustment / write_off / duplicate / customer_dispute_resolved / accounting_correction)
     * @param  int  $amountCents  integer
     * @param  string  $currency  string
     * @param  string  $issuedByUserId  ULID
     * @param  string  $createdAt  ISO 8601
     */
    public function __construct(
        public readonly string $creditNoteId,
        public readonly string $creditNoteNumber,
        public readonly string $tenantId,
        public readonly string $originalInvoiceId,
        public readonly string $reason,
        public readonly int $amountCents,
        public readonly string $currency,
        public readonly string $issuedByUserId,
        public readonly string $createdAt,
    ) {
    }
}
