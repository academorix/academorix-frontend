<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SendInvoiceEmailJob successfully delivered the invoice email. Sets invoices.sent_at.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - monitoring::MetricsCollector (increments stackra.invoice.sent_total)
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class InvoiceSent implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $invoiceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceNumber  string
     * @param  string  $customerType  string
     * @param  string  $customerId  ULID
     * @param  string  $emailChannel  'mail' | 'in_app'
     * @param  string  $sentAt  ISO 8601
     */
    public function __construct(
        public readonly string $invoiceId,
        public readonly string $tenantId,
        public readonly string $invoiceNumber,
        public readonly string $customerType,
        public readonly string $customerId,
        public readonly string $emailChannel,
        public readonly string $sentAt,
    ) {
    }
}
