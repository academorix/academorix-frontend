<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * CreditNoteObserver.updating detected status transition issued → applied. The credit was consumed — either applied to a refund, offset against a future invoice, or added to the customer's account balance. Sets applied_to_type + applied_to_id.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - monitoring::MetricsCollector
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CreditNoteApplied implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $creditNoteId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $appliedToType  'refund' | 'next_invoice' | 'customer_balance'
     * @param  string  $appliedToId  ULID (polymorphic)
     * @param  string  $appliedAt  ISO 8601
     */
    public function __construct(
        public readonly string $creditNoteId,
        public readonly string $tenantId,
        public readonly string $appliedToType,
        public readonly string $appliedToId,
        public readonly string $appliedAt,
    ) {
    }
}
