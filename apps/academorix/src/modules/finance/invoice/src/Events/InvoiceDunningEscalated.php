<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Stackra\Invoice\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * DispatchDunningRemindersJob progressed to a later stage of the dunning cadence (day+3 → day+7 → day+14 → day+30 → day+60). Each escalation fires once.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class InvoiceDunningEscalated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $invoiceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $escalationStage  'day_3' | 'day_7' | 'day_14' | 'day_30' | 'day_60'
     * @param  int  $daysPastDue  integer
     * @param  string  $escalatedAt  ISO 8601
     */
    public function __construct(
        public readonly string $invoiceId,
        public readonly string $tenantId,
        public readonly string $escalationStage,
        public readonly int $daysPastDue,
        public readonly string $escalatedAt,
    ) {
    }
}
