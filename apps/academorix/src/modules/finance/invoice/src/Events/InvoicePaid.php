<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * LOAD-BEARING. InvoiceObserver.updating detected status transition any → paid. Requires amount_paid_cents == total_cents. Fires the marketing::CaptureMembershipPurchasedMarketingEvent when membership_id is set (the marketing lane's revenue signal).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — critical financial event)
 * - activity::WriteToActivityLog
 * - notifications::DispatchInvoicePaidNotification (customer receipt + admin confirmation)
 * - monitoring::MetricsCollector (increments academorix.invoice.paid_total; buckets on total_cents)
 * - growth::marketing::CaptureMembershipPurchasedMarketingEvent (when membership_id present — the load-bearing marketing signal)
 * - membership::ActivateMembershipOnInvoicePaid (when membership_id present — transitions Membership status=active + generates Passes)
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class InvoicePaid implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $invoiceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceNumber  string
     * @param  string  $customerType  string
     * @param  string  $customerId  ULID
     * @param  string  $membershipId  ULID | null
     * @param  string  $membershipRenewalId  ULID | null
     * @param  int  $totalCents  integer
     * @param  string  $currency  string
     * @param  string  $paidAt  ISO 8601
     */
    public function __construct(
        public readonly string $invoiceId,
        public readonly string $tenantId,
        public readonly string $invoiceNumber,
        public readonly string $customerType,
        public readonly string $customerId,
        public readonly string $membershipId,
        public readonly string $membershipRenewalId,
        public readonly int $totalCents,
        public readonly string $currency,
        public readonly string $paidAt,
    ) {
    }
}
