<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Stackra\Invoice\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Compliance-critical. Terminal write-off after collections gives up (typically 180 days past due). Sets invoices.collection_status = 'written_off'. Feeds bad-debt expense line on the accounting ledger.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchInvoiceWrittenOffNotification (admin — quarterly digest)
 * - monitoring::MetricsCollector (every fire is a bad-debt loss)
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class InvoiceWrittenOff implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $invoiceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $invoiceNumber  string
     * @param  int  $amountWrittenOffCents  integer
     * @param  string  $currency  string
     * @param  string  $writtenOffAt  ISO 8601
     */
    public function __construct(
        public readonly string $invoiceId,
        public readonly string $tenantId,
        public readonly string $invoiceNumber,
        public readonly int $amountWrittenOffCents,
        public readonly string $currency,
        public readonly string $writtenOffAt,
    ) {
    }
}
