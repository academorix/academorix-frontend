<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Stackra\Invoice\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * InvoiceLineObserver.deleting committed. Only allowed while parent invoice.status='draft'. Recomputes invoice totals + re-assigns line_numbers.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class InvoiceLineRemoved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $invoiceLineId  ULID
     * @param  string  $invoiceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $removedAt  ISO 8601
     */
    public function __construct(
        public readonly string $invoiceLineId,
        public readonly string $invoiceId,
        public readonly string $tenantId,
        public readonly string $removedAt,
    ) {
    }
}
