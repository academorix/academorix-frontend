<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Stackra\Invoice\Actions\Tenant;

use Stackra\Invoice\Contracts\Repositories\CreditNoteRepositoryInterface;
use Stackra\Invoice\Data\CreditNoteData;
use Stackra\Invoice\Data\Requests\CreateCreditNoteRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/credit-notes` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsAction(name: 'invoice.credit_notes.create')]
#[Post('/api/v1/credit-notes')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateCreditNoteAction
{
    use AsController;

    public function __construct(
        private readonly CreditNoteRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `credit-note` from the validated request payload.
     *
     * @param  CreateCreditNoteRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateCreditNoteRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(CreditNoteData::from($model), JsonResponse::HTTP_CREATED);
    }
}
