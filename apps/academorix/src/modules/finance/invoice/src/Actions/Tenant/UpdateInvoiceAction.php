<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Stackra\Invoice\Actions\Tenant;

use Stackra\Invoice\Contracts\Repositories\InvoiceRepositoryInterface;
use Stackra\Invoice\Data\InvoiceData;
use Stackra\Invoice\Data\Requests\UpdateInvoiceRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/invoices/{invoice}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsAction(name: 'invoice.invoices.update')]
#[Patch('/api/v1/invoices/{invoice}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateInvoiceAction
{
    use AsController;

    public function __construct(
        private readonly InvoiceRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `invoice` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateInvoiceRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateInvoiceRequestData $data): InvoiceData
    {
        $model = $this->repository->update($id, $data->toArray());

        return InvoiceData::from($model);
    }
}
