<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Actions\Tenant;

use Academorix\Invoice\Contracts\Repositories\InvoiceRepositoryInterface;
use Academorix\Invoice\Data\InvoiceData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/invoices/{invoice}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsAction(name: 'invoice.invoices.show')]
#[Get('/api/v1/invoices/{invoice}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowInvoiceAction
{
    use AsController;

    public function __construct(
        private readonly InvoiceRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `invoice` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): InvoiceData
    {
        return InvoiceData::from($this->repository->findOrFail($id));
    }
}
