<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Stackra\Invoice\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `status` on `credit-note`.
 *
 * Enum: draft / issued / applied / voided.
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
enum CreditNoteStatus: string
{
    use Enum;

    case Draft = 'draft';
    case Issued = 'issued';
    case Applied = 'applied';
    case Voided = 'voided';
}
