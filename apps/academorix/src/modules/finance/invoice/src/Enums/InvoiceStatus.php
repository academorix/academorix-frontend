<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Stackra\Invoice\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `status` on `invoice`.
 *
 * Enum: draft / open / paid / partially_paid / void / uncollectible / pastdue / refunded / disputed / written_off.
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
enum InvoiceStatus: string
{
    use Enum;

    case Draft = 'draft';
    case Open = 'open';
    case Paid = 'paid';
    case PartiallyPaid = 'partially_paid';
    case Void = 'void';
    case Uncollectible = 'uncollectible';
    case Pastdue = 'pastdue';
    case Refunded = 'refunded';
    case Disputed = 'disputed';
    case WrittenOff = 'written_off';
}
