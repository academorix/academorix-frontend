<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Stackra\Invoice\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan invoice:void` — Void — invoice command.
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'invoice:void',
    description: 'Void — invoice command.',
)]
final class VoidCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Void — invoice command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Void — invoice command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
