<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Stackra\Invoice\Services;

use Stackra\Invoice\Contracts\Services\CreditNoteNumberGeneratorInterface;
use Illuminate\Container\Attributes\Scoped;
use Stackra\Invoice\Contracts\Repositories\CreditNoteRepositoryInterface;

/**
 * Concrete service — CreditNoteNumberGenerator.
 *
 * Implements {@see CreditNoteNumberGeneratorInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[Scoped]
final class CreditNoteNumberGenerator implements CreditNoteNumberGeneratorInterface
{
    /**
     * @param  CreditNoteRepositoryInterface  $creditNoteRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly CreditNoteRepositoryInterface $creditNoteRepository,
    ) {
    }
}
