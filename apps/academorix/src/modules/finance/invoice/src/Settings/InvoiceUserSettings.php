<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Academorix\Invoice\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * InvoiceUserSettings — My invoicing preferences.
 *
 * Per-user preferences for invoice reports + alerts.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'invoice_user',
    label: 'My invoicing preferences',
    description: 'Per-user preferences for invoice reports + alerts.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 35,
)]
class InvoiceUserSettings extends Settings
{

    // ── Report subscriptions ──────────────────────────────────
    #[SettingGroup(label: 'Report subscriptions', icon: 'chart-bar', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Send me the monthly revenue report', group: 'Report subscriptions', sortOrder: 1)]
    public bool $receive_monthly_revenue_report = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Send me the quarterly write-off digest', group: 'Report subscriptions', sortOrder: 2)]
    public bool $receive_quarterly_write_off_digest = true;


    // ── Alert subscriptions ──────────────────────────────────
    #[SettingGroup(label: 'Alert subscriptions', icon: 'bell', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me on invoices marked uncollectible', group: 'Alert subscriptions', sortOrder: 1, helpText: 'This preference only takes effect if the tenant admin listed you in uncollectible_notify_users. Owners always receive.')]
    public bool $receive_uncollectible_alerts = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me on payment reconciliation drift (ops only)', group: 'Alert subscriptions', sortOrder: 2)]
    public bool $receive_reconcile_drift_alerts = false;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'invoice_user';
    }
}
