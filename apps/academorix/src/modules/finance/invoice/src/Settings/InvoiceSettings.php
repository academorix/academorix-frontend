<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py finance invoice --force`.

declare(strict_types=1);

namespace Stackra\Invoice\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * InvoiceSettings — Invoicing.
 *
 * Tenant-wide defaults for invoice numbering, delivery, and dunning.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Invoice
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'invoice',
    label: 'Invoicing',
    description: 'Tenant-wide defaults for invoice numbering, delivery, and dunning.',
    icon: 'document-text',
    scope: 'tenant',
    permission: 'invoice.invoices.viewAny',
    sortOrder: 30,
)]
class InvoiceSettings extends Settings
{

    // ── Numbering ──────────────────────────────────
    #[SettingGroup(label: 'Numbering', description: 'Invoice + credit note numbering conventions.', icon: 'hashtag', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Text, label: 'Default series prefix', group: 'Numbering', sortOrder: 1, helpText: 'Invoice numbers start with this prefix. Small tenants locked to \'INV\'; Medium+ may set to \'SO\', \'RECEIPT\', or a custom 3-8 char slug.')]
    public string $default_series = 'INV';

    #[SettingField(controlType: ControlType::Toggle, label: 'Reset counter on Jan 1st', group: 'Numbering', sortOrder: 2, helpText: 'true = counter resets each year (2026-000001 → 2027-000001). false = monotonic across years. Change requires ops review — mid-year change can create numbering ambiguity.')]
    public bool $per_year_reset = true;


    // ── Delivery ──────────────────────────────────
    #[SettingGroup(label: 'Delivery', description: 'How invoices are delivered to customers.', icon: 'envelope', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Auto-send email on finalize', group: 'Delivery', sortOrder: 1, helpText: 'true = SendInvoiceEmailJob dispatches automatically on finalize. false = admin manually invokes POST /invoices/{invoice}/send.')]
    public bool $auto_send_on_finalize = true;

    #[SettingField(controlType: ControlType::Text, label: 'BCC on every invoice email', group: 'Delivery', sortOrder: 2, helpText: 'Typically your finance-team email(s). Applied to every outbound invoice email.')]
    public array $email_bcc = [];

    #[SettingField(controlType: ControlType::Toggle, label: 'Include tracking pixel', group: 'Delivery', sortOrder: 3, helpText: 'true = fire InvoiceViewed when the customer opens the email. false = disable (some tenants prefer for privacy).')]
    public bool $tracking_pixel_enabled = true;


    // ── Dunning ──────────────────────────────────
    #[SettingGroup(label: 'Dunning', description: 'Past-due reminder cadence.', icon: 'clock', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Text, label: 'Reminder cadence (days past due)', group: 'Dunning', sortOrder: 1, helpText: 'Enterprise-only — customize the default day+3/7/14/30/60 schedule.')]
    public array $cadence = [3, 7, 14, 30, 60];

    #[SettingField(controlType: ControlType::Number, label: 'Uncollectible threshold (days past due)', group: 'Dunning', sortOrder: 2, min: 60, max: 365)]
    public int $uncollectible_after_days = 90;

    #[SettingField(controlType: ControlType::Toggle, label: 'Pause dunning when customer responds', group: 'Dunning', sortOrder: 3, helpText: 'true = pause when customer opens payment link or contacts support. false = dunning continues regardless.')]
    public bool $pause_on_customer_action = true;


    // ── Collections ──────────────────────────────────
    #[SettingGroup(label: 'Collections', description: 'Third-party collections agency handoff. Enterprise-only.', icon: 'briefcase', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Text, label: 'Collections agency slug', group: 'Collections', sortOrder: 1, helpText: 'Must be a pre-configured licensed agency in the notifications module.')]
    public ?string $collections_agency = null;

    #[SettingField(controlType: ControlType::Number, label: 'Handoff threshold (days past due)', group: 'Collections', sortOrder: 2, min: 30, max: 365)]
    public int $send_to_collections_after_days = 60;

    #[SettingField(controlType: ControlType::Number, label: 'Write-off threshold (days past due)', group: 'Collections', sortOrder: 3, min: 90, max: 730)]
    public int $write_off_after_days = 180;


    // ── PDF branding ──────────────────────────────────
    #[SettingGroup(label: 'PDF branding', description: 'Invoice PDF template selection.', icon: 'photo', sortOrder: 5)]
    #[SettingField(controlType: ControlType::Select, label: 'PDF template', group: 'PDF branding', sortOrder: 1, options: ['tenant-branded' => 'Tenant branded (logo + colors from branding module)', 'minimal' => 'Minimal (compact single-page)', 'vat-eu' => 'VAT (EU) — Art. 226-compliant layout'])]
    public string $pdf_template = 'tenant-branded';


    // ── Notification routing ──────────────────────────────────
    #[SettingGroup(label: 'Notification routing', description: 'Where invoice-related admin notifications are delivered.', icon: 'bell-alert', sortOrder: 6)]
    #[SettingField(controlType: ControlType::Text, label: 'Notify these users on uncollectible invoices', group: 'Notification routing', sortOrder: 1, helpText: 'Every user listed receives InvoiceUncollectibleNotification. Tenant owners always notified regardless.')]
    public array $uncollectible_notify_users = [];

    #[SettingField(controlType: ControlType::Text, label: 'Notify these users on written-off quarterly digest', group: 'Notification routing', sortOrder: 2)]
    public array $write_off_notify_users = [];

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'invoice';
    }
}
