<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The is_primary flag flipped on any event_facilities row. Auto-demote of the previous primary happens in the same transaction.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Event
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EventPrimaryFacilityChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $previousPrimaryFacilityId  ULID | null
     * @param  string  $newPrimaryFacilityId  ULID
     * @param  string  $changedBy  ULID
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $previousPrimaryFacilityId,
        public readonly string $newPrimaryFacilityId,
        public readonly string $changedBy,
    ) {
    }
}
