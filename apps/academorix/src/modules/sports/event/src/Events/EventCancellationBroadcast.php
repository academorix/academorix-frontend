<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Fires alongside EventCancelled to trigger the cannot-opt-out notification fan-out to every registered participant + coach. Separated from EventCancelled because notification consumers subscribe to THIS event (event lifecycle) while audit consumers subscribe to EventCancelled (state transition).
 *
 * ## Consumers
 *
 * - notifications::DispatchEventCancellationBroadcastNotification (cannot-opt-out; individual email + push per registrant + guardian)
 *
 * @category Event
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EventCancellationBroadcast implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $cancelledAt  ISO 8601
     * @param  string  $cancellationReason  string
     * @param  int  $recipientsCountEstimate  integer
     * @param  string  $auditSeverity  warn
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $cancelledAt,
        public readonly string $cancellationReason,
        public readonly int $recipientsCountEstimate,
        public readonly string $auditSeverity,
    ) {
    }
}
