<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Stackra\Event\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Event.status transitioned registration_open -> registration_closed. Automated (ExpireExpiredEventRegistrationsJob) OR manual. Late-registration window is independent — see allow_late_registration flag.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Event
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EventRegistrationClosed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $closedAt  ISO 8601
     * @param  int  $registeredCountSnapshot  integer
     * @param  string  $trigger  'automated_phase_roll' | 'manual_admin'
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $closedAt,
        public readonly int $registeredCountSnapshot,
        public readonly string $trigger,
    ) {
    }
}
