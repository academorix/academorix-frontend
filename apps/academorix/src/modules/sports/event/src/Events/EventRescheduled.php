<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Event.starts_at OR Event.ends_at changed on an announced-or-later event. AUDIT-WARN: affects every registered participant's calendar.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchEventRescheduledNotification (cannot-opt-out to registered)
 *
 * @category Event
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EventRescheduled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $shiftedBy  ULID
     * @param  string  $before  {'starts_at': 'ISO 8601', 'ends_at': 'ISO 8601'}
     * @param  string  $after  {'starts_at': 'ISO 8601', 'ends_at': 'ISO 8601'}
     * @param  int  $affectedRegistrationCount  integer
     * @param  string  $auditSeverity  warn
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $shiftedBy,
        public readonly string $before,
        public readonly string $after,
        public readonly int $affectedRegistrationCount,
        public readonly string $auditSeverity,
    ) {
    }
}
