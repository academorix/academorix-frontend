<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * EventProvisioner completed a successful create. Fires exactly once per `events.id`.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Event
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EventCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID (eve_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $organizationId  ULID (org_*)
     * @param  string  $branchId  ULID (brn_*) | null
     * @param  string  $seasonId  ULID (sea_*) | null
     * @param  string  $name  string
     * @param  string  $slug  string
     * @param  string  $eventType  string (tournament / match / showcase / workshop / camp / conference / social / graduation / awards_ceremony / open_house / other)
     * @param  string  $startsAt  ISO 8601
     * @param  string  $endsAt  ISO 8601
     * @param  string  $visibility  string
     * @param  string  $status  string
     * @param  string  $primarySportKey  string | null
     * @param  string  $createdBy  ULID (usr_*) | null
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $organizationId,
        public readonly string $branchId,
        public readonly string $seasonId,
        public readonly string $name,
        public readonly string $slug,
        public readonly string $eventType,
        public readonly string $startsAt,
        public readonly string $endsAt,
        public readonly string $visibility,
        public readonly string $status,
        public readonly string $primarySportKey,
        public readonly string $createdBy,
    ) {
    }
}
