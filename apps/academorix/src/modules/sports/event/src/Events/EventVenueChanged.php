<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Stackra\Event\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Event.branch_id OR primary facility changed post-registration_open. AUDIT-WARN — participants planned around the venue.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchEventVenueChangedNotification (cannot-opt-out to registered)
 *
 * @category Event
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EventVenueChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $before  {'branch_id': 'ULID | null', 'primary_facility_id': 'ULID | null'}
     * @param  string  $after  {'branch_id': 'ULID | null', 'primary_facility_id': 'ULID | null'}
     * @param  string  $changedBy  ULID
     * @param  int  $affectedRegistrationCount  integer
     * @param  string  $auditSeverity  warn
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $before,
        public readonly string $after,
        public readonly string $changedBy,
        public readonly int $affectedRegistrationCount,
        public readonly string $auditSeverity,
    ) {
    }
}
