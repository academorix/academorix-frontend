<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Stackra\Event\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * An event_facilities row soft-deleted. Fires per detachment. Refused via observer when the event is in_progress + the row is primary + no other facility can replace it.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Event
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EventFacilityRemoved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $eventFacilityId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $facilityId  ULID
     * @param  bool  $wasPrimary  boolean
     * @param  string  $removedBy  ULID
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $eventFacilityId,
        public readonly string $tenantId,
        public readonly string $facilityId,
        public readonly bool $wasPrimary,
        public readonly string $removedBy,
    ) {
    }
}
