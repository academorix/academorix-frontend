<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Event.max_participants OR max_teams changed. Informational; the enrollment surface enforces the cap.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Event
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EventCapacityChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $before  {'max_participants': 'integer | null', 'max_teams': 'integer | null'}
     * @param  int  $after  {'max_participants': 'integer | null', 'max_teams': 'integer | null'}
     * @param  string  $updatedBy  ULID
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly int $before,
        public readonly int $after,
        public readonly string $updatedBy,
    ) {
    }
}
