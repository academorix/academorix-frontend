<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Stackra\Event\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Archived -> completed OR archived -> cancelled (whichever it was prior to archive). Only within the retention window (default 2555 days = 7 years).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Event
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EventRestored implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $eventId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $restoredAt  ISO 8601
     * @param  string  $restoredBy  ULID
     * @param  int  $daysArchived  integer
     */
    public function __construct(
        public readonly string $eventId,
        public readonly string $tenantId,
        public readonly string $restoredAt,
        public readonly string $restoredBy,
        public readonly int $daysArchived,
    ) {
    }
}
