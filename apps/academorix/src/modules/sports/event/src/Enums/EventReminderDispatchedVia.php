<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Stackra\Event\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `dispatched_via` on `event-reminder`.
 *
 * email / sms / push / in_app / webhook.
 *
 * @category Event
 *
 * @since    0.1.0
 */
enum EventReminderDispatchedVia: string
{
    use Enum;

    case Email = 'email';
    case Sms = 'sms';
    case Push = 'push';
    case InApp = 'in_app';
    case Webhook = 'webhook';
}
