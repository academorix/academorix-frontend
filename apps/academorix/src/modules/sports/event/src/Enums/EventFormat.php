<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `format` on `event`.
 *
 * Enum: single_elimination / double_elimination / round_robin / group_stage / seeded_bracket / friendly / clinic / lecture / social_only.
 *
 * @category Event
 *
 * @since    0.1.0
 */
enum EventFormat: string
{
    use Enum;

    case SingleElimination = 'single_elimination';
    case DoubleElimination = 'double_elimination';
    case RoundRobin = 'round_robin';
    case GroupStage = 'group_stage';
    case SeededBracket = 'seeded_bracket';
    case Friendly = 'friendly';
    case Clinic = 'clinic';
    case Lecture = 'lecture';
    case SocialOnly = 'social_only';
}
