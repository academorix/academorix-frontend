<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Stackra\Event\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `status` on `event`.
 *
 * Enum: draft / announced / registration_open / registration_closed / in_progress / completed / cancelled / archived.
 *
 * @category Event
 *
 * @since    0.1.0
 */
enum EventStatus: string
{
    use Enum;

    case Draft = 'draft';
    case Announced = 'announced';
    case RegistrationOpen = 'registration_open';
    case RegistrationClosed = 'registration_closed';
    case InProgress = 'in_progress';
    case Completed = 'completed';
    case Cancelled = 'cancelled';
    case Archived = 'archived';
}
