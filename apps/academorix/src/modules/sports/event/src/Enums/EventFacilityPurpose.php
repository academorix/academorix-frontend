<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `purpose` on `event-facility`.
 *
 * Enum: competition / warm_up / registration / spectator / vip.
 *
 * @category Event
 *
 * @since    0.1.0
 */
enum EventFacilityPurpose: string
{
    use Enum;

    case Competition = 'competition';
    case WarmUp = 'warm_up';
    case Registration = 'registration';
    case Spectator = 'spectator';
    case Vip = 'vip';
}
