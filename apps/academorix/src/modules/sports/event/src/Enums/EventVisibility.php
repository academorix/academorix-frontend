<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `visibility` on `event`.
 *
 * Enum: public / tenant_only / participants_only / private.
 *
 * @category Event
 *
 * @since    0.1.0
 */
enum EventVisibility: string
{
    use Enum;

    case Public = 'public';
    case TenantOnly = 'tenant_only';
    case ParticipantsOnly = 'participants_only';
    case Private = 'private';
}
