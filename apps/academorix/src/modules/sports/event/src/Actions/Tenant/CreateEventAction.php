<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports event --force`.

declare(strict_types=1);

namespace Academorix\Event\Actions\Tenant;

use Academorix\Event\Contracts\Repositories\EventRepositoryInterface;
use Academorix\Event\Data\EventData;
use Academorix\Event\Data\Requests\CreateEventRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/events` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Event
 *
 * @since    0.1.0
 */
#[AsAction(name: 'event.events.create')]
#[Post('/api/v1/events')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateEventAction
{
    use AsController;

    public function __construct(
        private readonly EventRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `event` from the validated request payload.
     *
     * @param  CreateEventRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateEventRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(EventData::from($model), JsonResponse::HTTP_CREATED);
    }
}
