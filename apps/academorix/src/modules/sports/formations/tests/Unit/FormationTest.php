<?php

declare(strict_types=1);

use Stackra\Formations\Models\Formation;

it('is a placeholder — replace with actual Formation unit tests', function (): void {
    expect(Formation::class)->toBeString();
});
