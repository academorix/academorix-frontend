<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports formations --force`.

declare(strict_types=1);

namespace Stackra\Formations\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan formations:list` — List every formations row.
 *
 * @category Formations
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'formations:list',
    description: 'List every formations row.',
)]
final class ListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List every formations row', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List every formations row completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
