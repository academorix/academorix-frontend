<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports formations --force`.

declare(strict_types=1);

namespace Academorix\Formations\Actions\Tenant;

use Academorix\Formations\Contracts\Repositories\FormationRepositoryInterface;
use Academorix\Formations\Data\FormationData;
use Academorix\Formations\Data\Requests\UpdateFormationRequestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/formations/{formation}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Formations
 *
 * @since    0.1.0
 */
#[AsAction(name: 'formations.update')]
#[Patch('/api/v1/formations/{formation}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateFormationAction
{
    use AsController;

    public function __construct(
        private readonly FormationRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `formation` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateFormationRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateFormationRequestData $data): FormationData
    {
        $model = $this->repository->update($id, $data->toArray());

        return FormationData::from($model);
    }
}
