<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports formations --force`.

declare(strict_types=1);

namespace Academorix\Formations\Actions\Tenant;

use Academorix\Formations\Contracts\Repositories\FormationRepositoryInterface;
use Academorix\Formations\Data\FormationData;
use Academorix\Formations\Data\Requests\CreateFormationRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/formations` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Formations
 *
 * @since    0.1.0
 */
#[AsAction(name: 'formations.create')]
#[Post('/api/v1/formations')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateFormationAction
{
    use AsController;

    public function __construct(
        private readonly FormationRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `formation` from the validated request payload.
     *
     * @param  CreateFormationRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateFormationRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(FormationData::from($model), JsonResponse::HTTP_CREATED);
    }
}
