<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports formations --force`.

declare(strict_types=1);

namespace Academorix\Formations\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Formations module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Formations
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum FormationsPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `formations.viewAny` — Formations ViewAny
     */
    #[Label('Formations ViewAny')]
    #[Description('Formations ViewAny')]
    case FormationsViewAny = 'formations.viewAny';

    /**
     * `formations.view` — Formations View
     */
    #[Label('Formations View')]
    #[Description('Formations View')]
    case FormationsView = 'formations.view';

    /**
     * `formations.create` — Formations Create
     */
    #[Label('Formations Create')]
    #[Description('Formations Create')]
    case FormationsCreate = 'formations.create';

    /**
     * `formations.update` — Formations Update
     */
    #[Label('Formations Update')]
    #[Description('Formations Update')]
    case FormationsUpdate = 'formations.update';

    /**
     * `formations.delete` — Formations Delete
     */
    #[Label('Formations Delete')]
    #[Description('Formations Delete')]
    case FormationsDelete = 'formations.delete';

    /**
     * `formations.set-pieces.viewAny` — Formations Set-pieces ViewAny
     */
    #[Label('Formations Set-pieces ViewAny')]
    #[Description('Formations Set-pieces ViewAny')]
    case FormationsSetPiecesViewAny = 'formations.set-pieces.viewAny';

    /**
     * `formations.set-pieces.view` — Formations Set-pieces View
     */
    #[Label('Formations Set-pieces View')]
    #[Description('Formations Set-pieces View')]
    case FormationsSetPiecesView = 'formations.set-pieces.view';

    /**
     * `formations.set-pieces.create` — Formations Set-pieces Create
     */
    #[Label('Formations Set-pieces Create')]
    #[Description('Formations Set-pieces Create')]
    case FormationsSetPiecesCreate = 'formations.set-pieces.create';

    /**
     * `formations.set-pieces.update` — Formations Set-pieces Update
     */
    #[Label('Formations Set-pieces Update')]
    #[Description('Formations Set-pieces Update')]
    case FormationsSetPiecesUpdate = 'formations.set-pieces.update';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::FormationsViewAny => Guard::Sanctum,
            self::FormationsView => Guard::Sanctum,
            self::FormationsCreate => Guard::Sanctum,
            self::FormationsUpdate => Guard::Sanctum,
            self::FormationsDelete => Guard::Sanctum,
            self::FormationsSetPiecesViewAny => Guard::Sanctum,
            self::FormationsSetPiecesView => Guard::Sanctum,
            self::FormationsSetPiecesCreate => Guard::Sanctum,
            self::FormationsSetPiecesUpdate => Guard::Sanctum,
        };
    }
}
