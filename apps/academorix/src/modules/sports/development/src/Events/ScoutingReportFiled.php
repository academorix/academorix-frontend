<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports development --force`.

declare(strict_types=1);

namespace Stackra\Development\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Scout filed a scouting report on an athlete (may be from another tenant). Compliance audit chain.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention 7yr)
 * - notifications::NotifyScoutingReport
 *
 * @category Development
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ScoutingReportFiled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $reportId  ULID (dsc_*)
     * @param  string  $scoutedAthleteId  ULID
     * @param  string  $scoutUserId  ULID
     * @param  int  $rating  int
     * @param  string  $recommendation  'sign'|'monitor'|'reject'|'refer'
     */
    public function __construct(
        public readonly string $reportId,
        public readonly string $scoutedAthleteId,
        public readonly string $scoutUserId,
        public readonly int $rating,
        public readonly string $recommendation,
    ) {
    }
}
