<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports development --force`.

declare(strict_types=1);

namespace Academorix\Development\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New AthleteGoal row (SMART goal for an athlete).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::NotifyGoalSet
 *
 * @category Development
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class GoalSet implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $goalId  ULID (dgl_*)
     * @param  string  $athleteId  ULID
     * @param  string  $coachUserId  ULID
     * @param  string  $targetDate  date
     * @param  string  $goalType  'skill'|'performance'|'attendance'|'other'
     */
    public function __construct(
        public readonly string $goalId,
        public readonly string $athleteId,
        public readonly string $coachUserId,
        public readonly string $targetDate,
        public readonly string $goalType,
    ) {
    }
}
