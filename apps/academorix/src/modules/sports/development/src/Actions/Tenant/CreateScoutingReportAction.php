<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports development --force`.

declare(strict_types=1);

namespace Academorix\Development\Actions\Tenant;

use Academorix\Development\Contracts\Repositories\ScoutingReportRepositoryInterface;
use Academorix\Development\Data\ScoutingReportData;
use Academorix\Development\Data\Requests\CreateScoutingReportRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/scouting-reports` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Development
 *
 * @since    0.1.0
 */
#[AsAction(name: 'development.scouting_reports.create')]
#[Post('/api/v1/scouting-reports')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateScoutingReportAction
{
    use AsController;

    public function __construct(
        private readonly ScoutingReportRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `scouting-report` from the validated request payload.
     *
     * @param  CreateScoutingReportRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateScoutingReportRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(ScoutingReportData::from($model), JsonResponse::HTTP_CREATED);
    }
}
