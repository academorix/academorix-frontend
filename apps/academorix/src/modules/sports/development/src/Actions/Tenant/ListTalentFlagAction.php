<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports development --force`.

declare(strict_types=1);

namespace Academorix\Development\Actions\Tenant;

use Academorix\Development\Contracts\Repositories\TalentFlagRepositoryInterface;
use Academorix\Development\Data\TalentFlagData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Get;
use Illuminate\Http\Request;
use Illuminate\Pagination\LengthAwarePaginator;
use Spatie\LaravelData\PaginatedDataCollection;

/**
 * `GET /api/v1/talent-flags` — list action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Development
 *
 * @since    0.1.0
 */
#[AsAction(name: 'development.talent_flags.list')]
#[Get('/api/v1/talent-flags')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ListTalentFlagAction
{
    use AsController;

    public function __construct(
        private readonly TalentFlagRepositoryInterface $repository,
    ) {
    }

    /**
     * List `talent-flags` for the current caller.
     *
     * The `Repository` base picks up `filter[...]` / `sort=...` /
     * `include=...` query parameters via the `#[Filterable]` attribute
     * on the concrete repository — no per-Action wiring needed.
     *
     * @return PaginatedDataCollection<int, TalentFlagData>  Paginated wire-visible DTOs.
     */
    public function __invoke(Request $request): PaginatedDataCollection
    {
        /** @var LengthAwarePaginator<int, \Academorix\Development\Models\TalentFlag> $page */
        $page = $this->repository->paginate(
            perPage: (int) $request->integer('per_page', 15),
        );

        return TalentFlagData::collect($page, PaginatedDataCollection::class);
    }
}
