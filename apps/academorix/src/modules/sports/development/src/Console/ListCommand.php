<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports development --force`.

declare(strict_types=1);

namespace Academorix\Development\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan development:list` — List every development row.
 *
 * @category Development
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'development:list',
    description: 'List every development row.',
)]
final class ListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List every development row', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List every development row completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
