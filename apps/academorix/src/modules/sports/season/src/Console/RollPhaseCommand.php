<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan season:roll-phase` — Roll phase — season command.
 *
 * @category Season
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'season:roll-phase',
    description: 'Roll phase — season command.',
)]
final class RollPhaseCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Roll phase — season command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Roll phase — season command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
