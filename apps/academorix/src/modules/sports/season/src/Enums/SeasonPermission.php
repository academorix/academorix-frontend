<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Season module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Season
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum SeasonPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `seasons.viewAny` — Every tenant role reads the season list — seasons are UI reference data (every enrollment surface + team dropdown reads them).
     */
    #[Label('Every tenant role reads the season list — seasons are UI reference data (every e')]
    #[Description('Every tenant role reads the season list — seasons are UI reference data (every enrollment surface + team dropdown reads them).')]
    case SeasonsViewAny = 'seasons.viewAny';

    /**
     * `seasons.viewAny.tenant` — Seasons ViewAny Tenant
     */
    #[Label('Seasons ViewAny Tenant')]
    #[Description('Seasons ViewAny Tenant')]
    case SeasonsViewAnyTenant = 'seasons.viewAny.tenant';

    /**
     * `seasons.view` — Single-row read.
     */
    #[Label('Single-row read')]
    #[Description('Single-row read.')]
    case SeasonsView = 'seasons.view';

    /**
     * `seasons.create` — Consumes 1 unit of the season_slot entitlement per overlapping calendar year.
     */
    #[Label('Consumes 1 unit of the season_slot entitlement per overlapping calendar year')]
    #[Description('Consumes 1 unit of the season_slot entitlement per overlapping calendar year.')]
    case SeasonsCreate = 'seasons.create';

    /**
     * `seasons.update` — Mutable fields: name, slug, description, season_type, registration timestamps, competition_starts_at, has_playoffs, playoff timestamps, sort_order, primary_color, capacity_target, max_enrollments_per_...
     */
    #[Label('Mutable fields: name, slug, description, season_type, registration timestamps, c')]
    #[Description('Mutable fields: name, slug, description, season_type, registration timestamps, competition_starts_at, has_playoffs, playoff timestamps, sort_order, primary_color, capacity_target, max_enrollments_per_athlete, allows_late_registration, late_registration_fee_cents. sport_key mutable only pre-in_progress. start_date/end_date mutable in-progress only with `seasons.dates.shift`.')]
    case SeasonsUpdate = 'seasons.update';

    /**
     * `seasons.dates.shift` — Owner-only.
     */
    #[Label('Owner-only')]
    #[Description('Owner-only. Permits start_date / end_date / playoff timestamps changes on Seasons with status IN (in_progress, playoffs). Fires SeasonDatesShifted (audit-warn) + a cannot-opt-out notification to enrolled athletes + coaches.')]
    case SeasonsDatesShift = 'seasons.dates.shift';

    /**
     * `seasons.delete` — Owner-only.
     */
    #[Label('Owner-only')]
    #[Description('Owner-only. Soft-delete (archive path). Hard-delete via retention purge only.')]
    case SeasonsDelete = 'seasons.delete';

    /**
     * `seasons.set_current` — Owner-only.
     */
    #[Label('Owner-only')]
    #[Description('Owner-only. Changing the current season affects year-over-year reporting queries + the default scope for downstream enrollment surfaces — narrow blast radius.')]
    case SeasonsSetCurrent = 'seasons.set_current';

    /**
     * `seasons.open_registration` — Seasons Open Registration
     */
    #[Label('Seasons Open Registration')]
    #[Description('Seasons Open Registration')]
    case SeasonsOpenRegistration = 'seasons.open_registration';

    /**
     * `seasons.registration.open-early` — Owner-only.
     */
    #[Label('Owner-only')]
    #[Description('Owner-only. Permits opening registration before `registration_opens_at`. Bypasses the scheduled-open guardrail.')]
    case SeasonsRegistrationOpenEarly = 'seasons.registration.open-early';

    /**
     * `seasons.close_registration` — Seasons Close Registration
     */
    #[Label('Seasons Close Registration')]
    #[Description('Seasons Close Registration')]
    case SeasonsCloseRegistration = 'seasons.close_registration';

    /**
     * `seasons.start` — Seasons Start
     */
    #[Label('Seasons Start')]
    #[Description('Seasons Start')]
    case SeasonsStart = 'seasons.start';

    /**
     * `seasons.start.early` — Owner-only.
     */
    #[Label('Owner-only')]
    #[Description('Owner-only. Permits starting a season before `start_date`. Bypasses the scheduled-start guardrail.')]
    case SeasonsStartEarly = 'seasons.start.early';

    /**
     * `seasons.begin_playoffs` — Seasons Begin Playoffs
     */
    #[Label('Seasons Begin Playoffs')]
    #[Description('Seasons Begin Playoffs')]
    case SeasonsBeginPlayoffs = 'seasons.begin_playoffs';

    /**
     * `seasons.playoffs.begin-early` — Owner-only.
     */
    #[Label('Owner-only')]
    #[Description('Owner-only. Permits beginning playoffs before `playoff_starts_at`.')]
    case SeasonsPlayoffsBeginEarly = 'seasons.playoffs.begin-early';

    /**
     * `seasons.complete` — Seasons Complete
     */
    #[Label('Seasons Complete')]
    #[Description('Seasons Complete')]
    case SeasonsComplete = 'seasons.complete';

    /**
     * `seasons.archive` — Seasons Archive
     */
    #[Label('Seasons Archive')]
    #[Description('Seasons Archive')]
    case SeasonsArchive = 'seasons.archive';

    /**
     * `seasons.restore` — Restore an archived Season within the retention window.
     */
    #[Label('Restore an archived Season within the retention window')]
    #[Description('Restore an archived Season within the retention window.')]
    case SeasonsRestore = 'seasons.restore';

    /**
     * `seasons.manage` — Umbrella permission for the settings blueprint gate.
     */
    #[Label('Umbrella permission for the settings blueprint gate')]
    #[Description('Umbrella permission for the settings blueprint gate. Admin + owner.')]
    case SeasonsManage = 'seasons.manage';

    /**
     * `seasons.enroll.late` — Permission for the downstream enrollment surface (Wave 3) to accept enrollments in the late-registration window.
     */
    #[Label('Permission for the downstream enrollment surface (Wave 3) to accept enrollments')]
    #[Description('Permission for the downstream enrollment surface (Wave 3) to accept enrollments in the late-registration window. NOT seeded on the enrollment_creator role by default — admins explicitly grant it to their reception staff who handle late-signups + surcharge collection.')]
    case SeasonsEnrollLate = 'seasons.enroll.late';

    /**
     * `platform.seasons.viewAny` — Platform Seasons ViewAny
     */
    #[Label('Platform Seasons ViewAny')]
    #[Description('Platform Seasons ViewAny')]
    case PlatformSeasonsViewAny = 'platform.seasons.viewAny';

    /**
     * `platform.seasons.view` — Platform Seasons View
     */
    #[Label('Platform Seasons View')]
    #[Description('Platform Seasons View')]
    case PlatformSeasonsView = 'platform.seasons.view';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::SeasonsViewAny => Guard::Sanctum,
            self::SeasonsViewAnyTenant => Guard::Sanctum,
            self::SeasonsView => Guard::Sanctum,
            self::SeasonsCreate => Guard::Sanctum,
            self::SeasonsUpdate => Guard::Sanctum,
            self::SeasonsDatesShift => Guard::Sanctum,
            self::SeasonsDelete => Guard::Sanctum,
            self::SeasonsSetCurrent => Guard::Sanctum,
            self::SeasonsOpenRegistration => Guard::Sanctum,
            self::SeasonsRegistrationOpenEarly => Guard::Sanctum,
            self::SeasonsCloseRegistration => Guard::Sanctum,
            self::SeasonsStart => Guard::Sanctum,
            self::SeasonsStartEarly => Guard::Sanctum,
            self::SeasonsBeginPlayoffs => Guard::Sanctum,
            self::SeasonsPlayoffsBeginEarly => Guard::Sanctum,
            self::SeasonsComplete => Guard::Sanctum,
            self::SeasonsArchive => Guard::Sanctum,
            self::SeasonsRestore => Guard::Sanctum,
            self::SeasonsManage => Guard::Sanctum,
            self::SeasonsEnrollLate => Guard::Sanctum,
            self::PlatformSeasonsViewAny => Guard::PlatformAdmin,
            self::PlatformSeasonsView => Guard::PlatformAdmin,
        };
    }
}
