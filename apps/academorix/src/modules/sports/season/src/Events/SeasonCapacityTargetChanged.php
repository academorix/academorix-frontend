<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Season.capacity_target mutated (set / changed / cleared). Feeds capacity-planning dashboards. Not enforced anywhere — informational only.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Season
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SeasonCapacityTargetChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $fromCapacityTarget  integer | null
     * @param  int  $toCapacityTarget  integer | null
     * @param  string  $updatedBy  ULID
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly int $fromCapacityTarget,
        public readonly int $toCapacityTarget,
        public readonly string $updatedBy,
    ) {
    }
}
