<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SeasonProvisioner finished a successful create. Fires exactly once per `seasons.id`.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::MaybeDispatchSeasonRegistrationOpeningNotification (scheduled when registration_opens_at is set + within the reminder window)
 *
 * @category Season
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SeasonCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID (sea_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $organizationId  ULID (org_*) | null
     * @param  string  $branchId  ULID (brn_*) | null
     * @param  string  $name  string
     * @param  string  $slug  string
     * @param  string  $sportKey  string | null
     * @param  int  $seasonType  string (annual / academic_year / summer_camp / winter_camp / half_year / quarter / trimester / custom)
     * @param  string  $startDate  ISO 8601 date
     * @param  string  $endDate  ISO 8601 date
     * @param  bool  $isCurrent  boolean
     * @param  string  $source  'admin_created' | 'tenant_provisioning' | 'legacy_backfill_command' | 'clone_from_prior'
     * @param  string  $createdBy  ULID (usr_*) | null
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly string $organizationId,
        public readonly string $branchId,
        public readonly string $name,
        public readonly string $slug,
        public readonly string $sportKey,
        public readonly int $seasonType,
        public readonly string $startDate,
        public readonly string $endDate,
        public readonly bool $isCurrent,
        public readonly string $source,
        public readonly string $createdBy,
    ) {
    }
}
