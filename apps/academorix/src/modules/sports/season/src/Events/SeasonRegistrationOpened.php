<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Stackra\Season\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Season.status transitioned planned -> registration_open. Fires from automated RollSeasonPhaseJob (when now() reaches registration_opens_at) OR manual admin open action. Fires alongside SeasonStatusChanged.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchSeasonRegistrationOpeningNotification (only from manual open — automated open already dispatched T-24h and T-1h reminders)
 *
 * @category Season
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SeasonRegistrationOpened implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $openedAt  ISO 8601
     * @param  string  $registrationClosesAt  ISO 8601 | null
     * @param  string  $trigger  'automated_phase_roll' | 'manual_admin'
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly string $openedAt,
        public readonly string $registrationClosesAt,
        public readonly string $trigger,
    ) {
    }
}
