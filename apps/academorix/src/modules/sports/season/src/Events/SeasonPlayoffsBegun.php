<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports season --force`.

declare(strict_types=1);

namespace Academorix\Season\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Season.status transitioned in_progress -> playoffs. Fires from automated RollSeasonPhaseJob (when now() reaches playoff_starts_at) OR manual admin action. Only fires when Season.has_playoffs=true (guarded by observer).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Season
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SeasonPlayoffsBegun implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $seasonId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $beganAt  ISO 8601
     * @param  string  $playoffEndsAt  ISO 8601 | null
     * @param  string  $trigger  'automated_phase_roll' | 'manual_admin'
     */
    public function __construct(
        public readonly string $seasonId,
        public readonly string $tenantId,
        public readonly string $beganAt,
        public readonly string $playoffEndsAt,
        public readonly string $trigger,
    ) {
    }
}
