<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports drills --force`.

declare(strict_types=1);

namespace Academorix\Drills\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Drills module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Drills
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum DrillsPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `drills.viewAny` — Drills ViewAny
     */
    #[Label('Drills ViewAny')]
    #[Description('Drills ViewAny')]
    case DrillsViewAny = 'drills.viewAny';

    /**
     * `drills.view` — Drills View
     */
    #[Label('Drills View')]
    #[Description('Drills View')]
    case DrillsView = 'drills.view';

    /**
     * `drills.create` — Drills Create
     */
    #[Label('Drills Create')]
    #[Description('Drills Create')]
    case DrillsCreate = 'drills.create';

    /**
     * `drills.update` — Drills Update
     */
    #[Label('Drills Update')]
    #[Description('Drills Update')]
    case DrillsUpdate = 'drills.update';

    /**
     * `drills.publish` — Drills Publish
     */
    #[Label('Drills Publish')]
    #[Description('Drills Publish')]
    case DrillsPublish = 'drills.publish';

    /**
     * `drills.publish-public` — Publish to public (cross-tenant) drill marketplace.
     */
    #[Label('Publish to public (cross-tenant) drill marketplace')]
    #[Description('Publish to public (cross-tenant) drill marketplace. Requires moderation.')]
    case DrillsPublishPublic = 'drills.publish-public';

    /**
     * `drills.archive` — Drills Archive
     */
    #[Label('Drills Archive')]
    #[Description('Drills Archive')]
    case DrillsArchive = 'drills.archive';

    /**
     * `drills.rate` — Drills Rate
     */
    #[Label('Drills Rate')]
    #[Description('Drills Rate')]
    case DrillsRate = 'drills.rate';

    /**
     * `drills.import` — Bulk import from CSV / partner libraries.
     */
    #[Label('Bulk import from CSV / partner libraries')]
    #[Description('Bulk import from CSV / partner libraries.')]
    case DrillsImport = 'drills.import';

    /**
     * `platform.drills.moderate` — Moderate the public drill marketplace.
     */
    #[Label('Moderate the public drill marketplace')]
    #[Description('Moderate the public drill marketplace.')]
    case PlatformDrillsModerate = 'platform.drills.moderate';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::DrillsViewAny => Guard::Sanctum,
            self::DrillsView => Guard::Sanctum,
            self::DrillsCreate => Guard::Sanctum,
            self::DrillsUpdate => Guard::Sanctum,
            self::DrillsPublish => Guard::Sanctum,
            self::DrillsPublishPublic => Guard::Sanctum,
            self::DrillsArchive => Guard::Sanctum,
            self::DrillsRate => Guard::Sanctum,
            self::DrillsImport => Guard::Sanctum,
            self::PlatformDrillsModerate => Guard::PlatformAdmin,
        };
    }
}
