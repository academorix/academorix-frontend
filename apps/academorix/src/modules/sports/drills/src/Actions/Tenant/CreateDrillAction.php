<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports drills --force`.

declare(strict_types=1);

namespace Stackra\Drills\Actions\Tenant;

use Stackra\Drills\Contracts\Repositories\DrillRepositoryInterface;
use Stackra\Drills\Data\DrillData;
use Stackra\Drills\Data\Requests\CreateDrillRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/drills` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Drills
 *
 * @since    0.1.0
 */
#[AsAction(name: 'drills.create')]
#[Post('/api/v1/drills')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateDrillAction
{
    use AsController;

    public function __construct(
        private readonly DrillRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `drill` from the validated request payload.
     *
     * @param  CreateDrillRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateDrillRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(DrillData::from($model), JsonResponse::HTTP_CREATED);
    }
}
