<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Coaching\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the Coaching module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum CoachingPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `coaching_profiles.viewAny` — Coaching Profiles ViewAny
     */
    #[Label('Coaching Profiles ViewAny')]
    #[Description('Coaching Profiles ViewAny')]
    case CoachingProfilesViewAny = 'coaching_profiles.viewAny';

    /**
     * `coaching_profiles.viewAny.own` — Coach-scoped listing — narrows to the profile linked to the caller's Staff row.
     */
    #[Label('Coach-scoped listing — narrows to the profile linked to the caller\'s Staff row')]
    #[Description('Coach-scoped listing — narrows to the profile linked to the caller\'s Staff row.')]
    case CoachingProfilesViewAnyOwn = 'coaching_profiles.viewAny.own';

    /**
     * `coaching_profiles.view` — Coaching Profiles View
     */
    #[Label('Coaching Profiles View')]
    #[Description('Coaching Profiles View')]
    case CoachingProfilesView = 'coaching_profiles.view';

    /**
     * `coaching_profiles.view.own` — Self-view for the coach on their own profile.
     */
    #[Label('Self-view for the coach on their own profile')]
    #[Description('Self-view for the coach on their own profile.')]
    case CoachingProfilesViewOwn = 'coaching_profiles.view.own';

    /**
     * `coaching_profiles.view.rate` — View hourly_rate_cents + rate_currency.
     */
    #[Label('View hourly_rate_cents + rate_currency')]
    #[Description('View hourly_rate_cents + rate_currency. Distinct permission — regulated_financial-tier.')]
    case CoachingProfilesViewRate = 'coaching_profiles.view.rate';

    /**
     * `coaching_profiles.view.pii` — View bio + photo_url_signed at full resolution.
     */
    #[Label('View bio + photo_url_signed at full resolution')]
    #[Description('View bio + photo_url_signed at full resolution. Coaches see their own; admins see all.')]
    case CoachingProfilesViewPii = 'coaching_profiles.view.pii';

    /**
     * `coaching_profiles.create` — Elevate a Staff row to Coach.
     */
    #[Label('Elevate a Staff row to Coach')]
    #[Description('Elevate a Staff row to Coach.')]
    case CoachingProfilesCreate = 'coaching_profiles.create';

    /**
     * `coaching_profiles.update` — Update profile fields (primary_sport, specializations, availability).
     */
    #[Label('Update profile fields (primary_sport, specializations, availability)')]
    #[Description('Update profile fields (primary_sport, specializations, availability). Excludes hourly_rate (dedicated permission) + verified_by_admin_at (dedicated /verify route).')]
    case CoachingProfilesUpdate = 'coaching_profiles.update';

    /**
     * `coaching_profiles.update.own` — Coach updates their own bio, availability, is_accepting_new_athletes, photo_url_signed.
     */
    #[Label('Coach updates their own bio, availability, is_accepting_new_athletes, photo_url_')]
    #[Description('Coach updates their own bio, availability, is_accepting_new_athletes, photo_url_signed. Rate + verification remain admin-only.')]
    case CoachingProfilesUpdateOwn = 'coaching_profiles.update.own';

    /**
     * `coaching_profiles.update.rate` — Update hourly_rate_cents + rate_currency.
     */
    #[Label('Update hourly_rate_cents + rate_currency')]
    #[Description('Update hourly_rate_cents + rate_currency. Regulated_financial-tier permission.')]
    case CoachingProfilesUpdateRate = 'coaching_profiles.update.rate';

    /**
     * `coaching_profiles.delete` — Soft-delete the coaching profile.
     */
    #[Label('Soft-delete the coaching profile')]
    #[Description('Soft-delete the coaching profile. Coach remains as Staff.')]
    case CoachingProfilesDelete = 'coaching_profiles.delete';

    /**
     * `coaching_profiles.verify` — Admin verifies a coaching profile.
     */
    #[Label('Admin verifies a coaching profile')]
    #[Description('Admin verifies a coaching profile. Sets verified_by_admin_at.')]
    case CoachingProfilesVerify = 'coaching_profiles.verify';

    /**
     * `coaching_profiles.deactivate` — Flip is_active=false.
     */
    #[Label('Flip is_active=false')]
    #[Description('Flip is_active=false. Cascades to open assignments.')]
    case CoachingProfilesDeactivate = 'coaching_profiles.deactivate';

    /**
     * `coaching_profiles.reactivate` — Flip is_active=true (re-verification required).
     */
    #[Label('Flip is_active=true (re-verification required)')]
    #[Description('Flip is_active=true (re-verification required).')]
    case CoachingProfilesReactivate = 'coaching_profiles.reactivate';

    /**
     * `coaching_profiles.export.own` — Coach export their own record (CCPA compliance).
     */
    #[Label('Coach export their own record (CCPA compliance)')]
    #[Description('Coach export their own record (CCPA compliance).')]
    case CoachingProfilesExportOwn = 'coaching_profiles.export.own';

    /**
     * `coach_assignments.viewAny` — Coach Assignments ViewAny
     */
    #[Label('Coach Assignments ViewAny')]
    #[Description('Coach Assignments ViewAny')]
    case CoachAssignmentsViewAny = 'coach_assignments.viewAny';

    /**
     * `coach_assignments.viewAny.own` — Coach-scoped listing — narrows to assignments on the caller's coaching_profile.
     */
    #[Label('Coach-scoped listing — narrows to assignments on the caller\'s coaching_profile')]
    #[Description('Coach-scoped listing — narrows to assignments on the caller\'s coaching_profile.')]
    case CoachAssignmentsViewAnyOwn = 'coach_assignments.viewAny.own';

    /**
     * `coach_assignments.view` — Coach Assignments View
     */
    #[Label('Coach Assignments View')]
    #[Description('Coach Assignments View')]
    case CoachAssignmentsView = 'coach_assignments.view';

    /**
     * `coach_assignments.view.own` — Coach Assignments View Own
     */
    #[Label('Coach Assignments View Own')]
    #[Description('Coach Assignments View Own')]
    case CoachAssignmentsViewOwn = 'coach_assignments.view.own';

    /**
     * `coach_assignments.create` — Create a CoachAssignment.
     */
    #[Label('Create a CoachAssignment')]
    #[Description('Create a CoachAssignment.')]
    case CoachAssignmentsCreate = 'coach_assignments.create';

    /**
     * `coach_assignments.update` — Coach Assignments Update
     */
    #[Label('Coach Assignments Update')]
    #[Description('Coach Assignments Update')]
    case CoachAssignmentsUpdate = 'coach_assignments.update';

    /**
     * `coach_assignments.delete` — Cancel an assignment (soft-delete).
     */
    #[Label('Cancel an assignment (soft-delete)')]
    #[Description('Cancel an assignment (soft-delete).')]
    case CoachAssignmentsDelete = 'coach_assignments.delete';

    /**
     * `coach_assignments.end` — Set end_date on an active assignment.
     */
    #[Label('Set end_date on an active assignment')]
    #[Description('Set end_date on an active assignment.')]
    case CoachAssignmentsEnd = 'coach_assignments.end';

    /**
     * `coach_certifications.viewAny` — Coach Certifications ViewAny
     */
    #[Label('Coach Certifications ViewAny')]
    #[Description('Coach Certifications ViewAny')]
    case CoachCertificationsViewAny = 'coach_certifications.viewAny';

    /**
     * `coach_certifications.viewAny.own` — Coach Certifications ViewAny Own
     */
    #[Label('Coach Certifications ViewAny Own')]
    #[Description('Coach Certifications ViewAny Own')]
    case CoachCertificationsViewAnyOwn = 'coach_certifications.viewAny.own';

    /**
     * `coach_certifications.view` — Coach Certifications View
     */
    #[Label('Coach Certifications View')]
    #[Description('Coach Certifications View')]
    case CoachCertificationsView = 'coach_certifications.view';

    /**
     * `coach_certifications.view.own` — Coach Certifications View Own
     */
    #[Label('Coach Certifications View Own')]
    #[Description('Coach Certifications View Own')]
    case CoachCertificationsViewOwn = 'coach_certifications.view.own';

    /**
     * `coach_certifications.create` — Coach Certifications Create
     */
    #[Label('Coach Certifications Create')]
    #[Description('Coach Certifications Create')]
    case CoachCertificationsCreate = 'coach_certifications.create';

    /**
     * `coach_certifications.create.own` — Coach captures their own certifications.
     */
    #[Label('Coach captures their own certifications')]
    #[Description('Coach captures their own certifications.')]
    case CoachCertificationsCreateOwn = 'coach_certifications.create.own';

    /**
     * `coach_certifications.update` — Coach Certifications Update
     */
    #[Label('Coach Certifications Update')]
    #[Description('Coach Certifications Update')]
    case CoachCertificationsUpdate = 'coach_certifications.update';

    /**
     * `coach_certifications.delete` — Coach Certifications Delete
     */
    #[Label('Coach Certifications Delete')]
    #[Description('Coach Certifications Delete')]
    case CoachCertificationsDelete = 'coach_certifications.delete';

    /**
     * `coach_certifications.verify` — Admin verifies a certification.
     */
    #[Label('Admin verifies a certification')]
    #[Description('Admin verifies a certification.')]
    case CoachCertificationsVerify = 'coach_certifications.verify';

    /**
     * `coach_certifications.expiring` — Read expiring-cert compliance surface.
     */
    #[Label('Read expiring-cert compliance surface')]
    #[Description('Read expiring-cert compliance surface.')]
    case CoachCertificationsExpiring = 'coach_certifications.expiring';

    /**
     * `coach_skill_ratings.viewAny` — Coach Skill Ratings ViewAny
     */
    #[Label('Coach Skill Ratings ViewAny')]
    #[Description('Coach Skill Ratings ViewAny')]
    case CoachSkillRatingsViewAny = 'coach_skill_ratings.viewAny';

    /**
     * `coach_skill_ratings.viewAny.own` — Coach sees ratings on themselves.
     */
    #[Label('Coach sees ratings on themselves')]
    #[Description('Coach sees ratings on themselves.')]
    case CoachSkillRatingsViewAnyOwn = 'coach_skill_ratings.viewAny.own';

    /**
     * `coach_skill_ratings.view` — Coach Skill Ratings View
     */
    #[Label('Coach Skill Ratings View')]
    #[Description('Coach Skill Ratings View')]
    case CoachSkillRatingsView = 'coach_skill_ratings.view';

    /**
     * `coach_skill_ratings.view.own` — Coach Skill Ratings View Own
     */
    #[Label('Coach Skill Ratings View Own')]
    #[Description('Coach Skill Ratings View Own')]
    case CoachSkillRatingsViewOwn = 'coach_skill_ratings.view.own';

    /**
     * `coach_skill_ratings.create` — Record a rating on a coach.
     */
    #[Label('Record a rating on a coach')]
    #[Description('Record a rating on a coach. Self-rating refused at the observer.')]
    case CoachSkillRatingsCreate = 'coach_skill_ratings.create';

    /**
     * `coach_skill_ratings.update` — Update an existing rating's value.
     */
    #[Label('Update an existing rating\'s value')]
    #[Description('Update an existing rating\'s value.')]
    case CoachSkillRatingsUpdate = 'coach_skill_ratings.update';

    /**
     * `coach_skill_ratings.delete` — Coach Skill Ratings Delete
     */
    #[Label('Coach Skill Ratings Delete')]
    #[Description('Coach Skill Ratings Delete')]
    case CoachSkillRatingsDelete = 'coach_skill_ratings.delete';

    /**
     * `coaches.report` — Generate per-branch roster reports.
     */
    #[Label('Generate per-branch roster reports')]
    #[Description('Generate per-branch roster reports.')]
    case CoachesReport = 'coaches.report';

    /**
     * `platform.coaching_profiles.viewAny` — Platform Coaching Profiles ViewAny
     */
    #[Label('Platform Coaching Profiles ViewAny')]
    #[Description('Platform Coaching Profiles ViewAny')]
    case PlatformCoachingProfilesViewAny = 'platform.coaching_profiles.viewAny';

    /**
     * `platform.coaching_profiles.view` — PII + hourly-rate always redacted on platform reads.
     */
    #[Label('PII + hourly-rate always redacted on platform reads')]
    #[Description('PII + hourly-rate always redacted on platform reads.')]
    case PlatformCoachingProfilesView = 'platform.coaching_profiles.view';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::CoachingProfilesViewAny => Guard::Sanctum,
            self::CoachingProfilesViewAnyOwn => Guard::Sanctum,
            self::CoachingProfilesView => Guard::Sanctum,
            self::CoachingProfilesViewOwn => Guard::Sanctum,
            self::CoachingProfilesViewRate => Guard::Sanctum,
            self::CoachingProfilesViewPii => Guard::Sanctum,
            self::CoachingProfilesCreate => Guard::Sanctum,
            self::CoachingProfilesUpdate => Guard::Sanctum,
            self::CoachingProfilesUpdateOwn => Guard::Sanctum,
            self::CoachingProfilesUpdateRate => Guard::Sanctum,
            self::CoachingProfilesDelete => Guard::Sanctum,
            self::CoachingProfilesVerify => Guard::Sanctum,
            self::CoachingProfilesDeactivate => Guard::Sanctum,
            self::CoachingProfilesReactivate => Guard::Sanctum,
            self::CoachingProfilesExportOwn => Guard::Sanctum,
            self::CoachAssignmentsViewAny => Guard::Sanctum,
            self::CoachAssignmentsViewAnyOwn => Guard::Sanctum,
            self::CoachAssignmentsView => Guard::Sanctum,
            self::CoachAssignmentsViewOwn => Guard::Sanctum,
            self::CoachAssignmentsCreate => Guard::Sanctum,
            self::CoachAssignmentsUpdate => Guard::Sanctum,
            self::CoachAssignmentsDelete => Guard::Sanctum,
            self::CoachAssignmentsEnd => Guard::Sanctum,
            self::CoachCertificationsViewAny => Guard::Sanctum,
            self::CoachCertificationsViewAnyOwn => Guard::Sanctum,
            self::CoachCertificationsView => Guard::Sanctum,
            self::CoachCertificationsViewOwn => Guard::Sanctum,
            self::CoachCertificationsCreate => Guard::Sanctum,
            self::CoachCertificationsCreateOwn => Guard::Sanctum,
            self::CoachCertificationsUpdate => Guard::Sanctum,
            self::CoachCertificationsDelete => Guard::Sanctum,
            self::CoachCertificationsVerify => Guard::Sanctum,
            self::CoachCertificationsExpiring => Guard::Sanctum,
            self::CoachSkillRatingsViewAny => Guard::Sanctum,
            self::CoachSkillRatingsViewAnyOwn => Guard::Sanctum,
            self::CoachSkillRatingsView => Guard::Sanctum,
            self::CoachSkillRatingsViewOwn => Guard::Sanctum,
            self::CoachSkillRatingsCreate => Guard::Sanctum,
            self::CoachSkillRatingsUpdate => Guard::Sanctum,
            self::CoachSkillRatingsDelete => Guard::Sanctum,
            self::CoachesReport => Guard::Sanctum,
            self::PlatformCoachingProfilesViewAny => Guard::PlatformAdmin,
            self::PlatformCoachingProfilesView => Guard::PlatformAdmin,
        };
    }
}
