<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Coaching\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * is_active flipped true after prior deactivation. Requires admin re-verification (verified_by_admin_at is set again).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchCoachingProfileVerifiedNotification (reuse — coach welcome-back email)
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CoachingProfileReactivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $profileId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $staffId  ULID
     * @param  string  $reactivatedByUserId  ULID
     */
    public function __construct(
        public readonly string $profileId,
        public readonly string $tenantId,
        public readonly string $staffId,
        public readonly string $reactivatedByUserId,
    ) {
    }
}
