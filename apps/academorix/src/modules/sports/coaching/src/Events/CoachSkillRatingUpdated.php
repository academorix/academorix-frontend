<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Stackra\Coaching\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Existing rating updated with a new rating_value.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidateCoachRatingsCache
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CoachSkillRatingUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $ratingId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $coachingProfileId  ULID
     * @param  int  $fromValue  integer
     * @param  int  $toValue  integer
     * @param  string  $changedByUserId  ULID
     */
    public function __construct(
        public readonly string $ratingId,
        public readonly string $tenantId,
        public readonly string $coachingProfileId,
        public readonly int $fromValue,
        public readonly int $toValue,
        public readonly string $changedByUserId,
    ) {
    }
}
