<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Coaching\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * NotifyCertificationExpiredJob detected certification with expires_at <= NOW(). Fires ONCE per certification lifecycle.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchCoachCertificationExpiredNotification (to coach + admin — cannot-opt-out; action required)
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CoachCertificationExpired implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $certificationId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $coachingProfileId  ULID
     * @param  string  $issuingBody  string
     * @param  string  $certificationName  string
     * @param  bool  $wasVerified  boolean
     */
    public function __construct(
        public readonly string $certificationId,
        public readonly string $tenantId,
        public readonly string $coachingProfileId,
        public readonly string $issuingBody,
        public readonly string $certificationName,
        public readonly bool $wasVerified,
    ) {
    }
}
