<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Coaching\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * is_active flipped false. May be admin-triggered (via /deactivate) OR cascade-triggered (via StaffOffboarded).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - coaching::CancelActiveAssignmentsOnCoachingProfileDeactivated (priority 20)
 * - notifications::DispatchCoachingProfileDeactivatedNotification (to coach + admin)
 * - cache::InvalidateStaffCoachingProfileCache
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CoachingProfileDeactivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $profileId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $staffId  ULID
     * @param  string  $trigger  'manual' | 'staff_offboarded' | 'staff_suspended'
     * @param  string  $reason  string | null
     * @param  int  $activeAssignmentsAtDeactivate  integer
     * @param  string  $deactivatedByUserId  ULID | null (null on cascade)
     */
    public function __construct(
        public readonly string $profileId,
        public readonly string $tenantId,
        public readonly string $staffId,
        public readonly string $trigger,
        public readonly string $reason,
        public readonly int $activeAssignmentsAtDeactivate,
        public readonly string $deactivatedByUserId,
    ) {
    }
}
