<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Coaching\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan coach-assignment:create` — Create a new coach assignment.
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'coach-assignment:create',
    description: 'Create a new coach assignment.',
)]
final class CreateCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Create a new coach assignment', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Create a new coach assignment completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
