<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Academorix\Coaching\Actions\Tenant;

use Academorix\Coaching\Contracts\Repositories\CoachAssignmentRepositoryInterface;
use Academorix\Coaching\Data\CoachAssignmentData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/coach-assignments/{assignment}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
#[AsAction(name: 'coaching.coach_assignments.show')]
#[Get('/api/v1/coach-assignments/{assignment}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowCoachAssignmentAction
{
    use AsController;

    public function __construct(
        private readonly CoachAssignmentRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `coach-assignment` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): CoachAssignmentData
    {
        return CoachAssignmentData::from($this->repository->findOrFail($id));
    }
}
