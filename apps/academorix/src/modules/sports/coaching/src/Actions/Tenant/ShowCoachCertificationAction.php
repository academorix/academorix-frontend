<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Stackra\Coaching\Actions\Tenant;

use Stackra\Coaching\Contracts\Repositories\CoachCertificationRepositoryInterface;
use Stackra\Coaching\Data\CoachCertificationData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/coach-certifications/{cert}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
#[AsAction(name: 'coaching.coach_certifications.show')]
#[Get('/api/v1/coach-certifications/{cert}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowCoachCertificationAction
{
    use AsController;

    public function __construct(
        private readonly CoachCertificationRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `coach-certification` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): CoachCertificationData
    {
        return CoachCertificationData::from($this->repository->findOrFail($id));
    }
}
