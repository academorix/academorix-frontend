<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports coaching --force`.

declare(strict_types=1);

namespace Stackra\Coaching\Actions\Platform;

use Stackra\Coaching\Contracts\Repositories\CoachingProfileRepositoryInterface;
use Stackra\Coaching\Data\CoachingProfileData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/platform/coaching-profiles/{profile}` — show action (platform-admin audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Coaching
 *
 * @since    0.1.0
 */
#[AsAction(name: 'coaching.coaching_profiles.show')]
#[Get('/api/v1/platform/coaching-profiles/{profile}')]
#[Middleware(['api', 'auth:sanctum', 'platform_admin'])]
final class ShowCoachingProfileAction
{
    use AsController;

    public function __construct(
        private readonly CoachingProfileRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `coaching-profile` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): CoachingProfileData
    {
        return CoachingProfileData::from($this->repository->findOrFail($id));
    }
}
