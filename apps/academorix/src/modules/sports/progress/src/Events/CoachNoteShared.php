<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

namespace Academorix\Progress\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Visibility widened (coach_only → shared_with_parent / shared_with_athlete). Never fires on narrowing (refused by observer).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchCoachNoteSharedNotification
 *
 * @category Progress
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CoachNoteShared implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $noteId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteEnrollmentId  ULID
     * @param  string  $previousVisibility  string
     * @param  string  $newVisibility  string
     * @param  string  $sharedByUserId  ULID
     */
    public function __construct(
        public readonly string $noteId,
        public readonly string $tenantId,
        public readonly string $athleteEnrollmentId,
        public readonly string $previousVisibility,
        public readonly string $newVisibility,
        public readonly string $sharedByUserId,
    ) {
    }
}
