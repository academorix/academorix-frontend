<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

namespace Academorix\Progress\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New CoachNote committed. Immutable — never fires again on the same row (edits create revisions via revision_of_id).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Progress
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CoachNoteCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $noteId  ULID (cno_*)
     * @param  string  $tenantId  ULID
     * @param  string  $athleteEnrollmentId  ULID
     * @param  string  $coachUserId  ULID
     * @param  string  $visibility  'coach_only' | 'shared_with_parent' | 'shared_with_athlete' | 'shared_with_both'
     */
    public function __construct(
        public readonly string $noteId,
        public readonly string $tenantId,
        public readonly string $athleteEnrollmentId,
        public readonly string $coachUserId,
        public readonly string $visibility,
    ) {
    }
}
