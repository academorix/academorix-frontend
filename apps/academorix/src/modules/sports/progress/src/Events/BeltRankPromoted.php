<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

namespace Stackra\Progress\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Belt promotion committed from GradingResult with result='pass'. Load-bearing signal for awards + activity + notifications.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 10 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchBeltPromotedNotification (cannot-opt-out — celebration event)
 * - awards::AutoGrantBeltPromotionAward
 * - growth::analytics::TrackBeltPromotion
 *
 * @category Progress
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class BeltRankPromoted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $gradingResultId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID (ath_*)
     * @param  string  $athleteEnrollmentId  ULID
     * @param  string  $sportKey  string
     * @param  string  $fromBeltId  ULID | null
     * @param  string  $toBeltId  ULID
     * @param  string  $fromBeltCode  string | null
     * @param  string  $toBeltCode  string
     * @param  string  $promotedAt  ISO 8601
     */
    public function __construct(
        public readonly string $gradingResultId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $athleteEnrollmentId,
        public readonly string $sportKey,
        public readonly string $fromBeltId,
        public readonly string $toBeltId,
        public readonly string $fromBeltCode,
        public readonly string $toBeltCode,
        public readonly string $promotedAt,
    ) {
    }
}
