<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

namespace Academorix\Progress\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Card shared via signed URL — sharing action recorded for later expiry / revoke.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Progress
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ProgressCardShared implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $cardId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $shareSignature  string
     * @param  string  $shareExpiresAt  ISO 8601
     * @param  string  $sharedByUserId  ULID
     */
    public function __construct(
        public readonly string $cardId,
        public readonly string $tenantId,
        public readonly string $shareSignature,
        public readonly string $shareExpiresAt,
        public readonly string $sharedByUserId,
    ) {
    }
}
