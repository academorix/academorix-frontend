<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

namespace Academorix\Progress\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * ProgressCard rendered — either automatically after assessment save (per sport config) or on-demand via regenerate endpoint.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - cache::WarmCardRender
 *
 * @category Progress
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ProgressCardGenerated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $cardId  ULID (pcd_*)
     * @param  string  $assessmentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $cardType  'fifa_card' | 'attribute_card' | 'radar' | 'time_trial' | 'apparatus_scores'
     * @param  string  $renderedDocumentId  ULID (fil_*) | null
     */
    public function __construct(
        public readonly string $cardId,
        public readonly string $assessmentId,
        public readonly string $tenantId,
        public readonly string $cardType,
        public readonly string $renderedDocumentId,
    ) {
    }
}
