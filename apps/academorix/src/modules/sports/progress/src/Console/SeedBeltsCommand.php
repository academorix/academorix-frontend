<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

namespace Academorix\Progress\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan progress:seed-belts` — Seed belts — progress command.
 *
 * @category Progress
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'progress:seed-belts',
    description: 'Seed belts — progress command.',
)]
final class SeedBeltsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Seed belts — progress command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Seed belts — progress command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
