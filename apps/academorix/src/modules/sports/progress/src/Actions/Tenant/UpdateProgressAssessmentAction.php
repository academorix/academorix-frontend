<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

namespace Stackra\Progress\Actions\Tenant;

use Stackra\Progress\Contracts\Repositories\ProgressAssessmentRepositoryInterface;
use Stackra\Progress\Data\ProgressAssessmentData;
use Stackra\Progress\Data\Requests\UpdateProgressAssessmentRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/progress-assessments/{assessment}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Progress
 *
 * @since    0.1.0
 */
#[AsAction(name: 'progress.progress_assessments.update')]
#[Patch('/api/v1/progress-assessments/{assessment}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateProgressAssessmentAction
{
    use AsController;

    public function __construct(
        private readonly ProgressAssessmentRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `progress-assessment` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateProgressAssessmentRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateProgressAssessmentRequestData $data): ProgressAssessmentData
    {
        $model = $this->repository->update($id, $data->toArray());

        return ProgressAssessmentData::from($model);
    }
}
