<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

namespace Academorix\Progress\Actions\Tenant;

use Academorix\Progress\Contracts\Repositories\CoachNoteRepositoryInterface;
use Academorix\Progress\Data\CoachNoteData;
use Academorix\Progress\Data\Requests\UpdateCoachNoteRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/coach-notes/{note}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Progress
 *
 * @since    0.1.0
 */
#[AsAction(name: 'progress.coach_notes.update')]
#[Patch('/api/v1/coach-notes/{note}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateCoachNoteAction
{
    use AsController;

    public function __construct(
        private readonly CoachNoteRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `coach-note` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateCoachNoteRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateCoachNoteRequestData $data): CoachNoteData
    {
        $model = $this->repository->update($id, $data->toArray());

        return CoachNoteData::from($model);
    }
}
