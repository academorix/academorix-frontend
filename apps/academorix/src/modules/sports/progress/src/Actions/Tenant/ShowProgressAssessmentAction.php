<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

namespace Academorix\Progress\Actions\Tenant;

use Academorix\Progress\Contracts\Repositories\ProgressAssessmentRepositoryInterface;
use Academorix\Progress\Data\ProgressAssessmentData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Get;

/**
 * `GET /api/v1/progress-assessments/{assessment}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Progress
 *
 * @since    0.1.0
 */
#[AsAction(name: 'progress.progress_assessments.show')]
#[Get('/api/v1/progress-assessments/{assessment}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowProgressAssessmentAction
{
    use AsController;

    public function __construct(
        private readonly ProgressAssessmentRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `progress-assessment` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): ProgressAssessmentData
    {
        return ProgressAssessmentData::from($this->repository->findOrFail($id));
    }
}
