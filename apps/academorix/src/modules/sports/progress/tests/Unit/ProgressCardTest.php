<?php

declare(strict_types=1);

use Academorix\Progress\Models\ProgressCard;

it('is a placeholder — replace with actual ProgressCard unit tests', function (): void {
    expect(ProgressCard::class)->toBeString();
});
