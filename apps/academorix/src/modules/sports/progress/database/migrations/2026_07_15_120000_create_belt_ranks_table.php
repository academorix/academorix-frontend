<?php

/**
 * @file database/migrations/2026_07_15_120000_create_belt_ranks_table.php
 *
 * @description
 * Create the `belt_ranks` table for the `BeltRank` entity.
 * Owned by the progress module. Every column + index reference
 * goes through {@see Academorix\Progress\Contracts\Data\BeltRankInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports progress --force`.

declare(strict_types=1);

use Academorix\Progress\Contracts\Data\BeltRankInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(BeltRankInterface::TABLE, function (Blueprint $table): void {
            $table->string(BeltRankInterface::ATTR_ID, 30)->primary();
            $table->string(BeltRankInterface::ATTR_TENANT_ID, 30)->nullable();
            $table->foreign(BeltRankInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(BeltRankInterface::ATTR_SPORT_KEY, 32);
            $table->string(BeltRankInterface::ATTR_CODE, 32);
            $table->jsonb(BeltRankInterface::ATTR_LABELS)->default('{}');
            $table->string(BeltRankInterface::ATTR_COLOR_HEX, 7)->nullable();
            $table->integer(BeltRankInterface::ATTR_RANK_ORDER);
            $table->integer(BeltRankInterface::ATTR_MIN_TRAINING_HOURS)->nullable();
            $table->jsonb(BeltRankInterface::ATTR_METADATA)->nullable();
            $table->string(BeltRankInterface::ATTR_CREATED_BY, 30)->nullable();
            $table->string(BeltRankInterface::ATTR_UPDATED_BY, 30)->nullable();
            $table->timestampTz(BeltRankInterface::ATTR_DELETED_AT)->nullable();
            $table->timestampTz(BeltRankInterface::ATTR_CREATED_AT);
            $table->timestampTz(BeltRankInterface::ATTR_UPDATED_AT);
            $table->index([BeltRankInterface::ATTR_SPORT_KEY, BeltRankInterface::ATTR_RANK_ORDER], 'belt_ranks_order_idx');
        });

        DB::statement('CREATE UNIQUE INDEX belt_ranks_unique ON ' . BeltRankInterface::TABLE . ' (' . BeltRankInterface::ATTR_TENANT_ID . ', ' . BeltRankInterface::ATTR_SPORT_KEY . ', ' . BeltRankInterface::ATTR_CODE . ') WHERE deleted_at IS NULL');
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(BeltRankInterface::TABLE);
    }
};
