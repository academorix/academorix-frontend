<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports competition --force`.

declare(strict_types=1);

namespace Academorix\Competition\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan competition:recompute-standings` — Recompute standings — competition command.
 *
 * @category Competition
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'competition:recompute-standings',
    description: 'Recompute standings — competition command.',
)]
final class RecomputeStandingsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Recompute standings — competition command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Recompute standings — competition command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
