<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports competition --force`.

declare(strict_types=1);

namespace Stackra\Competition\Actions\Tenant;

use Stackra\Competition\Contracts\Repositories\CompetitionRepositoryInterface;
use Stackra\Competition\Data\CompetitionData;
use Stackra\Competition\Data\Requests\CreateCompetitionRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/competitions` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Competition
 *
 * @since    0.1.0
 */
#[AsAction(name: 'competition.competitions.create')]
#[Post('/api/v1/competitions')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateCompetitionAction
{
    use AsController;

    public function __construct(
        private readonly CompetitionRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `competition` from the validated request payload.
     *
     * @param  CreateCompetitionRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateCompetitionRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(CompetitionData::from($model), JsonResponse::HTTP_CREATED);
    }
}
