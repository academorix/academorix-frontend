<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports competition --force`.

declare(strict_types=1);

namespace Academorix\Competition\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Post-match correction. Full audit chain. Triggers full re-standings recompute.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention 7yr)
 * - jobs::RecomputeStandingsJob
 * - notifications::NotifyResultAmended
 *
 * @category Competition
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class FixtureResultAmended implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $fixtureId  ULID
     * @param  int  $previousHomeScore  int
     * @param  int  $previousAwayScore  int
     * @param  int  $newHomeScore  int
     * @param  int  $newAwayScore  int
     * @param  string  $amendedByUserId  ULID
     * @param  string  $reason  string
     */
    public function __construct(
        public readonly string $fixtureId,
        public readonly int $previousHomeScore,
        public readonly int $previousAwayScore,
        public readonly int $newHomeScore,
        public readonly int $newAwayScore,
        public readonly string $amendedByUserId,
        public readonly string $reason,
    ) {
    }
}
