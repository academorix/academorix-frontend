<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports competition --force`.

declare(strict_types=1);

namespace Stackra\Competition\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Winner advanced to next bracket node. status: node → completed; child node gets team assignment.
 *
 * ## Consumers
 *
 * - realtime::BroadcastBracket
 * - jobs::ScheduleNextRoundJob
 *
 * @category Competition
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class BracketProgressed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $competitionId  ULID
     * @param  string  $completedNodeId  ULID (brn_*)
     * @param  string  $advancedTeamId  ULID
     * @param  string  $nextNodeId  ULID|null
     */
    public function __construct(
        public readonly string $competitionId,
        public readonly string $completedNodeId,
        public readonly string $advancedTeamId,
        public readonly string $nextNodeId,
    ) {
    }
}
