<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports competition --force`.

declare(strict_types=1);

namespace Stackra\Competition\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New Competition row committed with status='draft'.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Competition
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class CompetitionCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $competitionId  ULID (cmp_*)
     * @param  string  $tenantId  ULID
     * @param  string  $kind  'league'|'tournament'|'friendly'
     * @param  float  $format  'single_elimination'|'double_elimination'|'round_robin'|'group_stage'|'seeded_bracket'
     * @param  string  $seasonId  ULID
     */
    public function __construct(
        public readonly string $competitionId,
        public readonly string $tenantId,
        public readonly string $kind,
        public readonly float $format,
        public readonly string $seasonId,
    ) {
    }
}
