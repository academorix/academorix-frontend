<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan matches:record-result` — Record result — matches command.
 *
 * @category Match
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'matches:record-result',
    description: 'Record result — matches command.',
)]
final class RecordResultCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Record result — matches command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Record result — matches command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
