<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan matches:publish-lineup` — Publish lineup — matches command.
 *
 * @category Match
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'matches:publish-lineup',
    description: 'Publish lineup — matches command.',
)]
final class PublishLineupCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Publish lineup — matches command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Publish lineup — matches command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
