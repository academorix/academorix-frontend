<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan matches:reschedule` — Reschedule — matches command.
 *
 * @category Match
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'matches:reschedule',
    description: 'Reschedule — matches command.',
)]
final class RescheduleCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Reschedule — matches command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Reschedule — matches command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
