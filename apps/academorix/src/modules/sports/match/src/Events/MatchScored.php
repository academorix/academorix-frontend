<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * A match_events row with type IN ('goal', 'own_goal', 'apparatus_score', 'lane_time') was inserted while status=IN_PROGRESS. Powers the live scoreboard.
 *
 * ## Consumers
 *
 * - monitoring::MetricsCollector
 * - broadcasts::PushLiveScoreUpdate (tenant.{tenantId}.matches.{matchId} channel)
 *
 * @category Match
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MatchScored implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $matchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $matchEventId  ULID (mev_*)
     * @param  string  $eventType  string
     * @param  string  $athleteId  ULID | null
     * @param  int  $minute  integer
     * @param  int  $stoppage  integer
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $matchId,
        public readonly string $tenantId,
        public readonly string $matchEventId,
        public readonly string $eventType,
        public readonly string $athleteId,
        public readonly int $minute,
        public readonly int $stoppage,
        public readonly string $at,
    ) {
    }
}
