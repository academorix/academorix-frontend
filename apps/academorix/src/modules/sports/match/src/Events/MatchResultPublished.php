<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * POST /matches/{match}/results/publish. match_results.published_at set on all owned rows. Terminal for scoring. Kicks the standings recompute.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - match::RecomputeStandingsOnMatchResultPublished (dispatches RecomputeStandingsJob when competition_id IS NOT NULL)
 * - notifications::DispatchMatchResultPublishedNotification (families + squad + public site)
 * - monitoring::MetricsCollector (increments stackra.matches.result_published_total)
 *
 * @category Match
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MatchResultPublished implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $matchId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $competitionId  ULID | null
     * @param  string  $outcome  'win' | 'draw' | 'loss' | null
     * @param  string  $motmAthleteId  ULID | null
     * @param  array  $resultSummaryJson  object (denormalized snapshot)
     * @param  string  $publishedAt  ISO 8601
     * @param  string  $publishedByUserId  ULID
     */
    public function __construct(
        public readonly string $matchId,
        public readonly string $tenantId,
        public readonly string $competitionId,
        public readonly string $outcome,
        public readonly string $motmAthleteId,
        public readonly array $resultSummaryJson,
        public readonly string $publishedAt,
        public readonly string $publishedByUserId,
    ) {
    }
}
