<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Match.status transitions SCHEDULED -> LINEUP_SET. Head coach chose the squad + roles. Lineup is not yet parent-facing — that requires MatchLineupPublished.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - monitoring::MetricsCollector
 *
 * @category Match
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MatchLineupSet implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $matchId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $squadSize  integer
     * @param  int  $starterCount  integer
     * @param  string  $at  ISO 8601
     */
    public function __construct(
        public readonly string $matchId,
        public readonly string $tenantId,
        public readonly int $squadSize,
        public readonly int $starterCount,
        public readonly string $at,
    ) {
    }
}
