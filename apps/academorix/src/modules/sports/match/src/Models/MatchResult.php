<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Stackra\Match\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Attributes\WithoutIncrementing;
use Illuminate\Database\Eloquent\Model;
use Stackra\Match\Contracts\Data\MatchResultInterface;
use Stackra\Match\Database\Factories\MatchResultFactory;
use Stackra\Foundation\Concerns\Filterable;
use Stackra\Foundation\Concerns\HasMetadata;
use Stackra\Match\Policies\MatchResultPolicy;
use Stackra\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Attributes\UsePolicy;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use OwenIt\Auditing\Contracts\Auditable as AuditableContract;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a MatchResult.
 *
 * Per-participant final result for a match.
 *
 * @category Match
 *
 * @since    0.1.0
 */
#[Table(name: MatchResultInterface::TABLE, key: MatchResultInterface::PRIMARY_KEY, keyType: MatchResultInterface::KEY_TYPE)]
#[Fillable([
    MatchResultInterface::ATTR_TENANT_ID,
        MatchResultInterface::ATTR_MATCH_ID,
        MatchResultInterface::ATTR_PARTICIPANT_ID,
        MatchResultInterface::ATTR_PERIOD_SCORES_JSON,
        MatchResultInterface::ATTR_FINAL_SCORE_HOME,
        MatchResultInterface::ATTR_FINAL_SCORE_AWAY,
        MatchResultInterface::ATTR_TIME_RECORDED_SECONDS,
        MatchResultInterface::ATTR_DISTANCE_RECORDED_METERS,
        MatchResultInterface::ATTR_APPARATUS_SCORES_JSON,
        MatchResultInterface::ATTR_OUTCOME,
        MatchResultInterface::ATTR_MOTM_ATHLETE_ID,
        MatchResultInterface::ATTR_PUBLISHED_AT,
        MatchResultInterface::ATTR_METADATA,
])]
#[UseFactory(MatchResultFactory::class)]
#[WithoutIncrementing]
#[UsePolicy(MatchResultPolicy::class)]
final class MatchResult extends Model implements MatchResultInterface, AuditableContract
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    // TODO(gen): resolve unknown trait `BelongsToMatch` — add its import + `use` line.
    use HasMetadata;
    use Userstamps;
    use Auditable;
    use LogsActivity;
    use Filterable;

    /**
     * Cast map — from the blueprint's `x-eloquent.casts`.
     *
     * @var array<string, string>
     */
    protected $casts = [
        MatchResultInterface::ATTR_OUTCOME => MatchOutcome::class,
        MatchResultInterface::ATTR_PERIOD_SCORES_JSON => 'array',
        MatchResultInterface::ATTR_FINAL_SCORE_HOME => 'integer',
        MatchResultInterface::ATTR_FINAL_SCORE_AWAY => 'integer',
        MatchResultInterface::ATTR_TIME_RECORDED_SECONDS => 'float',
        MatchResultInterface::ATTR_DISTANCE_RECORDED_METERS => 'float',
        MatchResultInterface::ATTR_APPARATUS_SCORES_JSON => 'array',
        MatchResultInterface::ATTR_PUBLISHED_AT => 'datetime',
        MatchResultInterface::ATTR_METADATA => 'array',
    ];
}
