<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Attributes\WithoutIncrementing;
use Illuminate\Database\Eloquent\Model;
use Academorix\Match\Contracts\Data\MatchParticipantInterface;
use Academorix\Match\Database\Factories\MatchParticipantFactory;
use Stackra\Foundation\Concerns\Filterable;
use Stackra\Foundation\Concerns\HasMetadata;
use Academorix\Match\Policies\MatchParticipantPolicy;
use Stackra\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Attributes\UsePolicy;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use OwenIt\Auditing\Contracts\Auditable as AuditableContract;

/**
 * Eloquent model for a MatchParticipant.
 *
 * Polymorphic participant row for multi-team events (galas, swim meets, festivals) where a single match aggregates N teams / athletes / external opponents instead of a home/away pair.
 *
 * @category Match
 *
 * @since    0.1.0
 */
#[Table(name: MatchParticipantInterface::TABLE, key: MatchParticipantInterface::PRIMARY_KEY, keyType: MatchParticipantInterface::KEY_TYPE)]
#[Fillable([
    MatchParticipantInterface::ATTR_TENANT_ID,
        MatchParticipantInterface::ATTR_MATCH_ID,
        MatchParticipantInterface::ATTR_PARTICIPANT_TYPE,
        MatchParticipantInterface::ATTR_PARTICIPANT_ID,
        MatchParticipantInterface::ATTR_LABEL,
        MatchParticipantInterface::ATTR_DISPLAY_ORDER,
        MatchParticipantInterface::ATTR_METADATA,
])]
#[UseFactory(MatchParticipantFactory::class)]
#[WithoutIncrementing]
#[UsePolicy(MatchParticipantPolicy::class)]
final class MatchParticipant extends Model implements MatchParticipantInterface, AuditableContract
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    // TODO(gen): resolve unknown trait `BelongsToMatch` — add its import + `use` line.
    use HasMetadata;
    use Userstamps;
    use Auditable;
    use Filterable;

    /**
     * Cast map — from the blueprint's `x-eloquent.casts`.
     *
     * @var array<string, string>
     */
    protected $casts = [
        MatchParticipantInterface::ATTR_DISPLAY_ORDER => 'integer',
        MatchParticipantInterface::ATTR_METADATA => 'array',
    ];
}
