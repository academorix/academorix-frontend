<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Academorix\Match\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `status` on `match-fixture`.
 *
 * Enum: SCHEDULED / LINEUP_SET / IN_PROGRESS / COMPLETED / POSTPONED / CANCELLED / ABANDONED.
 *
 * @category Match
 *
 * @since    0.1.0
 */
enum MatchFixtureStatus: string
{
    use Enum;

    case Scheduled = 'SCHEDULED';
    case LineupSet = 'LINEUP_SET';
    case InProgress = 'IN_PROGRESS';
    case Completed = 'COMPLETED';
    case Postponed = 'POSTPONED';
    case Cancelled = 'CANCELLED';
    case Abandoned = 'ABANDONED';
}
