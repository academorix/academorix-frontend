<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Stackra\Match\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `type` on `match-event`.
 *
 * Enum: goal / assist / own_goal / yellow_card / red_card / substitution / apparatus_score / lane_time / penalty_awarded / penalty_scored / penalty_missed / timeout.
 *
 * @category Match
 *
 * @since    0.1.0
 */
enum MatchEventType: string
{
    use Enum;

    case Goal = 'goal';
    case Assist = 'assist';
    case OwnGoal = 'own_goal';
    case YellowCard = 'yellow_card';
    case RedCard = 'red_card';
    case Substitution = 'substitution';
    case ApparatusScore = 'apparatus_score';
    case LaneTime = 'lane_time';
    case PenaltyAwarded = 'penalty_awarded';
    case PenaltyScored = 'penalty_scored';
    case PenaltyMissed = 'penalty_missed';
    case Timeout = 'timeout';
}
