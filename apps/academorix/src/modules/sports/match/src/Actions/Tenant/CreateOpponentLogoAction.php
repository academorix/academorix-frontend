<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports match --force`.

declare(strict_types=1);

namespace Stackra\Match\Actions\Tenant;

use Stackra\Match\Contracts\Repositories\OpponentLogoRepositoryInterface;
use Stackra\Match\Data\OpponentLogoData;
use Stackra\Match\Data\Requests\CreateOpponentLogoRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/opponent-logos` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Match
 *
 * @since    0.1.0
 */
#[AsAction(name: 'match.opponent_logos.create')]
#[Post('/api/v1/opponent-logos')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateOpponentLogoAction
{
    use AsController;

    public function __construct(
        private readonly OpponentLogoRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `opponent-logo` from the validated request payload.
     *
     * @param  CreateOpponentLogoRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateOpponentLogoRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(OpponentLogoData::from($model), JsonResponse::HTTP_CREATED);
    }
}
