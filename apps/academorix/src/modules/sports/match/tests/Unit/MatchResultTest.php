<?php

declare(strict_types=1);

use Stackra\Match\Models\MatchResult;

it('is a placeholder — replace with actual MatchResult unit tests', function (): void {
    expect(MatchResult::class)->toBeString();
});
