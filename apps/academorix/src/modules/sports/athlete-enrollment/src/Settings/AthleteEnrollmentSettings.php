<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Academorix\AthleteEnrollment\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * AthleteEnrollmentSettings — Enrollments.
 *
 * Tenant-wide enrollment workflow settings.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'athlete_enrollment',
    label: 'Enrollments',
    description: 'Tenant-wide enrollment workflow settings.',
    icon: 'clipboard-document-list',
    scope: 'tenant',
    permission: 'enrollments.viewAny',
    sortOrder: 47,
)]
class AthleteEnrollmentSettings extends Settings
{

    // ── Deadlines ──────────────────────────────────
    #[SettingGroup(label: 'Deadlines', icon: 'clock', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Number, label: 'Enrollment expiry TTL (days past registration close)', group: 'Deadlines', sortOrder: 1, min: 1, max: 365)]
    public int $expiry_ttl_days = 30;

    #[SettingField(controlType: ControlType::Number, label: 'Provisional enrollment TTL (days)', group: 'Deadlines', sortOrder: 2, min: 1, max: 90)]
    public int $provisional_ttl_days = 14;

    #[SettingField(controlType: ControlType::Text, label: 'Deadline reminder cadence (days before close)', group: 'Deadlines', sortOrder: 3, options: ['30' => '30 days', '14' => '14 days', '7' => '7 days', '3' => '3 days', '1' => '1 day'])]
    public array $deadline_thresholds_days = [14, 7, 1];


    // ── Waitlist ──────────────────────────────────
    #[SettingGroup(label: 'Waitlist', icon: 'queue-list', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Automatically promote waitlisted enrollments when capacity opens', group: 'Waitlist', sortOrder: 1)]
    public bool $auto_promote_from_waitlist = true;


    // ── Late registration ──────────────────────────────────
    #[SettingGroup(label: 'Late registration', icon: 'banknotes', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Allow late enrollments after registration closes', group: 'Late registration', sortOrder: 1)]
    public bool $allow_late_registration = true;

    #[SettingField(controlType: ControlType::Number, label: 'Late registration fee (cents)', group: 'Late registration', sortOrder: 2, min: 0, max: 100000, helpText: 'Additional fee applied to late enrollments. Wave 4 Finance charges this.')]
    public int $late_registration_fee_cents = 0;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'athlete_enrollment';
    }
}
