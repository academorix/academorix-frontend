<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Academorix\AthleteEnrollment\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `status` on `athlete-enrollment`.
 *
 * Enum: submitted / payment_pending / consent_pending / confirmed / active / completed / withdrawn / expelled / expired / waitlisted.
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
enum AthleteEnrollmentStatus: string
{
    use Enum;

    case Submitted = 'submitted';
    case PaymentPending = 'payment_pending';
    case ConsentPending = 'consent_pending';
    case Confirmed = 'confirmed';
    case Active = 'active';
    case Completed = 'completed';
    case Withdrawn = 'withdrawn';
    case Expelled = 'expelled';
    case Expired = 'expired';
    case Waitlisted = 'waitlisted';
}
