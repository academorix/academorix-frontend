<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Academorix\AthleteEnrollment\Enums;

use Academorix\Enum\Enum;

/**
 * Closed-set enum for `enrollment_source` on `athlete-enrollment`.
 *
 * Enum: self_serve / admin / import / migration / api / trial_conversion.
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
enum AthleteEnrollmentEnrollmentSource: string
{
    use Enum;

    case SelfServe = 'self_serve';
    case Admin = 'admin';
    case Import = 'import';
    case Migration = 'migration';
    case Api = 'api';
    case TrialConversion = 'trial_conversion';
}
