<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Stackra\AthleteEnrollment\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The age_group_snapshot_matches_team_age_group rule fires as a SOFT-WARN — the snapshot doesn't match the team's declared age_group_id. Non-fatal, but flagged for admin review. Common cases: an under-age athlete playing up (allowed with note), an over-age athlete playing down (typically disallowed).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::DispatchAgeGroupMismatchNotification (to admin + coach only — informational)
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EnrollmentAgeGroupSnapshotMismatched implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $enrollmentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteAgeGroupId  ULID
     * @param  string  $teamAgeGroupId  ULID
     * @param  int  $athleteAgeYears  integer
     * @param  string  $mismatchKind  'playing_up' | 'playing_down' | 'no_team_age_group' | 'no_athlete_snapshot'
     */
    public function __construct(
        public readonly string $enrollmentId,
        public readonly string $tenantId,
        public readonly string $athleteAgeGroupId,
        public readonly string $teamAgeGroupId,
        public readonly int $athleteAgeYears,
        public readonly string $mismatchKind,
    ) {
    }
}
