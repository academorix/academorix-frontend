<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Stackra\AthleteEnrollment\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Status transitioned active/confirmed → withdrawn. Terminal (parent-initiated exit). Cascades: soft-deletes the linked TeamMember.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchEnrollmentWithdrawnNotification (to primary guardian + admin + coach)
 * - teams::SoftDeleteTeamMemberOnEnrollmentWithdrawn (Wave 3+; cascade cleanup)
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EnrollmentWithdrawn implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $enrollmentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $teamId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $withdrawalReason  string
     * @param  string  $withdrawnAt  ISO 8601
     * @param  string  $withdrawnByUserId  ULID
     */
    public function __construct(
        public readonly string $enrollmentId,
        public readonly string $tenantId,
        public readonly string $teamId,
        public readonly string $athleteId,
        public readonly string $withdrawalReason,
        public readonly string $withdrawnAt,
        public readonly string $withdrawnByUserId,
    ) {
    }
}
