<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Stackra\AthleteEnrollment\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Status transitioned active → completed. Terminal happy path. Season ended — athlete completed the enrollment. Also fires via season::SeasonCompleted cascade.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchEnrollmentCompletedNotification (to primary guardian + admin + coach)
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class EnrollmentCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $enrollmentId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $teamId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $seasonId  ULID
     * @param  string  $completedAt  ISO 8601
     * @param  int  $daysActive  integer
     */
    public function __construct(
        public readonly string $enrollmentId,
        public readonly string $tenantId,
        public readonly string $teamId,
        public readonly string $athleteId,
        public readonly string $seasonId,
        public readonly string $completedAt,
        public readonly int $daysActive,
    ) {
    }
}
