<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Academorix\AthleteEnrollment\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan enrollment:submit` — Submit — enrollment command.
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'enrollment:submit',
    description: 'Submit — enrollment command.',
)]
final class SubmitCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Submit — enrollment command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Submit — enrollment command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
