<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-enrollment --force`.

declare(strict_types=1);

namespace Stackra\AthleteEnrollment\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan enrollment:waitlist` — Waitlist — enrollment command.
 *
 * @category AthleteEnrollment
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'enrollment:waitlist',
    description: 'Waitlist — enrollment command.',
)]
final class WaitlistCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Waitlist — enrollment command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Waitlist — enrollment command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
