<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Stackra\Attendance\Actions\Tenant;

use Stackra\Attendance\Contracts\Repositories\AttendancePolicyRepositoryInterface;
use Stackra\Attendance\Data\AttendancePolicyData;
use Stackra\Attendance\Data\Requests\CreateAttendancePolicyRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/attendance-policies` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsAction(name: 'attendance.attendance_policies.create')]
#[Post('/api/v1/attendance-policies')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateAttendancePolicieAction
{
    use AsController;

    public function __construct(
        private readonly AttendancePolicyRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `attendance-policie` from the validated request payload.
     *
     * @param  CreateAttendancePolicyRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateAttendancePolicyRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(AttendancePolicyData::from($model), JsonResponse::HTTP_CREATED);
    }
}
