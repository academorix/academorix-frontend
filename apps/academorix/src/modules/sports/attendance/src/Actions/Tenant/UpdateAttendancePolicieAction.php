<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Actions\Tenant;

use Academorix\Attendance\Contracts\Repositories\AttendancePolicyRepositoryInterface;
use Academorix\Attendance\Data\AttendancePolicyData;
use Academorix\Attendance\Data\Requests\UpdateAttendancePolicyRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/attendance-policies/{policy}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsAction(name: 'attendance.attendance_policies.update')]
#[Patch('/api/v1/attendance-policies/{policy}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateAttendancePolicieAction
{
    use AsController;

    public function __construct(
        private readonly AttendancePolicyRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `attendance-policie` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateAttendancePolicyRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateAttendancePolicyRequestData $data): AttendancePolicyData
    {
        $model = $this->repository->update($id, $data->toArray());

        return AttendancePolicyData::from($model);
    }
}
