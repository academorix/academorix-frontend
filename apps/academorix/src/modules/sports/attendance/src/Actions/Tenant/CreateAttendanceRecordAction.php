<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Actions\Tenant;

use Academorix\Attendance\Contracts\Repositories\AttendanceRecordRepositoryInterface;
use Academorix\Attendance\Data\AttendanceRecordData;
use Academorix\Attendance\Data\Requests\CreateAttendanceRecordRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/attendance-records` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsAction(name: 'attendance.attendance_records.create')]
#[Post('/api/v1/attendance-records')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateAttendanceRecordAction
{
    use AsController;

    public function __construct(
        private readonly AttendanceRecordRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `attendance-record` from the validated request payload.
     *
     * @param  CreateAttendanceRecordRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateAttendanceRecordRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(AttendanceRecordData::from($model), JsonResponse::HTTP_CREATED);
    }
}
