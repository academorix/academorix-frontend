<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Stackra\Attendance\Actions\Tenant;

use Stackra\Attendance\Contracts\Repositories\AttendanceRecordRepositoryInterface;
use Stackra\Attendance\Data\AttendanceRecordData;
use Stackra\Attendance\Data\Requests\UpdateAttendanceRecordRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/attendance-records/{record}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsAction(name: 'attendance.attendance_records.update')]
#[Patch('/api/v1/attendance-records/{record}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateAttendanceRecordAction
{
    use AsController;

    public function __construct(
        private readonly AttendanceRecordRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `attendance-record` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateAttendanceRecordRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateAttendanceRecordRequestData $data): AttendanceRecordData
    {
        $model = $this->repository->update($id, $data->toArray());

        return AttendanceRecordData::from($model);
    }
}
