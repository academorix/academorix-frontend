<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the Attendance module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum AttendancePermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `attendance.viewAny` — Attendance ViewAny
     */
    #[Label('Attendance ViewAny')]
    #[Description('Attendance ViewAny')]
    case AttendanceViewAny = 'attendance.viewAny';

    /**
     * `attendance.viewAny.coach` — Coach-scoped listing — sees attendance rows for sessions where the coach is head_coach or assistant_coach.
     */
    #[Label('Coach-scoped listing — sees attendance rows for sessions where the coach is head')]
    #[Description('Coach-scoped listing — sees attendance rows for sessions where the coach is head_coach or assistant_coach.')]
    case AttendanceViewAnyCoach = 'attendance.viewAny.coach';

    /**
     * `attendance.viewAny.own` — Guardian + adult-athlete scoped listing.
     */
    #[Label('Guardian + adult-athlete scoped listing')]
    #[Description('Guardian + adult-athlete scoped listing.')]
    case AttendanceViewAnyOwn = 'attendance.viewAny.own';

    /**
     * `attendance.view` — Attendance View
     */
    #[Label('Attendance View')]
    #[Description('Attendance View')]
    case AttendanceView = 'attendance.view';

    /**
     * `attendance.view.own` — Attendance View Own
     */
    #[Label('Attendance View Own')]
    #[Description('Attendance View Own')]
    case AttendanceViewOwn = 'attendance.view.own';

    /**
     * `attendance.create` — Admin-recorded check-in.
     */
    #[Label('Admin-recorded check-in')]
    #[Description('Admin-recorded check-in.')]
    case AttendanceCreate = 'attendance.create';

    /**
     * `attendance.create.coach` — Coach-verified check-in — scoped to sessions the coach is assigned to.
     */
    #[Label('Coach-verified check-in — scoped to sessions the coach is assigned to')]
    #[Description('Coach-verified check-in — scoped to sessions the coach is assigned to.')]
    case AttendanceCreateCoach = 'attendance.create.coach';

    /**
     * `attendance.check-in.own` — Self / kiosk check-in for the caller's athlete or their guardian's athletes.
     */
    #[Label('Self / kiosk check-in for the caller\'s athlete or their guardian\'s athletes')]
    #[Description('Self / kiosk check-in for the caller\'s athlete or their guardian\'s athletes.')]
    case AttendanceCheckInOwn = 'attendance.check-in.own';

    /**
     * `attendance.check-out` — Attendance Check-out
     */
    #[Label('Attendance Check-out')]
    #[Description('Attendance Check-out')]
    case AttendanceCheckOut = 'attendance.check-out';

    /**
     * `attendance.update` — Post-check-in mutations within the 48h correction window.
     */
    #[Label('Post-check-in mutations within the 48h correction window')]
    #[Description('Post-check-in mutations within the 48h correction window.')]
    case AttendanceUpdate = 'attendance.update';

    /**
     * `attendance.cancel` — Cancel an accidental check-in.
     */
    #[Label('Cancel an accidental check-in')]
    #[Description('Cancel an accidental check-in. Refunds consumed pass.')]
    case AttendanceCancel = 'attendance.cancel';

    /**
     * `attendance.add-session-note` — Coach-only session notes on an attendance record.
     */
    #[Label('Coach-only session notes on an attendance record')]
    #[Description('Coach-only session notes on an attendance record.')]
    case AttendanceAddSessionNote = 'attendance.add-session-note';

    /**
     * `attendance.reconcile` — Trigger a manual attendance reconciliation sweep.
     */
    #[Label('Trigger a manual attendance reconciliation sweep')]
    #[Description('Trigger a manual attendance reconciliation sweep.')]
    case AttendanceReconcile = 'attendance.reconcile';

    /**
     * `attendance.post-window-override` — Bypass the 48h post-session correction window with a documented reason.
     */
    #[Label('Bypass the 48h post-session correction window with a documented reason')]
    #[Description('Bypass the 48h post-session correction window with a documented reason.')]
    case AttendancePostWindowOverride = 'attendance.post-window-override';

    /**
     * `attendance-policies.viewAny` — Attendance-policies ViewAny
     */
    #[Label('Attendance-policies ViewAny')]
    #[Description('Attendance-policies ViewAny')]
    case AttendancePoliciesViewAny = 'attendance-policies.viewAny';

    /**
     * `attendance-policies.view` — Attendance-policies View
     */
    #[Label('Attendance-policies View')]
    #[Description('Attendance-policies View')]
    case AttendancePoliciesView = 'attendance-policies.view';

    /**
     * `attendance-policies.create` — Create attendance policy.
     */
    #[Label('Create attendance policy')]
    #[Description('Create attendance policy. Plan-scoped policies require attendance_advanced_policies entitlement.')]
    case AttendancePoliciesCreate = 'attendance-policies.create';

    /**
     * `attendance-policies.update` — Attendance-policies Update
     */
    #[Label('Attendance-policies Update')]
    #[Description('Attendance-policies Update')]
    case AttendancePoliciesUpdate = 'attendance-policies.update';

    /**
     * `attendance-policies.activate` — Attendance-policies Activate
     */
    #[Label('Attendance-policies Activate')]
    #[Description('Attendance-policies Activate')]
    case AttendancePoliciesActivate = 'attendance-policies.activate';

    /**
     * `attendance-policies.deactivate` — Attendance-policies Deactivate
     */
    #[Label('Attendance-policies Deactivate')]
    #[Description('Attendance-policies Deactivate')]
    case AttendancePoliciesDeactivate = 'attendance-policies.deactivate';

    /**
     * `attendance-policies.delete` — Attendance-policies Delete
     */
    #[Label('Attendance-policies Delete')]
    #[Description('Attendance-policies Delete')]
    case AttendancePoliciesDelete = 'attendance-policies.delete';

    /**
     * `absences.viewAny` — Absences ViewAny
     */
    #[Label('Absences ViewAny')]
    #[Description('Absences ViewAny')]
    case AbsencesViewAny = 'absences.viewAny';

    /**
     * `absences.viewAny.own` — Absences ViewAny Own
     */
    #[Label('Absences ViewAny Own')]
    #[Description('Absences ViewAny Own')]
    case AbsencesViewAnyOwn = 'absences.viewAny.own';

    /**
     * `absences.view` — Absences View
     */
    #[Label('Absences View')]
    #[Description('Absences View')]
    case AbsencesView = 'absences.view';

    /**
     * `absences.view.own` — Absences View Own
     */
    #[Label('Absences View Own')]
    #[Description('Absences View Own')]
    case AbsencesViewOwn = 'absences.view.own';

    /**
     * `absences.report.own` — Guardian / adult athlete self-report absence.
     */
    #[Label('Guardian / adult athlete self-report absence')]
    #[Description('Guardian / adult athlete self-report absence.')]
    case AbsencesReportOwn = 'absences.report.own';

    /**
     * `absences.excuse` — Admin flips unexcused → excused.
     */
    #[Label('Admin flips unexcused → excused')]
    #[Description('Admin flips unexcused → excused.')]
    case AbsencesExcuse = 'absences.excuse';

    /**
     * `absences.reverse-excuse` — Admin flips excused → unexcused (audit-sensitive).
     */
    #[Label('Admin flips excused → unexcused (audit-sensitive)')]
    #[Description('Admin flips excused → unexcused (audit-sensitive).')]
    case AbsencesReverseExcuse = 'absences.reverse-excuse';

    /**
     * `absences.update` — Absences Update
     */
    #[Label('Absences Update')]
    #[Description('Absences Update')]
    case AbsencesUpdate = 'absences.update';

    /**
     * `absences.delete` — Only with admin override + documented reason.
     */
    #[Label('Only with admin override + documented reason')]
    #[Description('Only with admin override + documented reason.')]
    case AbsencesDelete = 'absences.delete';

    /**
     * `late-arrivals.viewAny` — Late-arrivals ViewAny
     */
    #[Label('Late-arrivals ViewAny')]
    #[Description('Late-arrivals ViewAny')]
    case LateArrivalsViewAny = 'late-arrivals.viewAny';

    /**
     * `late-arrivals.view` — Late-arrivals View
     */
    #[Label('Late-arrivals View')]
    #[Description('Late-arrivals View')]
    case LateArrivalsView = 'late-arrivals.view';

    /**
     * `reports.attendance.view` — Aggregate attendance report.
     */
    #[Label('Aggregate attendance report')]
    #[Description('Aggregate attendance report. Requires attendance_analytics entitlement.')]
    case ReportsAttendanceView = 'reports.attendance.view';

    /**
     * `platform.attendance.viewAny` — Platform Attendance ViewAny
     */
    #[Label('Platform Attendance ViewAny')]
    #[Description('Platform Attendance ViewAny')]
    case PlatformAttendanceViewAny = 'platform.attendance.viewAny';

    /**
     * `platform.attendance.view` — Platform Attendance View
     */
    #[Label('Platform Attendance View')]
    #[Description('Platform Attendance View')]
    case PlatformAttendanceView = 'platform.attendance.view';

    /**
     * `platform.absences.viewAny` — Platform Absences ViewAny
     */
    #[Label('Platform Absences ViewAny')]
    #[Description('Platform Absences ViewAny')]
    case PlatformAbsencesViewAny = 'platform.absences.viewAny';

    /**
     * `platform.late-arrivals.viewAny` — Platform Late-arrivals ViewAny
     */
    #[Label('Platform Late-arrivals ViewAny')]
    #[Description('Platform Late-arrivals ViewAny')]
    case PlatformLateArrivalsViewAny = 'platform.late-arrivals.viewAny';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::AttendanceViewAny => Guard::Sanctum,
            self::AttendanceViewAnyCoach => Guard::Sanctum,
            self::AttendanceViewAnyOwn => Guard::Sanctum,
            self::AttendanceView => Guard::Sanctum,
            self::AttendanceViewOwn => Guard::Sanctum,
            self::AttendanceCreate => Guard::Sanctum,
            self::AttendanceCreateCoach => Guard::Sanctum,
            self::AttendanceCheckInOwn => Guard::Sanctum,
            self::AttendanceCheckOut => Guard::Sanctum,
            self::AttendanceUpdate => Guard::Sanctum,
            self::AttendanceCancel => Guard::Sanctum,
            self::AttendanceAddSessionNote => Guard::Sanctum,
            self::AttendanceReconcile => Guard::Sanctum,
            self::AttendancePostWindowOverride => Guard::Sanctum,
            self::AttendancePoliciesViewAny => Guard::Sanctum,
            self::AttendancePoliciesView => Guard::Sanctum,
            self::AttendancePoliciesCreate => Guard::Sanctum,
            self::AttendancePoliciesUpdate => Guard::Sanctum,
            self::AttendancePoliciesActivate => Guard::Sanctum,
            self::AttendancePoliciesDeactivate => Guard::Sanctum,
            self::AttendancePoliciesDelete => Guard::Sanctum,
            self::AbsencesViewAny => Guard::Sanctum,
            self::AbsencesViewAnyOwn => Guard::Sanctum,
            self::AbsencesView => Guard::Sanctum,
            self::AbsencesViewOwn => Guard::Sanctum,
            self::AbsencesReportOwn => Guard::Sanctum,
            self::AbsencesExcuse => Guard::Sanctum,
            self::AbsencesReverseExcuse => Guard::Sanctum,
            self::AbsencesUpdate => Guard::Sanctum,
            self::AbsencesDelete => Guard::Sanctum,
            self::LateArrivalsViewAny => Guard::Sanctum,
            self::LateArrivalsView => Guard::Sanctum,
            self::ReportsAttendanceView => Guard::Sanctum,
            self::PlatformAttendanceViewAny => Guard::PlatformAdmin,
            self::PlatformAttendanceView => Guard::PlatformAdmin,
            self::PlatformAbsencesViewAny => Guard::PlatformAdmin,
            self::PlatformLateArrivalsViewAny => Guard::PlatformAdmin,
        };
    }
}
