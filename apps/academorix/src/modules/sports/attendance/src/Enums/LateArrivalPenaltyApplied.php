<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `penalty_applied` on `late-arrival`.
 *
 * Enum: none / consume_pass_anyway / fractional_consume / notify_only.
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
enum LateArrivalPenaltyApplied: string
{
    use Enum;

    case None = 'none';
    case ConsumePassAnyway = 'consume_pass_anyway';
    case FractionalConsume = 'fractional_consume';
    case NotifyOnly = 'notify_only';
}
