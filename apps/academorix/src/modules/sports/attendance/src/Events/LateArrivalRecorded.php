<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AttendanceOrchestrator computed status='late' during check-in and created a LateArrival satellite row. Fires alongside AttendanceRecorded in the same commit.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchLateArrivalNotification (to guardian + coach when policy.notification_config.notify_on_late)
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class LateArrivalRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $lateArrivalId  ULID (lat_*)
     * @param  string  $attendanceRecordId  ULID (atd_*)
     * @param  string  $tenantId  ULID
     * @param  string  $sessionId  ULID
     * @param  string  $athleteId  ULID
     * @param  int  $minutesLate  integer
     * @param  string  $policyId  ULID (apo_*) | null
     * @param  string  $penaltyApplied  'none' | 'consume_pass_anyway' | 'fractional_consume' | 'notify_only' | null
     */
    public function __construct(
        public readonly string $lateArrivalId,
        public readonly string $attendanceRecordId,
        public readonly string $tenantId,
        public readonly string $sessionId,
        public readonly string $athleteId,
        public readonly int $minutesLate,
        public readonly string $policyId,
        public readonly string $penaltyApplied,
    ) {
    }
}
