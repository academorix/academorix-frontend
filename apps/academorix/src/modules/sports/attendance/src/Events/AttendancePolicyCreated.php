<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Stackra\Attendance\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New AttendancePolicy committed.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidatePolicyResolverCache
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AttendancePolicyCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $policyId  ULID (apo_*)
     * @param  string  $tenantId  ULID
     * @param  string  $branchId  ULID | null
     * @param  string  $membershipPlanId  ULID | null
     * @param  string  $name  string
     * @param  int  $gracePeriodMinutes  integer
     * @param  int  $maxUnexcusedAbsencesPerPeriod  integer
     */
    public function __construct(
        public readonly string $policyId,
        public readonly string $tenantId,
        public readonly string $branchId,
        public readonly string $membershipPlanId,
        public readonly string $name,
        public readonly int $gracePeriodMinutes,
        public readonly int $maxUnexcusedAbsencesPerPeriod,
    ) {
    }
}
