<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Stackra\Attendance\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AbsenceRecord created — either by nightly ReconcileAttendanceJob (auto-detected unexcused no-show) OR by guardian self-report (POST /absences/report).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchFirstAbsenceNotification (to guardian + athlete when policy.notification_config.notify_athlete_on_first_absence)
 * - attendance::EvaluateAbsenceThresholdOnAbsenceRecorded (in-band rolling-window check)
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AbsenceRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $absenceRecordId  ULID (abs_*)
     * @param  string  $tenantId  ULID
     * @param  string  $sessionId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $athleteEnrollmentId  ULID
     * @param  string  $status  'unexcused' | 'excused' | 'no_show_advance_notice_given' | 'vacation' | 'medical' | 'bereavement'
     * @param  string  $absenceReason  string | null
     * @param  string  $reportedAt  ISO 8601
     * @param  string  $reportedByUserId  ULID | null
     * @param  bool  $countedTowardFreeze  boolean
     */
    public function __construct(
        public readonly string $absenceRecordId,
        public readonly string $tenantId,
        public readonly string $sessionId,
        public readonly string $athleteId,
        public readonly string $athleteEnrollmentId,
        public readonly string $status,
        public readonly string $absenceReason,
        public readonly string $reportedAt,
        public readonly string $reportedByUserId,
        public readonly bool $countedTowardFreeze,
    ) {
    }
}
