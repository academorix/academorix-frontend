<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * is_active flipped false → true. Refuses if it would create an active-policy collision at the same (branch, plan) specificity.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - cache::InvalidatePolicyResolverCache
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AttendancePolicyActivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $policyId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $activatedAt  ISO 8601
     * @param  string  $activatedByUserId  ULID
     */
    public function __construct(
        public readonly string $policyId,
        public readonly string $tenantId,
        public readonly string $activatedAt,
        public readonly string $activatedByUserId,
    ) {
    }
}
