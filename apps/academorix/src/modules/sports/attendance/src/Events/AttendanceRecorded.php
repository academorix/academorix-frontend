<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AttendanceOrchestrator committed the atomic (attendance-create + pass-consume) transaction. Fires exactly once per successful check-in.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchAttendanceRecordedNotification (opt-in — some guardians want per-session confirmation)
 * - cache::InvalidateAthleteAttendanceCache
 * - cache::InvalidateSessionRosterCache
 * - growth::analytics::TrackAttendanceEvent (Wave 8+)
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AttendanceRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $attendanceRecordId  ULID (atd_*)
     * @param  string  $tenantId  ULID
     * @param  string  $sessionId  ULID (ses_*)
     * @param  string  $athleteId  ULID (ath_*)
     * @param  string  $athleteEnrollmentId  ULID (aen_*)
     * @param  string  $consumedPassId  ULID (pss_*) | null
     * @param  string  $membershipId  ULID (mbr_*) | null
     * @param  string  $status  'present' | 'late' | 'left_early' | 'present_with_note'
     * @param  string  $checkedInAt  ISO 8601
     * @param  string  $checkedInVia  'self_kiosk' | 'coach_verified' | 'admin_recorded' | 'geofence' | 'qr_scan'
     * @param  string  $checkedInByUserId  ULID (usr_*) | null
     */
    public function __construct(
        public readonly string $attendanceRecordId,
        public readonly string $tenantId,
        public readonly string $sessionId,
        public readonly string $athleteId,
        public readonly string $athleteEnrollmentId,
        public readonly string $consumedPassId,
        public readonly string $membershipId,
        public readonly string $status,
        public readonly string $checkedInAt,
        public readonly string $checkedInVia,
        public readonly string $checkedInByUserId,
    ) {
    }
}
