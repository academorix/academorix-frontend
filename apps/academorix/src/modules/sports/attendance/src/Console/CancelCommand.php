<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Stackra\Attendance\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan attendance:cancel` — Cancel — attendance command.
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'attendance:cancel',
    description: 'Cancel — attendance command.',
)]
final class CancelCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Cancel — attendance command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Cancel — attendance command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
