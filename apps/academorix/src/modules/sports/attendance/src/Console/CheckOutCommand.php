<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan attendance:check-out` — Check out — attendance command.
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'attendance:check-out',
    description: 'Check out — attendance command.',
)]
final class CheckOutCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Check out — attendance command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Check out — attendance command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
