<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports attendance --force`.

declare(strict_types=1);

namespace Academorix\Attendance\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * AttendanceSettings — Attendance.
 *
 * Tenant-wide attendance workflow settings.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Attendance
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'attendance',
    label: 'Attendance',
    description: 'Tenant-wide attendance workflow settings.',
    icon: 'clipboard-document-check',
    scope: 'tenant',
    permission: 'attendance.viewAny',
    sortOrder: 48,
)]
class AttendanceSettings extends Settings
{

    // ── Check-in methods ──────────────────────────────────
    #[SettingGroup(label: 'Check-in methods', icon: 'device-tablet', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Enable self-service kiosk check-in', group: 'Check-in methods', sortOrder: 1)]
    public bool $enable_self_kiosk = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Enable QR-code check-in', group: 'Check-in methods', sortOrder: 2)]
    public bool $enable_qr_scan = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Enable GPS/geofence-verified check-in', group: 'Check-in methods', sortOrder: 3, helpText: 'Requires the products/geofencing module and Medium+ tier.')]
    public bool $enable_geofence = false;

    #[SettingField(controlType: ControlType::Toggle, label: 'Require coach signature on paper roster (S3-hosted image)', group: 'Check-in methods', sortOrder: 4)]
    public bool $require_signature_on_paper_roster = false;


    // ── Default attendance policy ──────────────────────────────────
    #[SettingGroup(label: 'Default attendance policy', icon: 'adjustments-horizontal', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Number, label: 'Grace period (minutes)', group: 'Default attendance policy', sortOrder: 1, min: 0, max: 120, helpText: 'Athletes arriving within this window count as present; past this = late.')]
    public int $default_grace_period_minutes = 15;

    #[SettingField(controlType: ControlType::Number, label: 'Late-after (minutes)', group: 'Default attendance policy', sortOrder: 2, min: 1, max: 120, helpText: 'Late arrivals past this threshold trigger the late-penalty rule. Must exceed grace_period_minutes.')]
    public int $default_late_after_minutes = 30;

    #[SettingField(controlType: ControlType::Select, label: 'Late-arrival penalty', group: 'Default attendance policy', sortOrder: 3, options: ['none' => 'None — log only', 'consume_pass_anyway' => 'Consume pass fully', 'fractional_pass_consume' => 'Half-pass consume', 'notify_only' => 'Notify guardian + coach'])]
    public string $default_late_penalty_type = 'notify_only';

    #[SettingField(controlType: ControlType::Number, label: 'Max unexcused absences (rolling)', group: 'Default attendance policy', sortOrder: 4, min: 1, max: 100)]
    public int $default_max_unexcused_absences = 3;

    #[SettingField(controlType: ControlType::Number, label: 'Rolling window (days)', group: 'Default attendance policy', sortOrder: 5, min: 7, max: 365)]
    public int $default_absence_freeze_period_days = 30;


    // ── No-show fees ──────────────────────────────────
    #[SettingGroup(label: 'No-show fees', icon: 'banknotes', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Auto-invoice for no-show fees', group: 'No-show fees', sortOrder: 1)]
    public bool $no_show_fee_enabled = false;

    #[SettingField(controlType: ControlType::Number, label: 'Default no-show fee (cents)', group: 'No-show fees', sortOrder: 2, min: 0, max: 100000)]
    public int $default_no_show_fee_cents = 0;

    #[SettingField(controlType: ControlType::Select, label: 'Currency', group: 'No-show fees', sortOrder: 3, options: ['USD' => 'USD', 'EUR' => 'EUR', 'GBP' => 'GBP', 'AUD' => 'AUD', 'CAD' => 'CAD'])]
    public string $default_no_show_fee_currency = 'USD';


    // ── Notifications ──────────────────────────────────
    #[SettingGroup(label: 'Notifications', icon: 'bell', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify athlete + guardian on first absence', group: 'Notifications', sortOrder: 1)]
    public bool $notify_athlete_on_first_absence = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify guardian on second absence', group: 'Notifications', sortOrder: 2)]
    public bool $notify_guardian_on_second_absence = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify admin on third absence (warning)', group: 'Notifications', sortOrder: 3)]
    public bool $notify_admin_on_third_absence = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Send coach a post-session attendance summary', group: 'Notifications', sortOrder: 4)]
    public bool $notify_coach_post_session_summary = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Weekly admin digest email', group: 'Notifications', sortOrder: 5)]
    public bool $weekly_admin_digest_enabled = false;


    // ── Freeze enforcement ──────────────────────────────────
    #[SettingGroup(label: 'Freeze enforcement', icon: 'pause', sortOrder: 5)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Auto-pause membership when unexcused absence threshold reached', group: 'Freeze enforcement', sortOrder: 1, helpText: 'When ON, AbsenceFreezeThresholdReached triggers membership.status=\'paused\'. Admin manually resumes.')]
    public bool $auto_freeze_membership_on_threshold = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'attendance';
    }
}
