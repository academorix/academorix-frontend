<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-guardian --force`.

declare(strict_types=1);

namespace Academorix\AthleteGuardian\Enums;

use Academorix\Authorization\Contracts\PermissionEnum;
use Academorix\Authorization\Enums\Guard;
use Academorix\Enum\Attributes\Description;
use Academorix\Enum\Attributes\Label;
use Academorix\Enum\Attributes\Meta;
use Academorix\Enum\Enum;

/**
 * Permissions the AthleteGuardian module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category AthleteGuardian
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum AthleteGuardianPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `athlete_guardians.viewAny` — Athlete Guardians ViewAny
     */
    #[Label('Athlete Guardians ViewAny')]
    #[Description('Athlete Guardians ViewAny')]
    case AthleteGuardiansViewAny = 'athlete_guardians.viewAny';

    /**
     * `athlete_guardians.viewAny.own` — Guardian-scoped listing — narrows to rows where user_id = current_user_id.
     */
    #[Label('Guardian-scoped listing — narrows to rows where user_id = current_user_id')]
    #[Description('Guardian-scoped listing — narrows to rows where user_id = current_user_id.')]
    case AthleteGuardiansViewAnyOwn = 'athlete_guardians.viewAny.own';

    /**
     * `athlete_guardians.view` — Athlete Guardians View
     */
    #[Label('Athlete Guardians View')]
    #[Description('Athlete Guardians View')]
    case AthleteGuardiansView = 'athlete_guardians.view';

    /**
     * `athlete_guardians.view.own` — Self-view for the guardian on their own row, OR guardian viewing another guardian on the same athlete (co-parent visibility).
     */
    #[Label('Self-view for the guardian on their own row, OR guardian viewing another guardia')]
    #[Description('Self-view for the guardian on their own row, OR guardian viewing another guardian on the same athlete (co-parent visibility).')]
    case AthleteGuardiansViewOwn = 'athlete_guardians.view.own';

    /**
     * `athlete_guardians.create` — Add a new guardian.
     */
    #[Label('Add a new guardian')]
    #[Description('Add a new guardian. Fires GuardianAdded + dispatches VerifyGuardianByEmailDomainJob. Refuses when the tier limit is exceeded.')]
    case AthleteGuardiansCreate = 'athlete_guardians.create';

    /**
     * `athlete_guardians.update` — Update role flags + relationship + notes.
     */
    #[Label('Update role flags + relationship + notes')]
    #[Description('Update role flags + relationship + notes. is_primary + verification transitions use dedicated permissions below.')]
    case AthleteGuardiansUpdate = 'athlete_guardians.update';

    /**
     * `athlete_guardians.update.own` — Guardians update their own role-flag preferences (can_receive_communications, can_pickup, can_authorise_medical_care — subject to has_legal_custody).
     */
    #[Label('Guardians update their own role-flag preferences (can_receive_communications, ca')]
    #[Description('Guardians update their own role-flag preferences (can_receive_communications, can_pickup, can_authorise_medical_care — subject to has_legal_custody). Relationship, has_legal_custody, and is_primary are admin-only.')]
    case AthleteGuardiansUpdateOwn = 'athlete_guardians.update.own';

    /**
     * `athlete_guardians.delete` — Soft-delete a guardian.
     */
    #[Label('Soft-delete a guardian')]
    #[Description('Soft-delete a guardian. Refused when the guardian is the sole active guardian for a minor athlete.')]
    case AthleteGuardiansDelete = 'athlete_guardians.delete';

    /**
     * `athlete_guardians.set-primary` — Atomic promote-and-demote of the primary guardian.
     */
    #[Label('Atomic promote-and-demote of the primary guardian')]
    #[Description('Atomic promote-and-demote of the primary guardian. Same permission covers both directions.')]
    case AthleteGuardiansSetPrimary = 'athlete_guardians.set-primary';

    /**
     * `athlete_guardians.verify` — HR verify action.
     */
    #[Label('HR verify action')]
    #[Description('HR verify action. Requires athlete_guardian_verification entitlement (Enterprise) for the endpoint to be enabled. Transitions pending/disputed → verified with document upload.')]
    case AthleteGuardiansVerify = 'athlete_guardians.verify';

    /**
     * `athlete_guardians.dispute` — Flag a verified guardian for review.
     */
    #[Label('Flag a verified guardian for review')]
    #[Description('Flag a verified guardian for review. Blocks consent recording until resolved.')]
    case AthleteGuardiansDispute = 'athlete_guardians.dispute';

    /**
     * `athlete_guardians.revoke` — Terminal revocation.
     */
    #[Label('Terminal revocation')]
    #[Description('Terminal revocation. Requires reason. Auto-flips is_primary + auto-elects a new primary.')]
    case AthleteGuardiansRevoke = 'athlete_guardians.revoke';

    /**
     * `athlete_guardians.audit.verification` — Compliance export — the tenant's guardian verification audit trail.
     */
    #[Label('Compliance export — the tenant\'s guardian verification audit trail')]
    #[Description('Compliance export — the tenant\'s guardian verification audit trail.')]
    case AthleteGuardiansAuditVerification = 'athlete_guardians.audit.verification';

    /**
     * `platform.athlete_guardians.viewAny` — Platform Athlete Guardians ViewAny
     */
    #[Label('Platform Athlete Guardians ViewAny')]
    #[Description('Platform Athlete Guardians ViewAny')]
    case PlatformAthleteGuardiansViewAny = 'platform.athlete_guardians.viewAny';

    /**
     * `platform.athlete_guardians.view` — Platform Athlete Guardians View
     */
    #[Label('Platform Athlete Guardians View')]
    #[Description('Platform Athlete Guardians View')]
    case PlatformAthleteGuardiansView = 'platform.athlete_guardians.view';

    /**
     * `platform.athlete_guardians.audit-verification` — Support-tooling verification audit.
     */
    #[Label('Support-tooling verification audit')]
    #[Description('Support-tooling verification audit. Super_admin only with two-person approval.')]
    case PlatformAthleteGuardiansAuditVerification = 'platform.athlete_guardians.audit-verification';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::AthleteGuardiansViewAny => Guard::Sanctum,
            self::AthleteGuardiansViewAnyOwn => Guard::Sanctum,
            self::AthleteGuardiansView => Guard::Sanctum,
            self::AthleteGuardiansViewOwn => Guard::Sanctum,
            self::AthleteGuardiansCreate => Guard::Sanctum,
            self::AthleteGuardiansUpdate => Guard::Sanctum,
            self::AthleteGuardiansUpdateOwn => Guard::Sanctum,
            self::AthleteGuardiansDelete => Guard::Sanctum,
            self::AthleteGuardiansSetPrimary => Guard::Sanctum,
            self::AthleteGuardiansVerify => Guard::Sanctum,
            self::AthleteGuardiansDispute => Guard::Sanctum,
            self::AthleteGuardiansRevoke => Guard::Sanctum,
            self::AthleteGuardiansAuditVerification => Guard::Sanctum,
            self::PlatformAthleteGuardiansViewAny => Guard::PlatformAdmin,
            self::PlatformAthleteGuardiansView => Guard::PlatformAdmin,
            self::PlatformAthleteGuardiansAuditVerification => Guard::PlatformAdmin,
        };
    }
}
