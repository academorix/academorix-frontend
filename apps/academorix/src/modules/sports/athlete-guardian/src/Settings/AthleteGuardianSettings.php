<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-guardian --force`.

declare(strict_types=1);

namespace Academorix\AthleteGuardian\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * AthleteGuardianSettings — Guardians.
 *
 * Tenant-wide guardian workflow defaults.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category AthleteGuardian
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'athlete_guardian',
    label: 'Guardians',
    description: 'Tenant-wide guardian workflow defaults.',
    icon: 'users',
    scope: 'tenant',
    permission: 'athlete_guardians.viewAny',
    sortOrder: 46,
)]
class AthleteGuardianSettings extends Settings
{

    // ── Verification ──────────────────────────────────
    #[SettingGroup(label: 'Verification', icon: 'shield-check', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Number, label: 'Pending TTL (days)', group: 'Verification', sortOrder: 1, min: 1, max: 90, helpText: 'Days after which pending guardians auto-revoke if never verified.')]
    public int $pending_ttl_days = 30;

    #[SettingField(controlType: ControlType::Number, label: 'Silent acceptance window (hours)', group: 'Verification', sortOrder: 2, min: 1, max: 168, helpText: 'Hours after Guardian Added at which silent-acceptance kicks in (Small + Medium tiers; Enterprise requires HR manual verify).')]
    public int $silent_acceptance_hours = 24;

    #[SettingField(controlType: ControlType::Text, label: 'Trusted email domains for auto-household', group: 'Verification', sortOrder: 3, helpText: 'Domains matching auto-verify guardians as household members.')]
    public array $trusted_domains = [];


    // ── Communications ──────────────────────────────────
    #[SettingGroup(label: 'Communications', icon: 'bell', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Number, label: 'Bounce threshold (permanent bounces)', group: 'Communications', sortOrder: 1, min: 1, max: 10, helpText: 'After N permanent bounces on a guardian\'s email, auto-disable can_receive_communications.')]
    public int $bounce_threshold = 3;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'athlete_guardian';
    }
}
