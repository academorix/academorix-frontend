<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-guardian --force`.

declare(strict_types=1);

namespace Stackra\AthleteGuardian\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan guardian:revoke` — Revoke — guardian command.
 *
 * @category AthleteGuardian
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'guardian:revoke',
    description: 'Revoke — guardian command.',
)]
final class RevokeCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Revoke — guardian command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Revoke — guardian command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
