<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-guardian --force`.

declare(strict_types=1);

namespace Academorix\AthleteGuardian\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Guardian soft-deleted (deleted_at set). If the removed guardian was is_primary=true, the observer auto-elects a new primary + fires PrimaryGuardianChanged in the same transaction.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchGuardianRemovedNotification (to admin + remaining guardians)
 * - athlete-guardian::ReleasePrimaryGuardianSlotOnGuardianRemoved (auto-elect new primary when applicable)
 *
 * @category AthleteGuardian
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class GuardianRemoved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $guardianId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $userId  ULID
     * @param  bool  $wasPrimary  boolean
     * @param  string  $removedByUserId  ULID
     * @param  string  $reason  string | null
     */
    public function __construct(
        public readonly string $guardianId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $userId,
        public readonly bool $wasPrimary,
        public readonly string $removedByUserId,
        public readonly string $reason,
    ) {
    }
}
