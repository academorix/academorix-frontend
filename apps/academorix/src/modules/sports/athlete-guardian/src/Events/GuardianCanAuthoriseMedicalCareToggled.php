<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-guardian --force`.

declare(strict_types=1);

namespace Stackra\AthleteGuardian\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * can_authorise_medical_care flag flipped. Medical clearance workflow (Wave 4+) refreshes.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchGuardianMedicalAuthorisationChangedNotification (to guardian + safeguarding_officer)
 *
 * @category AthleteGuardian
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class GuardianCanAuthoriseMedicalCareToggled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $guardianId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  bool  $fromValue  boolean
     * @param  bool  $toValue  boolean
     * @param  string  $changedByUserId  ULID
     */
    public function __construct(
        public readonly string $guardianId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly bool $fromValue,
        public readonly bool $toValue,
        public readonly string $changedByUserId,
    ) {
    }
}
