<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-guardian --force`.

declare(strict_types=1);

namespace Academorix\AthleteGuardian\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * AthleteGuardianProvisioner committed successfully. Fires once per guardian row.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchGuardianAddedNotification (to admin + existing guardians + the new guardian's User)
 * - athlete-guardian::VerifyGuardianByEmailDomainJob (dispatched immediately for auto-verify attempt)
 * - cache::InvalidateAthleteGuardiansCache
 * - cache::InvalidateGuardiansForUserCache
 *
 * @category AthleteGuardian
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class GuardianAdded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $guardianId  ULID (agu_*)
     * @param  string  $tenantId  ULID (ten_*)
     * @param  string  $athleteId  ULID (ath_*)
     * @param  string  $userId  ULID (usr_*)
     * @param  string  $relationship  string
     * @param  bool  $isPrimaryAtCreate  boolean
     * @param  bool  $hasLegalCustody  boolean
     * @param  string  $addedByUserId  ULID (usr_*)
     */
    public function __construct(
        public readonly string $guardianId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $userId,
        public readonly string $relationship,
        public readonly bool $isPrimaryAtCreate,
        public readonly bool $hasLegalCustody,
        public readonly string $addedByUserId,
    ) {
    }
}
