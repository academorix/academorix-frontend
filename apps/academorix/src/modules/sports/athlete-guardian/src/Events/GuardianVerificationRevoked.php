<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete-guardian --force`.

declare(strict_types=1);

namespace Academorix\AthleteGuardian\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * verification_status transitioned {pending, verified, disputed} → revoked. Terminal — the guardianship row stays for audit but is treated as inactive across every downstream consumer.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchGuardianRevokedNotification (to guardian + admin + safeguarding_officer — cannot-opt-out)
 *
 * @category AthleteGuardian
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class GuardianVerificationRevoked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $guardianId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $revokeReason  string
     * @param  string  $revokedAt  ISO 8601
     * @param  string  $revokedByUserId  ULID
     * @param  bool  $wasPrimary  boolean
     */
    public function __construct(
        public readonly string $guardianId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $revokeReason,
        public readonly string $revokedAt,
        public readonly string $revokedByUserId,
        public readonly bool $wasPrimary,
    ) {
    }
}
