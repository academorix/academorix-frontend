<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports performance --force`.

declare(strict_types=1);

namespace Stackra\Performance\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `unit` on `performance-test`.
 *
 * seconds / meters / centimeters / ml_per_kg_per_min / count / percent.
 *
 * @category Performance
 *
 * @since    0.1.0
 */
enum PerformanceTestUnit: string
{
    use Enum;

    case Seconds = 'seconds';
    case Meters = 'meters';
    case Centimeters = 'centimeters';
    case MlPerKgPerMin = 'ml_per_kg_per_min';
    case Count = 'count';
    case Percent = 'percent';
    case Kilograms = 'kilograms';
    case Watts = 'watts';
}
