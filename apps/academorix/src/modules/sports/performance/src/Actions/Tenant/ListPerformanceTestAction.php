<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports performance --force`.

declare(strict_types=1);

namespace Academorix\Performance\Actions\Tenant;

use Academorix\Performance\Contracts\Repositories\PerformanceTestRepositoryInterface;
use Academorix\Performance\Data\PerformanceTestData;
use Academorix\Routing\Attributes\AsAction;
use Academorix\Routing\Attributes\Middleware;
use Academorix\Routing\Concerns\AsController;
use Academorix\Routing\Attributes\Get;
use Illuminate\Http\Request;
use Illuminate\Pagination\LengthAwarePaginator;
use Spatie\LaravelData\PaginatedDataCollection;

/**
 * `GET /api/v1/performance-tests` — list action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Academorix\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Performance
 *
 * @since    0.1.0
 */
#[AsAction(name: 'performance.performance_tests.list')]
#[Get('/api/v1/performance-tests')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ListPerformanceTestAction
{
    use AsController;

    public function __construct(
        private readonly PerformanceTestRepositoryInterface $repository,
    ) {
    }

    /**
     * List `performance-tests` for the current caller.
     *
     * The `Repository` base picks up `filter[...]` / `sort=...` /
     * `include=...` query parameters via the `#[Filterable]` attribute
     * on the concrete repository — no per-Action wiring needed.
     *
     * @return PaginatedDataCollection<int, PerformanceTestData>  Paginated wire-visible DTOs.
     */
    public function __invoke(Request $request): PaginatedDataCollection
    {
        /** @var LengthAwarePaginator<int, \Academorix\Performance\Models\PerformanceTest> $page */
        $page = $this->repository->paginate(
            perPage: (int) $request->integer('per_page', 15),
        );

        return PerformanceTestData::collect($page, PaginatedDataCollection::class);
    }
}
