<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports performance --force`.

declare(strict_types=1);

namespace Academorix\Performance\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan performance:generate-report` — Generate report — performance command.
 *
 * @category Performance
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'performance:generate-report',
    description: 'Generate report — performance command.',
)]
final class GenerateReportCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Generate report — performance command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Generate report — performance command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
