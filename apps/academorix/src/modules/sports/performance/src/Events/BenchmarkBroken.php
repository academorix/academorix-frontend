<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports performance --force`.

declare(strict_types=1);

namespace Academorix\Performance\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Athlete beat existing benchmark (personal / team / branch / tenant / national). Cascade to awards.
 *
 * ## Consumers
 *
 * - awards::ConsiderBenchmarkAward
 * - notifications::NotifyBenchmarkBroken
 * - realtime::BroadcastBenchmark
 *
 * @category Performance
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class BenchmarkBroken implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $testResultId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $benchmarkScope  'personal'|'team'|'branch'|'tenant'|'national'
     * @param  string  $previousHolderAthleteId  ULID|null
     * @param  float  $previousValue  float
     * @param  float  $newValue  float
     */
    public function __construct(
        public readonly string $testResultId,
        public readonly string $athleteId,
        public readonly string $benchmarkScope,
        public readonly string $previousHolderAthleteId,
        public readonly float $previousValue,
        public readonly float $newValue,
    ) {
    }
}
