<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports performance --force`.

declare(strict_types=1);

namespace Academorix\Performance\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Test administered to an athlete. Result recorded.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - progress::UpdateAthleteProgress
 *
 * @category Performance
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class TestConducted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $testResultId  ULID (tst_*)
     * @param  string  $athleteId  ULID
     * @param  string  $testDefinitionId  ULID
     * @param  float  $rawValue  float
     * @param  string  $unit  string
     * @param  string  $administeredByUserId  ULID
     * @param  string  $conductedAt  ISO 8601
     */
    public function __construct(
        public readonly string $testResultId,
        public readonly string $athleteId,
        public readonly string $testDefinitionId,
        public readonly float $rawValue,
        public readonly string $unit,
        public readonly string $administeredByUserId,
        public readonly string $conductedAt,
    ) {
    }
}
