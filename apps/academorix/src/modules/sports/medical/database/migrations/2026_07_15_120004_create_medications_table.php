<?php

/**
 * @file database/migrations/2026_07_15_120004_create_medications_table.php
 *
 * @description
 * Create the `medications` table for the `Medication` entity.
 * Owned by the medical module. Every column + index reference
 * goes through {@see Stackra\Medical\Contracts\Data\MedicationInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

use Stackra\Medical\Contracts\Data\MedicationInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(MedicationInterface::TABLE, function (Blueprint $table): void {
            $table->string(MedicationInterface::ATTR_ID, 30)->primary();
            $table->string(MedicationInterface::ATTR_TENANT_ID, 30);
            $table->foreign(MedicationInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(MedicationInterface::ATTR_ATHLETE_ID, 30);
            $table->foreign(MedicationInterface::ATTR_ATHLETE_ID)->references('id')->on('athletes')->onDelete('cascade');
            $table->text(MedicationInterface::ATTR_NAME_ENCRYPTED);
            $table->text(MedicationInterface::ATTR_DOSAGE_ENCRYPTED)->nullable();
            $table->string(MedicationInterface::ATTR_FREQUENCY, 64)->nullable();
            $table->text(MedicationInterface::ATTR_PRESCRIBING_PROVIDER_ENCRYPTED)->nullable();
            $table->date(MedicationInterface::ATTR_STARTED_AT)->nullable();
            $table->date(MedicationInterface::ATTR_EXPIRES_AT)->nullable();
            $table->string(MedicationInterface::ATTR_STATUS, 16)->default('active');
            $table->text(MedicationInterface::ATTR_NOTES_ENCRYPTED)->nullable();
            $table->jsonb(MedicationInterface::ATTR_METADATA)->nullable();
            $table->timestampTz(MedicationInterface::ATTR_CREATED_AT);
            $table->timestampTz(MedicationInterface::ATTR_UPDATED_AT);
            $table->timestampTz(MedicationInterface::ATTR_DELETED_AT)->nullable();
            $table->index([MedicationInterface::ATTR_TENANT_ID, MedicationInterface::ATTR_ATHLETE_ID, MedicationInterface::ATTR_STATUS], 'medications_athlete_status_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(MedicationInterface::TABLE);
    }
};
