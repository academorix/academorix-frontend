<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

namespace Stackra\Medical\Actions\Tenant;

use Stackra\Medical\Contracts\Repositories\MedicalClearanceRepositoryInterface;
use Stackra\Medical\Data\MedicalClearanceData;
use Stackra\Medical\Data\Requests\CreateMedicalClearanceRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/medical-clearances` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Medical
 *
 * @since    0.1.0
 */
#[AsAction(name: 'medical.medical_clearances.create')]
#[Post('/api/v1/medical-clearances')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateMedicalClearanceAction
{
    use AsController;

    public function __construct(
        private readonly MedicalClearanceRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `medical-clearance` from the validated request payload.
     *
     * @param  CreateMedicalClearanceRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateMedicalClearanceRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(MedicalClearanceData::from($model), JsonResponse::HTTP_CREATED);
    }
}
