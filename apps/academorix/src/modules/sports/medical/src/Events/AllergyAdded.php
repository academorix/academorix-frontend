<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

namespace Stackra\Medical\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New Allergy committed. Coach summary cache invalidated.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - cache::InvalidateCoachAllergySummaryCache
 * - notifications::DispatchAllergyAddedNotification (medical + coach, redacted summary)
 *
 * @category Medical
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AllergyAdded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $allergyId  ULID (alg_*)
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $allergen  string
     * @param  string  $severity  'mild' | 'moderate' | 'severe' | 'life_threatening'
     */
    public function __construct(
        public readonly string $allergyId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $allergen,
        public readonly string $severity,
    ) {
    }
}
