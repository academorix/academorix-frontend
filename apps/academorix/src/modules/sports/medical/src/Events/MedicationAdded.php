<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

namespace Academorix\Medical\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New Medication committed.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 *
 * @category Medical
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class MedicationAdded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $medicationId  ULID (med_*)
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $status  'active' | 'discontinued' | 'completed' | 'on_hold'
     */
    public function __construct(
        public readonly string $medicationId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $status,
    ) {
    }
}
