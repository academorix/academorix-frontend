<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports medical --force`.

declare(strict_types=1);

namespace Academorix\Medical\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * New MedicalClearance committed with expires_at > now.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 10 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchClearanceIssuedNotification (guardian + athlete)
 * - medical::UpdateEligibilityGate
 *
 * @category Medical
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class ClearanceIssued implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $clearanceId  ULID (mcl_*)
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $issuedAt  ISO 8601
     * @param  string  $expiresAt  ISO 8601
     * @param  string  $scope  'all_activities' | 'limited' | 'pending_review'
     * @param  string  $issuedByUserId  ULID
     */
    public function __construct(
        public readonly string $clearanceId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $issuedAt,
        public readonly string $expiresAt,
        public readonly string $scope,
        public readonly string $issuedByUserId,
    ) {
    }
}
