<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Athlete\Actions\Tenant;

use Academorix\Athlete\Contracts\Repositories\AthleteRepositoryInterface;
use Academorix\Athlete\Data\AthleteData;
use Academorix\Athlete\Data\Requests\UpdateAthleteRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/athletes/{athlete}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
#[AsAction(name: 'athlete.athletes.update')]
#[Patch('/api/v1/athletes/{athlete}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateAthleteAction
{
    use AsController;

    public function __construct(
        private readonly AthleteRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `athlete` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateAthleteRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateAthleteRequestData $data): AthleteData
    {
        $model = $this->repository->update($id, $data->toArray());

        return AthleteData::from($model);
    }
}
