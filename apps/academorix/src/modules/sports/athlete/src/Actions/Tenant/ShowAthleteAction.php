<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Stackra\Athlete\Actions\Tenant;

use Stackra\Athlete\Contracts\Repositories\AthleteRepositoryInterface;
use Stackra\Athlete\Data\AthleteData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/athletes/{athlete}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
#[AsAction(name: 'athlete.athletes.show')]
#[Get('/api/v1/athletes/{athlete}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowAthleteAction
{
    use AsController;

    public function __construct(
        private readonly AthleteRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `athlete` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): AthleteData
    {
        return AthleteData::from($this->repository->findOrFail($id));
    }
}
