<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Athlete\Settings;

use Academorix\Settings\Attributes\AsSetting;
use Academorix\Settings\Attributes\SettingField;
use Academorix\Settings\Attributes\SettingGroup;
use Academorix\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * AthleteUserSettings — My athletes.
 *
 * Per-user preferences for how the /me/athletes surface + notification cadence work.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'athlete_user',
    label: 'My athletes',
    description: 'Per-user preferences for how the /me/athletes surface + notification cadence work.',
    icon: 'user-circle',
    scope: 'user',
    sortOrder: 60,
)]
class AthleteUserSettings extends Settings
{

    // ── Reminders ──────────────────────────────────
    #[SettingGroup(label: 'Reminders', icon: 'bell', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me when my child\'s age group changes', group: 'Reminders', sortOrder: 1)]
    public bool $receive_age_group_change_notifications = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify me when my child\'s consents are expiring', group: 'Reminders', sortOrder: 2)]
    public bool $receive_consent_expiring_reminders = true;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'athlete_user';
    }
}
