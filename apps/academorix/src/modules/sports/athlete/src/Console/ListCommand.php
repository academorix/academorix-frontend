<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Stackra\Athlete\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan athlete:list` — List every athlete row.
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'athlete:list',
    description: 'List every athlete row.',
)]
final class ListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List every athlete row', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List every athlete row completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
