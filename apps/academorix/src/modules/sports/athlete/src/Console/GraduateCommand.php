<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Stackra\Athlete\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan athlete:graduate` — Graduate — athlete command.
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'athlete:graduate',
    description: 'Graduate — athlete command.',
)]
final class GraduateCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Graduate — athlete command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Graduate — athlete command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
