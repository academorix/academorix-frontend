<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Athlete\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Athlete.consent_recorded_at transitioned null → non-null OR was refreshed with new values (re-consent). Fires alongside AthletePhotoConsentChanged when photo consent specifically flipped.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — safeguarding compliance)
 * - activity::WriteToActivityLog
 * - notifications::DispatchAthleteConsentRecordedNotification (to guardian + admin)
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AthleteConsentRecorded implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $athleteId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $consentRecordedByUserId  ULID (usually the primary AthleteGuardian for minors)
     * @param  bool  $consentPhotoRelease  boolean
     * @param  bool  $consentMedicalDisclosure  boolean
     * @param  bool  $consentThirdParty  boolean
     * @param  string  $consentRecordedAt  ISO 8601
     * @param  string  $expiresAt  ISO 8601 | null (12 months from recorded_at when athlete_consent_workflow entitlement is on)
     */
    public function __construct(
        public readonly string $athleteId,
        public readonly string $tenantId,
        public readonly string $consentRecordedByUserId,
        public readonly bool $consentPhotoRelease,
        public readonly bool $consentMedicalDisclosure,
        public readonly bool $consentThirdParty,
        public readonly string $consentRecordedAt,
        public readonly string $expiresAt,
    ) {
    }
}
