<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Athlete\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Any consent_* flag transitioned true → false. Consent revocation is a compliance-critical event. Fires per revocation (multiple flags flipping in one write emits multiple events for auditability).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 * - notifications::DispatchAthleteConsentRevokedNotification (to guardian + admin — cannot-opt-out; compliance-critical)
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AthleteConsentRevoked implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $athleteId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $revokedByUserId  ULID
     * @param  string  $revokedConsent  'photo_release' | 'medical_disclosure' | 'third_party'
     * @param  string  $reason  string | null
     * @param  string  $revokedAt  ISO 8601
     */
    public function __construct(
        public readonly string $athleteId,
        public readonly string $tenantId,
        public readonly string $revokedByUserId,
        public readonly string $revokedConsent,
        public readonly string $reason,
        public readonly string $revokedAt,
    ) {
    }
}
