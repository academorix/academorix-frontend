<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports athlete --force`.

declare(strict_types=1);

namespace Academorix\Athlete\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Athlete.status transitioned active → graduated. Soft-terminal — the row is retained for alumni relations.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchAthleteGraduatedNotification (to guardian + admin — celebratory)
 * - athlete-enrollment::CloseActiveEnrollmentsOnAthleteGraduated (Wave 3+ — every active enrollment transitions to 'completed')
 *
 * @category Athlete
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AthleteGraduated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $athleteId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $graduatedAt  YYYY-MM-DD
     * @param  string  $graduatedByUserId  ULID
     * @param  int  $daysOfMembership  integer (created_at → graduated_at)
     */
    public function __construct(
        public readonly string $athleteId,
        public readonly string $tenantId,
        public readonly string $graduatedAt,
        public readonly string $graduatedByUserId,
        public readonly int $daysOfMembership,
    ) {
    }
}
