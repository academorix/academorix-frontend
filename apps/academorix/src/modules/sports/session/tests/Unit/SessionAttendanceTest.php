<?php

declare(strict_types=1);

use Stackra\Session\Models\SessionAttendance;

it('is a placeholder — replace with actual SessionAttendance unit tests', function (): void {
    expect(SessionAttendance::class)->toBeString();
});
