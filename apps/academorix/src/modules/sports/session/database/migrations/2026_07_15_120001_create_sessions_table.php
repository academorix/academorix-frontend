<?php

/**
 * @file database/migrations/2026_07_15_120001_create_sessions_table.php
 *
 * @description
 * Create the `sessions` table for the `Session` entity.
 * Owned by the session module. Every column + index reference
 * goes through {@see Academorix\Session\Contracts\Data\SessionInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

use Academorix\Session\Contracts\Data\SessionInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(SessionInterface::TABLE, function (Blueprint $table): void {
            $table->string(SessionInterface::ATTR_ID, 64)->primary();
            $table->string(SessionInterface::ATTR_TENANT_ID, 64);
            $table->foreign(SessionInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(SessionInterface::ATTR_BRANCH_ID, 64);
            $table->foreign(SessionInterface::ATTR_BRANCH_ID)->references('id')->on('branches')->onDelete('restrict');
            $table->string(SessionInterface::ATTR_RESOURCE_BOOKING_ID, 64);
            $table->foreign(SessionInterface::ATTR_RESOURCE_BOOKING_ID)->references('id')->on('resource_bookings')->onDelete('restrict');
            $table->string(SessionInterface::ATTR_TEAM_ID, 64)->nullable();
            $table->foreign(SessionInterface::ATTR_TEAM_ID)->references('id')->on('teams')->onDelete('restrict');
            $table->string(SessionInterface::ATTR_SEASON_ID, 64)->nullable();
            $table->foreign(SessionInterface::ATTR_SEASON_ID)->references('id')->on('seasons')->onDelete('restrict');
            $table->string(SessionInterface::ATTR_EVENT_ID, 64)->nullable();
            $table->foreign(SessionInterface::ATTR_EVENT_ID)->references('id')->on('events')->onDelete('restrict');
            $table->string(SessionInterface::ATTR_SPORT_KEY, 32)->nullable();
            $table->string(SessionInterface::ATTR_NAME, 128);
            $table->string(SessionInterface::ATTR_SLUG, 64)->nullable();
            $table->text(SessionInterface::ATTR_DESCRIPTION)->nullable();
            $table->string(SessionInterface::ATTR_SESSION_TYPE, 24);
            $table->string(SessionInterface::ATTR_HEAD_COACH_ID, 64)->nullable();
            $table->foreign(SessionInterface::ATTR_HEAD_COACH_ID)->references('id')->on('coaches')->onDelete('restrict');
            $table->jsonb(SessionInterface::ATTR_ASSISTANT_COACH_IDS)->nullable();
            $table->string(SessionInterface::ATTR_SUPERVISING_STAFF_ID, 64)->nullable();
            $table->foreign(SessionInterface::ATTR_SUPERVISING_STAFF_ID)->references('id')->on('staff')->onDelete('restrict');
            $table->integer(SessionInterface::ATTR_ATTENDANCE_CAPACITY)->nullable();
            $table->integer(SessionInterface::ATTR_ATTENDANCE_MIN)->nullable();
            $table->string(SessionInterface::ATTR_AGE_GROUP_ID, 64)->nullable();
            $table->foreign(SessionInterface::ATTR_AGE_GROUP_ID)->references('id')->on('age_groups')->onDelete('restrict');
            $table->string(SessionInterface::ATTR_GENDER_CATEGORY, 16)->nullable();
            $table->string(SessionInterface::ATTR_CURRICULUM_TOPIC, 256)->nullable();
            $table->string(SessionInterface::ATTR_LESSON_PLAN_URL, 512)->nullable();
            $table->jsonb(SessionInterface::ATTR_EQUIPMENT_NEEDED)->nullable();
            $table->string(SessionInterface::ATTR_STATUS, 24)->default('scheduled');
            $table->integer(SessionInterface::ATTR_CHECKED_IN_ATHLETES_COUNT)->default(0);
            $table->integer(SessionInterface::ATTR_ABSENT_ATHLETES_COUNT)->default(0);
            $table->text(SessionInterface::ATTR_CANCELLATION_REASON)->nullable();
            $table->timestampTz(SessionInterface::ATTR_CANCELLATION_NOTICE_AT)->nullable();
            $table->text(SessionInterface::ATTR_SESSION_NOTES)->nullable();
            $table->decimal(SessionInterface::ATTR_SESSION_RATING, 15, 4)->nullable();
            $table->integer(SessionInterface::ATTR_SESSION_RATING_COUNT)->default(0);
            $table->decimal(SessionInterface::ATTR_COACH_RATING_INPUT, 15, 4)->nullable();
            $table->boolean(SessionInterface::ATTR_SAFEGUARDING_INCIDENT_FLAG)->default(false);
            $table->jsonb(SessionInterface::ATTR_METADATA)->nullable();
            $table->string(SessionInterface::ATTR_CREATED_BY, 64)->nullable();
            $table->string(SessionInterface::ATTR_UPDATED_BY, 64)->nullable();
            $table->string(SessionInterface::ATTR_DELETED_BY, 64)->nullable();
            $table->timestampTz(SessionInterface::ATTR_DELETED_AT)->nullable();
            $table->timestampTz(SessionInterface::ATTR_CREATED_AT);
            $table->timestampTz(SessionInterface::ATTR_UPDATED_AT);
            $table->index([SessionInterface::ATTR_TENANT_ID, SessionInterface::ATTR_STATUS], 'sessions_status_idx');
            $table->index([SessionInterface::ATTR_TENANT_ID, SessionInterface::ATTR_BRANCH_ID, SessionInterface::ATTR_STATUS], 'sessions_branch_status_idx');
        });

        DB::statement('CREATE UNIQUE INDEX sessions_scope_slug_unique ON ' . SessionInterface::TABLE . ' (' . SessionInterface::ATTR_TENANT_ID . ', ' . SessionInterface::ATTR_SLUG . ') WHERE deleted_at IS NULL AND slug IS NOT NULL');
        DB::statement('CREATE UNIQUE INDEX sessions_booking_unique ON ' . SessionInterface::TABLE . ' (' . SessionInterface::ATTR_TENANT_ID . ', ' . SessionInterface::ATTR_RESOURCE_BOOKING_ID . ') WHERE deleted_at IS NULL');
        DB::statement('CREATE INDEX sessions_team_idx ON ' . SessionInterface::TABLE . ' (' . SessionInterface::ATTR_TENANT_ID . ', ' . SessionInterface::ATTR_TEAM_ID . ') WHERE team_id IS NOT NULL');
        DB::statement('CREATE INDEX sessions_season_idx ON ' . SessionInterface::TABLE . ' (' . SessionInterface::ATTR_TENANT_ID . ', ' . SessionInterface::ATTR_SEASON_ID . ') WHERE season_id IS NOT NULL');
        DB::statement('CREATE INDEX sessions_event_idx ON ' . SessionInterface::TABLE . ' (' . SessionInterface::ATTR_TENANT_ID . ', ' . SessionInterface::ATTR_EVENT_ID . ') WHERE event_id IS NOT NULL');
        DB::statement('CREATE INDEX sessions_head_coach_idx ON ' . SessionInterface::TABLE . ' (' . SessionInterface::ATTR_TENANT_ID . ', ' . SessionInterface::ATTR_HEAD_COACH_ID . ') WHERE head_coach_id IS NOT NULL');
        DB::statement('CREATE INDEX sessions_sport_idx ON ' . SessionInterface::TABLE . ' (' . SessionInterface::ATTR_TENANT_ID . ', ' . SessionInterface::ATTR_SPORT_KEY . ') WHERE sport_key IS NOT NULL');
        DB::statement('CREATE INDEX sessions_safeguarding_idx ON ' . SessionInterface::TABLE . ' (' . SessionInterface::ATTR_TENANT_ID . ', ' . SessionInterface::ATTR_SAFEGUARDING_INCIDENT_FLAG . ') WHERE safeguarding_incident_flag = true');
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(SessionInterface::TABLE);
    }
};
