<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Academorix\Session\Services;

use Academorix\Session\Contracts\Services\SessionPhaseCalculatorInterface;
use Illuminate\Container\Attributes\Scoped;
use Academorix\Session\Contracts\Repositories\SessionAttendanceRepositoryInterface;

/**
 * Concrete service — SessionPhaseCalculator.
 *
 * Implements {@see SessionPhaseCalculatorInterface}. `#[Scoped]` because most
 * services touch request state (current tenant, current user,
 * correlation id) — see `.kiro/steering/octane-first-di.md`. If
 * the service is provably stateless, promote to `#[Singleton]`.
 *
 * @category Session
 *
 * @since    0.1.0
 */
#[Scoped]
final class SessionPhaseCalculator implements SessionPhaseCalculatorInterface
{
    /**
     * @param  SessionAttendanceRepositoryInterface  $sessionAttendanceRepository  Primary persistence boundary.
     */
    public function __construct(
        private readonly SessionAttendanceRepositoryInterface $sessionAttendanceRepository,
    ) {
    }
}
