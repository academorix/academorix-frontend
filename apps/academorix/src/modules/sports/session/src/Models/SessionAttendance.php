<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Stackra\Session\Models;

use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Attributes\WithoutIncrementing;
use Illuminate\Database\Eloquent\Model;
use Stackra\Session\Contracts\Data\SessionAttendanceInterface;
use Stackra\Session\Database\Factories\SessionAttendanceFactory;
use Stackra\Foundation\Concerns\Filterable;
use Stackra\Foundation\Concerns\HasMetadata;
use Stackra\Session\Concerns\BelongsToSession;
use Stackra\Session\Policies\SessionAttendancePolicy;
use Stackra\Tenancy\Concerns\BelongsToTenant;
use Illuminate\Database\Eloquent\Attributes\UsePolicy;
use Illuminate\Database\Eloquent\Concerns\HasUlids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Mattiverse\Userstamps\Traits\Userstamps;
use OwenIt\Auditing\Auditable;
use OwenIt\Auditing\Contracts\Auditable as AuditableContract;
use Spatie\Activitylog\Traits\LogsActivity;

/**
 * Eloquent model for a SessionAttendance.
 *
 * Pivot row attaching an Athlete to a Session with attendance state.
 *
 * @category Session
 *
 * @since    0.1.0
 */
#[Table(name: SessionAttendanceInterface::TABLE, key: SessionAttendanceInterface::PRIMARY_KEY, keyType: SessionAttendanceInterface::KEY_TYPE)]
#[Fillable([
    SessionAttendanceInterface::ATTR_TENANT_ID,
        SessionAttendanceInterface::ATTR_SESSION_ID,
        SessionAttendanceInterface::ATTR_ATHLETE_ID,
        SessionAttendanceInterface::ATTR_ATHLETE_ENROLLMENT_ID,
        SessionAttendanceInterface::ATTR_STATUS,
        SessionAttendanceInterface::ATTR_CHECKED_IN_AT,
        SessionAttendanceInterface::ATTR_CHECKED_OUT_AT,
        SessionAttendanceInterface::ATTR_LATE_MINUTES,
        SessionAttendanceInterface::ATTR_NOTES,
        SessionAttendanceInterface::ATTR_METADATA,
])]
#[UseFactory(SessionAttendanceFactory::class)]
#[WithoutIncrementing]
#[UsePolicy(SessionAttendancePolicy::class)]
final class SessionAttendance extends Model implements SessionAttendanceInterface, AuditableContract
{
    use HasFactory;
    use HasUlids;
    use BelongsToTenant;
    use BelongsToSession;
    use HasMetadata;
    use Userstamps;
    use Auditable;
    use LogsActivity;
    use Filterable;
    use SoftDeletes;

    /**
     * Cast map — from the blueprint's `x-eloquent.casts`.
     *
     * @var array<string, string>
     */
    protected $casts = [
        SessionAttendanceInterface::ATTR_STATUS => AttendanceStatus::class,
        SessionAttendanceInterface::ATTR_CHECKED_IN_AT => 'datetime',
        SessionAttendanceInterface::ATTR_CHECKED_OUT_AT => 'datetime',
        SessionAttendanceInterface::ATTR_LATE_MINUTES => 'integer',
        SessionAttendanceInterface::ATTR_METADATA => 'array',
    ];
}
