<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Academorix\Session\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan session:archive` — Archive — session command.
 *
 * @category Session
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'session:archive',
    description: 'Archive — session command.',
)]
final class ArchiveCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Archive — session command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Archive — session command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
