<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Academorix\Session\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Console\Commands\BaseCommand;

/**
 * `php artisan session:aggregate-ratings` — Aggregate ratings — session command.
 *
 * @category Session
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'session:aggregate-ratings',
    description: 'Aggregate ratings — session command.',
)]
final class AggregateRatingsCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Aggregate ratings — session command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Aggregate ratings — session command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
