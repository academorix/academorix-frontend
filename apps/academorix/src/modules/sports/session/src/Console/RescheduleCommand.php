<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Academorix\Session\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan session:reschedule` — Reschedule — session command.
 *
 * @category Session
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'session:reschedule',
    description: 'Reschedule — session command.',
)]
final class RescheduleCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Reschedule — session command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Reschedule — session command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
