<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Stackra\Session\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan session:update-curriculum` — Update curriculum — session command.
 *
 * @category Session
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'session:update-curriculum',
    description: 'Update curriculum — session command.',
)]
final class UpdateCurriculumCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Update curriculum — session command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Update curriculum — session command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
