<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Academorix\Session\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Session.status transitioned in_progress -> completed. Fires from CompleteFinishedSessionsJob at ends_at + 15-minute grace.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchSessionRatingRequestNotification (opt-in to athletes + guardians — gated by session_coach_ratings entitlement)
 *
 * @category Session
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SessionCompleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $completedAt  ISO 8601
     * @param  int  $finalCheckedInCount  integer
     * @param  int  $finalAbsentCount  integer
     * @param  string  $attendanceRate  number (0.0-1.0)
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $tenantId,
        public readonly string $completedAt,
        public readonly int $finalCheckedInCount,
        public readonly int $finalAbsentCount,
        public readonly string $attendanceRate,
    ) {
    }
}
