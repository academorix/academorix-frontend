<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Academorix\Session\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * head_coach_id changed post-scheduled (was NOT null, now different). AUDIT-CRITICAL when < 24h before session starts_at — parents planned around the coach identity for minor participants. Safeguarding-adjacent.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years, critical severity when <24h)
 * - activity::WriteToActivityLog
 * - notifications::DispatchSessionCoachReassignedNotification (cannot-opt-out to registered athletes + guardians for minors when <24h)
 *
 * @category Session
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SessionCoachReassigned implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $oldCoachId  ULID | null
     * @param  string  $newCoachId  ULID | null
     * @param  string  $reassignedBy  ULID
     * @param  string  $noticeHoursBeforeStart  number
     * @param  string  $auditSeverity  'info' | 'warn' | 'critical' (critical when <24h AND minors registered)
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $tenantId,
        public readonly string $oldCoachId,
        public readonly string $newCoachId,
        public readonly string $reassignedBy,
        public readonly string $noticeHoursBeforeStart,
        public readonly string $auditSeverity,
    ) {
    }
}
