<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Stackra\Session\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SessionAttendance.status transitioned to late (checked in past grace window).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Session
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SessionAthleteMarkedLate implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID
     * @param  string  $attendanceId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $athleteId  ULID
     * @param  string  $checkedInAt  ISO 8601
     * @param  int  $lateMinutes  integer
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $attendanceId,
        public readonly string $tenantId,
        public readonly string $athleteId,
        public readonly string $checkedInAt,
        public readonly int $lateMinutes,
    ) {
    }
}
