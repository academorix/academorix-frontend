<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Academorix\Session\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * safeguarding_incident_flag set true on a session where any registered athlete is a minor. TOP-SEVERITY audit event — regulator-facing. Never opt-out.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years or 25 years per jurisdiction, critical severity, regulator-flagged)
 * - activity::WriteToActivityLog (redacted view for staff)
 * - notifications::DispatchSessionSafeguardingIncidentNotification (cannot-opt-out; to tenant owner + admin + safeguarding officer)
 *
 * @category Session
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SessionMinorSafeguardingIncident implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $flaggedAt  ISO 8601
     * @param  string  $flaggedBy  ULID
     * @param  int  $minorAthletesPresentCount  integer
     * @param  bool  $sessionNotesProvided  boolean
     * @param  string  $auditSeverity  critical
     * @param  bool  $requiresRegulatorNotification  boolean
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $tenantId,
        public readonly string $flaggedAt,
        public readonly string $flaggedBy,
        public readonly int $minorAthletesPresentCount,
        public readonly bool $sessionNotesProvided,
        public readonly string $auditSeverity,
        public readonly bool $requiresRegulatorNotification,
    ) {
    }
}
