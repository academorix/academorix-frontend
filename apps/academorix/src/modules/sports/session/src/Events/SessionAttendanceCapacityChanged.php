<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Academorix\Session\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * attendance_capacity OR attendance_min changed.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category Session
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SessionAttendanceCapacityChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID
     * @param  string  $tenantId  ULID
     * @param  int  $before  {'attendance_capacity': 'integer | null', 'attendance_min': 'integer | null'}
     * @param  int  $after  {'attendance_capacity': 'integer | null', 'attendance_min': 'integer | null'}
     * @param  string  $updatedBy  ULID
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $tenantId,
        public readonly int $before,
        public readonly int $after,
        public readonly string $updatedBy,
    ) {
    }
}
