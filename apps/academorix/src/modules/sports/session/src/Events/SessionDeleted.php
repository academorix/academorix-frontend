<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports session --force`.

declare(strict_types=1);

namespace Stackra\Session\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Hard-delete via PurgeCompletedSessionsJob (past 7-year retention + no inbound refs) OR platform-support two-person CLI path.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — the audit outlives the row)
 *
 * @category Session
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SessionDeleted implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $mode  'soft' | 'hard'
     * @param  string  $deletedBy  ULID
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $tenantId,
        public readonly string $mode,
        public readonly string $deletedBy,
    ) {
    }
}
