<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports private-sessions --force`.

declare(strict_types=1);

namespace Academorix\PrivateSessions\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Facility slot booked. status: approved → scheduled. Credits deducted.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - facility::CreateResourceBooking
 * - notifications::NotifyScheduleConfirmed
 *
 * @category PrivateSessions
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class PrivateSessionScheduled implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sessionId  ULID (pvs_*)
     * @param  string  $requestId  ULID
     * @param  string  $facilityId  ULID
     * @param  string  $scheduledAt  ISO 8601
     * @param  int  $creditsUsed  int
     */
    public function __construct(
        public readonly string $sessionId,
        public readonly string $requestId,
        public readonly string $facilityId,
        public readonly string $scheduledAt,
        public readonly int $creditsUsed,
    ) {
    }
}
