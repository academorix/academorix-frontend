<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports private-sessions --force`.

declare(strict_types=1);

namespace Stackra\PrivateSessions\Enums;

use Stackra\Authorization\Contracts\PermissionEnum;
use Stackra\Authorization\Enums\Guard;
use Stackra\Enum\Attributes\Description;
use Stackra\Enum\Attributes\Label;
use Stackra\Enum\Attributes\Meta;
use Stackra\Enum\Enum;

/**
 * Permissions the PrivateSessions module contributes.
 *
 * Every case pairs a `PermissionEnum` contract entry with the guard
 * it binds to (tenant reads via `sanctum`; platform-admin reads via
 * `platform_admin`). See `.kiro/steering/enum-db-seed-dual-source.md`
 * for the enum-primary catalogue pattern.
 *
 * @category PrivateSessions
 *
 * @since    0.1.0
 */
#[Meta([Label::class, Description::class])]
enum PrivateSessionsPermission: string implements PermissionEnum
{
    use Enum;

    /**
     * `private-sessions.requests.viewAny` — Private-sessions Requests ViewAny
     */
    #[Label('Private-sessions Requests ViewAny')]
    #[Description('Private-sessions Requests ViewAny')]
    case PrivateSessionsRequestsViewAny = 'private-sessions.requests.viewAny';

    /**
     * `private-sessions.requests.view` — Private-sessions Requests View
     */
    #[Label('Private-sessions Requests View')]
    #[Description('Private-sessions Requests View')]
    case PrivateSessionsRequestsView = 'private-sessions.requests.view';

    /**
     * `private-sessions.requests.create` — Private-sessions Requests Create
     */
    #[Label('Private-sessions Requests Create')]
    #[Description('Private-sessions Requests Create')]
    case PrivateSessionsRequestsCreate = 'private-sessions.requests.create';

    /**
     * `private-sessions.requests.approve` — Private-sessions Requests Approve
     */
    #[Label('Private-sessions Requests Approve')]
    #[Description('Private-sessions Requests Approve')]
    case PrivateSessionsRequestsApprove = 'private-sessions.requests.approve';

    /**
     * `private-sessions.requests.reject` — Private-sessions Requests Reject
     */
    #[Label('Private-sessions Requests Reject')]
    #[Description('Private-sessions Requests Reject')]
    case PrivateSessionsRequestsReject = 'private-sessions.requests.reject';

    /**
     * `private-sessions.sessions.viewAny` — Private-sessions Sessions ViewAny
     */
    #[Label('Private-sessions Sessions ViewAny')]
    #[Description('Private-sessions Sessions ViewAny')]
    case PrivateSessionsSessionsViewAny = 'private-sessions.sessions.viewAny';

    /**
     * `private-sessions.sessions.view` — Private-sessions Sessions View
     */
    #[Label('Private-sessions Sessions View')]
    #[Description('Private-sessions Sessions View')]
    case PrivateSessionsSessionsView = 'private-sessions.sessions.view';

    /**
     * `private-sessions.sessions.cancel` — Private-sessions Sessions Cancel
     */
    #[Label('Private-sessions Sessions Cancel')]
    #[Description('Private-sessions Sessions Cancel')]
    case PrivateSessionsSessionsCancel = 'private-sessions.sessions.cancel';

    /**
     * `private-sessions.sessions.mark-completed` — Private-sessions Sessions Mark-completed
     */
    #[Label('Private-sessions Sessions Mark-completed')]
    #[Description('Private-sessions Sessions Mark-completed')]
    case PrivateSessionsSessionsMarkCompleted = 'private-sessions.sessions.mark-completed';

    /**
     * `private-sessions.credits.viewAny` — Private-sessions Credits ViewAny
     */
    #[Label('Private-sessions Credits ViewAny')]
    #[Description('Private-sessions Credits ViewAny')]
    case PrivateSessionsCreditsViewAny = 'private-sessions.credits.viewAny';

    /**
     * `private-sessions.credits.view` — Private-sessions Credits View
     */
    #[Label('Private-sessions Credits View')]
    #[Description('Private-sessions Credits View')]
    case PrivateSessionsCreditsView = 'private-sessions.credits.view';

    /**
     * `private-sessions.credits.purchase` — Private-sessions Credits Purchase
     */
    #[Label('Private-sessions Credits Purchase')]
    #[Description('Private-sessions Credits Purchase')]
    case PrivateSessionsCreditsPurchase = 'private-sessions.credits.purchase';

    /**
     * The Laravel guard this permission binds to. Tenant-plane
     * permissions target `sanctum`; platform-admin permissions target
     * `platform_admin`.
     */
    public function guard(): Guard
    {
        return match ($this) {
            self::PrivateSessionsRequestsViewAny => Guard::Sanctum,
            self::PrivateSessionsRequestsView => Guard::Sanctum,
            self::PrivateSessionsRequestsCreate => Guard::Sanctum,
            self::PrivateSessionsRequestsApprove => Guard::Sanctum,
            self::PrivateSessionsRequestsReject => Guard::Sanctum,
            self::PrivateSessionsSessionsViewAny => Guard::Sanctum,
            self::PrivateSessionsSessionsView => Guard::Sanctum,
            self::PrivateSessionsSessionsCancel => Guard::Sanctum,
            self::PrivateSessionsSessionsMarkCompleted => Guard::Sanctum,
            self::PrivateSessionsCreditsViewAny => Guard::Sanctum,
            self::PrivateSessionsCreditsView => Guard::Sanctum,
            self::PrivateSessionsCreditsPurchase => Guard::Sanctum,
        };
    }
}
