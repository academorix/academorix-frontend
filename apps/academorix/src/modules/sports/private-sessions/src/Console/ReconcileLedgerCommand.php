<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports private-sessions --force`.

declare(strict_types=1);

namespace Academorix\PrivateSessions\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan private-sessions:reconcile-ledger` — Reconcile ledger — private sessions command.
 *
 * @category PrivateSessions
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'private-sessions:reconcile-ledger',
    description: 'Reconcile ledger — private sessions command.',
)]
final class ReconcileLedgerCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Reconcile ledger — private sessions command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Reconcile ledger — private sessions command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
