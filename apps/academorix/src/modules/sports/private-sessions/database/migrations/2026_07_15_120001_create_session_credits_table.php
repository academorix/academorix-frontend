<?php

/**
 * @file database/migrations/2026_07_15_120001_create_session_credits_table.php
 *
 * @description
 * Create the `session_credits` table for the `SessionCredit` entity.
 * Owned by the private-sessions module. Every column + index reference
 * goes through {@see Stackra\PrivateSessions\Contracts\Data\SessionCreditInterface} per the `models` steering
 * rule §1 — no raw string literals.
 */
// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports private-sessions --force`.

declare(strict_types=1);

use Stackra\PrivateSessions\Contracts\Data\SessionCreditInterface;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class() extends Migration {
    /**
     * Run the migration.
     */
    public function up(): void
    {
        Schema::create(SessionCreditInterface::TABLE, function (Blueprint $table): void {
            $table->string(SessionCreditInterface::ATTR_ID, 30)->primary();
            $table->string(SessionCreditInterface::ATTR_TENANT_ID, 30);
            $table->foreign(SessionCreditInterface::ATTR_TENANT_ID)->references('id')->on('tenants')->onDelete('cascade');
            $table->string(SessionCreditInterface::ATTR_ATHLETE_ID, 30);
            $table->foreign(SessionCreditInterface::ATTR_ATHLETE_ID)->references('id')->on('athletes')->onDelete('restrict');
            $table->string(SessionCreditInterface::ATTR_PURCHASED_BY_USER_ID, 30);
            $table->foreign(SessionCreditInterface::ATTR_PURCHASED_BY_USER_ID)->references('id')->on('users')->onDelete('restrict');
            $table->string(SessionCreditInterface::ATTR_KIND, 24);
            $table->integer(SessionCreditInterface::ATTR_TOTAL_PURCHASED);
            $table->integer(SessionCreditInterface::ATTR_TOTAL_CONSUMED)->default('0');
            $table->timestampTz(SessionCreditInterface::ATTR_EXPIRES_AT)->nullable();
            $table->string(SessionCreditInterface::ATTR_INVOICE_ID, 30)->nullable();
            $table->foreign(SessionCreditInterface::ATTR_INVOICE_ID)->references('id')->on('invoices')->onDelete('set null');
            $table->string(SessionCreditInterface::ATTR_STATUS, 16)->default('active');
            $table->jsonb(SessionCreditInterface::ATTR_METADATA)->nullable();
            $table->timestampTz(SessionCreditInterface::ATTR_CREATED_AT);
            $table->timestampTz(SessionCreditInterface::ATTR_UPDATED_AT);
            $table->index([SessionCreditInterface::ATTR_TENANT_ID, SessionCreditInterface::ATTR_ATHLETE_ID, SessionCreditInterface::ATTR_STATUS, SessionCreditInterface::ATTR_EXPIRES_AT], 'session_credits_athlete_active_idx');
        });
    }

    /**
     * Reverse the migration.
     */
    public function down(): void
    {
        Schema::dropIfExists(SessionCreditInterface::TABLE);
    }
};
