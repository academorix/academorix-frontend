<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

namespace Stackra\Registry\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * SportsRegistrySettings — Sports catalog.
 *
 * Configure the tenant's sports taxonomy — platform-seeded rows + tenant custom entries (Enterprise-only).
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category Registry
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'sports_registry',
    label: 'Sports catalog',
    description: 'Configure the tenant\'s sports taxonomy — platform-seeded rows + tenant custom entries (Enterprise-only).',
    icon: 'trophy',
    scope: 'tenant',
    permission: 'sports.manage',
    sortOrder: 60,
)]
class SportsRegistrySettings extends Settings
{

    // ── Catalog scope ──────────────────────────────────
    #[SettingGroup(label: 'Catalog scope', description: 'How the tenant reads the sports catalog.', icon: 'book-open', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Include beta sports in the picker', group: 'Catalog scope', sortOrder: 1, helpText: 'When off, sports flagged as beta (early-stage taxonomy) are hidden from the tenant picker. When on, tenants see them + can enroll athletes against beta slugs.')]
    public bool $include_beta_sports = false;

    #[SettingField(controlType: ControlType::Select, label: 'Default discipline picker behaviour', group: 'Catalog scope', sortOrder: 2, options: ['prompt' => 'Prompt the user to pick a discipline explicitly', 'auto_first' => 'Auto-select the first discipline (sort_order 0) when the sport has only one', 'auto_default' => 'Auto-select the discipline flagged as tenant default (if configured)'], helpText: 'Applies across every Wave 3 module\'s discipline picker (Team edit, Enrollment, Event).')]
    public string $default_discipline_selection = 'prompt';


    // ── Custom entries ──────────────────────────────────
    #[SettingGroup(label: 'Custom entries', description: 'Manage tenant custom Sport / Discipline / Position rows.', icon: 'sparkles', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Allow admins to create custom sports', group: 'Custom entries', sortOrder: 1, helpText: 'When off, only tenant owners can create customs (a common shape for tenants that want tight taxonomy control). When on, admins can too.')]
    public bool $allow_admins_to_create_customs = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Notify tenant admins on custom creation', group: 'Custom entries', sortOrder: 2, helpText: 'Sends TenantCustomSportCreatedNotification to admins + owners.')]
    public bool $notify_on_custom_created = true;


    // ── Reconciliation + drift ──────────────────────────────────
    #[SettingGroup(label: 'Reconciliation + drift', description: 'How the system detects + surfaces dangling sport_key references.', icon: 'arrow-path', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Alert on reference drift', group: 'Reconciliation + drift', sortOrder: 1, helpText: 'When on, ReconcileSportReferencesJob dispatches SportPrefixResolutionFailedNotification per detected drift row. Off = drift is only logged.')]
    public bool $alert_on_drift = true;

    #[SettingField(controlType: ControlType::Number, label: 'Drift-alarm threshold (rows)', group: 'Reconciliation + drift', sortOrder: 2, min: 0, max: 100000, helpText: 'When 24-hour drift-count exceeds this threshold, health probe `sports-registry-drift-count-bounded` flips to warn. 0 = never alarm; useful during migrations.')]
    public int $drift_alarm_threshold = 10;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'sports_registry';
    }
}
