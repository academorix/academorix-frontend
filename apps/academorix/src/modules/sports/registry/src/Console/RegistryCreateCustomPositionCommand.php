<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

namespace Academorix\Registry\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan sports:registry:create-custom-position` — Registry:create custom position — sports command.
 *
 * @category Registry
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'sports:registry:create-custom-position',
    description: 'Registry:create custom position — sports command.',
)]
final class RegistryCreateCustomPositionCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Registry:create custom position — sports command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Registry:create custom position — sports command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
