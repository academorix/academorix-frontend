<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

namespace Stackra\Registry\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan sports:registry:resolve` — Registry:resolve — sports command.
 *
 * @category Registry
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'sports:registry:resolve',
    description: 'Registry:resolve — sports command.',
)]
final class RegistryResolveCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Registry:resolve — sports command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Registry:resolve — sports command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
