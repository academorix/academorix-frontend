<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

namespace Academorix\Registry\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Sport is_active flipped from true → false. Historical downstream references continue to resolve; the tenant picker hides the row.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - sports-registry::InvalidateCatalogCache
 *
 * @category Registry
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SportDeactivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sportId  ULID
     * @param  string  $tenantId  ULID | null
     * @param  string  $slug  string
     * @param  string  $changedBy  ULID
     * @param  int  $downstreamReferenceCount  integer (count of Wave 3 rows still referencing this sport_key)
     */
    public function __construct(
        public readonly string $sportId,
        public readonly string $tenantId,
        public readonly string $slug,
        public readonly string $changedBy,
        public readonly int $downstreamReferenceCount,
    ) {
    }
}
