<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

namespace Stackra\Registry\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Platform admin updated the shipped catalog via a JSON edit + explicit reseed. Distinct from SportUpdated — this signals a PLATFORM-level catalog change affecting every tenant. Fires alongside the per-row SportUpdated / DisciplineUpdated / PositionUpdated events. Consumers may downstream-notify tenant admins that the platform catalog changed (e.g. new sports available).
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchSportCatalogUpdatedNotificationToTenantAdmins
 *
 * @category Registry
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SportCatalogUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  int  $catalogVersionBefore  integer
     * @param  int  $catalogVersionAfter  integer
     * @param  string  $addedSportSlugs  string[]
     * @param  string  $updatedSportSlugs  string[]
     * @param  string  $deactivatedSportSlugs  string[]
     * @param  string  $addedDisciplineSlugs  string[]
     * @param  string  $addedPositionSlugs  string[]
     * @param  string  $changedBy  ULID
     */
    public function __construct(
        public readonly int $catalogVersionBefore,
        public readonly int $catalogVersionAfter,
        public readonly string $addedSportSlugs,
        public readonly string $updatedSportSlugs,
        public readonly string $deactivatedSportSlugs,
        public readonly string $addedDisciplineSlugs,
        public readonly string $addedPositionSlugs,
        public readonly string $changedBy,
    ) {
    }
}
