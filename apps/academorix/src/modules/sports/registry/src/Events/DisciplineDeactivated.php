<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

namespace Stackra\Registry\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Discipline is_active flipped from true → false.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - sports-registry::InvalidateCatalogCache
 *
 * @category Registry
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class DisciplineDeactivated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $disciplineId  ULID
     * @param  string  $sportId  ULID
     * @param  string  $tenantId  ULID | null
     * @param  string  $slug  string
     * @param  string  $changedBy  ULID
     * @param  int  $downstreamReferenceCount  integer
     */
    public function __construct(
        public readonly string $disciplineId,
        public readonly string $sportId,
        public readonly string $tenantId,
        public readonly string $slug,
        public readonly string $changedBy,
        public readonly int $downstreamReferenceCount,
    ) {
    }
}
