<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

namespace Stackra\Registry\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Sport row created (either via platform seeder or via tenant custom POST). Fires exactly once per row. Consumers filter on `is_platform_seeded` + `tenant_id` to distinguish the two shapes.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - sports-registry::InvalidateCatalogCache
 * - notifications::MaybeDispatchTenantCustomSportCreatedNotification (only when is_platform_seeded=false)
 *
 * @category Registry
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SportCreated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $sportId  ULID (spo_*)
     * @param  string  $tenantId  ULID (ten_*) | null (null = platform-scoped)
     * @param  string  $name  string
     * @param  string  $slug  string (kebab-case)
     * @param  int  $category  'team_sport' | 'individual_sport' | 'racquet' | 'combat' | 'aquatic' | 'winter' | 'cycling' | 'track_field' | 'fitness' | 'recreational'
     * @param  string  $governingBody  string | null
     * @param  string  $genderTypical  'male_only' | 'female_only' | 'mixed' | 'any'
     * @param  bool  $isPlatformSeeded  boolean
     * @param  bool  $isActive  boolean
     * @param  string  $createdBy  ULID (usr_*) | null (null = platform seeder)
     * @param  string  $source  'platform_seed' | 'admin_created' | 'tenant_custom'
     */
    public function __construct(
        public readonly string $sportId,
        public readonly string $tenantId,
        public readonly string $name,
        public readonly string $slug,
        public readonly int $category,
        public readonly string $governingBody,
        public readonly string $genderTypical,
        public readonly bool $isPlatformSeeded,
        public readonly bool $isActive,
        public readonly string $createdBy,
        public readonly string $source,
    ) {
    }
}
