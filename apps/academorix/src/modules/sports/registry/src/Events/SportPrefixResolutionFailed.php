<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

namespace Academorix\Registry\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * SportKeyResolver::resolve(...) failed to resolve a downstream sport_key / discipline_key / position_key reference. Audit-CRITICAL — this signals a dangling reference in a Wave 3 domain row. Fires per resolution failure (not batched) so the reconciler + ops dashboard get per-row detail.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years — dangling-reference signal for compliance replay)
 * - activity::WriteToActivityLog
 * - notifications::MaybeDispatchSportPrefixResolutionFailedNotification (throttled — one notification per (tenant_id, source_table, source_row_id) per hour)
 *
 * @category Registry
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class SportPrefixResolutionFailed implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID | null
     * @param  string  $sourceTable  string (e.g. 'teams', 'athlete_enrollments')
     * @param  string  $sourceRowId  string (the row that carries the unresolved key)
     * @param  string  $sourceColumn  string (e.g. 'sport_key')
     * @param  string  $unresolvedValue  string (the string value that failed to resolve)
     * @param  string  $attemptedAt  ISO 8601 timestamp
     * @param  string  $resolverContext  'read' | 'write' | 'reconciler'
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $sourceTable,
        public readonly string $sourceRowId,
        public readonly string $sourceColumn,
        public readonly string $unresolvedValue,
        public readonly string $attemptedAt,
        public readonly string $resolverContext,
    ) {
    }
}
