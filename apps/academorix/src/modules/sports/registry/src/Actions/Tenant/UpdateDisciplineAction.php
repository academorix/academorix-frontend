<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registry --force`.

declare(strict_types=1);

namespace Stackra\Registry\Actions\Tenant;

use Stackra\Registry\Contracts\Repositories\DisciplineRepositoryInterface;
use Stackra\Registry\Data\DisciplineData;
use Stackra\Registry\Data\Requests\UpdateDisciplineRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Patch;

/**
 * `PATCH /api/v1/disciplines/{discipline}` — update action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Patch(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Registry
 *
 * @since    0.1.0
 */
#[AsAction(name: 'registry.disciplines.update')]
#[Patch('/api/v1/disciplines/{discipline}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class UpdateDisciplineAction
{
    use AsController;

    public function __construct(
        private readonly DisciplineRepositoryInterface $repository,
    ) {
    }

    /**
     * Update one `discipline` and return the wire DTO.
     *
     * @param  string  $id  Primary key.
     * @param  UpdateDisciplineRequestData  $data  Validated payload.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id, UpdateDisciplineRequestData $data): DisciplineData
    {
        $model = $this->repository->update($id, $data->toArray());

        return DisciplineData::from($model);
    }
}
