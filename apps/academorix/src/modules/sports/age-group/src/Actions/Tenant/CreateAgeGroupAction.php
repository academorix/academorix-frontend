<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports age-group --force`.

declare(strict_types=1);

namespace Stackra\AgeGroup\Actions\Tenant;

use Stackra\AgeGroup\Contracts\Repositories\AgeGroupRepositoryInterface;
use Stackra\AgeGroup\Data\AgeGroupData;
use Stackra\AgeGroup\Data\Requests\CreateAgeGroupRequestData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Post;
use Illuminate\Http\JsonResponse;

/**
 * `POST /api/v1/age-groups` — create action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Post(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category AgeGroup
 *
 * @since    0.1.0
 */
#[AsAction(name: 'age_group.age_groups.create')]
#[Post('/api/v1/age-groups')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class CreateAgeGroupAction
{
    use AsController;

    public function __construct(
        private readonly AgeGroupRepositoryInterface $repository,
    ) {
    }

    /**
     * Create a `age-group` from the validated request payload.
     *
     * @param  CreateAgeGroupRequestData  $data  Validated payload (Spatie Data DTO).
     *
     * @return JsonResponse  201 Created with the newly-persisted DTO.
     */
    public function __invoke(CreateAgeGroupRequestData $data): JsonResponse
    {
        $model = $this->repository->create($data->toArray());

        return response()->json(AgeGroupData::from($model), JsonResponse::HTTP_CREATED);
    }
}
