<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports age-group --force`.

declare(strict_types=1);

namespace Academorix\AgeGroup\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Archived AgeGroup transitioned back to active (deleted_at cleared). Restoration allowed within the 1095-day (3-year) retention window before PurgeArchivedAgeGroupsJob hard-deletes it.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 *
 * @category AgeGroup
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AgeGroupRestored implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $ageGroupId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $restoredBy  ULID
     * @param  string  $wasArchivedAt  ISO 8601 timestamp
     */
    public function __construct(
        public readonly string $ageGroupId,
        public readonly string $tenantId,
        public readonly string $restoredBy,
        public readonly string $wasArchivedAt,
    ) {
    }
}
