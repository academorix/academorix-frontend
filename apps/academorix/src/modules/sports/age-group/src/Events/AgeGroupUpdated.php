<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports age-group --force`.

declare(strict_types=1);

namespace Academorix\AgeGroup\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * Any AgeGroup field mutated except the four dedicated events (bounds, cutoff, sport_key, default). Fires for renames, description edits, color changes, gender_category edits, sort_order.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - teams::InvalidateAgeGroupPickerCache
 *
 * @category AgeGroup
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AgeGroupUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $ageGroupId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $updatedBy  ULID
     * @param  string  $changedFields  string[] (e.g. ['name', 'color', 'description', 'gender_category', 'sort_order'])
     */
    public function __construct(
        public readonly string $ageGroupId,
        public readonly string $tenantId,
        public readonly string $updatedBy,
        public readonly string $changedFields,
    ) {
    }
}
