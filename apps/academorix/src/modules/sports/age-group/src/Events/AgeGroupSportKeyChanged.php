<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports age-group --force`.

declare(strict_types=1);

namespace Academorix\AgeGroup\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * sport_key mutated (including null → non-null and non-null → null). Audit-warn severity — the sport-specific override chain reshapes on this change; downstream resolvers switch their answers.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog (retention: 7 years)
 * - activity::WriteToActivityLog
 *
 * @category AgeGroup
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AgeGroupSportKeyChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $ageGroupId  ULID
     * @param  string  $tenantId  ULID
     * @param  string  $changedBy  ULID
     * @param  string  $sportKey  { before: string|null, after: string|null }
     */
    public function __construct(
        public readonly string $ageGroupId,
        public readonly string $tenantId,
        public readonly string $changedBy,
        public readonly string $sportKey,
    ) {
    }
}
