<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports age-group --force`.

declare(strict_types=1);

namespace Stackra\AgeGroup\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The is_default flag flipped within a (tenant_id, organization_id, sport_key) tuple. Fires with the previous default's ID + the new default's ID. Downstream tier-pricing defaults (Finance Wave 4) key off this flag.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - activity::WriteToActivityLog
 * - notifications::DispatchAgeGroupDefaultChangedNotification (cannot-opt-out — tier-pricing impact)
 *
 * @category AgeGroup
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class AgeGroupDefaultChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $tenantId  ULID
     * @param  string  $organizationId  ULID | null
     * @param  string  $sportKey  string | null
     * @param  string  $fromAgeGroupId  ULID | null (null when this is the first default — AgeGroupCatalogSeeded fires that path)
     * @param  string  $toAgeGroupId  ULID
     * @param  string  $changedBy  ULID
     */
    public function __construct(
        public readonly string $tenantId,
        public readonly string $organizationId,
        public readonly string $sportKey,
        public readonly string $fromAgeGroupId,
        public readonly string $toAgeGroupId,
        public readonly string $changedBy,
    ) {
    }
}
