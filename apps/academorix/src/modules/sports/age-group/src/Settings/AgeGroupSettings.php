<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports age-group --force`.

declare(strict_types=1);

namespace Academorix\AgeGroup\Settings;

use Stackra\Settings\Attributes\AsSetting;
use Stackra\Settings\Attributes\SettingField;
use Stackra\Settings\Attributes\SettingGroup;
use Stackra\Settings\Enums\ControlType;
use Spatie\LaravelSettings\Settings;

/**
 * AgeGroupSettings — Age groups.
 *
 * Tenant-wide policy for age-group provisioning, resolution, and retention.
 *
 * Every field is a public typed property; the property default IS
 * the source of truth (§2 of the `settings` steering rule). Storage
 * goes through `Scope::write('settings', ...)` — never a bespoke
 * `settings_values` table.
 *
 * @category AgeGroup
 *
 * @since    0.1.0
 */
#[AsSetting(
    group: 'age_group',
    label: 'Age groups',
    description: 'Tenant-wide policy for age-group provisioning, resolution, and retention.',
    icon: 'user-group',
    scope: 'tenant',
    permission: 'age_groups.manage',
    sortOrder: 53,
)]
class AgeGroupSettings extends Settings
{

    // ── Provisioning ──────────────────────────────────
    #[SettingGroup(label: 'Provisioning', description: 'How the seeded catalog is created for new tenants.', icon: 'plus-circle', sortOrder: 1)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Seed the default catalog asynchronously', group: 'Provisioning', sortOrder: 1, helpText: 'When on, the 10-entry default catalog is provisioned on a background queue after tenant creation. Off = created inside the tenant-provisioning transaction (slower but the tenant is never briefly catalogless).')]
    public bool $async_seed = true;


    // ── Reconciliation ──────────────────────────────────
    #[SettingGroup(label: 'Reconciliation', description: 'How the system heals default-group drift.', icon: 'arrow-path', sortOrder: 2)]
    #[SettingField(controlType: ControlType::Toggle, label: 'Alert on default-group drift', group: 'Reconciliation', sortOrder: 1, helpText: 'When on, the reconciler pages ops on drift (a scoping tuple with 0 or 2+ defaults). Off = drift is only logged.')]
    public bool $alert_on_drift = true;

    #[SettingField(controlType: ControlType::Toggle, label: 'Auto-heal drift', group: 'Reconciliation', sortOrder: 2, helpText: 'When on, the reconciler auto-heals drift by promoting the oldest row in the tuple. Off by default — surface + acknowledge before healing.')]
    public bool $auto_heal = false;


    // ── Resolution ──────────────────────────────────
    #[SettingGroup(label: 'Resolution', description: 'How the resolver picks an age group when multiple candidates match.', icon: 'adjustments-horizontal', sortOrder: 3)]
    #[SettingField(controlType: ControlType::Select, label: 'Indeterminate resolution strategy', group: 'Resolution', sortOrder: 1, helpText: 'How AgeGroupResolver::resolveFor(...) behaves when multiple candidates match.')]
    public string $indeterminate_strategy = 'refuse';

    #[SettingField(controlType: ControlType::Toggle, label: 'Prefer sport-specific groups over generic', group: 'Resolution', sortOrder: 2, helpText: 'When resolving for an Athlete + sport_key, prefer the sport-specific override over the generic fallback. Off = always use the generic group; sport-specific groups become advisory-only.')]
    public bool $prefer_sport_specific = true;


    // ── Retention ──────────────────────────────────
    #[SettingGroup(label: 'Retention', description: 'How long archived age groups are kept.', icon: 'archive-box', sortOrder: 4)]
    #[SettingField(controlType: ControlType::Number, label: 'Days to hold archived age groups', group: 'Retention', sortOrder: 1, min: 730, max: 2555, helpText: 'Days a soft-deleted age group stays recoverable. Compliance floor: 730 days. Default: 1095 (3 years — matches youth-development cohort windows). Enterprise tenants may extend up to 2555 (7 years matching audit retention).')]
    public int $archived_age_groups_retention_days = 1095;

    /**
     * Return the Spatie Settings group identifier.
     *
     * @return string The settings group key.
     */
    public static function group(): string
    {
        return 'age_group';
    }
}
