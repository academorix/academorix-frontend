<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports awards --force`.

declare(strict_types=1);

namespace Stackra\Awards\Actions\Tenant;

use Stackra\Awards\Contracts\Repositories\AwardRepositoryInterface;
use Stackra\Awards\Data\AwardData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /api/v1/awards/{award}` — show action (tenant audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Awards
 *
 * @since    0.1.0
 */
#[AsAction(name: 'awards.show')]
#[Get('/api/v1/awards/{award}')]
#[Middleware(['api', 'auth:sanctum', 'tenant.resolve'])]
final class ShowAwardAction
{
    use AsController;

    public function __construct(
        private readonly AwardRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `award` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): AwardData
    {
        return AwardData::from($this->repository->findOrFail($id));
    }
}
