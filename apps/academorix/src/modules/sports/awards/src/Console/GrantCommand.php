<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports awards --force`.

declare(strict_types=1);

namespace Stackra\Awards\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan awards:grant` — Grant — awards command.
 *
 * @category Awards
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'awards:grant',
    description: 'Grant — awards command.',
)]
final class GrantCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Grant — awards command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Grant — awards command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
