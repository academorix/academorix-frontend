<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registrations --force`.

declare(strict_types=1);

namespace Stackra\Registrations\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan registrations:auto-offer-waitlist` — Auto offer waitlist — registrations command.
 *
 * @category Registrations
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'registrations:auto-offer-waitlist',
    description: 'Auto offer waitlist — registrations command.',
)]
final class AutoOfferWaitlistCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Auto offer waitlist — registrations command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Auto offer waitlist — registrations command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
