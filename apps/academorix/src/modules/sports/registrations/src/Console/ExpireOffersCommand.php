<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registrations --force`.

declare(strict_types=1);

namespace Academorix\Registrations\Console;

use Stackra\Console\Attributes\AsCommand;
use Stackra\Console\Commands\BaseCommand;

/**
 * `php artisan registrations:expire-offers` — Expire offers — registrations command.
 *
 * @category Registrations
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'registrations:expire-offers',
    description: 'Expire offers — registrations command.',
)]
final class ExpireOffersCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('Expire offers — registrations command', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('Expire offers — registrations command completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
