<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registrations --force`.

declare(strict_types=1);

namespace Academorix\Registrations\Console;

use Academorix\Console\Attributes\AsCommand;
use Academorix\Console\Commands\BaseCommand;

/**
 * `php artisan registrations:list` — List every registrations row.
 *
 * @category Registrations
 *
 * @since    0.1.0
 */
#[AsCommand(
    name: 'registrations:list',
    description: 'List every registrations row.',
)]
final class ListCommand extends BaseCommand
{
    /**
     * Execute the command.
     *
     * TODO(gen): wire the required services + implement the handler body.
     */
    public function handle(): int
    {
        $this->omni->titleBar('List every registrations row', 'sky');

        // TODO(gen): wire the required services + implement the handler body.

        $this->omni->success('List every registrations row completed.');
        $this->showDuration();

        return self::SUCCESS;
    }
}
