<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registrations --force`.

declare(strict_types=1);

namespace Academorix\Registrations\Enums;

use Stackra\Enum\Enum;

/**
 * Closed-set enum for `source` on `registration`.
 *
 * web_form / referral / walk_in / cold_outreach / imported / external_crm.
 *
 * @category Registrations
 *
 * @since    0.1.0
 */
enum RegistrationSource: string
{
    use Enum;

    case WebForm = 'web_form';
    case Referral = 'referral';
    case WalkIn = 'walk_in';
    case ColdOutreach = 'cold_outreach';
    case Imported = 'imported';
    case ExternalCrm = 'external_crm';
}
