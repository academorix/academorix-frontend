<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registrations --force`.

declare(strict_types=1);

namespace Academorix\Registrations\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. Waitlist reordered (removal + reorder). Positions shift for everyone below.
 *
 * ## Consumers
 *
 * - notifications::NotifyPositionChange
 *
 * @category Registrations
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class WaitlistPositionChanged implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $waitlistEntryId  ULID
     * @param  int  $oldPosition  int
     * @param  int  $newPosition  int
     */
    public function __construct(
        public readonly string $waitlistEntryId,
        public readonly int $oldPosition,
        public readonly int $newPosition,
    ) {
    }
}
