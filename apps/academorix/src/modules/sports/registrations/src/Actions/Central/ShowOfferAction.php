<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py sports registrations --force`.

declare(strict_types=1);

namespace Stackra\Registrations\Actions\Central;

use Stackra\Registrations\Contracts\Repositories\OfferRepositoryInterface;
use Stackra\Registrations\Data\OfferData;
use Stackra\Routing\Attributes\AsAction;
use Stackra\Routing\Attributes\Middleware;
use Stackra\Routing\Concerns\AsController;
use Stackra\Routing\Attributes\Get;

/**
 * `GET /offers/{signature}` — show action (central audience).
 *
 * Single-invoke controller wired via `#[AsAction]` + `#[Get(...)]`
 * + `#[Middleware(...)]` from `Stackra\Routing`. Discovered by the routing
 * package's boot-time `RouteRegistrar` — no route file needed.
 *
 * @category Registrations
 *
 * @since    0.1.0
 */
#[AsAction(name: 'registrations.offers.show')]
#[Get('/offers/{signature}')]
#[Middleware(['api'])]
final class ShowOfferAction
{
    use AsController;

    public function __construct(
        private readonly OfferRepositoryInterface $repository,
    ) {
    }

    /**
     * Fetch one `offer` by id.
     *
     * @param  string  $id  Primary key.
     *
     * @throws \Illuminate\Database\Eloquent\ModelNotFoundException  When the row is absent or hidden by scoping.
     */
    public function __invoke(string $id): OfferData
    {
        return OfferData::from($this->repository->findOrFail($id));
    }
}
