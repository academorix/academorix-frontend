<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py platform safeguarding --force`.

declare(strict_types=1);

namespace Academorix\Safeguarding\Events;

use Stackra\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * P1 event. status: investigating → resolved. Outcome + follow-up documented.
 *
 * ## Consumers
 *
 * - audit::WriteToAuditLog
 * - notifications::NotifyInvolvedParties
 *
 * @category Safeguarding
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class IncidentResolved implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $incidentId  ULID
     * @param  string  $resolvedByUserId  ULID
     * @param  string  $outcome  'no_action'|'warning'|'training_required'|'staff_reassigned'|'terminated'|'reported_to_authorities'|'other'
     * @param  string  $resolutionNotes  string
     */
    public function __construct(
        public readonly string $incidentId,
        public readonly string $resolvedByUserId,
        public readonly string $outcome,
        public readonly string $resolutionNotes,
    ) {
    }
}
