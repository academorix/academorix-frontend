<?php

// AUTO-GENERATED by generate-module.py from the module blueprint. Regenerate via `python3 modules/shared/blueprints/foundation/scripts/generate-module.py products geofencing --force`.

declare(strict_types=1);

namespace Academorix\Geofencing\Events;

use Academorix\Events\Attributes\AsEvent;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

/**
 * The HasGeofence trait's boot hook detected an update to any of the 5 fence columns on a Geofenceable model. Fires from the consumer's model save().
 *
 * ## Consumers
 *
 * - geofencing::InvalidateFenceableCache
 * - audit::AuditListener
 *
 * @category Geofencing
 *
 * @since    0.1.0
 */
#[AsEvent]
final readonly class FenceableGeometryUpdated implements ShouldDispatchAfterCommit
{
    /**
     * @param  string  $fenceableType  string
     * @param  string  $fenceableId  string
     * @param  string  $tenantId  ten_*
     * @param  string  $changedFields  string[]
     * @param  string  $updatedBy  uuid | null
     */
    public function __construct(
        public readonly string $fenceableType,
        public readonly string $fenceableId,
        public readonly string $tenantId,
        public readonly string $changedFields,
        public readonly string $updatedBy,
    ) {
    }
}
