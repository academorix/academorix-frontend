<?php

declare(strict_types=1);

use Stackra\Geofencing\Models\GeofenceCheck;

it('is a placeholder — replace with actual GeofenceCheck unit tests', function (): void {
    expect(GeofenceCheck::class)->toBeString();
});
