{
  "id": "academorix://modules/registrations/caches",
  "$version": 1,
  "title": "registrations — cache keys",
  "description": "Cache keys the module owns + TTLs + invalidation triggers + tags.",
  "keys": []
}
