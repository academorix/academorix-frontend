{
  "id": "academorix://modules/medical/caches",
  "$version": 1,
  "title": "medical — cache keys",
  "description": "Cache keys the module owns + TTLs + invalidation triggers + tags.",
  "keys": []
}
