{
  "id": "stackra://modules/digital-passes/caches",
  "$version": 1,
  "title": "digital-passes — cache keys",
  "description": "Cache keys the module owns + TTLs + invalidation triggers + tags.",
  "keys": []
}
