{
  "min_length": 12,
  "rules": [
    { "id": "min-length", "label": "At least 12 characters", "test": "min_length", "min": 12 },
    { "id": "has-letter", "label": "Contains at least one letter", "test": "[a-zA-Z]" },
    { "id": "has-digit", "label": "Contains at least one digit", "test": "\\d" }
  ]
}
