{
  "name": "academorix-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Academorix frontend monorepo — @stackra framework packages, app templates, and shared configs.",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "test:coverage": "turbo run test:coverage",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,cjs,mjs,json,md,css,yaml,yml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,cjs,mjs,json,md,css,yaml,yml}\"",
    "knip": "knip --no-config-hints",
    "size": "size-limit",
    "quality": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm knip",
    "quality:fix": "pnpm format && pnpm lint:fix",
    "check": "pnpm quality && pnpm test",
    "ci": "pnpm quality && pnpm test && pnpm build && pnpm size",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:report": "playwright show-report",
    "changeset": "changeset",
    "changeset:status": "changeset status",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset publish",
    "heroui:login": "pnpm dlx heroui-pro@latest login",
    "heroui:setup": "pnpm dlx heroui-pro@latest install react --yes",
    "heroui:update": "pnpm dlx heroui-pro@latest update --yes",
    "heroui:status": "pnpm dlx heroui-pro@latest status",
    "doppler:login": "doppler login",
    "doppler:setup": "doppler setup --no-interactive",
    "doppler:me": "doppler me",
    "clean": "turbo run clean && rimraf node_modules .turbo",
    "reset": "pnpm clean && pnpm install",
    "prepare": "husky || true"
  },
  "devDependencies": {
    "@academorix/config-eslint": "workspace:*",
    "@academorix/config-prettier": "workspace:*",
    "@academorix/config-tsconfig": "workspace:*",
    "@academorix/config-tsup": "workspace:*",
    "@changesets/cli": "catalog:",
    "@commitlint/cli": "catalog:",
    "@commitlint/config-conventional": "catalog:",
    "@playwright/test": "catalog:",
    "@size-limit/file": "catalog:",
    "eslint": "catalog:",
    "husky": "catalog:",
    "knip": "catalog:",
    "lint-staged": "catalog:",
    "prettier": "catalog:",
    "prettier-plugin-tailwindcss": "catalog:",
    "rimraf": "catalog:",
    "size-limit": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:"
  }
}
