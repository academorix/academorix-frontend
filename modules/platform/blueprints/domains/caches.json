{
  "id": "academorix://modules/domains/caches",
  "$version": 1,
  "title": "domains — cache keys",
  "description": "Cache keys the module owns + TTLs + invalidation triggers + tags.",
  "keys": []
}
