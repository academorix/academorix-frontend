{
  "id": "academorix://modules/settings/retention",
  "$version": 1,
  "title": "settings \u2014 retention windows",
  "description": "SettingValue rows retained while the owning scope is active. Cascade delete on scope erasure.",
  "compliance_regimes_referenced": [
    "gdpr Art. 5 (storage limitation) + Art. 17 (erasure cascades on user + tenant delete)",
    "soc2 CC8.1 (change management via audit dual-write)"
  ],
  "retention": [
    {
      "entity": "SettingValue (scope=system)",
      "hot": {
        "days": "forever",
        "description": "Platform defaults. Never purged unless the field itself is removed."
      },
      "purge": {
        "trigger": "field removed from the compiled registry",
        "action": "MigrateSettingsSchemaJob handles"
      }
    },
    {
      "entity": "SettingValue (scope=tenant)",
      "hot": { "days": "while tenant active" },
      "purge": {
        "trigger": "tenancy::TenantErased event",
        "action": "hard delete via cascade FK"
      }
    },
    {
      "entity": "SettingValue (scope=user)",
      "hot": { "days": "while user active" },
      "purge": { "trigger": "user::UserErased event", "action": "hard delete via cascade FK" }
    },
    {
      "entity": "audit_log + activity_log (settings-scoped)",
      "description": "Retained per activity + audit module rules. Activity: tier-based (30/90/365d). Audit: 365d hot + 2555d cold."
    }
  ]
}
