{
  "id": "academorix://modules/audit/relations",
  "$version": 1,
  "title": "audit \u2014 model relationships",
  "internal": [],
  "outbound": [
    {"from": "Audit", "kind": "belongsTo", "to": "tenancy::Tenant", "foreignKey": "tenant_id", "nullable": true, "description": "Null for system-level audits (deploys, cron). Otherwise required."},
    {"from": "Audit", "kind": "morphTo", "to": "*", "morphName": "auditable", "description": "owen-it contract: the model being audited."},
    {"from": "Audit", "kind": "morphTo", "to": "*", "morphName": "user", "description": "owen-it contract: the actor. Resolved from Sanctum by default."}
  ],
  "inbound": {"description": "Any model using HasAudit gets an `audits()` morphMany from owen-it's Auditable trait."}
}
