{
  "id": "academorix://modules/activity/listeners",
  "$version": 1,
  "title": "activity \u2014 subscribed events",
  "description": "Subscribes to core lifecycle events + workspaces erasure for cascade cleanup.",
  "listeners": [
    {"event": "foundation::ApplicationBooted", "listener": "Academorix\\Activity\\Listeners\\WarmActivityRegistry", "queue": null, "purpose": "Hydrate ActivityRegistry with #[LoggableActivity]-decorated model classes at boot. Uses foundation's DiscoversAttributes seam."},
    {"event": "workspaces::WorkspaceErased", "listener": "Academorix\\Activity\\Listeners\\PurgeActivityForErasedWorkspace", "queue": "notifications", "purpose": "Cascade-delete every activity_log row for the workspace. Foundation's FK cascade handles the row delete; this listener writes the audit trail entry (via audit module)."},
    {"event": "user::UserErased", "listener": "Academorix\\Activity\\Listeners\\AnonymiseActivityForErasedUser", "queue": "notifications", "purpose": "GDPR Art. 17 \u2014 replace causer_id + description with anonymised placeholders. Activity rows retained for workspace audit trail continuity."},
    {"event": "settings::SettingsChangeEvent", "listener": "Academorix\\Activity\\Listeners\\WriteSettingsChangeToActivity", "queue": "notifications", "purpose": "Dual-write leg #1 of the settings audit contract \u2014 writes each changed setting field to activity_log via HasActivityLog on the settings group class."}
  ]
}
