{
  "id": "academorix://modules/notifications-mail/policies",
  "$version": 1,
  "title": "notifications-mail \u2014 authorization policies",
  "description": "One policy on MailSuppression. HasSystemFlag enforces edit/delete refusal on is_system=true rows via the trait's global rule.",
  "policies": [
    {
      "model": "MailSuppression",
      "class": "Academorix\\Notifications\\Mail\\Policies\\MailSuppressionPolicy",
      "abilities": [
        {"action": "viewAny", "permission": "notifications.mail.suppressions.viewAny", "scope": "workspace admin OR platform_admin"},
        {"action": "view", "permission": "notifications.mail.suppressions.view", "scope": "suppression.workspace_id === auth.workspace_id OR platform_admin"},
        {"action": "create", "permission": "notifications.mail.suppressions.create", "scope": "workspace admin (creates workspace-scoped) OR super_admin (creates platform-wide with --platform-wide)"},
        {"action": "delete", "permission": "notifications.mail.suppressions.delete", "scope": "REFUSED when is_system=true (unless super_admin) OR reason IN hard_bounce/complaint (unless super_admin + explicit --force)"}
      ]
    }
  ]
}
