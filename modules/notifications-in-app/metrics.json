{
  "id": "academorix://modules/notifications-in-app/metrics",
  "$version": 1,
  "title": "notifications-in-app \u2014 OpenTelemetry metrics",
  "description": "Channel-specific metrics. Aggregate delivery outcomes are captured in core's metrics.json labelled by channel; these are the drill-down metrics only in-app cares about.",
  "instrumentation": "open-telemetry/sdk",
  "namespace": "academorix.notifications.in_app",
  "metrics": [
    {"name": "academorix.notifications.in_app.broadcasts.total", "kind": "counter", "unit": "broadcasts", "labels": ["tenant_id", "event"], "description": "event: 'created' | 'seen' | 'archived'."},
    {"name": "academorix.notifications.in_app.broadcast.duration_ms", "kind": "histogram", "unit": "ms", "buckets": [1, 5, 25, 100, 500, 2500], "labels": ["tenant_id"], "description": "Reverb dispatch duration. Should stay < 25ms P99."},
    {"name": "academorix.notifications.in_app.broadcast.failures_total", "kind": "counter", "unit": "failures", "labels": ["tenant_id", "reason"], "description": "reason: 'reverb_unreachable' | 'auth_failed' | 'payload_too_large' | 'unknown'."},
    {"name": "academorix.notifications.in_app.reverb.active_connections", "kind": "gauge", "unit": "connections", "description": "Live socket count on the tenant Reverb instance. Sampled every 30s."},
    {"name": "academorix.notifications.in_app.reverb.subscribed_channels", "kind": "gauge", "unit": "channels", "description": "Distinct subscribed channels. Should stay < active_connections * 2 (each user typically subscribes to their own notifications channel + a tenant-scope channel)."}
  ]
}
