{
  "id": "academorix://modules/workspaces/notifications",
  "$version": 1,
  "title": "workspaces — Laravel notifications",
  "description": "Laravel notifications this module owns. Each carries the channels it delivers to, the template refs (Markdown Mailable or Blade view), the notifiable model(s) it targets, and the localisation namespace. Consent tier is per-notification: transactional (essential) notifications ignore marketing opt-out; marketing notifications respect it.",
  "namespace": "Academorix\\Workspaces\\Notifications",
  "notifications": [
    {
      "class": "Academorix\\Workspaces\\Notifications\\WelcomeNotification",
      "channels": ["mail", "database"],
      "consentTier": "transactional",
      "targets": ["User"],
      "template": {
        "mail": "workspaces::mail.welcome",
        "markdown": true
      },
      "queue": "notifications",
      "description": "Sent to the newly-created owner User after WorkspaceProvisioned. Includes verification link + first-steps checklist keyed by BusinessType.default_config.onboarding_steps."
    },
    {
      "class": "Academorix\\Workspaces\\Notifications\\WorkspaceSuspendedNotification",
      "channels": ["mail", "database"],
      "consentTier": "transactional",
      "targets": ["User"],
      "template": {"mail": "workspaces::mail.workspace-suspended"},
      "queue": "notifications",
      "description": "Sent to every User with the `owner` or `admin` role in a suspended Workspace. Includes reason + support contact + grace window if applicable."
    },
    {
      "class": "Academorix\\Workspaces\\Notifications\\WorkspaceResumedNotification",
      "channels": ["mail", "database"],
      "consentTier": "transactional",
      "targets": ["User"],
      "template": {"mail": "workspaces::mail.workspace-resumed"},
      "queue": "notifications",
      "description": "Sent to owners + admins when a Workspace returns to active status."
    },
    {
      "class": "Academorix\\Workspaces\\Notifications\\WorkspaceArchivedNotification",
      "channels": ["mail", "database"],
      "consentTier": "transactional",
      "targets": ["User"],
      "template": {"mail": "workspaces::mail.workspace-archived"},
      "queue": "notifications",
      "description": "Sent to owners on archive. Includes data export link (valid 30d — matches retention window) and undo-instructions."
    },
    {
      "class": "Academorix\\Workspaces\\Notifications\\DomainVerifiedNotification",
      "channels": ["mail", "database", "broadcast"],
      "consentTier": "transactional",
      "targets": ["User"],
      "template": {"mail": "workspaces::mail.domain-verified"},
      "queue": "notifications",
      "description": "Sent to Users with `workspaces.manage_domains` permission when a Domain's verification succeeds."
    },
    {
      "class": "Academorix\\Workspaces\\Notifications\\DomainVerificationFailedNotification",
      "channels": ["mail", "database"],
      "consentTier": "transactional",
      "targets": ["User"],
      "template": {"mail": "workspaces::mail.domain-verification-failed"},
      "queue": "notifications",
      "description": "Sent after `verification_attempts >= 100`. Includes the last error + a manual retry link."
    },
    {
      "class": "Academorix\\Workspaces\\Notifications\\BrandingUpdatedNotification",
      "channels": ["database"],
      "consentTier": "functional",
      "targets": ["User"],
      "template": null,
      "queue": "notifications",
      "description": "In-app-only. Sent to owners + admins when the primary Branding profile changes."
    },
    {
      "class": "Academorix\\Workspaces\\Notifications\\IntegrationSyncFailedNotification",
      "channels": ["mail", "database", "slack"],
      "consentTier": "transactional",
      "targets": ["User"],
      "template": {"mail": "workspaces::mail.integration-sync-failed"},
      "queue": "notifications",
      "description": "Sent to Users with `workspaces.manage_integrations` after an integration sync fails. Slack channel per workspace is optional and configured via WorkspaceIntegration `webhook`."
    }
  ],
  "consentTiers": {
    "transactional": "Always sent — required to operate the service. Ignores marketing opt-out.",
    "functional": "In-app + email only; email respects marketing opt-out.",
    "marketing": "Fully respects opt-out per user preference + workspace-wide setting."
  }
}
