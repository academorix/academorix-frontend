{
  "id": "academorix://modules/geofencing/caches",
  "$version": 1,
  "title": "geofencing \u2014 cache keys + tags",
  "driver": "redis",
  "ownedTags": ["geofence", "geofence.branch", "geofence.checks"],
  "keys": [
    {"pattern": "geofence.branch:{branch_id}", "description": "Cached BranchGeofence value object. Read on every evaluate() call. Invalidated when the branch's geofence columns change (via facilities module event when it lands).", "ttlSeconds": 3600, "tags": ["geofence", "geofence.branch"]},
    {"pattern": "geofence.checks:{workspace_id}:recent", "description": "Sorted set of the last 1000 evaluation IDs per workspace. Read by the RecentGeofenceActivityCheck health probe.", "ttlSeconds": 86400, "tags": ["geofence", "geofence.checks"]},
    {"pattern": "geofence.override_rate:{workspace_id}", "description": "1h rolling counter: overrides_applied / evaluations_total. Populated by the metrics recorder. Read by GeofenceOverrideRateCheck.", "ttlSeconds": 3600, "tags": ["geofence"]}
  ]
}
