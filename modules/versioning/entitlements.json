{
  "id": "academorix://modules/versioning/entitlements",
  "$version": 1,
  "title": "versioning \u2014 entitlements",
  "description": "Empty. Versioning is a platform capability; per-tenant metered entitlements don't apply. The relevant entitlements (custom-templates.count, webhooks.subscriptions.count) live in the modules that own the metered resource. Reference kinds block retained for consistency with the module contract.",
  "reference": {
    "publishedBy": "entitlements",
    "resolvedBy": "GET /api/auth/me \u2192 data.quota_summary",
    "seededFrom": "BusinessType.default_config.entitlements"
  },
  "kinds": [
    {"kind": "slot", "description": "Countable slots. One-per-row consumption."},
    {"kind": "pool", "description": "Consumable pool. Decremented on use."},
    {"kind": "boolean", "description": "Feature toggle expressed as entitlement."},
    {"kind": "unlimited", "description": "'No cap' for a normally-slotted resource."}
  ],
  "entitlements": []
}
