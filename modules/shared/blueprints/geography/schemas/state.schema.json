{
  "id": "academorix://modules/geography/schemas/state",
  "draft": "https://json-schema.org/draft/2020-12/schema",
  "$version": 1,
  "title": "State",
  "description": "ISO-3166-2 state / province / region row. Vendor-owned schema. Nested under Country in the REST surface.",
  "type": "object",
  "x-module": "geography",
  "x-vendor": {
    "package": "nnjeim/world",
    "vendor_model": "Nnjeim\\World\\Models\\State",
    "we_own_migration": false,
    "we_own_seeder": false
  },
  "x-eloquent": {
    "baseClass": "Nnjeim\\World\\Models\\State",
    "table": "states",
    "primaryKey": "id",
    "keyType": "int",
    "incrementing": true,
    "timestamps": false,
    "model": "Academorix\\Geography\\Models\\State",
    "interface": "Academorix\\Geography\\Contracts\\Data\\StateInterface",
    "implements": ["Academorix\\Geography\\Contracts\\Data\\StateInterface"],
    "traits": ["HasFactory", "Filterable"],
    "observers": ["StateObserver"]
  },
  "x-database": {
    "engine": "any (vendor-supported)",
    "vendor_owned": true,
    "columns_summary": {
      "id": { "type": "integer", "primary": true },
      "country_id": { "type": "integer", "description": "FK to countries.id." },
      "name": { "type": "string", "description": "English state name." },
      "country_code": { "type": "string", "length": 2 },
      "state_code": {
        "type": "string",
        "nullable": true,
        "description": "ISO-3166-2 code (e.g. US-CA, FR-75)."
      },
      "type": {
        "type": "string",
        "nullable": true,
        "description": "state / province / region / district / territory."
      },
      "latitude": { "type": "string", "nullable": true },
      "longitude": { "type": "string", "nullable": true }
    }
  },
  "required": ["id", "country_id", "name", "country_code"],
  "properties": {
    "id": { "type": "integer" },
    "country_id": { "type": "integer" },
    "name": { "type": "string" },
    "country_code": { "type": "string", "pattern": "^[A-Z]{2}$" },
    "state_code": { "type": ["string", "null"] },
    "type": { "type": ["string", "null"] },
    "latitude": { "type": ["string", "null"] },
    "longitude": { "type": ["string", "null"] }
  },
  "additionalProperties": true,
  "x-wire": {
    "envelope": "spatie-data",
    "wrap": "data",
    "mapper": "SnakeCaseMapper",
    "resource_class": "Academorix\\Geography\\Data\\Resources\\StateResourceData"
  }
}
