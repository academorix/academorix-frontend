{
  "id": "academorix://modules/telemetry/jobs",
  "$version": 1,
  "title": "telemetry \u2014 background jobs",
  "jobs": [
    {
      "class": "Academorix\\Telemetry\\Otel\\Jobs\\FlushBufferedSpansJob",
      "queue": "telemetry",
      "signal": "traces",
      "purpose": "Escape hatch when the BatchSpanProcessor's in-process worker misbehaves. Force-flushes the current SDK provider.",
      "shouldQueue": true,
      "shouldBeUnique": true,
      "uniqueFor": 30,
      "tries": 2,
      "notes": "Rarely triggered manually. The primary export path is BatchSpanProcessor which flushes on interval + buffer size."
    }
  ]
}
