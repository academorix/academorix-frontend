{
  "id": "academorix://modules/offline-sync/caches",
  "$version": 1,
  "title": "offline-sync — cache keys",
  "description": "Cache keys the module owns + TTLs + invalidation triggers + tags.",
  "keys": []
}
