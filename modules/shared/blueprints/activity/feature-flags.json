{
  "id": "academorix://modules/activity/feature-flags",
  "$version": 1,
  "title": "activity \u2014 kill switches",
  "engine": "laravel/pennant",
  "flags": [
    {
      "key": "activity.logging",
      "kind": "kill-switch",
      "default": "on",
      "description": "Master toggle for spatie's LogsActivity. Off = every HasActivityLog model becomes a no-op. Emergency ops during ingest storm."
    },
    {
      "key": "activity.prune",
      "kind": "kill-switch",
      "default": "on",
      "description": "PruneActivityLogJob execution."
    },
    {
      "key": "activity.settings_dual_write",
      "kind": "kill-switch",
      "default": "on",
      "description": "WriteSettingsChangeToActivity listener. Off decouples settings-change UI feed from settings writes \u2014 rarely useful."
    }
  ]
}
